
## 1.0.0 - 2026-08-30 - gravitone
- The forge-shaped runner (scene x style-recipe crossing) cannot A/B a PROMPT property — composition, lighting, lens, performance, reference handling. The consuming repo needed a second runner taking a pair spec (two full prompts + optional reference/window per arm, one seed per duo) and reading every image back with the same annotator. Overlay now names both runners; a cycle picks by what is under test: recipe -> forge, prompt -> pairs.
- Baselines for prompt cycles must come from the consuming repo's LIVE compiler (a tsx driver over its shotPrompt/compilePrompt), never a retyped copy — retyping is the drift the A/B would then measure.
- An 8-cycle variety window (4 duos each) produced two unanimous winners, four probables and one clean null in ~5 GPU-hours; the null (invariants-only reference under a 0.35 denoise window) was as informative as the wins — the windowing already sheds the reference's pose, so the technique's failure mode may be mechanism-specific. Small cycles, many dimensions beats one deep cycle when the operator asks for breadth.
- Chained detached passes (each cycle: pass 1 + fill pass) self-heal the 1-3 per-pass ComfyUI races this class of box produces; the breaker never tripped across 80 renders.

### Redesign proposal
- Phase 4/5 assume the forge's manifest shapes. The pair-spec runner + a `judge prepare/park` helper (blind maps, Gemini with model fallback, unblinding, pick-rate arithmetic) were rebuilt in the consuming repo this run; if a second repo needs them, they belong in the skill's references as a named contract (gen-spec.json / readbacks.json / judging-worksheet.json) rather than re-derived.
- Control-arm cycles (challenger = a deliberate violation of a standing law) are the cheapest evidence a window can buy, but the gate reads backwards: the human must REJECT the challenger to confirm the law. Say so in the cycle's claim, or the verdict row will lie to the next reflection.
- A CORPUS-STUDY cycle (sample an A-tier work with the local recognizer, cross-analyze the annotations against the repo's own prompt-surface assumptions, convert the sharpest gap into a pair A/B) is fully automatable including the source pick: rank by corpus gap, acquire OFFICIAL promotional material only via yt-dlp with a candidate fallback ladder, and let the analysis numbers write the claim. Two studies on gravitone each produced a parked, judged A/B in ~1 GPU-hour; the second study's finding refined a rule the human had gated only a day earlier (one light source -> one dominant + one environmental layer), which is the loop feeding itself.
- When the studied property maps to a field the annotator already reads (composition, light_sources), the pre-filter stops being blind: state the expected readback delta in the claim so the cycle carries its own measurement.
- A corpus study that returns ONE clause-sized A/B has under-delivered (operator finding after two approved-but-thin studies). The study yield contract is three lanes, all owed per study: (1) styles - run the consuming repo's style-extraction engine over the frame set; (2) a BEAT MAP - technique distributions per position in the cut, the "which technique at which narrative moment" lesson the frames actually carry; (3) A/B claims as verification only. The report states what each lane produced or why it is empty.
- A dimension can ban a judge by operator instruction (video: no Gemini, cost). The park path must tolerate a missing judge rather than fabricate ties for it - absent is absent, and pick_rate alone carries the pre-human signal.
- Video cycles, first window (Wan 2.2 ti2v 5B, 8 clips, 0 failures): judge briefs over poster triptychs work, but must quote each pair's AUTHORED motion intent (a global one-readable-move standard punishes deliberate near-stillness), and serial continuity prompts must carry visual clauses only - editorial meta-language is rendered as content, the model painting the shot it was told it cut from.
- A one-seed serial video A/B flipped verdict on the next seed (meta-language continuity lost, then won, n=2 each). Video world-drift variance dominates single-seed pairs: a serial-continuity claim needs >=4 seeds per arm before it earns a cycle, and a contradiction across windows is a demand note, not a re-run.
- Train the engine the product ships. gravitone's first three image windows A/B'd prompt recipes against Flux 2 - the forge's lab instrument - while the product generates through its imaging router (Nano Banana); the transfer was never proven, and the operator caught it as rediscovering the wheel. Phase 3 must read the consuming repo's LIVE routing plan and generate through its chokepoint; a seedless product engine changes the protocol (k repeats per arm, distributions not rolls), and that is a protocol cost worth paying for product truth.
- Check the registry's provider-routing knowledge BEFORE choosing a runner: gravitone's Wan-over-H3 choice happened to be right, but for the wrong reason (H3's license excludes EU local deployment including outputs - a fact the registry already held while the session weighed models by VRAM).
- When a replication's failures are IDENTICAL in both arms, the variable under test is not the lever - close the claim as unmeasurable-by-that-variable and name the successor instrument, rather than running more seeds. (gravitone serial continuity: 3 windows, 10 pairs, phrasing a coin flip while both arms missed the shot's content; the successor is reference conditioning.)
- A window can run one GPU cycle and one vendor-API cycle in parallel - the budget is per-resource, not per-wall-clock.

## 1.0.0 - 2026-09-01 - gravitone-gcloud (Fable vs Opus bake-off, one local image cycle each)
- Two loops on one card is an interlock problem, not a convention problem. Both cycles honoured "one dojo loop per machine" through an agreed lock directory and still failed: a third job (another session's `serial-3seeds`) held the engine, one pass spent 10h48 on two renders, and the guard's refusal to recycle another job's engine was the first thing that told the truth. Phase 4 now reads the engine's run-id before launching and treats a foreign owner as `runner: busy`.
- "Demand outranks scan points" must name whose demand. `librarian-scan`'s deviation counts are fleet-aggregated; `visual-style-locking` showed six consumer deviations and 26 points while gravitone has no conformance pair for it at all. The Fable cycle ranked on that number; the Opus cycle read `.ai/conform-detail.json` and trained a subject this repo actually deviates on. Phase 1 now says so.
- A subject's live demand can be unreachable by the loop's only instrument: two of `frame-direction`'s three deviations here are a schema gap and a cross-frame lifecycle, which a seed-matched still cannot measure. Name the blind spot and pass rather than rediscover it each cycle.
- Whether a reference-governance claim is A/B-able depends on the runtime's reference channel, not on what the prompt says: Flux 2 via ComfyUI admits a reference only as an img2img latent, so `reference-role-map`'s ordering rule has nothing to order on the $0 lane. Check the channel in Phase 3, before the plan is written.
- `build-index.mjs` writes; a cycle on a shared or read-only registry checkout runs `--check`. `Out-File` stamps a BOM that `json.load` rejects; write manifests from Python or read `utf-8-sig`. The runner truncates the ComfyUI exception to 80 characters and starts the engine hidden; capture its stdout to `pipeline/foundry/logs/` or the cause is unrecoverable after a recycle.
- The chokepoint judge's blind is weak whenever the claim names the visible difference (an emblem versus a document); say so in the judge protocol rather than implying a strong blind.
- A lane bake-off truncated by the operator at ONE complete unit can still be decisive when the wins are categorical (figure present vs absent, interior vs exterior) rather than marginal - park it with the truncation named, and let the full-plan spec ride along for a resume.
- Big-model unit costs on consumer boxes are RAM stories, not GPU stories: H3 ref2va ran at 12x its reference rate and GLM-5.3 vision at 30x, both from host-RAM starvation paging experts. Record the hardware lever (RAM to spec) beside the verdict, or the lane reads as worse than it is.
- Before buying parameters, buy a better question: a 321B eye's apparent class-jump over a 27B local eye vanished when the small eye was re-prompted with the vocabulary the big one demonstrated (roles, shapes, layers - free prose, not enums). When comparing eyes, hold the PROMPT constant or the comparison measures elicitation, not capability; and a big model's cheapest contribution may be showing what to ask the small one.

## 1.0.0 - 2026-09-01 - gravitone (model bake-off)

- Context: one local-stack image cycle.
- No pin. Both cycles failed on GPU contention: Fable rendered five of eight pairs (one complete duo) before its breaker tripped, Opus two of twelve in eleven hours because a third, foreign dojo job held the card. Neither produced a judgeable set; both kept the registry read-only, released the lock and parked honestly. The operator kept Opus's method: the demand signal is the consuming repo's own conformance record (Fable ranked on the fleet-aggregate scan and picked a subject this repo had never deviated on), an engine-owner check before launch is the interlock, a zero-duo cycle never parks for a human, and Phase 1's registry commands are marked read-only versus writing. Those edits are in Phases 1, 4 and 6 and the safety rails above. Fable's cycle contributed the partial-readback reading that a contradicting tail clause is obeyed as well as an in-block override at ~1.1k characters on Flux 2, recorded in gravitone's overlay as a lead for `/deepen`.

## 1.2.1 - 2026-09-04 - ai-registry

- The dated `## Model choice (bake-off 2026-09-01, ...)` section moved out of the SKILL.md body into the block above. The lane spec (`docs/skills-lane.md`, "The body is generic") says a body may carry no project name, and this one named gravitone; a dated finding about a run is what `LESSONS.md` owns. Content preserved verbatim; nothing else in the body changed, so a patch bump.

## 1.2.2 - 2026-09-05 - gravitone

- Context: five consecutive local-stack image cycles in one operator-authorised window (40 renders, one vanish-race fill), one allowed subject each; all parked, none gated yet.
- Where a property under test has a NUMBER, give the chokepoint judge the number, blind: an edge-density meter on the reserved caption zone and a calibrated face-distance ruler each separated arms the enum readback called identical (it labelled every arm of the key-flip cycle "low-key"). Map the measure into the worksheet's A/B before the pick so the judge stays blind, and keep the number a pre-filter beside the human, never a verdict. This is the technique-under-test's own tell made mechanical, and it is cheap - both meters ran in seconds on PNGs already on disk.
- A reference-conditioned arm on a latent-reference runner (Flux 2 `ReferenceLatent`) is a CONTENT arm when it joins early: an "approved style sheet" at 0-35% of the denoise returned its own subject in three of four challengers. Plan reference cycles on such runners with the technique's timing dial (`late`), and treat "reference as style amplifier" as a labelled-attachment-stack claim only.
- Two arms of a seed-matched duo can render at very different speeds (44s vs 120-180s) when the second shares the first's text encode; a fast arm is not a skipped arm. Verify with a pixel delta and the workflow sidecar before calling it a bug or waiting on it as contention.
- Seed-matched control cuts both ways: when the challenger's clause changes nothing the render does (cycle 1, a leading function sentence), the arms come back near-identical and the judge picks on seed-level detail. A pick rate near 0.25 with near-identical arms is NULL, not a loss - say so in the park note rather than letting the number read as a rejection.

## 1.2.2 - 2026-09-06 - gravitone

- Context: one local-stack image cycle, `state-coupling`, stopped on the breaker at 7/24 renders. Instrument work landed; no cycle parked.
- **A recorded lesson is not a landed change, and the two are indistinguishable from the log.** This repo's overlay had carried "capture ComfyUI's stdout or the cause is unrecoverable after a recycle" since 2026-09-01. It was never applied — the engine was still being started `-WindowStyle Hidden` with nothing redirected — and the runner's own error text told this session to "read its stderr" about a stream nobody had ever written. Three anonymous unit failures were spent before anyone looked at the code. When a past session's note claims a fix, VERIFY IT IN THE CODE before relying on it; a lessons log records intent, and only a diff records a fix.
- **The human gate is a resource with a capacity, and this loop has no backpressure against it.** 16 cycles parked, nothing gated in five days, and an approved reflection branch unmerged for seven — while its ledger row read `reflected: <sha>`, which looks identical whether the commit is live or stranded on a review branch. The loop is right never to wait, but "never wait" is not "never notice": the value of parked cycle N+1 falls as N grows because they all queue on the same scarce act. Phase 1 now counts the backlog, checks that `reflected` shas are ancestors of main, and prefers cycles that need no human verdict at all.
- **A ledger that only the gate writes is not a record of what the loop did.** 9 of 13 committed cycles here have no ledger row; two of those were committed with `human: null`, so their verdicts are lost outright — including one whose claim (a reference sets the state channel whether or not it was supplied for state) a later cycle then had to re-derive from scratch. The evidence a cycle produces and the row a gate writes are different artifacts, and only the second is durable today.
- **A probe and an A/B are the same experiment when the baseline arm is what the pipeline already does.** A banked lead asking to MEASURE something (how much state rides along with a reference face) looks like a mismatch for a pairwise instrument. It is not: make the baseline the live recipe and the probe falls out of that arm for free, while the challenger turns it into a pair. One render budget discharges a fleet-level owed measurement and a repo-level A/B, and the probe's branch predicts whether the challenger could win at all.
- **Check the instrument can see the property before planning the cycle, not after.** The technique under test said its check "is not an embedding — it is a vision grader asked one question the frame-annotation vocabularies routinely lack". That was literally true here: the enum readback covers camera/lighting/composition and the deep-read prompt covers medium/palette/black/light/edges, and neither has a field for the performer. Reading both schemas took minutes and turned an unmeasurable cycle into a measurable one; discovering it after the renders would have wasted the window.
- **Slow units have two causes with opposite remedies.** The existing rule (>3x per-unit time = contention) is only half. With no foreign job on the engine and the guard recycling between every single render, the bottleneck is host memory, and no amount of re-checking the owner helps — it re-bases the per-unit budget instead. The tell that it is not free RAM: failures arriving ~60s after a fresh recycle that had just freed 38-40 GB.
- **Don't park a partial pre-registered roster.** Three of twelve duos completed, which is not zero — but what died was exactly the extremes the protocol's branches turn on, leaving only the easy half of the range. Count what the survivors can CONCLUDE, not how many there are; a partial answer entering the cross-machine ledger is worse than a `failed` row, because the next session reads the ledger as settled.

## 1.3.0 - 2026-09-09 - ai-registry

- Architecture review: the shared reflection clause assumed a writable registry link in every installation. Replaced that assumption with installation-aware scope and explicit adoption. This records an instruction audit, not a field effectiveness result.
