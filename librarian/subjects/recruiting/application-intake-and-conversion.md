---
subject: application-intake-and-conversion
domain: recruiting
last_touched: 2026-09-26
dry_streak: 0
---

# application-intake-and-conversion

First touch by `/deepen`, a single-subject run (dp-aic-0926) dispatched by the Curator
lane on "never swept by the librarian". Registry HEAD at dispatch was 55c6bce2. The
primary checkout's main was behind origin, so the run worked from origin/main in a
detached worktree. The consumer was read at kp 7ac877d0c. kp was claimed on the board
before the code apply.

## 2026-09-26 - a match identifies and only proof writes, three node applications, six claims corrected

**Depth rung:**
- L2 primary for the corrections: design-standard text, a controlled form-validation
  study read from its PDF, faking meta-analyses, regulation text.
- L3 empirical for the merge flip: a behavioural test over the consumer's real POST
  handler, both arms.

**Lanes:** counter-evidence (web, unconstrained), training-data-only (blind), and the
consumer tree (re-verification plus seams for the three techniques that had no
application).

**Landed** in 2a527bf0:
- **Flipped (tree + blind lane + code A/B):** merge-dont-drop. A match on a typed
  name or address identifies the record and authorises nothing; only the issued token
  writes. The golden path gains "what its match proves" as a per-door difference, and
  two new failure modes: the impostor merge and the trapped human.
- **Flipped (two lanes):**
  - The golden path's "not length against quality" becomes "length is a real cost,
    and honesty is a second axis".
  - always-live-submit: "report one blocker" becomes a summary of every blocker,
    leading with the first. First-blocker-only is kept for one-screen forms.
- **Conditioned (two lanes each):**
  - speed-to-lead: the minute-scale decay is sales-lead evidence; recruiting evidence
    runs in days.
  - The disabled-button accessibility claim depends on navigation mode.
  - The decoy field: autofill ignores the don't-fill attribute, so a trip is a
    signal, never a decline and never the eligibility message.
  - Tonal neutrality: the verification lever.
  - The knockout law: stated as registry policy, with its jurisdictional reach.
  - The mis-tap "known rate" is gone.
  - "Prefer not to say" is recorded as its own response.
- **Widened (node@24, read from the real tree):**
  - `node--eligibility-gate-versus-bot-defence`
  - `node--speed-to-lead-acknowledgement`
  - `node--recoverable-decline-and-no-dead-ends`

  Every technique now has an application.
- **Re-verified:** all three existing applications moved to 2026-09-26 with
  `verified_against`. The node merge application was rewritten, because the tree moved
  its merge into a shared filing core behind a proof table. The react tonal
  application's "`bau` is the neutral fallback" had been reversed by the tree on
  2026-08-21, one day after it was written. The react submit application's
  "conversational door has only restart" deviation is withdrawn: that door now
  re-asks a named step.

**Applied in the consumer:** kp 327e40e00, `fix(candidate-apply-api)`. The quick
form merged a name-matched contactless entry under integration-level proof. It set
the typed address as the contact of record and mailed it the entry's status link. It
now moves nothing. kp local main is not pushed: it carries other sessions' unpushed
commits.

**Verified and left untouched:**
- Provided-only semantics for third-party payloads; strict semantics on our own
  surface.
- The decoy construction details the axe rule settles: a focusable element inside a
  hidden region passes when it is taken out of the tab order.
- Structural unreachability of self-declared fields from selection.
- The attempt identifier as a measurement token that grants nothing.

**Declined:**
- The U.S. disability self-identification form rescinded in September 2026: law-firm
  summaries only, one lane, not verified against the Federal Register. Upper layers
  name no form. Banked below.
- UK draft guidance reportedly placing human-set rules outside automated
  decision-making: a secondary summary, unverified.
- The AI Act's delayed dates and a U.S. state law's replacement: law-firm summaries
  only.
- Record-linkage review bands (Fellegi-Sunter): blind lane only. Drafted into the
  merge technique, then removed before commit.
- "Bots skip natively hidden inputs": both lanes call it unmeasured folklore. The
  technique now says so rather than deleting the construction.

**Consumer defects found, recorded as deviations and not fixed:**
- A honeypot trip is answered with the eligibility decline. The field is named and
  labelled "Company URL". The value survives a retry, and a trip is recorded nowhere.
- The conversational ack is triggered after the profile build.
- The ack copy promises review "shortly" and thanks the candidate for "submitting
  your work".
- A knockout decline has no hold state, no actor on its audit event, and never names
  the failed gate to the candidate.
- The quick form's knockout pin omits the off-screen style.
- The route header still describes the pre-proof merge.

**Source classes:**
- A design-standard body's own component pages decided the error-summary and
  disabled-button rows.
- A peer-reviewed controlled study decided one-by-one versus all-at-once. Its web
  summary inverted the finding, and the lane caught it against the PDF text.
- Regulation text decided the knockout scope.
- Peer-reviewed meta-analyses decided the faking lever.
- Vendor apply-flow data only corroborated. A peer-reviewed field experiment decided
  the length flip.
- Browser issue trackers decided the autofill condition, with the blind lane
  agreeing.
- Law-firm summaries were declined every time they stood alone.
- The blind lane matched the web lane on every question both answered, and did not
  know the September 2026 rescission (past its horizon). Class priors are not
  drifting.

## Impact

`build-registry-map --project kp` (dc4c7cc24 in kp): 6 kp contexts carry this subject:

- candidate-apply-flow
- candidate-apply-api
- conversational-apply-flow
- quick-apply-flow
- apply-surface-tests
- jd-intake-studio (lexical-only)

None is judged, so no stale verdict rests on it. The map was built from origin
2a527bf0. It is committed on kp's local main, not pushed.

## Clocks and return conditions

- The knockout jurisdiction paragraph leans on the UK's 2025 reform and EU text.
  Refresh by 2027-03 or when the AI Act's employment obligations take effect.
- Lead (one lane, secondary): U.S. pre-offer disability self-ID rescinded (reported
  effective 2026-09-21). Return with the Federal Register text, then condition the
  self-declaration section by jurisdiction.
- Lead: webhook doors still backfill a name-matched contactless entry under
  integration proof. That is defensible for an authenticated partner, but untested
  for an email-inbound channel anyone can write to. Return when that channel is next
  read.
- Consumer defects, in order: the honeypot decline copy and naming, the
  conversational ack trigger, the ack copy, the decline's actor and hold state.
