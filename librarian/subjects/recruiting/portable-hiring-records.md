---
subject: portable-hiring-records
domain: recruiting
last_touched: 2026-08-23
touched_by: external-reconcile
dry_streak: 0
---

# portable-hiring-records

First touch: [[2026-08-23-8]], external reconcile against
`W3C/vc-data-model@2.0` (Recommendation of 2025-05-15, base context digest
verified against the spec's own published SHA-256 and a live re-fetch).
Gained `spec--versioned-record-envelope` - the lane's and the corpus's first
class-B (specification) application. Hint confirmed; its closed-world premise
refuted (the spec names the open world and closes it normatively). Executed:
the spec's own prescribed digest command, plus a worked clause procedure over
seven fixtures against the enumerated MUSTs.

## Technique-edit candidates (single-sighted, banked)

- versioned-record-envelope lacks a rule for a member you do not RECOGNIZE
  (vs a field you cannot fill): the counterpart requires the envelope to
  declare that it carries unrecognised members (a sentinel vocabulary
  appended last). "Mark it explicitly unknown" hoisted to vocabulary level.
- The migration rule should require the step to declare its DIRECTION
  (one-way vs round-trippable); a one-way migration used as a round trip is
  how a re-import asserts what the export never said.
- The version is an ordered VECTOR, not a scalar dispatch key, once
  extensions compose.

## Law-adjacent sighting

- "An unrecognised input is admitted only if it is labelled unrecognised" -
  rhymes with the bundle law absence-of-evidence-is-not-evidence and with
  unmapped-stage-stays-null-never-guessed in this subject. n=1.

## Open leads

- The counterpart's ecosystem-compatibility section (three MUSTs on any
  format transformation) is a second un-mined application here, pairing with
  human-versus-automated-decision-attribution-on-export.
- Its related-resource integrity hook (verifier MUST error on digest
  mismatch) - executed counterpart for a future integrity technique.
