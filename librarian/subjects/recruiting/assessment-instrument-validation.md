---
subject: assessment-instrument-validation
domain: recruiting
last_touched: 2026-08-23
touched_by: external-reconcile
dry_streak: 0
---

# assessment-instrument-validation

First touch: [[2026-08-23-8]], external reconcile against
`US-EEOC/29-CFR-1607` (eCFR currency date 2026-08-20, XML digest pinned).
Gained `spec--minimum-cohort-and-inconclusive-verdicts` (uncovered). Hint
confirmed with two corrections (the floor is qualitative; the third state
lives in the recordkeeping section, not the verdict section). Executed: the
regulation's own selection-rate procedure worked over a REAL published
bias-audit table (an AEDT vendor's published audit under a municipal
automated-hiring law) - reproduced both published ratios, then showed the
smallest cohort (n=121) is at once too small to fail and too small to pass
(p=0.058/0.061 vs the regulation's 0.05) - the technique's INCONCLUSIVE
exactly, published as an undifferentiated PASS.

## Technique-edit candidate (single-sighted, banked)

- The precedence rule "a real signal always beats the absence of one" is
  exploitable by whoever owns the harness: the regulation closes it - where
  the required data was NOT MAINTAINED, an inference of adverse impact may be
  drawn from that failure itself. "Not evaluable is never a violation" needs
  a manufactured-absence clause. Priority candidate.

## New shared observation (n=1, watch for the second)

- Substituting a SYNTHETIC cohort for an absent real one, then publishing an
  undifferentiated PASS - the audit's race/intersectional rows are LLM
  personas with demographics carried by generated names, folded into the
  same verdict as real-data rows. Two sightings make a technique; four, a
  law conversation.

## Cross-subject proposals (director places)

- adverse-impact-and-proxy-neutrality/selection-rate-ratio-testing:
  "per gate, always" is contradicted by the regulation's bottom-line rule
  (itself rejected by the Supreme Court) - worth a note on why the technique
  is right anyway.
- minimum-cohort-before-a-ratio-is-asserted: the 30-per-group floor has no
  regulatory backing - the regulation delegates the number to the user; a
  supporting citation, not a contradiction.
- too-small-to-assess-as-a-distinct-verdict: the regulation's
  data-insufficient state exits only via an affirmative clean determination -
  ready-made external precedent.

## Open leads

- The same vendor's matching-model audit excludes ~85M unknown-race records
  against 15.4M known under a PASS headline - a class-C numbers finding for
  a fairness-reporting subject.
- A state comptroller audit found >=17 of 32 reviewed employers potentially
  non-compliant with the municipal law - currency signal for the bias-audit
  subjects.
- The regulation's bottom-line clause vs case law divergence - unclaimed.
- 2025 pressure to rescind the part; still in force at the pinned date -
  watchlist currency note, not a claim.
