---
domain: agent-operations
subject: shared-research-record
last_touched: 2026-09-24
touched_by: intake
dry_streak: 0
---

# shared-research-record

Subject note. Part of [[index]]; graded against [[standard]].

## Touch log

### 2026-09-24 - `/intake`, founded from a paper (XL, forged in-session)

Founded with 5 techniques and 3 applications in a new category, `agent-operations/
coordination`. The source was [[2026-09-24-agora]]. It is a paper whose repository holds
no code. It describes self-directed agent sessions coordinating only through an
append-only record of typed contributions, and reports one measured community run. The
design read found three mechanisms no subject modelled, all sharing one home:
independent-reuse evidence, explicit explore slots, and a typed contribution vocabulary.
The spec is `librarian/handoffs/2026-09-24-agora-shared-research-record.md` (EXECUTED).

`software-engineering/llm-agent/orchestration` was full (10 subjects, V1). The bundle
boundary is stated in the golden path's prose: concurrent-vcs owns where-claims and
physical isolation, and fleet-orchestration owns assignment.

Two apply rows, both on seams chosen to falsify:

- **explicit-explore-slots against this registry's own fleet: `not-better`.** Work
  picked from the single defect-ranked worklist was *less* concentrated than
  human-routed intake work (top-5 share 0.138 against a size-matched p5 of 0.152;
  top subject about 4%, where the paper's trigger was one third). This drew the
  technique's first boundary: a frontier that empties itself when worked does not herd.
  Only an achievement leaderboard does.
- **resolution-bounded-leaders against the personas memory-year ladder: `better`,
  shipped.** Five successive leader claims from 0.86 to 0.92 over 194 shared probes.
  None survives an exact sign test on discordant probes, and the known-separated control
  resolves. This added the paired case-sampling margin to the technique, and it narrowed
  the "exact evaluator" exemption to claims about the cases themselves.

**Candidate law, not written:** "whatever the shared record ranks first, the whole
community does next". It needs convergence across independent runs. Return when a
second community run, or a second fleet's frontier data, is read.
