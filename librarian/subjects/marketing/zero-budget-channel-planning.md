---
subject: zero-budget-channel-planning
domain: marketing
last_touched: 2026-09-20
touched_by: intake-ltcwn-0920 (intake)
dry_streak: 0
---

# zero-budget-channel-planning

First touch. Forged 2026-09-09 in the founding wave of the `marketing` bundle
(category `local-and-zero-budget`), one worker, expert draft first, then reconciled against the
consumer workspace at `2893314` and the SEO prompt pipeline at `a47c1ecd`.

**use_when:** ranking the free places a business with a website can be found, generating or reviewing a channel plan for a business with no ad account, deciding whether a measured click may change a channel's rank, designing an onboarding path for a business type that cannot spend

**Techniques (8):** admissibility-before-fit-and-effort, channel-family-taxonomy-by-lifecycle-kind, fit-effort-quick-win-rule, free-channels-lead-onboarding, grounding-precedence-catalog-over-scan, keyword-to-channel-visibility-plan, measured-clicks-beside-not-inside-fit, owned-push-as-its-own-kind.

**Applications (5; stacks: node):** node--admissibility-before-fit-and-effort, node--grounding-precedence-catalog-over-scan, node--keyword-to-channel-visibility-plan, node--measured-clicks-beside-not-inside-fit, node--owned-push-as-its-own-kind.

## Sightings

- Every threshold the subject states is labelled documented / measured / convention in
  the technique that carries it; the worker's report listed the conventions and they
  are in the files, not only in the report.
- Deviations of the reconciled trees are recorded in the applications with the standard
  kept; the source note `librarian/sources/2026-09-09-seo-agent.md` carries the
  deviation backlog by area.

## Owed

- Single-stack debt: born with the stacks above; an external reconcile against a
  second tree is the next pass.
- No `librarian/applied.md` row yet except where the source note says so; the
  unapplied backlog is every technique here without a row.

## 2026-09-16 - intake `youtube:JEnO3a87RwQ` - a fifth kind

`owned-push-as-its-own-kind` landed here, and with it three corrections. The
subject's fold said seven families make four kinds and closed by refusing a fifth
row; the owned-content family was two kinds, split on pull versus push. The
consequences were the query rule (a newsletter was dealt target queries it cannot
rank for, because "carries content" was standing in for "findable") and the cadence
cap (which belonged to the conversational kind because community norms were named as
"the one place" over-participation costs a channel).

Corroboration came from two places the source could not be: `systedo-case` had
shipped the same four-kind fold with the seeded newsletter in the content kind, and
the deliverability half rests on a mailbox provider's published sender requirements
rather than on the video. Applied `code` / `better` there (`e18be2c6`), seam chosen
to falsify, target 2 -> 0 mis-dealt queries with a floor declared before the arms.

Owed here now:

- **The push cadence ceiling is a rule with no instrument.** The technique says the
  ceiling is discovered from a complaint or unsubscribe rate, and no managed tree
  reads what a send produced, so no application proves that half. First tree to grow
  the signal takes it.
- **A second tree for the fold.** One project's agreement is convergence; this run
  never checked whether any other tree implements a channel taxonomy, and two would
  argue for a higher altitude than a technique.
- `node--keyword-to-channel-visibility-plan` now carries a dated section recording
  that the by-construction rule it credits propagates a fold error silently. That is
  the reusable half for anyone copying the design.

## 2026-09-20 - intake-ltcwn-0920 (sponsored SEO review)

Landed `admissibility-before-fit-and-effort` and a golden-path holding, from the
enumeration hunt run twice over this subject's own declarations.

- **The kind table's own claim invited the question.** `channel-family-taxonomy-by-lifecycle-kind`
  says "The table is the whole technique" and folds seven families into five kinds,
  every one of them *won by doing work*. A supply won by enrolling is in none of them.
- **The ranking cannot refuse.** Fit is a prediction about audience match, effort a
  statement about labour. The quick-win rule surfaces the first untracked low-effort
  channel with fit >= 70, so a channel that pays in inbound links for no work is
  promoted by construction. The gate is therefore a stage upstream of the scoring,
  not a flag beside it - a flag on a ranked row still renders the row first.
- **The discriminator came from the primary, not the source, and inverted the obvious
  rule.** Not reciprocity - this subject already names the arrangement next door as
  legitimate - but whether the party whose page carries the link chose it and was free
  to qualify it.
- **Asymmetry worth remembering:** the bundle has a whole subject on whether a page set
  deserves to exist (`local-page-doorway-prevention`) and, until now, nothing on
  whether a channel deserves to be on the plan.
- **Second touch in twelve days, and the two touches share a root.** The 2026-09-16 run
  landed `owned-push-as-its-own-kind`, whose `use_when` ends "reading why a channel that
  costs nothing to send is still spending something". Both findings are about a channel
  whose cost is a spent asset rather than labour. Two instances; a third would make the
  root worth stating at golden-path level rather than in two techniques.
- Applied at `systedo-case` as an `experiment`, verdict `better`: target 1 of 1, floor
  0 of 23 curated channels refused. Ship 0 - the gate's output is a question and that
  tree has no advisory field, which is a direction rather than coverage.

dry_streak: 0
