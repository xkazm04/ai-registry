---
domain: media-generation
subject: platform-format-adaptation
last_touched: 2026-09-26
touched_by: deepen
dry_streak: 0
---

# platform-format-adaptation

Subject note. Part of [[index]]; graded against [[standard]].

## Touch log

### 2026-09-26 - `/deepen`, first pass (run `deepen-pfa-0926`)

**Why it ran.** The dispatcher named the **single stack (process)** clause.
The 2026-08-27 sweep had declined that clause as needing /reconcile's second
*tree*. Two further reasons made a first deepen pass legitimate:

- **Never deepened.** Revision 1, unchanged since 2026-08-21.
- **Three intake runs left untriaged rows pointing here.** They were the
  2026-09-14 motion-graphics teardown (row 8), 2026-09-16 HyperFrames (row 4) and
  2026-09-22 AI shorts guide (rows 1, 10, 14). Row 1 was `corrects-claim`.

**The lanes.** Four ran:

- counter-evidence (web);
- current platform facts (web);
- training-data-only (blind);
- a read of `systedo-case@451b6e17`, the fleet map's candidate tree.

I re-checked every load-bearing citation myself before drafting, and read the
current files first.

**Landed** (ai-registry `cbcf9f11`, `84b80eb7`):

- **Sound-off premise refuted (rule flipped).** The 69–85% muted share is 2016
  Facebook-feed data: three publishers' own reports, Digiday 2016-05-17. TikTok
  and Reels autoplay with sound. The only platform figure is an attitude survey
  (Kantar for TikTok, 88% say sound is essential). The golden-path section and
  the technique now key the silent share to the *surface*. The text layer
  survives as insurance and stays the starting point on muted-autoplay feeds.
  The counter-evidence and blind lanes converged on this independently. "80% of
  Reels viewed with sound on" circulates, but I could not trace it to any
  Instagram page, so it is recorded as untraced and not used.
- **Hook overclaim corrected (rule flipped).** "Banned, not merely discouraged"
  and "three shapes exhaust the useful space" said more than the source tree
  says. Gravitone's own OPEN-QUESTIONS s4 calls the rule "a reasoned preference,
  not a finding", drawn from an absence at n=3. The corpus had laundered an
  inference into an absolute, the failure its own `format-as-measured-template`
  names. The rule is now avoided-by-default, with its grade stated. The defect is
  narrowed to an opening that prices its payoff without opening a gap. Tooling:
  never the default or the fallback. A fourth, visual shape was added, the payoff
  shown first. Vendor evidence, both sources verified: OpusClip 34,635 clips
  Q1 2026; Content Labs 14,424 clips, 2026-08-20.
- **Retention.** The three-second percentages are downgraded to vendor lore. The
  Dashlet user study (arXiv 2204.12954, n=133 MTurk viewers) finds swipes bimodal:
  29% in the first fifth, 42% in the last. Curve-shape reading was confirmed and
  left alone.
- **Safe band.** Off-centre, because the right column is wider than the left
  margin. It is ≈980 px tall under Meta's 14/35/6 ad margins, as quoted by trade
  press; the help page body did not render.
- **Platform table re-dated** (`process--sound-off-first-design`, verified_on
  2026-09-26, refresh_by 2026-12-26):
  - YouTube Partner Program 2027 row corrected from YouTube's own post
    (2026-08-10): 20M Shorts views for entry, and 10M in the trailing 90 days
    becomes an ongoing condition for Shorts revenue share;
  - Reels ceiling 20 min, rolling out;
  - Shorts view metric changed 2025-03-31;
  - Content ID change for 1–3 min Shorts, 2026-09-24;
  - RPM gap widened to ≈10–100×, unaudited.

  The vendor band rows were carried from 2026-08-20 and not re-sourced.
- **Amendment**, `format-as-measured-template`: *say how the sample was drawn*.
  - absence read as underperformance;
  - shared read as causal;
  - a band censored by its era's ceiling;
  - outlier ranking (median, one format and era) to choose what to study, never
    to certify.

  This settles 2026-09-22 row 1, and the blind and counter lanes converged on
  its weaknesses list.
- **Widen**: `next--format-as-measured-template`, the subject's first
  non-`process` application (systedo-case, next@16.3.3). It is a platform-limit
  table with no provenance, enforced as a 422 refusal and a silent truncation,
  with a ceiling (X 280) and a craft target (Newsletter 600) in one object. A
  transplant to text posts; the video half is declared not to apply.
  **The single-stack clause clears; points 5 -> 3.** "Never swept by the
  librarian" remains.
- Minor: "the single most common derived-short defect" became "the one
  practitioners and clipping tools warn about most". No frequency data exists.

**Verified and left untouched:**

- reframe-not-crop: AutoFlip, plus a 2026 paper;
- retention-curve shape reading;
- derived-clip re-authoring, in direction;
- the image-led vs narration-led 2× spread. No external data either way; it
  rests on gravitone's measured witnesses. The 2026-09-26
  short-form-narrative-structure pass separately demoted 200–250 wpm to "the fast
  top", which is consistent.

**No new technique.** Nothing new reached technique-level convergence. Payoff
shown first is one vendor voice plus the blind lane's taxonomy, so it landed as
a shape inside the existing technique.

## Impact

gravitone's map rebuilt, once by this run from a base missing a sibling's landing
(`169b6cf`), and repaired from the union tree (`2c1f836`, commit messages
describe both). This subject joins gravitone's `video-clip-pipeline` only, state
`unknown`: **0 stale verdicts on this subject.** systedo-case has no map pair for
it; the join is the fleet map's `candidate` row.

## Applied (3 rows, 2026-09-26)

- **format-as-measured-template, amended:** gravitone, simulation, **better**.
  The old text flagged 0 of 3 cases, the amendment 3 of 3, and case 1 matches the
  tree's own s4.
- **hook-shape-selection, flipped:** gravitone, simulation, **unmeasurable**. The
  instrument is per-shape swipe-away rates, and the tree publishes nowhere.
- **sound-off-first-design, flipped:** **unapplied**. No fleet tree composites
  captions onto a vertical render.

## Declined / not placed

- **2026-09-22 row 10**, "leave top and left empty for the overlay": still
  contradicted by the occlusion map. It is render-bound, so it stays banked
  under intake's render gate.
- **2026-09-22 row 14**, bold text with thin outline: thin, and styling is
  delegated to the template.
- **2026-09-14 row 8**, "pair two incongruous things as the hook": a catch.
  It is contradiction's collision mechanism, already in the table.
- **2026-09-16 row 4**, hostile codecs to proxies: not this subject's claim
  (render ingest). Left for `video-assembly`.

## Leads (return conditions)

- **systedo-case limit tables**: split each into ceiling + target and stop
  refusing on targets. A code apply is available (`route.ts:130`,
  `social.ts:185`) and was not run this pass, because the Facebook and TikTok
  ceilings need a dated platform source first. Return: the next apply pass on
  systedo-case, or a user report of a refused post.
- **Hook evidence**: return when a fleet project posts shorts and reads back
  viewed-vs-swiped per clip.
- **Instrument defect, not this subject's**: `scripts/check-citations.mjs`
  trusts a HEAD 404. support.google.com and tiktok.com answer HEAD with 404 and
  GET with 200, so four live platform citations here read as `gone`. Return:
  whoever next owns the citation checker (fall back to GET on 404 from known
  HEAD-hostile hosts).
