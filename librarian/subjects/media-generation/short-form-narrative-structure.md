---
domain: media-generation
subject: short-form-narrative-structure
last_touched: 2026-09-26
touched_by: deepen
dry_streak: 0
---

# short-form-narrative-structure

First touch: a `/deepen` dispatch on 2026-09-26, ranked for **single stack
(process)**. The same signal was declined on 2026-08-27 as "needs a second
*tree*, which is /reconcile's"; this pass found the second *stack* inside
the one joined tree - its code, which the two doctrine applications never
recorded. The single-stack clause is cleared; the single-tree one is not.
`systedo-case`, the fleet map's one candidate, has no seam: it writes
social post text, not scripts.

## Landed (ai-registry `b08251b7`)

- `applications/next--but-therefore-beat-linking.md` - next@16.3.3,
  `applied: code`, `better`. The connector alphabet at three layers, the
  trailer's computed four-verdict check, and the explainer's one door that
  defaulted a missing connector to THEREFORE - fixed in the tree
  (`f7ecedb`, 4/4 bad inputs refused, controls held both arms).
- **Golden path, answer-early**: rule kept, boundary added - a reveal format
  that *declares* its withheld answer is honest. The "suspicion" mechanism had
  no source; replaced by the signaling meta-analysis (g 0.53 retention,
  Schneider et al. 2018, verified at source).
- **Golden path, rate**: 200-250 wpm demoted from "the rate" to the fast
  top; voiceover 150-160; image-led lower; speed-up costs retention
  (g -0.41, 12 studies, 2025, verified) and captions soften it.
- **reframe-close + golden path**: the recap refusal is practice, not a law
  of learning; instructional pieces close on a retrieval prompt.
- **question-stack-architecture**: 12 s is the spoken question's deadline,
  not the gap's, in a feed short; plan-first generation evidence (Re3, +14
  plot coherence / +20 premise relevance, verified) with its limit stated.
- **but-therefore-beat-linking**: undeclared is unmeasured, never
  defaulted; causal-network recall evidence cited.
- Both `process` applications re-resolved at the tree's `3ee32c6`; the
  pattern study is unchanged since `31d1777` and every cited range lands.

## Verified and left untouched

Information-gap theory (inverted-U replicated, 2023); causal linking
(Trabasso & van den Broek); "nothing before the gap"; "one question under
three minutes" (unverifiable either way - a practitioner heuristic, and the
file already marks the 4-8 minute middle untested).

## Impact

Map rebuilt for gravitone (`1f4fc7f`): the subject joins `script-phase`,
`trailer-script`, `research-run-engine`, all `unknown`. **0 stale verdicts
on this subject.** The rebuild surfaced 7 stale verdicts on subjects that
moved earlier (test-harness, quality-gates, video-assembly x2,
review-iteration-loops, agent-cli-transport, evidence-bound-visuals) -
gravitone's `/conform --stale` queue, not this run's.

## Applied (5 rows, 2026-09-26)

code better (connector door) · simulation better (answer-early boundary,
3/3 vs 1/3) · simulation better (rate by register, 3/3 vs 0/3) ·
simulation unmeasurable (feed-short first seconds; instrument named) ·
unapplied (recap condition; no instructional template in the fleet).

## Declined

- **Worst-to-best + visible remaining list** (2026-09-22 source, row 11):
  listicle and vendor sources only; the blind lane disagreed on order
  (strong first *and* strongest last). Held until a measurement exists.
- **Loop endings for replay**: no platform ties loops to ranking; TikTok
  documents completion and early replays as signals, YouTube says its view
  change does not touch recommendations. Commentary only.
- **"Incidental facts inside an open gap are remembered worse"**: one study.
- **Lead 4-142, non-speaking shots as rhythm**: shot rhythm is an assembly
  concern; wrong home for beat structure. Left queued for its right home.

## Leads (return conditions)

- **TRANSFER** - a deduction or hand-off has no honest BUT/THEREFORE; the
  tree widened the research vocabulary and refused to widen the render's.
  Return: a second tree that meets a ledger inside a mechanism chain.
- **Computed explainer connector check** - the explainer's craft rows are
  transcribed; a twin of the trailer's check is small. Return: next apply
  pass on the tree.
- **Rewrite breaks a relation invisibly** - shared with
  review-iteration-loops. Return: a semantic re-check of rewritten beats.
- **Retention evidence for any of this** - the tree publishes nowhere.
  Return: the first fleet project that publishes shorts.

## Source classes this pass

Peer-reviewed meta-analyses carried every correction that landed;
platform help pages settled the metric questions and refused the loop
lore; listicles supplied every declined claim.
