---
domain: media-generation
subject: trailer-structure
last_touched: 2026-09-01
touched_by: external-reconcile
dry_streak: 0
---

# trailer-structure

First touch: [[2026-09-01-1]], the librarian sweep that found this the bundle's one
application-less subject. Class D (craft canon) has no external counterpart, so the
binding went to the connected consumer tree that builds trailer-shaped pieces (project
`gravity`, `main` @ `51c0eea`, node@24) rather than to the reconcile lane's A/B/C classes.

## Landed

- `applications/node--cue-first-assembly.md` (130 lines). Fate **confirmed**: the cut's
  type graph makes the cue the parent (movements and beats point into it; the six-part
  cue shape is closed; `frozen` records procedure step 5), and a 1296-line structural
  checker quotes this subject by registry path in every finding. Six of seven techniques
  are realized; the seventh (`concealment-and-its-tells`) is named `unmeasured` in a
  signed comment rather than omitted.
- **The director's expected negative was refuted**: `promise-ledger` and
  `withholding-budget` are both fully modelled (payer required, ledger advisory; five
  asset kinds and three allowances closed; missing budget reported undecided, not clean).

## Sharpest finding

The tree obeys the technique's tool rule in the *plan* and breaks it in the *picture*:
the shot lane derives every boundary from beat timecode against measured pace bands and
never reads a cue mark (a grep for the cue on the shot placer returns 0), while its own
comment claims the shot layer inherits the marks. Removing the cue from a well-formed cut
dropped the checker's enforced fraction 67% -> 53% with violations unchanged - the
technique's "measured against positions the music does not mark" as arithmetic.

## Technique-edit candidates (banked, one sighting)

- `cue-first-assembly` decision rule: parenthood must survive to whatever layer actually
  places cuts; a plan-layer-only cue is the partial that looks like compliance. Also the
  falsifier: a checker whose enforcement drops when the cue is removed has a parent;
  one whose score is unchanged has a field and a track.

## Leads

- The shot lane has no cue seam. Return: when the shot placer gains a cue-aware path or
  the cut step learns real shot boundaries.
- `dynamic-reset` and `withholding-budget` are each near-complete in the same tree -
  a cheap second application on the same counterpart.
- `concealment-and-its-tells` has no rule; return if a cut ever carries a signal about
  the work itself.

## Cross-subject proposals (placed by the director in the run note)

- The tree is the strongest realization of `unmeasured-is-not-pass` seen so far.
- `short-form-narrative-structure/but-therefore-beat-linking`: the connector check
  implements the neighbour's AND-THEN test and reports undeclared as unmeasured.
