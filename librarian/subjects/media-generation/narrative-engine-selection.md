---
domain: media-generation
subject: narrative-engine-selection
last_touched: 2026-09-26
touched_by: deepen
dry_streak: 0
---

# narrative-engine-selection

Subject note. Part of [[index]]; graded against [[standard]].

## Touch log

### 2026-09-26 - `/deepen`, first pass (dispatched on "single stack (process)")

**Why it ran.** The dispatcher ranked the subject on the single-stack
clause, which [[media-generation]] had already declined for `/deepen` on
2026-08-28 as `/reconcile` work. The clause did not earn the run. The
subject had never had a deepen pass, so its strongest claims had never met
counter-evidence (dry streak 0, never saturated). That is what the pass was
for. The single-*stack* flag cleared anyway, and honestly: once the
sibling's same-day pass showed the source tree's app code binds as `next`,
this subject's seam was found there too (see `next--fit-vs-hazard-axes`).
The single-*tree* clause stands: every application still reads one studio.

**Lanes.** Counter-evidence (web), primary sources (web, opened), and a
blind training-data-only lane, plus a re-read of the source tree (the
studio whose `knowledge/ENGINES.md` both applications cite).

**Corrected (3 lanes plus the tree converged):**
- **Causal-opener density** was read as a measure of derivation, with "a
  verdict engine at the bottom of the band is announcing theories". It now
  reads as a measure of *explicit marking*, largely the writer's voice,
  compared only within one writer.
  - The source's own tone file sets same-engine, comparable-quality writers
    at 14.6 vs 32.0 connectives per thousand words.
  - Coh-Metrix essay studies found no cohesion index (connectives included)
    that correlated with ratings.
  - PDTB-3 leaves about 52% of relations between sentences implicit.
  - Causality-by-default (Sanders; Torabi Asr & Demberg) says readers infer
    unmarked causal links.
  - **Transcription error fixed:** the registry's definition dropped "and",
    but the 15-40% band was counted with it.
- **The steel-man as "the single most reliable honesty signal"** is now
  bounded: a missing one is strong evidence, a present one weak. Refuted
  two-sided messages persuade better (O'Keefe 1999).
- **Tell 1 (premise as a candidate)** gained a boundary: staging a settled
  premise as a live candidate is false balance. It distorts perceived
  consensus even with weights shown (Koehler 2016; Dixon & Clarke 2013).
- **The AND-THEN zero** is kept, but two things are added:
  - It is gameable toward invented causation. This cross-links the landed
    `pre-linked-causal-mechanisms` and does not duplicate it.
  - A deliberate non-link has to be spoken, because adjacency reads as
    causal.
  - Boundary added: a countdown held by a declared gap is a different
    contract.
  - The abandonment rationale is marked unmeasured. Parker and Stone said
    "boring", and no retention study was found.
- **Hazard** now names the doctrine: defamation by implication.
  - US: *White*, *Chapin*, *Toney*. A verdict beat is the "endorses the
    inference" signal.
  - UK: *Chase* levels, bane and antidote.
  - A verdict can be pitched at the level the record carries.
- **Duration invariance** is bounded to the machine's middle. The entry and
  exit differ on feed surfaces (Sołodki 2026; blind lane).

**Verified and left untouched:** `engine-arbitration-order` (nothing
refuted). The catalogue's pleasure key held up as well.

**Applications.** Both existing ones were re-verified (ENGINES.md unchanged
since 2026-08-12, all cited lines resolve, `candidates[]` still absent) and
moved to `verified_on: 2026-09-26`. The adjudication application gained a
re-read: the render surface now shows the tells as hand-stated check rows,
and a cross-writer `≥ 30%` density threshold sits on it. One new process
application, `process--fit-vs-hazard-axes` (fit-vs-hazard),
from three real hostile-review notebooks, applied as a simulation, verdict
better. One new `next` application, `next--fit-vs-hazard-axes`, witnessed
`next@16`. The app stores `engineFit[].hazard` with the technique's thesis
in its doc comment, but the schema does not require the field (so unasked
reads as clean) and no component renders it. The `no-tension` run state
realizes the blocker verdict as distinct from failure. The source tree was
not modified: a live session was committing there during this pass.

**No new technique.** The candidate "a connector is an assertion" was
already landed in `content-research-grounding/pre-linked-causal-mechanisms`
(typed transfer, fake THEREFORE). Placing it here would have been a
duplicate.

## Impact

The subject joins no context in any project's registry map (dry run
2026-09-26: not present in any of the 12 maps; gravitone and systedo-case
are candidate-absent in the fleet map). Stale verdicts caused by this
landing: **0**. No `/conform --stale` queue.

## Contended

A sibling `/deepen` pass on `short-form-narrative-structure` ran the same
day. It committed `f7ecedb` in the source tree ("an inserted beat's
connector is declared, never defaulted"), which is the gameable-zero defect
fixed in code. That row is the sibling's, under `but-therefore-beat-linking`,
and this pass does not claim it.

## Open leads

- **Proposal for [[creator-voice-and-tone]]:** its golden path and
  `dial-vs-subject-property` say causal-opener density "is decided by the
  engine". The same source's tone file contradicts that (same engine, 14.6
  vs 32.0 connectives per thousand words), and this subject's correction
  applies. Not edited here because it belongs to another subject.
- **Continued influence** (Johnson & Seifert 1994, blind lane only). A
  spoken non-link without an alternative explanation may not undo an
  inferred cause. It is one voice, held as the falsifier of the non-link
  row.
- **Retention evidence for "and then".** No study compares list-format with
  causal-format retention. Return when one appears.
- **The second tree** is still owed. The single-stack clause is cleared by
  the `next` application, but every application reads one studio. That is
  `/reconcile`'s engine, and systedo-case is the named candidate.
- **A code apply in the source tree, owed and not taken.** Require
  `engineFit[].hazard` in the notebook schema, where an empty string means
  "assessed, none found", and render it beside `fit` at selection. It was
  not taken because a live session was committing in that tree during this
  pass. Return: the tree is quiet, or its next engine-selection change.
