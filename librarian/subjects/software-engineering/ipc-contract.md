---
subject: ipc-contract
domain: software-engineering
last_touched: 2026-08-29
dry_streak: 0
---

# ipc-contract

First touch: 2026-08-29, /deepen architecture batch. 6 techniques, 3→5 applications
(rust--error-shape-mapping incl. a live second-emitter defect; node--command-
registration incl. the resolved dual-regex post-mortem; all three prior applications
re-verified with witnesses).

Landed: the fourth name set **P (permitted)** for ACL-carrying transports (regime-aware,
per surface; "denied is not missing"); the envelope-rides-whichever-channel-keeps-
structure rule (message-flattening transports; a guard in front of the handlers as a
second error emitter); two-directional casing strictness (responses land in an
erased-type world).

Survived counter-evidence: the no-version-skew claim (fenced by the sole-transport
caveat); generated-types-close-drift for the argument direction; engine-authors-the-
shapes.

Proposals routed: snapshot-then-subscribe contract → realtime-events (the personas-web
mirror-surface question — one section there, not a technique here); mirrored-classifier
parity fixtures → error-handling; one-floor-per-inventory → codegen.

Banked: payload class / streaming channels (return: a fleet tree ships a real
channel-based command or measures a slow crossing); cancellation through the transport
(return: any fleet command gains a cancel token); per-window capability matrices
(return: personas adds a second window or remote origin).

Forecast: techniques near-dry after this pass (the first check against a second
transport was the fat yield); L2 with L3 traces.
