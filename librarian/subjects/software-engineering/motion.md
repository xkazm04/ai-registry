---
subject: motion
domain: software-engineering
last_touched: 2026-08-27
dry_streak: 0
---

# motion

First touch: [[2026-08-22-4]] — the 2026-08-22 harvest wave. Class: EXTENDS.

## State

9 -> 10 techniques, 3 applications. Touched 2026-08-27 by [[2026-08-27-kciter-animation-design]] (intake): one new technique and three amendments, landed under a new golden-path boundary section rather than separately.

Prior state (2026-08-22 wave): 6 -> 9 techniques, 2 -> 3 applications. **Single-stack debt retired**: the new `node` application is the custom static-analysis rule that projects the subject's own written contract into an executable check. Merged from FOUR independent scout proposals across four territories.

## The boundary this subject now states about itself

The preset contract was forged for **one curve, started by something, running to completion on its own clock** — and three classes it blesses elsewhere each break a different one of its assumptions. This was invisible to the slug map and to any summary; it was reachable only by reading the contract's required fields against the sibling technique's list of permitted engines.

- A **spring** has no duration and no easing role. `engine-selection` recommends one; `preset-vocabulary` demanded two fields it cannot fill; `taste-budgets` filed it as "one characterful curve" — three incompatible readings in one subject. Resolved: timing and character are declared **per track**, and a physics track declares a **settle bound** instead of a duration, because a gesture with no duration was escaping the millisecond entrance cap entirely.
- A **composite gesture** is more than one curve. Per-property tracks are now the stated common case.
- **Value-driven motion** has no clock of its own, so the one-shot policy that governs scroll-*triggered* reveals is a bug when applied to scroll-*driven* ones.

`gesture-decomposition` closes the missing stage in front of all three: the subject was thorough from "a preset exists" onward and silent on how a movement becomes one.

## Open leads (banked, with return conditions)

- **user-intent state is never merged with machine-derived state** (proposed law, not added). Three independent sightings measured in this subject: a transient hover-pause kept separate from a sticky user-pause, a manual flag exposed beside the merged signal, and a one-directional stop path. The failure is always the same shape — collapsed into one flag, a machine condition clearing silently undoes a person's decision.
- The lint rule is registered at warning severity and the pipeline runs with no maximum-warnings flag, so the contract cannot fail a build; and the rule is structurally blind to the very rule that motivated it (all three named past failures would pass it clean). Both recorded as deviations. Worth re-checking whether the consuming repo closes either.
- **Randomness is bounded, never pure** (2026-08-27, untriaged — offered at triage, not picked). Controlled ranges over pure random, with the designer explicitly deciding the boundary between what is controlled and what is delegated to variance. Verified genuinely absent from both `motion` and `design-tokens`. A two-paragraph amendment to `taste-budgets`; the cheapest unclaimed item in this subject. Return condition: any run touching `taste-budgets`.
- **Displayed value chases true value** (2026-08-27, untriaged). Exponential approach applied to a jittery real signal so a stuttering data source renders as smooth progress. Home genuinely contested between this subject and `async-ui-states`, and it carries an honesty tension adjacent to `content-bearing-degradation` — the reader is shown a number that is not the current one. Return condition: a run that opens `async-ui-states` and can settle the boundary from both sides.

## Declines

- The worker refused the director's `verified_against: node@20` instruction and wrote `node@22`, citing the workflow as the only runtime witness. It was right; the director was wrong, and five sibling applications were corrected afterwards. Recorded because the trap is reusable: a types-package version looks exactly like a runtime pin in a manifest and here was off by two majors.
- **`atan2` and trigonometric phase-offset math** (2026-08-27). Real and correct, and below the altitude this corpus writes at — implementation craft rather than a decision rule. Do not re-propose as a technique.
- **Mid-flight toggle must reverse, not restart** (2026-08-27, caught as already covered). `engine-selection` has the spring retargeting from position and velocity; `taste-budgets` has enters-decelerate/exits-accelerate and states it more precisely than the source did.
