---
subject: lazy-section-addressability
domain: software-engineering
last_touched: 2026-08-22
dry_streak: 0
---

# lazy-section-addressability

First touch: [[2026-08-22-2]] — the 2026-08-22 harvest wave. Class: NEW.

## State

Founding subject of the `published-surfaces` subcategory. 5 techniques, 2 react applications. Gate clean on first report.

## Open leads (banked, with return conditions)

- **address-precedes-content** (proposed law, not added). Where a name is used earlier than the thing it names can exist, the name must be hosted by something unconditional and only the refinement may be conditional. Three sightings inside one wave: the always-present wrapper, the tracker's late registration, and `guided-tours`' never-strand invariant. `identity-survives-reuse` covers identity across reuse, not identity preceding existence. Return when a second domain sights it.
- The forger found two anchors the scout missed (a resolver fallback carrying an incident comment, and a late-registration watch with a hard cap). Scout coverage of shared hooks was the weak spot, not the contexts.
