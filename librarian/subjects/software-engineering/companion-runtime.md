---
domain: software-engineering
subject: companion-runtime
last_touched: 2026-09-23
touched_by: deepen
dry_streak: 0
---

# companion-runtime

Subject note. Part of [[index]]; graded against [[standard]].

## Touch log

### 2026-09-23 - `/deepen` batch ([[2026-09-23-1]])

First sweep since the 2026-08-24 forge. Chosen on live demand the librarian scan could
not see: its signals files dated from 2026-08-29/30, while the regenerated registry map
carried 10 fresh (non-stale) `deviation` verdicts on this subject in one project - the
largest fresh block in the fleet.

**Eight corrections, no new technique** (every candidate had a home). The load-bearing
ones: the taught/accepted/executed equality holds **per producer**, because the model is
rarely the only thing that files actions and a whole-executor check is false by design;
**a pin written as a list is a copy** - the project's parity guard was not retired for
cost, as its verdict implied, but deleted by a dead-code sweep that removed the helper
it alone used; surfaces are **exhaustive when they ship with the table and tolerant when
they deploy independently**, and a per-kind judgment names both arms; undo, a first-run
default and an uncalled restore are each **half a reset**; for an embedded engine the
**throwaway real instance is the better store double**, so the store capability pays for
the runtime's nouns and a second host, not testability; a **package boundary** is the
cheapest direction check, blind to the acyclic data-layer edge; a second consumer proves
**only what it substituted**. Blind lane converged with the web lane on all five of its
mechanism claims.

Verified and left untouched: the one door for model calls, the headless turn signature
and lock/interrupt rules, cycle admission and its denominator, the operative working set,
report-or-absorb.

Per-deviation: 7 project-falls-short (now `/conform`'s queue, sharpened by the new
rules), 1 misread lane (the e2e suite is not the runtime's test lane), **2 stale** - the
project fixed both on 2026-09-17 with the both-arms test the subject now cites.

**Apply (five rows, [[applied]]):** 4 `better`, 1 `not-better`. The not-better moved a
rule: the throwaway store is a sufficient double only while the *whole* store is reached
through the handle - the project's markdown half is found through a process-global
variable that three lock domains write, one of them lock-free. Condition written into
`host-seam-contracts`. Two pre-run predictions were wrong in useful ways (3 of 5 extra
arms have real producers; 25 not 41 build-refused files).

### Impact (registry map, regenerated 2026-09-23 after this landing)

personas: 10 verdicts stale on this subject, all `deviation`. That is the project's
`/conform --stale` queue for companion-runtime; 2 of the 10 are already fixed in the tree.

## Open leads

- A closed kind type exported to the frontend for the action catalog. Return: the project
  adds the risk column or kind enum its risk module names as the durable fix.
- The producer column wants a *retired* value (an arm kept for draining rows vs an arm
  nobody can reach). Return: the next pass over `action-catalog-single-source`.
- An allowed-edges architecture test over the crate graph. Return: a fleet project
  extracts a runtime crate.
- `node--action-catalog-single-source`, `node--signal-economy-contract`,
  `rust--metered-llm-seam` not re-verified (oldest `verified_on` 2026-08-23 stands).
  Return: next open of kp, and the metered-seam sweep.

## Proposals placed elsewhere or banked

- "Exhaustive when co-deployed, tolerant across a deploy boundary" generalizes beyond
  companions (schema evolution / type boundaries, or a law candidate). Operator call.
- "A test-only helper is deleted by dead-code sweeps" belongs with the gate material or
  the `gate-sees-target` law.
- Test isolation broken by process-global environment - converged on independently by
  both apply workers this run; wants a home in a testing-isolation subject.

## Declines

- Generative-UI catalog practice (covered by structured-output, no demand); a single
  listicle's "partial render recovery" (uncorroborated); a vendor blog with nothing on
  reset.
