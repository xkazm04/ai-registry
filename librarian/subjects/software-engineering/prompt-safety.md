---
domain: software-engineering
subject: prompt-safety
last_touched: 2026-08-31
touched_by: intake
dry_streak: 0
---

# prompt-safety

Touched by [[2026-08-31-voltagent-agent-papers]] (reference-index run over a 382-paper
bibliography). Three amendments, all from **Family A**: the subject's closing ranking
("the worst fully successful injection yields an embarrassing sentence, not an action")
is false for the class where the deliverable IS the model's prose - the capability
ladder holds perfectly and buys nothing, because nothing downstream needs to act.

Three independent sightings from the index (verified disjoint on authors, institutions
and benchmark lineage, the earliest predating the others so it cannot be a relay), but
**the decisive evidence is internal**: the golden path already calls a secret recalled
into prose "the attack proper" in the section on the symmetric boundary, two sections
above the ranking that ignores the text sink entirely. The subject named the hazard in
one place and modelled only the action sink where it ranks its defenses. That asymmetry
is invisible to `research-map` - both sections "cover" output.

Landed: the read-side half of the last fence (golden path); a fourth payoff class
**persuasive** in `payoff-removal`, for a prose channel that works to lower its reader's
chance of rejecting it; and a second residual in `untrusted-span-fencing` that is a
property of a **pair** of spans, where each is individually unobjectionable. That last
one is not an inventory gap - the inventory names retrieved memory explicitly - it is a
remedy gap, because every operation in the subject is per-span and none can see a pair.
Verified in-run: a grep for combination / combined / in-aggregate / jointly / cumulative
across the whole subject returns zero hits.

Not landed, deliberately: the recall-side rule (what a store may recall against a given
request). Its home is `agent-memory`, which a sibling session held for the whole run.

## 2026-09-04 - lead from the `/deepen` batch ([[2026-09-04-1]])

- Landed (Director, one sentence): the third-party-span inventory now points at
  agent-memory/memory-governance for what a store does with a tainted author at its
  write door - the receiving end of this subject's rule. The measured poisoning
  channels (compaction, trace-to-procedure; weak-signal facts evading every input
  detector by >40 points) live there, not here.
