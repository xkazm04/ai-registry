---
domain: software-engineering
subject: companion-identity
last_touched: 2026-09-23
touched_by: deepen
dry_streak: 0
---

# companion-identity

Subject note. Part of [[index]]; graded against [[standard]].

## Touch log

### 2026-09-23 - `/deepen` batch ([[2026-09-23-3]])

First sweep since the 2026-08-24 forge, chosen on 3 fresh `deviation` verdicts in one
project (see [[companion-runtime]] for why the scan could not rank it).

**Four corrections, no new technique.** The package-manager upgrade ladder was reached
by both the blind lane and the packaging web lane, but already had a home, so it landed
as a correction to `constitution-self-model-split`: keep the product's grammar out of the
person's law; fingerprint the shipped text so an untouched file upgrades silently and an
edited one is never overwritten by default; no automatic merge; a backup must succeed
before the write; layering as the cleaner shape. Also: "no amendment path" now includes
the path through the product's own source and build; **self-model diffs sit outside
blanket consent**, with review-after allowed only on four stated conditions; the
index-only-state ban softened to *state whose reset would make the companion say
something false*, with a new **document-first write order**. Blind/web convergence 5 of 8;
the blind lane alone caught three internal contradictions.

Verified and left untouched: the closed three-operation diff grammar and content anchors;
refusing an automatic merge of the law; files as source of truth for one person, one
store, locally; the per-block read-only split between law and self.

Per-deviation: whole-content replacement under autopilot - both (project falls short,
and the subject lacked the autonomy and first-draft conditions); constitution overwrite
plus dev-mode - missing conditions, now written; backlog markdown/SQL split - **stale**,
the project fixed resolution-to-markdown on 2026-09-03, with a write-order residue the
new rule names.

**Apply (three rows, [[applied]]):** 2 `better`, 1 `not-better`. The not-better is the
run's most important result. Under autonomous mode the consent rule moved **none** of
the install's 26 unreviewed self-model writes, because none came through a product door:
the project's test processes write the operator's real brain directory through an
unlocked process-global root, and the live self-model has been a test fixture since
2026-09-20. Condition written into `anchored-identity-diffs`: enumerate doors over every
process that resolves the document's location. The upgrade-ladder row refuted the
landing's worst case *on this install* (0 of 39 backups held a person's edit - there is no
in-product law editor) while confirming the grammar rule (at most 1 of 57 bumps was
durable law).

### Impact (registry map, regenerated 2026-09-23 after this landing)

personas: 6 verdicts stale on this subject - 3 `deviation`, 1 `conformant`,
2 `not-applicable`. The project's `/conform --stale` queue; one deviation is already fixed.

## Open leads

- Layering in the project once the op catalog leaves the constitution file. Return: that
  move.
- Backlog write order. Return: a rebuild-from-disk pass lands - check it names what it
  resets and what it adds.
- Review-after-the-fact. Return: the project ships a visible diff of what autonomy changed.

## Proposals banked

- Link "keep the product's grammar out of the person's law" to companion-runtime's
  `action-catalog-single-source` (the project's test forcing every op into the law file is
  where they meet).
- `capability-exercise-ledger`: a law's statements about its own gates are behaviour
  claims that drift ("never auto-fires" false since 2026-08-10, and 6 of 7 such claims
  conflict with the resolver).
- agent-memory: memory-injection persistence wants a home; cited here only as a condition.

## Declines

- A separate upgrade-ladder technique (home existed). A reading that a vendor's
  whole-block rewrite tool refutes the whole-document ban (having the tool does not make it
  reviewable). Persona-drift literature (relevant, unverified, no rule needed it).

## 2026-09-23 - [[2026-09-23-1]]

Lead drain (run lib-0923), L126 AMEND x2 + APPLICATION. **Correction:** `constitution-self-model-split` claimed an absolute "two files"; the structural property is the write path, and one heading-partitioned document qualifies under four stated conditions (including refusing a law-targeted proposal at propose time). `anchored-identity-diffs`: whatever apply would refuse without reading the live document is refused at propose.

**Impact** (stale verdicts before this landing, from the map rebuilt at the run start): none recorded.
