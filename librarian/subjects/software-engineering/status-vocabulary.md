---
domain: software-engineering
subject: status-vocabulary
last_touched: 2026-09-01
touched_by: librarian-inbox-writer
dry_streak: 0
---

# status-vocabulary

First touch: [[2026-09-01-1]], the librarian sweep that drained the consumer-lead inbox. Not previously swept; the subject had 0 external applications and one deviation stream.

## 2026-09-01 - inbox leads landed

Two leads, two trees (ascent, personas), one inverse mechanism, landed in
`vocabulary-chain-integrity`: when the map IS the enumeration, gate the literal against the
vocabulary with a conformance check that verifies coverage without widening and derive the
runtime list from the gated map - the key-list cast is a derivation, never a gate; and an
ORDER over a closed vocabulary is a total map, not an array, because an array checks entries
and never completeness and the not-found sentinel sorts first. Closing rule: every derived
container over a closed vocabulary is total by construction, checked in the direction the
derivation runs. Applications `next--vocabulary-chain-integrity` (ascent, one documented fix
`79d7d14c` plus the live near-miss) and `react--vocabulary-chain-integrity` (personas,
convergent practice, no conversion commit - said so). Owed: add `unknown-is-not-a-value` to
the technique's `laws:` (body cites it; frontmatter left alone to avoid desyncing the index
mid-wave). Proposal: i18n `token-label-separation` states only the forward rule.
