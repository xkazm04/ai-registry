---
subject: i18n
domain: software-engineering
last_touched: 2026-09-01
dry_streak: 0
---

# i18n

First touch: [[2026-08-22-4]] — the 2026-08-22 harvest wave. Class: EXTENDS.

## State

6 -> 9 techniques, 2 -> 3 applications. Golden path 188 -> 253 lines, additive only.

## Open leads (banked, with return conditions)

- **tolerance-ratchets-down** (proposed law, not added). A committed tolerance ceiling for a known defect class names the mechanism and the moment by which it is lowered; a ceiling that only ever holds becomes permanent headroom new defects occupy silently. THREE recurrences, two in EXISTING techniques (`size-budgets`, `string-extraction-enforcement`). A strong proposal on external evidence.
- The kill switch is one global flag for all thirteen non-source locales, so the first locale ready to ship waits for the last. Per-locale readiness is the obvious next question and the tree does not answer it.

## 2026-09-01 - inbox leads landed under the librarian sweep ([[2026-09-01-1]])

Two leads (ascent). `token-label-separation` and `catalog-architecture` gain the token-vs-
content split: a miss on a token renders the identifier and reports; a miss on content omits
the record, reports the same way, and falls through to a real empty state; totality is a
property of the label path, marked at design time. The golden path's "never a raw key" now
carries that boundary. `completeness-gates` and `string-extraction-enforcement` gain the
classification: an unreferenced key whose source value appears verbatim as a code literal is
a wiring bug, detected by a join of catalog values against source literals, routed to
extraction never to the purge list - single-sighting for the detector, and no mainstream tool
was found that performs the join (the finding). Application `react--token-label-separation`
at ascent `7ed00bb9`, with an unguarded content dereference at the wire edge recorded as the
open gap.
