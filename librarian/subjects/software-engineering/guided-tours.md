---
subject: guided-tours
domain: software-engineering
last_touched: 2026-09-02
dry_streak: 0
---

# guided-tours

First touch: [[2026-09-02-1]]. Class: MATURE (6 techniques; was 2 react
applications, single-stack; 6 consumer deviations on the floor).

## State

6 techniques, 4 applications (react ×2, node ×2). **Single-stack closed** this
run: two framework-agnostic tour engines read at pinned commits.

## What run 2026-09-02-1 changed

- `anchor-contracts`: "reuse the test suite's identifiers" carries its condition —
  production-only build passes strip test attributes, so the attribute must be
  declared as shipping and the drift gate pointed at production output.
- `missing-anchor-degradation`: engines ship a GLOBAL fallback (usually
  re-center) with per-step skip as opt-in, so per-step policy is the integrator's
  discipline; bounded patience is a mutation watch with a deadline; skipped steps
  fire no engine event, so the degradation ledger is written by the integration;
  first-step stranding named as the invariant's commonest failure site.
- `overlay-precedence`: the focus-trap absolute refined, not reversed — the
  field's scoped trap (dialog + anchor) is acceptable only when escape truly
  exits (SC 2.1.2, A) and the anchor is inside the loop; needs a per-step off
  switch; restoration target named; SC 2.4.11 (AA) anchors the dimming.
- `narration-and-pacing`: SC 2.2.2 (A) bound to auto-starting motion.
- Two node applications, one per engine, each with a "what this tree does not
  cover" section. WCAG 2.2 edition dated at the application layer.

## Open leads (banked, with return conditions)

- **Offer policy / targeting technique** (eligibility, triggers, frequency
  capping, one-tour-at-a-time, viewport gating). Blind lane predicted it; no
  primary source reached. Return when a published onboarding or
  product-analytics spec can be read, or a consumer deviation names it.
- **Third engine read** (global-skip default, a target-not-found event, the
  first-step hang issue). Return when a sweep wants a third application on
  missing-anchor-degradation.
- **Golden path "exit is never a confirmation chain"** has two engine-level
  opt-ins against it; one sentence naming the opt-in as the thing to refuse is
  owed to the golden path. Not landed: worker left the golden path untouched.

## Cross-subject proposals (placed)

- modal-stack: third-party overlays mint their own z-index and cannot register
  into a product layering authority; popover placement/collision belongs to
  anchored-positioning, not here. Recorded on [[modal-stack]].

### Impact (2026-09-02)

Stale verdicts after this landing: personas (2), personas-web (1). Apply row: see `librarian/applied.md`.
