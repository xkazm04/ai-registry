---
subject: tool-result-economy
domain: software-engineering
last_touched: 2026-09-04
dry_streak: 0
---

# tool-result-economy

First touch: 2026-09-04, forged in-session by an `/intake` run over a first-party
practitioner account of one team's harness cost work (run `copilot-cost`). Created
because three picked candidates and the general form of a fourth shared one
`HOME IF NEW`, and the nearest existing home — `prompt-assembly` — was at 16
techniques and owns a different artifact.

Placement: the 9th subject of `llm-agent → prompt-and-context`, verified against
`taxonomy.json` (flat subcategory; sibling `runtime-and-io` runs at 10).

## State

4 techniques, 1 application (`rust`, `applied: code`, `ab_verdict: unmeasurable`).
Single-stack and single-source: everything here rests on one team's account plus
corpus-internal corroboration. That is the subject's standing debt.

Landed:

- `end-to-end-unit-of-optimization` — the unit of optimization is the unit the
  payer is charged for; anything narrower can be improved by displacing work
  across its boundary. Carries the workload-locality corollary (a result does not
  transfer between surfaces even on a shared harness).
- `compressibility-follows-the-producer` — classify by what produced the bytes,
  not by the bytes or their size; exact-preserve → lossless reorganization →
  lossy, and only the third for output repetitive by construction.
- `formatting-before-information` — information-preserving transforms first,
  because their saving is unconditional; the vestigial affordance and its
  per-emission-against-per-consumer asymmetry.
- `escape-hatch-usage-as-the-safety-metric` — the recovery path is the transform's
  own regression test, with the advertisement precondition and the conditional
  denominator.

## Boundaries stated, not linked away

- vs `prompt-assembly`: *assembly asks what fits; this asks what the result costs
  by the time the task is done.* Written into the golden path's opening.
- vs `prompt-assembly/elision-to-a-refetch-pointer`: recoverability vs production
  as discriminators; they compose (producer class gates admissibility,
  recoverability gates elision-vs-compaction) and run at different stages.
- vs `prompt-assembly/compression-hardens-deferred-decisions` (landed the same
  run): an instruction contains deferrals that compression makes; a tool result
  contains none.
- vs `eval-harness/metric-role-contract`: roles vs scope. Independent failures,
  both required. The forge worker confirmed nothing in `eval-harness` owns scope.

## Near miss worth recording

A parallel session landed `eval-harness/outcome-conditioned-cost` mid-run, which
conditions *which trials* enter a cost mean. It does not collide with
`end-to-end-unit-of-optimization`, which governs the *boundary* the cost is
measured inside — but the two are one shelf apart and a later run should read
both before adding a third cost-measurement technique anywhere.

## Leads

- The forge worker flagged that `context-budgeting` already argues "shrinking can
  cost more than keeping" from the assembly side (break-even in inclusions, cache
  multipliers), which is the same shape as `end-to-end-unit-of-optimization`'s
  displacement argument arriving in a different currency. The corpus now says it
  in two places with different objects. Return: if a third sighting appears, this
  is a law candidate about measurement boundaries rather than two techniques.
- The subject's one application is `unmeasurable` because a test binary would not
  launch. Return: when it is re-run on the primary dev box.
