# Inbox - leads from connected projects

Knowledge candidates originated by a sweep in a consuming repository and folded here by
[`scripts/leads-collect.mjs`](../scripts/leads-collect.mjs). **A lead originates a finding;
it never authorizes one.** Nothing below is in a bundle. Triage is `/intake`-class work:
check prior art against the named subject, corroborate against a second source, and land
only what survives - as a technique, an application, or a decline with a reason.

Append-only. Strike a line through when it is ruled on, and record the ruling in the
subject note; never delete, or the same lead arrives again next quarter looking new.

| date | bundle | nearest subject | kind | claim | conf | from |
| --- | --- | --- | --- | --- | --- | --- |
| ~~2026-08-27~~ | software-engineering | agent-cli-transport | technique | ~~When a spawn passes through a shell, assert the child's OBSERVED argv carries the restriction rather than the vector you built - a zero-length argument is deleted by concatenation, and the flag it belonged to then consumes the next flag as its value, silently removing a declared permission stance with no error anywhere.<br>*why:* Measured on win32 where the CLI is a .cmd shim needing shell:true: --allowed-tools "" --max-turns 1 reached the child as --allowed-tools=--max-turns plus a stray positional, so both the tool allowlist and the turn cap were absent on the only platform the app runs on, while the source comment described a sandbox. No in-process assertion could see it - the loss happens after the array stops existing.~~ **COVERED - prior art already states it; see run note** | high | gravity / scan-sweep@2.0.0 |
| ~~2026-08-27~~ | software-engineering | error-handling | technique | ~~When classifying a dependency's failure, order the signals: what the dependency STATED outright (status code, structured field) decides first, and a text heuristic over its prose is the last resort reached only when the stated signals are silent - a heuristic evaluated first will overrule the code, and it will do so on ordinary vocabulary rather than on exotic input.<br>*why:* Measured: an adapter computed refusal = status===451 \|\| /moderat\|policy\|not allowed\|prohibited/i.test(body) and threw on it before checking 429/401/403. 'policy' is ordinary throttling and auth vocabulary, so a 429 reading 'quota policy exceeded' was classified as the model declining the brief - and in this taxonomy a refusal is never retried while a throttle always is, so the two are opposites. Three of six probe cases flipped. taxonomy-design already states 'the stated interval always wins' for retry intervals; the same precedence is not stated for the classification itself.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: error-handling/structured-propagation (stated signals decide first at the birth site; text heuristic last) - subject claimed by a sibling intake run at sweep time** | high | gravity / scan-sweep@2.0.0 |
| ~~2026-08-27~~ | software-engineering | status-vocabulary | technique | ~~When a closed vocabulary needs an ORDER, hold it as a total map from the vocabulary type (Record<Kind, rank>) rather than as an array of that type - an array type-checks its entries and never its completeness, and the lookup that follows (indexOf) answers a missing member with a value that is not 'unknown' but 'first'.<br>*why:* Measured: SPINE_ORDER was readonly MovementRole[] and spineSort used indexOf, so a role absent from the list ranked -1 and led the composed trailer. A probe composing an unranked role produced 'button -> cold-open -> climax'. Converting to Record<MovementRole, number> makes the omission a build error and the derived order cannot disagree with the rank. The failure is invisible precisely because the compiler IS checking the declaration - just not the property that matters.~~ **LANDED [[2026-09-01-1]] -> status-vocabulary/vocabulary-chain-integrity** | medium | gravity / scan-sweep@2.0.0 |
| ~~2026-08-27~~ | software-engineering | browser-credential-boundary | technique | ~~A URL that arrives inside a DEPENDENCY'S RESPONSE is attacker-controlled input to your server, not a trusted address: validate scheme, IP literals and resolved addresses before fetching it, and re-validate every redirect hop with redirect:manual - a pre-flight check on the first hop alone is defeated by a legitimate host answering 302 to a private address.<br>*why:* Measured twice in this fleet on the same shape. Here: an image downloader bounded response SIZE and TIMEOUT for a URL its own comment described as 'a URL a model handed us', and never checked where it pointed; a plate URL naming the cloud metadata endpoint was fetched and returned to the caller as base64. In a sibling app the identical class was found where the first hop WAS checked and the redirects were not. The two bounds that were present are the ones a reviewer thinks of; the destination is the one that carries the credential.~~ **LANDED [[2026-09-01-1]] -> browser-credential-boundary/outbound-fetch-destination-validation (new)** | high | gravity / scan-sweep@2.0.0 |
| ~~2026-08-27~~ | software-engineering | quality-gates | technique | ~~A gate whose POPULATION is a hand-written list can only ever describe the codebase on the day it was typed: derive the set it walks from the filesystem or the schema, exempt members one at a time with a stated reason, and assert the walk found something before trusting its clean verdict - otherwise the gate reads the implementation's own list and reports 'all covered' about the four things it knows.<br>*why:* Measured twice in one repository. An auth probe written to catch an unauthenticated spending route enumerated four handlers by hand; five routes arrived afterwards, one of them ungated and spawning an 800-second paid run, and the probe could not see it. A bundle gate's server-only variable list carried the comment 'kept in step with .env.example' and had drifted six names behind, including a live vendor credential. Both were fixed by deriving the population; both had been green throughout.~~ **LANDED [[2026-09-01-1]] -> quality-gates/self-reported-gate-inputs + operation-assertion-gates** | high | gravity / scan-sweep@2.0.0 |
| ~~2026-08-27~~ | software-engineering | - | technique | ~~When an affordance is revealed on hover-or-focus, track hover and focus as two separate flags, because one shared flag lets a mouse leaving the region erase the hint that only the keyboard user still needs.<br>*why:* Landed and verified this round in QuestionnaireStackedOptions: onMouseLeave and onBlur both wrote a single `focused` state, so moving the mouse away hid the 1-9 hotkey badges while keyboard focus was still inside the picker — i.e. the affordance disappeared for exactly the input modality it was built to teach. The component's own docblock named that purpose, which is what made the inversion legible. Fix was `hovered \|\| focused`. No nearest subject: this repo's map does not join this context to a UI-interaction subject.~~ **LANDED [[2026-09-01-1]] -> ui-controls/micro-interaction-contracts** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | - | application | ~~When a readiness gate offers a manual fallback for an unmet requirement, the fallback's value must not be what clears the gate, because the escape hatch then satisfies the check it was created to bypass.<br>*why:* Found (not landed — the fix is outside this worker's paths) in two independent consumers of one blocked-question set: a free-text box is rendered specifically under the missing-credential branch, and both readiness counters subtract any answered question, so typing one character turns a blocked template green. Offered as a sharpening of readiness-prerequisites' existing 'degraded adoption is legitimate, silent degradation is not' rule: the corpus states the two conditions (declared, durable) but does not name this mechanism, where the degradation path and the gate share a single field.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: templates-scaffolding/readiness-prerequisites (degradation path and gate must not share a field)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | app-shell | technique | ~~When a reset is keyed on a lifecycle identifier that goes empty -> value at submission, key it on the RETURN to empty instead, because the id changes at the START of the operation and the reset then erases the very state the submission carried.<br>*why:* usePersonaCore reset on the build-session id; that id is minted at launch, so the persona core cleared at the instant the intent shipped with its directives and the locked build panel then displayed 'unconfigured' for the whole run. Fixed, and proved by removing the guard and watching the new test go red.~~ **LANDED [[2026-09-01-1]] -> client-state/identity-scoped-eviction** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | app-shell | technique | ~~Derive 'the user has configured this' from equality with the default state, never from a latch set on first interaction, because a latch cannot come back down and keeps injecting defaults the user has explicitly cleared.<br>*why:* A latched `configured` flag meant toggling a trait on and off left the badge reading 'Custom core' and still appended a default disposition + model tier into the user's prompt. Replaced with a field-wise comparison against DEFAULT_CORE; seeding the old latch back reproduced the failure exactly.~~ **LANDED [[2026-09-01-1]] -> client-state/persistence-and-migration** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | consolidation | technique | ~~When one item pair can satisfy two conflict classifiers, order them by semantic decisiveness rather than by score — an explicit contradiction signal settles the kind BEFORE any similarity threshold is consulted, because negation is a one-token edit and every similarity measure therefore scores a real contradiction as a near-duplicate.<br>*why:* Measured and fixed in this run. detectConflicts ran its duplicate branch first and short-circuited, so 4 of 4 tested negation families ('always/never', 'enable/disable', 'allow/deny', 'should/should not') were classified as duplicates — and 'duplicate' was the only kind whose UI offered a Merge that concatenates both bodies, i.e. the recommended action on two opposite standing instructions was to fuse them into one memory asserting both. The relationship is structural, not incidental: the sharper the contradiction, the higher its textual similarity, so score-ordered classification is guaranteed to lose exactly the contradictions that matter most. Fail-before/pass-after proven by a new test; reordering the two branches fixed all four cases with no change to any threshold. The subject's consolidation technique covers supersedence, the directional measure and states-vs-events, but says nothing about adjudication ORDER between competing classifiers.~~ **COVERED - prior art already states it; see run note** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | quality-gates | technique | ~~A ratcheting baseline that fails on a DROP as well as a rise makes every conformance fix a two-part change — the fix plus the re-baseline — so any worker who can edit code but not the baseline can only fix violations the ratchet does not count, and the gate quietly redirects improvement away from precisely the conditions it was built to name.<br>*why:* Measured across this round. 20 census rules match this one context; four separate correct fixes were blocked by the ratchet in BOTH directions — adding aria-expanded to a disclosure control and converting a hand-painted modal to BaseModal would each have DROPPED a baseline, while a document keydown listener and a hand-rolled outside-click each RAISED one. The two rises were genuinely useful signals (they pointed at the repo's own AppKeyboardProvider and useClickOutside primitives, and both fixes were re-done correctly through them), but the two drops converted a landable S fix into backlog with no defect involved. The asymmetry is the lesson: a rise is information, a drop is a coordination cost, and a gate that treats them identically taxes the improvement it exists to encourage.~~ **LANDED [[2026-09-01-1]] -> quality-gates/ratchet-design** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | diff-comparison | technique | ~~When a strip of pairwise deltas is built from a bounded fetch window, emit no delta for the window's FIRST element unless the window is provably the whole history — its baseline fell outside the window, so the 'first ever' rule and the 'first fetched' case are indistinguishable at the call site and the fallback always renders the whole set as new.<br>*why:* useRunDiffSummaries caps its fan-out at 12 runs and then applied the first-in-history rule to the first run in the WINDOW, so on every team with more than 12 runs the oldest visible timeline marker asserted a false '+N added, 0 removed'. Fixed and verified this round; diff-honesty covers disclosing truncation on the output but not this poisoned-head case in a sequence of diffs.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: diff-comparison/pair-and-baseline-selection (oldest element of a bounded window has no baseline; render 'no baseline' not first-in-history)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | diff-comparison | technique | ~~A latest-wins stale-response guard must cover the APPEND path of a paged list, not only the replace path: load-more computes its offset from the list it is about to append to, so a concurrent filter refresh turns an append into duplicate rows or rows from the previous filter — a corruption the replace-path guard cannot see.<br>*why:* useTeamMemories guarded nothing at all; adding createLatestWins to refresh() alone would have left onLoadMore able to append a page computed against an offset the list no longer had. Both paths now share one token sequence. computation-offload states request identity for a diff computation but not the pagination-append variant, where the stale request also carries a stale INPUT (the offset), not just a stale answer.~~ **LANDED [[2026-09-01-1]] -> client-state/async-race-guards** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | quality-regression-gating | technique | ~~When a comparison surface aggregates results by a label, verify the label is derived from the compared PARAMETERS and not from the slot's position or creation order — because a positional label makes every ranking a histogram of chance that no downstream significance test can rescue.<br>*why:* This context's strategy leaderboard groups by `strategy_label`, which the client assigns positionally as Alpha/Beta/Gamma/Delta while the genes that constitute the strategy are drawn at random per slot — so 'Alpha wins 43%' means 'slot 0 won 43%'. The corpus's `tested-superiority-claims` covers everything downstream of the comparison (test it, correct the family, print only what the evidence supports) but assumes the grouping key already carries the identity being compared. It does not cover the upstream failure where the key is a coincidence of ordering. The bug is also self-confounding here: on a rematch only slot 0 is seeded from the previous winner, so the positional label the leaderboard ranks is systematically advantaged on exactly the runs the user cares about.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: quality-regression-gating/paired-per-case-testing (grouping key derived from compared parameters, never slot position)** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | multi-project | technique | ~~When a repo's lint config disables no-undef for TypeScript, treat a scoped lint pass as no evidence that an edit compiles — an inserted helper call whose import was not inserted with it produces zero lint findings, so any automated edit that adds a call must assert the type-checker rather than the linter.<br>*why:* An earlier uncommitted round in this exact context added `useRef(0)` and `silentCatch(...)(err)` to CompetitionList.tsx and never added either import. ESLint over all 20 context files reported 0 messages across 20 visited files (verified with -f json, so it was not a no-op run) while `tsc --noEmit` reported two TS2304 errors in that file — and those were 2 of only 3 errors in the entire repository. The failure mode is silent, survives commit, and is invisible to the fast gate a parallel worker is most likely to run because the slow one costs four minutes. I fixed and verified it this round (2 errors -> 0). Filed against `multi-project` (engineering-process/standards-and-gates) as the nearest home for a gate-coverage rule; it is not a natural fit for any existing technique there.~~ **LANDED [[2026-09-01-1]] -> quality-gates/instrument-answers-only-its-own-question (new)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | llm-observability | glyph-encoded-business-thresholds | technique | ~~When a texture or pattern vocabulary exists to encode a categorical distinction WITHOUT colour, and the shapes it fills are themselves rotated per category, counter-rotate the pattern by each element's own rotation — because userSpaceOnUse resolves the tile in the referencing element's rotated user space, so orientation-bearing textures silently collapse into duplicate pairs and the accessibility affordance fails while looking correct.<br>*why:* Measured in this round: eight textures referenced from eight petals at 45-degree increments collapsed to six distinct renderings — vertical-at-45 and forward-diagonal-at-90 both land at 135 degrees, grid-at-180 and cross-hatch-at-225 both land on the same orthogonal grid. Derived geometrically, then fixed with patternTransform and pinned by a test that was red before the change and green after.~~ **LANDED [[2026-09-01-1]] -> data-viz/encoding-vocabulary** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | llm-observability | single-render-layer-many-consumers | technique | ~~When a repo ratchets its violation counts with a baseline that fails on a DROP as well as a rise, an agent working under a no-shared-files constraint must classify every candidate fix by whether it reduces a counted violation, and demote those to backlog — because a correct fix that lowers a baselined count fails the gate exactly like a regression, and the only remedy (re-baselining) lives in the very file the constraint forbids.<br>*why:* Four findings this round were demoted for exactly this reason rather than on their own merits, and the demotion was invisible until I read scripts/census/rules.json instead of running the gate. Not earned by a landed fix, so this is a weaker lead than the first — filed because it changed four routing decisions.~~ **LANDED [[2026-09-01-1]] -> quality-gates/ratchet-design (one clause)** | low | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | - | technique | ~~When reduced motion is enforced through CSS (animation-duration zeroing, scroll-behavior overrides), treat imperative motion as uncovered by construction: a Web Animations API animation and an explicit scrollIntoView({behavior:'smooth'}) argument are both unreachable from a stylesheet, so every module that animates imperatively must read the preference signal itself — and a CSS-only audit will report those surfaces as compliant.<br>*why:* This round found a spotlight that animated and smooth-scrolled for users with both the OS preference AND the app's own Reduce-motion setting on, inside a codebase whose global CSS handles reduced motion thoroughly and whose shared hook exists. The fail-before test proved the CSS override did not reach either motion. The subject already says 'centralize the signal'; what it does not say is that the usual centralization mechanism has two blind spots that look identical to compliance.~~ **COVERED - prior art already states it; see run note** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | - | technique | ~~Distinguish a lookup miss on a TOKEN from a miss on CONTENT: rendering the identifier is an honest degradation for a token (a neighbouring label supplies the meaning) but never for content (a title, a body, the reason a record exists) — there the identifier IS the whole card, so omit the record, instrument the miss, and pair the omission with a real empty state so the surface degrades to honest emptiness rather than a wall of markers.<br>*why:* Landed this round: a roadmap whose per-item title fell back to the literal `[roadmap.<id>]` on both the live and the bundled path, in a file that had the displayability predicate already written but applied only as an all-or-nothing question about the live payload. The repo's own history records the same marker (`[0.0.2.21]`) reaching every user once before. Filtering created a new failure mode — a blank surface — which is why the empty state is part of the rule and not a separate one.~~ **LANDED [[2026-09-01-1]] -> i18n/token-label-separation + catalog-architecture** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | - | technique | ~~When a cache exposes an invalidate entry point, every in-flight request must carry an identity token checked at settle time, because dropping only the cached value leaves the pre-invalidation request both awaitable by the next caller and able to write its stale result back — so the forced refetch serves exactly the data that was invalidated.<br>*why:* Landed and probe-verified in staleWhileRevalidate.ts this round: the fail-before run failed 2 of 4 new tests, including one proving the refetch returned the invalidated value. The same token also fixed a second latent bug the naive fix would have introduced — an unguarded .finally on the disowned request evicting a NEWER request from the in-flight map. Confidence held low because bar 3 is unmet: the governing subject I read this round was motion, not a caching subject, so I cannot claim this is novel against the corpus.~~ **LANDED [[2026-09-01-1]] -> client-fetch-cache/in-flight-dedup + swr-design** | low | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | - | technique | ~~When one hand-written map doubles as the enumeration of a domain vocabulary, gate it with `satisfies Record<DomainUnion, …>` and derive the enumeration from it, because `Object.keys(map) as Domain[]` is an assertion that erases the only signal capable of reporting that the map has fallen behind the union.<br>*why:* Two fixes landed this round on the same shape, each proven by seeding a missing member and watching the compiler name it: the trigger taxonomy (a new TriggerKind silently fell into the 'manual' fallback, in a file whose own header claimed totality for three sibling maps) and ALL_MEMORY_CATEGORIES (the app's memory-category list derived from a colour table and cast into the domain type). Two independent instances in one 17-file context is what raises this above an anecdote; novelty against the corpus is still unverified.~~ **LANDED [[2026-09-01-1]] -> status-vocabulary/vocabulary-chain-integrity** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | attach-detach-lifecycle | technique | ~~When an eviction loop pops its victim only as a side effect of the disposal function, make the queue's own removal unconditional — because a disposer that early-returns on an unknown id never advances the queue head, turning a stale entry into an infinite loop inside a teardown path.<br>*why:* Measured in this run: fleetTerminalManager's `while (parked.length > MAX_PARKED)` relied on disposeTerminal to unpark its victim, and disposeTerminal returns early for an id with no registry entry BEFORE unparking. Reverting the fix hung the test runner past a 120-second wall-clock timeout — a synchronous infinite loop on the UI thread, reached from a plain detach. The technique specifies eviction POLICY (least-recently-viewed, focused-unevictable, eviction-is-a-normal-detach) but says nothing about the eviction loop terminating, and the natural implementation — let the reaper do the removal — is the one that does not.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: terminal-multiplexing/attach-detach-lifecycle (the eviction loop must itself remove the victim; a no-op disposer stops termination)** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | renderer-economics | technique | ~~When you cache the last size pushed to the child to skip no-op resizes, clear that cache on detach — because the next attach may land in a slot with identical dimensions but a child that has been told nothing since, and a suppressed reconciliation there is a permanently mis-sized terminal.<br>*why:* Implementing the technique's own no-op-skip rule in this file produced the trap immediately: a naive lastCols/lastRows cache that survives detach makes the second attach into identically-sized geometry push nothing. The technique states both halves separately ('skip the no-ops' and 'reconciling size is an attach-time step') without noting that the first silently defeats the second unless the cache is attach-scoped. I added a dedicated test ('re-reconciles the size on a fresh attach even into identical geometry') because the obvious implementation passes every other test.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: terminal-multiplexing/renderer-economics (the no-op resize cache is cleared on detach)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | search | technique | ~~When tab panels stay mounted and are hidden with opacity plus pointer-events-none, mark the inactive ones `inert` and `aria-hidden` — because pointer-events-none is a mouse-only guard and keyboard focus and screen readers still walk into the invisible panel's inputs.<br>*why:* Earned on VectorKbModal: the modal keeps every visited tab mounted so state survives switching, hiding inactive panels with opacity-0 + pointer-events-none. Tab from the tab bar landed in the hidden Search input and the hidden schema editor's text fields. Adding inert (plus the WAI-ARIA tabs roles) fixed it; tsc and eslint green. The keep-mounted-for-state pattern is stack-independent and the mouse-only nature of pointer-events-none is a property of CSS, not of this repo.~~ **LANDED [[2026-09-01-1]] -> accessibility/hidden-but-mounted-inertness (new)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | search | technique | ~~When a shared primitive is disabled app-wide, delete it rather than leaving a null-rendering compatibility shim — because callers keep writing `busy ? <Shim/> : <Icon/>`, which compiles, reads correctly, and ships an empty slot, whereas a removed export fails the build at every site that needs revisiting.<br>*why:* Earned on IngestProgressBar: feedback/LoadingSpinner is a deliberate shim that renders null and ignores className, emitting an sr-only status only when passed a `label` no caller passed. Two call sites in this context therefore had no visible indicator AND no screen-reader announcement — the shim delivered neither of the two things it stands in for. Removing them and rendering a real glyph plus role=status/aria-live fixed it. The repo already has a 50-file/68-match census rule chasing this shape after the fact, which is the cost of the shim made visible.~~ **LANDED [[2026-09-01-1]] -> ui-controls/control-inventory-and-discovery (technique only; no shim found in the tree)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | llm-observability | llm-call-telemetry-model | technique | ~~When you introduce a null-aware replacement for a null-blind primitive, ship a gate with it in the same change — because the remaining un-migrated call sites are exactly where the lie survives, and a doc comment naming the anti-pattern enforces nothing.<br>*why:* evalFramework.ts:97-99 documents the `compositeScore(x ?? 0, ...)` pattern as the thing compositeScoreFromRow exists to replace. Every other call site in the repo had migrated; two had not, and they were the ones rendering a per-scenario A/B comparison, where a null dimension scored 0/100 and could flip the declared winner. Landed and verified this round (CompareResultsTable.tsx:87-88). The corpus's nullable-cost-never-zero covers what to STORE; nothing covers the migration's tail.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: llm-call-telemetry-model/nullable-cost-never-zero (ship the ban as a mechanical gate in the same change)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | llm-observability | draft-editing | technique | ~~When a save helper converts a rejection into a returned error field instead of rejecting, it degrades to a silent no-op in every consumer that forgets to render that field — so return a discriminated result the caller cannot ignore, or keep rejecting.<br>*why:* useAppSetting.save() catches, logs and parks the message on `error`; ProviderCredentialField never read it, so a failed write of an API key produced no toast, no message and no visible state change — the user leaves believing the credential is stored. The optional-field shape is what made the omission invisible: the code compiles, lints clean, and looks complete. Fixed and verified this round. The corpus's failure-not-empty-success law states that failure must surface; this names the specific API shape that reliably prevents it.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: error-handling/swallowed-error-prevention (optional error field relocates the swallow to the caller) - subject claimed by a sibling intake run at sweep time** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | llm-observability | single-shape-rule | technique | ~~When a summary tile sits directly above the itemized list of the same thing, pin both to the same COLLECTION, not merely the same predicate — a merged or unified view makes the set the variable while the rule looks identical, so the two paths pass every review and still disagree.<br>*why:* TraceSummary folded trace.spans (backend) while TraceInspector listed cards from unifiedTrace.spans (frontend pipeline spans merged in). Both applied 'a span with an error', so a reviewer of either path saw the correct rule; a run that failed at frontend validation still printed '0 errors' with the error card rendered beneath it. Fixed by computing the set once in the caller and passing the count; the summary now states no rule at all.~~ **LANDED [[2026-09-01-1]] -> trace-rollup-and-attribution/single-shape-rule** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | versioning-snapshots | technique | ~~When a surface seeds a placeholder for data an agent or a later process will supply, the placeholder may describe the SHAPE of the work but must never carry a completed or succeeded state — because a progress rollup computed over it is indistinguishable from real progress, and because an always-seeded placeholder makes the surface's own empty state unreachable, so that state ships unreviewed.<br>*why:* Studio's MOCK_PHASES shipped two of six phases as `done` and one as `active`, with notes describing one specific portfolio site, for every project regardless of its stated vision. The plan button read '2/6' before a single file existed. The second half of the rule is the part that surprised me: because the placeholder was seeded on every creation path, the drawer's careful 'No plan yet — Athena will lay one out as you build' empty state (with its own motion-traced glyph) could never render in practice. I changed the seed to all-pending, pinned it with a test, and proved the guard bites by seeding the old value back.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: demo-data-plane/fake-surface-honesty-contract + async-ui-states/empty-state-design (a seeded placeholder makes the empty state unreachable; assert it renders on one real path)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | multi-project | technique | ~~When a host page receives postMessage reports from frames it embeds, authenticate the reporting frame by contentWindow identity, never by a self-declared field in the payload — the window message event fires for every frame on the page including anything the embedded document itself embeds, so a `source: 'our-agent'` marker is a string any nested frame can copy.<br>*why:* Studio's preview handler gated on `d.source === 'athena-agent'` for both of its message kinds. One branch happened to resolve the reporting frame by contentWindow (it needed the tab id) and was therefore accidentally safe; the sibling branch did not, and would move a global UI element to a rectangle supplied by any nested frame in a user-authored dev site. The same guard was one line away in the same handler and had simply not been generalised — which is what makes this a rule worth stating rather than a bug worth fixing.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: embedded-preview/origin-validation (match event.source against the frame's own contentWindow; origin alone cannot separate guest from what it embeds)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | settings | technique | ~~When a client coalesces reads from many independent callers into one bulk request, it must enforce the backend's batch ceiling itself by chunking, because the per-call-site bound that made the ceiling unreachable ceases to exist the moment an aggregator sits in front of it — and the overflow fails the whole batch, so unrelated readers all resolve to 'absent' at once.<br>*why:* Measured this run: the settings coalescer aggregated every same-tick read into one get_app_settings_bulk with no size awareness, while the backend hard-rejects >256 keys with a Validation error, and the coalescer's documented rejection fan-out then failed every waiter. A fail-before test with 296 keys reproduced it; chunking at the ceiling fixed it and made a single bad chunk fail only its own waiters.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: settings/read-batching (the coalescer owns the backend's batch ceiling; chunk so one oversized tick fails only its own waiters)** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | client-fetch-cache | technique | ~~An effect's dependency list is the identity of the session it starts, so caller-supplied callbacks must never appear in it — latch them in refs and key only on the values that name the session; if the effect drives a poll whose result sets state, a callback in the deps makes the restart self-sustaining and the loop runs at network speed instead of at the configured interval.<br>*why:* Measured this run: a polling hook depended on nine caller callbacks; one consumer was constructed with an inline arrow, so every render restarted the effect — clearing the timer, resetting the backoff and the consecutive-failure counter (making the session-lost path unreachable), and re-polling immediately, while the poll's own onLines state write triggered the next render. A fail-before test showed 6 polls where 1 was due.~~ **LANDED [[2026-09-01-1]] -> client-state/effect-identity-and-latched-callbacks (new)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | sql-console | technique | ~~When one confirm-then-execute guard serves several statements authored on the same surface, bind the pending statement to its execution target when it is stashed (or drop it when the target changes), because a guard keyed only on the connection cannot see the target swap and will report the confirmed statement's result against the wrong author.<br>*why:* Landed and verified in ChatTab: the shared safe-mode hook's drift guard compares the runQuery identity, which is bound to credentialId alone, while the message the result is written to lives in a ref that every Run press re-points. Stash a mutation on message A, run a read on B, confirm: A executed and A's rows appeared under B. The guard was working exactly as designed and still could not see it.~~ **LANDED [[2026-09-01-1]] -> sql-console/safe-mode-guarding** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | sql-console | application | ~~A result surface that can be re-run in place must clear the previous result when the new run fails, because an error rendered above stale rows reads as that error's output and is indistinguishable from a partial success.<br>*why:* Landed and verified in ChatTab with a fail-before probe: the catch set the error field and left the result field, so AssistantSqlBlock painted the previous run's grid directly under a fresh engine error. result-fidelity names the zero-rows/error pair but not the stale-success/error pair, which only exists once a statement is re-runnable in place.~~ **LANDED [[2026-09-01-1]] -> sql-console/result-fidelity** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | none | technique | ~~When a locale catalogue holds a key whose source-language value appears verbatim as a string literal in the code, treat it as a wiring bug rather than a duplicate, because it means the key was minted and translated for that exact site and never referenced — a state no coverage gate can see, since the catalogues are complete and the literal is syntactically valid.<br>*why:* Found and fixed three of them in one 244-line file: db.query_generated, db.query_failed and db.cancelled exist in en.json and all 13 locales (spot-checked cs/ja/ar) and are referenced nowhere in 4,900 files, while ChatTab rendered the identical English literals. Both of this repo's i18n gates were green throughout: the strict coverage check compares catalogues to each other, and the hardcoded-JSX lint rule does not see a string in an object-property position. The detection is a cheap join of catalogue values against source literals.~~ **LANDED [[2026-09-01-1]] -> i18n/completeness-gates + string-extraction-enforcement** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | import-normalization | technique | ~~When a patch/overlay document merges into a canonical one, an EMPTY collection in the patch must mean 'not mentioned', never 'delete' — because `[].every(p)` is vacuously true, so the wholesale-replace branch guarded by it erases exactly the structure the patch format promised it could not touch.<br>*why:* Measured and fixed in this round: a strings-only translation overlay writing "use_cases": [] replaced the canonical list with [], defeating the module's own stated contract that structural fields stay single-sourced in the canonical file — and defeating it AFTER the checksum that protects the canonical had already passed, so no integrity layer could see it. Fail-before proven by removing the guard; the test goes red.~~ **LANDED [[2026-09-01-1]] -> import-normalization/overlay-merge-absence-semantics (new)** | medium | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | intermediate-representation | technique | ~~When an adapter OVERWRITES the shared pipeline's output after the pipeline has run, it silently opts out of every guarantee the pipeline enforces — so either the override runs before the waist, or the override must re-apply them explicitly.<br>*why:* Earned in this round: sanitizing at the waist covered three of four adapters, and the GitHub Actions adapter then rewrote identity, instructions, summary and full_prompt_markdown from raw source fields — reintroducing the exact prompt-injection path the waist had just closed, in the one adapter that looked most carefully written. The intermediate-representation technique says adapters lower INTO the IR and says nothing about adapters mutating it afterwards, which is how the hole stayed invisible.~~ **LANDED [[2026-09-01-1]] -> import-normalization/intermediate-representation** | high | personas / scan-sweep@wave |
| ~~2026-08-27~~ | software-engineering | adapter-capability-tables | technique | ~~When a build-time static analyzer forces one hand-written copy of an enumeration that has a single source of truth elsewhere (a bundler glob, a macro list), derive every other copy from the source and add a source-text assertion over the unavoidable one — because a missing entry there fails OPEN: the feature silently never loads, with no runtime error to notice.<br>*why:* Earned in this round: three copies of the locale list existed (a regex, a Vite glob, and a test that re-typed the same 13 codes), and the manifest they should all derive from asserts in its own header that 'no other file references a locale by code'. Two copies were replaced with derivations; the glob cannot be derived because Vite analyses it statically, so a test now reads the source text and fails when a manifest locale has no pattern. Fail-before proven by deleting one glob line.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: codegen/drift-gating (re-homed from adapter-capability-tables: an underivable copy gets a source-text assertion because it fails open)** | medium | personas / scan-sweep@wave |
| ~~2026-08-29~~ | software-engineering | agent-cli-transport | technique | ~~When a spawned agent CLI is fed its prompt over stdin, attach an 'error' listener to the child's stdin stream: a stream failure is an EVENT, not a throw at the call site and not something child.on('error') reports - that one covers the SPAWN, not the pipe - and an unhandled 'error' event is re-raised by Node as an uncaughtException that ends the host process instead of the call.<br>*why:* Measured 2026-08-29 on Windows, where the spawn must go through shell:true because the binary is a .cmd shim. With the binary absent, cmd starts, exits, and the 2MB prompt lands on a closed pipe: 'Error: write EOF', uncaught, process gone. The transport had a carefully written not-installed verdict and a router above it whose whole promise is a fallback ladder - and neither could ever run, because nothing survived long enough to classify anything. The absence of the binary is exactly the case the ladder exists for, so this defect is invisible on every machine that has it installed and total on every machine that does not.~~ **COVERED - prior art already states it; see run note** | high | gravity / scan-sweep@2.0.0 |
| ~~2026-08-29~~ | software-engineering | quality-gates | technique | ~~A gate that detects compliance by MATCHING THE NAME of a chokepoint must enumerate every door into that chokepoint and assert each name still resolves at its source - a chokepoint grows siblings (a variant wrapper for a surface the first one does not fit), and a single-name matcher then reports compliant code as violating, which is the failure that kills a gate rather than the one that embarrasses it.<br>*why:* Deriving the POPULATION from the filesystem - the fix this repo already applied to the same probe - did not save it. lib/apiAuth.ts grew two more doors after the matcher was written (an access-check without the money rate bucket, for a surface that legitimately bursts; and the raw verdict, for a route serving an <img> that cannot carry an Authorization header). Nine committed, genuinely gated routes read as UNGATED and the blocking suite was red on main. A gate red for a false reason stops refusing anything: everyone learns to step over the failure, and the next actually-ungated route lands inside it. The repair is a door TABLE plus an assertion that each door is still exported - so a renamed door reddens the gate loudly instead of silently reclassifying the codebase.~~ **LANDED [[2026-09-01-1]] -> quality-gates/chokepoint-tag-registry** | medium | gravity / scan-sweep@2.0.0 |
| ~~2026-08-29~~ | software-engineering | structured-output | technique | ~~When a model is asked for a value from a CLOSED vocabulary and the schema is not natively enforced, a value outside the set is UNMEASURED, not a mismatch: drop the field from both sides of the score rather than crediting it zero - because the same number almost always gates a retry loop or a selection, so scoring unknown as wrong spends real money chasing a field nobody read.<br>*why:* Measured on a style-extraction engine. `styleScore` compared a vision readback against a target with `===` and scored anything else 0; a readback agreeing on every field but drifting on ONE scored 0.900 instead of 1.000 over the nine weights actually readable. That number is compared against the loop's target (0.85) and is what picks the recipe a style ships with, so the drift bought another generation round AND could change the winning recipe. The same codebase already refused off-vocabulary values at the synthesis door and already returned null rather than zero for a missing readback - the collapse survived only in the one function whose output was load-bearing, which is the pattern: the honest handling gets written where it is cheap and forgotten where it decides.~~ **COVERED - prior art already states it; see run note** | medium | gravity / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | concurrency-guards | technique | ~~Write-temp-then-rename is only half of an atomic write on Windows: os.replace/MoveFileEx raises PermissionError while any other handle to the DESTINATION is open, so on a file something polls - the exact case the pattern is reached for - the bare rename converts a torn read into a failed write. Retry the rename briefly and re-raise if it will not settle.<br>*why:* Measured on a 30 KB JSON index that a page polls and a CLI appends to. The plain write lost 38 of 50 reads to unparseable content. Swapping it for write-temp-then-rename took torn reads to zero and lost 32 of 40 WRITES - a different defect wearing the fix's clothes, and one that would have shipped as an improvement on the strength of the pattern's reputation. With a short retry: 0 torn reads, 0 failed writes, and the residue is a PermissionError the READER sees (never ENOENT, so never a silently empty document). The sibling module in the same directory had carried the bare rename with a comment explaining that the page polls the file, so the exposure was already live in the path whose failure aborts an hours-long GPU run.~~ **LANDED [[2026-09-01-1]] -> concurrency-guards/atomic-file-publish (new)** | high | gravity / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | quality-gates | technique | ~~A test runner's own defaults are part of the gate, and the focus primitive (.only / fit / fdescribe) is the one that turns a green suite into a green lie: it does not fail, it SHRINKS - so audit the runner config for it before writing another probe, because no test can catch it (under .only, the test that would have caught it is one of the tests that does not run).<br>*why:* Measured on a repo whose probe lane is a blocking CI job and a pre-push hook, and which already carried a probe named gate-vacuous-pass opening with 'a check that examined nothing must not count as enforcement'. Playwright's forbidOnly defaults to false and neither config set it; adding .only to one case took `npm test` from 317 passed to '1 passed', exit 0, green everywhere - the 316 probes guarding money routes, auth, SSRF and the identity wipe silently did not run. The team had internalised gate-liveness thoroughly enough to write a probe about a vacuous pass inside one function, and the same hole sat in the runner that ran it, one config line away, for the whole suite.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: test-harness/suite-partitioning near-NOVEL focus-primitive-lockout (reconcile executed vs collected case count; forbid-focus flag in gate config) - subject claimed by a sibling intake run** | high | gravity / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | media-generation | production-pipeline-phasing | technique | ~~When a long run's JOB record is persisted and its RESULT record is not, the job outlives the screen as a standing claim about results that no longer exist - so either give the result the job's lifetime or make the surface reconcile the two out loud. The reload correction ('interrupted') fires only on a job still RUNNING, so a job that already SETTLED is never corrected, and the settled one is therefore the only one that can lie.<br>*why:* Measured on a follow-up research queue whose record was deliberately lifted above the component tree so navigation could not lose it, beside a job store persisted to localStorage. The subject's own technique already says 'the job record is the truth, and it outlives the screen', and warns about the inverse failure - a run living only in component state, whose result 'lands nowhere'. This is that failure turned around and it is the one that ships: after a reload the bell read 'done - Results are staged against the notebook' and the queue it pointed at was empty, with nothing on either side accounting for the difference. The module's own header asserted the case was already covered because lib/jobs writes 'interrupted' over a reload - true only of a RUNNING job, and a returned follow-up settles itself 'done' in the same tick it writes its results. A comment stating the guarantee was what stopped anyone looking.~~ **LANDED [[2026-09-01-1]] -> production-pipeline-phasing/long-run-as-background-job** | high | gravity / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | credential-vault | technique | ~~When a verdict is widened from a boolean to a named state, retire the boolean in the same change -- a back-compat boolean derived from the new enum IS the collapse the widening was meant to prevent, and every surface still reading it silently re-manufactures the old two-state answer.<br>*why:* Personas widened its credential healthcheck to HealthProbeState{Verified,Unverifiable,Failed} and kept success = (state != Failed) for back-compat. Unknown therefore reads TRUE. The badge renderer migrated to the enum; the result panel next to it kept branching on success and painted an unverifiable probe emerald with a check mark -- the exact green claim the third state was introduced to stop. Neither the type checker nor the census rule written for this defect class saw it, because both were watching the type boundary while the collapse happened at the render.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: credential-vault/health-probing (a compatibility boolean derived from the widened state is itself a narrowing; retire it; audit renders)** | medium | personas / scan-sweep@2.7.1 |
| ~~2026-08-29~~ | software-engineering | agent-chaining | technique | ~~When a server writes a typed non-continuation (a stop reason plus a machine code), assert that a CLIENT renders it, because a stop-reason ledger no human surface displays is indistinguishable from having no ledger at all - and the docs will keep claiming it works, since the route half genuinely does.<br>*why:* kp's agent-refresh route built a four-branch typed reply (refreshed:false + reason + AGENT_BRIDGE_KEY_INVALID code), pinned it with route tests, and documented that the operator sees it. The only UI calling that route awaited the fetch and read nothing, so a dead 24h pairing key, an undispatched agent, an unchanged status and a real transition all rendered as the same non-event. Route tests, the doc and the code constant were all green; the ledger reached nobody.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: agent-chaining/stop-reason-ledgers (assert a client surface renders the reason)** | medium | kp / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | metric-forecasting | technique | ~~When a projection SUBSTITUTES a measured rate for one derived leg of a chain, assert the leg structurally exists (a distinct intermediate step, not merely an in-range index) before substituting, because index arithmetic that only avoids underflow will silently address the chain's own start and collapse the substitution to the measured rate alone.<br>*why:* kp's hire forecast rebuilt its offer-to-hire leg as (offerReached/firstReached) x observedAccept, taking the offer stage as funnel[length-2] under a length>=2 guard. On a two-column board (a legal saved axis: entry + terminal) that row IS the entry row, so the ratio is 1 and the projection became velocity x weeks x acceptRate - 72 projected hires where the real 10% conversion gives 12. The signal-floor gate (hasSignal) did not catch it: the cohort was real, only the leg was fictional.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: metric-forecasting NOVEL derived-leg-structural-validity (a range guard is not an existence guard for a distinct intermediate stage) - one repo** | medium | kp / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | client-fetch-cache | technique | ~~A TTL is not a bound: when expiry is only evaluated on read, cap the store and reclaim expired entries before evicting live ones - and never mark an absent optional key field with a printable sentinel when that field arrives from a URL, because a caller can simply send the sentinel and key onto the unfiltered entry.<br>*why:* kp's server-side memo checked expiresAt on get() and never deleted, so entries keyed on raw ?candidate / ?roleFamily params (open vocabularies, on a long-lived self-hosted process where maxDuration does not apply) were retained one payload each forever. The same builders marked 'no filter' with '*', so ?candidate=* computed the filtered result (empty) and stored it under the full-list key, serving an empty decision-records list for the TTL. Both fixes are in the shared core; the joiner the module already used (NUL) made the absent-marker unforgeable.~~ **LANDED [[2026-09-01-1]] -> client-fetch-cache/cache-key-discipline** | medium | kp / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | design-tokens | technique | ~~A theme-parity gate must assert that a themed surface RESOLVES its colour at render time, not that a literal equals its light-theme token: a lockstep check makes a hardcoded palette look compliant while it ships one theme's drawing into the other.<br>*why:* kp's diagram renderer imported the JS colour mirror precisely so its literals would fall under the repo's lockstep gate, and the gate passed - but those constants are the light half by construction (the mirror exports a separate DARK object nothing forked on), so every architecture diagram painted a near-white box onto the dark canvas, in the shapes AND in the legend explaining them. The tell was a stroke byte-identical to the coral token. Fixed by painting var(--color-*) from the SVG presentation attributes, which an in-DOM SVG resolves per theme for free.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: design-tokens/cross-language-token-parity (mirror-equality gate is vacuous for theme-varying values; assert render-time resolution)** | high | kp / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | measurement-honesty | technique | ~~A metric's SAMPLE must be the population its statistic was computed over, not the population it was selected from - and the publishable/certifiable gate must read that same number, because the two diverge silently wherever the statistic drops rows the selector kept.<br>*why:* kp's metric pack sampled time-to-hire with the hire count (9) while the median was computed only over hires carrying both timestamps and a non-negative duration (5). The floor is 8, so the buyer-facing artifact printed 'over 9 hires', status:measured and certifiable:true for a figure resting on five observations. The producer had already been fixed to return the true count and pinned by a test; nothing consumed it, so the divergence was visible in the payload and still published. The fix had to reach the basis PROSE too - a basis naming a bigger population than the statistic is the same claim one sentence further down.~~ **COVERED - prior art already states it; see run note** | high | kp / explorer@2.0.0 |
| ~~2026-08-29~~ | software-engineering | agent-memory | subject | ~~When building an owner-style agent (not a task executor), give it a responsibilities layer distinct from mechanics - standing obligations, decision rules, refusal rules, escalation rules, what good looks like and how it is measured for the thing it owns - because the llm-agent bundle covers memory, chaining, fleet and transport and nothing says what a responsible owner of a codebase or a hiring pipeline actually does, which is the layer between LLM training data (generic) and a project instruction file (specific).<br>*why:* kp/personas App-master program, 31 sweeps: integrity and mandate obedience solved, product sense absent - the agent never chose its own work because nothing told it what an owner is obliged to attend to. Filed as a lead, not authored: the first subject (codebase owner) should be extracted FROM the P1-P3 evidence in the W8-pre program, per the registry doctrine that knowledge is extracted from operated experience.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: NEW SUBJECT candidate agent-role-charters under llm-agent/prompt-and-context - extract from operated evidence, not authored from one repo** | medium | kp / scan-sweep@2.7.1 (develop round, brainstorm) |
| ~~2026-08-29~~ | software-engineering | draft-editing | technique | ~~When a form's captured fields are summarized into an LLM prompt, any field whose default is also a legal user choice will assert that choice on every record nobody touched - so absent-versus-default stops being a persistence concern and becomes a grounding bug.<br>*why:* A BANT capture panel defaulted disposition to 'warm' and its prompt helper reported it unconditionally, so every AI reply generated on an unrated lead was told the lead was lukewarm. The other three fields had an 'unknown' member and skipped it correctly; disposition had none to skip, and the defect was invisible in the UI because a pre-selected pill looks like a choice. field-defaults-and-bounds names the absent-versus-default distinction for storage and clamping; it does not say that a prompt-feeding summary is the place the distinction gets LOUD, or that a default which is a real option is the shape to hunt for.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: draft-editing/field-defaults-and-bounds (a default that is also a legal choice must not be reported to a model as a choice)** | medium | systedo-case / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | client-state | technique | ~~When a module-scoped cache is invalidated to reflect a state change, push the fresh value to registered subscribers rather than only clearing it, because a hook that fetches once on mount makes "the next subscriber" the next NAVIGATION, not the next render - and a long-lived shell that keeps its panels mounted never navigates.<br>*why:* Landed in systedo-case: invalidateAiStatus() cleared its cache after every AI generation so the remaining-budget banner would refresh, but AiAssistant keeps all six tool panels and the one banner mounted across tab switches, so no subscriber ever refetched and the count sat frozen at the page-load snapshot until the user hit a 429. The nearest technique (singleton-lifecycle) covers duplicate instances and refcounted teardown, not the mount-time-only read.~~ **COVERED - prior art already states it; see run note** | medium | systedo-case / scan-sweep@2.7.1 |
| ~~2026-08-29~~ | software-engineering | data-access | technique | ~~When a read SORTS and then SLICES to a cap, an ordering difference between two drivers stops being a presentation difference and becomes a different RESULT SET - so a cap makes an explicit, same-direction tiebreak mandatory, not merely nice.<br>*why:* cross-driver-invariant-parity names total ordering as an invariant one engine states and the other does not, but treats it as an ordering property. Measured here: three capped reads (twin audit archive, AI response-cache eviction, cron health probe) sorted on a timestamp alone. Firestore's implicit __name__ ASC and a stable Array#sort broke ties one way, sqlite's explicit `id DESC` the other, and because both then sliced to a cap the two drivers RETAINED AND EVICTED DIFFERENT RECORDS - including audit records. Fixed in 90a1e7bf by adding explicit id tiebreaks; the ordering-only framing would not have flagged these as invariant violations.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: data-access/cross-driver-invariant-parity (sort-then-slice: a tie decides membership, not layout; same-direction tiebreak on both drivers)** | high | systedo-case / architect@1.5.0 |
| ~~2026-08-29~~ | software-engineering | demo-data-plane | technique | ~~one-interface-many-planes says a plane-identity method stays OUT of the interface; for products whose catastrophic failure is presenting fabricated data as the user's own, invert it - provenance should be a REQUIRED field of every result envelope, and the coupling that creates is the price of the guarantee.<br>*why:* This repo (adtech analytics) deliberately puts plane identity in everywhere - ResolvedDataset.live, meta.demo, PublishResult.simulated, ModulePage sample - and the standard scores that as ~42 consumer-side breaches. The failure the rule prevents (consumers coupled to a plane) is cheaper here than the one the inversion prevents. Evidence that it is not theoretical: 16 catalog consumers call the source-BLIND loader, and the repo's own 2026-08-29 L2 run measured a real tenant advised to write an article about 'Ukazkova sluzba A' - the seed catalog's placeholder service name - fed to the model as fact. NOT earned from a fix I landed; raised from a scan plus that recorded incident.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: demo-data-plane/one-interface-many-planes (scoped inversion: required branchable provenance where fabricated-as-own is the catastrophic failure) - weakest lead, one repo** | medium | systedo-case / architect@1.5.0 |
| ~~2026-08-29~~ | software-engineering | uninstrumented-degradation | technique | ~~When the document carries stamps but the hovered element carries none, say so as its OWN state - the three-state table (disarmed / armed-instrumented / armed-uninstrumented) collapses it into 'armed, instrumented', where it renders identically to 'nothing under the cursor'.<br>*why:* ascent's DevInspector implemented all three documented states correctly and still had the failure the technique exists to prevent: hovering a portal or an unstamped module showed 'Hover a component...', the same string as an idle overlay. A fourth state and one line of copy fixed it (502654f9).~~ **COVERED - prior art already states it; see run note** | high | ascent / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | call-site-vs-implementation-resolution | technique | ~~When the library/product classifier is an allowlist of repo paths, assert in a test that every entry still resolves on disk - a moved shared root silently reclassifies it as product code and every existing assertion stays green.<br>*why:* ascent's LIBRARY_ROOTS carried a comment saying it MUST track layout, and its suite re-typed the same four prefixes rather than reading the list; moving one root in a probe left the suite green until a filesystem guard was added (aa68cbf4).~~ **LANDED [[2026-09-01-1]] -> quality-gates/chokepoint-tag-registry** | medium | ascent / scan-sweep@2.7.0 |
| ~~2026-08-29~~ | software-engineering | data-retention | technique | ~~When a destructive operation has more than one disposition, the receipt must report the one that ran and count every disposition's rows - not just the destructive counter - because the default-safe disposition leaves that counter at zero.<br>*why:* ascent's erasure UI read auditDeleted alone while includeAudit:true resolved to redact, so redacting an entire audit trail rendered as '0 rows - audit trail kept'; confirm-by-echo's 'report what was actually destroyed' reads as satisfied by that code. Fixed by reading the response's disposition and summing deleted+redacted, sharing the wording with the preview.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: data-retention/confirm-by-echo + erasure-requests (receipt names the disposition that ran and counts every disposition)** | medium | ascent / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | browser-credential-boundary | technique | ~~Enumerate a client-boundary type guard by what a client IMPORTS, never by which module path the import came through - an audit scoped to the barrel misses every deep-path import, which is where the richest types usually live.<br>*why:* ascent's compile-time wire-safe-date guard listed the 15 client imports from @/lib/db and missed 7 reached via @/lib/db/scans etc., including the entire report trend/diff surface - a third of the real set, unenforced. Widened the audit and the list; all 7 passed, so the gap was in the guard, not the code.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: codebase-scanning/the-tree-is-not-the-population (re-homed: enumerate a boundary guard's population by what clients import, not the barrel path)** | medium | ascent / explorer@1.2.0 |
| ~~2026-08-29~~ | software-engineering | authorization | technique | ~~A visibility rule enforced on reads must be enforced on writes in the same terms, and the write refusal should reuse the read's status code - a rule applied to reads alone is a display rule, not an access rule.<br>*why:* ascent scoped private org-memory rows on every read path (list, candidates, get-by-id) and on no write; the update was keyed on id alone, so a member who got a 404 READING a colleague's private memory could still overwrite it or flip it to shared. The asymmetry survived review because each layer looked correct alone. Fixed by applying the read's own author test in the write gate, answering 404 rather than 403 so the write path does not disclose existence either.~~ **LANDED [[2026-09-01-1]] -> authorization/read-write-predicate-symmetry (new)** | high | ascent / explorer@1.3.0 |
| ~~2026-08-29~~ | software-engineering | client-state | technique | ~~A debounce and a cancellation answer different questions - the timer stops you issuing a request per keystroke, an abort stops a request you no longer want from landing - so a debounced fetch still needs the abort, or the slower of two in-flight reads wins the setState.<br>*why:* two ascent list hooks debounced the timer and fired unguarded fetches; changing a filter twice let the older read overwrite the newer one's rows and its setLoading(false) stop the live spinner. The correct pattern existed three times in the same codebase (including twelve lines below in the same hook), which is evidence the debounce reads as sufficient to authors who know better.~~ **COVERED - prior art already states it; see run note** | high | ascent / explorer@1.3.0 |
| ~~2026-08-29~~ | software-engineering | repo-manifest-standard | technique | ~~When you ship a GENERATED artifact that executes in a consumer's environment, your own green build is not evidence about it - pin the generated content against the producing repo's own equivalent decision (its runtime pin, its lint config, its command table) so the two cannot drift.<br>*why:* ascent pinned Node 24 for itself in .nvmrc, package.json engines and both its workflows while generating node-version: 20 - EOL since April 2026 - into every adopting repo from three separate sites, and a 7,900-test suite stayed green throughout because none of it runs in the consumer's CI. The same blind spot produced a <placeholder> provenance the generated doctor then skipped in silence. Both fixed by deriving the generated value from the producer's own declaration (34691f4d, 0d04f126).~~ **LANDED [[2026-09-01-1]] -> repo-manifest-standard/generated-from-provenance** | high | ascent / scan-sweep@2.7.1 |
| ~~2026-08-29~~ | software-engineering | repo-manifest-standard | technique | ~~When the standard ships a reference reader, never let it be the only reader in the tests: its parsing model is a SUBSET of the contract, so it is structurally blind to exactly the portability defects the contract exists to prevent.<br>*why:* ascent's manifest serializer emitted YAML-ambiguous plain scalars (a repo named `on` produced `name: on`, which a YAML 1.1 parser reads as the boolean true). The suite round-trips the serializer against the shipped zero-dependency doctor field by field - and the doctor treats every value as text, so the one reader in the kit was the one reader that could never detect it. The subject already promises 'an arbitrary tool must be able to read it'; what is missing is that the promise needs a second reader in the test bed to be checkable (562c60e3).~~ **LANDED [[2026-09-01-1]] -> repo-manifest-standard/spec-ships-with-artifact** | medium | ascent / scan-sweep@2.7.1 |
| ~~2026-08-29~~ | software-engineering | anchor-contracts | technique | ~~When a contract is split across two authorities that ship in the same repo - a constant declaring identifiers and the elements that must carry them - the comment asserting they cannot drift is the tell that nothing checks. Grep the declared set against the source set; the gap is usually exactly one.<br>*why:* ascent's GETTING_STARTED_ANCHORS says 'declared HERE and stamped onto their elements by the UI lane - one shared constant so the model and the DOM can't drift', and 5 of 6 were stamped. The sixth pointed at nothing for as long as it had existed, and the only test asserted step.anchor === ANCHORS[step.id] - the constant against itself, green either way. The technique already asks for a generated manifest plus a gate; what is worth adding is the DIAGNOSTIC: a prose guarantee of non-drift, in a repo where both halves are visible, is a reliable marker for an unenforced contract, and the check costs one grep (bc18bbcd).~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: guided-tours/anchor-contracts (diff declared vs source set; a constant asserted against itself is vacuous)** | medium | ascent / scan-sweep@2.7.1 |
| ~~2026-08-30~~ | media-generation | image-prompt-composition | application | ~~When a declared duotone reads back as monochrome or complementary-split, rewrite the palette clause as ground/figure/accent roles with a named forbidden second colour, because a listed palette is re-cast per generation while an assignment holds.<br>*why:* Flux 2 on 3 scenes x 2 seeds: chokepoint pick_rate 0.33, Gemini challenger 4/6, human APPROVED (better); reflected as PALETTE_AS_ROLES in lib/foundry/extract/prompts.ts @ f33275c~~ **COVERED - prior art already states it; see run note** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-30~~ | media-generation | visual-style-locking | technique | ~~When rejected proofs repeat a black_handling/palette miss, stating that boundary as text-channel exclusion clauses does NOT recover the look on Flux 2 — the human judged the exclusion arm not-better despite Gemini picking it 4/6.<br>*why:* Cycle 2026-08-30-oil-blacks-boundary: qwen readback flat (crushed 11/11 both arms), chokepoint pick_rate 0.17, Gemini 4/6 challenger, human REJECTED (not-better). rejections-as-negative-evidence's 'negative guidance travels in the text channel' rule needs a measured limit: a whole-image tonal property (lifted blacks) may not be recoverable by exclusion at all.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: visual-style-locking/rejections-as-negative-evidence (whole-image tonal property may not be recoverable by exclusion text; one cycle)** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-30~~ | software-engineering | app-shell | technique | ~~When a client app lazy-loads routes, make the route registry a single `satisfies Record<AllSections, LazyRoute>` map so an un-lazy addition is a typecheck failure, because convention-level lazy discipline dies by one static import - here a single static overlay import silently made 890KB of vendor JS eager while every documented route stayed lazy<br>*why:* 2026-08-30 architect run measured the eager closure: sectionRouter typed registry held for 10 routes/19 overlays, the one surface added outside it (App.tsx static LiveChannelOverlay import) dragged markdown+chart vendors eager; fix + codification landed (commits d27d7d885, c49344551)~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: app-shell/lazy-section-loading (exhaustively typed route registry as the enforcement of the lazy prohibition)** | high | personas / architect@1 |
| ~~2026-08-31~~ | media-generation | cinematic-language | application | ~~When a shot compiler renders camera angle as mechanics ('from a low angle looking up'), rewrite the phrase table as the stance's meaning plus its headroom consequence, because the model's null sentence is eye-level-centred-with-headroom and only attitude language moves it.<br>*why:* camera-attitude: pick_rate 0.50, Gemini 4/4, human APPROVED; reflected in shotPrompt.ts ANGLE_PHRASE @ 93e688d~~ **COVERED - prior art already states it; see run note** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | cinematic-language | application | ~~When a generation prompt carries no lighting clause, add one named in-world source, its hardness, its direction on the subject, and what stays dark, because Flux 2 lights evenly unless told what not to light.<br>*why:* what-stays-dark: 8/8 unanimous both judges, human APPROVED; reflected as FRAMES-SCENE-PROMPT.md rule 7 + three SUBJECT_RECIPEs @ 93e688d~~ **COVERED - prior art already states it; see run note** | high | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | cinematic-language | application | ~~Describe optical intents as visible effects (compression, looming wide, shallow/deep focus) in plain words, never notation - EXCEPT long-lens compression, which did not land from words on Flux 2 in either arm.<br>*why:* lens-as-effect: pick_rate 0.75, Gemini 3/4, human APPROVED with the compression tie preserved as a caveat; rule 8 @ 93e688d~~ **COVERED - prior art already states it; see run note** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | cinematic-language | application | ~~Replace a genre adjective with the contract's layer rows - light-source honesty, camera intentionality, lens habit, grade, and an explicit imperfection budget - because the unstated imperfection budget is what makes a register arrive sterile on Flux 2.<br>*why:* genre-as-contract: pick_rate 0.75, Gemini 4/4, human APPROVED; rule 9 @ 93e688d~~ **COVERED - prior art already states it; see run note** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | cinematic-language | application | ~~Write performance as three to five observable prop-anchored beats, never a category verb, because the verb samples the training mean and the counted beats can be checked against the output.<br>*why:* counted-beats: 8/8 unanimous both judges, human APPROVED; rule 10 @ 93e688d~~ **COVERED - prior art already states it; see run note** | high | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | character-identity-continuity | technique | ~~Cropping a character reference to the head alone buys nothing when the reference already conditions only an early denoise window (0.35 on Flux 2) - the windowing itself sheds the reference's pose, so reference-shows-only-invariants may be mechanism-specific to full-strength conditioning.<br>*why:* invariants-only: pick_rate 0.25, Gemini 0/4, two ties, human REJECTED (not-better).~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: character-identity-continuity/reference-shows-only-invariants (early-window conditioning sheds pose; single-model negative)** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | visual-style-locking | technique | ~~Machine judges unanimously preferred a text-only style block read back from one sample over the sample-as-reference arm, and the human still rejected the improvement - the block carried palette and posterization but lost something the human's eye required; style-onboarding-from-sample's readback step needs a fidelity clause the grammar does not yet name.<br>*why:* sample-to-block: 8/8 unanimous challenger both judges, human REJECTED - the maximum score-vs-eye disagreement measured so far.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: visual-style-locking/style-onboarding-from-sample (readback loses a fidelity component machine judges cannot score)** | high | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | image-prompt-composition | technique | ~~At ~1.3k characters a tail-appended checkable clause landed identically to a front-placed one on Flux 2 - prompt-budget-limits does not bite at this prompt length; position discipline is unearned overhead below the model's obeyed window.<br>*why:* clause-position: all pairs tie or split, Gemini 4/4 tie, human REJECTED (null confirmed).~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: image-prompt-composition/style-first-token-ordering (measured null on tail placement at ~1.3k chars, long-window model)** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | image-prompt-composition | application | ~~Control arm: reversing style-first ordering (action before style) cost style adherence on 3 of 4 pairs, with readbacks drifting photoreal-cg to stylised-realistic - style-first-token-ordering has current seed-matched evidence on Flux 2.<br>*why:* order-control: challenger pick_rate 0.00, human REJECTED the reversed arm = law confirmed.~~ **COVERED - prior art already states it; see run note** | high | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | frame-direction | technique | ~~A-tier cut rhythm jumps two or more shot-size ladder rungs between neighbouring sampled frames 31-87% of the time (159 frames, six sources) - a shot-list generator that steps the ladder gently produces a rhythm no A-tier cut exhibits; vary-from-your-neighbours needs a measured jump quota.<br>*why:* corpus study 2026-08-31, dojo_study.py adjacency stats; sequencing-layer, not A/B-able as stills - filed as evidence for the beat/shot planner.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: frame-direction/motif-establish-vary-payoff (measured shot-size jump quota; partially contradicts camera-position-semantics)** | medium | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | media-generation | visual-style-locking | application | ~~In one A-tier animated feature trailer the signature complementary pair is magenta-cyan (8 of 21 warm-cool frames) and texture SPLITS within one production (glossy-cg 33 / digital-clean 20 / painterly 10 of 63) - a per-world style key inside one project, which the one-style-block-per-project law does not model.<br>*why:* corpus study 2026-08-31 (Across the Spider-Verse official trailer, local qwen annotation).~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: visual-style-locking/draft-proofing-locked-ratchet (per-world style key inside one project; low confidence, application first)** | low | gravity / dojo@1.0.0 |
| ~~2026-08-31~~ | software-engineering | companion-identity | technique | ~~when a pipeline stamps a generated character/self document, the stamp must be write-only-if-absent - any rebuild that re-stamps unconditionally overwrites the operator's authored identity behind a green build<br>*why:* personas' promote/adopt raw-UPDATEd core_profile unconditionally; the living-agent rebase had to convert both sites to seed-if-absent or the new Core editor's writes would die on the next rebuild~~ **COVERED - prior art already states it; see run note** | high | personas / spark@1.1.0 |
| ~~2026-08-31~~ | software-engineering | companion-runtime | technique | ~~when an autonomous cycle's refusals are frequent and identical, dedupe the durable refusal record to one per reason-kind per period while narrating every tick in logs - a refusal ledger that grows per tick trains the operator to ignore it<br>*why:* the persona attention loop at a 300s tick with a 30-min floor would write ~240 identical interval_floor rows/day/persona; deduped to one per reason-kind per UTC day with per-tick tracing, accepted on measured evidence~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: companion-runtime/autonomous-cycle-hosting + signal-economy-contract (one durable refusal row per reason-kind per period; ticks in traces)** | medium | personas / spark@1.1.0 |
| ~~2026-08-31~~ | software-engineering | quality-gates | technique | ~~when a file's tail hosts a feature-gated test module, tests appended at file end silently join it and never run under the default feature set - assert a new test RAN (count it), never just that the suite is green<br>*why:* WP4's memory-door tests landed inside an ml-gated vec_tests tail module and executed zero times while every gate read green; caught only because the builder counted its own test runs~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: test-harness/negative-control-tests (assert a new test appears in the executed set; a feature-gated module passes by never existing) - subject claimed by a sibling intake run** | high | personas / spark@1.1.0 |
| ~~2026-09-01~~ | media-generation | cinematic-language | application | ~~When a shot compiler's placement vocabulary is {crosshair, thirds}, add 'diagonal' (the line of action corner to corner) as a director-choosable value, because it is the most common extreme-wide action composition in A-tier work (10/31 EW frames across six sources) and a vocabulary that cannot say it never produces it.<br>*why:* study-diagonal: pick_rate 0.67, Gemini 3/3, agreement 1.0, composition readback moved 0/3->2/3 diagonal, human APPROVED; reflected @ 1dfce8f~~ **LANDED [[2026-09-01-1]] -> cinematic-language/camera-position-semantics** | high | gravity / dojo@1.0.0 |
| ~~2026-09-01~~ | media-generation | cinematic-language | application | ~~In wide shots, one dominant light source plus ONE named environmental layer (a dusk sky state, distant practicals) separates depth planes without lifting the darks - a measured refinement of single-source lighting doctrine (89% of A-tier animated frames name >=2 in-world sources).<br>*why:* study-light-layers: pick_rate 0.67, Gemini 3/3, agreement 0.67, human APPROVED; reflected into rule 7 @ 1dfce8f~~ **LANDED [[2026-09-01-1]] -> cinematic-language/lighting-as-dramatic-instrument** | medium | gravity / dojo@1.0.0 |
| ~~2026-09-01~~ | media-generation | generative-provider-routing | technique | ~~When sizing a LOCAL vendor row for MoE checkpoints, gate on NVMe bandwidth and RAM, not VRAM: expert-streaming engines (Colibri-class) run 321B-with-vision on a 25 GB-RAM floor, and the decode floor is bytes-touched-per-token divided by disk bandwidth (4.8 GB/token for GLM-5.3-Flash - approximately 24 s/token at 200 MB/s, approximately 1-3 s/token on PCIe-4 NVMe with a warm RAM cache).<br>*why:* Colibri v1.10.1 docs, measured reference numbers; local experiment on Wolf in progress - see librarian/sources/2026-09-01-colibri-glm53-experiment.md for the pre-registered protocol and decision rule.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: generative-provider-routing/capability-to-vendor-plan (MoE local row gated on NVMe bandwidth + host RAM) - experiment in flight, land the axis not the numbers** | medium | gravity / dojo@1.0.0 |
| ~~2026-09-01~~ | media-generation | generative-provider-routing | technique | ~~When comparing local MoE engines, classify by WHERE the expert tier ends: RAM-resident engines (FreeToken-class: offload/hybrid backends, PCIe-streamed misses, order-of-magnitude faster decode) cap at what host RAM holds, while NVMe-streaming engines (Colibri-class) extend the tier to disk and cap at disk bandwidth - on a 64GB-RAM box the two reach entirely different model classes (~120B vs 321B+), and 'runs 290B+ locally' claims are meaningless without the RAM assumption stated.<br>*why:* FreeToken docs/models.md (RAM-resident backends; 'Multimodal checkpoints are served text-only') vs Colibri glm53-flash.md (25GB RAM floor, 4.8GB/token disk touch); full comparison in librarian/sources/2026-09-01-colibri-glm53-experiment.md.~~ **BANKED [[2026-09-01-1]] - triaged EXTENDS/NOVEL with prior art checked; amendment: generative-provider-routing/vendor-fact-ledger (merge with L15 as local-tier-memory-hierarchy-sizing)** | medium | gravity / dojo@1.0.0 |
| 2026-08-30 | media-generation | review-iteration-loops | technique | when a human review gate must sync across machines, put the verdict ledger and one kept thumbnail per approval in git-tracked paths and leave bulk media machine-local, because git is the only channel both machines already share and the ledger outlives the images<br>*why:* dojo gating contract landed and behavior-verified (22 assertions + observed UI smoke): destructive commit, tracked thumbs, reflected:false work queue | medium | gravity / spark@repo |
| 2026-08-30 | software-engineering | - | technique | When a UI mode default is computed from live decision state, freeze it when the surface opens, because acting inside a mode must never change which mode the user is standing in.<br>*why:* Driven bug in the candidates duel: adopting a card created the record the default read, flipping guided->expert and unmounting the surface under the click; fixed by latching the default at open (render-adjustment form, keyed useState initializer in the sibling step). | high | gravity / spark@card-decision-engine |
| 2026-09-01 | llm-observability | deterministic-span-derived-ids | technique | When an idempotent ingest resolves a primary-key collision by reading the stored row back, a failure of that read must be reported as a retryable internal error, never as a payload conflict, because a conflict verdict tells the client to mint a new id for an event that is already recorded and double-counts spend.<br>*why:* LightTrack batch ingest .ok().flatten()-ed the read-back and reported store failures as `conflict: different payload`; fixed in e26d6cb and verified with the api suite (217 tests). The single-event door already propagated the error - the two doors had drifted on exactly the retry semantics the technique exists to protect. | medium | tracklight / explorer@2.0.0 |
| 2026-09-01 | software-engineering | - | technique | When a write API accepts a boolean control (enabled, locked, frozen), grep for its readers before trusting it: a flag with a setter, a column and a display but no enforcement site is a label, and the fix is to route it through the same cache/path the feature's other per-row policy already uses.<br>*why:* LightTrack's Project.enabled was accepted by PUT /v1/projects/:id, persisted by all three stores and shown in listings, and read by nothing on the ingest or auth path; a disabled project's keys kept ingesting. Fixed in aa1b2aa by widening the existing per-project redaction-policy cache into a project-policy cache and checking it on both ingest doors; the grep that found it (`\.enabled` across the API crate) took one call. | medium | tracklight / explorer@2.0.0 |
| 2026-09-01 | llm-observability | incremental-window-accounting | technique | When an incremental usage cache folds rows inside a transaction, a rollback must reset the cache (or restore its insertion cursor), because a store that reuses insertion ids after rollback will hand the freed ids to the next events and the advanced cursor makes them invisible - an under-count, not the over-count the naive reading expects.<br>*why:* LightTrack SQLite batch admission: `events.id` is TEXT so the implicit rowid is max+1 and a rolled-back batch frees it; the cache's `seen_rowid` stayed past it. A test drove the rollback, proved the rowid reuse, and read $9 from the cache where the full-scan reference read $12. Fixed in ba63d35 by resetting the cache on commit failure. The technique text covers eviction and exactness but says nothing about the fold running inside a transaction that can fail. | high | tracklight / explorer@2.0.0 |
| 2026-09-01 | recruiting | pipeline-aging-and-attention-triage | application | When a board's stage axis is workspace-editable, key the per-stage aging default by stage ROLE and make the threshold resolver take the axis as a parameter, because a name-keyed default table silently degrades every workspace-authored column to the flat fallback cut while the shipped columns keep firing and nothing on screen admits the change.<br>*why:* kp 0f1d0a59: STAGE_SLA_DEFAULTS was keyed by the shipped five names; on a composed axis (the repo's own test fixture carries an 'Interview 2' custom column) every added column aged on the 10-day legacy cut and the SLA editor listed columns the board did not render. Replaced with ROLE_SLA_DEFAULTS + slaForStage(stage, overrides, axis); 4 new tests pin role resolution, override precedence, byte-identity on the shipped axis and the retired-id fallback; 4331/4333 unit, typecheck/lint/design/docs clean. | high | kp / friend@1.1.0 |
| ~~2026-09-01~~ | software-engineering | quality-gates | technique | ~~When a config format lets a LATER declaration REPLACE an earlier one for the same rule (eslint flat config, and any last-writer-wins layering), a rule can be fully present in the config text and apply to nothing - so a liveness guard must read the RESOLVED effective config for a representative file per scope, never the config source, because a source-grep guard passes for exactly as long as the shadowing holds.<br>*why:* kp's eslint.config.mjs declared four design-token no-restricted-syntax selectors in its first block; a later block matching a strict superset of those files set the same rule with only its own selectors, so flat config replaced them and `npm run lint` had not seen a hardcoded color under app/ since. The config DOCUMENTS this exact trap beside another selector group ('Spread them into both') and every other group was restated correctly - a known, written-down hazard, still committed. Two documents pointed at each other about which gate held the law and neither did. Proved by probe rather than reasoned: a file carrying both a hex literal and an await-in-transaction reported only the transaction violations. Fixed, and guarded by a test that calls ESLint#calculateConfigForFile for eleven representative files and asserts each selector group is present in scope and absent in its exemptions - verified non-vacuous by re-introducing the shadowing (3 of 12 fail, naming the missing group and the block to spread it into). The distinguishing detail: the population of hex violations was ZERO the whole time the gate was dead, so no symptom, no finding count, no drift - the only observable was the resolved config.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | kp / architect@1.5.0 |
| 2026-09-01 | software-engineering | job-coordination | technique | When a route wraps several store calls in one SQLite transaction, every one of them must run on the connection the transaction was opened on; a module-private connection is invisible to it and, under WAL, a write that follows another connection's commit fails with SQLITE_BUSY_SNAPSHOT which busy_timeout never retries.<br>*why:* kp POST /api/jobs/[id]/publish: gate (main handle read) -> setJobStatus (private handle commit) -> debit (main handle write) threw SQLITE_BUSY_SNAPSHOT on every genuine go-live, rolling back the debit while the flip had already committed; reproduced with a real-DB test, fixed by routing job-ingest.ts through ensureDb() (c2749fbc). | high | kp / explorer@2.0.0 |
| 2026-09-01 | recruiting | requisition-lifecycle-governance | application | A derived matchable record that is re-synced from edited prose must carry provenance-fixed fields (a grounded pay band) across the re-parse explicitly, or the second ingest silently honors the hand-typed override the first one refused.<br>*why:* kp PATCH /api/jds/[slug] re-parsed the markdown and insertJob's upsert wrote salary_min/max from the parse, replacing the analysis band with the recruiter's typed figure or the taxonomy phantom; fixed with one shared helper used by both ingests (65a254fd). | medium | kp / explorer@2.0.0 |
| 2026-09-01 | software-engineering | concurrency-guards | technique | When a compare-and-swap lives inside an embedded single-file datastore's transaction, the transaction's ISOLATION MODE is part of the guard: a DEFERRED transaction takes a read lock at the SELECT and upgrades only at the first write, so two writers can both pass the same comparison inside that gap - the CAS must either take the write lock at BEGIN (IMMEDIATE) or re-assert its comparand in the UPDATE's WHERE with a zero-rows-changed skip.<br>*why:* kp's JD editor CAS (updateJd/revertJd, app/_lib/db/jobs.ts) ran a bare better-sqlite3 tx() while its own doc comment claimed 'the read+compare+write run in ONE synchronous transaction, so a stale base is detected'. It does not follow: single-threaded JS makes the body uninterruptible WITHIN a process, and that is exactly why the gap is invisible - the second writer is another connection (a second app process, a CLI, a WAL writer), which the in-process reasoning never considers. Fixed to .immediate() and pinned behaviourally plus at the source, because a single-process test cannot produce the second connection - which is itself the reason the defect survived a test suite. The repo already had the sibling shape correct one file away, so this is a knowledge gap about isolation modes, not about CAS. | high | kp / explorer@2.0.0 |
| ~~2026-09-01~~ | software-engineering | test-harness | technique | ~~A test that asserts over a file's SOURCE TEXT makes that file's comments part of its input, so an ordering or absence assertion can be broken by prose alone - such a test must pin a marker that cannot occur in commentary (the call including its opening brace or an `await` prefix, never the bare identifier), and the code it guards should say so where a future editor will read it.<br>*why:* kp pins each rate-limited route by asserting the limiter appears BEFORE a marker for the expensive call. Editing a stale comment in two intake routes to name the pinned markers put those strings above the limiter and failed the ordering assertion for both - the documented trap catching the note about itself. Two earlier entries in the same spec had already hit it from the other direction (the bare identifier matching the route's import line), which is the second sighting that makes it a rule rather than an anecdote.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | kp / explorer@2.0.0 |
| 2026-09-01 | recruiting | candidate-communication-integrity | technique | When a candidate-facing link rides inside a letter, pin the link locale where the letter locale is resolved (inside the dispatcher), never at the link builder, because the builder cannot see the letter locale and re-deriving it is a second source that drifts.<br>*why:* kp offer links were built bare at two call sites while the letter was composed in entry.locale inside comms-dispatch; the pin only became single-sourced by moving it beside candidateLocale() via a pure pinLinkLocale helper (04ec795b). | medium | kp / perfect@2.4.0 |
| 2026-09-01 | software-engineering | - | subject | When a public capability-link route mutates or discloses (erasure, invite redemption, offer view that lapses on read), throttle it per token AND client BEFORE the token lookup and pin the call site in a contract test, because a leaked link is otherwise a one-shot destructive open endpoint and no software-engineering subject currently governs open-route rate limiting.<br>*why:* kp had 5 sibling token routes throttled and 3 unthrottled (one irreversible POST); the repo law only named routes that spend money or spawn a subprocess, and the registry index has no rate-limiting/abuse-containment subject to cite (596a2d46). | medium | kp / perfect@2.4.0 |
| ~~2026-09-01~~ | software-engineering | design-tokens | technique | ~~When a lint gate is expressed as options on a shared rule (eslint no-restricted-syntax) in flat config, every later block that matches the same files REPLACES those options, so a design-law selector must be named once and restated in each overriding block, and its liveness must be probed with a fixture that is supposed to fail - not inferred from a clean run.<br>*why:* kp: the raw-hex/rgba design-law rule had been silently off for the entire app/features + app/_components layer since later import-law blocks were added; a bare bg-[#d6d3d1] linted clean while npm run lint and design:check both reported OK. Re-armed via DESIGN_LAW_SELECTORS restated in both blocks (92d228e7); the -vs-Tailwind-underscore anchor gap was the smaller of the two defects.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | kp / perfect@2.4.0 |
| 2026-09-01 | software-engineering | webhook-ingestion | technique | When an inbound receiver keeps a clock for ACCEPTED work, add a second one for CONTACT - stamped after the token/auth check and before any shape validation - because without it "the sender never called" and "the sender is calling and every payload is rejected" render as the same silence, and they need opposite fixes.<br>*why:* kp/agent-workforce: the hired-agent report route recorded nothing unless a report parsed, so an operator staring at a silent roster could not tell a mis-pointed bridge from a schema drift. The sibling channels receiver already stamped a receipt in exactly that position; copying the position (not the storage) fixed it in one UPDATE. Two traps the fix had to respect: the stamp must NOT move for an unknown or retired token, or the 404 stops being indistinguishable and becomes a token oracle; and it must not be folded into the OUTBOUND liveness field, which answers the opposite direction and would trade one blind spot for a false statement. | high | kp / perfect@2.4.0 |
| 2026-09-01 | software-engineering | mcp-tools | technique | When an MCP server declares readOnlyHint on a tool, pin the claim to behaviour with a stub-transport probe that drives every read-only handler against a recording client and refuses any POST to a route not allow-listed with a reason (and static-check that route's source for write primitives), because a host may relax consent on the hint and a write wearing read-only is the failure that auto-approves.<br>*why:* pof-mcp shipped 49 annotations this way; fault-injecting one write tool to read-only failed the guard on its exact POST, and reading the routes found one description-identical write (saveRun) among eight pure POSTs. | medium | pof / research@1.8.0 |
| 2026-09-01 | software-engineering | mcp-tools | technique | When a stdio tool server cannot rely on tools/listChanged, ship progressive discovery as an OPT-IN static group gate plus an always-visible discovery tool, not a trimmed default, because an unattended agent cannot re-list its way back to a capability the host hid.<br>*why:* Gating 49 tools on pof-mcp: the sprawl standard says load what the task plausibly needs, but its worked examples assume an attended host that can activate a group mid-session. MCP clients honour listChanged unevenly, so a core-only default would have silently removed capability from an autonomous harness run with no recovery path. Landed the inverse - all groups on unless POF_MCP_TOOL_GROUPS trims them, a pof_tool_groups meta-tool that is never gated and lists every hidden group WITH its tool names, and refusal-at-the-door naming the group so listed-is-callable holds in both directions. | medium | pof / research@1.8.0 |
| 2026-09-01 | software-engineering | - | technique | When a module owns a corrupt-vs-missing distinction for a persisted file, grep every other reader of that file name for a raw parse-with-fallback, because an API edge that JSON.parses the same file and returns the fallback on catch silently re-collapses corrupt into empty.<br>*why:* pof harness: state-io.ts made game-plan/progress corruption fatal in the loop; GET /api/harness?action=progress read progress.json by hand and mapped a truncated file to [] (fixed a53c933f, test reproduces the truncated write). | medium | pof / scan-sweep@2.8.0 |
| 2026-09-01 | game-production | unattended-build-loop | technique | When a loop repairs a failure and then re-runs the failing check to confirm the repair, the confirmation must run under the SAME limits as the gate that produced the failure - same output budget, same timeout, same judging rule - because a confirmation that is stricter than its gate converts a successful repair into a fresh failure and spends the next retry on a measurement artefact.<br>*why:* attemptSelfHeal re-ran the failing gate's own command through exec with Node's 1MB default maxBuffer while the verifier runs that identical command at 10MB; exec kills the child and returns ENOBUFS on overflow, so a chatty-but-passing tsc/UBT re-run reported 'verify command still failing after the fix session'. Fixed and seeded: only the loud-success case goes red when the budget is reverted. | high | pof / scan-sweep@2.8.0 |
| ~~2026-09-01~~ | software-engineering | test-harness | technique | ~~When a test asserts that a rule now holds UNIFORMLY across a closed set of kinds, derive the case population from the type that defines the set (a Record over the union, which fails to compile when a member is added) rather than from a hand-written list - because the member the author forgot to list is the member where the rule is still broken, and the hand-written list makes that absence read as coverage.<br>*why:* gate-honesty.test.ts opened 'no gate self-certifies ... it is now uniform' and enumerated 6 of the 10 members of VerificationGate['type']; the visual branch - one of the 4 omitted - still answered passed:true when it had nowhere to write captures, and had done so through every prior green run of that file. Replacing the list with Record<VerificationGate['type'], ...> put the broken member inside the population and it failed immediately.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | pof / scan-sweep@2.8.0 |
| 2026-09-01 | software-engineering | analytics-time-windows | technique | When counting 'opened since <start>' over entities that are re-materialized on every snapshot with identity carried across (a scan re-creating every recommendation row), do not read the row's createdAt - diff the identity set of the latest snapshot against the last snapshot strictly before start, seed an EMPTY baseline for every entity that has one, and report 'no pre-window snapshot' as unmeasurable rather than 0.<br>*why:* ascent's Recommendation rows are recreated per scan with status carried by (dimId,title); createdAt would have reported every gap on every rescanned repo as opened this week. The identity diff landed in src/lib/db/org-followups-week.ts (fdffc34f); its first draft built the baseline map lazily from rows and silently dropped a repo whose pre-window scan found zero gaps - the test caught it, and the fix (seed a Set per measurable repo) is the half of the rule cohort-matched-comparison does not state. | medium | ascent / spark@1.2.0 |
| 2026-09-02 | media-generation | image-prompt-composition | technique | When deciding whether a reference-governance rule can be measured on a given stack, ask what the RUNTIME does with the reference, not what the prompt says about it: an img2img denoise-window runner (Flux 2 via ComfyUI) admits the reference as a latent and has no labelled attachment channel, so reference-role-map/s ordering rule (the role declaration must LEAD the prompt, never trail it) has nothing to order there and is untestable on the local /usr/bin/bash lane; it is a claim about attachment-carrying multimodal stacks (Nano Banana, Gemini) and can only be closed on a billed one.<br>*why:* dojo cycle 2026-09-01-emblem-slot, phase 2/3: gravitone records a LIVE deviation on reference-role-map (lib/imaging/providers/google.ts:188-192 attaches a subject image plus up to 13 style references with no role declaration; on the generate path the label trails the prompt) but the deviation cannot be trained on the stack the cycle was pinned to. Not a verdict - no pairs were judged; the cycle failed on GPU contention. | medium | gravity / dojo@1.0.0 |
| 2026-09-03 | media-generation | generated-output-grading | technique | Keep two readback instruments per eye and never conflate them: a closed-enum schema for GRADING (comparable, arithmetic-safe, flattening by design) and a taught free-prose deep read for SEEING (roles, shapes, layers) - and when comparing models as eyes, hold the prompt constant, because a 321B-vs-27B class gap can be pure elicitation (measured: a taught prompt closed it at 25s/$0 on the 27B).<br>*why:* gravitone 2026-09-03 three-frame A/B (taught-eye-results.json) against GLM-5.3-Flash's free-prose readback; prompt preserved as pipeline/foundry/DEEP-READ-PROMPT.md. | high | gravity / dojo@1.0.0 |
| 2026-09-03 | llm-observability | breach-alerting-and-attribution | technique | when an alert carries a delta a counter has already discarded (a periodic flush of rejected calls), its dedup key must include the flush instant, because a cooldown that suppresses the second flush deletes the only copy of that delta<br>*why:* lighttrack: ingest-rejected flushes at 900s under a 3600s cooldown lost 3 of every 4 deltas (store answered Suppressed, no row); fixed by keying on the flush timestamp, pinned by a store-backed test | medium | tracklight / scan-sweep@2.8.0 |
| 2026-09-03 | software-engineering | agent-cli-transport | subject | a multi-tenant HTTP API's credential boundary is its own subject: bearer-key minting (prefix + salted digest, shown once), constant-time compare via fixed-width digests, failed-attempt throttling keyed on a trusted source identity, key capabilities narrower than the tenant, expiry as a distinct code from a wrong guess, a tenant kill switch enforced at the credential, and a scope carried on every store read so a foreign id is not-found rather than forbidden<br>*why:* lighttrack: crates/api/src/{auth,guards,auth_scopes,auth_throttle}.rs implement all of these and the registry joined the context to agent-cli-transport / client-state (homonyms); three rounds of findings here (enforced-without-admin-key lockout, per-request last_used write, unpinned error-code contract) had no technique to be judged against | medium | tracklight / scan-sweep@2.8.0 |
| 2026-09-03 | llm-observability | multi-provider-event-normalization | technique | a normalized token-usage record must state whether cached input is a SUBSET of input or ADDITIONAL to it, and every per-provider extractor must convert to that one convention - OpenAI's prompt_tokens includes cached tokens, Anthropic's input_tokens excludes cache_read_input_tokens - or the price book bills the same cache hit two different ways<br>*why:* lighttrack: PriceBook::cost_usd_mode computes billable_input = input - cached (subset convention); the Python, Rust and TS Anthropic extractors pass input_tokens through unchanged, so an Anthropic call with input_tokens=1000 and cache_read=800 bills 200 uncached instead of 1000 - the wire type's doc says only 'provider-dependent' | high | tracklight / scan-sweep@2.8.0 |
| ~~2026-09-04~~ | software-engineering | quality-gates | technique | ~~When a gate hunts literal strings in a built artifact, assert each string is present in the source file it names (comments stripped) before asserting its absence in the output, because a reworded literal silently narrows the gate to nothing and its clean verdict is manufactured.<br>*why:* first run of the assertion in check-bundle.mjs found 1 of 10 fingerprints ('[text] ') that had existed only in a comment since 2026-08-27 - the joined literal is '[text]' - so that leg had matched nothing in any bundle; the same assertion also exposed a fingerprint split across two template-literal lines~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | gravity / scan-sweep@3.0.0 |
| 2026-09-04 | software-engineering | companion-identity | application | When a self-model document gains a second, operator-only author, refuse the agent's diffs at BOTH the propose and the apply door, not just at apply, because a proposal that can never be applied still reaches a human inbox and reads as a legitimate request.<br>*why:* Shipped a two-author manifest (operator law sections + agent self-model sections) and found that guarding only the apply door let refused-by-design proposals accumulate in the review queue; guarding propose as well made the refusal legible at the moment it was earned. | medium | personas / spark@1.1.0 |
| 2026-09-04 | software-engineering | agent-memory | technique | A memory dashboard must distinguish an unmeasured value from a measured zero at every tile, and the distinction has to survive into the type: model the count as nullable rather than defaulting a lookup miss to 0.<br>*why:* The coverage tile's whole purpose is showing what the brain holds nothing about; a `counts.get(id) ?? 0` rendered never-measured charters as a confident 0 and a repo lint caught it. Making the field nullable forced every consumer to state which case it was drawing. | high | personas / spark@1.1.0 |
| 2026-09-04 | software-engineering | - | technique | When a spring/follow-value hook is fed a plain number instead of a motion value, push every new target through .set() in an effect, because the library reads a number once at attach time and a re-render with a new number never moves the animation.<br>*why:* framer-motion 12 useSpring(number) in a polled status gauge: motion-dom attachFollow subscribes only to MotionValue sources, so both the arc and the counter stayed at the first poll's value for the life of the view; a render test went red on the old code and green after spring.set() (commit 7c45a4f2c). | high | personas / scan-sweep@3.0.0 |
| 2026-09-04 | game-production | catalog-pipeline-authoring | technique | When a UI offers a re-run as a fix, derive whether the re-run CAN change anything from the producer's own code (an arity probe of the produce body, with an explicit override where arity lies) rather than from a declared flag, because a flag on hundreds of specs is a claim about the body that rots out of step with it.<br>*why:* PoF wave 26 (cb0b2f09): 344 of 344 registered produce bodies ignored the direction, so the headline Produce fix was a byte-identical no-op on ~303 steps; the arity probe + shrink-only linter ratchet withheld the button honestly and needed no per-spec authoring. | medium | pof / perfect@2.5.0 |
| 2026-09-04 | game-production | catalog-pipeline-authoring | technique | A producer stamp on an artifact must be written by the party that can PROVE the producer ran: let a client declare only engines it can assert about itself (deterministic code), refuse remote-engine claims with a 400, and preserve a claimed engine only when the server already recorded that same engine on that exact row, because a client-writable stamp is the fabricated-pass hole reopened one field over.<br>*why:* PoF wave 26 (7c7b7872): 817/817 artifacts had no recorded engine; the one-shot route now stamps Claude{model,effort} from the same object it hands to the dispatcher, the artifacts route refuses top-level Claude via a zod enum and downgrades a smuggled data._provenance claim to unknown unless the row already attests it (the lab write-through re-POSTs what the server persisted). | medium | pof / perfect@2.5.0 |
| ~~2026-09-04~~ | software-engineering | quality-gates | technique | ~~When a structural gate detects a convention by looking for a symbol in source TEXT, strip comments and string literals before matching, because in a codebase whose house style is to explain each rule beside the code that obeys it, the gate's own documentation satisfies the matcher - so the gate is most reliably blind exactly where the convention is best documented.<br>*why:* ascent's src/app/api/org/id-routes-gated.test.ts enforces 'an [id] route authorizes against the row's org' with src.includes(gateName) over raw file text. Deleting the import AND both requireOrgAccess/requireOrgRole call sites from goals/[id]/route.ts, leaving only the three COMMENT lines that name them, kept the suite at 21/21 green: a route with authorization entirely removed read as compliant. Fixed by stripping comments/strings first; the same seeded route then failed. The corpus already names seeding a violation (gate-liveness, vacuous-by-evaluation) and rule false-POSITIVES (rule-precision-discipline), but not this direction - the artifact that documents the rule being the artifact that defeats its detector, which is a false negative that scales with documentation quality.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / scan-sweep@2.7.1 |
| 2026-09-04 | software-engineering | session-continuation | technique | When a child is spawned through a shell (shell:true, needed for .cmd shims on Windows), cancel must kill the process TREE (taskkill /PID /T /F on win32; detached spawn + signal to the negative pid on POSIX), because child.kill() only reaches the shell and the real worker survives — and the cancel path must record confirmed vs unconfirmed termination rather than assume it.<br>*why:* ascent c8de0a5f: measured on a Windows host — a real grandchild behind a shell:true parent survived child.kill() and died to taskkill /T /F; before the fix a stopped loop run freed its slot while the claude process kept spending tokens for up to the full agent timeout. | high | ascent / perfect@2.5.0 |
| 2026-09-05 | software-engineering | termination-and-reaping | technique | When a child is spawned through `shell: true` on Windows, a timeout must kill the tree from the LIVE shell pid (`taskkill /pid <shell> /T /F`) before any plain signal, because `child.kill()` ends cmd.exe alone, the grandchild keeps running and holds the inherited pipes so `close` never fires, and once the shell is gone the tree can no longer be walked from it.<br>*why:* Measured 2026-09-05 with the engine's exact spawn shape: after child.kill() shell dead / grandchild alive; taskkill /T on the live shell pid ended both in under a second; taskkill on the dead shell pid found nothing. Landed as lib/claudeCli.ts#killTree with a real-process probe and a negative control. | high | gravity / scan-sweep@3.0.1 |
| 2026-09-05 | software-engineering | availability-probe | technique | When a shell resolves the command name for you, detect a missing binary from the shell's own stderr sentence ('is not recognized as an internal or external command', 'command not found'), not from the documented exit code, because cmd.exe under Node's shell spawn exits 1 rather than the 9009 an interactive prompt reports, and a classifier written from the docs is green against the wrong number while the real case stays misclassified.<br>*why:* Measured 2026-09-05: spawn('definitely-not-a-binary', {shell:true}) -> code 1, stderr the sentence. The first cut of the fix keyed on 9009 and would have missed every real Windows case; the transport probe flipped from `failed` to `not-installed` only once the sentence was the primary signal. | high | gravity / scan-sweep@3.0.1 |
| ~~2026-09-05~~ | software-engineering | accessibility | technique | ~~When an aria-live region is driven by framework state (React setState), put the clear and the write in separate tasks, because two state writes in one task batch into one commit, the clear never reaches the DOM, and an utterance whose text equals the previous one produces no mutation and is never voiced.<br>*why:* Found in a queue that deliberately wrote '' then text 'so a repeat is not swallowed' - the sink was React state and the repeat was exactly what was swallowed (two plates with one name removed in a row voice once). Fixed by scheduling the write one tick after the clear; the queue probe pins the two-tick contract.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | gravity / scan-sweep@3.0.1 |
| ~~2026-09-05~~ | software-engineering | quality-gates | technique | ~~When a repo's gate suite covers one language and its build contains another, write that second language's FIRST check to a stdlib-only constraint, because a check that needs a dependency install is input-nondeterministic and can therefore only ever be advisory -- which is the reason it stays unwritten.<br>*why:* gravitone-gcloud's CI installs Node and nothing else; both test lanes are TypeScript, and 965 lines of Python that run for hours on a GPU and then write a verdict were executed by no gate at all. Every gate was green and every gate was honest -- gate-liveness does not cover this, because the gate never existed to die. A 200-line stdlib-only selftest (verified green with PIL, numpy, requests, httpx, cv2 and torch blocked at import) found 3 real defects on its first run, two of them falsifying the README's two headline promises; being stdlib-only is what lets a CI job run it with no pip step and still block under this repo's own blocking-by-input-determinism doctrine.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | gravity / scan-sweep@3.0 |
| ~~2026-09-05~~ | software-engineering | quality-gates | technique | ~~When a string needs a control character as a field separator, write it as an escape sequence rather than a raw byte, because one raw NUL makes grep report the whole file as binary and git render every diff of it as Bin, so the file drops out of code search and review while still compiling.<br>*why:* useHealthCheck.ts carried two raw 0x00 bytes in a template literal; grep -n over the file returned zero lines for any pattern and four commits touching it showed as Bin in git stat; escapes restored 507 addressable lines with identical hashes (pinned by test)~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | personas / scan-sweep@3.0.1 |
| 2026-09-05 | software-engineering | draft-editing | application | When the durable write of a draft is an async IPC call (desktop webview → native process), write a synchronous browser-storage shadow of the edited fields BEFORE scheduling the debounced durable write and clear it only when the saved payload equals current state, because the platform closing hook cannot await IPC and a last-gasp drain there completes zero writes.<br>*why:* Personas notepad (Tauri 2): the 500 ms debounced SQLite save via invoke cannot flush on beforeunload/pagehide; a sync localStorage shadow keyed per note, reconciled at load when newer than the row, is what makes kill-mid-typing lossless. Covered by 8 store tests incl. shadow-newer-than-row restore and failed-save-keeps-dirty. | medium | personas / spark@1.3.2 |
| 2026-09-05 | software-engineering | draft-editing | technique | When an undo entry's restore persists through I/O, it must reject on failure even after reporting it to the user, because the history store decides success from the promise and a swallowed failure moves the entry to redo and loses it.<br>*why:* personas-desktop useEditorSave: restore toasted and returned; EditorDocument.undo() filed the entry as undone, canUndo went false, disk kept the value; fixed by rethrow, hook test red-then-green | medium | personas / scan-sweep@3.0.1 |
| 2026-09-05 | software-engineering | health-checks | technique | When a health scorer spends a FIXED budget per subject (100 points less per-issue penalties), never reuse it as the rollup by flattening every subject's issues into one call - the composite then measures population size rather than health and saturates at the floor; aggregate by combining per-subject SCORES (mean / worst-of / counts), because health-rollup names which function to choose but not that the member scorer is the wrong function to reuse.<br>*why:* Personas' weekly agent-health digest flattened every agent's issues into computeHealthScore, whose budget is per agent. Measured: 25 agents each carrying one info-level 'no telemetry yet' note scored 50/unhealthy; four errors anywhere in the fleet scored 0. Any real install was told grade unhealthy every week by construction. Switching to the mean of per-agent scores gave 98/healthy for the same fleet and made the number independent of fleet size (5 tests, red-then-green, commit 540c21caf). | high | personas / scan-sweep@3.0 |
| 2026-09-05 | software-engineering | i18n-and-localization | technique | When a gate decides 'is this value translated' by comparing it to the source string, corrupted data DEFEATS it in the passing direction - a mangled English value differs from the source byte-for-byte and is scored as translated. Pair every compare-to-source gate with an integrity gate on the value itself, because the failure is silent and grows: the more damaged the catalog, the greener the report.<br>*why:* Personas' check-untranslated passed 27 English strings in bn/ko/cs for as long as an escaping cascade had mangled them. Repairing 176 backslash-corrupted values across 12 locales made all 27 visible at once - the repair APPEARED to introduce 27 regressions it had only exposed. Closed with a self-testing integrity gate (scripts/i18n/check-escapes.mjs) proven to bite by seeding a violation (commits c947e3cb7, f8daeaf79). | high | personas / scan-sweep@3.0 |
| 2026-09-05 | game-production | catalog-pipeline-authoring | application | Before authoring a provenance label across hundreds of items, measure how often the existing heuristic DECIDES the displayed value and how often it agrees with an audit where both exist; if the heuristic decides almost nothing, the campaign is unwarranted and the honest fix is to author only where the code reads unambiguously and document the agreement rate.<br>*why:* PoF wave 27 (9bdc466d): the /status engine heuristic decided 6 of 344 cells and named none of them (27% agreement where it would have named one over an audit), so a planned 300-step authoring pass was closed by measurement; 129->189 authored where the code was legible. | medium | pof / perfect@2.5.0 |
| 2026-09-05 | game-production | procedural-level-planning | technique | When two engines must agree on a generated layout, ship the layout as DATA one engine exports and the other replays, and state the parity rung in the agreement reason itself (exact by construction on the data; runtime placement unverified until observed) rather than promising seed-reproducibility across regenerating engines.<br>*why:* PoF wave 28 (d09208ac): the browser preview and the UE generator could never agree from a seed (LLM regenerates freehand); a grid-replay engine consuming the exported cells made one pair exact by construction while the reason string carries UNVERIFIED for runtime placement. | medium | pof / perfect@2.5.0 |
| 2026-09-05 | software-engineering | cost-metering | technique | When one paid operation has two entry points (a JSON route and a streaming twin), put the reserve/refund gate in the shared pre-gate module both routes already call, never inline in one route - the enforcement points must be enumerable from one file.<br>*why:* ascent's /api/scan carried six inline credit gates while /api/scan/stream - the only route the report UI actually drives - imported no credit code at all and ran paid LLM inference on private org repos unmetered, under a comment asserting the opposite. Moving the gate into scan-gates.ts (already the declared shared-gates home) closed it for both and shrank the JSON route. | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | audit-logging | technique | An at-most-once claim marker stored in the audit ledger must be cancelled by appending a signed release record, and the claim reader must treat a marker with a release as not-live - never by deleting the marker, which is the only mutation the ledger will ever grow.<br>*why:* ascent's releaseAuditClaim was the sole delete on AuditLog outside retention and erased the evidence that a digest dispatch had been attempted; a sibling table (AlertEvent) had been created specifically to escape that delete. Appending claim.released and widening claimOrgAuditOnce's read kept the at-most-once semantics and the trail. | medium | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | rate-limiting | technique | When a request passes through a per-key limiter and then a global ceiling, check the per-key window first but record the per-key hit only after the global gate admits - a refusal at any later gate must leave the caller's own budget untouched.<br>*why:* ascent recorded the per-IP hit before the global check, so during global saturation innocent callers drained their own 20/min budget on requests never served, and the fail-closed body's 'no budget of yours was exceeded' was false. Splitting hit() into checkWindow/recordHit fixed all three paths; three red-then-green tests pin it. | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | codebase-scanning | technique | When a tolerant gather stage catches a sensor's failure into the same null or empty value a successful-but-empty read returns, record the failure per sensor and make every downstream rule that would score that value as absence return unknown instead - a caught exception is not a finding about the population.<br>*why:* ascent's scan pipeline had fixed this for its PR sensor and left four others (governance, security posture, App inventory, CI health) scoring a thrown fetch as absence; a failed posture read persisted score 0 plus a remediation for a SECURITY.md the org has. A per-sensor failure list threaded into the existing blind-refutation path returned null for the affected checks and one warning named the reads that failed. | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | metrics-rollups | application | When a producer starts excluding a placeholder class (mock scores) from one aggregate, audit every sibling reader that averages, deltas or trends the same rows in the same request - the exclusion is a population rule, not a badge fix, and a half-applied rule produces two fleet numbers on one page.<br>*why:* ascent fixed the headline average in one round and the next round's scout found five sibling readers (dimension averages, trend + forecast, movers, team rollup, header chip) still folding mock rows, two of them rendered beside the fixed badge. One exported predicate applied at each reader plus a test per reader closed it. | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | adoption-measurement | technique | A recency floor on a per-scan snapshot must be anchored on the snapshot's own present (its newest observed activity), never on the wall clock, or a stale snapshot reads as a fleet nobody works on.<br>*why:* ascent's new 90-day enablement floor measured from Date.now() would have emptied 'who to enable next' for any fleet last scanned over a quarter ago; anchoring on the roster's newest lastActiveAt (the same anchor the stale-repo guard already used) kept the cohort honest for stale snapshots. | medium | ascent / perfect@2.5.2 |
| ~~2026-09-05~~ | software-engineering | quality-gates | technique | ~~A gate that exposes policy flags on an unauthenticated surface must announce, per flag, that the flag cannot be evaluated there when its input requires a credential the surface never carries - otherwise the policy echo asserts a bar the verdict never tested and the CLI advertises flags that can never fire.<br>*why:* ascent's public gate endpoint always scanned token-less, so three of its advertised bars (branch protection, AI-governed rate, AI authorship) were unconditionally skipped while the body answered pass with requireProtectedBranch: true; a typed skipped[] rendered on all five surfaces plus a structural test keyed on the skip vocabulary closed it.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | executive-reporting | application | When one document model feeds several renderers, grep each composed sentence for its caller count: a composer with exactly one caller means the other surfaces are hand-rolling the sentence and will carry the last fix's bug forward.<br>*why:* ascent's benchmarkCaption and movementLine each had one caller (the PDF); the tab and the share page rendered the pre-fix wording ('vs 1 repos') that a UAT run had marked resolved after inspecting only the PDF. Routing the HTML through the composers closed the class. | medium | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | mcp-tools | technique | On an agent door, the identity that keys audit actors, per-caller quotas and work-queue leases must be the credential's minted id, never its human-chosen name - names are labels and collide, and a collision on a lease holder is a cross-credential authorization hole.<br>*why:* ascent's MCP door keyed the audit actor, the daily write counter and the follow-up claim holder on token:<name>; token names were not unique, so two tokens named ci shared a counter and one could brief and report on the other's leased rows. Keying on the token id with the name as a meta label closed it; a transitional dual-match handles rows written under the old form. | high | ascent / perfect@2.5.2 |
| 2026-09-05 | software-engineering | readiness-passports | application | When a decision surface has a write route and a component that renders decisions, grep the route for callers: a decision API with zero product callers means every rendered decision came from a seed or a test, and the honesty machinery around it (re-confirmation, authorship) has never been exercised by a human.<br>*why:* ascent's passport decline PATCH had no caller in the product; the hero strip, fleet list, Pareto hollow marks and re-confirmation prompts were all fed by data no owner could enter, and the report-side card had drifted from the fleet list's re-confirm rendering unnoticed. A decline control on the existing PATCH plus a server-stamped author closed it. | medium | ascent / perfect@2.5.2 |
| ~~2026-09-05~~ | software-engineering | design-tokens | technique | ~~When a shared bar/meter primitive takes a width class from its caller, do not also hard-code a width utility inside it, because with utility CSS the stylesheet order decides which of two same-property classes wins, not the order they were composed in.<br>*why:* ascent's Spectrum carried w-full and the Atlas tree passed w-14; w-full won in the cascade, the bar took the row and every subject label was squeezed to zero width. Making the caller own the width class (default w-full) fixed it (c59b8914). Found only in a screenshot; jsdom tests passed.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / spark@1.3.2 |
| ~~2026-09-06~~ | software-engineering | test-harness | technique | ~~When a derived check accepts a cleanup/restore hook by its PRESENCE, require the hook body to touch the resource the check is about, because a hook that restores something else satisfies a presence test and the leak it was written to catch stays invisible.<br>*why:* env-isolation.probe.spec.ts accepted any test.afterEach( as an env restore; music-failure-classification's afterEach restores fetch only and its beforeEach sets ELEVENLABS_API_KEY - witnessed as 'probe-key' in every later file with the check reporting 0 offenders. Fixed dbd5b9a by reading the afterEach body.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | gravity / scan-sweep@4.0.0 |
| ~~2026-09-06~~ | software-engineering | quality-gates | technique | ~~When you remove a duplicated condition by extracting it into a shared predicate, assert BOTH the predicate's behaviour AND the call site's adoption of it, because a test that drives the predicate stays green when a call site reverts to its own copy â€” the extraction is what you fixed, the adoption is what you shipped.<br>*why:* Extracting a notebook section's render condition into sectionRenders() and driving it from a probe: reverting the component to map the unfiltered list left all five assertions green. Only seeding the component revealed it; the probe now reads the call site too (e28b972).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | gravity / scan-sweep@4.0.0 |
| ~~2026-09-06~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~When a per-call generation knob is encoded as a suffix on the model string, split it off at the ONE boundary every adapter crosses, never inside the adapters - one adapter will parse it and the rest will forward it into the provider's model field.<br>*why:* Measured in tracklight: split_effort had exactly one caller (the CLI adapter) out of four generation paths. The default judge spec was opus@xhigh and the preferred Anthropic route was the bare Messages API, which passed the whole suffixed string as a model id. Fixed by splitting in generate_once above every adapter, with a per-adapter test asserting no '@' reaches the wire model field.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | tracklight / perfect@2.5.2 |
| ~~2026-09-06~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~A defect on a provider path can hide behind CREDENTIAL SHAPE, not just behind a rare input: whichever auth the developer's own environment uses is the path that gets exercised, so a bug can fire on every deployment configured as documented and never once locally.<br>*why:* The suffixed-model bug survived because this machine authenticates Anthropic through the CLI's subscription OAuth - the single adapter that stripped the suffix - while .env carried only Gemini/OpenAI keys. The broken path was the one the module's own doc calls preferred, reached by setting the API key the deployment docs tell you to set.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | tracklight / perfect@2.5.2 |
| 2026-09-06 | software-engineering | - | technique | An ORDER BY with no deterministic tie-break is a latent correctness bug wherever a reader takes 'the latest row', because adding an index can change which tied row comes first - the row order is a query-plan artifact, not data.<br>*why:* Landed in tracklight: adding idx_benchmark_runs_bench turned a table scan into an index walk and flipped which of two same-instant benchmark runs read as latest, moving a prompt-promotion gate from refuse to allow. Fixed with (started_at DESC, id DESC) - deterministic, and explicitly NOT a claim that one simultaneous run was later. | high | tracklight / perfect@2.5.2 |
| 2026-09-06 | software-engineering | - | technique | A regression test for a plan-dependent ordering bug must be falsifiable against the FIX, not against a repeated read: two identical queries reuse one plan and agree with or without a tie-break, so a read-twice-and-compare test passes on the broken code.<br>*why:* Wrote exactly that test first and it passed on the unfixed code. Replaced it with one that inserts rows in an order disagreeing with the tie-break and asserts the pinned sequence, then verified it by deleting ', id DESC' and watching it fail. Same method caught a second, opposite error: a reported pricing bug whose fix was a no-op, proven because the test still passed after reverting the fix. | high | tracklight / perfect@2.5.2 |
| ~~2026-09-06~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~Raising a token ceiling to permit deeper reasoning is a wall-clock decision as much as a token one: the request timeout must scale with the effort level, or 'think harder' becomes 'time out harder'.<br>*why:* tracklight maps xhigh/max onto output_config.effort and raises max_tokens to 64000 there, behind a process-wide 120s non-streaming HTTP timeout that had been measured against default-effort calls. Fixed with a per-request timeout keyed on effort - per-request because the client is shared, so widening it globally would hand a black-holed endpoint the same budget.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | tracklight / perfect@2.5.2 |
| 2026-09-06 | software-engineering | agent-instruction-files | subject | When reusable agent know-how is stored beside the integrations it runs on, split it into two layers with different authors: a registry artifact that names connector TYPES and a recommended trigger (craft, versioned, shared), and an adopted instance that binds concrete connectors, a trigger and credentials (configuration, per installation). Mixing them yields one private copy per installation and no shared learning.<br>*why:* Measured on a 299-recipe corpus: 290 recipes were referenced by exactly one template each because connector ids and crons were baked into the recipe; separating the layers let 109 recipes be rewritten once with 68 of them self-paced and every vendor name demoted to a stored example. | high | personas / spark@1.3.2 |
| 2026-09-06 | software-engineering | mcp-tools | technique | A connector catalog category is a browsing taxonomy, not a capability contract; before letting a recipe declare a connector by TYPE, check whether the catalog records what a connector can actually do (emit events, read an inbox, expose cost), or adoption will bind a connector that satisfies the type and cannot do the job.<br>*why:* Across 134 connectors and 49 categories: stripe declares 0 events, cloud connectors declare no resources or events, exactly one messaging connector declares an inbound event, and three reviewed recipes recommended an event trigger against connectors that can never fire one. | high | personas / spark@1.3.2 |
| 2026-09-06 | software-engineering | knowledge-registry | technique | When a catalog carries a content digest as identity, also publish a derived ordering (git commit count on the item dir, +1 while the dir is dirty, carried forward from the previous catalog when the checkout is shallow), because a digest can say stale but never how far behind, and the dirty +1 is what keeps the generated file idempotent both before and after the commit that changes an item.<br>*why:* build-index.mjs gained revision/changedAt for 418 subjects in 5 git spawns; --check stayed idempotent across a subject edit and a depth-1 CI checkout, where a naive count would have reported 1 for every subject | high | ascent / spark@1.1.0 |
| 2026-09-06 | software-engineering | knowledge-registry | technique | When a derived join carries human verdicts keyed by a name the source can rename or delete, never drop a verdict on regeneration: park it as an orphan that persists until adopted, and re-attach by measured path overlap (>= 0.6) with the provenance written on the pair, because a silent drop erases paid-for judgement and a guessed re-attach lies about what the verdict covered.<br>*why:* build-registry-map.mjs dropped every verdict of a deleted context at one `if (!row) continue`; the churn-safe rewrite reproduced ascent's 230 pairs exactly and a fixture proved delete/rename/arrive/persist | medium | ascent / spark@1.1.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a composite gesture is authored, split it into per-property tracks before naming it, because per-element transitions fuse lift, recolour and disclose into one curve that cannot be retuned separately (ascent motionReveal.ts).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a product keeps presets as CSS keyframe utilities, still declare the duration ladder and easing roles as tokens, because utilities that inline their milliseconds cannot be audited against a budget (ascent globals.css ascent-* keyframes).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a scroll-deck entrance is CSS-owned, pin its SSR state as visible in a test, because a framer-owned version once baked opacity:0 into server HTML (ascent deck/Reveal.tsx + Reveal.test.tsx).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a playhead drives a loop, write progress through a ref and notify React only at start and settle, because routing 0-1 progress through setState re-renders every consumer at frame rate (ascent aboutOrgLoopMotion.ts).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When entrance caps exist only as per-file literals, a preset can exceed the budget without touching a budget, so keep the caps as named constants beside the presets (ascent globals.css 280ms and LOOP_RUN_MS).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a tab shell keys its panel on the tab id, the entrance replays on every switch by construction and never on poll, which is the correct one-shot seen-set boundary (ascent OrgTabChunks.tsx key={tab}).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When ordinary panels need the reduced-motion preference, read it through a framer-free hook so the preference read does not drag the animation library into every chunk (ascent ui/useReducedMotion.ts).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a loop degrades, derive the surface's liveness label from whether the loop actually ran, because a label set independently keeps saying live after the loop stopped (ascent aboutOrgLoopMotion.ts).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | motion | application | ~~When a brand rule forbids always-on loops, the few pulse loops that remain still owe a visible stop and a separate user-stop state, because the rule removes the need for a control without removing the loops (ascent .live-dot / animate-pulse).<br>*why:* authored and observed in the motion showcase scene (src/features/shared/surfaces/motion) during spark ui-surfaces-showcase~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~A form primitive associates its label implicitly by wrapping the control and names a control group through a real legend, so no call site threads ids.<br>*why:* src/components/ui/Field.tsx wraps the control in its <label> and switches to <fieldset>/<legend> via as="fieldset", with .focus-ring on every control; shown by the worklist catalog audit in WorklistRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~A composite menu is one tab stop: members carry tabindex -1, arrows and Home/End move real focus, and Tab hands focus back to the trigger before the members unmount.<br>*why:* src/components/header/IdentityMenu.tsx implements role=menu with roving tabIndex={-1} rows and returns focus to the trigger on Tab; shown by the roving segment strip in StripRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~A drawer kept mounted and translated off-screen closes the focus and tree channels with inert at its subtree root from the same open condition, with the pull tab placed outside the inert boundary.<br>*why:* src/components/onboarding/tour/TourChecklist.tsx carries inert={!open} on the <aside> root and its toggle button outside it; shown by the inert mechanism in DrawerRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~When a control's accessible name is a fixed aria-label, its visible outcome swap is announced through a dedicated polite live region rather than by mutating the button.<br>*why:* src/components/CopyForLlm.tsx renders a role=status aria-live=polite sr-only region beside the button and CopyForLlm.test.tsx asserts its text; shown by the announcer homes in AnnouncerRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~A field failure is wired as one contract: aria-invalid on the control, aria-describedby pointing at the error's id, and focus returned to the control so the reason is voiced.<br>*why:* src/components/onboarding/OnboardingInvitePanel.tsx sets aria-invalid and aria-describedby="invite-error" and refocuses the input, pinned by OnboardingScanStep.dom.test.tsx; shown by the submit-empty path in FormRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~The reduced-motion preference is read by one framer-free subscriber and the stylesheet states that call sites never add their own media check.<br>*why:* src/components/ui/useReducedMotion.ts subscribes to the media query once and globals.css carries the never-at-a-call-site rule beside its fallbacks; shown by the derived signal in PreferenceRegion.tsx and signal.ts.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~Component tests assert the screen-reader-visible output: computed ids and ARIA attributes, document.activeElement after the failure, and role-plus-name queries.<br>*why:* src/components/onboarding/OnboardingScanStep.dom.test.tsx asserts aria-invalid, aria-describedby, the focus destination and getByRole with a name; shown by the layered gate readouts in GatesRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | accessibility | application | ~~A live-region decision is justified against how named screen readers actually treat a polite backlog, not against conformance alone.<br>*why:* src/features/inflight/live/LiveWarRoomPanels.dom.test.tsx removes a live region from the movers ticker because NVDA/JAWS keep reading a queued backlog; shown by the dated pairing matrix in PairingsRegion.tsx.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| 2026-09-06 | software-engineering | adaptive-fidelity-tiers | application | A settled measurement is re-armed by an event gated on how long the situation has plausibly changed, never by a poll or by every focus event.<br>*why:* src/components/launch/useFleetData.ts skips its visibilitychange re-pull when Date.now() - lastStartedAt < POLL_INTERVAL_MS, with the comment that a re-pull on every focus is a poll with extra steps; shown in the scene's measurement-settle-budget region (the two named re-arm events). | medium | ascent / surface@1.0.0 |
| 2026-09-06 | software-engineering | adaptive-fidelity-tiers | application | Deferred work is scheduled as idle OR a timeout, whichever first, with both handles cancelled by the same cleanup so a slow device still gets the work and a torn-down surface gets no callback.<br>*why:* src/components/ui/Defer.tsx requests requestIdleCallback with DEFER_IDLE_TIMEOUT_MS (500) and cancels the handle in the effect cleanup; shown in the scene's probe-deferral-to-idle region (the two handles and 'cancelled by the winner'). | high | ascent / surface@1.0.0 |
| 2026-09-06 | software-engineering | adaptive-fidelity-tiers | application | An expressed reduced-motion preference decides whether the scheduling machinery is created at all, as the first statement before any request or timer, so the teardown is structural rather than a guard inside the loop.<br>*why:* src/components/ui/Defer.tsx computes mountsImmediately() per render and opens its effect with `if (now \|\| ready) return;`, creating no idle request or timer under the preference; shown in the scene's preference-short-circuits-measurement region (the existence ledger). | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~A loading/error/data union whose initial value is the loading member makes the empty rendering unreachable before a response, and a cancelled flag drops a stale response.<br>*why:* src/components/org/followups/FollowupHistory.tsx starts at "loading", renders empty copy only from a delivered array and never from the error branch; shown by the state-model ledger region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~The placeholder's appearance delay rides on its own entrance animation with backwards fill and must keep its window when motion is off.<br>*why:* src/components/ui/deferPolicy.ts QUIET_PLACEHOLDER_DELAY_MS mirrors globals.css .reveal-quiet, whose reduced-motion partner ascent-hold-hidden keeps the delay; the scene imports the constant for its GhostRows in the placeholder-design region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~A pressed control disarms synchronously through a ref set inside the activation handler and released in finally, so a double-press cannot double-fire before React re-renders.<br>*why:* src/components/CopyForLlm.tsx sets inFlight.current in copy() before awaiting and clears it in finally; the scene's BusyButton in the action-busy-states region does the same with a scripted double-press.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~Empty and notice states route through one canonical component with page and section variants so they stay proportionate in composition.<br>*why:* src/components/EmptyState.tsx is the single empty/notice primitive every hand-rolled notice routes through; the empty-state-design region shows the cause-typed taxonomy it lacks.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~Failure renders as a role=alert with a retry that reissues the same request under the same context, never the empty copy.<br>*why:* src/components/org/followups/FollowupHistory.tsx retries via a nonce for the same id; the failure-states region shows the same with a BusyButton retry and the held-data degrade path Ascent lacks.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~An entrance cascade uses tens-of-ms steps capped at a fixed child count and goes flat under reduced motion.<br>*why:* src/app/globals.css .stagger-children (40-50ms steps, capped at child 6, animation:none under reduced) and OrgTabChunks.tsx key={tab}; the arrival-choreography region adds the load-edge coupling and identity seen-set.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | async-ui-states | application | ~~An identifying coordinate change resets the dependent windowing coordinates in one direction only, declared once where the key is defined.<br>*why:* src/lib/org/orgTabs.ts TAB_SCOPED_PARAM_KEYS + buildOrgTabUrl() clear cursor/subject/technique on a tab switch while a param change never touches the tab; the windowing-vs-identifying-keys region shows KEY_CLASS applied to a fetch key.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~Color roles and typography recipes are declared once by intent, with the raw drift each one replaced recorded beside the binding.<br>*why:* src/app/globals.css @theme block (--color-surface, --color-divider, --color-on-accent with their replaced literals) and @utility type-*; shown in the token-taxonomy ledger region~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~The foreground drawn on a colored fill is chosen by contrast arithmetic, never fixed.<br>*why:* src/lib/ui.ts readableTextOn() and heatCell() pick ink vs light text via rgbOf/relLuminance; the theme-architecture region's contrast column reuses those helpers~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~A token consumed from both the style and the script runtime must be kept in sync by generation or a gate, not by a comment.<br>*why:* src/components/ui/format.ts DIRECTION_TONE is a TS mirror of --color-tone-* held together by a 'change BOTH together' comment; the cross-language-token-parity region shows the gated and generated alternatives~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~A semantic token whose expansion is byte-identical to the raw form it replaces is lexically unenforceable.<br>*why:* src/components/ui/chip.ts documents the success token expanding to the same emerald-500/300 classes it replaced; the token-enforcement region's gate is the allow-list this makes impossible for that axis~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~Reduced motion is honored at one door in the token layer, never per call site.<br>*why:* src/app/globals.css single prefers-reduced-motion block over every animate-* utility with the rule written beside it; the motion-tokens region rebinds the ladder at the scope root the same way~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | design-tokens | application | ~~A text-scale change is a rebinding of the type ramp that every consumer follows without knowing.<br>*why:* src/app/globals.css re-based every --text-* token one pixel up so all surfaces lifted at once; the density-and-scale-axes region makes the same move a user axis~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~Gate a presentation map against its union with a total Record annotation, then derive the runtime member list from the gated map's keys; the cast is a derivation, never the gate.<br>*why:* src/lib/integrations/providers.ts FIDELITY_META: Record<Fidelity,...> then FIDELITY_TIERS = Object.keys(FIDELITY_META) as Fidelity[]; shown in the ChainRegion layers list of the status-vocabulary scene.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~A status colour table keyed by the union pairs a non-colour glyph with every member and clamps an unrecognised member toward the attention end.<br>*why:* src/lib/ui.ts LEVEL_CLASSES + LEVEL_GLYPH are Record<LevelId,...> with the CVD rationale in-source and LevelBadge.tsx clamps to L1; shown in the ColorRegion legends and drop-colour toggle.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~Per-quantity precision ladders live in one formatter module that call sites invoke instead of re-deriving inline.<br>*why:* src/lib/ui.ts fmtCompact and fmtPts hold the compact and one-decimal ladders for the fleet columns; the NumberRegion's fmtNumber generalises them with the locale bound inside.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~An elapsed label clamps negative elapsed time to zero and is re-evaluated on a ticker rather than frozen into state.<br>*why:* src/lib/ui.ts freshness() uses Math.max(0, ...) and src/components/report/FreshnessControl.tsx re-renders it on a 30s interval; the TimeRegion's shared ticker and fmtElapsed clamp are the generalisation.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~Rich untrusted text renders through one deliberately tiny markdown door with no href, no src and no raw-HTML injection.<br>*why:* src/components/report/MarkdownLite.tsx renders model prose as four inert constructs; the LabelRegion shows the text-only half of the same contract.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | status-vocabulary | application | ~~A presentation table typed against the status union turns the checklist's 'extend the presentation table' step into a compile error.<br>*why:* src/components/org/shared/backlogShared.ts STATUS_ACCENT: Record<RecStatus,string> carries its own post-mortem; the EvolutionRegion's previewFor shows the colourless pill that step otherwise ships.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~Delta tone and format are one authority every surface imports, with within-noise and non-finite deltas muted to flat rather than shown as confident moves.<br>*why:* src/components/ui/format.ts DIRECTION_TONE/toneFor/fmtDelta are the single delta vocabulary for fleet surfaces; MetricRegion colours its delta through deltaHex(favourable(def, delta)).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~Every chart projects through one scale helper whose domain is declared, not sampled, so sibling charts share ticks and a bad point cannot break the path.<br>*why:* src/components/report/chartScale.ts vScale fixes 0..100 with a NaN clamp and linScale demands domainMax; BAND_EDGES puts ticks on level boundaries; ScaleRegion's project(series, domain) is the same signature.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~Heavy below-the-fold widgets mount on a one-shot viewport observation with a generous margin, releasing the observer in cleanup and failing open where it is absent.<br>*why:* src/components/ui/Defer.tsx strategy="visible" (240px rootMargin, disconnect on first intersection, cleanup) with a geometry placeholder; LoadingRegion reserves slot height and names the timer's reaper.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~A sparkline inside a dense surface uses the column's fixed domain and a real reference edge, never its own sample range.<br>*why:* src/components/report/TrendChart.Sparkline.tsx projects through vScale on 0..100 with a reference line at the L4 band edge; MicroRegion's column shares SCORE_DOMAIN.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~Status hue is one ramp shared by charts, rings and badges, paired with a non-colour glyph so hue is never the only channel.<br>*why:* src/lib/ui.ts LEVEL_HEX/scoreHex with LEVEL_CLASSES locked to the same stops and LEVEL_GLYPH as the redundant channel; EncodingRegion's status mode uses scoreHex and moves identity to the label.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | data-viz | application | ~~Below the threshold of shape a chart degrades to an honest form that still shows the numbers, and a zero is drawn as an explicit empty track, not a hairline fill.<br>*why:* src/components/report/RadarFallback.tsx renders labelled bars under three axes and an explicit 'Zero: nothing detected' line; StatesRegion draws two observations as points plus the number, no line.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | feed | application | ~~A feed is read newest-first on the authority's own stamp with a hard window cap, never a client-chosen sort.<br>*why:* src/lib/db/alert-events.ts listAlertEvents orders by createdAt desc with a capped take; shown in the reverse-chronology-semantics region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | feed | application | ~~A live top-growing list prepends by a minted identity and caps its length rather than re-keying by position.<br>*why:* src/features/inflight/live/liveWarRoomFold.ts foldRepoEvent prepends a monotonic-id row to a TICKER_MAX-capped ticker; shown in the live-prepend region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | feed | application | ~~Repeated related occurrences from one producer are collapsed under a per-relation time window so the busiest producer cannot own the whole channel.<br>*why:* src/lib/alerts.ts claimRegressionAlert admits one regression alert per repo per cooldown window (write-time, not a read-time view); shown in the event-clustering region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | feed | application | ~~Store a read-position anchor per (reader, feed) and derive the unseen count by comparison, with a stated fallback for a reader who has never looked and a capped display.<br>*why:* src/lib/db/members.ts getAlertsWatermark/markAlertsSeen keep Membership.alertsSeenAt (join-date fallback) and AlertsMovement.tsx derives the count with a 9+ cap; shown in the read-position-and-unseen region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | feed | application | ~~Retention is a named reaper with operator-visible settings that composes an age bound with a per-entity keep-window and reaps only settled rows.<br>*why:* src/lib/db/retention.ts: per-org retentionMaxScans/retentionAuditDays with env defaults, settled-only queue pruning, labelled prune sweeps under a wall-clock budget; shown in the feed-retention region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~Every pointer reading on a drawn field passes through one screen-to-data conversion function owned by the field module.<br>*why:* src/features/inflight/live/observatory/ObservatoryLasso.tsx toData() is the sole conversion for the observatory svg, though the field is fit-to-container with no zoom or pan; the scene's viewport-transform region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~Memoize placed geometry by the only inputs that move it so a data-only frame recomputes zero positions, and turn ambient per-node animation off past a fleet-size threshold.<br>*why:* src/components/launch/fleetMapStars.ts positionCache keyed by (index,total,seed) and DENSE_FLEET_STARS gating the twinkle in ConstellationField.tsx; the scene's render-budget region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~Capture the pointer at press on a whole-target element, keep the gesture provisional outside the model, and decide click versus drag by movement on release, releasing capture on every exit path.<br>*why:* src/features/inflight/live/observatory/ObservatoryLasso.tsx useLasso captures at pointerdown, holds the rubber band as gesture state, and treats sub-threshold release as a click that clears; the scene's direct-manipulation region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~A generated layout is deterministic and seeded by node identity, and a node arriving mid-session gets a placement policy that never shifts existing nodes.<br>*why:* src/components/launch/fleetMapStars.ts starPosition (phyllotaxis seeded by fullName, quantized for SSR agreement) and appendedStarPosition (outer ring independent of total); the scene's graph-layout region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~Edges and nodes read one shared per-node derivation for position so a link cannot point at where a node used to be, and out-of-focus links recede rather than disappear.<br>*why:* src/components/launch/ConstellationField.tsx computes starData once for both the lines pass and the stars pass and drops filtered lines to 0.03 opacity; the scene's edge-management region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | canvas-graph | application | ~~A drawn graph announces itself as the graph: a group with a name that states the real total (and how many are drawn), and each node labelled as itself rather than as a shape.<br>*why:* src/components/launch/ConstellationField.tsx svg role=group with a counted fieldLabel and per-star <a aria-label>, while making every star a tab stop, the failure the technique names; the scene's canvas-accessibility region.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~A severity vocabulary is one closed union that every renderer derives from, with any non-alarm band added as a level rather than a parallel axis.<br>*why:* src/lib/alerts.ts defines AlertSeverity once and SEV_EMOJI: Record<AlertSeverity,...> derives from it, with celebration justified in-source as a level; shown in the severity-taxonomy region~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~A repeat of the same semantic key inside a cooldown is suppressed by a single check-and-stamp step and still written to the record with its suppression reason.<br>*why:* src/lib/alerts.ts claimRegressionAlert stamps per repoFullName atomically and alert-events.ts records suppressedReason: cooldown; shown in the queue-discipline region's cooldown counter~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~A notice's availability ends visibly and durably: its dismissal removes the state that produced it so a reload cannot resurrect it.<br>*why:* src/components/org/shared/BillingReturnNotice.tsx strips the credits param via router.replace on dismiss; shown in the actionable-toasts region's undo window ending visibly~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~Admission to the durable record is decided by the event's classification at the source, independent of whether delivery happened.<br>*why:* src/lib/db/alert-events.ts recordAlertEvent writes one row per alert the product decided to raise, delivered or not, and AlertsHistory.tsx lists the outcome; shown in the durable-notification-ledger region~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~The out-of-app tier has one delivery door that is deadline-bounded, returns a result on every failure path, and sits behind an admission gate that keeps a flat period silent.<br>*why:* src/lib/alerts.ts dispatchAlert (webhook or mailto sink, AbortSignal.timeout, false on failure) and digestHasSignal; shown in the os-escalation region's decision log and failure counter~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | toasts-notifications | application | ~~A visible state swap is announced through a dedicated live region mounted before the news and written into on the transition, not through a live attribute on the control itself.<br>*why:* src/components/CopyForLlm.tsx mounts role=status aria-live=polite beside the button and writes the Copied/failed outcome into it; shown in the announcement-accessibility region's empty-at-mount regions~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~A capped read reports the cap and turns each unreadable entry into a counted warning plus null, never an aborted or silently short listing.<br>*why:* src/lib/registry/index-walk.ts cappedReader/selectArtifacts push warnings for oversized, unreadable and over-cap blobs and continue; shown in the listing-and-refresh region's skipped/excluded readouts.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~Persisted view state is one blob per store root, written on change, read tolerantly so a corrupt value degrades to defaults rather than throwing.<br>*why:* src/features/inflight/live/outcome/useColumnWidths.ts persists column widths per org in one localStorage key inside try/catch with clamping; shown in the navigation-state region's live blob and restart.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~Selection is a set of identities that one producer feeds to every bulk consumer.<br>*why:* src/components/org/followups/FollowupsWorklist.tsx keeps selected as a Set<string> of follow-up ids driving bulk done/dismiss; shown in the selection-model bulk bar.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~A bulk mutation applies items independently under bounded concurrency and returns per-item outcomes with a count that carries its predicate.<br>*why:* src/app/api/practices/apply-batch/route.ts fans out via mapPool, each repo owns its error, and returns { results:[{repo,ok,error?}], attempted, skipped }; shown in the file-mutations report.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~A renderer that may crash lives behind its own boundary so the surrounding surface stays operable.<br>*why:* src/components/org/shell/OrgTabErrorBoundary.tsx contains one tab panel's render crash and resets on tab change; shown in the thumbnails-and-previews failed tile that keeps the row actionable.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | file-browsing | application | ~~A closed vocabulary has exactly one definition that every consumer, including its guard test, derives from.<br>*why:* src/lib/org/surface-catalog.ts SURFACE_SUBCATEGORIES/SurfaceSubcategory feed the gallery sections, records and surfaceCatalog.test.ts; shown in the kind-taxonomy vocabulary table.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | search | application | ~~User text reaches a query engine only as literal field matches composed by one server function, never as engine syntax.<br>*why:* src/lib/db/org-memory.ts listOrgMemories wraps the raw search in Prisma contains/insensitive clauses over three named fields; SearchBox.tsx region query-parsing~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | search | application | ~~Below a few thousand short rows a declared-scope scan is the correct engine and needs no derived index to reconcile.<br>*why:* src/lib/db/org-skills.ts listOrgSkills scans name+description with Prisma contains; prisma/schema.prisma carries relational @@index only; IndexPanel.tsx region full-text-indexing~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | search | application | ~~A ranked list's sort key ends in a unique stable identity so two runs never disagree.<br>*why:* src/components/org/followups/followupsModel.ts sortByValue orders by value rank, projected points, then title.localeCompare as the final key; ResultsPanel.tsx region ranking-and-excerpts~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | search | application | ~~Facet option lists derive from the full row set and any filter change resets the page rather than clamping it.<br>*why:* src/components/org/SecurityFindingsTable.tsx builds repoOpts/statusOpts from all rows and withReset re-opens the window at page one; FacetPanel.tsx region faceting-and-filters~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | search | application | ~~Filter and selection state is navigational state serialized into the address, with one declared reset.<br>*why:* src/lib/org/orgTabs.ts TAB_SCOPED_PARAM_KEYS + buildOrgTabUrl + clearedTabScopedParams carry scope in the URL; ViewsPanel.tsx region saved-views~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~A compare surface detects when the server did not honor the requested pair and says so above the picker instead of silently rendering the default pair.<br>*why:* src/app/report/compare/page.tsx computes `unhonored` from ?a/?b against the resolved pair and renders a status notice; WhatChanged.tsx labels the same-scan pair. Shown in the pair-and-baseline-selection region (self-pair and pruned-baseline notices).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~A field-level diff enumerates the schema that scores the entity and aligns evidence on a normalized content key, choosing the level per audience (exact within one repo, name-key across repos).<br>*why:* src/lib/report/compare.ts `diffScans` walks DIMENSIONS with null deltas unless both sides scored; `signalNameKey`/`diffSignalSets` blank embedded counts; exemplar.ts states why. Shown in the semantic-level-selection region (keyed vs positional, ledger).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~An async answer is stored with the request key it answers and applied only on exact match, with an in-flight flag dropping superseded responses.<br>*why:* src/features/admin/settings/DataErasurePreview.tsx `useErasePreview` keys state by (slug, includeAudit, open) and uses `live` to drop a stale response. Shown in the computation-offload region (seq identity, dropped counter).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~A direction/change vocabulary is defined once as glyph + colour + label together, and a non-measurement renders as a bare dash rather than a confident mark.<br>*why:* src/components/ui/format.ts `DIRECTION_TONE` triad and `fmtDelta` returning a dash for non-finite deltas. Shown in the presentation-modes region (CHANGE_KINDS legend, three distinct zero/failed/computing states).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~A drift finding against a declared artifact carries two hashes so stale (source moved) and hand-edited (copy changed) are distinguishable, and a check that could not run reports itself as unchecked rather than fresh.<br>*why:* src/lib/analyze/guidance-projection.ts two-hash projection header; src/lib/standard/doctor.ts check 5 emits one aggregated `unchecked` finding. Shown in the drift-against-declared region (coverage readout, unevaluated never passing).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~Every reason a comparison was not made is spelled as its own sentence, and a dimension scored on only one side is a distinct not-comparable state with no gap claimed.<br>*why:* src/app/report/compare/ExemplarPanel.tsx `ExemplarFailureNotice` per failure kind and the `notComparable` line; WhatChangedParts.tsx `OneSidedBadge`. Shown in the diff-honesty region (failure with retry, not-compared rows with reasons).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | diff-comparison | application | ~~BOM and CRLF are folded before parsing as an explicit normalization step, so an invisible platform difference never reads as an edit.<br>*why:* src/lib/standard/memory-read.ts `parseRepoMemoryEntry` strips the BOM and folds CRLF; src/lib/sse.ts documents the CRLF frame boundary. Shown in the invisible-differences region (CRLF to LF mark, ledger entry).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | low | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | table | application | ~~A keyset cursor is the whole ordering tuple (sort value + immutable id), compared as a tuple in the next-window predicate, with limit+1 standing in for a count.<br>*why:* src/lib/db/scans-audit.ts encodeAuditCursor/decodeAuditCursor over (at desc, id desc) and getAuditLog's OR predicate + take limit+1; shown by the table scene's pagination region (LedgerFooter.tsx).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | table | application | ~~A sortable header is a real button carrying aria-sort, absent values get one declared home, and selection keyed by identity survives a resort.<br>*why:* src/features/standing/repositories/RepoLeaderboardParts.tsx SortTh + activityValue(-1 for absent) and useRepoLeaderboard.ts selection by fullName; shown by the sorting region (SortInstrument.tsx).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | table | application | ~~The presentation sequence is a memoized derivation over named inputs, recomputed exactly when one changes, never pruned back into state by an effect.<br>*why:* src/features/standing/repositories/useRepoLeaderboard.ts sortedRows/selected as useMemo over rows, sort, rawSelected, rowNames; shown by the performance region's derivation counter (PerfInstrument.tsx).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | table | application | ~~A table placeholder is delayed ~150ms so a warm load never paints it, and the delay is kept under reduced motion because it is anti-flash, not decoration.<br>*why:* src/components/ui/deferPolicy.ts QUIET_PLACEHOLDER_DELAY_MS mirrored by .reveal-quiet in globals.css; shown by the ledger's ghost rows (sceneParts.tsx ghostAnimation).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / surface@1.0.0 |
| ~~2026-09-06~~ | software-engineering | table | application | ~~In the all-server regime, filter and window are request parameters, a monotonic request id drops superseded responses, and every derived artifact (export href) comes from the last APPLIED predicate.<br>*why:* src/features/admin/audit/useAuditLogFilters.ts reqId latest-wins + csvHref from `applied`; shown by the client-server-split region's echo readout (SplitToolbar.tsx).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | ascent / surface@1.0.0 |
| 2026-09-07 | llm-observability | quality-regression-gating | technique | A paired test must align on case IDENTITY, never on vector length - and a compacted per-case vector (one that skips failed cases rather than holding a gap) makes equal length a lie about equal case sets.<br>*why:* Landed in tracklight: two benchmark targets that each errored on a DIFFERENT case finished with equal-length score vectors, and the paired test zipped them by position. Two targets scoring identically on every case they both judged produced mean_delta 0.15 with p=0.0 and significant=true - a certain win manufactured out of a one-case offset. Fixed by carrying the case id in the score type and aligning on the intersection. | high | tracklight / perfect@2.5.2 |
| 2026-09-07 | llm-observability | quality-regression-gating | technique | Misaligned pairing is WORSE than an unpaired test, not merely weaker: differencing two different cases adds between-case variance to the deltas while the paired standard error still claims it was removed, so the verdict comes back wrong AND overconfident.<br>*why:* Measured on the tracklight fix: the misaligned pairing reported zero stderr and p=0.0 - maximal confidence - on a comparison that had no real gap at all. A tool that degrades to an unpaired test on mismatched sets would have been safer than one that silently paired by position. | high | tracklight / perfect@2.5.2 |
| 2026-09-07 | software-engineering | - | technique | A test NAME can assert a guard the code never performed, and it hides the gap more effectively than a missing test - anyone grepping 'is this checked?' finds the name and stops.<br>*why:* tracklight had a test named paired_deltas_refuses_mismatched_case_sets guarding a function that only compared vector lengths. The name made the same overstatement as the doc comment, and both survived months of review. Renaming it to say what it does was part of the fix, not cosmetic. | high | tracklight / perfect@2.5.2 |
| 2026-09-07 | software-engineering | - | technique | When verifying a test is falsifiable by breaking the code it guards, confirm the FILE ACTUALLY CHANGED before believing the result - a break that silently fails to apply produces a passing test that reads as proof of the opposite.<br>*why:* Happened twice in one session on tracklight, to a builder and to its reviewer independently. A string replacement with no assertion no-opped; the test passed; the momentary conclusion was that the guard was worthless. An assert on the replacement or a diff check costs nothing and separates a proof from a coincidence. | high | tracklight / perfect@2.5.2 |
| ~~2026-09-07~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~When a statistical claim gains a caveat field, audit every RENDERER of that claim - a presentation layer that branches on significance will often print notes on the weak branch only, so the strongest sentence the tool emits becomes the one that cannot say what it rests on.<br>*why:* tracklight's winner_line returned early on significant claims and never printed caveats, so a best claim tested over a case-set intersection rendered as an unqualified winner. The layer is shared by the runner, the CLI and MCP, so a compensating stdout print in the runner reached one consumer of three.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | tracklight / perfect@2.5.2 |
| 2026-09-07 | software-engineering | - | technique | A tolerant parse that degrades an unknown enum to a default must not be shared between a READ path and a WRITE path: degrading is correct for deserialising stored rows (forward compatibility) and is silent data loss when accepting operator input.<br>*why:* tracklight shared one de_difficulty between both. POSTing a benchmark case with a tier off the closed ladder returned 200 and stored ungraded; a live 18-case run shipped 8 cases that way and the per-tier report then covered a corpus whose grading had been discarded. Fixed by strict validation at the API boundary with the deserialiser left tolerant, and one test pinning both halves together. | high | tracklight / perfect@2.5.2 |
| ~~2026-09-07~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~When a benchmark gains a new axis or a graded field, add it to the machine-readable contract callers read - an axis absent from the schema is an axis nobody varies, and the omission fails silently by construction.<br>*why:* tracklight shipped reasoning-effort targets and graded difficulty tiers with tests and docs; both were absent from the JSON Schema an MCP agent receives for create_benchmark. Grep for effort in that file returned zero. That absence is why the first live run graded cases with a rung that does not exist.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | tracklight / perfect@2.5.2 |
| ~~2026-09-07~~ | llm-observability | cross-provider-benchmark-operations | application | ~~Report per-tier discrimination descriptively alongside the scorecard: a tier every target scored identically on separated nothing, and the operator is paying for it. State it as an observation, never as a significance test - per-tier power is far below the run's.<br>*why:* Measured in tracklight: a 6-target 9-case run spent 36 of 54 generation calls on two tiers that every target scored 1.00 on. The framework could not say so; the finding needed a hand-written script. After shipping the verdict a live run printed it unaided. Per-tier significance was deliberately NOT built - the discriminating tier in that run had three cases.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | tracklight / perfect@2.5.2 |
| ~~2026-09-07~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~Difficulty-for-humans is not difficulty-for-LLMs: classic cognitive-reflection traps make a useless benchmark tier because they are famous and in training data.<br>*why:* A live tracklight run graded bat-and-ball, 5-machines and lily-pads as a medium tier. All six model/effort configurations scored 100% on every one of them. The tier separated nothing and cost a third of the run.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | tracklight / perfect@2.5.2 |
| ~~2026-09-07~~ | llm-observability | cross-provider-benchmark-operations | technique | ~~Do not assume a cheaper model tier means a cheaper or faster call - measure per-case cost and latency, because a small model can burn far more reasoning tokens on a hard case than a large one.<br>*why:* Measured on the Claude CLI path: haiku at low effort cost 0.086 USD and took 142 seconds on a constraint puzzle that opus at high effort answered in 11 seconds for 0.085 USD - haiku emitted 15466 thinking tokens against opus's 445. Any frontier that ordered targets by assumed tier rather than measured cost would have got this backwards.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | tracklight / perfect@2.5.2 |
| 2026-09-07 | software-engineering | cicd-monitoring | technique | When a poll loop derives its backoff and its data-age signal from a THROWN error, the fetch it wraps must not own its own catch - in a codebase whose lint requires every catch to reach an error door, the wrapper routes the failure there and resolves, and the loop reads every failure as a success; because the failed-poll trap is then reintroduced by the caller's error-door convention, the loop should take a result shape (or the wrapper must report-then-rethrow) so a failure can be both reported and seen.<br>*why:* Personas' CloudHistoryPanel wrapped its fetch in silentCatch (the repo convention, lint-enforced) and handed it to usePolling, which backs off and withholds lastRefreshed only on a thrown error. Measured: a dead orchestrator was polled at full 15s cadence and the Live dot stayed green over the last snapshot. Fixed by report-then-rethrow on the poll path with a Stale indicator (commit 4c8aa8031, test red-then-green). | medium | personas / scan-sweep@3.0.1 |
| 2026-09-08 | recruiting | hiring-need-as-structured-brief | application | When a model re-emits a structured brief, the coercer must accept the field names the prompt actually exposes (and the object form of annotated prose lists), because a row dropped for one missing key produces the eloquent empty brief in code while the transcript reads perfectly.<br>*why:* kp intake: live JD-grounded dialogs returned requirements[] on every role; the model emitted graded rows under label not skill and success criteria as {text,provenance,sourceTurn}; coerce_role_brief discarded them; fixed and pinned by test (f5c7dec0). | high | kp / spark@1.x |
| 2026-09-08 | software-engineering | eval-harness | technique | When a dialog harness drives the real product over HTTP, run it before polishing the surface that renders its output, and give it per-role incremental dumps plus worker concurrency from the first version, because serial LLM-vs-LLM roles cost minutes each and a killed run without dumps loses everything.<br>*why:* kp intake sim: 2 live roles found 2 engine bugs the unit suite never saw; the first 50-role sweep ran ~8 min/role serially and had written no dumps after 40 minutes; --workers and per-role run.json rewrites were added afterwards (6b7a82b6). | medium | kp / spark@1.x |
| ~~2026-09-08~~ | software-engineering | async-ui-states | technique | ~~When a control needs explaining, put the sentence on the control as a hover-and-focus tooltip and draw the unavailable case as a struck glyph, because a surface that spends layout on explaining itself reads as unfinished and the explanation is not lost, only reached.<br>*why:* kp intake studio carried ~40 chrome sentences; replacing them with glyph+tooltip, negative-state icons and shape-of-what-fills-it empty states was the change that made the surface read as designed; a real Tooltip element was required because title= is invisible to keyboard and touch (84b9ae38).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | kp / prototype@1.x |
| ~~2026-09-08~~ | software-engineering | motion | technique | ~~Spend the motion budget on state changes rather than entrances, and render a change as one object morphing via a shared layout id, because a pending element that unmounts while its settled twin mounts discards scroll anchoring, selection and focus at the exact moment the reader engages.<br>*why:* kp intake: the thinking marker and the arrived turn were two components swapping; giving them one layoutId removed the flicker and made the wait legible, and diffing so only changed rows animate kept a large brief update from reading as a queue (84b9ae38).~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | kp / prototype@1.x |
| 2026-09-09 | software-engineering | - | technique | An absent correlation statistic renders as a definite value: crates/render/src/labels.rs:139-141 formats opt_f(c, "pearson"\|"mae"\|"rmse").unwrap_or(0.0) as {:.3}, so a label set with no computed agreement shows 0.000 - and 0.000 is a meaningful reading for a correlation (no relationship), not a recognisable placeholder. opt_u(c, "n").unwrap_or(0) has the same shape one line above.<br>*why:* Found while applying the number-formatting amendment to traces.rs. The corpus already owns this under number-formatting's 'Zero, unknown, and too-small are three facts' and the law unknown-is-not-a-value; this tree already has the correct habit elsewhere (render/src/schedules.rs::every and three .map(\|m\| format!("{m}ms")).unwrap_or_else(\|\| "—") sites), so the fix is to follow the file's own convention, not to invent one. Not fixed in this run because the right sentinel for a numeric column that tooling may parse is a judgment the project owns. | high | tracklight / intake:llmfit-delta |
| ~~2026-09-10~~ | software-engineering | test-harness | application | ~~When an application emits shell setup snippets, execute them in the target shell with synthetic credentials and assert the resulting environment, because clipboard and rendered-text checks cannot establish shell parsing.<br>*why:* Run 04 commit 07ee938740edc04cf7dc61ee53a4dafb35cac29a fixed an unquoted Bearer header: four setup DOM tests passed before the repair, while a real Bash execution rejected the token as an export identifier; two synthetic shell fixtures then preserved the exact endpoint and header, including metacharacters.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / maintain@1.0.0 |
| 2026-09-10 | software-engineering | repo-manifest-standard | application | When a generated tool rewrites one field in an open manifest, constrain the write to its owned block and direct field, then prove foreign, quoted and nested fields survive a real writer-reader round trip.<br>*why:* Run 05 repaired guidance projection writes and doctor --run flag updates that targeted foreign rows; real LF/CRLF fixtures prove only owned hashes and verification flags change. | high | ascent / maintain@1.0.0 |
| ~~2026-09-10~~ | software-engineering | test-harness | application | ~~When configuring Git behavior in a linked worktree, resolve the effective metadata path through Git and assert the result with Git itself, because a successfully written private metadata file may never be read.<br>*why:* Run 05 commit 4db2d3512103c5805936c5f8288ec2abee62f85b reproduced an unignored report after a successful private info/exclude write; six checkout/rule fixtures now use check-ignore and actual staging to prove report exclusion.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | high | ascent / maintain@1.0.0 |
| 2026-09-11 | software-engineering | agent-memory | technique | When a memory store's decay clause scores a last-used column, assert in a test that the column has a WRITER, because a read-only decay input degrades silently into decay-by-age and every instrument still reports health.<br>*why:* This repo's forgetting SQL scored COALESCE(last_recalled_at, created_at) and last_recalled_at had zero writers in the whole source tree. The formula, the floor, the tombstones and the tests were all correct; the feedback edge simply did not exist, and the existing test pinned the column NULL, so nothing failed. Measured after wiring it: age-only decay collapsed importance-5 memories from 13 to 2 in one sweep while use-aware decay held 11. | high | athena-everywhere / perfect@2.6.0 |
| 2026-09-11 | software-engineering | fleet-orchestration | application | A path-scoped commit (git commit --only <paths>) is not sufficient isolation in a shared checkout: it takes whole files, so a concurrent session's in-flight edit to a file in your write set is committed under your name, and if that edit references files the sibling has not yet tracked, HEAD stops importing.<br>*why:* Two /perfect builders used --only correctly and a third unrelated session was mid-edit on src/athena/cli.py. The builder's commit carried 16 lines of the sibling's new voice CLI, whose modules were still untracked, so HEAD's argument parser imports two files that are not in HEAD. Tests still passed because they never build the parser. The parallel-dispatch technique treats write-set disjointness as the collision rule; it holds only against agents you can enumerate. | medium | athena-everywhere / perfect@2.6.0 |
| 2026-09-16 | software-engineering | agent-browser-control | technique | When an embedded webview host refuses IPC from remote-origin pages, carry page-to-shell results over a loopback WebSocket authenticated by a per-tab token minted at tab creation and held only in the injected script closure, because loopback is exempt from mixed-content blocking while the host ACL is not something a page may be granted; state the residual cost that the page connect-src policy governs the channel.<br>*why:* Tauri 2.11 refuses invoke from remote-origin webviews unless an app ACL manifest gates all 1,656 commands; the ported bridge_reply command was inert. Rerouted over the existing extension-relay primitive; 92 bridge tests green; CSP cost reasoned not measured. | medium | personas / spark@1.1.0 |
| 2026-09-16 | software-engineering | metric-gates | technique | When a change retires code in a checkout that other work is also moving, ratchet a count-based gate's baseline by the delta of the retired subtree alone (measure that subtree at the change's base and at its tip in isolated roots) rather than re-baselining to the whole tree, because a whole-tree update silently absorbs everyone else's regressions into the new floor.<br>*why:* twin-atelier-v2 retired six sub-pages beside six sibling sessions: a global census --update would have baked six unrelated rising rules into the baseline; subtree measurement moved exactly 23 baselines and left the foreign rises failing where they belong. A first attempt that measured only changed files on one side faked extra drops, so both sides must carry the whole subtree. | medium | personas / spark@1.1.0 |
| 2026-09-16 | software-engineering | concurrent-vcs | technique | When a shared aggregate file (a locale catalog, a generated types file) carries a sibling session's in-flight edits, stage a composite of HEAD plus only your own section as a blob into an isolated index and regenerate derived files from that composite in a scratch working directory, because adding the file sweeps the sibling's work under your message and waiting for them blocks the commit indefinitely.<br>*why:* browser-control adjust wave: 14 locale JSON files and the generated i18n types held a sibling twin-retirement; composite blobs via hash-object --stdin --path (raw bytes, not text mode, which CRLF-rewrote whole files) landed 16-line diffs with zero foreign lines. | medium | personas / spark@1.1.0 |
| 2026-09-16 | software-engineering | credential-vault | technique | When an OAuth credential can be re-authorized, bind it to the provider's stable subject at first consent and refuse a re-consent whose subject differs before any token reaches storage, because a chooser-driven reconnect otherwise silently rebinds the row to whichever account was clicked and every automation reading that credential is misdirected.<br>*why:* Personas Google credentials carried no identity (id_token discarded, prompt=consent without login_hint); implemented id_token decode -> ledger sub/email, login_hint on reconnect, sub-check refusal that leaves the old token and the needs_reauth flag intact; 8 Rust tests. Not yet live-verified against a second account. | medium | personas / spark@1.1.0 |
| 2026-09-16 | software-engineering | web-scraping | technique | When a product must ingest third-party job boards, tier the sources by legal posture (rights-clean feed / robots-permitted but terms-forbidden / blocked) and make enabling a tier-B source an explicit, hashed owner acknowledgement of the quoted clause, because a single global consent cannot name what is being accepted and a changed clause must re-ask.<br>*why:* Built in kp WP3 (sources API + catalog with termsHash; a changed hash re-shows the acknowledgement); observed live. | medium | kp / spark@2026-09-16 |
| 2026-09-16 | software-engineering | background-jobs | application | When a job-runner hub is imported by many request routes, register heavy job implementations at process boot through a leaf registry instead of importing them from the hub, because the hub's import graph is paid by every route and a dynamic import does not escape a static-graph budget.<br>*why:* kp task hub went 272 -> 222 first-party modules on every route after moving the scan runner to boot registration (scripts/perf/check-budget.mjs counts dynamic imports). | high | kp / spark@2026-09-16 |
| 2026-09-16 | recruiting | compensation-banding-and-market-honesty | technique | When scoring a seeker's own preferences against postings, express salary and location as eligibility FLAGS with an explicit unknown state and never as a knock-out or score input, because a posting that states no pay must read as unknown and a currency mismatch as not comparable, not as a lower fit.<br>*why:* kp WP4b: MatchResult.eligibility added with identical totals pinned with and without preferences; defaulted market bands read as unknown. | medium | kp / spark@2026-09-16 |
| ~~2026-09-16~~ | software-engineering | async-ui-states | technique | ~~When a list page's emptiness depends on a chain of prerequisites the server already knows (a profile, an enabled source, a completed run), compute the chain facts server-side and skip the client fetch when the chain is broken, because the chain-aware empty state is then entitled even when the data request would have failed, and failure is reserved for the one request that actually ran.<br>*why:* kp /me/jobs: shouldFetchRows(chain) issues zero row requests on a broken chain (measured with Playwright: rowsFetched=0, data-empty-state=no_profile) while an intact chain with a forced 404 renders a first-class failure notice with Retry; both themes observed.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | kp / perfect@2.3 |
| 2026-09-16 | software-engineering | background-jobs | technique | When a periodic job re-derives a per-row result and the row already carries a version and a timestamp of the last derivation, write the skip predicate as a positive disjunction over the NULL-able columns (version IS NULL OR version != current OR derived_at IS NULL OR derived_at < input_changed_at), because NOT(version = current AND derived_at >= t) evaluates to NULL for never-derived rows and silently excludes exactly the rows that most need the work.<br>*why:* kp incremental matching (c086bfc1): the first draft with NOT() dropped never-matched postings; the positive OR form is pinned by a store test and cut an unchanged re-scan to zero spawns. | high | kp / perfect@2.3 |
| ~~2026-09-16~~ | software-engineering | design-tokens | application | ~~When a new module in a themed codebase looks wrong, audit it against a catalogue of the techniques the rest of the product already achieved (recipes with usage counts, motion presets, loading choreography laws, shell rail structure) and fix by inheritance per defect, because every 'terrible' symptom maps to an existing primitive the module bypassed and no new design decision is needed.<br>*why:* kp /me: 12 ranked defects, 7 directions, all resolved by adopting existing recipes/primitives/motion classes; zero new design vocabulary; verified live in both themes at three widths.~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | kp / perfect@2.3 |
| ~~2026-09-17~~ | software-engineering | file-browsing | technique | ~~When generating a thumbnail for a source smaller than the requested edge, re-encode at the source size instead of resizing, because generic resize helpers upscale and a thumbnail larger than its source is wasted bytes and a blurred preview.<br>*why:* drive_thumbnail on personas: image 0.25 resize(96,96) turned a 40x20 source into 96x48; pinned by test decode_to_jpeg_downsizes_and_keeps_aspect after the builder measured it~~ **RULED [[2026-09-20-1]] - per-lead verdict in the run note** | medium | personas / spark@1.1.0 |
| 2026-09-17 | software-engineering | metric-gates | technique | When a source-scanning gate keys on a literal idiom (a role attribute, a call name) and the codebase ships a helper that emits that idiom at runtime, either the gate must recognise the helper or the helper must be unusable for the case — otherwise every conformant call through the helper reads as a violation and authors route around the primitive.<br>*why:* personas census rule tabstrip-with-no-declared-panel wants a literal role=tabpanel in the same file; the SegmentedTabs primitive ships segmentedTabPanelProps() which emits it via spread, and a spread never clears the match — fixed by writing the attributes literally, which is the opposite of what the primitive teaches. | medium | personas / spark@1.1.0 |
| 2026-09-17 | software-engineering | hitl-approval | application | When an agent writes INTO a surface the user is already looking at (a web form, a terminal), make the surface's own commit control the human gate and write the decision record at insert time, not at submit time - the app never sees the final text, so the record binds to what the agent placed, labelled as such.<br>*why:* Browser Twin toolbar shipped 2026-09-17: the operator rejected an app-side draft panel; the twin fills the page input, the site's send button confirms, and twin_communications records the inserted draft (channel browser) - a second app-side confirm would have been two answers to one question. | medium | personas / spark@3 |
| 2026-09-17 | software-engineering | fleet-orchestration | technique | When the dispatch queue must survive a restart, make a queued dispatch a registry row in a pre-life state that keeps its id through promotion and spawn on that id, because every surface that was handed the id before promotion (an operator's tool result, a task row, a board tile) can still find the session afterwards.<br>*why:* Personas added FleetSessionState::Queued with promotion via FleetRegistry::adopt_spawn on the queued row's own id; the alternative (a separate intent table, new id at promotion) would have broken Athena's tool-result addressing and the Dev-runner task binding, both of which are keyed before the process exists. Verified by 16 queue tests + full gate chain on 2026-09-17. | medium | personas / spark@personas-2026-09-17 |
| 2026-09-18 | software-engineering | agent-cli-transport | technique | When a second agent CLI is proposed as the fast lane, measure cached-prefix tokens per spawn and time to first visible token under the real system prompt before designing the route, because a CLI that wins a trivial cold spawn can be several times slower once it re-reads an uncached production prompt on every spawn.<br>*why:* 66 dispatcher-scored turns: grok 1.0.34 won a trivial cold spawn by ~2 s but reached first text in 11.4 s vs 3.4 s (p50) on a 148 KB prompt, with 128-256 cached tokens per spawn against 64k for the incumbent; the matrix row that decided it was cross-spawn prompt caching. | high | personas / spark@1.1.0 |
| 2026-09-18 | software-engineering | model-routing | application | When an interactive class is slow, shrink its prompt family and keep its process warm before changing its model, because a 6x smaller certified prompt plus a persistent session moved first-token time more than any model or vendor swap measured.<br>*why:* Chat-class family 25k vs 154k chars certified with no class drop (76.3% -> 89.5% pass, 38 scenarios x 2); warm turn 2 first text 2.2 s vs 4.4 s cold on the same model; the vendor swap was 3x slower. | medium | personas / spark@1.1.0 |
| 2026-09-18 | software-engineering | structured-output | technique | When an LLM must return several DIVERSE candidates on a closed numeric domain, draw the anchor vectors in code (pins, coherence, pairwise spread, avoid-seen) and let the model move each value by at most one step, then validate each candidate against its own anchor at the door; asking the model for randomness collapses to one mode and a door cannot check diversity it never specified.<br>*why:* twin-presets style roll: Rust sampler with a 1,000-seed property test (pins, coherence, spread>=4, avoid) + a roll door that path-addresses any dim outside anchor+-1; the door can only enforce what the anchors made explicit. | medium | personas / spark@1.1.0 |
| 2026-09-18 | recruiting | ai-interviewer-brief-authoring | technique | When a server directs a live automated interview, make the interviewer EARN each coverage mark by quoting the candidate, and verify the quote against the persisted turns before accepting it - because a model asked to self-report coverage will mark a topic covered on its own summary, and the scorecard then cites evidence the candidate never gave.<br>*why:* We shipped mark_topic_covered(block_id, evidence_quote) over a director loop: the quote is fuzzy-matched against persisted candidate turns (>=3 words, span across adjacent turns allowed); a miss returns a refusal telling the model to ask one narrower question for a concrete instance. It is deterministic, needs no second model call, and it turns coverage-first pacing into something the record can prove. | medium | kp / spark@ai-interview-parity |
| 2026-09-18 | recruiting | candidate-consent-and-retention | technique | A retention promise needs a READ-TIME gate as well as a sweep: the door that serves the retained artifact re-checks the same predicate, so a stopped scheduler cannot keep serving data past the date the candidate was promised.<br>*why:* Our nightly recording-retention job and the recruiter playback route share one predicate (decision+30d, 180d backstop). A deployment whose clock job never runs still refuses the audio on read, which is the promise the candidate was actually given. | high | kp / spark@ai-interview-parity |
| 2026-09-18 | recruiting | conversational-assessment-validation | technique | When coverage evidence is tied to a topic boundary the AI interviewer announces itself (a begin-topic call), measure how often evidence crosses that boundary before tightening any rule on it - because the interviewer routinely asks a topic's question before it announces the topic, so a same-topic evidence rule refuses true answers.<br>*why:* A same-block rule (a coverage quote must come from a turn recorded while its block was active) was built after one transcript and re-run in the simulator: it refused true evidence 3/3 and coverage fell 6/6 -> 2/6. Withdrawn; the verdict package then measured 20 accepted covers: 19 same-block, 1 late announcement, 0 taken from another topic. | medium | kp / spark@interview-uat-tranche |
| 2026-09-20 | software-engineering | entity-lifecycle | technique | When a companion entity is auto-created per parent, key it on the parent id from the first commit - a derived NAME as the lookup key silently orphans the child on a parent rename and mints a duplicate on the next lookup, and neither failure raises anything.<br>*why:* A cross-project team per workspace was minted and re-found by a formatted name string; a rename orphaned it and the next adoption created a second one. Replacing the key with a real column plus a partial unique index made the duplicate unrepresentable rather than merely unlikely, and the backfill adopted the live name-keyed row instead of duplicating it. | high | personas / spark@1.1.0 |
| 2026-09-20 | software-engineering | data-access | technique | A door that writes an enum-constrained column must be verified against the constraint, not only against its callers - a CHECK violation on a secondary INSERT surfaces as a logged note, so the door reads as working for as long as nobody queries the row it failed to write.<br>*why:* A workspace-bound adoption passed a human label into a role column constrained to four values; that INSERT had failed on every such adoption ever made and was found only because a new test asserted the membership row existed. | high | personas / spark@1.1.0 |

## Banked 2026-09-20 — the tail, with a named home

The drain ([[2026-09-20-1]]) took the sixteen largest clusters, 110 leads. The 85 rows
still open above are a genuine long tail: 60 distinct keys, the largest holding four.
A worker per subject stops paying for itself at one or two leads, so the next pass should
batch by BUNDLE, not by subject.

Eight leads arrived with no `nearest` subject at all. The router does not rescue them —
asked to place "throttle a public capability link per token", it returns `design-tokens`
at score 22, which is the corpus-reachability problem in one line. Homes assigned by
reading, and owed to a writer:

| claim | home |
| --- | --- |
| a UI mode default computed from live decision state must freeze when the surface opens | `ui-controls` |
| a boolean control with a setter, a column and a display but no enforcement site is a label | `invariant-placement` |
| a public capability-link route throttles per token AND client before the token lookup | `rate-limiting` |
| a corrupt-vs-missing distinction is worthless while another reader parses the same file with a fallback | `error-handling` |
| a spring/follow-value hook fed a plain number needs every target pushed through `.set()` | `motion` (drained; land as an amendment) |
| an ORDER BY with no deterministic tie-break is a latent correctness bug wherever a reader takes "the latest row" | `data-access` |
| a tolerant parse degrading an unknown enum must not be shared between a READ path and a WRITE path | `bounded-enumeration` — and note `graded-case-difficulty` landed the same rule this run from the other end |
| an absent correlation statistic rendering as `0.000` is a definite value | `number-formatting` — the lead itself says the corpus already owns this; verify and strike |

Three further unrouted leads (a test NAME asserting a guard the code never performed;
confirming the FILE ACTUALLY CHANGED before believing a falsification; a regression test
being falsifiable against the FIX) were claimed and landed by the `test-harness` writer in
this run. Their rows above are struck.
| 2026-09-20 | software-engineering | quality-gates | technique | When a gated party writes the result file a door ingests, the door recomputes every derived number and the verdict from the raw dimensions plus a pinned rubric and refuses a file whose own numbers disagree, because a stated verdict is self-reported gate input even when every field validates.<br>*why:* The council ingest door (Rust) recomputes overall, coverage and outcome and refuses on a 1e-6 disagreement after rounding to 4 dp; the skill aggregate and the door were pinned to one digest vector and one outcome ordering, and a contract difference (round cap outranks hard failure) only surfaced because both sides were forced to agree. | medium | personas / spark@1.5.0 |
| 2026-09-20 | software-engineering | table | technique | When a table infers a column's start direction from its data rather than from a declared column model, sample the first row that HAS a value, because a missing cell carries no type and sampling row 0 opens a numeric column in the wrong direction.<br>*why:* kp app/_components/table/useTableSort.ts:88 initialDir skips missing cells and its comment records the shipped bug it fixed - an Economics board whose top channel had no spend entered sampled null, read "not a number", and opened the Spend column cheapest-first, the opposite of what the click meant. sorting's "compare by type" assumes a declared column model and does not name the inference hazard; where the type is inferred it has the same missing-value problem the comparator already guards. | medium | kp / conform@1.6.0 |
