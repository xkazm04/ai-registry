# The source ledger

Every external source [`/intake`](../../.claude/skills/intake/SKILL.md) has mined, one
line each, newest first. (`/deepen` may also write here when it is handed a URL; the
ledger does not care which skill mined a source, only that one did.) One note per run sits beside this file.

This ledger answers one question in one second: **has this been mined already?** A
re-ingested video costs a full extraction round to rediscover declines that are already
written down, and the answer to "did we look at this" is not something to reconstruct
from memory.

## What a source note holds

The candidates a source produced and what happened to each - accepted, declined with a
reason, banked as a lead with a return condition, or caught as already covered. The
declines are the most valuable part. A decline nobody wrote down gets re-proposed every
run forever, and a source class nobody characterised gets over-trusted every run
forever.

## What it never holds

A consumer's paths, repository internals, or the transcript itself. Transcripts are
run inputs; they live in a scratch directory outside the repository and are deleted
when the run ends. A note quotes an anchor, never a corpus.

## Mined

| Date | Source | Kind | Words | Extracted | Accepted | Leads | Caught | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-04 | `github:earendil-works/pi` @ `744a94d` | **vendor repository, sub-shape design-deep system** - and the sub-shape is the finding. The class table predicts "its docs' rules page and its client's types"; what this tree carries is a **27,820-word document that calls itself normative** plus **ten numbered work packages** that are ADRs in everything but name (problem, trace, decision, and an explicit list of what the package does NOT add), a 38-item invariant list, and a 28-row race catalog naming both durable orders per race. Round 10's lesson said operating documents may live inside source files when there is no `ADR/`; this says the other half - **when a repository ships a document that calls itself normative, that document IS the source and the README is not a summary of it**. Landing page 990 words against 324,241 in-tree. The **Phase 2d count was written before extraction and re-aimed it**: four systems, `corpus: NONE` counts of 4/1/0/1, and the 0 is the tree's most prominent half (a 30-provider unified LLM API, the biggest single README in the repo) - it was classified and then closed, and the whole pass went to documents the landing page does not link. Both landings came from there. Also: **a `benchmarks/` directory ranks first only when it publishes RESULTS** - this one publishes an honest protocol and registers exactly one target, so it ranked with the other operating documents and paid nothing; the discriminator costs one `ls`. **Re-scan when** the assistant-output handoff lands (it is the one part of the spec marked incomplete), or 2026-12-04 | 990 landing / 324,241 in-tree | 12 | 2 | 3 | 2 | 5 | [[2026-09-04-pi]] - **1 new subject (6 techniques), 1 amendment, 1 source-tree application, 7 applied rows (0c/0e/4s/1t + 2 unapplied behind 2 preconditions), 1 project commit, 0 of 3 fetches.** The subject fired the mechanical XL trigger and was forged in-session; its placement MOVED because the merits-correct category holds exactly 10 subjects against `MAX_CHILD_DIRS = 10` - verified against `taxonomy.json` as the authority, not a folder count. Both candidate homes disclaim the ground in their own words. The amendment refuted a four-case enumeration **corpus-internally at zero fetch cost**: the technique says a model request is side-effect-free and "re-issuing costs nothing", while its own bundle's spend subject counts mid-stream cancellations among billed units and lists a killed call recorded at cost zero as a defect. **A seam defect was found and is reported as its own outcome**, and the apply step's falsifier was RUN rather than deferred. |
| 2026-09-04 | `worldlabs.ai/blog/atlas` - "Atlas: A World Model for Spatial Intelligence" (2026-09-01) | **vendor release announcement** (first-party - the team that built it). Mined for its *protocol*, not its model: the highest-yield paragraph was the one hedging its own benchmark. **Design read run against the Technical Details section** (no tree, so anchors are section names): 5 decisions, routing count **2** with no shared home-if-new - below both thresholds, so no forge handoff and no XL trigger. **Re-scan when** the model leaves early access and a third party publishes a camera-control comparison it did not design; or when a fleet project grows a reconstruction surface, which is the return condition on this run's unapplied technique | 2,835 | 12 | 2 | 3 | 4 | [[2026-09-04-worldlabs-atlas]] - **0 of 3 fetches spent**, per the class rule: a first-party account corroborates corpus-internally, and both findings did (one against a law this corpus already holds, one against real code in a connected tree). **2 techniques.** `input-channel-parity` (generated-output-grading): the source benchmarks its native camera input against baselines handed the same path *as a sentence*, discloses the confound itself, and reports its lead **growing with trajectory complexity**. The corpus owned the design half as a law (`typed-input-owns-its-channel`) and nothing owned the measurement half - `cross-provider-flip-analysis` declares "the diff is only valid if the generator was the only variable" and that enumeration does not contain the difference that **cannot be removed**. `anchor-removal-ablation` (evidence-bound-visuals): the source's three-image progression read backwards is an instrument - the invented region is the region that changes when you remove an anchor - for the case `epistemic-draw-routing` cannot reach, where grounded and invented content are interleaved at the pixel with no layer to route by. **All three `partial` rows moved under the v2 promoting question**, two of them downward: a currency row became a bundle-scope lead (a counted grep found media-generation carries **zero** files on novel-view synthesis, splats or reconstruction - no clock to reset because nothing is there), and a lead became a catch (`one-authority-per-quantity` already models the world-and-observer forces). **Apply 2/2 owed, 1 shipped**: `code`/`better` into gravity, where the tree had independently built this technique's step 1 for one control dimension and the second dimension inverts across the same provider pair; the other is **unapplied for want of a seam in any of the eight authorized trees**, recorded as a state rather than a zero. Directions 0/0 - every fleet-map absence for both subjects classified `out-of-domain`. Gate was red mid-run on five sibling files under `llm-agent/`; reported, not fixed; green by Phase 9 |
| 2026-09-04 | `github:sozu-proxy/sozu` @ `cd02310` - "Sozu - hot-reconfigurable HTTP/1.x + HTTP/2 reverse proxy, built in Rust" | **first-party practitioner account in repository form** (a production proxy on the network's critical path; 61,681 words of design docs over ~175k lines of Rust in 6 crates; operator focus "Rust coding proficiency"). **Round 23 of the 2.x series, first on 2.5.0; three Explore-class verification workers, director Opus.** | 1,465 landing / 61,681 in-tree design docs (27 files) + 2,399 agent-instruction + 45,339 changelog; ratio 1:42; README read last, contributed nothing | 13 (9 design + 4 claim) | 5 | 1 | 8 | [[2026-09-04-sozu]] - **the routing count fired NEITHER clause** (per system A=1, B=1, D=0; no three entries sharing a HOME-IF-NEW), so no forge handoff: a 175k-line system produced **0 subjects and 0 techniques** because the corpus already models its ground, and all five landings are boundaries on mechanisms it already owns. **5 amendments**: `single-source-topology` (the link regime's platform precondition - **refuted in this run's own checkout**, where a `120000` index mode materialised as a 9-byte file reading `CLAUDE.md` under `core.symlinks=false`, against six fleet repos that all use import bridges that all resolve); `inside-out-invariants` (the severity ladder inverts where an adversary chooses the bytes - "stops" is then the attacker's selected outcome; the two-regime debug/release split, with the rule that you gate the assertion and never the assignment beside it); `swarm-feature-sampling` (the MANDATORY/SUPPRESSOR/OPTIONAL sort, where omitting a mandatory feature yields a **vacuous** green run, plus the coverage gate moving to the merged campaign tally) with a one-sentence discriminator landed on the opposite side in `blocking-by-input-determinism`, which had stated "a gate is the all-of-N position by definition"; `ratchet-design` (the blocking/advisory split by population rather than direction, and the **retention case** - resolving a standing corpus contradiction with a third independent instance); `instruction-freshness` (a sixth audit check: the file agrees with itself, the only check on that list that does not compare a line against the tree). 3 source-tree applications. **8 catches, and they are the honest bulk** - `io-free-core`, assertion density, swarm's core mechanism, seeds-are-not-reproductions, the dual-consumer detector (`gate-laddering:188-195`), which rules go unmechanised (`prose-rule-drift:46-49`), the symlink default itself, and the non-derivable-half principle. 3 untriaged with anchors, 1 lead. **apply 5/5, ship 0** - every row `not-better` or `unapplied by construction`, each verified against a tree rather than assumed; the peer study retired explicitly (no fleet project is a peer of a reverse proxy). 0 of 3 fetches. 1 sibling at claim holding `serving-process-topology`, which is this source's worker-topology home - deliberately not pursued |
| 2026-09-04 | `web:langchain.com/blog` (trigger) → `github:modelcontextprotocol/modelcontextprotocol` @ `e76e9c5` | **vendor release announcement routed to the standard it describes** — the operator's instruction was to master the protocol rather than the framework, so the 1,372-word post was read once as a trigger and the specification repository became the source: 800,505 words of markdown with `seps/` as its ADR directory (43 proposals, each carrying Motivation, Alternatives Considered and Backwards Compatibility). **Re-scan when** the conformance repository becomes visible from the spec tree; or the SDK tier table publishes a per-SDK assignment and a first relegation; or the tasks/apps extensions return to core; or 2026-11-27 | 1,372 (blog) / 800,505 in-tree md, ~106,000 read across five parallel readers | 43 design + 12 scored | 12 | 3 | 9 already covered / 13 untriaged | [[2026-09-04-mcp-specification]] — **the run where the thing that was wrong was the corpus.** `mcp-tools` claimed to document the 2026-07-28 revision and reproduced the **2025-06-18** protocol in six load-bearing places, each verified by the director against normative text, the wire schema, and the diff across three published revisions in one checkout. The provenance is the method finding: that section was written by round 5's mine of a large vendor *implementation catalog* — **mining implementations of a standard teaches you its architecture; only the standard teaches you its rules.** The corrections: input-validation errors are NOT protocol errors (the change landed in 2025-11-25, fifteen months before the revision we claim to document) and the real discriminator is **who can act on the answer**, proven by the same failure changing channel between a user-invoked prompt and a model-invoked tool; servers are not requesters (no `ServerRequest` union exists — control: 1 hit in the 2025-11-25 schema); three capabilities were deprecated not two, and logging is a SERVER capability while the omitted roots is the client one; a caller-varying catalog CAN be cached, per caller; one of four not-approved consent outcomes moved to the protocol channel; and a technique's stated reason rested on the first error. **3 new techniques**, one story told three times — statelessness removed the container and everything leaning on it had to be re-derived: `suspendable-request-classes` (only application semantics may pause; the protocol's own metadata surface must answer by machine or nothing bootstraps, caches or proxies — partition total at 3 invoke / 6 metadata / 1 stream over the complete request union), `sealed-continuation-state` (**a carrier is not a reference** — binding a handle to a principal works because the server stored the handle, and there is nothing stored to bind a carrier to, so integrity protection is mandatory rather than good practice; and the seal still cannot buy single-use, because that is a claim about history and history is the record you were avoiding), `enumeration-without-a-scope` (recognising one handle needs no cross-call caller identity and correlating two needs exactly that — so delete the operation rather than documenting the obligation, which makes the leak unrepresentable instead of merely discouraged). **3 amendments**: deterministic list ordering is a prompt-cache determinant the server alone controls and the client alone pays for; a discovery response carries a **sharing** scope that is never an access control and whose failure neither endpoint can detect; and `release-pipeline/deprecation-by-version-arithmetic` gains the regime where the declarer cannot execute the removal (so `removed` degrades from a promise to an eligibility floor, paid for by a registry, an announcement channel, and a marker obligation delegated to implementations that do have a runtime), plus the hole nobody had named — **a deprecation window only warns someone whose upgrade step is smaller than the window**. **Applied 5 rows: 1 code/better/ab-paired and shipped, 2 simulation, 2 unapplied with stated reasons.** pumper: `tools_call` never checked arguments against the schema it publishes, and the test was written and run BEFORE the change so Arm A is measured — a misspelled key returned `isError:false` and the tool answered confidently without it. **The tree then corrected the finding**: the handlers DO validate by hand and already used the in-band channel correctly, so the defect is drift between the set enforced and the set published, not absent validation. Routing count 21 whole-tree, 5/4/5/4/4 per system, every system clearing three — and **the handoff was declined on both clauses**: `runtime-and-io` sits at exactly `MAX_CHILD_DIRS` (10) so V1 vetoes a new subject mechanically, and 18 of 21 home into `mcp-tools` anyway. One candidate died in verification (pumper's `-32002` is correct for the revision it actually advertises). **0 of 3 fetches.** 2 siblings by Phase 9, one holding `mcp-tools`; index and catalog regenerated under the lock and left uncommitted over its WIP |
| 2026-09-04 | `github:fluxerapp/fluxer` @ `baa18aed` - "free and open source instant messaging and VoIP chat app" | **first-party practitioner codebase read as a SYSTEM** - a polyglot real-time chat platform: an Erlang/OTP gateway, a TypeScript API over a Cassandra-or-Postgres seam, a 685k-line client with a Rust/WASM core, a Rust edge/media plane, an Electron shell, and ~28k lines of Rust build tooling. The prose-to-code ratio is the routing signal and it is inverted: ~21,000 words of markdown against 1,352,689 lines, about one word per 64 lines, so the landing page is 24 lines of README and a claim-hunting pass returns badges. **Re-scan when** the desktop client gains self-hosted-instance support (issue #1088, the operator guide's stated gap); or the LLM localization pipeline grows any cost accounting (today it provably has none - no spend cap, no usage counter, 33 locales); or the gateway's production vm.args regains the partition flag its development twin carries; or 8 weeks elapse | 335 landing / ~21,000 in-tree md over 14,093 files / 1,352,689 lines | 14 (all design; 47 decisions in the record) | 4 | 2 | 4 | 9 untriaged | [[2026-09-04-fluxer]] - **the second 2.x round where the routing count is met and the answer is NOT a forge, and the first where the cross-system cluster was refuted by the corpus rather than by a count.** 47 decisions across 6 systems, per-system NONE 3/2/3/3/1/2 - no system clearing three against a *new* home, and the whole-tree cluster (forked-policy drift, four independent sightings) homes into `quality-gates`, which owns it precisely (`policy-projection`, `chokepoint-tag-registry`, `gate-laddering`). So: technique triple inside `admission-queue`. **3 techniques** (`zero-depth-admission` - the missing stage zero in a subject that explicitly surfaces its first two assumptions and never its third; `remediation-derived-bounds`; `refusal-without-release`), **1 amendment** (`admission-vocabulary`: two arms is correct where there is no waiting room), **5 applications**. **The apply step refuted the run's own headline technique** - a fleet peer that chose the opposite bound ran it on its real admission path and zero depth halved useful throughput (24 vs 41); the sweep that followed located the crossover and showed the corpus's existing wait-time formula had predicted it all along, so the technique was rewritten to the arithmetic *before* landing rather than shipped and amended. A second self-correction the same way: a claim that a clipped control signal puts a replica ceiling out of reach is false for an iterating controller (it costs convergence time, not range), caught by evaluating the peer's own generator. Two of five design readers refuted the director's brief and both refutations beat the questions. Prior-art mapping ran the round-9 check and the two queries **disagreed**, which was the signal: the AI-contribution-policy candidate is a capacity defence, not a provenance regime. One verified defect banked as a lead: the gateway's production vm.args omits `prevent_overlapping_partitions false` that its development twin carries, against OTP 28 where the default kills nodes - and the same repository holds the cure in another language (a text-level cross-implementation parity test) and never applied it here. 0 of 3 fetches |
| 2026-09-04 | `github:Sylinko/Everywhere` @ `a4345a5` - "on-screen aware desktop AI assistant" | **first-party practitioner account, repository form - design-deep**: five numbered design series (ScreenPicker is a 4-chapter *narrative* with every rejected approach and its failure written down), and the ingest returned 3% of it. Routing count 13 unhomed decisions across 6 systems; the two large clusters are 3 and 5, **both above threshold and both naming an EXISTING subject as their home**, so the XL trigger did not fire and no forge was dispatched - a count that is a statement about how well `native-shell-integration` and `terminal-multiplexing` were scoped. **Re-scan when** a second source shows the protocol-vs-heuristic tiering (System B), or when the corpus grows a home for the dependency-patching mechanism ladder (System E, whose driving force - runtime code modification trips antivirus heuristics and cannot inherit the app signature - returns zero across the corpus) | 1,279 landing / **39,237 in-tree design docs** | 13 | **1 amendment / 2 applications** | 0 declined | 0 | [[2026-09-04-everywhere]] - operator picked one row: `tool-schema-design` says *"Formats, ranges, enums and unconditional requiredness stay in the schema"* and names conditional requiredness as **"the one constraint that leaves the schema"** - an enumeration with one member, and the source is a second: a transformer that must strip exactly formats and ranges because the target endpoint accepts a narrower subset than the schema was authored in, where "keep it in the schema" means enforced by nobody *and* invisible to the model. Amendment adds the transport-rung ladder (exact lowering keeps enforcement; demotion into prose where it cannot; silence as the outcome to make impossible). **The apply row is the round-9 focus item landing**: tracklight was ahead of the registry - it demotes as house style, and unlike the source it carries the *reason* with the bound ("clamped to 4..=90 - below the evidence floor a trend cannot be presented"), which became the amendment's second paragraph. Audited 64 tools / 203 params / 6 keywords; exactly 1 param had a server-enforced bound reaching neither channel (`baseline_score` 0..1, described with no unit, so a caller thinking in percentages sends 85). A=1 B=0, gates green both arms, **shipped**. Structural fact the tree proves unbuilt-for: the pinned tool contract covers names/types/required and **not descriptions**, so a surface that demotes as policy routes its constraints into the one place with no drift guard |
| 2026-09-03 | `github:vllm-project/vllm` @ `facd9a7` | **vendor repository (inference engine)** - a SYSTEM, not a report. Ingest returned a 620-word landing page; the source is `docs/design/` (29 documents, 50,830 words) plus a 9,100-word security posture and a deprecation policy. The run 2.0.0 was written for: Phase 2d routing count 8 systems / 26 load-bearing decisions / **24 with no corpus home**, six systems clearing three-or-more independently - the largest count this skill has produced. Expected yield stated before triage: forge-shaped, not amendment-shaped. Held. The corpus had THREE files mentioning this class of cache beforehand, all client-side. **Re-scan when** the logits-processor API stabilizes (the source flags it as changing), or the hidden-metrics flag gains a reader, or 2026-11-03 | 620 landing / 50,830 design + 51,560 features + ~9,100 security | 26 | 8 | 3 | 1 | 0 untriaged | [[2026-09-03-vllm]] - **six subjects, 25 techniques, 17 applications, two amendments, five applied rows, one direction accepted and executed, zero fetches.** A new TOP-LEVEL category would have failed the gate (software-engineering sits at exactly 10, the cap), so the four serving subjects went into a new `backend-platform/inference-serving` subcategory - caught by reading taxonomy.json as the authority before dispatching. Three defects found that the source's own documents do not admit, each verified in code by the director rather than taken from a worker's report: a lease sweep defeated by its own renewal (monotonic write-through does not reorder an insertion-ordered map, so a renewed head shields expired entries - safe direction, but worst-case retention stops being bounded by the sweep period, which is the property the design exists to provide); a deprecation stage plumbed end to end and read nowhere; and a design document whose interval attribution rule is the opposite of its code's. Three of six workers had their expert draft CORRECTED by the tree rather than confirmed, and one corrected the director's design record (eviction ordering was described a level too coarsely - the queue is already coarse recency and reverse release is the tie-break within one release; non-cacheable blocks are prepended, and the release call takes an ordered sequence whose order IS the priority). Apply phase reshaped the direction pass: three ranked candidates were REFUTED by reading the trees (a fleet observability service already publishes counters and declines a scrape endpoint on the record; a fleet scraper already validates the lease invariant the source only states in prose, and already inverts its exposed-surface default). The valuable row ran upward - a fleet tree's fenced relay lease supplied two refinements the standard lacked, both taken into the technique. One direction proposed, not three; padding it would have been the feature-request generator the method warns about. index.json/catalog.json deliberately left uncommitted: two sibling runs held WIP the regenerated index referenced |
| 2026-09-01 | `youtube:6Ytluv_DiSo` — "I Built a Whole Game Level Using Only FREE AI" (Stefan 3D AI) | **practitioner build-walkthrough**, and the most tour-half-dominant one this ledger holds — roughly 85% of the runtime is which button in which tool, and it yielded nothing but proper nouns and catches. The class rule (*is the creator describing what the tool does, or what happened to them while using it?*) decided every row: the single landing came from one offhand aside, and six candidates were verified already-covered and covered **better** by `asset-production`, which holds ten subjects over exactly this territory. Expected yield stated as **low** before the triage table; actual 1 landing, 2 leads, 6 catches, 3 untriaged | 5616 | 12 | 1 | 2 | 6 | [[2026-09-01-stefan3d-free-ai-level]] — **one amendment, corroborated corpus-internally at zero fetches, and the corroboration is the finding.** `placeholder-is-not-an-asset` (generative-artifact-gating) ruled that a deterministic stand-in defers because *"the generator must run"*, and its golden path denied the symmetry outright — a generated asset *"is the only thing that can carry the line forward"*. The source is a tool tutorial that states the counter-case offhandedly while demonstrating it (a rope built from a curve rather than generated, *"five times faster"*, better quality) and never generalizes past ropes. What refutes the corpus is **two doors down in the same bundle**: `balance-validation/procedural-level-planning` ships a locally computed, seed-reproducible, **terminal** artifact, and its `seed-determinism-contract` is the evidence-of-work record that a generation history is for a generated asset. Two subjects of one bundle disagreeing about whether deterministic output can be finished work is stronger than anything the video could have said, and it cost no fetch. **The run corrected its own first reading twice.** (1) It wrote this up as a missing exception; the technique already carried one (*"Where the stand-in is the deliverable"*), found only by opening the file instead of trusting the map summary — but all three of its examples are a slot **accepting a lesser thing**, and the disjointness rule (*a real asset is a served reference, a stand-in is a locally computed value*) leaves a finished construction with no representation at all. The gap was a **third origin** (generated | constructed | stand-in), not a fourth exception. (2) The apply instrument lied first: a mutation probe reported four insensitive gates in `pof`, and all four were the probe's fault — it mutated every value at once (which leaves a ratio-reading predicate invariant) and walked only the top level (never reaching a nested `stats` object). Corrected, it reports **13/13 sensitive, 0 insensitive**, and shipping the first number would have indicted four working gates. **Applied `experiment`/`better`, shipped `pof@3de20873`** (master, pathspec, not pushed): of pof's 13 Items steps the two-valued origin classifies **2** correctly and would defer the other 11 to a generator that does not exist for them; the three-valued origin classifies **13**. The tree implements the three origins without naming them — a structural fact nobody designed. Seam was **predicted at triage** from `loadFleet()` + `.ai/registry-map.json` and held. 2 siblings live at claim, 4 by Phase 9, none holding either subject; index and catalog **deliberately left uncommitted** — they referenced two siblings' uncommitted applications |
| 2026-09-01 | `github:kunchenguid/firstmate` @ `a5f3cbe` | **OSS tool repository behaving as a first-party practitioner account** - and the most extreme landing-to-tree ratio this ledger holds: **2,434 rendered words against 174,027 words of in-tree markdown**, 76:1. The class does not predict its `docs/verification/` lane: eight documents whose job is recording what was measured, against which version, on which date, and **what remains uncovered**. That lane produced the entire yield; a run that mined the ingest would have mined an advertisement. Expected yield stated before triage as above the class norm, 3-5, mostly amendments | 2434 landing / 174027 in-tree | 14 | 4 | 2 | 2 | 8 untriaged | [[2026-09-01-firstmate]] - **two techniques, two amendments, and three of the four came from an enumeration declaring its own completeness.** `context-reset-redelivery` (agent-instruction-files): the subject's opening sentence says the harness injects the file at session start and all eleven of its techniques audit **the file**; none audits the **copy**. `instruction-freshness` couples the file to repo change, `substrate-coupled-expiry` adds the model axis, and a third divergence leaves the file innocent - the agent holds a snapshot, and a clear or compaction re-delivers what the injector cached. **`compact` returns zero prior art corpus-wide**; every hit is an unrelated sense of the word. The expensive case is not a new rule ignored but a *correction* ignored, minted mid-session into a file the agent will not re-read; the signature is an instruction obeyed early in a session and not late. `unmeasurable-criteria` amendment (quality-gates): the three honest resolutions are right for a gate standing **beside** the work and one branch inverts for a gate standing **in** it - when the remedy path runs through the capability the gate controls, fail-closed on a dead instrument is a deadlock whose exit is outside the system, and the pairing is what keeps it safe (it may open on its own breakage only because it still closes on every input it can see and dislikes). `child-observed-posture` amendment (agent-cli-transport): its three seams plus a fourth are **all read from the child's setup**; context *arrival* is a seam whose only witness is the model, and the subject already half-knows it in the suppressing direction only - `generate` mode's neutral cwd exists *so the tool loads no ambient instructions*, while the inverse case gets no probe and no row. `self-reported-gate-inputs` (quality-gates): a **fifth** way a gate is unfireable beside the three in `severity-by-construction` and the derived condition in `vacuous-by-evaluation` - severity real, exit code dependent, evaluation honest, and the input supplied by the thing being judged; the recovery instinct fails because no check keyed on the record can detect the record's absence. **Applied 4/4, all `better`, 1:1 with landings, every row two-armed.** The caching-injector arms delivered `RULE-V1` twice against `RULE-V1`/`RULE-V2`, with **all four opens producing an identical producer signal**; the delivery probe ran the same hook, same bytes, same exit 0, one variable (stream) and got the token back vs `NO_TOKEN`; the board arms returned `clear: no live sibling holds 1 target(s)` over a live unclaimed writer - **the exact bytes the gate emits when the board is genuinely empty**. **The apply refuted the run's own amendment before commit** for the second consecutive run: a fleet turn-end hook already ships three exit codes with could-not-check routed to the operator as non-blocking, so the draft's "fail-open, silently" was rewritten to **open to the actor, loud to the operator, on a code of its own** - silence being exactly what `absent-guard-is-loud` forbids. **0 of 3 fetches**, fourteenth consecutive zero-fetch run. Ship 0, and **three of four are `declined: no change warranted` rather than blocked** - no project holds the caching defect, the turn-end hook already implements the amendment (which is why it could validate it), and the probe is an instrument not a change; the fourth is a live decision with the operator. Two catches (version-as-evidence -> `dated-capability-matrix`; refresh-command-travels -> `derivation-names-recomputation`), two leads (load-time exposure independent of the toggle - no clean home; defect-record form with a stated falsifier), 8 untriaged with anchors. 2 siblings live at Phase 1, neither holding a routed subject |
| 2026-09-01 | `github:glukicov/slideops` @ `66af7de` | **practitioner repository, mined under an INVERTED rule** — the operator scoped the run to the README as an *artifact of form*, so the one file Phase 2b tells you to distrust became the primary source, and the tree supplied the evidence that the form was engineered rather than stumbled into. Worth recording as a class note: whenever the question is about a document's form, that inversion holds. **The corpus had no prior art for `readme` at all** and the four nearest neighbours each owned a different artifact — a new subject, `repository-landing-document`, forged this session (golden path + 7 techniques + 2 applications). One technique landed elsewhere: `docs-sync/rendered-surface-coupling`, an **asymmetry between two subjects the corpus already held** — a rendered figure is disqualified by `codegen/drift-gating` (rasterization is not byte-stable) and by `doc-rot-detection` (an image cannot be read) for *different* reasons, so both owners correctly disclaim it. **0 of 3 fetches**, fifth consecutive first-party-codebase run to spend none. Two self-corrections outrank the landings: a hand-count that credited a project with 7 images (they were badge images) was caught by the instrument written in the same run, and a draft counter that read the fleet's worst prose run at 39 lines was reconciled to the rule's own closed break set and read 96 — which invalidated the threshold argument the forge worker had written from the draft numbers, so the subject's measurements and its defence of fifteen were rewritten. The reference repository now fails the rule too, at nineteen, and the subject says so. | 1033 landing / ~2470 in-tree | 8 | 7 | 1 | 1 | Applied 1 (gravity, experiment, better: findings 3→0, words 2089→581, routed 0→7, prose run 90→8); 1 unapplied (no managed project has a single authored documentation figure). Ship 0 — mode capped at experiment by foreign WIP in the authorized tree's README, not by authorization. |
| 2026-08-31 | `github:semantica-agi/semantica` @ `f3c540c` | **vendor repository (hybrid)** — the README is marketing, the hand-curated 317 KB CHANGELOG is a first-party practitioner account recording **root causes**; GROWTH.md says why, as a *considered and deliberately skipped* checklist item declining a changelog bot. Densest file in the tree is `.github/requirements/README.md`, which states mechanisms rather than rules. **The corpus named one gap and declined it in a single line** — `absent-degrades-malformed-fails-fast` says "malformed is not the dependency rejected it — that is a runtime fact and a different technique", and that technique did not exist until this run. 6 landings (5 techniques + 1 amendment), **0 of 3 fetches**, 4th consecutive first-party-codebase run to spend none. Phase 7.5 ran 6 for 6: two `better` on real two-arm tests (a conformance gate scoring **byte-identical** with its manifest pointer block deleted; 3 unpinned installs of a dependency-policy engine), two `not-better` that each **corrected the technique they tested** (a consumer implementing the SSRF guard in a stronger form supplied the missing rule that a pre-flight check covers what DNS never sees; a strict-install command turned out to already be the lockfile oracle), one `better` from a lint plugin whose two rules miss the idiom between them, and one honest **unapplied** — no fleet project serves a stdio protocol, and a simulation was declined rather than invented. Index NOT regenerated: siblings landed content mid-run | 9060 landing / ~1.0 MB in-tree | 18 | 6 | 5 | 3 | 9 untriaged |
| 2026-08-31 | `github:nibzard/awesome-agentic-patterns` - curated agentic pattern catalogue | **curated pattern catalogue with a machine-generated research substrate** (queue graded it "paper aggregator / reference index" - CORRECTED at Phase 2c: 5,080 links over ~996k words is a code-repository ratio, so no waves were run) | 1748 page / ~996k tree | 11 | 2 | 1 | 1 | [[2026-08-31-awesome-agentic-patterns]] - the yield was its OWN verification artifacts: identifiers 413 checked/0 fabricated vs venue-year 617 checked/36 fabricated, so the attribute half is where invention lives and the identifier half is what every gate already checks |
| 2026-08-31 | `github:BurntSushi/ripgrep` @ `3fce3b5` | **first-party practitioner account (repository form)** - one expert maintainer, a decade of operating documents. The class prediction held exactly: corroborates corpus-internally, **0 of 3 fetches spent**, yield concentrated in amendments to mature subjects rather than in new ones | 2875 landing page / ~37000 in tree | 12 | 2 | 0 | 0 | 10 untriaged | [[2026-08-31-ripgrep]] - swept in yield order: GUIDE (6408), FAQ (6409), CHANGELOG (9642), crate API docs, the completion-drift gate, twelve dated benchmark result sets, 110 issue-numbered regression tests, README last. **Landing 1** - `codebase-scanning` models the *sensor* denominator with four measures and gives the *file population* denominator one sentence inside a foreign-tree-only section, so a sweep can report twelve of twelve sensors and zero findings over a tree whose traversal removed a third of the files, and every clause is true. The source's second chapter is what it *doesn't* search - three ignore layers in strict precedence, hidden files, a content heuristic, a link policy, all on by default - plus an escalating repeatable control whose stated purpose is diagnosing exactly this. Boundary drawn against `docs-sync/checked-vs-skipped-denominators`: **its three states cannot see this failure, because the removed files land in none of them - nothing skipped them, nothing enumerated them.** **Landing 2 is a contradiction, which is the best case** - `stake-before-merit` closes on "authorship of the accountability, **not** authorship of the text", and the source's AI policy draws exactly that denied distinction by channel: generated code welcome, generated replies to maintainers hidden without notice. The technique is right that stake is *asked for* at admission and only *tested* in the review round trip it separately calls the expensive half; the reviewer's question there is a probe of comprehension, not a request for text. The asymmetry is structural - **the diff has instruments that do not care who wrote it, the conversation has only the reviewer's attention, which is the scarce server the technique exists to protect.** Enumeration hunt: the technique denied a symmetry, and it denied too much. Both applied to `personas` (15.5% of a security gate's tree silently excluded, falsifier 184 raw -> **0** hand-classified; and a contribution guide that invented the amendment's rule for one field and could not generalize it). **Parallel conditions**: 3 siblings at claim, 5 by Phase 7; `optional-dependency-degradation`, `quality-gates` and `test-harness` all contended, which is why three real candidates sit untriaged rather than verified. Index and catalog regenerated and then **deliberately left uncommitted** - they referenced four sibling techniques absent from HEAD. The gate went red inside the `index` lock on a sibling's half-written subject; lock released before investigating, breakage named and not fixed |
| 2026-08-31 | ICSE 2026 SEIP track page | **reference index — degenerate (no annotations)**: a conference program is a bibliography whose curator sentence was stripped, leaving title+authors only. The substitute signal is the track's own admission rule quoted on the page ("present evidence for the paper's conclusions") — peer review at an industrial-practice track *is* the annotation, and it held: **8 of 8 references read returned something usable**. Enumeration needed two numbers: a raw grep says **150**, two independent structural parses (accepted-papers table, program schedule) both say **74** | 41,712 page / 74 refs, 8 read | 8 | 2 | 5 | 3 | [[2026-08-31-icse-2026-seip]] — **the convergence the wave was ranked on was fake and a real one arrived unranked.** The two benchmark-validity papers are not independent: the second cites the first and relays every validity number from four other papers, so per the tiering rule the pair is **one observation**. The genuine convergence came from two references ranked for unrelated reasons — different authors, institutions, countries, problem domains, neither citing the other — both naming the same missing discriminator in `false-positive-economics`: **the death spiral runs on the author's unilateral power to dismiss, not on the false-positive rate**. One ships a repair gate at precision 0.30 against a 0.19 base rate correctly (machine-generated artifact, no author to bypass); the other measures a mandatory gate at 76% false alarms surviving ten months because a *second* reviewer must confirm any dismissal. Second landing is the enumeration hunt's thirteenth consecutive pay, on an enumeration already extended once: `measurement-honesty` declared "six states, not two" with state 6 *Refuted* defined strictly internally, and a score taken on an instrument the subject was already exposed to passes all six and every technique in the subject. **State 7 `compromised` is invisible from inside the system**, where state 6 was only invisible from inside a single number. Boundary finding: of 74 papers ~46 are single-vendor tool papers in domains this registry does not carry, and `librarian-scan`'s highest-attention subjects appear in **none** of them — this index cannot feed measured demand, only the one seam where both fields stand (measurement of automated verdicts). 13 fetches, all spent by workers. 66 refs ranked and untriaged, never declined |
| 2026-08-31 | `web:postgresql.org/list/pgsql-hackers/2026-08` - a development mailing list month | **reference index (UNCURATED)** - value is entirely the outbound links, but nobody selected them: a chronological firehose, not a bibliography, so the lane's "what the curator included and left out" finding does not exist here | 2,480 (landing page) / **2,680 messages, 578 threads, 255 authors** enumerated across 20 pages | 9 | 2 | 0 | 4 | [[2026-08-31-pgsql-hackers-2026-08]] - **the ingest silently truncated a paginated source**: it returned Aug 1-4 of 31 days, reported `words: 2480`, and nothing in its metadata said "page 1 of 20". The operator authorised paginating before ranking and **it moved the ranking rather than extending it** - the month's strongest artifact (40 messages, 22 correspondents, the highest author count on the list by half again) is not in the first four days, and the truncated triage would have spent its budget on buildfarm attribution and a flag-combination crash and never seen it. Census reported as two figures per the standing focus: **899 threads first, 578 hand-verified** - an unstripped HTML entity (the attachment paperclip) split every thread with *some* attached messages into two, a **36% overcount from one character class**; authors 257 -> 255 on accent variants; and the 4-day sample held **7.5% of messages**, not the 13% its day count implied. Two landings, both from the *disagreements* rather than the claims. (1) `post-landing-repair-density` in `delivery-analytics`: a model was asked to rank landed features by "the number and type of bugs fixed post-freeze" to decide what to revert before shipping - and `revert-linkage` opened with "a revert is the only failure signal available from change history alone" while its own *when not to use this* named the fix-forward population it cannot read. The thread draws the discriminator the technique needed: **scope erosion** was rejected in-thread as "making perfect the enemy of good", **defect tail** got "I'll prepare a revert" from a co-author of the feature being pulled. Its failure mode is in-source too - "Uh, I am confused. We are now considering reverting these?" 25 minutes after the list was circulated as discussion. (2) `reclassification-is-not-repair` in `error-handling`, a **contradicted pick kept**: the patch proposed assigning real error codes wherever the suite reached the internal class, and the senior committer's objection is the finding - "labeling it otherwise is a lie ... blaming the messenger rather than looking for the root cause." `taxonomy-design` owned the closed set and the catch-all and not the opposite member, the one whose entire value is that it never fires. 4 catches against `test-harness` and `dead-code`, both held by siblings all run. **`test-harness` owes a configuration axis** (the corpus has the analyzer form and the runner form, not the suite form) - banked as a lead rather than written, because the sibling held it |
| 2026-08-31 | `tkdodo.eu/blog/creating-query-abstractions` - "Creating Query Abstractions" | **first-party practitioner account** - the maintainer of the library arguing against the abstraction his own users write; reliable for what he built and why at n=1, and it corroborated corpus-internally at **zero fetches** | 3,789 | 10 | 1 | 1 | 1 | [[2026-08-31-tkdodo-query-abstractions]] - ten candidates were **one argument**: four folded into the single landing as its supporting reasoning, one into the lead. `portable-read-definitions` -> `client-fetch-cache`, found by the hunt for the stage a pipeline leaves to a default. The subject owns the cache exhaustively and owns nothing about **where the read definition lives**; two documents brush the seam - the golden path says state all four policies at the construction site *"without reading its callers"*, `prefetch-and-defer` says a prefetch shares the *"same key builder"* - and both presume that site is **reachable**, which it is not when it is bound to a component-scoped primitive. Boundary with `plural-policy-claims` stated in both directions: that technique reads divergence as *claims* to resolve by quantifier, which is right only when the definition is shared; unshared, divergence is drift and no quantifier recovers an intent never formed. Applied `code` on `goat`, **better**: 45 declaration sites over 982 files, 3 questions declared more than once - and **hand-verification changed the conclusion on two of the three**, which is the run's most reusable result. Shipped the verified one (four declarations, three lifetimes -> one declaration, resolved lifetimes unchanged; typecheck 29 before and after, lint clean). **The compiler confirmed the technique**: the two hooks' pass-through options bags could not be composed with a shared definition at all, because their key generic was hand-widened to `readonly unknown[]` against a precise tuple - and one caller had already cast `as any` to get past it. Configurable pass-throughs are not merely inadvisable here, they are untypeable. Second sighting of a banked lead recorded: one key, two different API clients, still unfixed |
| 2026-08-31 | `web:arxiv.org/abs/2604.18071` Architectural Design Decisions in AI Agent Harnesses | **paper** (cross-sectional, 70 projects, corpus frozen 2026-03-23) - taxonomy paper, so its five dimensions and five patterns are the framework half and were treated as marketing; the measurement half carried the run | 16055 (661 landing / 16,055 full text = 24x; the `/abs/` ingest returns the abstract, the `/html/<id>v1` sibling returns the source) | 10 | 3 | 1 | 1 | [[2026-08-31-agent-harness-design-decisions]] - one technique + one golden-path amendment on `measurement-honesty`, **0 of 3 fetches**. The source refutes its own headline statistics: §5.1 defines support/confidence/lift correctly, then Table 10 reports four relationships whose support exceeds the marginal frequency of their own antecedents as published in Tables 4/6/7 - by **2.87x**, **4.34x** and 1.04x, the fourth unverifiable for want of a marginal. Recomputing the highest-lift row from the paper's own conditional gives support 0.31 and lift **2.13** against a reported 3.4, so the ordering it justified is wrong too. Second, independent defect: three of four "compact evidence statements" evidence a metric defined over **binary co-occurrence** with a difference of **continuous mean scores**, so the figure cannot have been computed from the evidence shown. `co-published-numbers-must-reconcile` was written from the identity (support(A^B) <= min(P(A),P(B))), not from the paper - training-data convergence in its strongest form, with the source supplying the worked counterexample. Home found by the **missing-stage** hunt: all six prior techniques are producer-side and the golden path's thesis is that a dishonest number is *unfalsifiable*, so the gap was the inversion - numbers that ARE falsifiable, by their neighbours. The **enumeration** hunt then paid on the same file: "A datum has five states, not two" is a completeness claim and was short by one; the sixth is *refuted* (instrument ran, value well-formed and in range, another co-published number proves it cannot be true), absorbed by none of the five. Applied to `goat` as an `experiment`, **better**: a collection panel computes a completion percentage with a whole-grid numerator over a page-size denominator (its own comment calls the denominator a *proxy*) and clamps at 100%, while the store that owns the data computes the same quantity correctly and needs no clamp. 120 states enumerated from the tree's own parameters; arm A published a well-formed percentage in **all 120**, arm B flagged **101 (84%)**, and the shown figure disagreed with the application's own ground truth in **66 (55%)**. A prediction was falsified en route and the correction is the useful half - the clamp fires in only 9 of 101 and never in the shipped configuration, so the defect is the denominator and the clamp is merely its fingerprint. **Structural fact**: the consumer subscribes to the grid store and reads placed ids off the very state object that also carries the real capacity and the already-correct percentage - the correct denominator was on the object it already held. Ship 0, blocker class **`confirmation`** (triage named registry rows only). 1 catch (the paper independently states Phase 2b's rule - documentation overstates architecture, code settles it), 5 untriaged with anchors, 1 lead |
| 2026-08-31 | `web:arxiv.org/abs/2604.11378` - "From Agent Loops to Structured Graphs" (Hu Wei) | **paper - position paper / design proposal**, the class's weakest sub-kind: it disclaims measurement twice in its own words ("not a production implementation or empirical results"; "they remain untested claims"). By the class rule ("framework papers are the class's marketing") its architecture authorises nothing; what it CAN authorise is a 5-member planning-failure taxonomy, two proved propositions, and a self-declared expressiveness boundary | 739 landing / **21597 full text** (29x) | 14 | 1 | 2 | 2 | [[2026-08-31-agent-loops-structured-graphs]] - expected yield stated as 1-2 amendment-shaped landings before the triage table; actual 1 technique + 1 application, so the calibration held. **0 of 3 fetches** (twelfth consecutive zero-fetch run). `valid-but-degraded-plans` on `pipeline-dag`, **found by the enumeration hunt**: `graph-validation` states its own coverage as a binary - provable at the door, or a named run-time check - and calls the space between them the category that "must never exist". The source demonstrates a *third* class neither half reaches, where the graph is valid, every check passes, **the run succeeds**, and the plan was still wrong. Discriminator: does the defect change the **result** or only the **price**? Price-only defects are invisible by construction, because nothing separates a correct cheap run from a correct expensive one without a second run to compare. The subject's blind spot is in its own boundary prose - every document is written for a graph *the user authored*, where the class is nearly harmless; under a planner it becomes a rate. Corroborated by **training-data convergence, no fetch** (build systems' unused-dependency tooling, `EXPLAIN`, critical-path scheduling all predate the paper). **Applied `experiment`/`better` to `gravity`**: its door validator is unusually strong on the correctness class - checks every reference against a universe of ids, then again against a tighter one, and treats an absent link as a defect after shipping six unlinked records under a green gate - and not one check asks whether an edge that *resolves* is *needed*. The structural fact nobody designed: severity is `declared - missing`, and one class of declared dependency can never go missing, so **5 of 11 nodes can never reach the severe end of the scale** however much real evidence is removed; every differing node carries exactly one inert edge, the predicted signature. The run's own contribution beyond the paper is the **inert edge** rule - any ratio over a declared set whose members have unequal ability to fail is silently capped - now the technique's first decision rule. **Ship 0, blocker named per the declared focus: confirmation + indeterminacy**, two of the four classes and both correct outcomes - the pick named no project, and two fixes are defensible (partition inert edges out of the denominator, or give explanatory nodes the evidence edges the code was built to carry) with nothing in the tree settling which. One answer from the owner selects it |
| 2026-08-31 | `github:remeda/remeda` - TypeScript utility library monorepo | **vendor repository**, mined as a **first-party practitioner account of running agents against a maintained codebase** (the class the tree actually supports) | 240 README / **47025 in-tree md** / 58249 ts | 20 | 4 | 1 | 2 | [[2026-08-31-remeda]] - **the library was not the source.** The README is 240 words; the tree carries 47,025 words of markdown and the densest part documents nothing about utility functions - it is `AGENTS.md`, an `.agents/skills/` tree, three path-scoped rule files, and a `.claude/hooks/` turn-end gate whose comments are paid-for failure modes. Class note for the ledger: **the `.agents/`-shaped tree is a source-class tell** and belongs at position 1 of the Phase 2b sweep, because a repository that instruments itself for agents gives a first-party account of agent operation regardless of what the repository is *for* - here it outproduced every other surface combined. Four landings, none touching the library. `mutating-local-gates` (new, machine-paced-delivery): `hook-hygiene`'s flat prohibition - *"Hooks observe; they never mutate"* - carves out only the editor-on-save loop, and the agent turn boundary is a third position where **two of its three contracts transfer** (review binds hardest, staging does not apply, shared-tree binds and is bounded by scope). `stake-before-merit` (new, same subject): `human-gate-capacity`'s four demand levers **all assume the overloaded party generates the arrival** - unavailable at an outward-facing gate; the filter that vanished was an *accident* (producing a plausible change used to be expensive, so submission carried evidence of investment) and was never written down, so nothing announced its removal; and a merit filter cannot replace it because **deciding whether a change is worth reviewing means reviewing it**. Reaper forms **ranked** in `suppression-hygiene` - our own enumeration presented as flat ("an expiry date, a re-review cadence, or the checkable condition") when the source ranks it and rejects our implied default: a date fires at whoever runs the instrument that day, works exactly as designed, arrives at the wrong person, and the cheapest repair is to postpone it. Plus the sharper half - **the suppression's form can carry its own reaper**, the one case `creation-names-reaper` is satisfied with no clause at all. `build-measurement` gained a **vantage** axis: it labels which build, which variant, which machine, when - and never *whose*. **Applied 2c/1e/1s.** The apply step **corrected its own technique twice**: a managed project's turn-end gate neither mutates nor blocks and still guards re-entry, because exit 2 re-enters the model's loop - so the termination contract is created by the output reaching the agent, and the technique now carries a three-tier ladder. Decisive A/B on the other: **0 warnings vs 63**, same crate one minute apart, only the suppression form changed - 63 of 71 (89%) stale, against a surface that is 0.7% self-retiring. Four of twelve candidates lost to contention, all to one sibling (`release-pipeline`, `quality-gates`, `test-input-generation`, `test-harness`) - recorded untriaged with anchors, not declined. 0 of 3 fetches, eleventh consecutive. 5 siblings live. |
| 2026-08-31 | `youtube:3IyKC5EtNkM` - "9 Ways to do Inheritance in Rust" (Carl Kadie, Seattle Rust User Group) | **first-party practitioner account** (conference talk) with a **live peer-review property the dialogue sub-class does not describe**: the audience are peers who correct the speaker on camera, twice, and both corrections are load-bearing. Hardest possible source shape for this corpus - a language-mechanics talk against upper layers that may carry no language names. | 7358 | 11 | 3 | 2 | 0 | [[2026-08-31-nine-ways-inheritance-rust]] - **the predicted low yield held exactly, and the three survivors were the three about BOUNDARIES rather than syntax.** Six of nine puzzles strip to `nothing` (they are the syntax for a standard OO move). One NEW TECHNIQUE `borrowed-surface` on `module-design`, landed by **merging the operator's two separate picks** once verification found they share a root and the root is higher altitude: `module-depth` already corrects the author's interface denominator *downstream* (with enough callers every observable behaviour is interface) and has **no model running upstream** - that part of your interface is authored in someone else's file, on their release schedule. Two forms: implicit delegation (the source states its own cost - *"the whole reason I gave it a new type was that if I got mixed up I would get a compiler error. And now I'm not going to get a compiler error"*), and a disjointness premise taken from a contract you do not own. The discriminator the source states without generalising: delegation is correct when the wrapper's job is **orthogonal** to the delegated surface, self-defeating when the wrapper's job **is** the distinction. Plus two amendments, both from the enumeration hunt: a **fourth signal** on `seams-and-adapters`, which declared "three signals" and whose *When not to use it* would have told you NOT to own the contract in exactly the case where you must; and the **open-type case** on `settings/typed-accessors`, whose 4-step contract rests on an unstated premise (the type is declared at the accessor) and uses parse failure as the type detector - zero hits for type-mismatch vocabulary across the whole subject, and its `Boundaries` section claims typing rather than scoping it out. `ipc-contract` rejected as the home on its own stated boundary ("no version skew in the field"). **Applied 2/2 `better`, and the tree confirmed the technique in the state it predicts**: a scrubbing wrapper forwards the wrapped type's whole mutating surface, exactly one caller reaches through it, and that caller **hand-rolls the invariant in four lines under a comment explaining why** - the guard has left the type and become a convention held by one author's memory. Same probe line both arms: A compiles clean, B is `error[E0596]`. Not committed - the crate's build script fails before rustc reaches the code, and unproven does not commit to credential code. Second: a settings store with a closed key registry and a real write door where enforcement is nonetheless **per key with nothing counting it** - 58 of 90 key constants reachable inside the validator, and within its own "limits" category 3 of 4, the fourth being a spend ceiling whose sibling is enforced *and* carries six negative test cases. One test over the whole category: FAILED on A, ok on B. **Ship 1.** 0 of 3 fetches. 6 untriaged with anchors, 2 leads (both about source-class calibration: an enumeration that names its own count invites its own refutation, and a practitioner shipping the option they doubt at equal cost). |
| 2026-08-31 | `tigerbeetle.com/blog` (31 posts) | **reference index whose references are all ONE organisation's first-party accounts** — 335 index words against 31 articles (ratio says index), but deduped by author it is 3–4 voices and matklad wrote 13. Phase 2c's *mechanics* apply in full; its *triage signal* (within-index convergence) is **void**, and the same org's repository was mined the same morning, so overlap is depth, not independence. | 335 index / 11 articles read | 11 | 3 | 3 | 3 | [[2026-08-31-tigerbeetle-blog]] — **one NEW SUBJECT plus two techniques, and 3 of 3 apply rows came back `better`.** `test-input-generation` on `engineering-process/build-and-release` (6 techniques, specced and forged same-session): four posts by three authors converge on test-input generation and the corpus held **nothing** — 0 hits for `deterministic simulation`, `swarm test`, `property-based`, and all 56 `fuzz` hits are *fuzzy matching*, which is exactly why the slug map returned confident noise and the hole survived 153 subjects. Home decided on the neighbour's **stated boundary** (`test-harness` opens *'the tests themselves assert facts'* — an explicit exclusion), not on a slug. Its strongest pair: **both clever and naive generators collapse the reachable space, in opposite directions**. One technique was written **against the literature rather than the source** — the post presents swarm testing with no limits and says 'Please steal this'; Groce et al. ISSTA 2012 (the run's one corroboration fetch) states the decisive boundary, so the technique carries both halves. `two-clock-records` on `audit-logging` **corrects a stated rule** — the golden path said *'one clock per ledger'*, right where the ledger originates the fact and wrong for anything learned from elsewhere; found by the asymmetry hunt, since `append-only-design` already owns correction-as-append and records all of it on the *recorded* clock. `toolchain-floor-drift` on `supply-chain` is the mirror of `update-automation-review`: the update you never made, carrying a rare real protocol (top-100 packages, binary search by build, 1.0–1.94, dated). **Applied 3/3 `better`, two of them `ab-paired` experiments**: `personas` declares `rust-version 1.80.0` against a resolved graph whose **effective floor is 1.88.0** (60 of 518 packages above the claim, led by a transitive `time` at patch level) while all 11 Rust CI jobs run `@stable` — and the structural fact the tree could not have been built to prove is that the *same repo* declares a runtime floor of 20 **and runs a job at 20**, so two identical claims are enforced asymmetrically purely because pinning a compiler is an extra argument to an action whose default reads as correct. Second: a property suite running ≥1,000 cases per invariant asserts a violation branch its own generator makes **structurally unreachable** (`media_duration` pinned to 60 while source-end is bounded far below), plus 8 more pinned dimensions. Third: `politicas` independently reached a bitemporal schema and splits enforcement exactly where the technique predicts — record clock `not null`, world clock nullable and unpopulated by the migration's own admission. **Ship 0** — the declared focus (carry a project on every triage row) was missed for the 4th run running, root cause now located in Phase 1, which never reads the focus. 2 catches, 6 untriaged with anchors, 3 leads. 12 fetches, per-reference budget. **WAVE 3** (run `tigerbeetle-w3`, +6 refs read = 17/31): three more landings, **all inside the subject wave 2 created** — which is the evidence its boundary was right, since it absorbed them with no home argument. `liveness-needs-a-quiet-period`: safety and liveness need **opposite fault regimes**, and a safety-only suite does not merely fail to test liveness, it **erases** liveness defects — the stuck component is rescued by the next random draw. Remedy is a phase change inside the run (freeze the fault set, heal only the subset that should finish, keep the rest permanently down, assert monotone progress against a bound); the failure class is two individually reasonable policies that phase-lock, invisible to component testing because neither has a bug. `seed-is-not-a-reproduction`: closes the seed lead wave 2 banked, and produced a **cross-bundle catch** — `game-production/seed-determinism-contract` states the same rule *better* (four terms; the source's 'reproduces at a specific commit' is one of them). Links across bundles are forbidden, so the landing states the **discriminator**: content generation keeps the seed and states the drift because the seed is the artifact's *identity*; a test suite cannot, because the recorded case is *evidence* and exists to survive the fix. Plus a **fourth rung** on the oracle ladder (recorded expected output) as an amendment rather than a competing technique. **Applied 1/3 `better`, `ab-paired`, and the apply refuted my own remedy**: over 50 fixed seeds, the repair this corpus had *already recommended* for that generator re-points **50/50** recorded seeds (structural, not probabilistic — one added draw replaces the sequence); and arm B, included as a mere control, showed **38/50** persisted inputs returning one ULP off, verified at bit level as the JSON library's *default decoder* (its writer is exact, and the language's own parser handles the identical string correctly). Its documented exact-decode opt-in moved that to **50/50**. The technique gained a section: persisting the derived input is necessary and **not sufficient**. Two rows honestly `unapplied` with measured absences (0 fault-injecting lanes; **0 recorded-output assertions across the fleet**). **Ship 0 again, but by a third blocker the focus does not name** — not size (~40 lines) and not confirmation (the tree was authorized): the build could not relink because a running app held the binary, and killing the operator's app is not this run's call. Honest negatives about wave 2's own ranking: row 16 was banded B expecting a docs taxonomy that turns out to live in the *repository*, not the post. 18 fetches total. |
| 2026-08-30 | `github:blue-az/operator-control-plane` - local ledger for claims, evidence and verification across agents | **practitioner build-walkthrough in repository form** (solo builder, 267 commits) | 2639 README / **168969 in-tree md** / 40828 py | 22 | 5 | 2 | 3 | [[2026-08-30-operator-control-plane]] - **the run that changed the method before it changed the corpus.** Triaged first off 2,639 words of rendered landing page; the operator asked whether the repo or only the readme had been read, and the answer was the readme. The tree is 168,969 words of markdown - the README is 1.5% of its prose. `SKILL.md` **1.2.0** adds Phase 2b (clone, pin the commit, sweep operating docs -> instrument -> measurement -> types -> tests -> README last, then sweep again for engineering worth reusing rather than claims worth quoting). Fourteen repository sources audited by the new tell (a `words:` holding one small number, a body citing no file from the source's tree): ten had cloned, one is class-exempt, three had not - two re-runs dispatched. Five landings, and none of the best three is mentioned in the README: `prose-rule-drift` (new, quality-gates - the missing stage one, where the tell is that the forbidden action simply works), `measurement-revision` (new, eval-harness - a re-run at the same n is a second sample, not a correction; pass 3 at n=5 restored pass 1's ratios exactly), an eighth owner in `failure-attribution` (a loop terminating on the first state-changing command defeats every tell in the funnel and lands in the *model* bucket), a `brief-carries-the-session` amendment (the corpus decorrelated reviewers by model family only and never asked what the brief carries in), and a `task-envelope` amendment after the **XL was downgraded on reading the file**. Applied: `prose-rule-drift` -> ascent, experiment, **better** - a documented checker that nothing invokes, 0 violations observed vs **27 across four projects** one invocation apart, which produced the technique's third state (an instrument nobody invokes is still prose). 0 of 3 fetches, tenth consecutive. |
| 2026-08-29 | `youtube:USEk7F9GIZk` - "2 Loop Process for RSI: LLM and Harness" (Discover AI) | **second-hand survey** - single-voice explainer of one unnamed paper, no build, no measurement | 4198 | 10 | 1 | 2 | 6 | [[2026-08-29-two-loop-rsi-llm-and-harness]] - **the class behaved exactly as predicted, and the one finding came from the asymmetry hunt, not from the source.** Six of ten candidates were catches in `eval-harness`, `agent-chaining`, `mcp-tools` and `fleet-orchestration`; two thin leads bank the paper itself. The keeper was the video's throwaway line "if you change the LLM, get rid of your harness": the subject already says the **suite** side of that rule (`unaided-baseline-screening` - the screen expires with the candidate) and `hitl-approval` already says a compensating capability names what retires it, and neither was said where a red case gets its fix assigned. Amendment **held mid-run, then landed** in `failure-attribution` once the parallel session's own section in the same file reached HEAD (`297a7d5`): three of the six owners' fixes are compensations for a *model* defect, a compensation carries the model version as its retirement condition, and on upgrade it is in one of three states - still pays, dead weight, or **now the failure** - so re-ablate the compensation set as a matrix cell instead of carrying the previous winner forward. The lead "harness search is combinatorial, not gradient" got its second sighting from that same pending section. 0 of 3 fetches, ninth consecutive; training-data convergence plus two corpus-internal siblings. |
| 2026-08-29 | `youtube:MvI17TNFW9c` - "AI Creates New Harness by Ignoring 80% of Evaluation?" | **second-hand paper review** (one preprint, arXiv 2608.20169) | 2744 | 8 | 1 | 2 | 2 | [[2026-08-29-task-coevolve-harness-optimization]] - the class predicted itself: the video was a pointer and the paper was the extraction (2 of 3 fetches, first spend in three runs). `eval-harness` gained `discriminating-task-selection` at a **missing stage** - which cells a selection search runs - and the higher-yield half was an **inversion**: `eval-economics` praises the golden set for being frozen and saturated, correct for a regression gate and exactly wrong for a ranking, so the discriminator is the question not the pool. Boundary stated against the same-day `oracle-frozen-during-repair`: the pool is the oracle and stays frozen, the selection moves, from a rule the ranked party cannot write. Two leads (the numbers, and the presenter's doctrine), two catches, three untriaged. |
| 2026-08-29 | `youtube:Z90xlttVx3Y` - "Can Graph Engineering Create System Intelligence?" | second-hand paper review (one framework survey) + **first-party critique** (opinion, not measurement) | 4813 | 9 | 1 | 2 | 4 | [[2026-08-29-graph-engineering-system-intelligence]] - **a critique of a framework is a list of enumerations being challenged.** The paper half stripped to nothing ("system intelligence = graph engineering" is a taxonomy, not a rule) and the critic's counter-cases were boundaries. One amendment: `civic-entity-ontology` had a gate that rejects unknown kinds and a ritual that starts when a kind is demanded, with nothing carrying the one to the other - the rejection ledger is now the ritual's inbox, corroborated from four bundles (`confidence-gates-report-unknown`, `three-state-attribution-that-fails-to-unknown`, `unknowns-as-constraints`, `failure-not-empty-success`). The source argued the opposite (against fixed ontologies); the finding keeps the closed set and gives its refusals somewhere to go. Four catches, the sharpest against `heterogeneous-model-panels` ("two roles are not two opinions" was already "that average with two samples"). Two leads: the survey's own paper list as a future aggregator source, and the critic's real point - nothing in `agent-chaining` or `fleet-orchestration` says WHEN a topology pays over a loop. 0 of 3 fetches, ninth consecutive zero-fetch run. Unattended; picked by impact. |
| 2026-08-29 | `youtube:F1XqqVNBa4o` - "Dual-Memory Optimization for Self-Improving AI Agents" | **single-paper explainer** (paper-aggregator class, n=1) | 5886 | 8 | 2 | 1 | 3 | [[2026-08-29-recuris-dual-memory]] - **the class rule held: the video was a pointer and the fetch was the extraction.** The channel never mentions the ablation table, and the ablation was the whole finding: a verified working state worth +23.9 points, the skill store alone +2.0, and removing the write review costing 13.5 - so `working-memory` (which said the state is "rewritten by judgment" and never named who checks the judgment) got a **propose / check / commit** stage anchored on `gate-sees-target`, and `procedure-promotion` got the sharper scope key (select by current verified state, not by history). Missing-stage shape again; cross-bundle convergence with `no-gate-self-certifies` and `coverage-is-counted-not-claimed`, named not linked. Three catches (trace-attributes-failure, patch-behind-held-out-gate = `oracle-frozen-during-repair` second sighting, workflow-is-not-capability). 2 of 3 fetches, both extraction. Unattended. |
| 2026-08-28 | `web:gamebalanceconcepts.wordpress.com` - Schreiber 10-week balance course (L1/3/7/9) | **first-party pedagogy corpus** (batch, /harvest) | course | 11 | 4 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - cost-curve-object-audit + intransitive-equilibrium-solving landed; reward-cadence + curve corollaries into the thinnest subject. Provenance catch: the four-term equation appears verbatim in L7. Lead: L4-5/8 unread |
| 2026-08-28 | `web:github.com/mxgmn/WaveFunctionCollapse` | **research-model release** (batch, /harvest) | repo+code | 8 | 3 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - three code-corroborated fixes: surviving-seed identity, pinned-seed-can-fail, success-flag-is-not-completeness, per-output-vs-corpus check classes. Lead: backtracking ports |
| 2026-08-28 | `web:github.com/CleverRaven/Cataclysm-DDA` JSON schema docs | **vendor repository** (batch, /harvest) | docs | 12 | 1 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - typed-operator-inheritance landed (relative/proportional deltas, abstract bases, restraint ladder, layer-safe merge); retirement-as-content approved -> spec bank |
| 2026-08-28 | `web:gamedeveloper.com` Machinations intro | **co-author promotion** (class corrected; batch, /harvest) | 3k~ | 7 | 0 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - runnable-diagram cluster unpicked (untriaged). ATTRIBUTION FLAG: source never mentions tornado diagrams - check tornado-sensitivity-sweeps' citation. Lead: artificial players, three fetch routes failed |
| 2026-08-28 | `web:arxiv.org/abs/2503.18748` RL level balancing | **paper** (batch, /harvest) | paper | 4 | 0 | 1 | 0 | [[2026-08-28-game-production-harvest-batch-1]] - premise corrected: RL edits levels, players are scripted - confirms the corpus assumption. Approved cluster -> spec bank (goal-seek monotonicity precondition; estimator-convergence sim sizing). No human validation in the paper |
| 2026-08-28 | `web:github.com/jeffcampbellmakesgames/unity-asset-validator` | **vendor repository** (dormant; batch, /harvest) | docs | 8 | 0 | 3 | 2 | [[2026-08-28-game-production-harvest-batch-1]] - gate-design cluster unpicked (untriaged with anchors: check-scope ladder, one-ruleset-two-surfaces, contract-reified lookups). Catches on verdict shape |
| 2026-08-28 | `web:derek-lieu.com/essays` (3 of ~89) | **first-party craft corpus** (batch, /harvest) | essays | 9 | 3 | 2 | 1 | [[2026-08-28-media-generation-harvest-batch-1]] - concealment-and-its-tells landed as trailer-structure's 7th technique; mid-climax + opening amendments on escalation; claim-footage pairing on promise-ledger. No provenance echo of the six techniques. Lead: cue-first attribution UNVERIFIED |
| 2026-08-28 | `web:opendoclab.mit.edu` -> APA GenAI documentary standards | **field standard, PRIMARY UNREACHED** (batch, /harvest) | pointers | 8 | 0 | 2 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - PARKED: Drive-hosted PDF egress-blocked; nothing landed from model knowledge. Return: fetch the PDF, then triage audience-disclosure/consent/cue-sheet/archival-marking - real gaps |
| 2026-08-28 | `web:github.com/comfyanonymous/ComfyUI_examples` | **vendor first-party example corpus** (class corrected; batch, /harvest) | repo | 5 | 0 | 3 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - recipe-carrying-renders unpicked (untriaged). Leads: filename-only model pinning; params-below-the-prompt dialect |
| 2026-08-28 | `web:github.com/openai/openai-cookbook` image prompting guide | **vendor repository** (batch, /harvest) | notebook | 15 | 0 | 1 | 4 | [[2026-08-28-media-generation-harvest-batch-1]] - micro-content unpicked (untriaged with anchors). Currency: no negative-prompt param; long-prompts-fine vs prompt-budget-limits' rationale; constraints-LAST divergence. input_fidelity self-contradiction flagged |
| 2026-08-28 | `web:github.com/facebookresearch/audiocraft` | **research-model release** (batch, /harvest) | docs+cards | 10 | 0 | 1 | 2 | [[2026-08-28-media-generation-harvest-batch-1]] - music-family cluster approved -> spec bank (contour-only anchor 0.44, silence-collapse, 32kHz/-14LUFS, provenance-vs-NC-license split). Lead: JASCO temporal conditioning |
| 2026-08-28 | `web:github.com/hkchengrex/MMAudio` | **research-model release** (batch, /harvest) | readme+paper | 7 | 2 | 1 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - picture-as-timing-brief landed as sound-effect-generation's 5th technique + three defect-taxonomy entries. Text-vs-sync conflict flagged as inference, not author-stated |
| 2026-08-28 | `web:github.com/open-telemetry/semantic-conventions-genai` - GenAI semconv repo | **spec, pre-release** (batch lane, /harvest) | repo | 10 | 0 | 2 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - conventions split to a dedicated repo, all Development, breaking churn active; CONFIRMS attribute-precedence-lists' premise, precedence lists must now span two repos' registries. Return: first tagged release. Token-model cluster left unpicked - untriaged with anchors |
| 2026-08-28 | `web:github.com/Arize-ai/openinference` - OpenInference trace spec | **vendor spec** (batch lane, /harvest) | repo | 9 | 3 | 1 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - declared two-axis identity landed on provider-family-matching; shapes-not-names + cost-name currency on attribute-precedence-lists; no-crosswalk catch confirms two-doors-one-pipeline. Lead: sender-side redaction config |
| 2026-08-28 | `web:github.com/pydantic/genai-prices` - public GenAI price DB | **reference data repo** (batch lane, /harvest) | repo | 10 | 0 | 1 | 5 | [[2026-08-28-llm-obs-harvest-batch-1]] - five catches, zero content, the expected good run for the class: dated append-only history, prices_checked provenance, bundle+hourly refresh, flat variant fields all confirm price-book techniques in the wild. Untriaged: match-expression trees, time-of-day constraints |
| 2026-08-28 | `web:arxiv.org/abs/2306.05685` - founding LLM-as-judge paper | **paper** (batch lane, /harvest) | paper | 10 | 2 | 0 | 3 | [[2026-08-28-llm-obs-harvest-batch-1]] - reference-guided-grading landed as a new judge-contract-design technique (70/30/15 misgrade ladder); golden-set gained difficulty-conditioned agreement + verbosity-inflation fixtures. Swap-order already in the corpus's structural layer. Self-enhancement bias NOT established, by the paper's own admission |
| 2026-08-28 | `web:eugeneyan.com/writing/llm-evaluators` - evaluating LLM evaluators | **first-party practitioner synthesis** (batch lane, /harvest) | 8k~ | 12 | 1 | 2 | 5 | [[2026-08-28-llm-obs-harvest-batch-1]] - panel-of-judges landed on judge-selection-by-spread (externally corroborated ~1/7 cost). Binary-verdict claim CONVERGED with the trace-corpus source and resolved already-covered: anchor for authoring, binarize for kappa. Metric-layer cluster largely already present at body depth - see the batch note's lesson |
| 2026-08-28 | `web:hamel.dev/notes/llm/evals` - trace-driven evals corpus | **first-party practitioner account** (batch lane, /harvest) | corpus | 7 | 1 | 1 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - error-analysis-first-taxonomy landed as a new production-trace-scoring technique: saturation rule, guaranteed random review slice, critique-to-few-shot. Tension recorded: capability-ceiling vs spread bake-off. Lead: active-learning loop video |
| 2026-08-28 | `web:habitat-thinking.github.io/ai-literacy-superpowers/plugins` + all 35 `explanation/` children - "ai-literacy-superpowers Concepts" | **first-party practitioner account, documentation form - channel corpus** (one author, 35 pages), wrapped in an **operator dispatch** (mine the batch real-gap-only; add a paired-proof phase to the X lane) | 61142 (after nav strip) | 26 | 4 | 2 | 13 | [[2026-08-28-ai-literacy-superpowers-concepts]] - **four sentinel pages were one missing stage.** Cadence governance, adversarial review, decision archaeology and the challenge-refine protocol each mapped near `hitl-approval` and none inside it: the corpus owns the pause, the rate and what an agent may change, and nothing owns *what a human is shown at the plan gate, in what order, and by which reader that is not the author*. XL spec dispatched (`plan-review` -> `llm-agent/orchestration`, 6 of a cap of 10 verified against `taxonomy.json`) folding the routing rule, the lens priority, the inadmissibility rule and the escalation ladder as four proposed techniques, with the emit/persist/dispose ordering explicitly NOT re-minted. **The asymmetry hunt paid twice in the inverse direction** - the corpus had the instrument and the source supplied the case one step outside its stated domain: `decision-records` reads approval-near-100% as a dead gate, and when the record's subject is an OBJECTION the same six fields read backwards (mostly-rejected-with-rationale is healthy; the why becomes mandatory on every disposition because it IS the engagement); `human-gate-capacity` measures the gate with four numbers, and at the subject's own one-person floor those numbers describe the person who is the queue - amended: count never score, advisory never a gate, local never a persisted judgment, the stop decided before the session; corroborated from training data (ego depletion at d=0.04 in the 2016 23-lab RRR; task-switching cost and vigilance decrement are what is robust), not from the page. **Skill 0.15.0 -> 0.16.0**: Phase 8 gains the operator's paired-proof step before any cross-repo commit (name the measurable; A/B at any scale preferred; before/after under `before-after-outcome-pairing`'s never-invent-the-missing-half rule; closed status set; `unproven` does not commit); corroboration gains *tier sources, never count them* (relays are downstream of primaries, so relay agreement with a stale primary is evidence of its PAST state); Phase 5 gains the unattended real-gap-only rule. 13 catches, all against a mature `llm-agent` / `quality-gates` neighbourhood (read-only emitter, rung-vs-reach = `unmeasurable-criteria`, range-not-point = `preflight-estimation`, structured refusal = `stop-reason-ledgers`, affordance identity = agent identity + human behalf already both retained). 4 untriaged partials with anchors (calibration refusals as signal, path-as-state records, advisory precedence by re-emittability, the rung/reach vocabulary failure). 2 leads with return conditions (the sentinel category; calibration refusals). Zero fetches - tenth consecutive corpus-internal run. No connected tree touched: the dispatch's project half was the skill phase itself |
| 2026-08-28 | `web:addyo.substack.com/p/audit-your-agent-files` - "Audit your Agent files" | **second-hand practitioner listicle + first-party audit half** (hybrid: three papers relayed, one own-setup account) | 3046 | 13 | 2 | 2 | 4 | [[2026-08-28-audit-your-agent-files]] - the routing table's listicle tiebreaker decided the whole run: *items touching this registry's own machinery outrank items about bundle content*, and every picked row was machinery. Subject `agent-instruction-files` was forged four days earlier with six techniques over this source's exact territory, so nine of thirteen candidates were catches or unpicked - and the two that landed came from the one question the subject never asked. **`instruction-freshness` couples the file to change not calendars, and every trigger it lists is repo-side** (stack bump, command rename, restructure, deletion); its audit asks whether a rule's failure mode still exists *in the repo* and never whether the *agent* still has that failure. The subject's own denial hid the gap: rejecting calendar cadence ruled out the only schedule that catches substrate drift, because a model upgrade produces no diff to couple to. Corrective is not the source's "audit on a cadence" but **a second change list** - a model generation lands, the harness ships natively what the file instructed around; both datable. `line-earning` is the same seam from the other side: it asks whether removal changes behaviour then concedes the measurement (*"the honest way to know is the line's origin story"*), which is evidence about the model that FAILED and diverges exactly when the substrate moved. **The instrument was already in the subject at the wrong polarity** - `capability-before-steering` (landed by a parallel session mid-run) isolates a capability gap by checking whether a failure "persists at the top of a fresh, minimal file"; run the same rig with the rule ABSENT and it measures `line-earning`'s second half directly. The subject built one door of a two-door apparatus. **The fetch beat the source twice.** The relay flattens the primary to "instruction value can expire"; the vendor post supplies the mechanism - the expired line is disproportionately the RESTRAINT (its example guardrail was *correct* for older models, removed because newer ones "have better judgement"), so an expired restraint does not idle, it **suppresses behaviour the current model would have got right**, which is `restraint-amplifier-balance`'s loss by a route that subject did not model: a file that BECAME all-cage rather than one authored that way. And expired lines **contradict** - the vendor read its own transcripts and found conflicting directives spanning system prompt, installed capabilities and user request, so two lines can each pass `line-earning` and be a defect as a pair, invisible to any per-line audit. Class lesson worth carrying: **the primary states no removal procedure at all** - it reports the 80% result and offers a tool; the relay's protocol (complete the task on raw model and harness) is the only candidate procedure either source carries, which is why the finding is written from the corpus's own apparatus rather than from either. Second technique `sibling-floor-ownership` takes the half the subject explicitly declined: its opening names the siblings and leaves them to the harness, but they are not the harness's furniture - they are capabilities a person installed one at a time, and the position "paid, advisory, always-loaded" holds for them word for word while being the LARGER half. `creation-names-reaper` is the anchor: install is locally justified, nothing schedules the retention decision, and the admission funnel runs per line on a diff while an install produces no diff. **Phase 4's "one moment" warning fired for real** - a parallel session landed two techniques into this exact subject between the map and the write, and re-reading was load-bearing rather than bookkeeping. 2 of 3 fetches, one wasted guessing a vendor URL. Zero declines; unpicked rows recorded untriaged, the personalization paper flagged as a cross-run convergence candidate against the 2026-08-22 pair |
| 2026-08-28 | `web:blog.google/.../build-with-gemini-omni-1-1-flash` - "Gemini Omni 1.1 Flash lets you build with more control" | **vendor release announcement** (new class) - the marketing surface of a release, WITHOUT the walkthrough half | 1943 | 12 | 1 | 0 | 3 | [[2026-08-28-gemini-omni-1-1-flash]] - **a release announcement is not a release walkthrough, and the difference is measurable.** The walkthrough row says seek it out because a change carries its own motivation - the author says what was wrong before. An announcement is organised around changes it is PROUD of, which inverts exactly that property: it states what is now possible and never what was wrong, because what was wrong was its own last version. The discriminator to carry: **a walkthrough states failure modes; an announcement states NUMBERS, and its numbers are the yield** - the prose around them is the strip test's problem. Every accepted finding here came from a number, none from a sentence. **For this class the fetch is the extraction, for a reason specific to it**: the post said the model analyzes "up to 10 seconds of prior context", which is compatible with sampling ten seconds from anywhere; the API docs say **"the last 10s"** - a trailing window. That distinction IS the finding, and marketing blurred precisely the part carrying the mechanism, because *which* ten seconds is not a selling point. 2 of 3 fetches (the first hit the general video page, which carried no numbers and named the second). Landed one amendment on `generated-shot-sourcing`: the technique said continuity carry extends from the **"whole previous clip"** - wording that entered the corpus ONE DAY EARLIER from run 25 - and the vendor's own doc refutes it. Extension conditions on a bounded window off the tail; the window is a **versioned fact belonging in the vendor ledger** (final-second to final-ten in one release, on a parameter no caller sets); what has scrolled out must be re-supplied, which is chain drift arriving one level down and taking the answer the subject already owns (pin the origin, roll the window); the chain has a **cumulative** ceiling, so extension does not lift the clip cap but re-denominates it - what it buys is a larger unit to place the seam between; and the increments are quantized while authored durations are not, so a derived duration is paid at the timeline in a trim the file's own decision rules already warn about. Also corrected the moving-reference trim from economy to obligation: references cap at 3 clips x 3s, tight enough that a reference is a SAMPLE, and **an unstated span is chosen by the platform - a span nobody chose is not a scoped reference however carefully its negative scope was written.** Three catches, all where the corpus is AHEAD of the vendor: rung-3 anchors (corpus carries the mid-clip glitch and the one-cloth rule; the post demonstrates the feature and states neither), baked audio, and the 360p draft tier. **Self-correction on the last one**: I thought `resolution-as-stage-property` had an upscale-shaped hole in its promotion-is-a-risk-event enumeration - it does not, it already prefers true upscaling of accepted pixels over re-sampling for exactly that reason. Phase 6.1 paid; the correction would have been a phantom fix against my own summary. Operator picked row 1 only, declining the currency reset, the named consumer application and the id-addressed-continuation lead - **no project tree was touched**. 5 untriaged with anchors. **The subject's structural debt is now overdue and this run added to it**: yesterday's note said the conditioning ladder carries five riders and the next pass should rebuild the golden path around a widened ladder "rather than accreting a sixth section" - this run accreted the sixth. Right content, wrong shape; that is a /deepen job. Parallel session live in the checkout the whole run - three untracked techniques appeared under `agent-instruction-files`, the gate was red on their account from Phase 1, and a blind build-index swept them into the software-engineering index (reverted, catalog rebuilt from the corrected state, media-generation + catalog committed alone) |
| 2026-08-28 | `web:github.com/microsoft/AutoSaddler` - "AutoSaddler: Automatic Harness Optimization with Durable Updates from Agent Execution Traces" | **research-code release** (paper + implementation, one squashed commit) - ingested at its README, this class's LEAST reliable surface | 1696 | 14 | 3 | 1 | 1 | 9 untriaged | [[2026-08-28-autosaddler-harness-optimization]] - **the class row is right about reliability and wrong about where the yield is.** It says the README is an advertisement and the least useful file present; the ingest returned ONLY the README and still landed three findings. The property the row should carry: **a research release's README advertises a METHOD, and a method's advertisement is made of its DISTINCTIONS** - a patch taxonomy, a session-type taxonomy, a four-way outcome classification, all claimed as novel and therefore all present, and a taxonomy survives the strip test where a benchmark number does not. Contrast the vendor repository, whose README advertises a PRODUCT and is therefore made of names. The +9.0/+9.6/+10.0 pp table was proposed as nothing. **1 of 3 fetches, spent as extraction not corroboration**, and it made the finding bigger than the source did; the other two corroborated corpus-internally - ninth consecutive run where the corpus was its own second source. **Finding 1 came from one sentence in step 3 of a reproduction recipe** ("run from the external working directory so generated workspaces cannot inherit repository-level agent instructions through Git ancestry"). `agent-instruction-files` models the file entirely from the AUTHOR's chair and sorts loading into always-loaded / loaded-on-touch / per-user - **all three categories describe files an author placed deliberately**, and a directory a program creates has no author and is governed anyway. Confirmed a hole not a seam by the **enumeration hunt, paying a ninth consecutive run**: the golden path's seven failure modes are every one of them *the file's content being wrong*, and not one is *a correct file governing a directory nobody meant to brief*. The primary settled the mechanism and doubled it - files load from the cwd "and every directory above it", concatenated not overridden, **with no boundary at the repository root**, PLUS per-directory files BELOW the launch point loading lazily when the agent reads them. So it is a cone, not a chain, and **downward injection is sharper than the source's upward case**: a third-party tree cloned under the working directory ships its own instructions into context the moment the agent reads anything in it. The source's own recipe clones exactly such a tree and never mentions it. Fetch also yielded the fallback (`absent-guard-is-loud` - a path-exclusion setting protects the machine it is set on) and the verification (the harness can report which instruction files a session resolved), so the isolation claim is asserted against what loaded, not against the layout - the same treatment this subject already gives an enforcement claim. **Finding 2 is a second enumeration denial found in a subject's STRUCTURE rather than its prose**: `line-earning` asks is-it-unreachable and does-it-change-behavior, `enforcement-demotion` asks could-a-program-decide-compliance (yes->gate, no->prose), and **both branches of the demotion sort presuppose an agent that CAN perform the behavior**. Neither asks whether it could have complied at all. What makes it a technique rather than a nicety is corpus-internal cost: this subject rests on compliance falling with instruction count, so **a line written against a capability gap cannot work AND taxes every line that could - the one instruction-file edit that is strictly negative**. Named signature: **a rule restated with escalating force across several revisions is nearly always a capability gap being sharpened as though it were a wording problem.** Landed above the source's altitude - the mechanical discriminator (attempt the behavior outside the agent with exactly the means the agent had) is the corpus's, with `gate-sees-target` because asking the agent reads a proxy and a model will readily accept blame for an impossibility; the two short-circuit tells (uniform-across-sessions = capability, since genuine steering failure is probabilistic; persistence at the top of a minimal file excludes steering by construction) come from the dilution model the subject already owns. **Finding 3 triaged as a technique and resolved as an amendment, the cheaper and more honest landing.** `failure-attribution` says a red case is "owned by exactly one" of six owners - complete for a single-call system, and an agent is not that shape. The finding is not that a row is missing, it is that **the funnel's own tells route tool failures to the wrong owner and the wrong owner is the expensive one**: an agent calling the right tool with wrong arguments because a field name was ambiguous fails the prompt's tell (a person reading only the prompt would NOT make the same mistake) and fails the pipeline's tell (raw output and recorded outcome AGREE), so it falls through to **model**, whose prescribed response is a different model or an accepted limit. **A strictly correct application of the technique produces the exact mis-attribution the technique exists to prevent**, prescribing a model migration for what a rewritten tool description would fix. Seventh owner placed between pipeline and prompt; golden path's "six layers", the two-of-six heading and the resist-all-six line all corrected. **The XL subject proposal dissolved on verification and that is the catch**: the near-empty was misleading (no subject slug matches "harness optimization") but the material decomposes cleanly across two mature subjects - **a method with a paper is not automatically a subject, it is often a route through subjects the corpus already has.** Lead banked on the +9-10pp claim with a return condition; 9 untriaged with anchors, row 4 (the *regressed* cell a shallow reflection loop never computes) flagged as the likeliest real finding on a second look. Zero declines - the operator left rows unpicked rather than rejecting them, so the decline ledger is untouched |
| 2026-08-28 | `web:acceptmarkdown.com/reference` + its eleven named children - "Reference - Markdown content negotiation for AI agents" | **standards index** (new class) - a link hub whose children are PRIMARY standards | 281 (hub) / ~87k (children) | 11 | 2 | 3 | 0 | [[2026-08-28-acceptmarkdown-reference]] - **the class whose hub is structurally too thin and whose yield is structurally high.** `research-ingest` exited 3 on a 281-word page of pure pointers, correctly; everything mined came from the eleven documents it points at. The row it superficially resembles is the second-hand survey - somebody else's links - and it is the opposite: a survey is reliable only for *that* the world moved, while a standards index points at documents that **authorize a golden path outright**, no convergence wait, no second source. Corrective: ingest the hub for its link list, then run the children as a batch; do not judge by `--min-words`. **6 fetches, all extraction, corroboration cost ZERO** - when the children are RFCs the primary is already in hand, which is the inverse of the review/listicle economics the budget was written for. **A hole, not a seam, verified three ways**: `content negotiation`, `Accept header`, `Vary`, `406`, `text/markdown`, `rel="alternate"`, `q-value`, `llms.txt` all return ZERO across 3,389 documents in 8 bundles; `research-map` returned only slug collisions ("content" in `content-drift-and-revision`, "header" in `browser-credential-boundary`), the near-empty signature - so the two nearest real neighbours were opened before anything was written, and both are different actors (`client-fetch-cache` owns the CLIENT's memory cache, not a shared intermediary key; `docs-content-model` names a "machine-readable site inventory" among its projections but gives every consumer the same body, so it has no representation axis). The 150-subject bundle has no HTTP-representation subject at all: a layer, not an opinion, which is what made it `XL`. Subject spec dispatched (`representation-negotiation` -> `integration`, placement verified against `taxonomy.json`: the category is **flat**, 8 of a cap of 10, so a flat add is legal and a subcategory would be wrong) with **five fragments folded in** that each looked standalone at extraction, both rejected placements argued from their own boundary statements, four must-not-absorb boundaries, and an explicit instruction to override the brief. **The hub's two framing errors were worth more than its correct links.** It files `.md` sibling addresses + `Link: rel="alternate"` under a heading citing the *proactive* negotiation section - those are **reactive** negotiation, and the standard makes the choice a real cache trade (proactive "limits the reusability of responses for shared caching"; reactive is advantageous "generally when public caches are used"), so the conflation hides the trade and became proposed technique 1. And it asserts a refusal status is "the right response" when `Accept` cannot be satisfied - the standard says an origin "can either honor the header field... **or disregard** it", making the refusal a policy choice, not a correctness requirement; that correction is written INTO the proposed technique so the received wisdom cannot enter through the back door. Third error recorded as a class signal only: it cites "RFC 9110 SS12.5.1 Proactive Content Negotiation" - SS12.5.1 is `Accept`, proactive negotiation is SS12.1. **A standards index mis-citing its own headline entry is the failure this class walks into, and it costs one grep to check.** **The landed finding was the enumeration hunt, paying an eighth consecutive run** - and the enumeration was stated TWICE in one file, which is the reusable form: `web-scraping` gives its alternatives ladder in the opening ("an official API, a feed, an export, a negotiated data license") and again as a four-bullet section, and neither contains *the same page in another representation from the same server* - the cheapest entry on the list, one request, no contract, no key, same address. When an author reaches for a list from two directions and gets the same set, the gap is in the model, not the typing. The acquire row had exactly two modes (plain fetch / rendering engine), both assuming the browser document is all that is on offer. **The naive reading is wrong and that is what made the technique**: "negotiation replaces scraping" holds only for a representation **authored at the origin** (the feed branch - rules are overhead); one **converted from the same markup on the fly** - the common managed case - is a derived value whose recomputation path is "convert today's markup", so it inherits the redesign it appeared to protect against, relieving the parse stage and buying nothing against the shape-change adversary (`derivation-names-recomputation`). So `negotiated-representation-probe` exists to close a trap that only appears AFTER the probe runs: derived case returns, text looks clean, pipeline gets RELAXED - tripwires loosened, hit-rate baselines abandoned because no selectors remain to count - converting the subject's own shape-change detector into a **shape-change blindfold at the moment the input looks most trustworthy**. Its table makes one row unmissable: derived => every instrument unchanged. Third probe outcome kept from the standard: the server may simply disregard the preference, so read the response's declared type and never your own request (`unknown-is-not-a-value`). **The application named in the dispatch reports a structural fact, not the ratio**: an edge offering publishes a worked response measuring 725 tokens against 12,345 (~17x, doc dated 2026-07-13, one input format, 2 MB origin cap) - an existence proof, not a distribution - but it is BY CONSTRUCTION the conversion arm of proposed technique 5 and not the projection arm, since "no origin changes" is the selling point. **The most widely deployed instance of this subject is the one that structurally cannot make the drift guarantee**, and nobody designed that. Zero declines - a property of the class: primary standards do not produce the half-corroborated middle that generates them. Housekeeping: `build-index --check` was stale on arrival from commit `ccc7798`, regenerated and folded in |
| 2026-08-27 | `web:latticedb.org` + its documentation - "LatticeDB, embedded property-graph database" | **vendor documentation set** (marketing surface + four comparison guides), wrapped in an **operator dispatch** | 1069 (landing) + 1524 (two comparison guides) | 12 | 5 | 4 | 2 | [[2026-08-27-latticedb]] - **the anti-vendor-repository, and the class file needs one amendment.** The row predicted it exactly: the benchmark table was the least useful thing present and every accepted finding came from the docs' honest-boundary pages. But the densest first-party document here is **the page titled after the competitor** - "vs Kuzu" states above its table (not in a footnote) which single row was measured head to head and which are third-party figures on hardware it does not control, then says to treat the rest as "worth investigating on your own data, not a benchmark result", and devotes a section to what the archived rival did better. **For a vendor in a crowded class, seek the page arguing against its own product.** The operator dispatch's three sub-questions routed three ways and only the third paid - second consecutive run confirming split-at-Phase-3. The run's best finding came from the vendor **getting it wrong**: its flagship query computes BM25 relevance and discards it into a boolean filter while vector distance alone orders the result, in the headline example of an engine whose whole pitch is answering the lanes together - so `hybrid-lane-fusion`'s rule that multi-lane convergence is evidence took an amendment naming its unstated precondition (**lanes must be independent**; a lane seeded by another's output counts one signal twice, hardest where the seeding lane was most confident, and diversity cuts pass it because the items genuinely differ). The **enumeration hunt paid a seventh consecutive time**: `retrieval`'s roster is lexical/semantic/recency/always-include - two similarity matchers, a clock and a pin, and **none can surface the item that DISAGREES with the top hit**. New technique `relationship-proximity-lane`: not a peer lane (a function of results, so its budget is a fan-out cap), reach is not path enumeration, per-relation policy, and **owning edges is not the trigger - enumerate the readers of the relation**. The application is negative and the tree proved it: `personas` implements the roster faithfully and holds TWO typed relation stores with no request-path reader - one written by a ledger and read only by export/import/wikilink-render, the other a table plus a **reverse** index built for inbound traversal, a six-name vocabulary including `contradicts`, and an 8-line module ending `Phase 0: stub. Phase 2: traverse, add_edge, contradict_scan` with no writer and no reader. It named the one relation similarity cannot reach, years before anything could consume it. Two more techniques folded into the storage-engine spec (now eight, second reconciliation tree recorded): **a dataset is not a workload** (`politicas` HAD the workload and measured the engine losing; `personas` has the data and no workload) and **a multi-modal engine consolidates seams, not costs** - read its benchmark suite for the class it OMITS, since LatticeDB is strong in the four classes `politicas` measured as "add nothing" and structurally cedes the one it measured as "add something" (43x). Executed cross-repo in both trees on operator instruction; `personas` scoped to a decision record only because its `db` crate is mid-refactor under a parallel session. 2 of 3 fetches, both extraction not corroboration; corroboration cost zero and came from two connected trees - seventh consecutive locally-corroborated run. |
| 2026-08-27 | `web:github.blog` - "How to evaluate LLMs before production" | first-party practitioner account (**hybrid**: numbered how-to wrapped around one shipped system) | 2716 | 12 | 4 | 1 | 3 | [[2026-08-27-evaluate-llms-before-production]] - **the run where every finding came from a sentence in which a forged document claimed its own completeness.** Route-per-half decided the whole triage and held perfectly: sections 1-7 are generic how-to with a listicle's reliability, the first-party half is the sentences about their own classifier, and every landing came from the second half while every catch came from a section heading. 0 of 3 fetches - eighth consecutive zero-fetch run - and the strongest corroboration was sitting two subjects over in the same bundle. The neighbourhood was the corpus's most mature (`eval-harness` at 8 techniques, five `quality-scoring` subjects on judges and gating), so **nothing was a hole and all four findings were too-narrow enumerations**. `scenario-design` listed five ugly-case regions and the source's failure is in none of them: **`distractor` returned ZERO corpus-wide** and the missing region is well-formed, in-distribution, unambiguous, single-feature and non-adversarial - the model evaluates the wrong object because the context holds a more attention-grabbing neighbour than the designated target. The mechanism is the keeper: **curating a scenario set removes distractors as a side effect of tidying it**, so the suite measures *identify the salient item* when the task is *evaluate the designated item*. **Cross-bundle inversion, named not linked**: `recruiting/llm-era-work-sample-design` forbids distractors outright because a decoy hands a judgment probe an answer key - the discriminator is **captured or planted**, and a subject note was created on the recruiting side to record it. Second amendment corrected "captured reality is representative by construction": true of the **input**, false of the **label**, which gets back-filled from a workflow outcome that renders four ground-truth states as one definite value (`unknown-is-not-a-value`). That one corroborated **entirely corpus-internally** - `proactive-nudges/efficacy-feedback` keeps a waved-away prompt "deliberately ambiguous" and `remediation-handoff/evidence-based-auto-close` refuses to collapse a dismissal into a resolution, so the rule already lived in this bundle and had a free pass in the eval lane. `metric-role-contract` landed **above the source's altitude** - the source gives a three-tier table, the durable rule is that its third tier is just more of its second, so: **exactly one metric is optimized and everything else is a threshold**, with the constraint chosen by **irreversibility, not magnitude**, and a breach disqualifying regardless of the primary's gain. Home was contested (gating owns "between two runs"; a metric contract precedes the first run) and the boundary is stated in prose, not linked, because the subjects are in different bundles. `failure-attribution` closed a **missing stage** found by the asymmetry hunt: the subject distrusts a green verdict in two techniques and a golden-path section and had never distrusted a red one. Six owners checked most-upstream-first, and **two of them are not the system** - a label or dataset failure goes red with no defect present, and the fix for either improves the score while moving the system away from correct. Banked lead: `comparison-modes` calls absolute scoring "the only mode that supports a gate (ship if above X)", now the one-metric degenerate case - return on a second independent source or an opened harness. Shared checkout: another session landed 4 techniques, 2 applications and a practices lane mid-run; index/catalog deliberately NOT committed |
| 2026-08-27 | `web:github.com/context-labs/whip` - "whip, a fast coding-agent harness in Go" | open-source infrastructure engine (**engine + operating docs in one tree**) - sub-class: **comparative-design corpus** | 678 (page) / ~15800 (in-tree docs) / ~10300 (rival teardowns) | 16 | 7 | 0 | 0 | [[2026-08-27-whip-coding-agent-harness]] - **the sub-class whose yield is other people's refusals.** The archetype held (seventh consecutive zero-fetch run; the 678-word landing page was install instructions, the source was 15.8k words of design docs plus the tree), but this tree carried something picomq did not: first-party teardowns of FOUR rival implementations of the same product category, each cited `file:line` into that rival's source, plus a PTY probe of five competitors' first paint. **Every "what we should NOT take, and why" section is a discriminator someone was forced to draw** - the research-model-release sibling-instruction property, at a section per rival instead of one per release, and four of six landings came from that half of the tree rather than the engine's own docs. The prediction that high relevance means a high catch rate was reasonable and instructively wrong: the corpus held every *concept* in this source and was missing *stated boundaries* in five places, so every landing came from an enumeration, an asymmetry or a missing producer. Two enumerations paid: `subscription-lifecycle` says its fan-out loop "has one sharp edge" and gives snapshotting, which answers set mutation and not lock ownership - the second edge is the ABBA deadlock between a dispatcher holding the registry mutex and a subscriber parked on the UI queue. `single-flight-primitives` took its second amendment in two runs (picomq added `merge` as a sixth policy; this scopes `join`): join is written for a computation, and pointed at a durable resource the execution recurs while the one-shot completion signal cannot. The missing stage was bigger: the `prompt-assembly` layer table has five layers and every one is *authored*, so the growing tool-use transcript - the only part of the prompt that grows because the system works - was owned by nothing, and `history-compaction` landed with the pairing invariant, the third resume regime `continuation-prompts` does not enumerate (carried and structurally broken), and the reactive compaction path with its once-per-turn guard. The missing *producer*: `mid-turn-steering` models the injection mechanism completely and models exactly one producer, the human - and its sharpest rule ("refuse loudly when no turn is in flight") inverts for machine sources because its own justification rests on the caller having a second door. | 
| 2026-08-27 | `web:github.com/SenteLabsAI/OpenExecutive` - "AI-powered virtual executive team" | vendor repository (**open tree** - the archetype's engine is closed, this one's client is not) | 2723 (landing page) / ~1950 (in-tree operating docs) / 894 files | 14 | 5 | 4 | 0 | [[2026-08-27-openexecutive-virtual-executive]] - **the class variable is not whether the engine is hosted but what fraction of the product is in the tree.** The class entry sends you hunting for the data model in a client's types because the archetype's client is closed; here the engine is hosted and the whole application is open, so the "stated production rules" third is not a page but nine hundred files, and every landing came from code rather than from any document. 0 of 3 fetches - seventh consecutive zero-fetch run for a source carrying its own primary material. **Three of five accepted candidates share one shape: the prose states an absolute, the code knowingly breaks it for a stated correct reason, and the prose never names the reason** - the contradicted-source pattern arriving from inside a single source, and cheaper than usual because both halves ship in one tree and no fetch can adjudicate what the code settles. Landing 1: four artifacts (readme, both environment configs, architecture doc) blame the claim for forcing single-instance while the comment beside the claim says it is safe across processes - the line that forces it is an unconditional startup requeue named nowhere. `terminal-state-recovery` already calls the blanket sweep an anti-pattern in depth and frames it **entirely as verdict correctness**; a grep for instance/scale across it and `lease-renewal` returns two incidental matches. **A subject that models process death in five techniques, silent on the one number process death decides** - amendment, not technique, since the fix is defined in the existing `adopt` verdict's own terms. Landing 2: `layered-composition` orders by volatility and stops at ONE implicit boundary; `context-budgeting` prices compression; `cache-continuity` prices the prefix as a monolith - **nobody owned the cut**, that declared breakpoints are a scarce request-wide budget shared with tools and history, each with its own lifetime. New technique `cache-breakpoint-allocation`; the golden path's layer table already had a *Changes* column and the technique names it the allocation map (cut where the column changes value, not where the topic does). Folded in the correction that *no dynamic content in a cached block* is the wrong discriminator - it bans process-stable computed values and permits a hand-authored string rewritten per request; the real test is **can this value change before this block expires**. The advertised "85% hit rate" is a count with its predicate removed and went in as a caution, not a citation. Landing 3: found by the **asymmetry hunt** - `authentication-and-scoping` models inbound in full, egress gets ONE bullet in `untrusted-result-handling`; both files "cover" the seam and only opening both shows one has a model and the other a sentence. New technique `egress-argument-gating`: two orthogonal scans each blind where the other sees (value-shaped survives unknown field names, key-shaped survives non-string capabilities, and the gateway writes its own residual hole down); strategy chosen by whether the argument surface is enumerable, with **both strategies in one file** and the discriminator stated; the gate is directional so revoking stays permitted; and the sharpest sentence in the repo - **a principal allow-list cannot express a population**, so a share-to-anyone passes a well-formed roster check that found nothing to reject, and must be refused by a rule running BEFORE the allow-list. 4 leads (mandatory segments outside the overridable region; topology from tenancy cardinality; auto-disable of non-portable provider features; agent must not sign as a rostered human), 5 untriaged with anchors, 0 declines. Shared checkout: a parallel session registered its own new technique mid-run, after this run's `build-index`; generated files deliberately NOT committed |
| 2026-08-27 | `web:langchain.com/blog` + `web:github.com/langchain-ai/openwiki` - "Building Self-Correcting Memory in OpenWiki" | **paired single-voice source**: first-party release walkthrough + its own vendor repository | 1559 (blog) / 4739 (repo) | 17 | 8 | 3 | 0 | [[2026-08-27-openwiki-self-correcting-memory]] - **the run whose best finding was the one I got wrong first.** Two artifacts, one vendor, so within-batch convergence is VOID and every landing earned its way corpus-internally: 0 of 3 fetches, seventh consecutive zero-fetch run for a source carrying its own primary material. Mapping our own contribution lane I read the two live `signals/` files, saw `consults` + `deviations`, and concluded there was no channel for a verification verdict - then read the checker, which accepts a third key `citations` (`resolved`/`moved`/`gone`, keyed by slug never by path, counts-only with the reason in the failure message). **The corrected finding is strictly stronger**: the channel is specified, the collector - the lane's ONLY writer - has no code path that emits it, and the scan reads `gone` while discarding the two counts that carry the denominator. Specified, unpopulated, half-consumed. Two runs now say it, so treat it as settled: **structural claims are verified against the authority - the checker, the schema, the taxonomy - never inferred from the data lying around**; data shows what contributors happened to send, only the validator says what the lane is. Eight of seventeen candidates clustered on one neighbourhood - the lifecycle of a claim whose evidence moved - which the corpus covers at file granularity, at artifact granularity, with verdicts in another bundle and by recency decay, and owned nowhere at **proposition** granularity. Landed 4 techniques, 2 amendments, 1 negative application. The best knowledge finding was an **asymmetry**: `docs-sync` measures what a REPORT may claim with total rigour and says nothing about when a DOCUMENT may advance its own review date - the exact field wall 7's detector consumes to choose its query window, so an unearned bump moves the detector's horizon past the changes it exists to find and it reports clean forever. Seam: `docs-content-model` owns the field's shape, `docs-sync` owns queries over it, neither owned the write rule. The contradicted pick was again the best one - a complete-set reconciliation contract that is right, with **omission as its retraction signal**, which makes forgetting and deciding byte-identical. Two picks merged into one technique twice; when the operator picks two rows that are one decision, land one file and say so. |
| 2026-08-27 | `web:github.com/picomq/picomq` - "PicoMQ, durable streams over object storage" | open-source infrastructure engine (**engine + operating docs in one tree**) | 453 (page) / ~14000 (in-tree docs) | 11 | 3 | 2 | 3 | [[2026-08-27-picomq-durable-streams]] - **the class whose promise is in-run falsifiability, and it fired twice.** Engine, ~14k words of design docs and a wire spec ship in one tree, so every claim was checkable against the code implementing it: 0 of 3 fetches, sixth consecutive zero-fetch run for a source carrying its own primary material. The ingest returned 453 words because a repository landing page returns install instructions; pricing the run off that number was the first mistake, corrected in one phase. **Age predicts the reliability of a project adoption claims, not the density of its design decisions** - 15 commits and days old, and it had just finished making every hard choice it will ever make. The yield was a contradiction: the write-path doc describes group commit as self-tuning and knob-free (batch = whatever arrived during the previous flush), while the shipped config carries a 250 ms timer, an 8 MiB cap and a 50-deep in-flight pool - **and the same repo implements the idealised version correctly in its metadata sink**, which blocks for the first command, drains what has queued, commits, no timer. Two group commits, one tree, opposite defaults: the **sibling-systems property**, not specific to model releases after all, with the discriminator visible in the diff - serial flushes self-close, pipelined flushes cannot and must reintroduce a timer. Home came from the **enumeration hunt**, sixth consecutive paying run: `single-flight-primitives` lists four second-caller policies and tells the reader to pick one explicitly, and **merge is a fifth it does not contain** - join returns one shared result, coalesce discards intermediate arrivals as waste, merge carries N distinct payloads through one execution and discards nothing. A designer walking that list for a durable write path finds no correct option. All three findings landed as **amendments, no new techniques**: the merge policy plus a window-closing section; a cross-store **inversion** of the purge ordering rule (payload before reference - one direction leaks a retryable pointer, the other leaks bytes nothing references, and only one leaves a reaper); and lease-cost-as-engineered rather than measured. Two leads banked (stale-view decision classification, awaiting a second sighting to land as doctrine; transfer pre-warm), three untriaged, three caught. |
| 2026-08-27 | `web:kciter.so` - "How to design animation" (Korean) | first-party practitioner account (**methodology essay** - new sub-class) | 3615 | 8 | 4 | 0 | 2 | [[2026-08-27-kciter-animation-design]] - **the class inverts the usual economics: the strip test killed nothing.** The source is mathematics and design vocabulary, not proper nouns, so every candidate survived the cheap filter and the whole cost moved to prior art. `motion` is a 9-technique forged subject that a slug map returns for every animation term - but it is a **governance** system (what the vocabulary permits, who owns the engine, what stops a loop) and the source is an **authoring** method (how a movement becomes a thing worth naming). They touch at one seam and the seam held all four findings. **`research-map` returned a TOTAL empty on `spring physics` over material the corpus covers in depth** - the purest near-empty failure yet recorded: springs are all through `engine-selection` (retargeting from position *and* velocity, the interruption story) and in the consumer evidence file as a real rAF engine. A zero from the instrument is a claim about slugs. The genuine gap was invisible for the same reason - no slug expresses "the contract assumes a timed curve". **Landed as one root plus three amendments, not four amendments**: the preset contract (intent, duration class, easing role, fallback) was forged for one curve started by something and running to completion on its own clock, and each under-served class breaks a *different* assumption - a **spring has no duration** (and `taste-budgets` files it as "one characterful curve", a third incompatible reading), a **composite gesture is more than one curve**, and **value-driven motion has no clock of its own**. New golden-path section states the boundary; the rule is now intent+fallback per gesture, timing+character **per track**, each track declaring a duration class with an easing role OR physics parameters with a **settle bound** - because a gesture with no duration escapes the millisecond entrance cap entirely, which is how the one unbounded gesture becomes the one nobody audits. Its reduction is an **instant settle**, not a shorter curve. `engine-selection` opened "one of **three** engines" - an enumeration, and the missing member is an authored timeline exported as data, whose ownership story inverts every answer in the section (no global switch reduces it because the platform does not consider it animation; its reduced-motion story must be a **second exported variant**); the discriminator is not complexity but **must it respond to input while running**, with the trap that a trigger is not an input. `unprompted-motion-lifecycle` said "for unprompted reveals the answer is **always** one-shot" - correct for scroll-*triggered*, a bug for scroll-*driven*, where reversal is the point; scoped rather than deleted, with a one-question review test (**if the user stopped scrolling, would anything keep moving?**) and the substitution that a driven gesture owes bounded travel but no stop control. New technique `gesture-decomposition` (10th) closes the missing stage - the subject was thorough from "a preset exists" onward and silent on how a movement becomes one, which is the door per-component keyframes return through. **0 of 3 fetches** - sixth consecutive zero-fetch first-party run, and here the claims are mathematics, so no fetch could have added anything. Reusable: **when a contract has mandatory fields, check them against what the sibling technique permits** - two of four fields were unfillable by an engine the subject itself recommends and neither file noticed. Shared checkout: 4 commits landed mid-run and another session's WIP was live; index/catalog deliberately NOT committed |
| 2026-08-27 | `web:scottspence.com` - "Agentic engineering: a practical guide to reliable coding agents" | first-party practitioner account (**guide-shaped**) | 2600 | 15 | 1 | 2 | 8 | [[2026-08-27-agentic-engineering-practical-guide]] - one landing out of fifteen, and the ratio is the lesson. **Every accepted word came from the author's "they also exposed weaknesses" list; every recommendation in the guide was a catch.** New operating note for the class: in a first-party account read the failure list first and the recommendations last - a recommendation is what the author believes, a stated failure is what their system did to them, and only the second is reliably absent from a mature corpus, because the corpus was built from other people's recommendations too. **Guide shape named as a hazard**: it reads high-yield (organised, confident, comprehensive) and is structurally low-yield, since comprehensiveness over known ground is what a mature bundle already has. The picked candidate's headline - put the trust boundary where the agent cannot edit it - is already owned and better (`hitl-approval` has a section titled "The gate lives in the substrate, not the prompt", anchored to `gate-sees-target`, with two corollaries the source never reaches); stopping there would have shipped a duplicate. The finding came from the **enumeration hunt**, fifth consecutive run it has paid: the golden path declares "the subject owns **two flows that are mirror images of each other**", then adds "A third flow" - and all three gate an **action**, while the source's measured failure ("plans needed legitimate amendments more often than I expected", over 173 real harnesses) names a transition none of them covers. The machine's **terms** - scope, route, definition of done - were governed by nothing, assumed static from dispatch. The subject had also **denied too much**: `gate-state-machines` says approval transitions are driven only by a human, correct for verdicts, but applied to the whole harness record it forbids the amendment lane the evidence says is the normal case. New technique `fixed-policy-amendable-plan` splits the governed record by **write authority rather than by content** - fixed tier (allowed paths, forbidden ops, validation commands, stop-and-ask condition) outside the executor's context and read-only to it; the route inside is the executor's to revise, each revision naming the fact that made the old plan wrong - and the split then IS a trigger predicate: inside the boundary amend with no gate, requiring the boundary to move stop and escalate, and the executor may **propose** the new policy but never adopt it. Three of the source's five measured weaknesses folded in rather than becoming separate candidates (over-broad forbidden patterns as the asymmetric-cost section - too broad and every task escalates, reaching gate fatigue by the route that looks most responsible; stale task state as the reaper paragraph; uninterpretable failed runs as "what this cannot do"). Adoption numbers cited in as an **existence proof, not a distribution**, stated in the document so a later reader cannot mistake n=1 for a rate. Row 2 declined as a genuine catch - the corpus's "When a gate is mandatory" (four triggers, explicit complement, "a correct gate map is mostly white space") says the proportionality claim better, and the one non-identical edge (corpus prices over-gating as human attention, source measured task latency) was too thin to carry a technique. **0 of 3 fetches - sixth consecutive zero-fetch run for the first-party class.** Shared checkout: a parallel session ran `build-index` mid-run and swept this run's in-flight technique into its own regeneration, so generated files were left uncommitted by this run |
| 2026-08-27 | `youtube:LeUUxLRdvho` - "Why The Best Software Engineers Focus On System Design" | first-party practitioner account (**interview format**, graded rather than segmented) | 8864 | 15 | 1 | 3 | 0 | [[2026-08-27-best-engineers-focus-on-system-design]] - **the bundle owns every mechanism of scale and no decision about scale.** `vertical scal` returns ZERO across 149 subjects; `horizontal scal` returns one, inside `limiter-topology`. Verified three ways before anything was written: every scale-adjacent subject begins AFTER the sizing decision (`rate-limiting` presupposes a chosen ceiling, `admission-queue` a fixed capacity, `runner-fleet` CI capacity), and the nearest-looking neighbour is not one - `metric-forecasting` states its own boundary as owning "whether the claim may be **displayed** at all", a dashboard discipline that governs drawing a growth curve honestly and says nothing about what to build because of one. Missing stage one under an excellent stage two. Subject spec dispatched (`scale-investment-timing` -> `backend-platform/resilience`, placement verified against `taxonomy.json`: the category is NESTED so a flat add is illegal, `resilience` holds 7 of 10) with **five fragments folded in** that each looked standalone at extraction, the two rejected placements argued, and an explicit instruction to override the brief. **The accepted technique came from a denial, not from the source**: `structure-is-not-delegable` partitions design decisions on whether the outcome is scoreable inside the run, owns the unscoreable half, and hands the scoreable half to `orchestration-to-tool-migration` - whose subject is the LLM tool surface, which decisions a *model* makes in a pipeline, not an engineer choosing between two implementations of one interface. **The class was defined twice in this bundle and owned by nobody.** New technique `scoreable-designs-are-built-not-argued` closes it constructively - what changed is the price of the experiment, not the principle ("measure don't guess" was honoured selectively because building every candidate cost days, so it was reserved for decisions worth days; agent authorship moved the cost an order of magnitude and the threshold moved with it) - carrying three things the source did not say: one harness over substitutable candidates rather than N benchmarks (three benchmarks produce numbers that cannot be ranked but look ranked), the workload choice as the undelegable residue one rung down, and **the cheapness as the hazard** - a number on a two-quarter-lag decision does not inform it, it wins it, which is the mirror image of the subject's existing "taste argument" failure and worse, because it does not look like an opinion. Sibling edited to point back; the partition now has both halves written. **Reusable form: an enumeration that delegates a case to another document is a claim ABOUT that document, and it is checkable in one read** - second time in three runs a "does not apply where..." sentence was the highest-value thing on the page. Class note: the interview format is **graded, not segmented** - the first ~20 min (his own systems) produced everything, the last ~10 (curiosity, learning, motorcycles) produced nothing; read the first third closely and skim. **0 of 3 fetches - fifth consecutive zero-fetch run for this class.** Shared checkout with a live parallel intake run throughout |
| 2026-08-27 | `youtube:zPfxlcVpFgs` - "How I manage 15 AI agents 24/7 as a solo founder" | first-party practitioner **dialogue** - two accounts compared (new sub-class) | 8347 | 12 | 1 | 0 | 0 | [[2026-08-27-managing-15-agents-solo-founder]] - operator picked one row, the doctrine one, so breadth is 11 untriaged and the run is one finding deep. **New sub-class: the dialogue.** Two practitioners with the same job comparing their own systems - where they chose opposite defaults the discriminator is already drawn, for free, no fetch (agent-threads vs public chat: context locality vs adoption legibility); where they converge unprompted, that is the other signal, and both independently said they deliberately ship *less*. Same property as a lab shipping two contradicting sibling instruction docs - **diff the practitioners, not just the transcript.** 0 of 3 fetches, as the class predicts. **My triage read was too strong and verification corrected it downward, which is what made the finding.** I expected "the corpus models machine throughput and missed that the human is the constraint"; `proposal-not-push` says it in its own words - *at machine pace the reviewer is the bottleneck.* The corpus knew. The gap is an **asymmetry, not an omission**: the machine bottleneck gets four measures, distributions, denominators and a whole demand-reduction section; the human one gets one sentence and no capacity model, and everything done about it is per-item (one concern, small, provenance, consistent shape) - **nothing said how many verdicts there are.** So the subject sizes its first server against machine-paced arrival and routes the entire output into a second server whose rate was never written down. Reusable form: **when a corpus states a constraint in prose in one technique and models it thoroughly in another, the finding is the asymmetry between them** - invisible from a slug map or a summary, reachable only by opening the file. New technique `human-gate-capacity`: the merge gate is a server with a fixed rate and it is the only one that cannot be bought, so demand reduction is not the preferred lever but the *only* one. Two things the source supplied that the corpus could not derive - **the overload signatures are opposite** (a machine queue past capacity stalls; a human gate *accelerates*, so rising throughput against an empty backlog is the failure), which let the technique separate a **stall** from a **rubber stamp** by four measures with opposite remedies; and the **independence limit at the subject's own stated floor** - at one person the reviewer is also the dispatcher, so the gate is a check, not an independent review. Enumeration hunt paid a fourth consecutive run: *"Two consequences follow, and the second one is the one that gets missed"* - the third consequence was the finding. Boundary written both ways: `review-queues` owns whether a single decision can be made well, `machine-paced-delivery` owns the rate at which decisions are demanded |
| 2026-08-27 | `web:allthingsdistributed.com` - "DuckDB and the changing physics of analytics" | first-party practitioner account (hybrid: vendor announcement + systems-design essay) | 3583 | 11 | 4 | 2 | 0 | [[2026-08-27-duckdb-changing-physics-of-analytics]] - operator dispatched it at a named weakness ("we default to postgres/pglite"), and the framing was correct, structurally verifiable and understated. **The source originated four findings and authorized none of them** - the class row held exactly, and every landing was authorized by peer-reviewed literature or by a connected project's own measured benchmark. The hole verified three ways: `data-layer`'s four subjects ALL begin after the engine exists (stage zero unowned); `embedded-db` opened by defining embedded as the transactional quadrant only, and all seven techniques are transactional disciplines - a denial that denied too much; and `analytics-store-design` enumerated four portability targets whose 2x2 has exactly one hole. `olap` returns ZERO corpus-wide, while `postgres`/`sqlite` returning zero is the purity floor working - **a concept returning zero is the finding, a product name returning zero is not**. New technique `analytical-reads-off-the-serving-store`: form factor and workload shape as INDEPENDENT axes, threshold is query shape not row count, and the uncounted cost is contention - an analytical read through a single-writer store buys the whole `single-writer-holder-discipline` surface for a workload that never needed exclusion, and **the tell is an operating instruction, not a benchmark** ("run against a COPY when the dev server holds the store", carried verbatim by two trees). `analytical-copy-partitioning` amended in the neighbour bundle: the copy does not have to be a warehouse, every structural rule survives the substitution because they are rules about the ROLE not the topology, so **the fork's trigger drops** - the technique had left only two answers below it. Subject spec dispatched with four fragments folded in. **Corrected my own triage mid-run**: the claim "both projects carry both engines" came from a `grep -r` that swept vendored `node_modules`; a tracked-files survey inverted it to "both evaluated it and neither adopted it", which is the interesting fact - one has a 392-line measured guide, 4 dated cases, cross-engine checksums, negative results, and a rule R3 naming a workload that **still runs on the incumbent**. A decision in a document and a decision in the import graph are different states. 1 of 3 fetches; the canonical primary 403'd twice and the author's own write-up supplied the mechanism |
| 2026-08-27 | `github:herdrdev/herdr` - "the runtime your coding agents live on" | first-party agent-runtime repository (new class) | 607 | 15 | 3 | 0 | 2 | [[2026-08-27-herdr]] - the ingest returned 607 words and they were the wrong 607; every accepted finding came from files the README does not mention. Class carries three first-party documents that check each other: production rules for agents building it (`AGENTS.md`, 290 lines), a skill for agents driving it, and the hooks/recipes that enforce both. **Amends the vendor-skill lesson**: this bundled skill is the counter-example - a *negative* description ("Do not use merely because a task could benefit from a background terminal, delegation, or parallel work") plus a runtime precondition that halts it when unmet. Read a bundled skill for whether invocation pressure produced an ad or a gate; do not assume the ad. All three picked candidates landed in `terminal-multiplexing`. **The golden path's "the ring never blinks" claim fails on the subject's own primary tenant** - a child on the alternate screen has no scrollback to lose rows into, so a byte-faithful ring replays the current grid, not a transcript, and no budget increase recovers it; `bounded-replay-buffers` knew the alternate screen only as a replay-fidelity problem. New technique `occupant-state-detection` merged two candidates once the home contest resolved: `fleet-orchestration/lifecycle-signals` owns the state machine and ranks raw output weakest, and this source's whole situation is the case where its tier one *does not exist* - so the screen-derived substrate lands here and supplies that door. Grounded in 21 per-occupant rule manifests, not the docs; sharpest item is a code comment preventing **the user's own typed text from impersonating a working signal**. 2 of 3 fetches, both missed - the citation was in the tree's vendored emulator bindings all along |
| 2026-08-27 | `youtube:jp0PGmAwK3w` - "This 600M Model Fixes What Whisper Leaves Behind" | second-hand practitioner review (vendor release demo, new class) | 1565 | 10 | 4 | 1 | 1 | [[2026-08-27-s1-mini-transcript-cleanup]] - shortest source ever mined (1.5k words) and a good run, because the operator asked for installation requirements and that forced the primary source. **For this class the fetch IS the extraction**: a demo is organised around a happy path and states no operating constraints, so the control-line grammar, greedy-only decoding, the output ceiling, the behavioral-toggle trap and "empty output is expected" were all in the model card and none in the video. New technique `transcript-normalization` closes voice-io's missing stage - the corpus owned capture->transcript, transcript->command and display-text->speech, but nothing owned transcript->**written text a person reads**, one half of a symmetry it had already named. Its best section is written AGAINST the source: normalize toward a reader, never toward a reasoner - the video's proudest example (clean the transcript before the coding agent) is the one placement where the rule inverts, because a 0.6B normalizer seeing one utterance edits input for a model holding the whole thread. `stt-pipeline` amended: a stage further down gets a different empty (three empties, opposite meanings; the default integration raises an anomaly every time somebody clears their throat). Consumer verdict **do not adopt**, written as a negative application - the tree has exactly two dictation destinations and the reader/reasoner rule disqualifies both, which nobody designed. 2 of 3 fetches |
| 2026-08-27 | `youtube batch:x3Go2UiHZWk+RwynWBXuRaA+H3yU28Mqu9E+oECv0lTU8mo` - 4 AI-editing tutorials, 3 voices | practitioner-tutorial batch (editing lane, operator-dispatched) | 7636 | 9 | 4 | 0 | 4 | [[2026-08-27-video-editing-batch]] - the dispatch batch: 7.6k words -> `cut-compiled-from-source` (NEW, "compiled" had zero prior art - the edit as a declarative composition; the critique pass READS the source instead of watching pixels), transcript-as-derivation-spine on derived-turn-markers (3 authors), plans-as-markers on edit-plan-over-regeneration (propose/approve/apply in the editor annotation channel), generate-to-see-rebuild-natively on anchored-variation-slate. Plus an executed headless-editor probe on the dev machine (boots -nogui, full API, one one-time GUI preference gates external connections) landed as the consumer plan appendix. Shortest source (699w, a builder narrating their own connector) was the strongest |
| 2026-08-27 | `youtube batch:DfdlAwJ4SKw+8tnO15fJWVE+8obne_qS6MY+Whh8vCqboxQ+olXpy9Hu7R8+W-DJnoL1Y18+OF9xcQCQSFc+hpR_4UXGpuo+aTnek5komeE+kv1o8eKOcas+yyZQXxkVP14` - 11 AI-video tutorials, ONE creator | practitioner-channel-corpus (new sub-class) | 22371 | 12 | 6 | 1 | 5 | [[2026-08-27-video-workflow-batch-3]] - the channel corpus inverts batch economics: one voice voids in-batch convergence, so yield ran on corpus-vs-source novelty and cross-run corroboration only. Six amendments, zero new techniques: paired-panel anchors (both rung-3 frames cut from ONE image), the blocking frame as the camera schematic (third member of annotate-not-describe), the built previz plate (motion-plate-library first corroboration + fourth source), board chaining/panel repair/pacing contract (storyboard technique third author), voice minted from either direction, sample-set coherence before capture. Exemplar-conditioned story generation banked. Closes with the consumer-app video-generation integration plan (operator dispatch). 0 fetches |
| 2026-08-27 | `youtube:rKo9iLGjUbs` - "I Built a FREE App That Runs Your Entire Business" | practitioner-build-walkthrough (personal tool, new class) | 5750 | 12 | 3 | 0 | 2 | [[2026-08-27-control-center-dashboard]] - new class, a hybrid whose halves have opposite reliability: the product tour is a demo (strip test deletes all of it), the sentences about the tool he has run daily for months are a first-party account, and every finding came from the second half. The discriminator is cheap - is he describing what the tool does, or what happened to him while using it. Three findings, **0 fetches** (budget 3), all corroborated corpus-internally. `dedup-records-who-carried-it` (grant-source-landscape's 6th normalization-contract clause) closes a seam INSIDE one bundle: `verification-passport`, `majority-rule-doc-consensus` and `funder-portal-resolution` all consume a source count that the bundle's own ingest layer had and threw away one merge earlier - and the source implements it wrongly (counts newsletters that relay each other), so the technique carries the half it lacks: carrier sets are recorded at PUBLISHER granularity or they measure promotion, not truth. `task-envelope` gained the enumeration case - a brief that lists the parts supplies a machine-checkable done criterion by accident, and the model satisfies THAT one: fires on time, on the wrong axis, every surface present and every link dead (probe the leaf, not the shape). `acquisition` gained the per-user-installed rung - the ladder assumes one registered client, so a tool run once per user reaches the ceiling by walking the floor (delegated grant whose precondition is a manual paste), plus a 5th provenance case and the rule that a setup guide must never resolve the provider's trust prompt for the reader. Synthesis banked not landed: candidates 1+2 are independence accounting inflating and deflating; only one half was picked |
| 2026-08-27 | `youtube batch:Hjj-yiDJXTw+LsfcOoCc88I+HMVSCEh72n4+0v534yAyhwg+R8QvtGlVI_E+-ZqilAZOFLE+tZReV23ncuc+lvzPURroPxE+z3uX2YLGudM+VmDpBD_47M0+pYsc01oioKg+Ie0niOVuWzg` - 12 AI-video tutorials, ~5 voices | practitioner-tutorial batch (2nd) | 25336 | 13 | 10 | 0 | 7 | [[2026-08-27-video-workflow-batch-2]] - count voices, not videos (6 of 12 are one creator; same-author repetition is one sighting). Back-to-back batches compound: four landings were widenings of run 24's day-old amendments from independent sources, and the typed-input shape hit its third run in a row and landed as **law `typed-input-owns-its-channel`** (camera path / beat grid / emotion dial). New: `reference-role-map` (every attachment one named role, negative scope, group multiplication) + `storyboard-grid-conditioning` (numbered textless panel grid as one conditioning image; "storyboard" had zero prior art); moving-reference amendment (continuity carry vs scoped choreography transfer); the-import-is-the-lever (grade the anchor, the look rides in); edit passes are lossy (3 practitioners, 2 runs). Mid-run: a parallel session landed `character-identity-continuity`; triage re-based on HEAD. 0 fetches |
| 2026-08-27 | `youtube batch:3rDs6FhFoUQ+6arKHnBNJMI+5dWgZDka3Ww+9Y1A2nw_Veg+83NI19L7fhQ+tYJQusOS2jI+d7J82el-6AU+zYPgz6sOy74` - 8 AI-video workflow tutorials | practitioner-tutorial batch (first batch ingest) | 26578 | 15 | 10 | 1 | 3 | [[2026-08-27-video-workflow-batch]] - first batch run: 8 sources deduped at extraction, and within-batch convergence became the triage signal (every 2-source candidate landed). The yield clustered on the bundle's missing stage - multi-clip sequence continuity - as 2 new techniques (`performance-direction` completes cinematic-language's stack with the performer; `motion-plate-library` restores the layout/animatic split as a sourcing channel) + 6 amendment sections (identity sheet single-face rule, persistent-state variant sheets, voice as second identity surface, default-with-scoped-override style blocks, schematic spatial anchors, briefed match cuts, output-derived extensions, anchor-texture provenance). 0 fetches; vendor-channel tutorials read as first-party accounts wearing an ad - the strip test deletes the sponsor |
| 2026-08-29 | `web:claude.com/blog` batch - "Claude on call: first responder for CI/CD failures" + "The AI-Native SDLC playbook" | batch: first-party practitioner account + vendor doctrine document | 14050 | 15 | 5 | 1 | 3 | [[2026-08-29-ai-native-sdlc-and-ci-on-call]] - one technique in the #2 worklist subject: `oracle-frozen-during-repair` (quality-gates) - the in-task half of the rule `proposal-not-push` already enforced at the merge gate; red-first ordering is the proof of defect, the freeze is mechanical, the release valve is a separate human task. Four amendments: eval golden-set trigger includes instruction files/skills/hooks; self-healing gets a second ladder (magnitude buys invocation, confidence buys aggression, compose by minimum - the source mapped 3σ straight to "propose"); failure-diagnosis rule "diagnose from the measured, not the configured"; gate-laddering places asking controls at stage boundaries. Law lead (2nd sighting): verification is the product when authorship is cheap. 0 fetches |
| 2026-08-29 | `youtube:T6HLeCiUyI0` "The Future of AI (From Harness to RSI)" - Discover AI | second-hand survey (one commentator digesting two papers) | 8162 | 10 | 1 | 2 | 5 | [[2026-08-29-future-of-ai-harness-to-rsi]] - one amendment: `failure-attribution` (eval-harness) gained the missing stage between attribution and re-attribution - the change is an experiment designed before it runs: one harness component per round, model pinned while the harness moves, prediction written first, parent kept. Generalizes the prompt-only rule game-production already carries (`mutation-taxonomy`) to every harness component. Leads: the two primary papers; optimizer architecture vs state as the recursion boundary. 0 fetches |
| 2026-08-26 | `youtube:NYFGCESmikA` - "DHH: Future of Programming, AI, Agentic Engineering, Vibe Coding & Linux" (Lex Fridman #501) | first-party-practitioner-account (podcast interview, new sub-class) | 54597 | 13 | 3 | 0 | 0 | [[2026-08-26-dhh-lex-fridman]] - longest source ever (3.7x prior record), same yield as sources a tenth its size; the sub-class's property: a five-hour conversation leaks the counterexample to its own stated advice (said "be as vague as you can", narrated a textbook tight envelope), and the contrast IS the discriminator. Three amendments, zero fetches: `task-envelope` gained its stated precondition (when done is not knowable the envelope inverts - finish-line constraints free, route constraints spend; discovery's deliverable is the next dispatch's done criterion), `heterogeneous-model-panels` gained the produce-review pair (cross-family sequential decorrelation for routine generation, which the panel rule excludes; corroborated against judgment-guardbands' correlated-judges argument), `fleet-orchestration` golden path gained the operator-medium section (chat couples the one human to fleet latency; work item in, batched decision surface out). 10 untriaged with anchors, incl. the single-owner doctrine's second independent sighting |
| 2026-08-26 | `web:githubnext.com/posts/knowledge-compressor` - "Knowledge Compressor" | first-party-practitioner-account (prototype report) | 4649 | 10 | 3 | 2 | 0 | [[2026-08-26-knowledge-compressor]] - the source's method landed as two techniques that both say the method AS PUBLISHED is unsound: it filters questions the model cannot answer from the article but never those it can answer WITHOUT it, and its require-a-failure guard is only safe after that screen. `unaided-baseline-screening` + `overshoot-and-restore` (eval-harness, first new techniques there since forge) + the recurring-bill/break-even amendment on `context-budgeting`, sharpened by 2026-08-25's cache multipliers. **Zero fetches** - all corroboration from corpus-internal convergence. Then built the corrected method and ran it: `scripts/compression-scan.mjs` + `docs/compression-lane.md` + `docs/compression-brief.md`. Result NEGATIVE and that is the finding - mean repetition 0.94% over ~3,350 docs, top entry document two-thirds irreducible under a real screen; the prototype's 50% came from a synthetic article written to be redundant. Lane default: run the scan, never the compression. Instrument caught its own overlap-counting defect on first read (74.3% -> 15.1%). Law lead banked: *a green result is evidence only where red was reachable*, 4 sightings/3 subjects, return on a sighting outside `software-engineering` |
| 2026-08-26 | `repo:jd-opensource/JoyAI-Echo` @ c796303 | research-model-release (new class) | n/a | 12 | 4 | 3 | 1 | [[2026-08-26-joyai-echo]] - new class: engine AND operating instructions in one tree, so every doc claim is checkable against the code that implements it, no fetch (budget 3, spent 0). The class's signature: a repo shipping two sibling systems hands you discriminators for free - two shot-writers from one lab gave opposite camera instructions, which landed as a boundary at almost no cost. Yield sat in the first-party prompt-engineering artifacts, not the README. `identity-split-from-state` + the `image-prompt-composition` anatomy going three blocks -> four (the restatement law's unstated precondition: a restated block may hold nothing that varies, and the constant only acquires moods when it stops being a style and starts being a subject); camera-as-control-channel discriminator into `movement-motivation`; pin-the-origin-roll-the-recent into `generated-shot-sourcing`, implemented twice at two altitudes in the source; joint-waveform spotting into `music-spotting-against-picture`, whose neighbour's keep/demote/strip enumeration it breaks |
| 2026-08-26 | `youtube:g3X8JauSWTM` - "Stop Building AI Slop - Build High-End Web Apps with AI" | practitioner-tutorial (course walkthrough) | 14649 | 12 | 3 | 0 | 9 | [[2026-08-26-stop-building-ai-slop]] - longest source in the ledger, three findings, none from the curriculum: cross-medium probes on `asset-vs-disposable-render`; `slots-claim-their-subjects` + the "two families" golden-path root (identity-slot convergence with 2026-08-23, landed not banked); structure-vs-restyle amendment on `style-onboarding-from-sample`; class row earned |
| 2026-08-26 | `repo:supermemoryai/supermemory` @ shallow clone | vendor-repository (new class) | 2091 | 16 | 3 | 1 | 4 | [[2026-08-26-supermemory]] - the engine is NOT in the repo (hosted); its ontology leaks through the visualization package's TypeScript types, which is the class's cheapest good artifact. `decay-and-forgetting` gained a third axis (the fact that expires by its own terms; the retrieval term *protects* time-boxed claims), `episodic-capture` gained the batch-shape half of the distiller ceiling at root altitude (3rd sighting, 2 runs). Realized + committed in a connected tree the same run; that tree then corrected the technique - overflow defers, it does not drop. Four catches, all cases where the corpus outclasses a SOTA vendor's own schema |
| 2026-08-26 | `operator dispatch` + `next@16.3.3` release notes / GHSA-p293-qw3h-jr36 + GHSA-2xp9-vwfh-vxw4 | operator-dispatch (new class) | n/a | 8 | 3 | 3 | 2 | [[2026-08-26-next-16-3-3-fleet]] - the dispatch was wrong about its own subject: asked for a performance topic, 16.3.3 is two critical unauthenticated RCEs and its AVIF fix is a REMOVAL. Literal ask unbuildable (denylist). Landed `## When the dependency is the framework` amendment, first `next`-stack application, `scripts/fleet-deps.mjs`, six project commits. The new instrument caught the run own pin-rewrite defect on first run |
| 2026-08-26 | `repo:rohitg00/ai-engineering-from-scratch` @ 39ea8a1 | curriculum-repo (511 lessons, 12-language i18n) | n/a | 7 | 1 | 4 | 3 | [[2026-08-26-ai-engineering-from-scratch]] - new category `localization/craft` + subject `translation-pipeline-topology` (6 techniques) forged same-run by three workers; "comprehension is proven by reconstruction" banked at two sightings; the discriminator (human quality claim) beats either topology |
| 2026-08-26 | `youtube:OzfgOjEfK98` - "Introducing Composer, a section-by-section song editor." | vendor-product-announcement | 76 | 6 | 4 | 1 | 1 | [[2026-08-26-composer-song-editor]] - thinnest source ever (exit 3, correctly); the operator's placement question became the session: `audio-generation` category (3 subjects, 14t), `partial-regeneration-seams`, the four-way audio discriminator, a music engine landed live in a consumer tree; the source itself yielded one catch (its headline is our `edit-do-not-regenerate` law) and one clock |
| 2026-08-25 | `youtube:wa6o-0C9UWE` - "Karpathy Listed What's Wrong With AI Coding" | practitioner-judgment relay | 3470 | 6 | 3 | 0 | 2 | [[2026-08-25-karpathy-coding-file]] - law 13 `silent-state-is-ungoverned` (five-way convergence closes), `restraint-amplifier-balance` technique, machine-checkable finish lines into task-envelope; altitude column added to the skill (0.10.0) on operator feedback |
| 2026-08-25 | `papers: 2607.09510 + 2605.29442 + 2607.21832 + 2606.08091` | commissioned-paper-batch | n/a | 4 | 4 | 3 | 0 | [[2026-08-25-agentic-dev-paper-batch]] - `worker-trajectory-anatomy` technique; measured paragraphs into enforcement-demotion and task-envelope; two independent corpora price fabricated success; PR study read and declined with reasons |
| 2026-08-25 | `repo:Shubhamsaboo/awesome-llm-apps` @ 11a4bc3 | app-aggregator (tutorial monorepo) | n/a | 8 | 3 | 3 | 3 | [[2026-08-25-awesome-llm-apps]] - the periphery beats the apps: skills eval ladder adopted (`scripts/check-skill-triggers.mjs`), cross-field epistemic coherence lands in structured-output; hash-chain audit declined on single-owner grounds |
| 2026-08-25 | `repo:DEEP-JLU/Awesome-Graph-Engineering` | paper-aggregator (~284 papers) | n/a | 7 | 3 | 3 | 2 | [[2026-08-25-awesome-graph-engineering]] - cluster triage, 3 papers read: `coordination-failure-triage` technique, two measured sections in `procedure-promotion`, store-shape paragraph; temporal-KG memory resolved as the corpus outreasoning the paper |
| 2026-08-25 | `repo:freestylefly/awesome-gpt-image-2` @ 6854698 | practitioner-codebase (curated prompt library) | n/a | 10 | 5 | 1 | 4 | [[2026-08-25-awesome-gpt-image-2]] - `verbatim-text-locking` + `screenshots-claim-a-record`, a third dialect in `prompt-dialect-matching`, one node application; the corpus meets the text-capable brief-reading model class |
| 2026-08-25 | `youtube:icM0ewXGvAw` - "19 Claude Code Mistakes \"Pro\" Users Are Still Making" | second-hand-practitioner-listicle | 4465 | 19 | 3 | 1 | 0 | [[2026-08-25-19-claude-code-mistakes]] - three techniques (`task-envelope`, `brief-carries-the-session`, `cache-continuity`), three rust applications, one correction; cross-repo self-check lines in the companion's worker prompts; 16 declined on operator scope |
| 2026-08-23 | `youtube:yCACmFTiCto` - "Turn Claude Into a One Person Marketing Team" | practitioner-tutorial (beginner walkthrough) | 9483 | 8 | 1 | 1 | 5 | [[2026-08-23-one-person-marketing-team]] - mined under `/deepen`; one technique on `evidence-bound-visuals` (synthetic-testimonial defect the source demonstrates), two corpus contradictions, aggregator topology banked |
| 2026-08-23 | `youtube:NUK_TBz46dM` - "Turn Claude Into A Web Design Genius in 3 Steps" | practitioner-deep-dive (technique demo) | 4037 | 6 | 1 | 0 | 5 | [[2026-08-23-seedance-web-design]] - mined under `/deepen`; one technique on `review-iteration-loops`, five catches, one corpus contradiction |
| 2026-08-22 | `repo:onecli/onecli` @ ff7a192 | practitioner-codebase | n/a | 10 | 7 | 0 | 3 | [[2026-08-22-onecli-repo]] - three new techniques, two amendments, two applications |
| 2026-08-22 | `web:linear.app/data` - "How teams build (Edition 01)" | vendor-telemetry-report | 4613 | 7 | 0 | 0 | 0 | [[2026-08-22-linear-how-teams-build]] - none picked; class characterised, 7 untriaged |
| 2026-08-22 | `web:pinglin.tw` - "The Shapes of Agent Memory" | first-party-empirical-study | 13767 | 12 | 6 | 1 | 2 | [[2026-08-22-shapes-of-agent-memory]] - five amendments across agent-memory + eval-harness |
| 2026-08-22 | `youtube:bxp4G-oJATM` - "Difficulty in Video Games" | designer-talk | 2332 | 6 | 5 | 0 | 0 | [[2026-08-22-game-difficulty]] - landed on `research/game-difficulty` + cross-repo |
| 2026-08-22 | `youtube:3MP8D-mdheA` - "How To De-Slop A Codebase Ruined By AI" | practitioner-deep-dive (technique demo) | 2456 | 6 | 0 (+1 dispatched) | 0 | 1 | [[2026-08-22-de-slop-a-codebase]] |
| 2026-08-22 | `youtube:gaDdrDdczO4` - "New Skills! v1.2..." | practitioner-deep-dive (skill-library release) | 2514 | 10 | 5 | 0 | 2 | [[2026-08-22-skills-v1-2-release]] |
| 2026-08-22 | `youtube:u8Im0l_vwqM` - "Inside DeepWiki: How Cognition Builds Wikis for Devin at Scale" | practitioner-deep-dive | 2974 | 10 | 2 (+1 proposed) | 0 | 1 | [[2026-08-22-inside-deepwiki]] |
| 2026-08-22 | `youtube:NC4h5kWH_-A` - "AI News: The AI Agent Race Just Exploded" | mixed-ai-news-roundup | 6958 | 10 | 4 | 1 | 2 | [[2026-08-22-ai-agent-race-exploded]] |
| 2026-08-21 | `youtube:EfGF7QbJItA` - "AI News: The Best Open Model Runs on Your Computer!" | mixed-ai-news-roundup | 6507 | 12 | 1 | 0 | 0 | [[2026-08-21-ai-news-open-model-local]] |
| 2026-08-28 | `youtube:RqcEK7sWesQ` - "You Think This Is Good OOP... It's Not" | **practitioner listicle on design canon** - six textbook class-design mistakes, toy examples, no vendor, no measurements | 3256 | 8 | 3 | 1 | 2 | [[2026-08-28-six-things-mistaken-for-good-oop]] - **the corpus carried every item at module altitude under its own vocabulary**, and the map reported three false holes ("liskov", "mixin", "duplication" - present as substitutability, the contract suite and the wrong-abstraction rule). What the source supplied was the class-hierarchy instance a deliberately mechanism-neutral subject never names, and the run wrote at the corpus's altitude, not the video's. Three amendments on `module-design`, folded from five items: `seams-and-adapters` gains the narrowing adapter (subtyping is a substitution promise; a substitute that narrows the guarantee broke it; fix the interface, not the adapter; inheritance-for-reuse is the accidental form) - the contract-suite section had only ever caught a drifting double; `module-depth` gains the counterpart of the options-bag failure (a variation is data until it changes a guarantee; ask whether the module would be wrong to choose a default) - the existing rule read alone invited a unit per combination; `locality-and-leverage` gains the type-erasure tell. Two catches where the corpus is ahead (god base class = the merged-module test; wrong abstraction = the co-change test). One lead with a return condition: a unit-kind selection rule (classes for invariants, functions for stateless work) that the subject has no place for until a second source converges. 0 of 3 fetches - canon corroborates by convergence. Unattended run; picks by registry impact; no declines; one untriaged aside |
| 2026-08-28 | `youtube:` batch of 12 - PaXuebdY75U, guq_bjGwzdw, SM5Un86ntNU, 8GF828QAyow, _VX6BZDKgrg, edrUbfeSPio, B0kVZvhkaq4, gJED9LCczc0, eDODZfOO7Fc, Cw11MnXArjY, pZRROwTYDqk, Hppz8XfdKHY | **practitioner-tutorial batch, fourth pass at one bundle** - 12 videos, **6 voices** (5 one creator, 3 a second), two Korean-language; operator constraint: real gaps or technique extensions only | 29421 | 24 | 7 | 1 | 12 | [[2026-08-28-media-generation-batch-4]] - **seven landings, all amendments, no new techniques and no new subjects**, which is the right shape for a batch arriving at a bundle runs 24-27 already widened 26 times. Author dedupe mattered most here: the two cross-author agreements (draft-resolution ladder, per-clip generated audio) were both catches, and what paid was the *disagreement* - two independent sources give opposite advice on in-prompt timestamps, so the discriminator is drawn for free (untriaged). Landings: `partial-regeneration-seams` gains the two operations time-based media has and a still does not (**excise an interval, reverse one**) with the defect's extent choosing among them and reversal's hard boundary (time-symmetric motion only - never gait, a mouth, flame, spilling), plus the unaddressable multi-panel sheet (a one-panel fix re-rolls every panel; composite back AND diff the panels you did not ask about); `generated-shot-sourcing` gains rung 3 over an *exact graphic* (a designed graphic is re-synthesised, not drifted - author the anchors as a cumulative single-delta chain; three or more states fall off head-and-tail onto ordered references) and a third cost of a frame anchor (it over-pins **cyclic** motion - pin one member of a symmetric pair mid-stroke, not both); `style-onboarding-from-sample` gains the house-style decomposition (stages fail independently and each is learnable only from evidence of a different kind - **where no channel can read the evidence the stage is hand-authored, and a pre-supplied asset-class decomposition is what makes that tractable**); `reference-shows-only-invariants` gains the ground's *brightness* as an axis hiding inside "plain" (white sweep = an exposure instruction nobody wrote; the tell is a character reliably brighter than the room); `medium-vocabulary-locking` gains the named-work quality proxy (**a proper noun is a content token wearing a quality adjective's clothes** - the render quality genuinely improves, which is what makes it durable; strongest where the subject class overlaps the work's signature subject). 12 catches, one of which **contradicts the source and the corpus wins** (one soundtrack across the edit is what `music-spotting-against-picture` calls wallpapering). 4 untriaged with anchors - the strongest is depth staging as a decision before optics, where "atmospheric perspective" returns zero prior art corpus-wide and `movement-motivation`'s speed-adverb rule may be correctable. 1 lead (currency scoped to a machine's own capability envelope). **Method lesson: an enumeration near the top of a long technique file is not the file's coverage** - a candidate was triaged as a real gap off a four-rung ladder that the same file resolves 200 lines later. Zero of 3 fetches - eleventh consecutive corpus-internal run. Demonstrated-*failure* density predicted yield better than demonstrated-mechanics density: the sources that showed something going wrong produced 6 of 7 landings |
| 2026-08-30 | `youtube:gvPMjz08DDA` - "AI Agent Builds My Game in a Loop Until It's Done" | **sponsored second-hand practitioner review with a build-walkthrough half** - one creator, five games built on a vendor's loop mode, on the vendor's credits | 5045 | 13 | 1 | 2 | 4 | [[2026-08-30-tesana-loop-mode-game-builds]] - the tour half strips to nothing; the operating half's one claim (four-to-five-hour loops came back with logic sound and presentation flat, and the six-to-ten follow-up prompts all went to what no gate could see) landed as `verifier-coverage-review-agenda` on `unattended-build-loop` - a missing run-end stage between the loop and the reviewer: certify each item only up to the rung a verdict reached, hand the rest to the human as a named agenda, print per-gate verdict counts because the static preflight excludes runtime-determined gates on purpose. **Corroborated by a connected tree's recorded runs, not by a fetch**: the perceptual gate was configured, advisory, and returned zero verdicts in 77/77 deciding iterations while 234 features were marked done - the tree made the finding larger than the source. Applied as an `experiment` (A/B over four recorded runs, n=323 features), verdict `better`. 4 catches, 5 untriaged with anchors, 2 leads. 0 of 3 fetches. Unattended run; only the `real gap` row advanced |
| 2026-08-31 | `github:Kevin-Liu-01/Claude-of-Tanks` @ `286bd2a` | **first-party practitioner repository (build-walkthrough form)** - the sub-class tell is a contract document whose sections are numbered AND dated with the owner directive plus the incident that caused each: the release-walkthrough property (motivation preserved) in repository form. Postmortem outranks the lessons file for correction-class findings. | 26,981 landing / ~60,000+ in-tree | 14 | 5 | 5 | 1 | [[2026-08-31-claude-of-tanks-geometry-gate]] - one NEW SUBJECT, `reference-parity-gating` on `game-production/content-pipeline`, with 6 techniques and 3 applications. Found by the enumeration hunt: `generated-mesh-acceptance` declares in prose that the perceptual/'is it the right shape' half is *'a different subject'* and nothing owned it; the bundle had a quality gate for motion and none for geometry against a reference. Techniques: `dual-anchor-scoring` (two authorities measuring different quantities whose conjunction a wrong artifact cannot satisfy - 'you cannot satisfy both without being actually right'), `register-once-from-the-invariant`, `no-average-hides-a-failure` (min/p95/symmetric coverage + the floored-metric rule), `findings-carry-the-correction` (the row payload is the product; no iteration cap because the gate defines done), `instrument-blindness-register` (enumerate what the rig structurally cannot see, one witness per class - four classes, each shipped a defect at a passing score), `defect-cap-bounded-to-its-reach`. Placement overrode Phase 4's HOME IF NEW: `asset-production` sits at exactly the 10-directory cap, so `content-pipeline` by the taxonomy authority - and better on the merits, since these are parity-measurement techniques that transplant across asset types. Applied same-run to a managed tree as an experiment: **not-better**, and it fired the technique's own decision rule - arm B read the same representation with a different parser, so both its catches were parser artifacts; the technique gained `a different parser is not a different authority`. 1 catch (candidate-hash freezing, fully owned by `quality-verdict-integrity`), 1 corroboration-not-discovery (envelope parity 12/12 within 4% of published dims, 0/12 on the perceptual review - cited into L9 rather than landed as a claim), 5 leads (render-bucket-is-not-a-structural-selector; small commits do not contain a shared global assumption; coupled-change proof by input interception; index-blob staging of a tool-written shared ledger; lock/ticket design scars), 3 untriaged with anchors. 0 of 3 fetches spent. |
| 2026-08-31 | `github:tc39/proposals` @ `600a427` | **reference index — but the lane was wrong for it.** 1,603 unique outbound links over ~10,900 in-tree words (ratio says index), yet the ~200 references are language-feature proposals that strip to `nothing` against all eight bundles: a full wave campaign spends ~40 fetches for ~200 honest negatives. **The rows are maintained RECORDS, not pointers** — stage, author, champion, a per-stage evidence column, a dated notes trail, a rationale for the dead — so the table's SCHEMA is the source and the bibliography is decoration. Second reference-index run after `voltagent-awesome-ai-agent-papers`, and the two disagree about where the yield sits, which is the class's real discriminator: **does the curator maintain per-row state over time, or only select rows?** | 1540 landing / ~10900 in-tree | 8 | 2 | 2 | 2 | [[2026-08-31-tc39-proposals]] — 1 of 3 fetches, spent on the process document as EXTRACTION not corroboration, and it inverted the source twice: the tracker's column set turns out to track the entrance criteria exactly, and **"no explicit rule addresses stalled proposals or automatic removal"** is what turned an observation into a technique. Both landings came from one asymmetry — all 14 prior techniques in `quality-gates` take the CHECKER as subject and none takes the item being gated. Measured from the source's own record: 7 of 46 terminated items name owner departure as the sole cause (3 closed the same day — an unscheduled decade-late sweep); 30 of 92 in-flight items silent ≥2 years, all listed active; and the natural experiment that carried the first technique — one board, two obligations, two conventions, 8 of 18 holes legible under explicit markers vs 16 of 29 (55%) unreadable under blanks. |
| 2026-08-31 | `youtube:DrsvFcPg8jY` - "How to Make 3D Documentary Videos with AI" | **practitioner build-walkthrough, tutorial form, affiliate-sponsored** - one creator walks a 60-second animated documentary end to end; tour half is a platform demo with zero failures shown, operating half states its craft rules as generalizations because a tutorial must be reproducible by a stranger | 2214 | 13 | 2 | 2 | 8 | [[2026-08-31-3d-documentary-ai]] - two amendments, both corroborated corpus-internally (0 of 3 fetches), both found by contradicting the corpus's own denials rather than by believing the source. `generated-shot-sourcing` gains **rung 3 across two worlds**: the ladder insisted "the two anchors must be cut from one cloth" and predicted a visible break; the source paired a head anchor with a separately generated plate of a *different room* and got a clean multi-shot clip with a hard cut - past some threshold of difference the pair stops being a move and reads as a scene boundary, which the model renders as an edit. The amendment states the cost the affordance carries: the cut lands inside the clip, so the assembly does not own it, which is the technique's own "a cut nobody made" arriving through a door its clip-cap section does not watch. `reference-role-map` gains **the seam between two references belongs to neither**: step 3 forbids re-describing a mapped asset, but a subject reference carries its own key light (`reference-shows-only-invariants`), so in a plate-plus-subject call both attachments hold lighting authority and the thing actually needed - the subject lit by the plate's source - is a *relation* no one-row-per-asset map can hold. Both applied same-run to a managed tree as simulations, three real cases each: shot-sourcing `better` (the tree has no video generation at all, yet its cut list already cannot represent a clip containing its own cut - one picked frame per scene, one span per clip), role-map `unmeasurable` with the instrument named (its own verdict grader over composite calls it does not yet make; the look/depiction split is a binary over ONE image, so a relation between two has no slot). 8 catches, 3 untriaged with anchors, 2 leads (where the two-worlds threshold sits; the corpus has no composite-integration grader). Currency confirmed the sourcing ledger's 1080p claim WITHOUT moving `verified_on` - one video confirming one row is not a re-resolution |
| 2026-08-31 | `youtube:xDHD09fDUkQ` - "BreezeTTS2 - 100% Local Real-Time Voice" | **second-hand practitioner review with a first-party operating half** - one creator reviews someone else's TTS release; tour half is a sponsored demo reel (vendor-supplied compute, zero failures shown in twelve minutes), operating half is a genuine first-party account - he ran it, built his own streaming demo, and volunteered the one thing wrong with the model | 2746 | 11 | 3 | 2 | 4 | [[2026-08-31-breeze-tts2-local-voice]] - **the class average is ~0 and this run landed 3, all from the operating half; nothing from the tour half survived.** Operator named the target subject up front ("our voice package"), so the run was scoped to `voice-io`. **Every landing came from reading the corpus's own enumerations while listening, not from ranking the source's emphasis** - the three enumerations were a six-axis decision matrix, a capability-axis list, and a four-item audition script, and the source walks past a case each one does not contain in a sentence it treats as uninteresting. Ranking by the source's own emphasis would have picked the leaderboard, the 50 languages and the streaming latency, all three of which are catches. Landings: `tts-pipeline` gains **a voice reference resolves to a recording chain, not only a speaker** (training audio's mic/room/EQ is learned into the weights; constant across content, therefore invisible to any content-based audition and audible only in comparison) with the cross-boundary half landing in `media-generation/spoken-delivery-direction`, whose four-passage audition enumeration it **falsifies**; NEW TECHNIQUE `authored-voice-identity` (three specification kinds - selected, described, cloned - and the inversion that for an authored voice the *specification* is durable and the *timbre* volatile, the opposite of the catalog's retirement hazard, plus the consent line between a clone and a description); `on-device-vs-cloud` goes **six axes to seven** with **rights** - a gate rather than a trade-off, unestablishable by any probe, and it inverts the matrix's own asymmetry, because the most private placement can be the one you are least permitted to ship. **All three applied to one connected tree, 1:1 with landings**: 2 experiments `ab-paired` on compile-checked reductions (both `better`), 1 simulation `unmeasurable` with its instrument named. The apply step produced corpus content twice - it **refuted** the technique's transcript rule (the tree's cloning engine is zero-shot, so self-containment is an engine property, not a rule) and supplied the run's best fact: the tree ships a **non-commercially-licensed** engine recorded only as a module doc comment, while the surface that writes publishable files refuses it via `if engine != <engine>` - the right outcome from an unrelated rule, which the obvious next feature deletes. 4 catches, 2 leads, 2 untriaged with anchors. **0 of 3 fetches** - corroborated corpus-internally and by a connected tree. Declared focus (parallelise Phase 6) NOT met: the session forbids dispatching agents, so verification ran serially; owed by the next run that can dispatch |
| 2026-09-01 | [Your LLM Has a New Boss: The Adaptive Harness](https://www.youtube.com/watch?v=lNlMXsUQggc) | **second-hand practitioner review (hybrid)** — a channel review of a 2026-08-28 corporate preprint; the relay half restates known harness mechanisms (the reviewer says so himself) and the critique half is a first-party reading of the paper's evidence tables | 4823 | 10 | 1 | 1 | 6 | [[2026-09-01-adaptive-harness-review]] — **one landing, from the critique half, found by the asymmetry hunt rather than by the source.** `baseline-carries-its-conditions` (new, `quality-regression-gating`): `paired-per-case-testing` enumerates a comparability predicate of four conditions — mode, target, case count, dataset version — **all about the experiment and none about the instrument**, with the judge absent even though the sibling subject `judge-calibration-and-drift` exists to say the judge is what drifts; the golden path's own "Honesty about what the test cannot see" enumerates three baseline limitations and **all three are sampling noise, none is currency**; and the routing is inverted — the paired test's comparator is the previous run and carries the predicate, the floor test's comparator is arbitrarily old and carries nothing, and the fallback to it is reached by exactly the condition proving comparability already failed. Source's occasion: a 2026-08 system compared against July-2025 baselines with the margin reported as a result. The source also supplied, without noticing, the counter-clock that makes the rule precise — it defends the authors three times on the grounds their work was novel when they started while refusing to date their baselines, so **a contribution is dated by when it was made and a comparison by when it is published**. Applied `experiment`/`better` to tracklight on the real canonicalization table: 8 identities vs 11, five instruments collapsed into two, 82% of entries carrying a discarded date — and **the apply corrected the technique**, adding the discriminator that aggregation wants the family and a predicate wants the variant, so the two identities are two fields. **0 of 3 fetches**, and the reason is a class refinement: this class's table says the fetch *is* the extraction, which holds for the relay half — but the relay half was 6/6 already covered, so the fetch would have bought corroboration nothing needed. **The fetch budget belongs to the half, not to the source.** 0 siblings live |

## Source classes, and what each is trusted for

Written from what runs actually observe, never assumed up front. A class earns a line
here after it has been seen twice, with the incident that taught it.

| Class | Reliable for | Not reliable for |
| --- | --- | --- |
| Designer talk (domain craft, no tooling) | The STRUCTURE of a domain's received wisdom - the decomposition, the named hazards, the shapes practitioners converge on. | Whether any particular studio's version is best, and anything about software. Its findings never route to `software-engineering`; they route to a domain bundle, which may not be on the branch you are standing on. |
| First-party practitioner deep-dive | What they built, what they measured, and what changed when they changed it. Facts about one system, from the person who changed it - no corroboration lane improves on that. | What works in GENERAL. The sample is one pipeline at one company, so a measured result is an existence proof, not a distribution. |
| Operator dispatch | The LOCAL facts: what the operator has observed in the fleet, what the fleet needs, which trees to open. Version numbers, adoption state, felt pain - all checkable against the checkouts, and cheap to check. | The GLOBAL frame. A dispatch is not written against the corpus and cannot know what is already forged, what the gate forbids, or why the subject it names is the wrong altitude. Verify its premises against the trees and its framing against the corpus; the second one is where it fails. |
| Mixed AI-news roundup | That the world moved: a release happened, a price changed, a benchmark was published. | Why it matters, whether it is true, whether it is new. Its claims are second-hand by construction. |

**First-party practitioner account carrying a vendor announcement, first observation
(2026-08-27) - and the run where the corroboration was entirely local.** A senior
engineer at a vendor arguing a systems-design position in the same post that announces
acquiring the team behind the system it argues for. The provenance is not disqualifying
and it is not authorizing: the post's job was to locate a hole, and it located a real one
three levels deep. Everything that landed was authorized by peer-reviewed literature or
by a connected project's own measured benchmark. Two instrument lessons that generalise
past this class:

- **The fetch budget was nearly irrelevant, and reaching for the web was the wrong
  instinct.** 1 of 3 spent; the canonical primary 403'd twice at its own host before the
  author's independent write-up supplied the mechanism. Meanwhile a connected project
  held a 392-line measured decision guide over the exact question - four dated cases,
  five workload classes, cross-engine correctness checksums, and negative results. When
  a first-party account names a decision an operator says the fleet keeps getting wrong,
  **check the fleet before the web**: the claim usually has a home, and the home has
  often already measured it.
- **Survey connected projects with `git grep`, never `grep -r`.** An untracked sweep
  counted vendored `node_modules` and manufactured the claim "both projects adopted this
  engine". The tracked-files answer was the opposite - both evaluated it, neither adopted
  it - and the opposite was the finding. It is also ~100x faster; the untracked sweep
  timed out at 120s on one tree.

The durable finding is one the post could not have supplied: **a decision recorded in a
document and a decision present in the import graph are different states, and only the
second one runs.** The tree that measured a 43x win, wrote it as a numbered rule, and
named the workload is still running that workload on the incumbent - and no gate can see
it, because a document recommending an engine and a script not using it are each
individually valid.

**First-party agent-runtime repository, first observation (2026-08-27).** An
open-source product whose *subject matter* is the same machinery this registry
indexes, so its repository is simultaneously engine, operating instructions, and
enforcement. Three files check each other: the production rules for agents building it,
the skill for agents driving it, and the hooks and task recipes that make both real -
and the third is where a rule's actual conditions live, because every rule worth having
had something behind it. Two things this class taught that generalise past it. **The
bundled skill is not automatically an ad**: this one's description is negative and its
first instruction is a precondition that halts it, which retires the 2026-08-26
prediction in favour of reading for what invocation pressure produced. And **when the
source implements the primitive, its own code is the primary source**: two web fetches
were spent hunting a terminal-behaviour citation that was sitting in the tree's vendored
emulator bindings, stated outright. Not yet a rule; one observation. The third sighting
of a related pattern - **the README is the least useful file present**, now seen in this
class, the vendor repository and the research-model release - is no longer per-class and
belongs in the method's class list once.

**Designer talk, first observation (2026-08-22).** A teacher explaining an established
body of practice - neither second-hand survey nor first-party account. The class has one
defining property for this registry: **it has a placement problem before it has a content
problem.** Its natural home was a bundle living on a different branch, which the mapping
instrument cannot see, so the instrument reported "no prior art" for a topic a sibling
branch covers in two subjects. The corpus is bigger than any one branch and the tooling
does not know it.

**First-party practitioner deep-dive, second observation (2026-08-22) - the row is
earned, and one sub-class is worth seeking out.** Both observations confirmed the split:
authoritative about what they built, silent on generality. The sub-class that outperformed
is the **release walkthrough** - a library author going through one version's changes.
It is organised around CHANGES, and a change carries its own motivation, so the author
states the failure mode out loud because it is the reason the release exists. Three of
five accepted findings came from that structure rather than from the feature described.
A feature demo shows the solution and hides the problem; a release walkthrough shows both.

Also confirmed across both: no corroboration fetch was needed in either run. The 3-fetch
budget binds on second-hand surveys and never on this class.

**First-party practitioner deep-dive, third observation (2026-08-23) - the failure mode
finally showed itself, and the yield profile against a MATURE bundle is different.** The
first two observations inferred the class's weakness from its structure; this one caught
it in the act. A device rule ("if it is a mobile user, serve the still instead of the
video") was stated with total confidence and is the named wrong answer in a subject the
corpus already carries, which requires the page's own measured frame cost rather than a
declaration the device makes about itself. That is precisely the shape the row predicts:
a general recommendation inferred from one system where it happened to work.

The yield profile is the other half. Five of six candidates were already covered - and in
four of those the corpus was not tied but strictly better hedged, holding the same rule
*with its condition attached* (a rights rule, a four-rung ladder, a measurement instead of
a guess). Against a mature bundle this class should be expected to produce catches, not
content, and **a catch is worth recording with the hedge that beat it**, because the hedge
is the reusable argument for the layer contract. Fetch budget was touched for the first
time in this class - not to corroborate what the practitioner did, which still needs no
lane, but to check a claim they made about generation in general.

**Cross-class, second sighting: the candidate a source explains badly is the one worth
the budget.** The 2026-08-21 roundup named a real gap and inverted the rule for filling
it; this practitioner named a real gap (no divergence phase before the review loop) and
inverted its mechanism, calling for more amplitude where the literature says amplitude is
the lever measured not to work. Both runs' single accepted finding came from their single
badly-explained candidate, while every correctly-stated claim was already owned. The
provisional reading, one sighting short of a rule: **a claim a source gets right is a
claim the corpus probably already has**, so the corroboration budget belongs on the
claims that sound wrong rather than on the ones that sound right.

**First-party practitioner deep-dive, first observation (2026-08-22).** Its authority
maps onto the layer contract almost exactly: strong evidence for the SHAPE of a
technique, weak evidence for its universality. So its claims land well as decision rules
with their conditions attached and badly as unqualified assertions - which is a
different editing job from the roundups, not merely a higher trust level. Yield differed
in kind as well as degree: a coherent account of one problem maps onto one region of the
corpus, so the run produced two techniques and a subject proposal rather than findings
scattered across six subjects. At 2,974 words it outproduced a 6,958-word roundup, which
is the first evidence that transcript length is not a yield proxy.

**Mixed AI-news roundup, second observation (2026-08-22) - the class row is now
earned.** Two runs, same channel, and the pattern held in both directions. The class
located four real gaps and was *wrong* about two things the corpus has measured (the
precision-tier rule in run 1, the throughput-buys-thinking claim in run 2), plus a third
it got backwards on agent identity. Yield is not a property of the class but of where
its segments happen to land: run 1 hit model and media subjects and produced one
finding, run 2 hit `llm-agent` and produced four.

The second observation adds something the first could not show: **the class becomes
authoritative in aggregate.** Two independent vendors across two runs shipped the same
"observed repetition becomes a named skill" feature, which met the upper-layer
corroboration bar with no web fetch at all. That only worked because run 1 wrote down
its untriaged candidates - a vocabulary with only "declined" in it would have lost the
first half of the pair.

**Mixed AI-news roundup, first observation (2026-08-21).** The class did exactly what the
row predicts. The one segment that reached a landed finding named a real hole in the
corpus and stated the rule backwards - the primary literature says the compression
*format* dominates the nominal bit count, while the source said take the highest number
that fits. Originating a finding and authorizing one are different acts, and this class
can only do the first. Not yet a rule; one observation.
| 2026-08-30 | `github:laude-institute/headlong` - "Headlong - an open source agent microharness" | **research-model release (harness repo)** - engine + design docs + code in one tree; README is the ad, `design/*.md` are first-party design docs with paid-for corrections recorded as revisions | 1882 | 8 | 2 | 2 | 1 | [[2026-08-30-headlong-agent-microharness]] - two techniques, both corroborated in-tree (0 of 3 fetches): `tiered-history-projection` on `prompt-assembly` (context as a regenerable projection of an append-only record; geometric staircase reaching birth; tiers as an id-index, not testimony; sealed provenance-stamped blocks) with the regime boundary stated on both sides of `history-compaction` - the corpus's "one compaction away from gone" holds only where the message list IS the record; and `engagement-paced-cadence` on `cost-metering` (the stage upstream of every gate: how often a self-driven loop wakes; reactivity never throttled, spontaneity descends exponentially with dwell to a settled cap; a ruminating loop must not score its rumination as work). Both applied same-run to a managed tree as simulations: cadence `better` (the tree already emits the wake-classification predicate and paces on nothing), projection `not-better` (histories fit the tail, no fetch-by-id affordance; the technique gained its adoption gate from the seam). 1 catch (one-mind-many-mouths), 3 untriaged with anchors, 2 leads (fork-test-merge self-improvement; brokered sandbox policy authority). Unattended run; only `real gap` rows advanced |
| 2026-08-31 | `github:tigerbeetle/tigerbeetle` @ `9d2f5d6` | **vendor repository (open engine)** - engine + operating docs in one tree, so every claim is checkable in-run at zero fetch; landing page 421 words against 111,264 in-tree (264x, the sharpest read-fraction tell the ledger holds) | 111264 | 15 | 1 | 0 | 2 | [[2026-08-31-tigerbeetle]] - one technique, `self-paced-intake` on `admission-queue`, landed on **cross-run convergence** with the prior day's Headlong run (0 of 3 fetches): a loop stepping on external events has delegated its work rate to its environment. Home chosen on a missing stage - the subject opens at "arrivals come faster than we execute" and never owned whether an arrival causes a step at all. Applied same-run to `personas` as a simulation, `better`, over three historical repairs; the structural fact was four long-lived consumers, three policies, no rule. 3 catches, 11 untriaged with anchors, 2 leads (a third sighting would make the convergence a law; ARCHITECTURE.md contradicts itself on replication topology and the code settles it). **The run's real output was a corpus finding**: the first triage produced twelve process rows and zero architecture rows from a database, the operator rejected that, and the audit that followed found a **construction frontier** - this bundle builds at the application layer and consumes everything below it. Fifteen builder-position systems concepts map to unrelated subjects by slug collision, four return zero corpus-wide, and the consumer-position control resolves cleanly. Written up in [[../domains/software-engineering]]; a second sighting (2026-08-27, `storage-engine-selection`) proves it is the default rather than an oversight |
| 2026-08-31 | `github:WingedGuardian/GENesis-AGI` - "GENesis-AGI" | **vendor repository (first-party practitioner account in repo form)** - 20,905 LOC memory subsystem shipped with its own architecture docs and ADRs; README is the ad | 5767 README / ~8200 mined | 13 | 3 | 0 | 2 | [[2026-08-31-genesis-agi]] - operator-directed at memory, which was the #1 attention point (56 pts) at scan time. Two techniques on `agent-memory`, both **below the pipeline** where eleven techniques of judgment could not see them: `lane-reconciliation` (a store is a record plus derived lanes with no shared transaction, so a half-completed fan-out leaves an item that passes every quality gate and cannot be recalled - and `coverage-instrumentation` reports it covered, because coverage joins the record) and `probe-without-write-back` (the read path writes, so a scheduled eval obeying `gate-sees-target` pumps the very loop `memory-value-model` bounds, over a fixed set whose expected answers are the items it inflates). Plus a per-class amendment to `health-checks/three-state-outcomes`. 0 of 3 fetches - third consecutive practitioner-codebase run to spend none. Applied same-run to a second managed tree, 3 rows: two `better`, one `not-better` that is independent corroboration rather than a rejection. **Both `better` verdicts amended the corpus from the tree, not the source** - the severity discriminator (does the lane have a reader fallback under it) and the structural-default section (`absent-guard-is-loud`: a suppression flag protects remembered callers, a split write endpoint protects every future one). 1 catch, 6 untriaged with anchors, 2 leads. Ship 0: no project confirmed by the operator. Unattended; only `real gap` rows advanced |
| 2026-08-31 | `github:tt-a1i/archify` @ `5de7275` | **practitioner build-walkthrough (repo form)** - an agent skill for generated diagrams whose operating layer is thicker than its product: 49 dated research rounds, a benchmark with a written fair-run protocol and three dated result sets, and a pre-registered kill experiment with its decision record intact. Landing page 2,051 words against ~130,000 in-tree (63x); a README-only read would have been a 1.6% sample of the ad | 130000 | 16 | 12 | 2 | 2 | [[2026-08-31-archify]] - **a new subject, forged in-session with no dispatched worker**: `machine-authored-documentation` on `codebase-stewardship` (golden path + 7 techniques + 1 application, 0 of 3 fetches). The operator asked whether the corpus covered automated docs creation; it did not, and the reason is the finding: three subjects sit adjacent and each one's own boundary statement excludes it. **Determinism is the discriminator** - `codegen` owns derived source and stands entirely on `derivation-names-recomputation`, which a model author cannot satisfy, because a regeneration diff over a non-deterministic generator is red on every run for reasons unrelated to correctness. So the acceptance burden moves off the derivation and onto the artifact: `three-gate-acceptance` (semantic AND deterministic AND rendered review; a skipped review never combines into a pass), `packaged-surface-evaluation` (measure the extract users install, harness outside the model-visible tree, attempt 1 frozen), `declared-truth-boundary` (the artifact publishes what its derived views do NOT claim; the receipt names counts, never impact), `evidence-without-verdict` (the instrument that gathers evidence for a human judgment reports `pending`, never `passed`), `stale-artifact-on-failed-write` (the atomic write is correct and it is what makes the failure silent), `rescored-baseline-uplift`, `preregistered-kill-criterion`. **The tree's best measurement reverses its own shipped conclusion**: a benchmark matrix published at 10/15 first-pass usable scores 8/15 under the corrected verifier, the post-fix matrix also 8/15, and a third run 8/15 again - the uplift never existed, and only frozen candidates made that knowable. 2 amendments on different axes than their targets (`checked-vs-skipped-denominators` gains the declared strictness tier, where the denominators are all correct and the examination simply asked less; `schema-validation-and-repair` gains the ordinal stop rule for a compositional validator, where the corpus's fixed budget is right for a binary door and wrong for a plateau). 2 catches, 2 untriaged with anchors, 2 leads. Applied same-run to `politicas` as an **experiment**, `better`: a visual detector returns `null` from five places, four of which mean not-measured - and the structural fact outranks the A/B, because the finding record has no skipped type at all, so the conflation is forced by the record shape rather than chosen at the call site. **Convergence with the same afternoon's `genesis-agi` run** on one root - an instrument must spell could-not-run differently from ran-clean - recorded as confirmation of `failure-not-empty-success` across three domains rather than proposed upward |
| 2026-08-31 | `github:herdrdev/herdr` @ `4dd9aa5` (re-run, Rust/backend lens) | first-party agent-runtime repository, **second sweep of an already-mined tree** | 3,304 (AGENTS.md) vs 420 (README) | 9 | 3 | 0 | 0 | 2 | [[2026-08-31-herdr-rust]] - the 2026-08-27 run mined this tree for terminal I/O; the operator's Rust/backend lens re-swept the same commit and it yielded 3 fresh landings from files the first run had left in its untriaged table. **A re-run under an orthogonal lens is not a duplicate** - the first sweep read `src/detect/manifests/`, this one read the justfile, the architecture test and the vendor ledger, and they did not overlap once. Landed: `operation-assertion-gates` (quality-gates) from a 200-line architecture test whose 4 of 7 tests test the scanner rather than the codebase; a third input class in `blocking-by-input-determinism` (deterministic subject, nondeterministic apparatus - the two-class axis mis-grades every timing gate); a `gate-laddering` section on source the compiler removed; and `vendored-fork-ledger` (supply-chain), where the subject's dependency model had exactly two states and **forking is a third that ends every guard rather than failing one**. 1 of 3 fetches, and it hit - the language reference's exact wording (a false predicate means the form 'is removed from the source code') is what let the cfg finding be written as analysis coverage rather than test coverage. Phase 7.5 returned `not-better` on a real 4-arm A/B: the consumer already refuses an empty scope in every checker tested, one of them stating the rule almost verbatim - independent convergence, and better evidence than an adopting tree. Index NOT regenerated: 5 siblings held uncommitted content, so a regenerated index would have referenced files absent from HEAD |
| 2026-08-31 | `github:diegosouzapw/OmniRoute` @ `b7a0c54` | **vendor repository (OSS LLM gateway)** - 13,026 files, ~50 CI gates, 4,207 unit tests; README is the ad (11,691 words) and the yield is in `docs/architecture/`, the middleware dir and a 23-item Hard Rules contract, where failure modes are recorded as issue-numbered revisions | 11691 README / ~34000 mined | 18 | 4 | 0 | 2 | [[2026-08-31-omniroute]] - operator-directed at LLM wrapping/middleware. Three techniques and one correction, 0 of 3 fetches (fourth consecutive practitioner-codebase run to spend none). **Two of the four contradict the corpus, which is the best case**: `resource-denominated-bounds` on `admission-queue` refutes `depth-bounds-and-shed`'s "size the count against the maximal entry" - that reasoning is exactly what produced the source's default cap of 1 and its 503s under ordinary load; and the `priority-and-fairness` amendment refutes the unstated assumption that origins are attested, after the source shipped per-key capacity and removed it (#10110) when unauthenticated callers minted identities to multiply the process-wide bound. Plus `speculative-work-admission` (redundant work is probed and skipped, never queued; release-on-admit; no wait knob) and `quality-axis-separation` on `model-routing` (a 200 measures transport, not answers - the semantic axis is null until an evaluator writes it and never shares a field with the operational one). 3 catches, all the corpus being confirmed independently; 11 untriaged with anchors; 2 leads. **Apply 4 rows, ship 2 commits** - `speculative-work-admission` A/B'd on the real prefetch queue in `goat` across three network tiers and shipped `code`/`better` (3g: 52 dispatches/18 useful -> 30/30; no-op at 4g). The run's best measurement was the arm that failed: skipping speculation indiscriminately served 0/6 high-intent prefetches, which promoted a caveat in the new technique to a boundary |
| 2026-08-31 | `github:calesthio/OpenMontage` @ `cd9f3c1` | **vendor repository (open engine, agent-as-control-plane)** - no runtime orchestrator; every creative decision, stage transition and review policy lives in Markdown skills and YAML manifests, so the prose files are executable and the strongest findings came from tests over Markdown | 943274 | 12 | 3 | 3 | 0 | [[2026-08-31-openmontage]] - three techniques, 1 of 3 fetches spent. Landing page 5,357 words against 943,274 in-tree (**176x**); the first clone silently dropped 2,159 files including the whole `.agents/skills/` tree under a long scratch path and reported success - `git status --short` after a fresh clone is the tell. `delivery-promise-lock` on `production-pipeline-phasing` (classify and lock the deliverable's medium category before the means; enumerate the metric's numerator adversarially, because the cheapest way to satisfy a quality ratio is to reclassify cheap output into it; halt rather than substitute) - home found by the enumeration hunt, the golden path covering only the *probe* that drops a dimension and not the *delivery* that does. `critique-carries-its-fix` on `review-iteration-loops` - the one fetch **corrected the source**: the cited paper is real (arXiv 2604.21718) but its axes are precision/recall/constructiveness, not the source's renamed trio, and the CMU/Harvard attribution is invented; written against the paper, and carrying an explicit cross-bundle discriminator against game-production's `severity-by-consequence` (does the consumer decide, or act?). `capability-coverage-contract` on `agent-instruction-files` - found as an asymmetry: ten techniques covered the direction with an error attached (named implies exists) and none covered the silent one (exists implies named); the source held **both forms of the test in one tree**, which is what made 'enumerate from the runtime, never from a list' evidence. All three applied to `gravity` same-run: 2 experiments + 1 simulation, all `better`, and two of them found the tree had reached the rule independently. 9 untriaged with anchors, 3 leads |
| 2026-08-31 | `github:cline/cline` @ `48d6385` | **vendor repository (open tree)** - no hosted engine to hide: agent loop, provider layer, hub daemon and eval harness all in the tree, so the class's "stated production rules" third is a 6,243-word architecture doc written as design rules with paid-for corrections recorded inline | 1178 landing / 124315 in-tree | 14 | 2 | 6 | 3 | 3 untriaged | [[2026-08-31-cline]] - two techniques, 0 of 3 fetches (tenth consecutive zero-fetch run for a source carrying its own primary material), read fraction ~105x and **zero accepted candidates from the README**. `absent-status-passthrough` on `fleet-orchestration` is a **missing producer**: the subject models the session reporting itself and the sweeper inferring from silence - both observed something - and never the layers between them that observe nothing and are asked for a value anyway (a transport projector, a probe whose identity provider is down, a creation-time seed). Landed as ONE technique from two candidates that shared a root, per the standing critique that the synthesis must come from the skill. **The sweeper is structurally blind to it** - the mislabelled session is alive and recently heard from, so every staleness budget passes; a fleet with both tiers correct still deadlocks. `lifecycle-signals` cites three laws and not `unknown-is-not-a-value`, the one that governs this exactly. `reliability-aggregation` on `eval-harness` came from the **enumeration hunt, eleventh consecutive pay**: the golden path lists the aggregation rules for repeated trials (mean, median, worst-of-N, pass-rate) and the list omits **all-of-N**, the rule a shipping decision rests on; the source's instrument supplies both estimators over one trial set with its own worked example (2 of 3 trials -> any-of-3 ~96%, all-of-3 ~30%). The subject held both halves - `count-carries-predicate` (label the rule) and `metric-role-contract` (name the decision) - and had never joined them. Both applied same-run to `personas`: the eval one as an **experiment, `better`, arms paired** - same trials, **the leaderboard winner flips** - plus an unsought second finding (an unscored metric averages to 0 and sorts as poor performance); the fleet one as a **simulation, `better`**, whose structural fact is the good kind - the auto-kill gate already fails closed on absence, but only because a map lookup returns an optional, so the discipline exists exactly where the type system imposed it and nowhere it had to be chosen. 3 catches (identifier reuse; failure classification, where the corpus is ahead twice over and the source's own regex-over-a-whole-log classifier is the anti-pattern `error-classification-for-retry` warns about; completion-vs-teardown, already drawn at higher altitude as lost-vs-exited), 6 leads, 3 untriaged with anchors, 0 declines. **Phase 4's "one moment" warning fired for real again**: `fleet-orchestration` was clear at Phase 4 and held by a sibling by Phase 7 - technique file uncontended, golden-path list edited under the `content` lock with a re-read inside it. 8 live siblings at claim time; gate was red on arrival from two siblings' untracked in-flight directories and was left alone |
| 2026-08-31 | `github:Aider-AI/aider` @ `5dc9490` | **vendor repository (open engine)** - mature practitioner codebase; landing page 1,156 words against ~9,500 LOC of core engineering mined and ~428k words of in-tree markdown | 1156 README / ~9500 LOC | 10 | 2 | 2 | 2 | [[2026-08-31-aider]] - operator-directed at architecture, code design and performance; routed away from `fleet-orchestration`, `eval-harness`, `retry-backoff`, `prompt-assembly`, `model-routing`, `agent-chaining` and `cost-metering`, all held by the 8 live siblings, which left the repo-map and edit-apply engineering nobody was near. Two landings on `retrieval`, both from **hunting an enumeration and an asymmetry rather than a gap** (0 of 3 fetches - fourth consecutive practitioner-codebase run to spend none). `structural-centrality-lane`: the golden path enumerates its lanes and denies a symmetry ("every other lane is a function of the query", proximity being "a function of results"), and the missing kind is a function of neither - the corpus's own link structure, ranked before any query exists and steered by the query as a personalization vector rather than filtered by it; carries the redistribution move (rank nodes, emit definitions) and the discriminative edge weights. `"pagerank centrality"` returned zero corpus-wide. Plus an amendment to `ranking-budgets`, which argues admission *value* is set-conditional and models admission *cost* as per-item additive in the same file - where the slice is rendered rather than concatenated the greedy pack is unavailable and the cut becomes a bisection over prefix length with a tolerance band, a best-under-budget ratchet and a sampled token estimator. Both applied same-run to `personas` as read-only experiments, both `better`: centrality found the stored edge table at **0 rows against 1,630 nodes** and the tier's `importance` field at its schema default for **93.7%** of the tier (0/10 top-10 overlap between arms), and the budget found a **25.1x** spread in rendered size at a constant count of 20. The centrality arm produced a finding nobody sought - `importance DEFAULT 3` is `unknown-is-not-a-value` in a schema default - which became a section of the technique rather than a footnote. 2 catches (elastic map budget, cache-breakpoint segmentation - both checked by opening the files), 4 untriaged with anchors, 2 leads (disabled-in-place fallbacks as a deliberate negative record; the architect/editor role split). Ship 0: experiments changed no product code and Phase 8 wants explicit confirmation before a cross-repo commit |
| 2026-08-31 | `github:firecrawl/anydoc` @ `261fc25` | **vendor repository (Rust crate + Node/Python/wasm bindings)** - no `docs/` at all; its operating documents are **four release notes under `.github/releases/`**, which the class's sweep checklist does not name. Add that path to the sweep: a repo with no `docs/` is not a repo with no operating documents | 2100 README / ~32000 lines in tree | 13 | 3 | 2 | 2 | 5 untriaged | [[2026-08-31-anydoc]] - operator-directed at Rust depth, a new OCR path, and a `personas` feature. 0 of 3 fetches. **A new subject was forged in-session**: `research-map` returned no prior art for `"ocr"`, `"decompression bomb"` or `"amplification"` across 340 subjects in 8 bundles, so `integration/document-text-extraction` was specced, dispatched to one forge worker, and diff-reviewed here - golden path plus `unreadable-region-refusal`, `screen-then-confirm-detection`, `extraction-yield-bands`, `recognition-boundary-and-escalation`, `structural-amplification-caps`. Read in order the four release notes are **the same defect found four times** (equations vanished; the fix escaped every `$` and broke prices; checkboxes dropped, "a sheet of tick boxes lost its answers"; scanned pages "silently missing"), and the rule that organises the subject is the source's own: a partial extraction errors "whether that is every page or one of a hundred, because output missing those pages would read as complete" - **the threshold is not a fraction, it is whether the consumer can tell**. Two amendments on `error-handling` from the enumeration hunt: `taxonomy-design` states **three** axes "because these are the questions consumers branch on", and the source's `is_fatal()` is a fourth - *recoverability in place* - whose membership inverts where absorption is the default, because **a limit that can be swallowed is not a limit** (relocate the payload into a part the reader treats as optional). Neither amendment touched a golden path, so no `content` lock in a six-sibling checkout. **The spec was wrong once and the worker caught it**: `import-validation` already owns bounded parsing including the derivation-beside-the-number rule, so two of three proposed amplification rules were already covered and only "bounds the input vs bounds the amplification factor" survived - a `research-map` empty for a phrase is not an empty for the concern. 2 catches (the two-channel error code, which `structured-propagation` already states; the cap rules above). **Apply 1, ship 1** - `unreadable-region-refusal` A/B'd in `personas` as `code`/`better`, arms paired on the project's own chunker test target: regions nameable to a caller 0 of 4 -> 4 of 4, tests 8 -> 12, no schema change. The structural fact is the good kind - a recognition command and a text-layer extractor in one binary that **could not be joined**, because the page identity was discarded by the loop that computed it. 5 untriaged with anchors, four of them in subjects live siblings were holding; the best is the source's own bench harness counting position-swap disagreement as a tie and then admitting **unpaired** verdicts at face value into the same win rate |
| 2026-08-31 | `github:VoltAgent/awesome-ai-agent-papers` @ `a2ffba2` | **reference index** - the class's first run. One 175KB README and nothing else: 16,346 words that are almost entirely link text plus a ~23-word curator annotation per row | 16346 landing / 0 in-tree docs | 40 | 7 | 11 | 9 | 366 untriaged | [[2026-08-31-voltagent-agent-papers]] - **382 distinct documents enumerated, 16 read across 2 waves, 366 ranked and recorded** in a companion file so the next pass over this index diffs instead of re-deriving. Signal mix over all 382 annotations quantifies the class's marketing half: 28 claim a negative result (7%), 126 a measurement (33%), **167 propose a framework with nothing measured (44%)**. **Three of 16 annotations were materially wrong** and each would have corrupted a top-3 sample: seeds named as a dominant effect the paper measures as its smallest (~3% vs 6-14.5 points for deterministic implementation choices); "56 scenarios" that are 53 with only 3 quantitatively evaluated; "48,000 scenarios" that are 300 base cases under ~160 conditions with the taxonomy induced from 170 hand-coded traces. Two convergence families landed. **Family A** (3 independent sightings, disjoint authors/institutions/benchmark lineage, earliest predating the others so not a relay): `prompt-safety`'s "the worst fully successful injection yields an embarrassing sentence, not an action" is false where the deliverable IS the prose - the capability ladder holds perfectly and buys nothing because nothing downstream needs to act. Landed as a bounding of its own ranking plus least privilege on the READ side, and the strongest support is internal: the subject calls a secret recalled into prose "the attack proper" two sections above the ranking that ignores it. Plus a fourth payoff class (**persuasive** - prose that works to lower its reader's chance of rejecting it) and a second fencing residual that is a property of a PAIR of spans, which every per-span operation in the subject is blind to - not an inventory gap but a remedy gap. **Family B**: the tool lane's triggers test shape, never truth; `untrusted-result-handling` is one-directional (all mechanisms lower a result's weight, none raises it) and `failover-horizon`'s six unusable-success forms are all parseability. The remedy came from **the baseline column of a paper whose own thesis failed** - agreement across repeated draws, canonicalized, precision 1.0 black-box, priced at n x tokens. **The run corrected its own premise mid-flight and the correction outranks the content**: wave 1 concluded the corpus owns nothing that scores a trajectory, verified by a grep for `ReAct`/`Plan-and-Execute` - scaffold proper nouns that `check-bundles` FORBIDS in upper layers, so the grep measured the purity gate and not the coverage. A manufactured total empty. `worker-trajectory-anatomy` (1,794 trajectories, 63k steps, 20,574-session field corpus) is exactly the missing thing and beats both papers proposing one; the full groundedness build is eight techniques in `civic-intelligence/llm-forensic-gating`. **Root cause and the run's most valuable output: `research-map` matches slugs** - a director test in-run returned six subjects for "evidence conditioned faithfulness" and `llm-forensic-gating` was not among them. Every "we have no material on X" drawn from that instrument is suspect. 2 applications, both **structural-only / unmeasurable** with the instrument named (the personas research-lab tables hold zero rows; the agent layer has no field in which a tool conflict could be recorded). 7 live siblings at claim time held most of the ground this index maps onto, which killed the top-ranked paper of all 382 (its only home is `retrieval`, held) and deferred two landings until the board cleared |
| 2026-08-31 | `github:whatwg/html` @ `778afd9` | **standard repository (living specification)** - the ledger's first, and the top tier of the corroboration table: a primary normative document, so its statements about its own conformance model authorize rather than merely originate. Landing page 302 words against 717,109 in-tree (**2,374x** - a README-only read would have been a 0.04% sample of the ad) | 302 landing / 717,109 in-tree | 13 | 2 | 2 | 2 | 6 untriaged | [[2026-08-31-whatwg-html]] - the class's calibration point is a ratio nothing else in this ledger approaches, and it inverts inside the tree too: **711,540 words of normative text produced zero landings**, while ~120 words of `<p class="note">` and a 5,472-word FAQ produced both. A standard's subject matter is un-strippable by construction; what survives is how the specification is engineered as an artifact. 0 of 3 fetches (eleventh consecutive zero-fetch run for a source carrying its own primary material). `fabrication-economics` on `quality-gates` is the **mirror of `false-positive-economics`** and the enumeration hunt's twelfth consecutive pay: a gate dies of firing on correct content; a *rule* dies of false compliance, which leaves the gate green forever. Where a requirement's satisfaction cannot be verified, the author with nothing true to write is offered two moves and the fabrication is always the cheaper one - **the gate is not fooled, the gate is the cause** - so the fix leaves the detector (which cannot improve; the distinction is not in the data) and moves to the rule's design: a declared inability token, artifact stays non-conforming, verdict channel silent, census counted elsewhere, and the token itself exempt or the pressure has only moved one rule down. `unmeasurable-criteria` states "there are exactly three honest resolutions" over a *missing* value; here nothing is missing and the fourth state is **known-violating, deliberately unreported, separately counted**. Two of its rules invert (a skip must be loud; this token must be silent) and the discriminator is written on both sides. Prior art was read and contradicted rather than duplicated: `generated-from-provenance` already holds "leave a marker, never invent" and says the checker *can* report the marker - the opposite of what the standard requires, for a reason the corpus had not reached. `declared-deviation-register` on `conformance-checking` came from a **corpus-wide empty**: `research-map` returned PRIOR ART: none for `"intentional nonconformance"` and `"why we deviate"` across 341 subjects in 8 bundles, verified by opening the four nearest subjects. The source breaks six other specifications on purpose and records every one in its own normative text, in its own conformance vocabulary, per site, with document + clause + motivation-as-cost + (twice) the upstream thread where the conflict is live, and refs pinned to a revision. It also carries the boundary: one entry marks the weaker "contradicts the spirit" class and one source comment *rejects* a candidate. Six entries over 711,540 words is what a maintained register looks like. The corpus-shaped consequence is arithmetic and lands on `pass-ratio-comparability`: an accepted deviation is a **third finding class**, never a suppression (which inflates the ratio by exactly the failures the team accepted) and never an ordinary failure (which calibrates a team to a permanent non-zero floor). Shared root with the first technique - a true, known violation declared in band and not reported as news - **stated in both files and deliberately not merged**, because an author who could not comply and a maintainer who chose not to have different failure modes and different required fields. **Apply 2 rows for 2 landings, both `better`.** The fabrication A/B ran `ab-paired` over 63 `<img>` elements in `personas`: arm A (the rule teams adopt - is the attribute present?) raises **1 finding, and it is a false positive**; arm B (is the no-content claim supportable?) raises **35, 55.6%** - 30 empty values on a runtime-computed source, 5 filenames used as descriptions. **The run's premise was disproved and the result got stronger**: the tree has no accessibility linter at all (21 hand-written lint rules, none touching the field), and the fabrication is at a clear majority anyway - so the technique gained a measured correction, *the gate is sufficient, not necessary*. The structural fact is the good kind: the one site that documents its decorative intent, using the platform's real remove-from-the-tree mechanism, is the one site arm A reports, and it is correct as written. The register simulation over `personas` found **1 of 8 behavioural conformance claims declared, 0 of 8 enumerable**, and a module header claiming a named RPC protocol whose entry point has no array handling anywhere in either file while the cited spec's §6 requires batch support - probably a correct deviation, which is exactly why a maintainer needs to be able to tell it from an omission. Three of the eight are near misses the register must *refuse*, which is where the boundary earned its place. 3 catches, 6 untriaged with anchors (the change-admission gate among them, **routed away** because `tc39-proposals-0831` held `quality-gates` live and owns the better authority for staged advancement - both landings came from material that source does not carry), 2 leads, 0 declines. Ship 0: both assessments are read-only and neither proposes a few-readable-lines change. **`index.json`/`catalog.json` regenerated under the lock and left uncommitted** - three siblings held uncommitted or untracked content, so a committed index would reference files absent from HEAD (same call as the herdr run) |
| 2026-08-31 | `brooker.co.za/blog` (163 posts; 33 ranked, 16 read in 2 waves) | **reference index (single-author archive)** - the lane's usual economics inverted: every reference shares ONE author, so within-index convergence deduped by author is structurally unavailable and every convergence is n=1. Most of the archive sits **below the construction frontier** (`fair queuing`, `uuid`, `microvm`, `overload`, `utilization`, `best of n` all returned zero corpus-wide) - third sighting | 1685 index / ~14000 read | 33 | 8 | 9 | 8 | [[2026-08-31-brooker-blog]] - **8 landings, 7 applied, 5 of them corrections to what the corpus already said and 3 of those against content sibling runs landed the same morning.** 24 fetches across 16 lanes. The instrument lied three times and workers caught it: `research-map` reported "never heard of this" for `fair queuing`, `utilization` and `queueing theory`, and all three were false - one lived under a slug nobody would reach (`scale-investment-timing` owns open-vs-closed arrivals and the 70-90% knee), two lived in an **application** document, which `research-map` does not rank. **A near-empty from that instrument is not a hole; twice in one run it was material we had written and never lifted to the technique layer.** The source's value is that it writes decompositions, and a decomposition is a probe: 10 of 16 lanes landed their best finding on a sentence where one of our own documents declares itself complete ("three policies", "three ways", "two answers", "per category, not per site", "not an age cutoff"). Its *assertions* landed almost nothing - 4 of 16 lanes found the stated class wrong (position essay, vendor product paragraph, teaching relay, 74-word aphorism) and returned `nothing`. Landed: `queue-cardinality` (new, on a subject whose nine techniques all governed policy inside ONE line; two lanes converged and our own APF application had recorded the mechanism 15 months ago); `admission-hypothesis` (new - the golden path said a cache declares **three** policies and admission's default is invisible); corrections to `reliability-aggregation` (opened with a model over a rate and forbade that move two sections later - needed no source at all), `priority-and-fairness` ("takes nothing from anyone else" is false for throughput share), `taxonomy-design` (never-absorbable is a predicate on category x **arrival correlation** - corroborated by a primary postmortem, not the essay), `retention-and-pruning` ("a query with guards, not an age cutoff" - an in-flight reader is the one reference with no row), `capability-typed-queues` (welded pool and queue, mispriced one); plus `wait-telemetry` gaining the sizing identity and the caller-vs-entry gap. **Apply 7 better / 1 not-better, all experiments on real rows, and four of them amended the corpus from the tree**: a 4.3x regression bounded `queue-cardinality` with a coexistence test; a fleet A/B put honest throughput at 1/min under the *correct* fairness policy, so the global bound protects the resource and abandons the user; a reaper gate supplied a **third** retention mechanism the amendment's enumeration excluded, plus its harm-bound and register-at-the-store-touch clauses; a broadcast-artifact experiment kept 32/32 assertions alive across 6/6 refusals that currently take them all to zero. Measured en route: failures cluster **123x** over independence in an eval corpus (chi-sq 1737), which is direct evidence for the independence correction. 8 catches, 9 leads (one at law altitude), 17 references ranked-and-unread. Ship 0: read-only lane, no cross-repo commit confirmed |
| 2026-08-31 | `danluu.com` - the six PUBLIC 2026 posts, read whole (operator filter: articles published in 2026) | **reference index (single-author archive)** - second sighting of the class in one afternoon, independently reached by `brooker-2026-08-31`. Its defining property: within-index convergence deduped by *author* is structurally unavailable, so a claim recurring across four posts is ONE observation. Unusual for the class in that the author publishes protocol, numbers, and the places he was refuted - including inverting a headline of his own. | 1600 index / **53,978 across 6 refs** | 12 | 5 | 5 | 4 | [[2026-08-31-danluu-2026]] - **100% sample of the reachable references** (6 of 6 public; 3 Patreon-only unreachable), not a top-N, which is what makes the four catches worth recording. 0 of 3 fetches - twelfth-ish consecutive for a source carrying its own measurements. Five landings, four of them in `eval-harness` which a sibling held: `candidate-write-access` (new - the system under test is an agent holding the harness's own shell; two write surfaces, instrument and neighbours; the corrective is structural and inverted - **declare the holdout rather than forbid overfitting**, whose tell is that the VISIBLE score gets worse while the held-out score rises), `resolution-precondition` (new - between-condition spread must exceed within-condition SD before an order is published; measured SD 0.075 against a full best-to-worst spread of 0.069, so every ranking that matrix could produce sat inside the noise of one of its own cells), `repeatability-floor` (new, and the run's best finding because it is a **cross-bundle asymmetry** - the builder side has carried a repeatability floor in `judge-stability` for weeks while the operator side's kappa, trust bar, per-cycle drop and windowed regression have none, so both its detectors fire on unmeasured re-score noise; 23% verdict flip re-grading ONE FIXED artifact, and the half that made it a technique is that repeatability is **per dimension** - 32%/5%/3% across one rubric), `grounding-over-deliberation` (new, `agent-chaining` - a subject that wires steps and never prices them; execution dominates deliberation and N deliberating steps do not sum to one grounding step, with the corollary that a self-produced ground truth is still the candidate's artifact), and a `comparison-modes` amendment naming the self-referential opponent. **Four catches verified by opening files rather than by slug** - `judge-stability` already owns the repeatability floor and the cross-instrument swing, `discriminating-task-selection` already owns the unanimous-cells argument, `retrieval-evaluation` already owns human tuning leak. **Apply 4 rows / 5 landings, three of them real two-arm A/Bs** against a managed project's own measurement apparatus: a 110-byte vacuous agent spec scoring 6/6 identically to the real 2,414-byte one (0 of 6 quality assertions survive candidate write access); a fabricated-citation proposal passing the shipped integrity gate 0-refused and refused by a 20-line source-fidelity arm; and **r = -0.378 between how much of a subject was judged and its deviation rate**, with 0 of 142 judged pairs ever double-judged. Ship 0 - operator's pick named no project, so no tree was edited. |
| 2026-08-31 | `lea.verou.me/blog/2026/` - all four 2026 posts | **channel corpus (first-party practitioner accounts)** - one author, a WHATWG/CSS WG spec editor, so within-batch convergence is structurally void and triage leaned on corpus-vs-source novelty. The class predicted amendments and corroborations, never new subjects; that is exactly what landed | 12768 (4 articles; index page 107, correctly thin) | 13 | 4 | 2 | 1 | [[2026-08-31-verou-2026-blog]] - **2 techniques, 1 correction, 2 golden-path sections, 0 of 3 fetches** (fourth consecutive run landing corpus-internally; this author can only *authorize* claims about the standards process, which no bundle here is about). `inherited-default-override` on `settings`: the subject models every default as a **static constant declared in code**, and a minority of keys default to a **live upstream source the app does not own** - an axis that redefines all three store operations (absent = follow continuously, a write = **detach**, a delete = **re-attach**), so the row's PRESENCE carries meaning independent of its content. Two feedback-free corruptions: writing unconditionally pins a key that was merely following, the instant the target coincides with the source; clearing an override when the source moves makes pinning **unachievable** for anyone whose environment switches on a schedule. Seam, not hole - `setting-kinds` already refuses to store *derived* limits (same freezing failure, opposite remedy: there you store nothing, here the user must be able to detach). `fallback-retirement-condition` on `optional-dependency-degradation`: that subject treats absence as a **stable per-deployment fact**; this is absence as a **closing frontier**, where the fallback is a bet the gap closes and owes a reaper the corpus already demands by law (`creation-names-reaper`) - every fallback in its ladder names a *consequence*, none named a reaper. Where the capability is testable the check IS the reaper; deleting it (proposed on integrity grounds) converts a bounded self-clearing cost into a permanent tax, and suppressing the fallback **disperses** the need into ad-hoc conditionals no upgrade retires. **The correction is the run's best shape**: `adaptive-fidelity-tiers/preference-short-circuits-measurement` had the requirement right (every statement retires a measured default; no permanent exit) and the remedy wrong (a visible third state) - invisible to any concept query built from this source's vocabulary, and only opening the file showed it. **Apply 3 rows / 3 landings, 1 shipped** (goat `facc957`, `code`, 1/2 -> 2/2 viewer environments, arms extracted from real revisions): the embed resolved `auto` **server-side**, where the viewer is invisible. Structural facts: the three-state *model* survived and the subscription died at the **resolution point**; and on personas, 8 seams test the capability not a proxy **8/8** while emitting the branch **0/8** - the check is load-bearing and the emission is not, so conformance tracks consequence rather than diligence. The apply step **amended the technique**: defer a resolution only to a consumer that can perform it. 1 catch (double-keyed caching - nothing in the corpus ever claimed the shared-CDN benefit, so there is no clock to reset), 8 untriaged with anchors (row 8, a locality/composability/scalability invariant triad, is subject-sized), 2 leads |
| 2026-08-31 | `github:TanStack/query` @ `1566c16d` | **vendor repository (open engine)** — mature practitioner codebase; 383-word README against 134,411 words of in-tree docs (497 files), 7,249 LOC core and **105,254 LOC of tests (14.5:1)** | 383 README / 134411 docs / 7249 LOC core | 12 | 3 | 2 | 7 | [[2026-08-31-tanstack-query]] — **three landings, all from hunting an enumeration or an asymmetry, and 3/3 applied to the one fleet project that consumes this library.** Fifth consecutive practitioner-codebase run to spend **0 of 3 fetches**: engine and operating instructions ship in one tree, so every claim was checkable in-run. Four siblings live at claim and **none held any of my four subjects**, so no `content` lock was ever contended — worth recording as the clean counter-case to the usual collision notes. `plural-policy-claims` (new, `client-fetch-cache`): the golden path declares *"Every cache declares four policies"* and says to state all four **at the construction site** — every one assumes a **single declarant**, and a cache keyed by argument rather than call site has a *set* of claims per policy. The code resolves that set with **three different quantifiers over one observer list** — retention by `Math.max` and monotonic, believability by `some`, focus-refetch by `find`-first — plus reference counting underneath, since an attached observer clears the GC timeout so the eviction clock starts only when the *last* claimant leaves. The unifying rule: **the quantifier is chosen by which direction of error is unrecoverable.** `suspension-is-not-failure` (new, `retry-backoff`): *"exactly four legitimate terminal states"* is correct on its own terms, which makes this a missing **stage** not a contradiction — all four are terminal and the ladder has a fifth that is not, **suspended**, where no attempt is made and no budget spent; its nearest neighbour `denied` is attributed oppositely (a breaker judges the dependency, a suspension reports the caller), and the predicate that *resumes* is strictly stronger than the one that *starts* because resuming is the machine's own initiative. `observed-read-subscription` (new, `client-state`): an **asymmetry a slug map cannot see** — the corpus models the **declared** form of subscription narrowing ("subscribe to the narrowest projection you read") and never the **observed** form, where a proxy records the read set; its three guards are each a rule, the sharpest being that an **empty read set means unknown and must fail open** (`unknown-is-not-a-value` — otherwise a never-yet-rendered consumer never renders again), and its defining hazard is that **enumeration is indistinguishable from reading**, so one spread subscribes to everything and the corrective is a linter rather than documentation. **Applied 3/3 `better`, all read-only experiments on `goat`**, with the instrument caught lying twice: a flat constant table let `GC_TIME_MS.STANDARD` overwrite `CACHE_TTL_MS.STANDARD` and reported a **2.0x spread that did not exist**, and a 900-char lookahead invented a divergence between two different keys — both found by opening cited lines by hand, after which the harness was made to assert its own parse and did refuse to print once. Measured: **2 of 7 shared cache keys diverge numerically (2.0x, 3.0x)**, both with the same shape (a prefetcher claiming longer freshness than its consumer, so the prefetch's stated warmth is inert), and the 2.0x case is a **vocabulary collision at the alias layer** — two modules binding the label `SHORT` to different constants under the same table name; the retry classifier **refuses 0 of 17** permanent error codes because it substring-scans user-facing copy while the status sits in a typed field it never reads (`gate-sees-target`), with the classification it needs already present one module away and never consulted; and the project relies on read-observation at **100% of call sites** with the lint plugin **not installed** and the explicit declaration appearing nowhere, its single known defeat sitting in a shared wrapper (blast radius honestly 2 consumers). 2 catches, 2 leads, **7 untriaged with anchors** — including the source's own best idea, that its **eight lint rules are a taxonomy of the type system's blind spots** (key order, referential identity, read set, return-ness, container lifetime), which the operator did not pick. Ship 0 at Phase 8: the triage pick named no project, so step 2 confirmation was absent. |
| 2026-08-31 | `lea.verou.me/blog/2026/` (XL follow-up, same source) | **channel corpus - the banked XL row, built** | - (no new ingest) | 4 untriaged rows folded | 7 (1 subject: golden path + 6 techniques) | 1 | 0 | [[2026-08-31-verou-2026-blog]] - the operator picked the banked `XL` row the same session, which is the outcome the scorecard's declared focus was written to produce. New subject **`dependency-declaration`** in `engineering-process/codebase-stewardship` (placement verified against `taxonomy.json`, the authority - the category holds 6 subjects and no subcategories, so a 7th adds no depth). It absorbs untriaged rows 8-11 as four of its six techniques rather than banking them as leads: `declaration-invariants` (locality/composability/scalability, and each failure's distinct cost), `logical-name-or-address` (**purview** is the discriminator; syntax and immutability are both plausible and both false), `shortcut-is-not-the-substrate` (a convenience binding that becomes the only door, and the extract-first repair order), `declaration-cost-floor` (advanced tools for advanced needs is correct; the defect is a *foundational* capability priced at an advanced tool, plus the inversion where the platform gets designed around the workaround). Two more fall out of the frame: `progressive-resolution` and `vendored-copy-loses-composition`. **The subject borrows rather than mints its central argument**: `module-design/locality-and-leverage` already holds *things that change together live together*, and a unit's dependency list changes exactly when the unit changes - so the locality invariant is an existing law applied to declarations, cited not restated. Applied 1 of 6 (experiment, `better`) with the other five carrying per-technique reasons: personas' plugin mechanism scores locality FAILS 3/3 surfaces, composability FAILS, scalability holds, **0/10 units self-declare**, and the three registries give three different answers to which plugins exist with no stated authority. Ship 0 - the repair is architectural |
| 2026-08-31 | `github:TkDodo/react-query-beyond-the-basics` - "React Query - Beyond the Basics" | **first-party practitioner account, repo form** - a workshop whose content is a *branch sequence*, not a file tree; README is 158 words, the source is seven ordered feature diffs | 158 (README) / ~700 diff lines (the source) | 10 | 1 | 1 | 3 | [[2026-08-31-tkdodo-rq-beyond-basics]] - one technique, `windowing-vs-identifying-keys` on `async-ui-states`, and it is a **stated doctrine corrected rather than a hole filled**. `state-model` named the decision (when does the sticky `settled` bit reset), called it *the subtle decision*, and prescribed "pick one policy per product and apply it everywhere"; `table/loading-and-empty-states` carried the same sentence and `arrival-choreography` the same undefined term - three sites, one primitive nobody defined. The source contradicts it in one line (`previousData?.filter === filter ? previousData : undefined`) and the same distinction independently governs three further subsystems in the same app (scroll reset on page links only, `page: 1` on filter change and never the reverse, and deferring `page` alone). So the corrective keeps the fear and drops the prescription: mixing per *call site* is the defect, mixing per **axis** is a rule the user can state. Golden path gains the `superseded` state and a **fourth** honesty lie; `state-model` gains the matching forbidden edge; `table`'s duplicate line now resolves per-axis and points here. 1 of 3 fetches, and it did two jobs - the primary confirmed the mechanism verbatim (deferral is a property of the *input*, not the response) **and** confirmed the gap (it warns only about *referential* stability, never semantic eligibility). Applied 1/1 as a read-only census over two React trees: instrument **self-asserted and refused to print** on the first run, 2 candidates, **1 survived hand-verification**, seam is a seven-field params bag (3 identifying, 4 windowing) with no retention and a search setter that does not reset the page. `unmeasurable` not `better` - both windowing coordinates are unreachable from the UI, so no gate can see it; instrument named. 3 catches, 1 lead, 4 untriaged. **Class lesson: a workshop repo's word count is a lie about its size - run `git ls-remote --heads` before calling a repository thin** |
| 2026-08-31 | `github:future-agi/future-agi` @ `5b84ef4` - open-core eval and observability platform | **vendor repository (open-core)** - holds **two agent memory systems built to opposite standards**, neither file aware of the other: the internal analysis agent's store is typed, hash-deduped, TTL-pruned and usage-tracked; the product-facing agent's is a flat key-value table where the newest twenty rows win by creation date. The managed one never faces a user | 2676 landing / **42422 in-tree md** + ~2500 LOC memory path | 9 | 2 | 3 | 3 | [[2026-08-31-future-agi]] - operator-directed at memory, which is why it landed: the yield is all in the memory code and the README is about neither system. **Two amendments, no new techniques** - the honest shape against `agent-memory`, the corpus's most mature subject and the librarian's #1 attention point (56pt), which already held 13 techniques. The headline amends `probe-without-write-back` **against its own closing claim**: that technique enumerates the counter's *readers* and rules an under-count "the better failure ... ranked conservatively". Here the usage columns are written by the persistence helper and a point lookup while the bulk read that actually serves the agent writes nothing, so `access_count` is a write counter wearing the usage axis's name - which smuggles back the very "order by last edit" failure `memory-value-model` installed that axis to repair. Absence would have been safer: zeros get noticed, plausible integers do not. Second amendment to `archive-restore-semantics` (other bundle): a same-row existence flag has two populations that fail in opposite directions - the set-level delete bypasses the per-entity soft-delete override (the agent's own delete tool hard-deletes while the human API soft-deletes), and a full unique index never heard of the flag, so a human-deleted key becomes permanently unwritable. **Applied 1 of 2, and the A/B is the run's best artifact**: both audits run on one managed tree with one instrument - arm A (the existing reader-side enumeration) returned **0** findings over four production read sites, arm B (the amendment) returned **1**, a delivery surface arm A's own "suppress new machine callers" default correctly blesses. The apply step then corrected the amendment (a read that hands its selection to a later consumer counts at the consumer). Shipped a comment-only slice: the tree's written memory contract named one caller and had three. Row 2 unapplied - no managed project carries the seam, because two independent trees already implement the branch the amendment recommends. 0 of 3 fetches |
| 2026-08-31 | `web:arxiv.org/abs/2606.10106` - "What makes a harness a harness" | **paper - the definitional subclass**: conceptual analysis over grey literature, **no measurement anywhere in it**. By the class rule ("framework papers are the class's marketing") and the tiering rule (commentary about primaries, not a primary) this is the lowest tier that can still originate a finding, and the expected yield was stated as 1-2 leads before the triage table | 768 landing / 11504 full text | 11 | 0 | 2 | 8 | 0 untriaged | [[2026-08-31-agent-harness-definition]] - **eight catches, zero landings, 0 of 3 fetches** (eleventh consecutive zero-fetch run). The corpus already owns all four of the paper's necessary-and-sufficient conditions, distributed across five subjects, and in six of the eight the corpus is *sharper*: the paper's declared central open problem - "an evaluation that isolates the harness's contribution, controlling for the model, is missing" - is `eval-harness/unaided-baseline-screening`, shipped, with the trigger "scores barely moved when the material under test was removed". Its admission that classifying six known-positive harnesses was "almost tautological" is `quality-gates/gate-liveness` as a practice. Its temporal discriminator is `hitl-approval`'s "review gates output after it exists; consent gates action before it happens". **Three near-empties in the map (`verifier`, `guardrail`, `containment`) were all seams, not holes** - believing them would have minted four duplicate techniques. Second lead is recorded to PREVENT a landing: the corpus's five-way decomposition of "agent harness" by concern is correct, and a subject named for the composition would be a container |
| 2026-08-31 | `web:tkdodo.eu/blog/the-vertical-codebase` - "The Vertical Codebase" | **first-party practitioner account** - a practitioner arguing his own product's ten-year structural decay into a general prescription; reliable for the decay, n=1 for the prescription | 1889 | 8 | 1 | 1 | 1 | [[2026-08-31-tkdodo-vertical-codebase]] - **the thesis was already covered and the yield was in the catch.** The post's argument (group by domain, not by technical kind) is carried in one sentence of `module-design/locality-and-leverage` - "where implementing one feature requires editing in three sibling trees, the grouping is describing a taxonomy of *kinds of file* rather than a taxonomy of *reasons to change*" - which catches the thesis and its detection signature together, stated more generally and without the proper nouns the post is built from. What landed came from the post's closing "Where's the catch?", the section it is least proud of: **private code means teams re-implement**. Found by the enumeration hunt - the golden path lists nine failure modes and **every one is structure being too weak, misplaced or decorative**; there was none for a boundary that *succeeded*, while the subject prescribes hiding aggressively ("hiding is active, not passive") and separately treats re-implementation as a defect fifteen times across the bundle, always fixed by *creating* a shared primitive, and never connects the two. Amendment `The cost that neither payoff prices` on `locality-and-leverage` + a tenth failure mode on the golden path: both payoffs are collected by people who already *found* the module, so leverage (capability per unit of interface learned) begins its accounting one step after discovery; hiding internals is free and is what depth buys, hiding *existence* is a separate act delivered by the same declaration as a side effect nobody chose. Discriminator is the file's own change-coupling test pointed the other way, which is why it is an amendment completing an axis rather than a competing technique - the "no" branch lands on the wrong-abstraction rule already there. **0 of 3 fetches - sixth consecutive zero-fetch run for this class**; corroboration was corpus-internal plus training-data convergence on the autonomy-vs-duplication trade, and was stronger than a fetch because the finding is a gap in *our* model. Applied 1/1 `experiment`/`better`: two arms on a Rust workspace, 232 candidates, 67 invisible to scatter, 52 after filtering forwarding wrappers; **the first instrument design was refuted by hand-verification** (its top hits were private adapters delegating to the public implementation - the single-door discipline, not duplication). Ship 0, blocker `confirmation`. 2 catches, 1 lead, 4 untriaged. Gate found red on a live sibling's file; reported, not touched |
| 2026-08-31 | `github:TkDodo/knip` - knip, find unused files, dependencies and exports | **OSS tool repository** - engine, operating documents and measurements in one tree, so every doc claim is checkable against implementing code at zero fetch | 380 (landing) / 32,535 (in-tree docs) / 28,319 LOC | 21 | 2 | 1 | 4 | [[2026-08-31-knip]] - mined the dead-code tool against the dead-code subject, and both landings are corrections rather than holes. **excess-indicts-the-instrument** (quality-gates): `gate-liveness` enumerates every instrument-failure signal and all of them are *deficiency* signals; there is no excess signal in the subject, while `blocking-by-input-determinism` asserts a large population is about the backlog rather than the check and `ratchet-design` then freezes it - the three compose into a trap where a ratchet defends a misconfiguration forever because the count only goes down. **precision-trades-have-a-direction** (codebase-scanning): false-negative appears **6 times in the entire 155-subject bundle and zero times** in the three checker techniques; wall 2 states that recall failures are invisible and forgiven and uses it to deprioritise recall, when invisibility is the reason recall needs a deliberate instrument. **The apply step refuted the run's own first technique**: on a real baseline (230/982 unreachable, 23.4%, frozen a week earlier) the distribution test fired on 7 clusters at 2.4-4.3x and all 7 were genuine dead code - dead code arrives in whole features, so both hypotheses predict the same distribution; clustering is a sampler and a referrer check is the discriminator, folded in before commit. Second apply `better` on a **zero**: 11/11 shadowing constructs detected from the maintainer doc's own enumeration, converting an unmeasurable baseline property into a measured negative. 0 of 3 fetches, as the class predicted. Ingest returned 380 words (the landing page); the entire yield came from `.agents/`, the blog and one reporter source file - a run that skipped the clone would have mined an advertisement. |
| 2026-08-31 | `github:TkDodo/pacer` @ `e5c2b53` | **vendor repository (open engine)** - hand-written guides are the source, not the README: 836-word landing page against 29,268 words of authored guides (the 87,063-word `reference/` tree is generated and was excluded from the count on purpose), 23,059 LOC core / 12,632 LOC tests | 836 landing / 29268 authored guides | 12 | 1 | 0 | 8 | 2 untriaged | [[2026-08-31-tkdodo-pacer]] - **one amendment, and eight of twelve candidates lost to the corpus.** 0 of 3 fetches, eleventh consecutive zero-fetch run on a source carrying its own primary material. Expected yield was called low before the triage table and was low: this source lands in three subjects already forged deeply (`admission-queue` 10t/8a, `retry-backoff` 7t/4a, `rate-limiting` 10t/5a), and the corpus beat it on jitter, on total-time retry budgets, on cooperative cancellation, on window families, and - most sharply - on `Infinity`-as-default, where `depth-bounds-and-shed` had already written *a bound whose degenerate value switches it off has quietly made 'off' the easiest configuration to ship* and the source's batcher ships all three flush triggers defaulted to never. The landing came from **the denial hunt**: `priority-and-fairness` states *priority orders the wait; it does not skip the gate* and forwards the one exception to `depth-bounds-and-shed`, which duly lists reject-by-class among three choosable shed policies. Both describe it as a policy a designer selects. Neither says what makes it **implementable** - that the gate must evaluate class before depth, and that the natural code shape (cheap length comparison first, class derived afterwards for the insertion sort) hands the designer refuse-newest while they believe they configured reject-by-class. That is `gate-sees-target` at the admission gate, and it diverges from the intended policy only at saturation, the one condition the levels were declared for. **The source refuted itself and that is why the pick was kept**: its guide asserts *when using maxSize, lower priority items may be rejected if the queue is full*, and its code checks capacity at `queuer.ts:410` and derives priority at `:419`, with no eviction path in either the sync or async queuer. Its own suites carry 8 priority tests and 7 capacity tests per package and **zero** that construct both. The same intersection was untested in the consuming project, found independently. |
| 2026-08-31 | `2026-agentic-coding-trends-report` (vendor PDF) | **vendor prediction report** - a class the reference does not carry; sibling of *vendor release announcement* and inherits its rule harder: the numbers are the yield, and here sixteen of seventeen pages are future tense, which the strip test cannot touch because there was never a rule in it. **The instrument failed first and failed LARGE**: `research-ingest` reported 13,029 words at exit 0 over `res.text()` on a PDF container - compressed FlateDecode streams counted as whitespace tokens. Hand-verified: **3,418**. A `--min-words` floor cannot catch this class of failure because binary always clears it. Fixed in-run (`scripts/lib/pdf-text.mjs`, font-aware via `/ToUnicode` or every bold run - where this class puts its numbers - returns mojibake) | 13,029 reported / 3,418 real | 9 | 3 | 2 | 3 | 1 untriaged | [[2026-08-31-agentic-coding-trends-report]] - both knowledge landings came from the two places the report cites measurement, and none from a prediction. `oracle-before-gate` on `hitl-approval`: the subject's four mandatory triggers are all properties of *consequence*, `verifiab*` appears **zero times** across its golden path and eleven techniques, and its rubber-stamp model is a volume model whose every cure reduces volume - so it cannot reach the reviewer who reads carefully and still has nothing to check against. The two axes come apart in the quadrant the corpus explicitly exempts: reversible-but-unverifiable work is white space on a correct gate map and is exactly what practitioners refuse to delegate. The amendment to `before-after-outcome-pairing` is the mirror: its step 5 excludes a unit missing from one side to prevent a delta manufactured by coverage change, and **induced scope has the identical shape and the opposite meaning** - absent because it did not exist, not because nobody measured it - so the rule as written deletes the effect and reports the residue. 0 of 3 fetches (twelfth consecutive) |
| 2026-08-31 | `youtube:tGbjIvvYuHE` Boundary, software factory design patterns | **first-party practitioner account / dialogue** - two founders comparing their own production agent fleets; both halves first-party, nothing relayed. Class read before triage, expected yield stated as 1-3 findings in the disagreements, **0 of 3 fetches spent** and corroboration entirely corpus-internal - the class's predicted economics held exactly | 15485 | 12 | 1 | 1 | 4 | [[2026-08-31-boundary-software-factory]] - **one amendment, applied, and the apply step refuted its own first number.** The asymmetry shape for the third time on `machine-paced-delivery`: `human-gate-capacity` models the rubber stamp as a *rate* phenomenon with four item-counting measures and an ordered lever ladder, `proposal-not-push` gives change *size* one sentence - and `batch-size-thresholds`, two categories away in a measurement subject, already named its top size bucket *approval is a formality*. The corpus owned the missing half and had filed it where the decision subject never looked. Landed as an amendment inside the technique file (so the `techniques:` list stayed untouched, which is what made it safe while **two siblings held the same subject**), plus one golden-path clause under the `content` lock. Carries the discriminator - rising post-merge repair with *low* arrival is the size case - the inverted remedy (this one raises arrival), the split predicate borrowed from `proposal-not-push` (independently mergeable and independently worth merging, because the neighbour already warns splitting is the canonical gameable move), and a correction to a decision rule: per-item slimming is a constant factor for shape work and not for size. **Applied `experiment`/`better` with the unit corrected**, and first application this technique has ever had - discharging a debt the subject note recorded on 2026-08-28. 7,773 changes over 195 days at 39.8/day. Arm B by changed lines gave a clean-looking 1.42x; the control took it away - repair tracks *files touched* monotonically 34% to 92%, and holding files fixed the line gradient vanishes and inverts in the single-file band. A line-based size measure reproduces the files result and confirms itself. Honest bound written into the application: the tree has no human merge gate, so the premise is corroborated and the mechanism unobserved. Four catches (the corpus refuted the source's own no-sandbox justification via `model-output-as-untrusted`), 4 untriaged (2 on subjects siblings held, 2 genuinely homeless), 1 lead. The four-layer stack - the source's most quotable artifact - was deliberately not landed: two vendors describing the market they sell into is a subject authored by a video |
| 2026-08-31 | `github:malisper/pgrust` @ `4c2e49f` | **vendor repository — but the class's usual ratio inverts.** The predicted three parts held for two (marketing surface, dense rules page) and the third did not apply, because nothing is hosted: the engine *is* the repository. What the class does not predict is that this one is 2,480 rendered words of benchmark claims against ~7,500 words of operating documents reading as paid-for failure modes, plus a 1,241-row per-unit catalog whose notes column carries assembly-level attribution and contract statements. It behaves like a **first-party practitioner account in repository form**. Expected yield stated before triage as above the class norm, 3-4, mostly amendments — and that is exactly what landed | 2,480 landing / ~7,500 in-tree operating docs | 13 | 3 | 0 | 3 | 2 | 5 untriaged | [[2026-08-31-pgrust]] — **one technique, two amendments, and all three came from the same hunt: an enumeration claiming completeness that misses a case the same file describes.** `tuning-corpus-disjointness` (measurement-honesty): `instrument-exposure-control`, landed six days earlier in the same subject, closes with "it cannot certify an instrument as uncompromised… there is no self-check" — true for exposure you *suffer*, false for exposure you **cause at build time**, where both sets are files you hold and disjointness is provable in advance. The half worth a document rather than a paragraph is the source's own honesty clause, which nothing in the corpus made: **enforced vs disclosed**. Item overlap is enforceable; shape overlap is not and must not be avoided, because "choosing a training workload *is* choosing which code paths get optimized, and a corpus that exercised nothing the product is used for would produce a worse binary and a less honest number" — so the residual travels with the number as a sentence. `packaged-surface-evaluation`, landed by a sibling the same day from the other side, treats contamination as fully eliminable by separating the surface: right for a package, impossible for a binary. `model-based-oracle` (test-input-generation, forged the same day by a sibling): its disqualifier list says "when the behaviour is the reference… compare against that one rather than writing a third" — correct about cost, silent about reach. A reference cannot be wrong *visibly*, because the artifact was derived from it, so every faithfully reproduced defect produces agreement and agreement is what correctness produces too; `one-authority-per-vocabulary` at its degenerate case. Scope claim must read **no divergence from the reference**, never correct. Remedy written against the mechanism, not the source's vendor claims: make the reference disagree with itself by running the same comparison under a condition it is not invariant to. `suppression-hygiene` (dead-code): the file names **two** rot axes in a sentence that reads as complete — target died, justification died, both properties of the entry as written — and names the third in its own prose without an instrument for it: an entry whose target is alive, whose reason holds, and whose **reach grew**. Corroborated by within-source convergence across three subsystems by different workstreams (a differential oracle's wart list, a lint's exemptions file, a training-corpus quarantine), all three saying suppressed-never-invisible. **Three apply rows, three `better`, 1:1 with landings, and the apply refuted the run's own document before commit**: the new technique covered the fitted corpus and not the fitted *predicate*, found by an arm that scored **11/11 while 9 of 11 payloads passed through the sanitiser unmodified** — a perfect score on inputs it never touched. The lint-ratchet row measured **372 vs 305 files and 19 warnings** hidden behind one correct ignore entry (45% of the walked tree, uncounted), in a tree whose own config records the day that subtraction changed and the gate read it as new debt. The oracle row is `ship 0 (declined: no change warranted)` — that tree already implements the remedy, which is why it could validate it on three real historical cases. **0 of 3 fetches**, thirteenth consecutive zero-fetch run on a source carrying its own primary material; both amendments were corroborated by opening the neighbour files. Two catches (named-panic frontier → `fake-surface-honesty-contract`; vendored-provenance audit → `vendored-fork-ledger` + `vendored-copy-loses-composition`), three leads with return conditions (frozen-surface rename doctrine — no clean home; dual-metric performance gate — `microbenchmark` returns **zero prior art** corpus-wide, a real hole; pinned comparison baselines), five untriaged recorded with anchors |
| 2026-08-31 | `youtube:rPWCYB62wvI` - "Agentic Engineering Operating Level: WHERE to FOCUS your AGENTS?" | **first-party practitioner account (doctrine talk)** - a framework the speaker developed over a career, presented with **no system, no artifact, no number and no n=1**; last third is a course pitch | 7937 | 14 | 2 | 2 | 2 | [[2026-08-31-agentic-operating-level]] - **the class inverts this method's usual economics and that is the reusable finding**: a news roundup is proper nouns and dies on the strip test, so corroboration is spent on survivors; a doctrine talk **strips perfectly** (the operating-level ladder is proper-noun-free by construction) and **corroborates not at all**, so everything cleared the cheap filter and nearly everything died at the expensive one. Carry as a class rule: **for a doctrine talk run corroboration first and the strip test second** - the strip test does no triage work here. Two amendments landed (0 of 3 fetches), both against enumerations that declare their own completeness, and **the source is contradicted on both while having located both**: `descend-an-altitude` as the **fourth** resolution in `oracle-before-gate` ("three honest resolutions" -> four; **narrowing keeps the artifact class and reduces its scope, descending keeps the scope and changes the class**; narrowing an item opaque *in kind* multiplies `no-oracle` while every queue metric improves; a lower verdict must state what it does not cover) - the technique's own four examples all refuse to narrow, which is what carried it; and the **two causes inside a correct model attribution** in `failure-attribution` (the Model row prescribes *a different model* as if the residual had one cause - absent capability fails **graded**, a trained constraint fails **sharp**, and the second reproduces on the next tier; the source's own prescribed repair, teach it, is backwards for the half it discovered). Both applied same-run: `oracle-before-gate` -> personas `experiment` **better** (13 real catalogs; shipped stack green at 262,106 strings while one descent to protected-term survival found 353 candidate violations, 0.3-9.9% spread by locale - and **the instrument's first run was wrong in the flattering direction at 29.6%**, having harvested the glossary's own must-translate callout); `failure-attribution` -> tracklight `simulation` **better** (the rollup's `floor_hit |=` beside an averaged value destroys the graded/sharp distribution in one character, and agreement is computed per-candidate-overall so nothing recovers it). 2 catches, 7 untriaged with anchors, 2 leads (the altitude ladder as a subject - a **real seam** where four subjects state boundaries that all key on consequence or on a given artifact and none owns the choice of artifact class; trained constraint as a routing input). Ship 0 - triage named no project. 5 siblings live |
| 2026-09-01 | `https://ai.reallusion.com/ai-studio/` | **vendor release announcement** (product landing page, v1.1) — 1,263 words of marketing prose; reliable for its numbers, and the strip test kills the rest | 1263 | 7 | 1 | 0 | 5 | [[2026-09-01-reallusion-ai-studio]] — **five of seven candidates already covered, including the one this run first wrote up as a verified hole.** Operator's premise (this vendor breaks through work we want in game-dev) was right; the conclusion inverted — the corpus holds nearly all of it, and holds it with more care than the vendor page states it. Both operator-picked "real gaps" fell: `reference-shows-only-invariants` **already assumes minting rather than choosing** the identity reference and already enumerates every render-time decision proposed for it, and the proposed cross-bundle inversion of game-production's synthesised-view rule would have licensed exactly the inheritance failure that technique already warns about. `movement-motivation` already owns the typed-camera-channel case whole. **The run's product is a method lesson, not a finding:** an "uncapped, concept-not-proper-noun" absence check returned a genuine zero for previz/blocking-frame authoring, and `scene-grammar-progression` has owned it since run 26 as the **blocking frame** — a neutral 3D stand-in staged in a simple 3D viewer, camera walked to the spot, frame exported, role scoped hard. The queries (`previsualization`, `blocking pass`, `proxy render`, `depth map`) were *industry jargon*, which this corpus strips as thoroughly as a product name, so the zero measured the purity gate rather than the corpus. The catch came from reading the subject's `librarian/` note, not from any grep. Landed: one golden-path **scope clause** on `cinematic-language` (its flat "models read described effects, not numbers" is about the CHANNEL, and its own technique inverts it) + one application carrying the structural fact that a connected project's scene schema answers the law's discriminating question **twice, oppositely, in one type** — numeric clamped overlay positions beside a prose motion field that is "authored here, rendered nowhere". Pattern inversion worth keeping: the documented failure mode is golden paths hedging better than techniques; here the technique hedged and the golden path did not. 3 siblings live, none on media-generation |
| 2026-09-01 | `https://www.youtube.com/watch?v=EChXCPolIDQ` | **practitioner build-walkthrough, tour-half dominant** (sponsored demo of a vendor MCP connector) — 2,837 words, one creator, ~4 sentences of genuine operating half | 2837 | 8 | 1 | 1 | 6 | [[2026-09-01-faceless-channel-claude-code]] — **the class predicted the yield and the corpus won every content row.** Six of eight candidates were verified catches against files opened in full, and the corpus does not merely cover them, it covers them BETTER: the video's headline rule ("lock the style on one image, not a video") is `asset-vs-disposable-render`, whose `use_when` already carries *rehearsing an expensive medium in a cheaper one*, while `delivery-promise-lock` already names the failure the video walks into unnoticed — a cheap probe cannot ratify the dimensions the cheap medium lacks, and the creator specifies "slow push-ins" and "slow pads like a pop-up book" as STYLE and then proofs both on a still frame. **The one landing came from being wrong at triage.** Row 6 was called a `partial` — an amendment to `never-the-account-default` for a provider whose product IS the routing and which therefore exposes no model slot — on a read of the technique's first 45 lines. The full file already contains the case twice: a decision rule (*when the response does not state which model served, treat identity as unverified*) and a *When NOT to use this* clause for a genuinely versionless contract. **An absence established from a truncated FILE, which is the grep failure the method already warns about, applied to `head`.** Verifying the catch is what found the real gap, and it was in the tree rather than the corpus: gravity's `Provenance` had modelled the epistemic status of cost, custody and re-routing, and left identity — the property the subject opens with — a bare required string. Landed as one application (`node--never-the-account-default`) and shipped to gravity as `e3b3f09`. **Currency (row 7) deliberately not inflated into content**: a provider disclosing no identifier at all is now a marketed product category rather than a hypothetical, which is why the new enum carries `undisclosed` as a third value instead of being a two-value type — that is the whole of what this source contributed to the standard. 0 of 3 fetches spent; corroboration was real code in a connected project, which the class predicted. Two siblings live at claim, neither holding my subject; one of them, however, had uncommitted work in `cinematic-language` and `generative-artifact-gating`, so **`index.json` and `catalog.json` were regenerated and deliberately left uncommitted** rather than baking a neighbour's in-flight applications into a hash under this run's name |
| 2026-09-01 | `web:github.com/langchain-ai/openwiki` @ `v0.4.0..v0.5.0` | **vendor repository, re-scanned as a version delta** (132 files, +10,542/-2,295, 7 days) | 4,673 (README, unmined) / 15,155 (5 changed operating docs) | 14 | 2 | 2 | 0 | [[2026-09-01-openwiki-v050]] - **the run where the source moved to our position.** Second scan of a repo first mined 2026-08-27; the prior note was the run's instrument, not its context. A delta's unique product is **reversals**, and this delta contains ours: on 2026-08-27 this corpus read v0.4.0's claim-reconciliation contract, accepted its set-level shape and rejected **omission as the retraction signal** into `dated-corrections`. v0.5.0 reverses exactly that - explicit `retractedClaimIds`, an omitted healthy claim retained, an omitted *flagged* claim throwing `invalid_input`, verified in `page-jobs.ts:402` rather than in the prose. **The finding is not the reversal but that theirs is cheaper than ours**: refusing every omission forces a full re-declaration each pass (the round-trip our own `repair-rides-the-open-page` exists to avoid), while binding omission to the non-destructive reading and reserving the refusal for already-flagged claims gets both. Three-way, not two-way, and the obligation set is enumerated by the **system**, not the worker. Landed 1 amendment (`dated-corrections`: three-way omission, idempotent-withdrawal/strict-confirmation, result floor), 1 technique (`test-harness/gate-scope-is-not-report-scope`), 1 golden-path amendment (its lane-health enumeration went two -> three silent failure modes; the third is a failure of the *denominator*). **2 apply rows, both `better`, 2 project commits.** Coverage A/B on two Node trees: 5 files/95.74% -> 1,171/4.32%, and 175 -> 1,529 files at 68.98% with **490 at 0% never previously visible**. Registry A/B on `signals-collect.mjs`: a 30d window + whole-file replace makes expiry and never-witnessed byte-identical; 16 of 87 (bundle/subject) pairs vanish with no record when the boundary is moved into real data, and **the first genuine silent drop lands 2026-09-22**. 0 of 3 fetches - sixteenth consecutive. **The untriaged table paid for the first time measurably**: prior rows 12 and 14 are both confirmed real by v0.5.0 fixes (#744, #743), which nobody had to re-derive. |
| 2026-09-01 | `github:gmh5225/awesome-game-security` @ `36d01f8` | **reference index, hybrid** — a 4,017-link README wrapped in an LLM curation pipeline (discover, fork-and-flatten, summarise a 200 KB prefix, translate, compile a wiki on a 30-min cron); the README is a bibliography, the pipeline is a practitioner build in repo form and its own `log.md`/`.state.json` are its measurement. **No waves run, on purpose**: every domain term returns a total empty on concept vocabulary and game-production has no security category, so all 3,871 references score `new-subject`; ranked at section granularity instead, all recorded unread. **Re-scan when** `wiki/log.md` gets its first `lint`/`skill-sync` entry, or the orchestrator gains a page-length check, or 2026-12-01 | 30,704 README / ~381,000 in-tree md / 10,851 lines scripts+workflows | 12 | 1 | 4 | 4 | 6 untriaged | [[2026-09-01-awesome-game-security]] — **one amendment, from the tree's own measurement of a failure the corpus names from the other side.** `prose-rule-drift` owns "a rule written and never mechanised" and "an instrument nobody invokes"; the converse it did not own is **an artifact rule enforced at the edit**: a per-edit cap (≤8 files, "short cite") obeyed by ~3,950 ingests while the page cap (~200 lines) sat at 13.6x, the lint mode that alone read the sum starved by an ingest queue that a 5-per-scan discovery cap keeps non-empty by construction (0 lint runs in 9 days beside 3,950 ingests — `quiet-window-maintenance`'s "a log that only records successes" sentence, measured). The violation is composed entirely of compliant actions. **Convergence arrived from a connected project unasked**: its shared session-memory file caps appends at 2 lines and the file at ~200 in one paragraph; a probe over 59 commits found the append cap obeyed by all 26 commits since the file crossed 200 on 2026-08-19 and the artifact over at every one. Applied `code`/`better` — `ab-paired`, same history, two checks (per-edit 0/26, artifact 26/26), calibrated on a known-clean backlog (188/300) and the source's known-over page (2,728/200) before the arms were read — and **shipped** (`49f05f22`, pof: artifact-reading check with the document's own prune remedy adjacent, wired at the end of the per-CLI gate, one prune to cap). 4 catches (`quiet-window-maintenance`, `three-state-outcomes` gating collapse licensed and used, `mirror-indexes` direction contract, `prose-rule-drift`'s own uninvoked-instrument row for the lint mode), 4 leads (game-security as a new game-production **category**, with the section ranking as the wave plan; a citation-tier rule — a resolvable citation to a projection of a summary of a prefix of a clone passes "verify every citation resolves"; archive-at-admission plus a liveness checker this registry lacks; self-fed demand is not a quiet gauge), 6 untriaged with anchors. 0 of 3 fetches. Blobless sparse clone required: a plain shallow clone of this "list" pulled 864 MB of flattened archives and did not finish. 2 siblings at claim, 3 by landing; `index.json`/`catalog.json` regenerated and left uncommitted (a sibling's `ordered-lane-blocking` is in the working tree and not in HEAD) |
| 2026-09-01 | `github:matrix-org/matrix-rust-sdk` @ `37a0ae4` | **vendor repository** - a company's client SDK over a federated protocol; the ingest returned 597 landing words and an **empty body**, so the clone was the source. The dense surface was not `docs/` but the **191 per-PR changelog fragments** in `changelog.d/` and three modules' rustdoc; three of four landings came from one `*.fixed.md` each. Expected yield stated before triage as 2-4, mostly amendments. **Re-scan when** the main crate's CHANGELOG.md gains a released section carrying PRs 6843 and 6869, or 8 weeks elapse (2026-10-27), or a second source states a serialization-snapshot rule for persisted types (untriaged #13) | 597 landing / ~30,200 in-tree md + 191 fragments | 20 | 4 | 4 | 4 | 8 untriaged | [[2026-09-01-matrix-rust-sdk]] - **one technique from a rule inversion, three amendments, four applied, one shipped.** `ordered-lane-blocking` (delivery-guarantees): the lifecycle table said what becomes of one dead-lettered event and nothing about the events queued behind it, and `optimistic-write-path` said a failed predecessor must not fail its successors; the source paid for that default in a conversation queue (out-of-order delivery). The discriminator - an entity lane skips a dead head, a stream lane blocks on it; would item N still mean what its author meant if N-1 vanished - is stated on both sides. `cross-process-exclusion` gained the lease generation's **second reader** (dirty on regain; one flag consumed once across every derived cache - the source reloaded one cache and left the rest), applied `not-better` against a fleet lease whose loops re-read the store per tick, with the condition written into the technique. `journal-and-durability-modes` gained "name the set from the engine": the source's `.wal`-for-`-wal` bug; a fleet tree used `with_extension("db-wal")`, correct for 2 of 6 store names by naming luck; **shipped** (`0f4a3b468`, personas, pathspec, not pushed) with the test the amendment prescribes. 0 of 3 fetches. Siblings 3; two golden paths with foreign WIP left untouched, landings went into technique files. |
| 2026-09-02 | `github:k2-fsa/sherpa-onnx` - "sherpa-onnx - on-device speech recognition, synthesis, VAD, keyword spotting and diarization" | **vendor repository** (open speech runtime over a fixed inference engine; one C++ core behind ~15 bindings) - landing 2,516 / ~38,300 in-tree md, of which 10,422 is a per-PR CHANGELOG; the yield came from the config headers, their validators and one recognizer's silence guard, not from any markdown | 2516 / 38300 | 15 | 3 | 3 | 4 | [[2026-09-02-sherpa-onnx]] - **declared focus applied (changelog fragments first): the sweep produced one landing's TRIGGER and no landing's content** - "Fix Qwen3-ASR hallucinating text on silent audio with hotwords/language set" named the hazard, the C++ that short-circuits before prompt tokens are built supplied the rule, and the other 10,400 words are packaging. NEW TECHNIQUE `decode-time-vocabulary-biasing` closes a missing stage: `spoken-intent-parsing` matches the known vocabulary AFTER decoding and nothing asked whether it could reach the engine (total empty on the concept). Two mechanisms with opposite failure physics - score-boost can only promote text the acoustics produced (needs a search that keeps alternatives; greedy cannot be biased), prompt biasing can produce text the audio did not - and the gate ordering that follows: silence check, then prompt, then decode. `stt-pipeline` amended twice: (1) **the silence threshold is not one number** - the enumeration hunt again: "trailing silence past a threshold" is one constant in the corpus and three content-conditioned rules plus a hard cap in every mature runtime, and the cap applies to explicit endpointing too (a push-to-talk pipeline has no silence rule and so has no cap, against a fixed engine timeout); VAD hysteresis and the lowered bar on a long segment folded in; (2) **the engine's empty is not always empty on the wire** - a bracketed no-speech marker printed with exit 0. **That second amendment came from arm A of the A/B, not from the source**: the connected tree's own engine, run at the product's flags on two near-silent captures, returned `[BLANK_AUDIO]` as a one-word transcript and the guard written for silence had never fired. Applied 3/3 to one tree: `not-better` experiment (n=5, ab-paired, the product's engine + catalog model: the eleven-word decision grammar is already recognized without help, and the prompt turned both silent captures into `you`) - the technique's "bias only where a confusion is measured" was written from that seam; `code` better, SHIPPED (marker stripped; crate gate red on an unrelated build-script permission, arms run as a rustc reduction); `simulation` better (no hold cap vs a 120 s timeout; three cases, one measured). 4 catches, 3 leads (word-timestamp accuracy vs forced alignment as its own measurement; diarization pipeline; enhancement with an attenuation cap), 5 untriaged with anchors. **0 of 3 fetches.** `rescan_when:` a released changelog section touching the VAD long-segment rule or the endpoint rules; or a second source measuring prompt hallucination on silence; or 8 weeks |
| 2026-09-02 | `github:getsentry/self-hosted` @ `c119983` | **vendor repository, packaging flavour** - a company's compose-and-install distribution of its own images; no `docs/`, a five-line README, and the yield in the install step scripts (each carrying the issue number of the failure it prevents), the compose anchors and the tests. **Re-scan when** the 26.9.0 changelog section lands (monthly, the 15th); or the file-healthcheck anchor changes shape; or a consumer runtime documents whether its heartbeat file is written on idle iterations; or 2026-11-01 | 308 landing / ~10,966 in-tree md + 2,242 lines installer shell + 1,727 lines tests | 16 | 4 | 2 | 4 | [[2026-09-02-sentry-self-hosted]] - **four amendments, none a technique, and the strongest one was completed by a fleet tree rather than by the source.** `probe-design` gained the **consuming probe**: a portless worker mints a token from its own loop and the probe *deletes* it - a stale pulse cannot pass twice, no cadence lives in the checker, and the pulse is honest exactly when the work loop writes it. The corpus held three heartbeat techniques (ownership lease, expectation check, pulse ranking) and none the consuming direction; tracklight's runner holds the *ownership* half built correctly, with the opposite coupling rule in its own comment, and no progress half at all - simulation, three cases from the tree, better, filed as its next change. `installer-authoring` gained the missing transition - **configuration the operator owns that the upgrade must change**: generate once, three states (shipped default -> migrate, migrated -> pass, custom -> report), consent with the flag printed, refusal graded by what the change prevents; discriminator against `idempotent-steps` stated (who owns the state decides report versus halt). Applied to the registry's own link script and came back **not-better**, correctly: the block it rewrites is a machine-owned region (0 operator lines inside markers across 7 checkouts), and the amendment now draws that boundary. `flake-lifecycle` gained **same code means same inputs** (a lane over floating inputs measures the world; route its transitions to a currency ledger) - **unapplied**, no fleet lane floats its inputs, and both scheduled Rust lanes pin the toolchain with the amendment's own discriminator in a comment. `update-automation-review` gained **release age as a tier input** (minimum age, security exempt; the operated-major ceiling for a distribution's stateful components; event expiry as an acceptable exception form). Experiment over 67 bot proposals in four fleet repos: 8 superseded, 4 inside three days, **1 real** after calibration removed grouped-proposal rebuilds - unmeasurable for the supply-chain half, instrument named. 4 catches, 2 leads (drain-before-upgrade converges with the matrix note's untriaged #13 from the other side), 6 untriaged with anchors. 1 of 3 fetches. 0 siblings at claim, 1 by landing (voice-io, no overlap); index and catalog regenerated and left uncommitted over its WIP |
| 2026-09-02 | `github:AmanVarshney01/create-better-t-stack` - "create-better-t-stack - a CLI for scaffolding end-to-end type-safe TypeScript projects" | **first-party practitioner account** in repository form (single-maintainer scaffolding CLI; ADR, domain glossary, openspec design and a 13-row upstream-defect ledger are the first-party half; the README is the ad) | 669 landing / 32,182 in-tree md excl. templates, plus the 717-line validator and the 400-line test oracle | 15 | 2 | 2 | 2 | [[2026-09-02-create-better-t-stack]] - two amendments, both from documents the README never mentions. **A** `fallback-retirement-condition` gains the pinned-upstream lane: evidence tiers ending in "a fix merged on upstream main is not evidence for removing anything", a three-part reaper (published artifact + pin moved to exactly it + minted case rerun), every row re-runs on a pin move because the accepted version can itself regress, a **disproved-claims ledger** (second sighting of the firstmate defect-record lead, landed as convergence), and policy-vs-workaround labelling with the prerelease-ordering hazard as the case (a caret over a prerelease admits any same-tuple prerelease, ordered lexically, so a stray `-test` tag outranks every `-beta.N`). **B** `inherited-default-override` gains the third default source - a sibling key - and the provenance rule from the validator's provided-flags set: a cross-key rule fires only when every key it names was decided; a derived operand means re-derive silently; a machine caller gets no derivation at all. 2 applications, both `simulation` / `better` / `structural-only` on personas: the CLI-floor ledger already separates decisive / not-reachable / not-adopted and carries no removal condition on any row, with its own comments recording the floor sitting inside a broken window twice; and a resume path that replays a constant where the first run derived a value. Ship 0 (signature change + live sibling in the tree). Full-matrix bound computed from the tree: 52,254,720 normalized cases, refused unsharded. 7 untriaged with anchors, the strongest being the oracle that predicts the rejecting *rule* and treats an unclassifiable rejection as a finding. 2 live siblings at claim; neither held either home |
| 2026-09-02 | `github:cjpais/Handy` @ `fbd4e15` | **practitioner build-walkthrough, repo form** - an offline desktop dictation tool; nine thin markdown files, and the yield in **module-level doc comments that open with the issue each rule was paid for** (#502, #1578, #1999, #1539, #1817), which is the changelog-fragment surface stored in the source. **Re-scan when** the receipt-sequenced paste leaves its debug gate (a release note makes it default, carrying field-measured timeout/quiet values), or a second source states the external-trigger activation rule (lead #4 -> amendment), or 8 weeks elapse (2026-10-28) | 3,369 landing / 9,108 in-tree md / ~28,300 lines backend source | 15 | 2 | 0 | 1 | 2 | 10 untriaged | [[2026-09-02-handy]] - **one amendment, one technique, both from source comments; one applied and shipped, one unapplied by construction.** `atomic-downloads` gained "the resume contract, check by check": five protocol-level checks the technique had compressed into "validated as belonging to the same remote artifact", each named by its corruption and each with a test against a local socket server; corroborated against RFC 9110 (1 fetch). **Applied** `experiment`/`ab-paired`/`better` on politicas' contract-dump harvester, which holds the registry index's per-month byte size and compared it to nothing: 6 replayed cases, silent-wrong 1/6 -> 0/6. Two structural facts landed in the technique as a caution: the runtime's HTTP client already fails a short body and stops at the advertised length (two of five checks made by the transport, so a check-1 test can pass for a reason the app did not build), and the hole no transport closes is a server that advertises AND delivers a smaller total - the catalog comparison alone. **Shipped** `73a9443` (politicas, master, pathspec, not pushed; the project's doc-sync hook required the coupled doc updated in the same change). Second stack for sidecar-provisioning's standing single-stack debt. `voice-io/transcript-handoff-receipts` (13th technique): stt-pipeline's stage table ended at "awaiting disposition" and nothing owned delivery into a foreign application through the shared clipboard - a timed restore races the target's event loop and pastes the OLD clipboard on a loss; restore on the OS read receipt, post-chord receipts only, ownership unchanged, quiet after the last receipt, bounded wait whose failure is "lingers" never "stale lands"; unapplied, no fleet seam. 1 lead (activation edges: external triggers never debounced, busy-window parity), 2 catches, 10 untriaged with anchors (secure-input shadow fallback with three grades per binding; guarded post-decode fuzzy correction as a discriminator candidate for the sibling's decode-time technique; a language-detection calibration with protocol). Declared foci: no re-scan condition fired (two checked); ten notes' leads read, none fired; the fragment-sweep focus had no changelog to sweep and found its equivalent in the module docs. 3 siblings at claim, 6 by Phase 9; the voice-io sibling committed before this run's Phase 7 |
| 2026-09-02 | `github:Project-MONAI/MONAI` @ `02201b8` | **vendor repository** (consortium medical-imaging framework over a hosted tensor engine) - the yield came from CONTRIBUTING's deprecation policy plus the decorator that enforces it, the release notes' four load-path advisories, and a test helper that probes the accelerator's effective precision; one monolithic CHANGELOG, no per-PR fragments (focus did not bind). **Re-scan when** the 1.7.0 CHANGELOG section lands (read its Fixed list for advisories only), or a managed project grows a geometric transform chain (leads 2+4 become a spec), or 6 months elapse | 779 landing / ~40,660 in-tree | 15 | 3 (technique release-pipeline `deprecation-by-version-arithmetic`; technique supply-chain `unsafe-deserialization-off-by-default`; amendment test-harness `platform-quirk-absorption`) - applied 3 (1c/1e/1s), shipped 1 (pof `9788290e`) | 3 | 0 (9 untriaged, unverified) | [2026-09-02-monai](2026-09-02-monai.md) |
| 2026-09-02 | `github:bytedance/deer-flow` @ `a5ec7f2` | **vendor repository** - a company's open long-horizon agent harness over hosted models; the 18k-word landing page is its advertisement and the yield sat in eight per-package module guides (3,000-5,200 words each; one holds the whole delegated-work verifier as a single 4,800-word paragraph). **Re-scan when** the Unreleased changelog becomes a released 2.1.0 section; or the report-contract judge layer lands; or a second source states the persistent-shell provenance rule; or 2026-10-28 | 18,157 landing / ~225,000 in-tree md | 22 | 4 | 3 | 4 | [[2026-09-02-deer-flow]] - **two techniques, two amendments, 3 applied (2 better, 1 not-better), 1 unapplied, 0 shipped.** NEW `completion-claim-verification` (fleet-orchestration): the stage between dispatch and harvest the corpus named as a target and never as a mechanism - runtime receipts the report must cite, decidable leaves checked parent-side with UNVERIFIED never spelled as pass, and evidence provenance (a test run in the worker's own shell proves nothing; unknown session semantics fails closed). `research-map` returned a total empty on the concept because the subject speaks session/member/worker. NEW `write-freshness-gate` (mcp-tools): proof of a fresh read as a hash on the read message so context loss is proof loss, writes never refresh it, check-and-write one critical section per path; unapplied, no fleet tree exposes its own write tool. `lease-renewal` gained absent-versus-lost, take-versus-claim and the held destroying marker; applied not-better to a single-store attempt-fenced job lease, condition landed. `decay-and-forgetting` gained "a review window is not an expiry" - a **contradicted pick** resolved by naming the field: expiry is the claim's, review deadline is the store's; applied better to a store whose synthesize restarts the clock at merge. Declared foci applied: two re-scan rows checked (neither fired), ten notes' lead conditions read (none fired), changelog swept first and - for the second run running - it produced every landing's trigger and no landing's content. 0 of 3 fetches. 6 siblings at claim, none on the four subjects; index regenerated under lock and left uncommitted over a sibling's WIP. 4 catches, 3 leads, 11 untriaged with anchors. |
| 2026-09-02 | `github:dora-rs/dora` @ `cb561cc` | **vendor repository** (open-source dataflow middleware; the yield sat in the docs' fault-tolerance page with its per-timer arming rules, two audit reports with per-finding verification flags, a QA proof-of-concept report that names which gate caught which bug, and a paragraph-per-entry Unreleased changelog; the README is a benchmark claim the tree's own audit lists as unsubstantiated). **Re-scan when** the Unreleased changelog becomes a released 1.0 with the coordinator-reconnect behaviour resolved (audit High #2); or `docs/plan-fault-injection.md`'s 8 scenarios are marked implemented (a test-input-generation source); or 8 weeks elapse | 3,732 landing / ~229,000 in-tree md | 17 | 4 (all amendments: `liveness-and-heartbeats` arm-at-first-contact + startup deadline + respawn reset + continuity-only staleness clocks; `persistence-and-migration` encoding-decides-shape-change + a version that covered two shapes is retired; `gate-liveness` assert the oracle population + smallest controlled experiment; `declared-deviation-register` key by identity never position) - applied 4 (0c/2e/2s, all better), shipped 0 | 3 | 7 (6 untriaged with anchors and a promoting question each) | [2026-09-02-dora](2026-09-02-dora.md) |
| 2026-09-02 | `github:openbao/openbao` @ `6b5f82e` | **vendor repository** - a foundation-governed fork of a secrets manager; ingest returned 1,086 landing words, the clone was the source. Per the declared focus the **424 per-PR changelog fragments were swept first** and produced 4 of 5 landings and all 6 catches at ~2% of the words; the 465,000-word docs tree produced one lead. The 46 `security` fragments, each with an advisory id, were the densest surface this skill has mined. Expected yield stated before triage: 2-4, mostly amendments. **Re-scan when** CHANGELOG.md gains a released section carrying PRs 3555 and 3913, or the invalidation dispatcher's default branch turns fatal for non-system keys, or 2026-10-28, or a fleet project adopts a read-serving replica | 1,086 landing / ~465,100 docs + 9,172 fragments + 13,220 CHANGELOG + 28 lint rules | 20 | 5 | 3 | 6 | 6 untriaged | [[2026-09-02-openbao]] - **five amendments, five applied (0 code / 2 experiment / 3 simulation), one shipped, zero new techniques, zero fetches.** (1) `scope-design`: four advisories in one year were one shape - a guard on the raw spelling beside a resolver on the canonical one, and a replay cache keyed on the unfolded input; "exact equality on canonical forms" never said where the form is made. Applied to the registry's own run board: SAME SOURCE missed 3 of 4 spellings of one repo; **shipped** (`run-board.mjs`, source-level fold, 4/4 with no false collision). (2) `failure-direction`: the half-success (an auth block beside an error minted a token, CVE-2026-46405, fixed in two lines and a lint rule) and the wrong minter, added to an enumeration that had only permissive *failures*; applied to a dashboard fetch hook `not-better` with the display-value condition landed. (3) `topology-declaration`: the mirror's derived state - twelve one-per-cache bugs on read-serving standbys over a year, ending in "restart on an unroutable key"; the corpus had one grep hit on standbys in 3,790 files; applied `better` to a per-process cache with in-process invalidation beside a cross-process TTL. (4) `lease-renewal`: renewal must not queue behind the work - and **the source's remedy inverted on a single-writer engine**, measured with a fleet project's own parameters (dedicated connection failed at 5.5 s where the shared writer waited 6 s and succeeded); the `not-better` row carries the engine tell. (5) `workspace-ancestry-isolation`: the author's chair - the first instruction file addressed to uninvited agents, with a disclosure canary; the discriminator with injection is who owns the tree; applied to the registry `not-better` (precondition absent). 6 catches incl. the source moving to our position on expiry warnings (30 days -> 15% of validity). 3 leads (count-based compaction resets with the process; a threat model that enumerates its out-of-scope incl. fingerprinting; native dependencies as auto-restarting plugins). 5 siblings at claim; `index.json`/`catalog.json` regenerated and left uncommitted (sibling WIP in the tree) |
| 2026-09-02 | `github:Project-MONAI/MONAI` @ `02201b8` (**second pass, intake 2.0.0**) | **vendor repository, forge-shaped** - the design read over the same commit the 1.6.0 row mined twelve hours earlier: eleven design decisions reconstructed from the type docstrings, the design pages and the release-note rationale; **routing count 9** (metadata-carrying tensors, traceable/invertible transforms, lazy geometric composition, array/dict duality, deterministic-prefix caching, the bundle's object-graph config and required properties, windowed inference, AutoML-as-bundle-generation, the federated client), 1 partial, 1 catch. **Handed off to /forge**: Phase 0 scouts dispatched, report banked at `librarian/handoffs/2026-09-02-monai.md`. The 1.6.0 note's three homeless leads are three of the nine. **Re-scan when** the forge run over the handoff completes, or the 1.7.0 CHANGELOG lands | 779 landing / ~40,660 in-tree + type docstrings | 11 design | 0 techniques (by design) + 2 source-tree applications (`python--guarded-singleton-accessor`, `python--spec-ships-with-artifact`); applied 1 (task, pof branch `intake-monai-0902-v2`), shipped 1 (branch, not pushed) | 0 new (3 prior leads closed into the handoff) | 1 (D9) | [2026-09-02-monai-v2](2026-09-02-monai-v2.md) |
| 2026-09-02 | `github:openbao/openbao` @ `6b5f82e` (**re-run under intake 2.0.0**, same commit as the 1.6.0 row above it) | **vendor repository, design read** - the replication the 2.0.0 lessons entry owed. The 1.6.0 run never opened `website/content/community/rfcs/`: 37 accepted design documents, 57,188 words. Three parallel readers reconstructed 17 decisions into a 7-entry design record; **6 of 7 read `corpus: NONE`**, so the source routed to `/forge`. Four Phase 0 scouts over the clone with the record as brief -> **9 NEW subjects, 10 EXTENDS, 11 proposed laws**, placement constraint stated (security holds 8 subjects directly; a tenth top-level category is the cheap placement). **Re-scan when** the handoff is forged, a new RFC lands in that directory, or 2026-10-28 | 1,086 landing / 57,188 RFCs + ~465,100 docs | 17 decisions / 7 entries | 0 techniques (handed off) | 0 | 1 (entry D, `encryption-at-rest`) | 0 untriaged | [[2026-09-02-openbao-v2]] -> [[../handoffs/2026-09-02-openbao]] - **same tree, same day, two methods, measured**: read as claims it produced 5 amendments in 5 existing techniques; read as decisions it produced a category-sized gap. One source-tree application written (`go--encryption-at-rest`, the tree's rotation-by-layer and upgrade-key extension of the standard). One record correction from the scouts: the client-carried consistency index is an accepted RFC not yet landed. 0 of 3 fetches. 4 siblings at claim (all 2.0.0 re-runs); gate red at commit on a sibling's two source-tree applications, not this run's |
| 2026-09-02 | `github:Yeachan-Heo/oh-my-claudecode` @ `e9e8fa3` | **practitioner build-walkthrough, repository form** - a community agent harness for one coding CLI; ingest 4,573 landing words, the clone was the source (~230,000 in-tree md; ~123,000 read by four sweep workers; the fifth, over the hook source, died on a rate limit). Read as design (v2): nine decisions, **three `corpus: NONE` with one home**, the XL trigger fired and one forge worker landed `llm-agent/orchestration/session-continuation` (7 techniques, 3 source-tree applications) in-session. Three amendments beside it (deprecation-by-version-arithmetic, fixed-policy-amendable-plan, advancement-evidence-fields). Rescan when the source publishes a graded benchmark, a first alias retirement under its four-condition gate, or a second harness publishes a hook fail-mode registry. | 4,573 / ~230,000 | 27 | 11 | 4 | 8 | [note](./2026-09-02-oh-my-claudecode.md) |
| 2026-09-02 | `github:bytedance/deer-flow` @ `08b27aef` (v2 back half) | **vendor repository**, mined under intake 2.0.0: the front half ([[2026-09-02-deer-flow-v2-replication]]) read the design and counted 4 unmodelled decisions; this run executed the handoff. **Re-scan when** the 1.6.0 row's condition fires, or a third custody decision appears in the memory/skills/channels/config guides (Q1) | 17,479 landing / ~330,000 in-tree md (front half); 15,602 re-read for review | 7 design entries (inherited) | 1 subject + 6 techniques | 1 new lead + 3 carried | 3 catches -> 4 source-tree applications | [[2026-09-02-deer-flow-v2]] - **first subject landed by intake in thirteen repository runs.** `agent-runtime-assembly` (llm-agent/runtime-and-io): one forge worker on the XL spec, expert-first then reconciled; six techniques, three source-tree applications by the worker, four more in neighbouring subjects from the design record's catches, four neighbour boundary sentences under the content lock. The worker decided the spec's four open questions (custody stays; two-layer authz is a sentence; no cross-bundle sharing; two fingerprints, boundary written). **Applied 2 of 6, both on seams chosen to falsify**: `bounded-projection-of-external-work` against two paired peers with no shared store - custody held with truth split by ownership, delivery did not (a peer's model-authored report entered the originator's System-role memory unfenced) - `code`, `ab-paired` 3 fixtures 3 -> 0, **shipped** `502abdc45` (personas, master, not pushed); `semantic-hook-placement` against a closed single-author stage enum, `not-better`, condition landed. Four unapplied with return conditions. 0 fetches. 5 siblings; a mid-write session-limit stop on the worker resumed with context intact. |
| 2026-09-02 | `github:volcengine/OpenViking` @ `85b4923` | **vendor repository, design-deep** - a cloud vendor's open-source agent context database (memory + knowledge + skills under one URI tree with per-directory summary tiers); 20 design documents, 16 concept pages, six dated bug-fix specs, a 6,932-word changelog read first; README is a benchmark ad whose judge prompts and per-arm budgets live only in `benchmark/`. **Re-scan when** the Unreleased changelog ships the lossless peer-id migration, or the concept docs stop describing pre-policy parent bubbling as current (they lag the code at this commit), or 8 weeks elapse (2026-10-28) | 1,779 landing / ~323,000 in-tree docs / ~23,700 benchmark md | 24 | 9 | 4 | 6 | 9 untriaged | [[2026-09-02-openviking]] - **first fresh 2.0.0 run and the first repository run to reach a subject by the count alone.** Design record 12 entries, **5 NONE**, three sharing one home -> `context-hierarchy` (4 techniques + 1 application, forged in-session, spec EXECUTED with three drafter overrides: the dominance ratio is declared and applied nowhere; the docs' "current" bubbling is the code's previous state). Two NONE entries -> `agent-memory` techniques (`owner-and-counterpart-scope`, `read-set-bounded-links`); two boundary cases -> amendments (`lane-reconciliation`: the invariant chooses the write order per operation; `baseline-ladder`: the judge and the per-arm budget travel with the rung); five applications against the source tree. **Applied 5: 0c/0e/3s/1t**, every seam chosen to falsify - three personas simulations all `not-better` with their conditions landed; one registry task (`docs/tasks/2026-09-02-context-hierarchy-descent.md`, branch `intake-openviking-0902`, first step `aa61f50`: a 14-row labeled query set, flat map baseline 8/14); `baseline-ladder` unapplied (no managed project runs a model-judged eval). Shipped 1 (registry branch, not pushed). Two worker deaths on rate limits: the code reader (covered by director greps) and the forge worker (resumed with context). Fetches 3 of 3, all by the forge worker. |
| 2026-09-02 | `github:dora-rs/dora` @ `bdd1516` (**v2 re-run** of the same-day 1.6.0 row) | **vendor repository read as a SYSTEM** under intake 2.0.0: design record of 8 decisions with forces and rejected alternatives; routing count **4 NONE of 7** -> forge handoff, executed in-session as a harvest into software-engineering (3 scouts, 5 forgers, branch `forge/dora-process-graph` @ `15634167`, not merged). **Re-scan when** the branch is merged and its applications are re-opened against a newer commit; or the tier-2 handoff (`librarian/handoffs/2026-09-02-dora-tier2.md`) is dispatched; or 8 weeks elapse | 3,732 landing / ~229,000 in-tree md; design read over ~39,000 words of architecture and plan documents | 8 design candidates | **5 subjects / 30 techniques / 10 source-tree applications on the branch** + 3 source-tree applications on main (streaming-output, pipeline-dag, audit-logging); 0 amendments | 0 new (v1's 3 stand) | 0 new | [2026-09-02-dora-v2](2026-09-02-dora-v2.md) - **first forge handoff the skill has produced.** The 1.6.0 pass over the same tree yielded four paragraphs; the 2.0.0 pass yielded a subcategory, because the unit of extraction was a decision rather than a sentence. Scouts corrected the design record on five points (flush retains correlated events; queue policy honoured on both transports; binding parity is two of four; the fault-tolerance page's recovery section is stale against code; a route-probe handshake with a freeze is the mechanism that makes "one path" honest). Five proposed laws returned, none added; two recur across three subjects. Apply: 5 unapplied - no fleet project runs a process graph. One application-naming limit surfaced (one application per stack per technique) and one method budget (golden-path length) was overridden with a corpus argument. |
| 2026-09-02 | `github:blader/Claudeception` @ `62dbb91` | **practitioner build-walkthrough, repository form** - a single-author prompt-only skill for one coding harness that extracts skills from sessions; 8,180 in-tree words read whole, 16 commits; the commit log carried both paid-for failures (duplicates -> a contributed dedupe step; under-firing -> an every-prompt hook) and the body carried neither. Design read: 7 entries, **0 NONE**, two boundary cases. **Re-scan when** the source publishes an activation measurement, or the template gains an observation count, or a second extraction tool ships the same dedupe table | 987 landing / 8,180 in-tree | 11 (7 design + 4 claim) | 2 amendments + 3 applications | 2 | 6 | [[2026-09-02-claudeception]] - `enforcement-demotion` gains "a hook that prints prose is not a demotion" (fleet replay: 1,631 human turns, 122 fires, every-prompt delivery 15-25x the words); `procedure-promotion` gains "when the promoted store is the only durable store" (a trigger search found the one true duplicate in a real 24-item store; the technique's suggested pairwise vocabulary lint ranked it 3rd of 5). Applied 2: 1 experiment `better`, 1 code `better` (registry harness memory store, two files on one trigger merged). 0 of 3 fetches. 1 sibling at claim, 3 by commit; index left uncommitted because it absorbed a sibling's unlanded subject. |
| 2026-09-02 | `github:garrytan/gstack` @ `0d1bd56` - "gstack - opinionated Claude Code workflow skills plus a persistent browser for agents" | **practitioner build-walkthrough, repository form** (a single author's daily-use skill suite for ten harnesses plus a browser daemon; the operating half - ARCHITECTURE.md, docs/designs, TESTING_INTERNALS, the changelog's why-paragraphs - is first-party; the README is the ad). **First round of the 2.x calibration series.** | 7,728 landing / ~840,000 in-tree md (153,670 CHANGELOG, 31,122 TODOS); the yield came from ARCHITECTURE.md, the host contract type, the egress-receipt test and the verify-gate script | 23 (16 design + 7 claim) | 6 | 2 | 9 | [[2026-09-02-gstack]] - **design record of 12 entries, routing count 3, all inside the browser subsystem; forge handoff scoped to that subsystem** (one worker, one subject, same session) rather than a scout wave over 840k words. **New subject `agent-browser-control`** (llm-agent/runtime-and-io, the tenth slot): `persistent-browser-daemon`, `references-over-selectors`, `socket-scoped-surface` (renamed from the spec, argued), `effect-classed-commands`, `agent-actionable-errors`; the protocol discriminator (MCP vs a plain local surface) in the golden path opening, with this harness's own MCP-reached browser as the unnamed counter-realization. **New technique `host-contract-compilation`** in agent-instruction-files - the case `single-source-topology` cannot solve (hosts differing in accepted fields, tool vocabulary, capability). **Four source-tree applications** (v2): host contract, egress receipts with polarity pinned as test data, the verify gate's content-hash trust store, the daemon. **Task row against the source tree** per the standing focus: the daemon's IRON RULE decision had no test and could not be imported in a test on one platform; moved to a side-effect-free module and pinned 5/5 on a branch of the clone (`8f76abb`, tests green). Direction pass proposed 0: the one real direction (a second wrapped-CLI provider in the desktop app, whose trait has one implementation) is technique-grain and the fleet map classifies at subject grain - recorded as the round's method finding. 9 catches by reading, 7 untriaged with promoting questions, 2 leads. 0 siblings at claim, 2 by Phase 8 |
| 2026-09-02 | `github:NousResearch/hermes-agent` @ `0cbc6e3` - "hermes-agent - the agent that grows with you" | **research-model release** (engine + operating instructions in one tree: 49k words of first-party docs over ~638k lines of runtime and 1M lines of tests; a peer of the fleet's agent runtime). **Round 2 of the 2.x series; every worker Opus, the director Fable.** | 2,059 landing / 49,248 in-tree operating docs (~22,500 read whole); README contributed zero entries | 30 (15 design + 15 claim) | 16 | 7 | 9 | [[2026-09-02-hermes-agent]] (+ companion `.design-read.md`, the front half verbatim) - **design record of 15 entries over six systems, 12 NONE; routing count per system 4/3/3/2/0/0.** One system had three NONE with no home → **new subject `tenant-scoped-agent-runtime`** (6 techniques; `orchestration`, since `runtime-and-io` hit its cap this morning). Two systems had three NONE with an existing home → technique triples: **agent-runtime-assembly +4** (observer/mutator surfaces, rewrite before the gate, honest hook registry, session-scoped capability), **prompt-assembly +2** (amortised compaction cadence with the operator's turns never absorbed; deferred interface invalidation), **embedded-db +1** (corruption class decides degrade-or-quarantine). Three amendments: stuck-loop-detection (involuntary interruption as a second key), webhook-ingestion (the relay clause's discriminator: who can hold the secret), advisory-guard-fail-mode (bound only handlers with a safe abandonment direction). 4 source-tree applications. **Task row on the source clone** (escalation writes now report their failure; patch exported, clone deleted). **Peer comparison study for personas: 76 points, keep-ours 39 / adapt 20 / adopt 9 / different forces 8**, three proposals; one proposal for pof. 9 catches, 7 leads, 10 untriaged. The director redid nothing the workers produced. 1 sibling at claim |
| 2026-09-02 | `github:Portkey-AI/gateway` @ `669825c` - "Portkey AI Gateway" | **vendor repository, the branch with no rules page** (22k words of cookbook, no operating document; yield ~9:1 code to prose from the request pipeline, config schema, adapter interface and mocked-boundary test suite). **Round 3 of the 2.x series; every worker Opus.** | 1,814 landing / 22,127 in-tree md (~6,000 operational) / ~5,500 lines of TypeScript read of 430 files | 25 (13 design + 12 claim) | 13 | 7 | 9 | [[2026-09-02-portkey-gateway]] (+ companion `.design-read.md`) - **whole-tree 6 NONE, per system at most 2, and the HOME-IF-NEW clause fired on four entries sharing one home** → scoped forge: **new subject `multi-provider-gateway-plane`** (backend-platform/resilience, ninth of ten; `stream-proxy-hop` is the N=1 shape and its stated boundary): strategy tree with inherited policy, router-versus-candidate failure, caller-scoped normalization strictness, per-provider stream framing, adapter direction asymmetry, policy verdict in the status space. **New technique** `allowlisted-operator-stream` (browser-credential-boundary). **Five amendments**: circuit-breakers (one breaker per candidate; all-open degrades to trying), backoff-design (over-budget stated wait ends the ladder; the fifth terminal state in the golden path; three spellings of retry-after; a jitter counterexample), storm-control (fan-in retry default ships off), brokered-egress (priced roster per credential), failover-horizon (check cadence). 3 source-tree applications. Task row on the clone (over-budget stop no longer collapses into exhaustion; typecheck unchanged from base; patch exported; clone deleted). **Peer studies: tracklight 32 points (adopt 8 / adapt 9 / keep ours 11 / different 4), pumper 13**; four proposals; one seeded point corrected (pumper's retries default off, like the source) and one convergence (pumper's capped retry sleep is the fleet's reference for the over-budget rule). Highest lead: the corpus disagrees three ways on guardrail fail direction. A foreign unclaimed restructure of game-production appeared mid-run; excluded by pathspec. 0 siblings at claim |
| 2026-09-03 | `github:HKUDS/LightRAG` @ `c124864` - "LightRAG - simple and fast RAG over an LLM-extracted entity graph" | **research-model release with a vendor-repository half** (paper reproduction beside a product server; 114k words of docs over 97k lines; 15 backends behind 5 base classes). **Round 4 of the 2.x series, first on 2.2.0; every worker Opus; the front-half worker survived a network outage by resume, with its unverified anchors marked.** | 5,819 landing / 113,763 in-tree md / 96,995 lines of Python; README last, contributed nothing | 22 (9 design + 13 claim) | 11 | see companion | 12 | [[2026-09-02-lightrag]] (+ companion `.design-read.md`) - **both routing clauses fired on the same cluster** (per system C at 3, HOME-IF-NEW 3 on C) → scoped forge: **new subject `llm-extracted-entity-graph`** (prompt-and-context, eighth of ten; the discriminator against the civic bundle's registry-joined graph in the opening, provenance required in one axis): surface-form identity and its risk, accumulate-then-threshold merge, recall passes with a declared cap, recoverable fan-out write. **Six techniques in five subjects**: `query-decomposition-before-the-lanes` (a missing stage in retrieval's pipeline), `durable-intermediate-representation`, `liveness-proof-reclaim`, `capability-declared-in-the-type`, `sequence-token-write-ordering`, `migrate-from-data-shape`; one amendment (the admission door below the body reader). 4 source-tree applications. Task row on the clone (a disabled reaper now says so; patch exported, clone deleted). **Peer study for politicas: 39 unique points, keep-ours 21 / different forces 14 / adapt 6 / adopt 5, 7 seeded points corrected, three proposals** - the peer is the other side of the corpus's own discriminator. 12 catches, 14 untriaged with anchors, 3 projects deliberately not proposed to. Directions waiting at Phase 1: 8 / 0 ledger rows. 1 sibling at claim, no overlap |
| 2026-09-03 | `github:microsoft/RustTraining` @ `9d19c48` - "Rust Training Books" | **doctrine corpus** - a NEW source class, proposed this run: seven books, 175 markdown files, ~217,000 words of prose, one .rs file and a 496-line build tool. Not a system; the repo-as-system routing count is 6 decisions, all in the operational periphery, none clustering three-on-one-home, so **no forge handoff on architecture grounds**. The design read was thin and the claim read was the whole run. **Re-scan when** a second doctrine corpus arrives to test whether the class is systematically weaker where it cannot execute its own rules; or when the 6 banked leads mature | 875 landing / ~217,000 in-tree md across 175 files | 66 | **1 subject / 23 techniques / 15 amendments / 5 applications** | 0 declined | 6 | [[2026-09-03-rusttraining]] - **the XL trigger fired mechanically at 7 shared homes against a threshold of 3.** New subject `invariant-placement` (standards-and-gates, fifth of five): the corpus used "makes X unrepresentable" as a closing idiom in 45 documents across all bundles and no subject, technique or law owned it; `gate-laddering` had no rung zero and `quality-gates`' own test ("name the input that makes it block") cannot describe a stage whose failure is a non-existent program. The source argues placement is FREE and states no compile-time, error-message, migration or onboarding cost in ~40,000 words - every "compile time" hit is a boast - so the subject carries five costs found by reading what the source does rather than what it says. Highest-value single landing: `deterministic-proxy-gate`, which closes a hole `operation-assertion-gates` states and leaves open (swap the apparatus, not the standard), on the #2 worklist subject. Missing stages found in `job-coordination` (the involuntary-interruption regime, where every existing technique presumes the interrupted party survives to participate) and `error-handling` (the library-vs-application ownership asymmetry its own "classify on structure, never on prose" rule makes mandatory). `scale-investment-timing` gains an in-process axis WITH a number (~1K-10K concurrent mostly-idle connections). Contradiction resolved against the source on wall-clock benchmark gating; our verdict held on the source's own evidence. Best application is a refutation: a THIRD host (the repo's own preview server) lacks the .html fallback the other two align on, so parity was written pairwise instead of across the host set. The predicted feature-flag-combinatorics hole was NOT one - covered twice and better. 21 catches, 14 untriaged with anchors, 0 of 3 fetches. All five landing workers died on a session rate limit and all five were RESUMED from their transcripts rather than re-dispatched; zero work redone. 3 siblings at claim, 5 by Phase 7; index.json and catalog.json deliberately left uncommitted (they had absorbed four siblings' unlanded subjects) |
| 2026-09-03 | `github:microsoft/mcp` @ `bc2a3b4` - "Catalog of official Microsoft MCP server implementations" | **vendor repository read as a SYSTEM** (one shared framework, ~50 tool areas, three servers, a distributed HTTP-server package; the landing page is a catalog advertisement and contributed zero entries). **Round 5 of the 2.x series; every worker Opus, the director Opus.** **Re-scan when** the output-schema migration moves past its five-command pilot; or a deprecation mechanism appears (today there is provably none); or the curated-mapping integrity assertions leave DEBUG-only; or 8 weeks elapse | 2,340 landing / 374,116 in-tree md over 5,354 files (~57,000 read whole, ~47,000 skimmed, ~4,000 lines of C# opened) | 49 (26 design + 23 claim) | 12 | 4 | 5 | 11 untriaged | [[2026-09-03-microsoft-mcp]] - **the first 2.x round where the routing count is met and the answer is NOT a forge.** Per-system NONE 5/4/6, whole-tree 15, every system clearing three - but nine of the fifteen home into `mcp-tools`, a subject the corpus already owns, and the rest scatter across five more existing subjects, so v2.2's own clause resolves to technique triples inside existing homes. A high count over ground the corpus owns is a sign the corpus was right about where the material lives. **4 new techniques**: `catalog-projection-modes` (the host's tool budget is hard, external and shared across every installed server - one host refuses >128 tools summed, and the source spent a documented breaking change to cut its own default from ~128 to 25; carries the annotation-equality invariant, the re-check at the resolved operation, and the priced second authority), `tool-identity-vs-tool-name`, `sanctioned-session-state` (a deliberate, argued inversion of `identity-survives-reuse`), and `recorded-interaction-fixtures` (test-harness; a counted grep returned ZERO files corpus-wide for test proxies, record/playback, cassettes or sanitizers-at-rest). **7 amendments**: tool-schema-design x2 (the third authority - schema vs handler USE, which no schema/validator unification detects, priced at ~50 breaking-change PRs; and conditional requiredness leaving the schema), client-integration (the envelope is not the decision), cross-boundary-propagation (when the sender is not yours), deprecation-by-version-arithmetic x2 (declining the window, with conditions and bill; and executable consumer pins), gate-laddering (the runtime rung, severity split by AUDIENCE not stage), same-change-enforcement (make the doc derived, assert regeneration is a no-op). **1 instrument** `scripts/surface-snapshot.mjs`, verified by the director not by its author. **4 source-tree applications** which refuted their own techniques twice and forced five text corrections. **Applied 3 rows: 1 code better (tracklight, ~16,000x measurable, Arm A proven by a negative control), 1 experiment not-better (pumper improved the technique), 1 unapplied with reasons.** Peer study for pumper: 42 points, keep-ours 28, two seeded points corrected, pumper ahead on three. 5 siblings live; `test-harness` genuinely contended - the golden path was re-read inside the content lock and only this run's line appended, which is the append that would otherwise have deleted two of a sibling's declarations. Gate closed red on 28 problems, none in any subject this run touched; index/catalog regenerated and left uncommitted over sibling WIP. **Phase 1 method finding: the directions ledger is not machine-portable** - the 12 proposals from rounds 2-4 live in checkouts that exist only on the other machine, so "how many are waiting" is unanswerable from this box. 0 of 3 fetches spent |
| 2026-09-03 | `github:kyrolabs/awesome-langchain` @ `9796b1d` | **reference index** - 413 links over 4,047 words, and the class profile is the finding: **211 distinct documents, 198 of them repositories, ZERO papers and ZERO specifications**, with only 13 annotations (6%) claiming a mechanism, failure mode or measurement. Two consequences reshaped the lane. (1) The wave design assumes a reference is a document readable in ~2 fetches; at 94% repositories each reference is itself a Phase 2b source, so wave-1 lanes got **clone briefs with the full sweep order** rather than fetch briefs - one lane found 46,000 words of in-tree operating docs behind a 2,000-word landing page. (2) The rankable band is **13 entries wide, not 211**; wave 1 took 9 covering 7 of the 13. Ranking on annotations has a false-negative class this run hit - the **under-annotated primary** (a five-word annotation hiding a paper and a benchmark). The curator's own finding: a bibliography with no papers and no specs, 82 of 211 rows competing frameworks, states that the field is an ecosystem of implementations - it has no row shape for a decision, which is why 9 references produced 21 candidates the list itself cannot represent. **Re-scan when** the index adds a papers section, or a wave-2 pass wants the 15 ranked unread, or 2026-11-03 | 3,665 landing / 211 references enumerated | 21 | 21 | 7 | 4 | 187 tail + 15 pool + 3 never offered | [[2026-09-03-awesome-langchain]] - **1 new subject, 6 new techniques, 7 amendments, 14 applied rows (0 code / 6 experiment / 8 simulation), 5 not-better, 1 defect fix shipped, 0 director fetches.** Every one of the six wave lanes returned a CONTRADICTION rather than a confirmation, each verified by the director against the registry file before landing. (1) `admission-hypothesis` claims "only a few bets available", enumerates four - all about time or adjacency - and denies the category by example ("free text the user will never retype"); a working system runs on a fifth, paraphrase recurrence, under which a hit can be WRONG rather than old. New technique `similarity-keyed-admission`, then amended three times from three projects: the reject-arm scope audit (pumper, from two shipped incidents at a correct threshold), the canonicalization boundary (tracklight), the projection boundary (politicas). (2) `extraction-yield-bands` aggregates worst-of-regions, which serves re-acquisition and breaks a document admission gate - a third consumer it never names. (3) `fallback-retirement-condition`'s "a number that fell to zero says delete the code" inverts for an incident-triggered failover; politicas is a clean inversion witness - the naive rule retires 90 live incident paths and keeps the one genuinely dead one. (4) The builder-side exemption in `attribute-precedence-lists` uses the wrong discriminator: a LIBRARY pins neither end of the skew. (5) A missing stage BETWEEN bundles - three observability subjects defer builder-side emission to a fourth that requires "one authority" and never entertains that the authority could be external; new technique `vocabulary-source-selection`, extended from politicas with the case the discriminator cannot reach (where a miss is a leak, depend on neither vocabulary). (6) The whole `quality-scoring` category presupposes a JUDGE across four verified enumerations, stated nowhere; XL trigger fired mechanically, new subject `generator-uncertainty-scoring` forged in-session. **The run's most important result is a process one:** the wave worker's headline comparative was marked `[H]` (summarizer-read); the forge worker's re-derivation from the papers found the spec conflated two documents, the cited figures were in neither, and a model judge was in fact the best non-ensemble scorer in **11 of 24** scenarios - the corpus would have published an inversion of the truth |
| 2026-09-03 | `github:jamiepine/voicebox` @ `51f49de` - "Voicebox - the open-source AI voice studio" | **vendor repository, design-deep** (a local-first voice studio: native desktop shell + hidden overlay + frozen-binary sidecar in three hardware variants + a machine-facing local surface + 9 locales + 7 synthesis engines behind one registry). Ingest returned 2,949 landing words - the README opens by positioning against two named commercial products and is the ad. Five parallel design readers swept **82,865 in-tree words of md/mdx** over ~71,000 lines, operating documents first, README last. **Re-scan when** the wrapper-attestation fix lands (`MACOS_NOTARIZATION.md` still reads "fix pending"), or the four inert artifacts (U1) are either removed or revived, or a second synthesis engine class ships that has a stop token to miss, or 8 weeks elapse (2026-10-29) | 2,949 landing / 82,865 in-tree / ~71,000 lines | 37 design | 11 techniques + 1 amendment + 9 applications | 4 | 11 | 7 untriaged | [[2026-09-03-voicebox]] - **routing count per system: E=4 NONE sharing one home-if-new, both v2.2 clauses fire on the same cluster.** Scoped forge executed in-session -> **new subject `client-architecture/native-shell-integration`** (8th, flat, under cap; `runtime-and-io` is AT its cap of 10 and `app-shell` is a web shell): `permission-gate-vector`, `capability-presence-contract`, `non-stealing-overlay`, `layout-resolved-input-synthesis`, `native-owned-stream` + 5 applications. The forge worker overrode the spec twice and was right both times - the optional fifth technique promoted into the golden path as the subject's thesis, and `capability-presence-contract` minted in its place because the tree showed grant-state and presence collapsing into one boolean. **`voice-io` +3**: a grep of the whole 13-technique subject for `unattended|programmatic|agent-initiated|impersonat` returned ZERO - every consent, arbiter and mute rule assumed an in-product caller, and the source ships a surface where an unattended client makes the machine speak in a clone of a real person. `render-acceptance` is `failure-not-empty-success` INVERTED (success that is actually failure); renamed from `synthesis-output-verification` on the worker's argument. Applications add **`python`, a stack the subject never carried**. **+3 techniques and 1 amendment elsewhere**: `change-rate-partitioning` (the stated boundary against `version-single-truth` - two facts, not two replicas), `adoption-time-packaging-audit`, `unsatisfiable-set-empirical-gate` (whose golden path claimed "exactly two answers" - falsified and corrected under the content lock, the subject being held by a live sibling), `signing-and-trust` amended with the wrapper direction of partial attestation. **Best evidence produced: a NEGATIVE application** - the tree implements `transcript-handoff-receipts` rule 2 and none of rules 1/3/4, so the race that technique exists to prevent is still reachable at a nameable line. **Applied 12: 0c/2e/8s/2 unapplied, 6 better / 4 not-better**, every not-better condition landed in its technique. Peer study for the fleet's structural peer: **49 points, keep-ours 24 / adopt 11 / adapt 9 / different-forces 5, 8 of 12 seeded points corrected**. 0 of 3 fetches. 2 siblings at claim, 5 at commit; `index.json`/`catalog.json` left uncommitted (regeneration absorbed four siblings' unlanded subjects) and `taxonomy.json` committed as a crafted blob carrying only this run's line |
| 2026-09-03 | `github:kube-rs/kube` @ `7a4641d` - "kube-rs - a control-plane client, runtime and derive crates" | **vendor repository, foundation-hosted** (49,580 words of doc comment against 19,885 of markdown; the doc comments ARE the docs; the README contributed nothing). **Round 5 on 2.3.1, every worker Opus**; the operator named two dimensions (language craft, cluster operations). 12 design entries; NONE per system 2/1/0/1/2, HOME-IF-NEW 4 on A3+A4+D1+E1; the director created the subcategory `operations/control-plane-operations` and widened the handoff to **three subjects** (`declarative-resource-lifecycle`, `convergence-loop-and-requeue`, `watch-cache-and-resync`, 14 techniques, 3 applications) + 2 techniques in existing subjects + 4 amendments (one by convergence of two peer-study sightings) + 3 craft applications + 2 practices. Task: uid precondition on marker removal, 4 tests, `cargo test` green, patch exported, clone deleted. Four peer studies (tracklight 30, kp 22, pumper 34, personas 33 points) and 11 proposals for the gate. Lead L5: gravitone is not in the fleet config. | [note](2026-09-03-kube-rs.md) · [design read](2026-09-03-kube-rs.design-read.md) |
| 2026-09-02 | `web:blog.google/.../3-8-flash-and-3-8-flash-cyber` - "Introducing Gemini 3.8 Flash and 3.8 Flash Cyber" | **vendor release announcement** - the class rule from 2026-08-28 held exactly: both accepted findings came from a number or a dated fact, none from a sentence. Mined as a news run AND a fleet-wide currency sweep, because the operator's ask had a second half: find every project pinning an older Flash and move it. **Re-scan when** the vendor publishes 3.8's input modalities (undocumented on release day - the reason two vision seams did not move), when a 3.8 Flash-Lite ships (its absence collapsed one project's fast tier), or on 2026-12-31 when the introductory rate ends | 1,287 | 11 | 2 | 4 | 0 already covered / 5 untriaged | [[2026-09-02-gemini-3-8-flash]] - **the sweep was worth more than the post.** Nine of eleven projects pinned Gemini Flash across eight generations (1.5 to 3.7); the docs fetch - which was the extraction, not corroboration, since the post never states an API id - returned `gemini-3.8-flash` as stable, **no Flash-Lite**, and three ids as SHUT DOWN. `goat` was calling two of them: 404s in production, not stale pins. Two amendments: `effort-calibration` gains the version successor as a re-measure trigger in its own right (same tier, same price, same dial is not the same model; the dial defaults to `high`; the predecessor is retained as the efficiency tier, so the version ladder is a second tier axis rather than an upgrade path), and `price-tables` gains a published end date as a scheduled event distinct from staleness plus the computed-key defect. **The fleet supplied the discriminator for the first by disagreeing with itself**: ascent books the introductory rate (its figure is current spend, guarded by a test that throws on the expiry date carrying the replacement rate), kp books the standard rate (its figure feeds cross-model comparison), both argued unprompted in opposite directions - so the rule is what the number is FOR. **The strongest finding is negative and structural**: systedo-case keys rate rows by the constant naming the current model, so the bump carried the key across, the completeness test stayed green, and the rate underneath still described the preview - a 10x understatement behind a passing assertion. Applied 2 (1 code better ab-paired, 1 task unmeasurable with the instrument named). Shipped 7 commits across 7 projects, none pushed. **Three projects declined the bump on their own recorded evidence and each refusal outranks the edit**: gravitone (dated measurement, promotion rule in the file), personas and pof (vision seams, modalities undocumented). tracklight and personas-web correctly untouched - tracklight's entries are leaderboard identity-normalization rules whose rewriting would corrupt historical merges. 4 fetches (1 over budget, spent deliberately on a capability check before shipping a vision change; recorded). 0 declines. index/catalog left uncommitted - the regeneration absorbed three sibling subjects not in HEAD |
| 2026-09-03 | `github:emdash-cms/emdash` @ `7a5d9c18` - "EmDash - a full-stack TypeScript CMS on Astro, the spiritual successor to WordPress" | **vendor repository, design-deep**; operator framing was "focus on code design patterns and architecture, not product", so the run was Phase 2d end to end. Ingest returned 1,334 landing words over a tree of 346,746 in-tree markdown (138,401 in docs/) and 57,514 lines of TypeScript across 20 packages, 5 apps, 9 agent skills. Four parallel design-read workers, one per system. **Re-scan when** a fleet project grows an extension surface; or the aggregator's public view gains a verification field (the write-path-only trust root); or the plugin registry's hostile-index acceptance test appears; or 8 weeks elapse (2026-10-29) | 1,334 landing / 346,746 in-tree md / 57,514 lines TS | 58 (32 design + 26 claim) | 16 | 3 | 4 | 26 untriaged | [[2026-09-03-emdash]] (+ companion [[2026-09-03-emdash.design-read]]) - **two systems fired the routing clause independently and both were forged in-session** (operator picked both at the gate): `security/untrusted-extension-host` (7 techniques - two-tier format split by where code runs, isolation-tier-independent API, pluggable isolation runner, canonicalizable privilege declaration, grant-change consent, declared-schema extension storage, per-callback failure policy) and `security/decentralized-artifact-distribution` (6 - origin-signed record with index-as-cache, verdict bound to the exact revision, split admit/state/redact authority, fail-closed trust dependency, signal that only subtracts, publisher-declared verification floor). Security is now at exactly 10 subjects, the cap. **The finding that decided the first**: `operator-tier-code-loading` states its tiers as a two-row table whose second row reads "service-writable configuration ... may name code: never", justified by a precondition ("the isolation story is unavailable in the host's own address space") that this source deliberately falsifies - an enumeration with a hole, found by walking the technique's own table. **10 further techniques in 8 existing subjects** and **5 amendments**, incl. `ratchet-design` gaining the one honest auto-update (the file said "Never auto-update"; the source does it because an outside contributor cannot regenerate a platform-specific baseline locally, so the diff becomes a mandatory review artifact - with the trade stated: never red, mechanical enforcement gone) and `prose-rule-drift` gaining a third tier between mechanised and unbacked (a rule no checker can express, enforced by a reviewer given the rulebook; cost: probabilistic and non-bisectable). `expand-deploy-contract` returned **zero prior hits bundle-wide**. Applied 3: `cache-value-not-handle` **code/better** on this registry's own run board (lock reclaim read the clock instead of the owner - broke a live holder's lock past ttl AND waited out 899s on a dead one; A 3/5, B 5/5, three controls identical, and an earlier B draft that was worse than A was caught by the fifth control), `post-merge-regeneration` **simulation/better** (three real collisions from this ledger), `truncated-verdict-space` **simulation/not-better** against kp, which truncates the ADVERSE verdict while auto-applying the admitting one - the technique was polarised too narrowly and gained the polarity rule plus kp's apply-boundary re-derivation. 0 directions proposed (the forged subjects' forces are absent fleet-wide; one real candidate found in politicas - a module-scope memoised promise on a runtime that tears down contexts - deliberately not written, tree not authorized this run). 4 catches, 3 leads incl. a fourth-domain sighting of the content-binding law this bundle lacks. 1 sibling at claim; 0 fetches of 3 |
| 2026-09-03 | `github:Chatterino/chatterino2` @ `fda51f0d` - "Chatterino 2 - a chat client for a live-streaming service" | **vendor repository, community-held**, through the no-rules-page branch: the operating documents are code (31,327 doc-comment words against 14,036 of non-changelog markdown; 11 of 13 design entries anchor in code; the README contributed nothing). Round 6 on 2.3.2; every worker on Fable because Opus returned 529 three times, and two session rate limits killed eight worker attempts - nothing was lost because workers wrote each file as drafted. | 499 landing / 32,550 in-tree md / 31,327 doc-comment | 27 (19 design + 8 claim/craft) | 9 techniques + 6 amendments + 11 `cpp--` applications + 6 fleet applications; 0 declined | 5 | 8 (+ 8 untriaged) | Routing: per-system NONE A2/B2/C0/D2/E1/F1, HOME-IF-NEW max 2, existing-home triple C=3 -> technique triple in `untrusted-extension-host`, no forge. Applied 6 rows (1 code better ab-paired, 1 experiment unmeasurable, 4 simulation: 3 better, 1 **not-better** that gave `applied-defaults-ledger` its boundary), 8 unapplied with return conditions. Shipped 6 commits in 2 projects (personas late-tick guard merged after a green gate; pof experiment row). Directions 1/2 (personas restore surface proposed; safe mode's falsifier fired, late tick applied as code instead); gate skipped, unattended. Note: `2026-09-03-chatterino2.md`, design read beside it. |
| 2026-09-03 | `web:lesswrong.com/.../adaptive-agentic-worms-are-here` - "Adaptive Agentic Worms Are Here" | **second-hand walkthrough of one paper + practitioner dialogue** (the post relays a preprint's results and containment appendix verbatim with protocol attached; the 14-comment thread is the dialogue half and carries a public retraction). **The canonical URL 429'd twice - exit 2, an instrument failure, not a thin source - and the `greaterwrong.com` mirror served the same document clean. A bot wall is a property of one frontend, not of the document.** | 4,381 (mirror; landing 429) | 12 | 4 | 2 | 2 | [[2026-09-03-adaptive-agentic-worms]] - **one technique, two amendments, and the run's finding was a lane rather than a rule.** `guard-input-custody` (agent-runtime-assembly, 11th): a guard's policy inputs sit outside the write reach of the party it constrains, carrying the **re-read trap** - refreshing a policy per decision is not freshness against a governed writer, it is re-reading that writer's latest edit, so the careful instinct produces the vulnerable design. The corpus already held the rule for the *measurement* lane (`candidate-write-access`: "whatever the candidate can write, the measurement cannot assert") and had never stated it for a *containment* boundary; the boundary is now written on both sides and the discriminator is real - **a measurement is defended by changing what the optimizer chases (a holdout needs no cooperation), a confinement cannot be, because it must hold rather than be believed in.** Two amendments: `candidate-write-access` +1 enumeration question (the run's own confinement - allowlist, ceiling, timeout - sits beside the harness, not the fixtures, which is why "what can the candidate write" missed it), and `scenario-design` + "a seeded environment moves the number one stage down the pipeline" (from a **retraction in the comment thread**: the protocol disclosed that every host was seeded vulnerable, so the 73.8% describes exploit-given-a-qualified-target; the predicate stayed in the methods while the figure travelled without it - and two careful readers disagreeing in opposite directions about one honest protocol is evidence about the claim, not the readers). The vendor-refusal candidate folded into the technique's last section rather than landing separately: a rented control binds only the parties who route through it and is *absent*, not weakened, for anyone else - corroborated at primary, verbatim. **Applied `code`/`better`/`ab-paired` against this registry's own purity gate**, which chose its denylist from a key in the governed bundle's own index and degraded to a weaker profile with a *note* when absent: one deleted line took a planted violation from red (1 violation, exit 1) to green (0, exit 0). Custody moved into the checker; four conditions, controls both directions, no false positives across eight bundles. 2 catches, both where the corpus outperformed the source (`refusal-reroute-hop` already forbids laundering a refusal by rerouting; `candidate-write-access` already owns scorer reachability). 2 leads, 5 untriaged with anchors. 3 of 3 fetches; the containment anecdote rests on the relayed verbatim quote plus training-data convergence on reference-monitor tamperproofness, and the note says so. **Method defect found and recorded: Phase 7.7's pending-proposal detection matched `status: proposed` in prose, presenting a comparison study at the operator's gate as a decidable item; no ledger row was written for it.** 2 siblings live |
| 2026-09-03 | `web:anthropic.com/research/automated-researchers-mitigate-alignment-failures` - "Automated researchers can reliably mitigate alignment failures" | **vendor release announcement**; the class's two rules both held, the second by refuting the first pick. Numbers were the entire yield (not one accepted finding came from a sentence), and **the fetch was the extraction**: the announcement says the agent cheated "by exfiltrating test labels from a remote API", and the primary report documents no such thing - its 39 flagged trajectories (2.4% of 1,601) are re-submission 67%, format-copying 26%, reviewer-misleading 21%, and the holdout was "kept out of the AAR's reach by the operating system rather than by trust". The exfiltration line is a capability learned elsewhere, folded in as motivation and readable as this study's finding. **Re-scan when** the open-sourced harness is mined as a repository (a separate intake with a design read - the agents' environment, the proposal-reviewer prompt and the ten categories' protocols are not in this post) | 1,306 landing / primary fetched | 10 | 3 | 3 | 1 already covered / 3 untriaged | [[2026-09-03-automated-alignment-researchers]] - **the corrected premise was worth more than the pick it replaced.** The report's own numbers say the winning attack needs no write access, no network and no intent worth the name: *"only lucky re-runs ever succeeded"* - neither category requiring the agent to model the instrument or its reviewer reached the top three. That is a third surface `candidate-write-access`'s central rule ("whatever the candidate can write, the measurement cannot assert") cannot see, because reaching it is not a write. Two techniques in `eval-harness`: **`selection-over-noise`** (repetition as a surface; best-of-N as an upward-biased estimator, in the report's own words *"biased upwards by taking a maximum over noisy evaluations"*; and the comparison face - 82-85% from an arm taking a maximum over ~150 attempts against 20% from 28 practitioners explicitly **unable to iterate**, which the reporting team itself reframed from *better than* to *supplies candidates at a scale the other arm cannot*) and **`monitor-inside-the-loop`** (roughly half the flagged population was modelling the review machinery; the surviving design is two reviewers - one reads the code, a second reads the write-up against that same code; and the monitorability the scheme rests on sits in the report's own **future-work** list, which is where a precondition sits when it has been relied on and not established). The second was drafted as an amendment and landed as a technique because **a sibling was mid-rewrite of `candidate-write-access.md`**, adding a fifth enumeration question and a measurement-vs-containment boundary while this run drafted. Boundary that earned the first a technique rather than an amendment: `comparison-modes` requires the aggregation rule to precede the data, which defeats post-hoc p-hacking and leaves the bias untouched - a pre-registered "best of 150" is honest, is not p-hacking, and is still not comparable to a single draw. Applied 2, both **better**: `code` on this registry's own `dojo` judge-retirement rule (its real ledger holds 15 pairs across 4 gated cycles, the judges disagreed on 6, and the rule pins an equally-good judge **22.8%** of the time; with a resolution precondition **5.8%**) and `experiment` on personas' memory eval (**11/15** planted cases, 4 misgrades, **all false positives**, all on the probes the suite exists to measure). 1 catch (`resolution-precondition`'s monotone-axis corollary already owns the 4.7x scale-transfer claim and states it better). 3 leads, 3 untriaged with anchors. **1 of 3 fetches** - the primary answered all three picks at once. Gate red twice, both times on a sibling's file; index/catalog left uncommitted. 1 sibling at claim, 4 by Phase 7 |
| 2026-09-03 | `github:AlexsJones/llmfit` @ `d19380ba` - "llmfit - hundreds of models & providers, one command to find what runs on your hardware" | **first-party practitioner codebase in repository form**, operator-named dimensions (gateway/wrapper composition; Rust design in mature codebases). Landing page 1,612 words against 28,584 of in-tree operating documents (~18:1) and ~54k lines of Rust with ~800 tests. The tree's distinguishing property: **its comments carry issue numbers, measurement dates and the refuted alternative** - `providers.rs:476-486` records which live servers were probed on which date to establish each identity discriminator; `how-it-works.md §6` records a removed heuristic and **both directions** in which it was wrong. **Re-scan when** the identity table's dated entries go stale (each carries its own measurement date, so the tree tells you); or when a fleet project grows a presence-shaped identity join | 1,612 landing / 28,584 in-tree md / ~54k lines Rust | 16 (13 design + 3 craft) | 10 | 2 | 2 | 1 untriaged | [[2026-09-03-llmfit]] - **routing per system A3/B3/C4/DE2; B forged, A landed as an existing subject's missing stage.** `multi-provider-gateway-plane` was forged the day before and is thorough from *"a request arrives"* onward - policy trees, failure attribution, translation strictness, stream framing - and every one of those begins from a **configured tree of candidates**. Nothing asked how the plane learns which upstreams exist or which implementation is behind each address. That is a **missing stage**, not a missing opinion, and it landed as the subject's stage zero: `upstream-identity-before-inventory` (a shared protocol is designed to make implementations interchangeable, so it deliberately does not identify the implementation; evidence hierarchy from native route down to root banner, with the **empty-inventory hole** - a runtime with nothing loaded returns a well-formed empty list, so every per-record discriminator vanishes exactly on a machine somebody is setting up for the first time), `named-members-over-a-uniform-collection` (**the trait is implemented eight times and dispatched through zero times in 54k lines** - per-member match semantics and per-member failure policy are what a uniform collection erases; carries the substrate rule, that the concurrency choice belongs to the interface's most constrained consumer), and `join-breadth-follows-the-wrong-match-cost`. **The run's strongest finding is that last one, and it is a contradiction:** `llm-observability`'s `provider-family-matching` says match by family, not equality, *because a miss silently removes real spend from cost accounting*; llmfit's #861 records the inverse, measured - broadening by family stem marked **238 of 9,250 catalog rows installed from one install**, including an entry ~30x the size of the artifact present. Both correct; the discriminator neither bundle stated is **does a miss delete something real, or does a false positive assert something false?** A priced total lives in the first world, a presence claim in the second. Landed on both sides in prose, no cross-bundle link. **New subject** `engineering-assessment/measurement-method/modelled-performance-estimates` (4 techniques, forged in-session by a dispatched worker from an XL spec; the worker overrode the spec four times and was right each time - renamed T1 to `provenance-travels-with-the-value` because "confidence" is the *neighbour's* word for a measured goodness-of-fit, declined to split the calibrated rung, put the resolved-basis procedure in T1 rather than only in the golden path, and established that `unsupported` and `missing input` are different states licensing opposite operator behaviour). **4 amendments**: `absent-degrades-malformed-fails-fast` gains a **third state** between absent and malformed - well-formed and wrong for the role, the wildcard bind address a server is correctly told to listen on and a client must never dial - plus the rule that **a fallback attaches to a defaulted value and never to an operator-supplied one**; `fallback-retirement-condition` gains the build-time sibling of its traffic rule (**the fallback is structurally the least-tested path**, because testability seams get built where somebody had a reproducible failure and a fallback has none - measured here, where the AMD device-tree probe was extracted to take an injectable root *"so tests can supply a fixture directory"* with three fixture tests, and its NVIDIA sibling hardcodes the path with **zero**, and the untested one is the fallback that runs only when the primary tool is missing); `provider-family-matching` gains the third posture; `hub-ingest-plausibility-gates` gains a bound whose authority is the generator's own history. **3 techniques** in `federated-benchmark-sharing` (`content-addressed-contribution` - naming a contribution by its content makes a multi-step publish idempotent, and **the checks are the contract while the generated naming convention is only a triage signal**; `capture-locally-publish-separately` - the obvious opt-in destroys what it gates, because a decline that discards makes *no* the expensive option; `strict-ingestion-lenient-consumption` - refuse where the author is standing, degrade where only bystanders are). **2 catches, both where the corpus was better**: the law `unknown-is-not-a-value` already owns B2 (the source reaches it independently at five separate stages, which is corroboration for the law), and `hub-ingest-plausibility-gates` already argues *"arithmetically possible for a benchmark to produce, not close to the consensus"* more sharply than the source's own comment. **8 applications** (6 source-tree `rust@1.85`, 1 the forge worker's, 1 for the apply). **Applied 1 code/better ab-paired** on tracklight: the k-anonymity floor's *enforcement* half was correct and its *disclosure* half absent, so an empty board could not be told from a withheld one - `buckets_withheld` added, deliberately kept out of the contribution hash (a second test pins that, because hashing it would make every instance re-push unchanged data once); `cargo test --workspace` green. **1 direction proposed, accepted at the operator's gate and executed in-session** (tracklight endpoint identity). 4 confirmation rows where an independent tree already held the technique. **A sibling's status-derived pathspec swept four of this run's finished knowledge files into commit `ff802432`** - content byte-identical, nothing lost, three landed techniques carrying another run's message; second sighting of that failure today. 1 sibling at claim; **0 of 3 fetches** - a first-party codebase corroborates corpus-internally |
| 2026-09-03 | `github:boa-dev/boa` @ `665f039` | **first-party practitioner codebase in repository form - a system** (an embeddable language engine: object model, register VM, tracing collector, strings, host contract, standards-layered runtime, guest introspection, conformance tester, fuzz targets). Read from a shallow clone at a short path, never from the ingest; expected yield stated as a forge handoff plus a few technique landings in existing subjects; fetch budget 0 of 3 spent. **Routing count 13 NONE across six systems, all one home-if-new -> forge handoff executed in-session.** Unattended. | 1123 landing / 37518 in-tree md / 246581 lines Rust | 20 (18 design + 2 craft) | 4 retained + 13 forged | 2 | 2 | [[2026-09-03-boa]] - **a new subcategory `backend-platform/language-runtime` with nine subjects and 52 techniques**, forged by nine workers in-session from three scout reports (the scouts corrected the design record three times against the tree: fixed-width operands, a stored frame pointer, a 10,240-slot default - the docs were the naive reading). Intake-retained: `conformance-checking/edition-stratified-conformance` (pin the suite, stratify by the edition that first requires each check, diff finding sets with crash as its own class, post beside the change) - **applied as code in ascent, ab-paired, dispatch 1 -> 0 under a rubric change**, whose outcomes lane had the rule and whose alert lane did not; `test-input-generation/stage-ordered-fuzz-targets` (one target per pipeline stage, drain upstream first, deterministic budget on the deepest); amendment to `model-based-oracle` (normalise once, then round-trip; report the discard fraction); amendment to `capability-subtraction-sandbox` (where the runtime counts, the ceiling set is not empty - two lists, counted and uncounted, and the uncounted list is empty exactly when the guest imports nothing) with pumper's import-free WASM host as the structural witness. Two leads (spec-step comments as a per-line deviation register; a compare mode for the registry's own gate). One direction proposal (pumper: a per-stage harness), gate skipped. |
| 2026-09-03 | `github:rowboatlabs/rowboat` @ `f4fce64a` - "rowboat - open-source AI coworker, with memory" | **vendor repository read as a system.** Landing page 810 words against **56,285** of in-tree design documents (~70:1); the largest single document carries 30 numbered sections, a recovery table keyed by last-durable-condition, and 30 enumerated reducer invariants. **Re-scan when** the operator lifts `--no-handoff` - System C is a subject-shaped hole and the design record is already written, so a later pass diffs against it rather than re-deriving it | 810 landing / 56,285 in-tree md | 10 | 5 | 3 | 1 already covered / 4 untriaged | [[2026-09-03-rowboat]] - **the routing count was met and the handoff was declined, which is the run's headline.** Phase 2d found three systems: a turn runtime (4 entries with no home), a session layer (1), and a multi-writer collaborative asset plane (**8** with no home). Both clauses of the count fire - A at 4 and C at 8 each clear three alone, and A5/A6/A7 share one `HOME IF NEW`. The operator took `--no-handoff` and picked rows 3-7, so the design record is banked in the note rather than forged. **5 landed**: `endpoint-sealed-continuation-metadata` (the half of a transcript that belongs to *where it happened* - provider continuation blobs are sealed to the minting endpoint, so replay is gated per **segment** on strict provider-instance + model-id equality AND a clean close, with the inline base stripped unconditionally because its provenance is unrecorded; the reason prompt composition needs the target model as an input, which the subject never said); `elision-to-a-refetch-pointer` (a third answer beside compaction and tiered projection, for material still addressable at its source - elide to a pointer, not a summary; decorator-sited so the record is untouched, pure per message so prefix caches keep hitting, with the recomputability caveat written down); `additive-input-at-the-call-boundary` (mid-unit input injected at the model-call boundary rather than cancel-and-restart, with the three reasons supersede loses - each a property owned by a *different* subsystem, and the third is the structural one: supersede cannot serve a turn suspended on a permission, which is exactly when a person types); `indeterminate-closure-on-interruption`; and an **amendment** to `same-value-still-conflicts` for the mergeable plane. **The best result is a technique that lost.** `indeterminate-closure` was applied against personas because the tree looked like it had the named anti-pattern - a boot sweep writing `failed` plus a prose marker, re-parsed by a string-matching classifier in *both* Rust and TypeScript. That reading was **wrong**: `restart_recovery.rs` already replaces it with a three-class boot classification and the string arms are backward compatibility for a measured 74 of 2,188 rows. The tree then refuted the technique on a case it did not cover - **a run that kills the process on every resume always looks fresh, so honest closure plus re-admission is a boot loop, and every iteration looks locally correct** - and four mechanisms went from the tree into the technique (classify rather than declare; reuse the existing liveness threshold rather than picking a fresh number; count involuntary interruption on its own key; clear the mark on completion, not on the attempt). Verdict `not-better`, recorded as the run's most valuable row. **Applied 1 code/better ab-paired** on tracklight: the tool server's resource read emitted the whole body pretty-printed beside its rendered form, unbounded - 206,644 -> 43,628 bytes on one fixture (4.7x), `cargo test -p lighttrack-mcp` 58 passed, clippy clean. **The negative half is pinned by a test**: elision does *not* reach the threshold that triggers it, because it bounds payload per item and cannot bound an unbounded item count; an intermediate measurement (63,844 bytes when eliding but still pretty-printing) showed compact serialization was a third of the win and nearly went untaken. **1 catch**: the source's durable-barrier rule is `record-precedes-effect`, already a law and stated better. **3 leads**, incl. a law candidate at two sightings - *a write's record is about the decision, not about the bytes* (a verifying transaction refuses an identical write for correctness; a merging document plane records one for attribution). **4 untriaged with anchors**. **0 of 3 fetches** - a first-party codebase corroborates corpus-internally, and the one claim needing outside support (endpoint-sealed metadata) had training-data convergence across four provider families. **Declared focus, all four answered**: the forces-phrased query is what moved row 4 off a UI subject onto its real home (the first deliberate query found `chat-transcript`, which was wrong); the row 3 absence was **asserted against a known positive** before being believed; **Phase 7.7's inventory defect is fixed** - rebuilt on the frontmatter `status:` plus a branch and ledger check, it shows **zero proposals waiting** across 36 files (25 accepted, 2 declined, 9 comparison studies that carry no status and correctly never reach the gate), so the twelve-proposal backlog is closed; and the board claim was beaten at every phase from 7 and was still live at Phase 10. **0 directions proposed**, both eligible candidates recorded as not-proposed with reasons - incl. a real defect found in tracklight's manifest, whose `scope.does_not` prose says "agent runtime" while `out_of_scope_categories` omits `runtime-and-io`, so the fleet map calls it a candidate. 1 sibling live (forge-monai-0903) |
| 2026-09-04 | `web:blog.google/.../introducing-agentic-video-in-gemini` - "Introducing agentic video understanding with Gemini" | **vendor release announcement** - third of this class, and the class rule held a third time: every accepted finding came from a number, none from a sentence | 1,151 | 9 | 1 | 1 | 2 already covered / 3 untriaged | [[2026-09-04-agentic-video-understanding]] - **the post explains its own mechanism by accident.** It prints -88% tokens, -66% cost and "no additional feature fee" and never puts the first two beside each other; no surcharge means the 22-point spread can only be consumption re-mixing across rate classes. The developer-guide fetch (1 of 3) named the classes the post does not - navigation reasoning to a **thought-token** counter, on-demand frames/audio/transcript to a **tool-use** counter, neither of which is input or output - and **corrected the post twice**: the model list was stale on release day (docs add 3.8 Flash "and later models", so the 2026-09-02 fleet-wide bump to 3.8 did NOT strand seven projects off the feature), and the documented boundary is **latency, not cost** ("under 5 minutes - use static processing", TTFT rises from internal reasoning), which **inverted my own extracted candidate** - I had predicted a cost crossover and had the axis wrong. Landed one technique, `unit-classes-are-open` in cost-metering, as a technique and not an amendment because `usage-ledgers` makes "raw units, direction-split - input and output separately" a *non-negotiable* column and the finding does not qualify that rule, it says the enumeration behind it is not closed. **The asymmetry that made it a real gap**: `price-tables` defends the model axis and `usage-ledgers` the spend-class axis, both with a lookup that can miss loudly; the unit axis has neither *and cannot use that pattern* - an unknown model is a lookup miss something can count, an unknown unit class is never looked up, so it fails to a slightly smaller number. The golden path already named the danger ("unit classes a local table cannot see") and spent it only on "trust the meter". Applied 1 (experiment/better/ab-paired in tracklight: 2,000 tokens reported against 157,000, silent), shipped 1 commit on a branch, not pushed. **The corpus was corrected by its consumer**: tracklight's null-cost-plus-floor beats `price-tables`' conservative declared default, and the technique carries the tree's version. 2 siblings live, no contention. Re-scan when a consumer surface actually ships the mode, or when the thought/tool-use token RATES are published - the run could not price them and said so |
| 2026-09-04 | `github:zvec-ai/zvec-grep` @ `7d73ca1` | **vendor repository** - and the class under-predicted in one recordable way: it ships a *measurement artifact* (two paired A/B suites, locked selections, isolated references, an independent judge, 3 trials per cell, a 300-trial report that argues its own aggregation in prose), and that half behaved like a first-party practitioner account and produced 2 of the 4 landings. **Routing lesson: a vendor repository carrying a published `benchmarks/` protocol is two sources, and the benchmark half ranks first.** Design read: 7 entries across 4 systems, 4 NONE; routing count computed on both clauses (per system A=2 B=0 C=2 D=0; per home-if-new mcp-tools=2 eval-harness=2) - **neither clause reached three, so no handoff, and two design pairs landed as techniques here.** **Re-scan when** the roadmap's stable release changes the freshness contract or the MCP toolset boundary, or a second independent paired agent-tool benchmark publishes an election rate | 1,325 landing / 16,284 in-tree | 10 (7 design + 3 claim) | 4 techniques + 1 amendment + 4 applications | 1 | 3 | [[2026-09-04-zvec-grep]] - `eval-harness` gains `treatment-election-rate` (when the treatment is a capability the candidate elects, the arm is "with it available" and the election rate belongs to the delta's predicate; corrects `unaided-baseline-screening`, whose discard rule cannot tell a bad scenario from a declined instrument) and `outcome-conditioned-cost` (the both-succeeded subset as the comparable denominator, the unconditioned primary retained, the incomplete trial in neither view). `mcp-tools` gains `caller-differentiated-capability` and `fluent-syntax-bounded-grammar` (zero corpus-wide hits for the second's concept). **The run's only amendment came from a fleet tree, not the source** (round-9 focus 2): a `not-better` row against pumper produced "the refusals are a channel, and they must not collapse either", cited to a four-verdict token gate whose code says why collapsing expired into unknown would hide a leaked token. Applied 4: 1 code `better` and shipped (personas `ed5fd8058` - a cost comparison that **inverts** under conditioning), 3 simulation (1 `unmeasurable` with the instrument named, 1 `not-better`, 1 unapplied for want of a seam anywhere). **Fleet reach reported** (focus 3): 1 of 4 landings has preconditions no authorized tree meets. Directions 0/3 - all three candidate projects report a missing `scope` block, so the admission test could not run. 0 of 3 fetches. 0 siblings at claim, 2 by Phase 7; a sibling holding `retrieval` is why 2 promoted rows stayed untriaged. |
| 2026-09-04 | `web:github.blog` - "How we make AI coding more cost efficient without sacrificing task quality" | **first-party practitioner account** (four controlled A/B experiments over the authors' own harness, plus one shipped-and-reverted regression). The class's rule held exactly — the framing prose is the strip test's problem, the measurements and the failures are the yield — and this source is unusually dense in *negative* results: it leads with a change measured and **not** shipped, then describes a prompt rewrite that regressed in production and was stopped. **Re-scan when** a fleet project grows a tool-result shaping layer, which is the precondition three of the five landings are waiting on | 2,225 | 12 | 5 | 2 | 1 already covered / 4 untriaged | [[2026-09-04-copilot-cost-efficiency]] - **a new subject forged in-session, plus a technique found by the asymmetry hunt in an existing one.** `prompt-and-context/tool-result-economy` (4 techniques, 1 application) was forged by one worker from an XL spec because three picked candidates and the general form of a fourth shared one `HOME IF NEW`, and `prompt-assembly` was at 16 techniques owning a different artifact — *assembly asks what fits; this asks what the result costs by the time the task is done.* `end-to-end-unit-of-optimization` carries **displacement**: a metric whose boundary is narrower than the payer's unit can always be improved by pushing work across it, and the displaced cost is charged at a rate the local metric cannot see because a recovery turn re-transmits the prefix — nobody cheated, the number was honest and the boundary was wrong; the boundary against `metric-role-contract` is **roles vs scope**, independent failures, both required. `compressibility-follows-the-producer` classifies by what produced the bytes, and the worker reframed it as a proxy failure — **a size threshold diverges from compressibility at exactly its own worst case**, because the largest results are the class that must never be touched. `escape-hatch-usage-as-the-safety-metric` is **the run's keeper**: the rate the model takes the way back *is* the transform's regression signal, moving within a turn where task success needs a large sample; five shapes of recovery, and **the last three do not look like recovery, they look like the agent working**; plus two boundaries the source does not state — a low rate is evidence only if the path is advertised **inside the result's own bytes**, and the rate is conditional on the triggered population, so **tightening the trigger improves the diluted number while quality is unchanged**. `formatting-before-information` puts information-preserving transforms first because their saving is unconditional, and finds its material in the **vestigial affordance** — cost per-emission against value per-consumer, with nothing re-checking the pairing when the consumer changes; the discriminator is that line numbers are still right in diffs and snippets, so the audit question is *which consumer needed this, and does it still exist?* Fifth landing, in `prompt-assembly`: **`compression-hardens-deferred-decisions`**, found by the asymmetry hunt — `context-budgeting` models the *economics* of shrinking a standing layer with break-even arithmetic, cache multipliers and a measured 2,000-inclusion anchor, and models the *risk* with nothing. **Compression preferentially deletes hedges**, because a hedge adds no assertion and so has no local justification, and a hedge is where authorship declined to decide; deleting it does not shorten the rule, it **makes the decision**, collapsing toward the restrictive branch because that is the branch statable as a rule. The fix that worked was **shorter AND less restrictive**. Second half: the most compressible lines are the least tested lines by construction, and the ratchet is ordered — on a regression write the test **before** changing the text again, which also repairs a one-directional edge in `certification-levels` (the ladder never said what an empirical catch owes the cheap level). **1 catch** — `certification-levels` already owns "offline missed what online caught" and says it better. **2 leads**, incl. a law candidate at two sightings (the measurement boundary, argued by `context-budgeting` and `end-to-end-unit-of-optimization` in different currencies). **4 untriaged with anchors**, incl. the batched-completion-notice finding the operator did not pick. **Applied 2**: `simulation`/`better` where **the tree refuted the premise the seam was picked on** — it already ran the ten-fixture behavioural bench the technique assumed was missing, and its own baseline had reached the technique's decision rule seven weeks earlier from a measurement (mean 0.48, round-cap 0–1 on all ten fixtures, the fully-specified control drawing four questions against a design that says zero); and `code`/`unmeasurable` on a truncation whose recovery rate is zero by construction — **compiles clean, but the lib test binary will not launch on this box (0xc0000139) before running any test**, so it is on a branch with the instrument named. **Fleet reach reported as its own state** (round-9 focus 3): three of five landings are unapplied for one *shared* precondition — no authorized project owns a layer that shapes tool results before a model reads them. The corpus outran the fleet. **The forge worker overrode its brief five times and was right each time** (fifth consecutive dispatch). **0 of 3 fetches.** `index.json`/`catalog.json` deliberately left uncommitted — they reference six sibling techniques not in `HEAD`. 1 sibling live at claim |
| 2026-09-04 | `github:KnockOutEZ/wigolo` @ `c6ad4479` | **vendor repository** (repo form, design-deep) - ~75k LOC TypeScript, 12,071 words of `docs/`, and the design rationale living in **source header comments rather than an `ADR/` directory**, which is the routing trap: a sweep that looks for a design folder reports none and mines the README. **Re-scan when** a second independent source carries an election-bearing refusal value, or when the plugin host closes its isolation gap | 3,768 landing / 12,071 docs / ~75k LOC | 9 (7 design + 2 claim) | 4 | 1 | 0 already covered / 5 untriaged | [[2026-09-04-wigolo]] - **the first run where both v2.2 routing clauses fired on ONE system and the handoff was scoped to it.** Per-system NONE: acquisition 4, extraction 0, search/research 1, cache/watch 1; cross-system `HOME IF NEW`: the same 4 sharing `integration`. So: an XL spec for one subject, forged in-session by one worker - not a `/forge` over the repository. **`integration/contested-acquisition`** (5 techniques, 1 source-tree application) fills a **missing stage one**: `web-scraping` is thorough from Extract onward, gives Acquire one row of its pipeline table, and owns exactly one acquire technique which is about content-type negotiation rather than refusal. **The landing was a discriminator, not a correction** - `scrape-scheduling`'s `blocked` run outcome (pause, page a human, never retry) is *right* for a scheduled harvest of a target you must still be welcome at tomorrow, and this subject governs the other side: a single-shot retrieval where stopping is a silent empty answer. Both subjects now state the question; neither corrects the other. Techniques: `classify-before-you-respond` (the class decides which responses are applicable, including the classes whose applicable set is empty, and the honest negative is a first-class return value), `under-claim-the-solvable-class` (**when capability and confidence are inversely ordered, ambiguity resolves to the least capable class** - the evidence for the tractable classes is exactly the evidence an adversary can fabricate), `holder-reconstructed-binding`, `rung-trust-does-not-promote`, `no-surface-no-rung`. **Amendment**: `document-text-extraction/recognition-boundary-and-escalation` - its rule is *escalate the refusals, never the format*, and its whole argument is about WHICH verdict to route on; the precondition that a verdict exists was never stated, and the tree found two populations where it does not (a connection that dies before any status; a response that clears the emptiness guard with chrome while the substance mounts later - **a verdict of success, which is worse**). The amendment adds a named pre-route and spends most of its length distinguishing it from the category routing the technique rejects: membership enumerated from observation not predicted from a property, operator-extensible at run time because the set is *discovered*, and a bypass rather than a policy. Found by asking whether its `When not to use this` enumerated too few. **`device-pairing/token-binding-and-transport`** gained one reciprocal paragraph: it is complete from the *granting* side and the holder's half - a credential bound by its issuer to a fingerprint never disclosed - is a mechanism it never had, so per the v2 rule it landed as a technique elsewhere rather than as an amendment there. **Applied 1, shipped 1**: politicas `7eac8cb5`, `code`/`better`/`ab-paired` - the seam contained a defect (a `!res.ok` throw inside the try that the network-error catch retried), so a 403 cost 3 upstream requests against a public civic-data host that had just declined; arm B issues 1, pressure and broken unchanged at 3. **Fleet reach: 0 landings unapplied for want of a seam.** **0 of 3 fetches** - fourth consecutive practitioner-codebase run to spend none. The forge worker overrode its brief and dropped a sixth technique with an argument (sixth consecutive dispatch to override and be right). Directions **skipped**: `fleet-map.json` carried a sibling's uncommitted edits and regenerating a shared artifact mid-flight is the one thing the parallel rules forbid. 2 siblings live at claim, 0 by Phase 9, no contention. |
| 2026-09-04 | `github:dullage/flatnotes` @ `7f5b773` | **vendor repository** - small, mature, single-author; 523 landing-page words against 4,222 in the server tree, so the ingest alone would have returned 11% of the source and all of the marketing. **No `docs/`, no ADRs, no tests, no CHANGELOG - and a full seven-entry design record anyway**, because the *contracts* were legible: three abstract base classes, a config object whose every field validates-or-exits, and a models module. **Where a tree has no design documents, the abstract surfaces ARE the design documents, and they cannot hedge because something compiles against them.** **Re-scan when** a second source shows the boot-patch-the-built-artifact shape (banked lead), or when a fleet project needs subpath hosting from a single build | 523 landing / 4,222 in-tree | 11 (7 design + 4 claim) | 1 | 1 | 0 already covered / 7 untriaged | [[2026-09-04-flatnotes]] - **found by the enumeration hunt against a passage that had already done the hard thinking.** `editor-interop:78-94` lists the watcher's silent failure modes one by one, concludes they turn the time bound "from prudence into the load-bearing mechanism", and demotes the watcher to "an optimization that usually fires first" - a confident two-member enumeration, and confidence is what makes an enumeration worth one question. The source held the third member: **bind the rescan to the READ** and neither is needed, because the staleness window collapses to the query itself. The subject modelled *how* a mirror is rebuilt in depth and *when* almost not at all. `read-triggered-reconciliation` also carries two halves the source does not argue: **the stamp lives in the mirror, so there is no ledger** (which removes the `gate-sees-target` confession `mirror-indexes` correctly owes rather than restating it), and **the comparison is inequality, not ordering** - justified from `replicated-substrate`, which the corpus already owns: a checkout or timestamp-preserving copy installs an OLDER file, and a `<` gate reads that as "the mirror is ahead" and serves the superseded derivation forever. The tree uses `!=` and never says why; **the corpus supplied the reason, which is this run's clearest case of the registry being stronger than the tree it read.** Routing count on both v2.2 clauses: whole-tree NONE=3, per system max=2 (A=2 B=0..1 C=1 D=0), and the three NONE do not share one HOME IF NEW (two into markdown-vault, one into browser-credential-boundary) - **neither clause fired, no handoff.** The count changed what was EXTRACTED and not only where it routed (round-10 focus 2): it re-aimed the sweep onto the notes/search module and away from the client, and the reconcile loop that became the run's whole yield **is not mentioned in the README's feature list at all**. **Applied 1, shipped 1** - and **the seam contained the defect** (round-10 focus 3): personas keeps a context index whose `reconciledToSha` was written by `touch` on every reconcile and resolved by nothing, so its pre-push gate computed staleness purely from the uncommitted working diff - the technique's first TWO failure modes, live, in the first tree tried. **The recorded stamp does not resolve to a commit in the repository at all**, and the root CONTEXT.md was last committed 2026-06-11 with 4,215 commits touching 8,788 files since; arm A had reported none of it for three months. A=1 warning/strict-0, B=2/strict-1, same command same repo. **Fleet reach: 1 of 8 projects carries this seam** (stated though small, per the focus). Two defects caught INSIDE the change and recorded: `git rev-parse <sha>^{commit}` is not portable through cmd.exe (`^` is the escape character, so every VALID stamp would read unresolvable - caught only because the resolvable branch was exercised deliberately rather than inferred from the failing one), and the git helper leaked stderr so a healthy run printed a bare `fatal:`. **0 of 3 fetches** - fifth consecutive practitioner-codebase run to spend none; corroboration was training-data convergence plus the corpus itself. 2 siblings live at claim, no contention. `index.json`/`catalog.json` left uncommitted - they reference a sibling's `remote-capability-probing`, not in HEAD. |
| 2026-09-04 | `github:duckdb/duckdb-wasm` @ `def100b4` | **first-party practitioner codebase in repository form - a system** (an analytics engine compiled to a browser sandbox: C++ core, TypeScript control layer, comparative benchmark suite, carried upstream patches) | 626 landing / 1,533 total in-tree md / ~18.6k C++ + ~17.2k TS | 13 | 4 | 1 subject (6 techniques) + 1 technique + 2 applications | 2 | 6 untriaged | [[2026-09-04-duckdb-wasm]] - **the tree has 1,533 words of markdown and no `docs/`, `design/` or `ADR/` at all**; every load-bearing decision lives in source header comments and header files (a 40-line I/O-stack diagram above the buffer manager with its forces in prose, the probe ladder documented inline in the runtime). Second consecutive run where the operating documents were inside the source - the sweep order is right, the tell is a header whose comment-to-code ratio is inverted. **Routing count per system A=3 B=3 C=1 D=1 E=1 F=1; System A fired the per-system clause at exactly 3 sharing one home-if-new -> scoped subject forge in-session, the system and not the repository.** The count **changed what got extracted, not only what got routed** (round 10's check): after computing it I stopped sweeping the shell and app packages and spent the rest of the sweep inside the HTTP lane and the buffer manager, which is the only reason the defect and rows 5-7 exist. New subject `backend-platform/resilience/remote-capability-probing`, and **the argument that made it a subject was a pair of denials**: `health-checks/probe-design` denies it ("a probe that runs an expensive representative workload is measuring performance, a different discipline; health asks only *does it work at all*") and `optional-dependency-degradation/probe-the-grant-not-the-config` denies it ("the narrow rule here is about the *input to the branch*: the grant, not the config", its whole frame a dependency you own and can harden) - neither denied too much, each correctly excluded a case nobody then owned, leaving the stage *choose the access protocol for a remote you do not control, before the first real read, at a cost*. `adaptive-fidelity-tiers` owns the same slogan for the local device where the probe is free. The worker overrode the spec twice and was right twice: technique 4 stays out of `fallback-retirement-condition` because that technique's gap is expected to CLOSE and this one never will (reaper vs ceiling are opposite instruments), and techniques 5-6 stay because the probe's real output is a latency class and a subject producing a class nobody reads is half a subject. It also corrected four spec anchors and found the assertion switch means "skip the *disambiguating* rung", not "skip the probe" - which is why it is the one of three safe to scope per peer. **The design read found a defect in the source**: `force_full_http_reads` is `nullopt` at both declarations and both serialization sites read `.value_or(true)`, so the unconfigured default emits the bypass ON and every remote read takes the whole-object path - the capability the README leads with, disabled by its own default; the enumerating test asserts the other two switches and, being a *presence* assertion under the empty configuration, would pass even against a wrong value. Second technique `handicap-disclosure-in-the-result-row` from the benchmark suite (a vendor publishing a comparison it WINS carries, per result row, the concession naming how each competitor's workload was weakened - and the carriage is the mechanism: a typed field that survives into the published set so no rendering path can show the number without it). The worker corrected two of my tree readings and both verified: the concession clears for exactly four query ids so it is per-benchmark not per-system, and the cell renders `{value} *` at rest, not hover-only - the asterisk is load-bearing because a hover caveat dies on screenshot. Apply 1, ship 2. **0 of 3 fetches** (sixth consecutive practitioner-codebase run at zero). `directions=skipped` with a reason: regenerating `fleet-map.json` would derive a shared committed artifact from an index that references two siblings' uncommitted subjects. `index.json`/`catalog.json` **left uncommitted** - 13 and 7 references to two siblings' work not in `HEAD`. Board read 0 siblings at Phase 1 and **2 by Phase 7**; the gate was red throughout on one of their subjects and zero findings named mine. |
| 2026-09-04 | `github:dullage/flatnotes` @ `7f5b773` **(second pass — rows 2 and 5)** | Same source, re-opened after the operator picked two rows the first pass had left untriaged with a `real gap` read. **The pass is the evidence for round 11's declared focus**: the rows were not marginal, they produced a second technique, an amendment, three applications and a second fleet commit, and they were sitting in the untriaged table only because a one-question gate returns one number | — | (from pass 1) | 2 | 1 technique + 1 amendment + 2 applications | 1 | 3 (2 struck from untriaged) | [[2026-09-04-flatnotes]] — **one row changed shape on verification and one changed the corpus back.** Row 5 → `settings/presence-decides-precedence`, a **mechanism** not an amendment: `key-registry` answers a rename with a migration and never states its precondition — *the application can write to the store* — which is false for an env block, a mounted file or an orchestrator manifest, so the middle step does not exist, both names stay live, and only precedence is left. `cross-source-precedence-chain`'s closing refusal (*order that depends on the value is a policy engine, not a chain*) is right and **assumes value-dependence was chosen**; this is the case where nobody chose it, because `typed-accessors` had already collapsed unset and set-to-the-default into the same bytes. Second half: the deprecation notice fires only where the old key WINS, so the operator who migrated and still carries a stale line is the one population never told. Row 2 → **downgraded from `real gap` to an amendment**, honestly: `credential-attachment-at-the-hop` already opens on the same protocol fact and already ranks the auto-attached credential first (uncapped absence check, 18 files, all read). **The amendment was then refuted by its own apply step** — gravity's asset route reaches the identical constraint and takes the query string, correctly, because it composes the URL per render and the value is already a public bundle credential; the discriminator was corrected from *content-issued vs app-issued* to **who owns the URL string and whether it outlives the credential**, and a third tree (kp, a markdown renderer with no image syntax at all) added *run the enumeration even when you expect it to be empty*. **Applied 2, shipped 1**: goat `323b1bd` (`code`/`better`/`ab-paired`, 2-of-4 → 4-of-4, 285/285 suite) where the seam again held a defect with a security half — the probe **spent** a bundle-named variable in an outbound `api_key` — and gravity (`simulation`/`not-better`/`structural-only`). One design-record entry (C1) **corrected in place**: its `corpus: NONE` was wrong, because the design read hunted the decision and missed the subject owning the constraint behind it — a streaming-proxy slug does not look like an attachment question. 2 siblings live, no contention. |
| 2026-09-04 | `github:h4ckf0r0day/obscura` @ `14ce5178` | **vendor repository, design-deep - a system** (a nine-crate headless browser engine: embedded third-party script engine, own DOM, own cascade/layout/paint, devtools-protocol server, automation server, fingerprint-consistent transport) | 2,719 landing / 18,997 in-tree md / 123,259 lines of engine source | 10 | 5 | 1 golden-path correction + 5 techniques + 3 applications | 2 | 0 untriaged, 1 lead | [[2026-09-04-obscura]] - **the run found a denial that had gone too broad, in a subject forged the previous day.** `guest-execution-bounding`'s "What the naive reading gets wrong" opens by rejecting the watchdog in three clauses, and its neighbour `capability-subtraction-sandbox` puts wall time on the *uncounted* list; together they read as *wall time is uncountable, therefore uncapped*. That position holds only where the host **owns the dispatch loop**. This source embeds a third-party script engine, can insert no counter anywhere, and answers all three objections instead of falling to them: nothing is killed (a thread-safe termination handle makes the engine unwind itself at its own safe point, the same unwind a counted breach produces); the non-determinism is conceded and reframed, because this is a **liveness** ceiling and not a correctness limit; and the single-threaded constraint is on *guest execution*, which a thread that only sleeps and calls one function never touches. **The thesis was scoped to its precondition rather than replaced**, and the neighbour's counted/uncounted pair was given a third category - *externally bounded*. **Routing count 22 decisions / 20 unmodelled across five systems**; system A had an existing home so it landed here, systems B-E (reference fidelity, protocol drop-in compatibility, egress under attacker-supplied code, identity consistency) hold 17 unmodelled decisions across four distinct force-sets and went to a **forge handoff**, not four loose techniques - filing those as techniques would have been exactly the routing miss the depth column exists to catch. **Round 12's declared focus met on both live items.** Three falsifiers were run in-run and **two refuted the run's own hypothesis**: the fleet does *not* leak detached children wholesale (the target project had already found the mechanism independently), two files a file-level scan showed as unguarded both kill explicitly, and the target's compile failure was proven pre-existing by stashing the seven changed files and reproducing it identically - attribution by experiment, not assertion. And the run produced the **independent-convergence** instance round 11 asked for: the mined engine wired external termination to stop a runaway page wedging a worker; personas wired the same mechanism to stop a timed-out subprocess continuing to bill a user's account, its comment stating the semantics in the same words (*without the guard the runtime **detaches** the child*). Two systems, unrelated symptoms, neither aware of the other. **The vocabulary gap is now a lesson**: `better`/`not-better`/`unmeasurable` has no cell for *the tree already does this, independently*, and this row only says `better` because 8 residual sites did change - had coverage been complete the correct verdict would have been unsayable. **Applied 1, shipped 2**: 16 files in that tree already guarded, 5 more killed explicitly on the timeout branch, **8 had neither** and a fired ceiling returned a clean error while the process kept running (the subtlest spawns its child *inside* the async block the 300s ceiling drops, with a kill only on the cancellation path). Measurable 8 -> 0; `a659dff78` + `b5fac8fed` on master, not pushed. **0 of 3 fetches** - seventh consecutive practitioner-codebase run at zero, and the one external fact the run needed was answered better by the target project's own prior investigation than a vendor document would have. 1 sibling live throughout, no contention. |
| 2026-09-04 | `github:langchain-ai/open_deep_research` @ `1b7d2e80` | **vendor repository, design-BEARING not design-deep** (a deep-research agent: clarify, brief, model-driven supervisor fan-out to parallel sub-researchers, per-researcher compression, report) | 1,061 landing / ~11,400 in-tree prose + prompt artifacts / 45 files | 13 | 5 | **2 techniques + 1 amendment in `llm-agent/orchestration/fleet-orchestration`, 1 technique in `llm-agent/evaluation-and-cost/eval-harness`, 1 amendment in `llm-agent/prompt-and-context/tool-result-economy`, 3 applications** | 1c/0e/1s/2t | 1 | [[2026-09-04-open-deep-research]] - **the run where the routing count said DO NOT hand off, and was right.** 14 decisions across 4 systems, 4 unmodelled, no system and no home-if-new cluster at three: a compact practitioner account in repository form, not a system, and landing stayed in intake. The most useful structural finding was a re-homing: two orchestration candidates mapped to `agent-chaining`, whose golden path splits the world into an orchestrator-driven pipeline (whole graph in hand) and an event-wired peer-to-peer chain (topology implicit in subscriptions) - and a **model-driven fan-out is neither**, because the topology is decided per turn and exists nowhere before it. It lands in `fleet-orchestration` instead, whose dispatcher/worker/brief/harvest model survives the dispatcher being a language model; every technique there that assumed a program at the dispatch door is now a candidate for the same boundary, and the `parallel-dispatch` amendment (refuse into the requester's context rather than queue, because a model turn cannot survive the wait) is the first one found. Two of the five landings came from the source getting it WRONG - the decision-level eval that is labelled for the first fan-out and probes the last while paying for the whole pipeline, and the summarizer whose timeout fallback returns the raw 50,000-character page - which is the class the method says to keep. Zero of three fetches (eighth consecutive zero-fetch run for a source carrying its own primary material). Applied all five to `pumper` at the operator's direction: one shipped `code` row whose falsifier was RUN before the change (a 200-turn request charged 96 and no field said so, because a literal step cap and a published turn budget were never derived from each other), two `task` plans committed in the project, and one `simulation` that came back **not-better at two of three real sites** - the tree already caps its fallbacks and one of its ceilings states the amendment's disclosure rule in its own source comments and refuses rather than truncating, which is stronger than what the amendment asks. |
| 2026-09-04 | `github:Panniantong/Agent-Reach` @ `da5044d2` | **vendor repository, repo form, design-deep** - a Python CLI giving an agent read access to 13 platforms without API fees, which it achieves by borrowing the operator's own logged-in browser sessions. **The premise is what makes the documentation load-bearing**: a tool handling live session cookies for six platforms has guarantees that are only expressible as sentences. No `docs/design`, no ADRs - and the design record came from a **235-line policy test suite** that asserts prose, which the README does not mention. **Re-scan when** a fourth run finds a design record in a non-obvious surface (three sightings now: source header comments, abstract base classes, policy tests) | 1,276 landing / 15,459 in-tree md / ~17k LOC | 18 decisions + 11 rows | 3 | 3 techniques + 5 applications | 5 already covered / 3 untriaged / 1 lead | [[2026-09-04-agent-reach]] - **a technique family filling a stage ONE that all twelve of `docs-sync`'s walls had assumed away.** Every wall discovers the source a document is *about*; that presupposes the claim HAS a source area, and a promise about what a system does NOT do has as its truth-maker the fact that **no code exists**. `negative-claims-are-pinned`'s strongest argument came from the subject's own prior work, not the source: **wall 12 had already found the permanent-`unverifiable` category for figures**, and the two resolutions are opposites - a figure cannot be *read* and is fixed by digesting its inputs; a promise has *no inputs*, so the comparison cannot be inverted and the mechanism must change. Two rot paths, neither a source change (somebody builds the thing; somebody deletes the sentence, because a negative claim reads as boilerplate to every editor who did not pay for it), and the honesty limit that decides the design: **a pin proves a promise is STATED, never that it is TRUE**, so its admission ticket is a dated human review. `prose-as-an-execution-surface` moves a document from something that can be false to something that **runs**: the source's index name is **owned by an unrelated project**, so a bare install line in any of four landing pages delivers a stranger's code under this project's own words - and the durable half is the scope, since the check's population is the prose corpus **plus the integration entry point that prints its own install instruction to stderr**, the only instruction reaching a user who never opened a page. `translations-drift-against-the-product` states a seam rather than a correction: the mature pipeline pins a translation to its SOURCE, and **staleness relative to a stale source is zero** - retire a capability, leave the primary page, and every hash matches while every locale is false; a page authored *directly* in a target language has no source unit at all. The tell is that **the assertion set differs per page** (two retired platform names forbidden in exactly two of four landing pages), and that asymmetry is the only written record anywhere in the repo of which pages were authored independently. **Routing count 18 decisions / 7 unmodelled across 4 systems; system D fired the per-system clause at 4 with an EXISTING home**, so a technique family and **no forge handoff** - and the count changed what was extracted, sending the sweep out of six near-identical channel adapters and into `tests/`, where the whole yield was. **Applied 2, shipped 3**: personas `code`/`better`/`ab-paired` where the seam is the product's central commitment (*never leave your machine … no telemetry … no cloud account*) and the project's **eleven-check chain is structurally blind to it, permanently** - arm A detected **0 of 4**, arm B **4 of 4**; the falsifier was **run uncapped rather than banked** (all 7 references to the page across `scripts/` opened, none reads its content), and the apply produced a structural fact the technique demanded into existence: **the promise lives in exactly one document**. Second row `simulation`/`not-better`/`structural-only` - the population claim confirmed (the app emits install hints from compiled source, outside every doc gate) and the vector absent because the fleet owns every namespace it names, **the technique's own disqualifier, so no amendment is owed**; return condition sharpened to *a rename creates the vector*. Third technique **unapplied for want of a seam** - no project ships prose docs in a second language. **6 catches**, incl. one against my own table: `probe-design`'s proxy table opens with the exact content of this source's `probe.py` docstring, said first and said better. **0 of 3 fetches** - eighth consecutive practitioner-codebase run at zero. `directions=skipped`: `fleet-map.json` carries uncommitted changes. `index.json`/`taxonomy.json`/`catalog.json` **left uncommitted** - 12 references to a sibling's two techniques not in `HEAD`. 0 siblings at claim, 1 by Phase 7, no contention |
| 2026-09-04 | `github:jetkvm/kvm` @ `3f7c709` - "Control any computer remotely" | **vendor repository read as a system (v2)**: an embedded appliance firmware, ~27,000 lines of Go plus a React UI, with unusually thin docs (5.6k words of markdown), so the yield sat in the linter, the release Makefile, the config schema, the test names and two package doc comments nobody links to. Routing count 13 decisions / 6 systems / 8 unhomed; system A cleared the per-system threshold. **The count said forge and the count was misread**: both prior-art maps missed `backend-platform/resilience/self-healing`, which shares a slug with none of the twenty terms queried and is the subject whose stated job the decisions answer - found by reading the category listing in `taxonomy.json`, which moved the landing from a new subject to a technique triple in an existing one. **Re-scan when** the tree documents why it rejected the platform network stack (`nmlite`, the run's largest unexplained decision), or when a second appliance tree makes the same rejection | 730 landing / ~5,600 in-tree md / ~27,000 lines Go | 12 | **5 techniques / 1 amendment / 4 applications** | 0 declined | 3 | [[2026-09-04-jetkvm]] - the two prior-art maps **disagreed and both were wrong in the same direction**: concern-phrased returned `PRIOR ART: none` for "failsafe", forces-phrased ranked `fault-signal-propagation` and `retry-backoff` first, and the real home was neither. `self-healing`'s five commitments all assume a healer that outlives the attempt; the configuration where the healed component is *mandatory* terminates the healer with it, and `retry-backoff`'s stated destination for an exhausted ladder has nothing to route into. Landed the triple (promotion written before the exit, declared marker over inferred stack trace, consume-once file-as-channel), `far-side-oracle` in `test-harness` (the oracle belongs on the system the product effects, not inside the product), `bypass-is-a-versioned-policy` in `signed-artifacts` (the first `go` application in that subject), and a boundary amendment to `updater-chain` (synthetic baseline beside the shipped one; they localise faults differently and only one can catch a break in the old reader). Applied `code` and **shipped** to gravity: its worker-pool give-up was unreadable by its own liveness probe, and the chart gate's remediation text prescribed the defect - 0/1 -> 1/1 correct pod replacements, 0 false. Three of four findings unapplied for want of a seam anywhere in the fleet |
| 2026-09-04 | `github:comet-ml/opik` @ `4ef186c6` - "Debug, evaluate, and monitor your LLM applications" | **vendor repository read as a system (v2)**: an LLM tracing/evaluation platform, 11,266 files, whose densest first-party surface is neither its product nor its README but its `.agents/` operating documents - ~85,000 words across 80 files (a landing page of 2,799, so ~30:1), holding 15 skills, 13 commands, 6 subagents and a rules set for three agent hosts. **Re-scan when** a second repository declares a foreign vendor's instruction directory an ignored surface (the run's one lead), or when the `llm-observability` bundle wants a second independent stack - this tree is one and the run did not spend it | 2,799 landing / ~85,000 in-tree `.agents` md / 11,266 files | 8 | **3 techniques / 1 amendment / 1 application** | 0 declined | 1 | [[2026-09-04-opik]] - **the routing count is the headline, and it says the corpus has outgrown this class of source.** Six unhomed decisions across four systems, per-system maximum three, and the second clause finds no three sharing one *new* home **because every home already exists** - `agent-instruction-files` (13 techniques), `pipeline-authoring`, `untrusted-extension-host` (forged the previous day), `metric-surface-contract`. A system of this size and quality now lands entirely inside subjects the registry owns; no forge handoff. **The strongest row inverts a stated default.** `single-source-topology` says the link/copy boundary is the edge of a checkout and that a repo copying where it could link "has invented a consistency problem it did not have". This tree symlinks the host that accepts the canonical bytes and *generates* a tree for the host that needs a different rule-file extension and a renamed frontmatter key - so the copy regime begins at a **format edge**, inside one checkout, and the amendment names it as a second boundary with the same drift obligation. **Its evidence is structural and negative**: the repository derives its entire lint matrix from one config file so local and remote gates cannot disagree, and applies **no drift check at all** to the tree it generates - verified across all 82 workflows and the hook config with the instrument asserted against a known positive first. The drift rule exists only as an anti-pattern bullet inside a subagent's prompt: a checkable condition promoted into advice, by a repository that demonstrably knows how to build the gate. The amendment also absorbs the **half-generated directory** - a sync step that preserves destination files with no source counterpart, so "regenerate and compare" has no defined answer - as `machine-owned-regions` owed at directory granularity. **3 techniques**: `foreign-config-replay` (a plan generator whose input is another tool's config becomes a second implementation of that tool's selection semantics - replay the matcher rather than approximate it, declare the modeled subset and **fail closed at config load** while still failing open at runtime, and address a generated unit by the tuple that isolates one entry because a tool's own ids are not unique in its config); `host-api-import-budget` (the host's own SDK is charged to the extension's clock - ~2.5s of a 9s timeout under pool contention, so the limit is a total and not an extension budget; the substitution registers under the real import path and must cover **both** resolution routes, since a stub in the module table misses dotted submodule imports, which are the ones that motivate the fallback); `fault-localizing-metric-set` (diagnosability is a property of the SET, which the per-metric admission bar structurally cannot see - a producer counter is redundant in the healthy case and decisive in the broken one, because without it a consumer's zero cannot distinguish an idle system from a lost hand-off). **5 catches**, incl. the derived CI matrix and its skipped-check list (`change-scoped-work-selection` owns both, with the three-outcome rule) and "success is derived, never double-counted" (`export-terms-not-ratios`). **Applied 4/4, shipped 1**: `foreign-config-replay` came back **better** on personas at n=11 - 3 local checks enforced nowhere else, including a secret scan whose own comment says CI catching a leak is already a leak - and the first step shipped to branch `intake/foreign-config-replay`, a parity checker that ships **red** deliberately. Two rows came back `not-better` and both are confirmations rather than defects: kp already sits correctly in the link regime the amendment's question routes it to, and tracklight's client has zero runtime dependencies so the import-budget technique's own escape clause fires. **The run's methodological catch**: the first parity measurement grepped workflow files directly, reported 9 gaps and was wrong about 6 - CI invokes npm wrappers, not script paths - and it was caught only because the instrument carried two known positives and a known negative. The assertion then failed on a *negative I had verified by hand*, and the instrument was right and the hand-check wrong. **Declared focus (round 10) answered**: the category listing was read for all four homes before any absence was believed, and it changed nothing this time - the maps and the listings agreed, which is worth recording as the case where the extra read costs one command and buys confidence rather than a correction. **0 of 3 fetches**; a first-party codebase corroborates corpus-internally. 2 siblings live at claim (fluxer, everywhere), no shared subjects |
| 2026-09-04 | [cargo-make](2026-09-04-cargo-make.md) | practitioner build-tool repository | 10 extracted / 6 accepted / 4 untriaged / 0 leads | 3 techniques + 2 amendments; routing count written, forge trigger NOT met (largest homeless cluster 2); 5 applied rows, 2 shipped |
| 2026-09-04 | [GameDev-Resources](2026-09-04-gamedev-resources.md) `github:Kavex/GameDev-Resources` @ `f7c89aa` | **reference index - tool-directory sub-class** (356 links / 4,245 words, the ratio inverts as the lane predicts, but ~90% of the references are product landing pages that strip to nothing, so the reference yield was called at zero before any was read and no wave was cut) **+ a design read of the four non-README files and 495 commits of history, which is where the whole run came from** | 4,308 landing / 4,487 in-tree across 5 files / 495 commits 2014-2026 | 10 | 2 amendments + 3 applications + 1 script + 1 CI job | 0 | 2 | 3 untriaged / 3 caught | Routing count **2 unhomed in one system - stayed in intake, no forge handoff**, and the two NONEs do not share a home so the XL trigger did not fire. Landed: `three-state-outcomes` gains remediation-per-state (destructive remedy needs a stronger predicate than the display verdict; 95% false-deletion measured), `gate-liveness` gains the gate whose observable is absence rather than green (65 content commits accepted under a gate dead ~6 years; its own tuning is the only evidence it existed). Applied to this registry: `scripts/check-citations.mjs` + a weekly `schedule:` on `knowledge.yml`. **Re-scan when** a second curated index is mined (the curator-boundary lead), or a source shows in-repo vs in-config suppression lists being chosen between (the one-day `IGNORECHECK.md` lead). |
| 2026-09-04 | [kdenlive](2026-09-04-kdenlive.md) `github:KDE/kdenlive` @ `b7124d9` | **first-party practitioner codebase read as a system (v2)**: a desktop non-linear video editor over a separate rendering engine, 572 C++ source files, whose landing page is 637 words against ~6,400 of `dev-docs/` plus the code that implements what those docs name. **Re-scan when** a second tree implements the emitted-inverse model *and* needs a persistable history - that pairing is the one case the new technique's boundary cannot price from a single instance | 637 landing / ~6,400 in-tree md / 572 C++ files | 10 | **2 techniques + 1 subject (6 techniques) + 2 golden-path corrections + 3 applications** | 0 | 2 | 3 caught / 0 untriaged | **Routing count: 4 systems, 6 unhomed; system C (the project document format) at 3 with no home fired the handoff, scoped to one subject** - spec written, one forge worker dispatched and reviewed in-session, `integration/native-document-format` landed with six techniques. **The two intake-retained landings are both incomplete enumerations in mature subjects.** `undo-history` opened "There are **two** ways to make an action reversible" and priced exactly two; the tree runs a third - the forward operation returns its own inverse as a composed closure, capturing the overwritten value at the instant it still existed (294 accumulator signatures, 185 composition sites in **both** directions at 6:1, 38 places where the accumulated closure is also the partial-failure rollback path). Its price is that the history becomes *unreadable* - a closure is not data - and the structural fact nobody designed is that this tree has no collaborative editing, no cross-process undo and no durable history anywhere, because after that choice it could not. `seed-is-not-a-reproduction` closed with "**three lanes**, and they are not interchangeable"; all three are *authored*, and the tree runs a fourth - one reflection table names the model's mutating operations, the fuzzer composes from it and the logger records against it with depth suppression, so a user's crash and a fuzzer find minimize into the same artifact (**448 of the suite's 775 whole-model invariant assertions live in the machine-generated regression file**, more than all fourteen hand-written suites combined). **3 catches, all of them the corpus winning**: the 194-line `checkConsistency` asserted 775 times is `inside-out-invariants` down to its "expose a checking entry point, not the state" step (and the tree reproduces its predicted under-investment - the cross-component direction is behind a boolean parameter defaulting to false); the single named test-friend facade in 15 classes is `flag-gated-debug-global`; the 10-type x 7-status broken-reference matrix is `defect-class-to-remedy-map`, which models it better. **Applied 2/2, shipped 2, and the apply step changed one landing's evidence.** goat's hand-written inverse carried a comment saying the displaced item could not be restored "without more info" - the plan builder already captured it, so the claim of information loss was false and the real cause was a reversal written in a different function from the mutation, which is exactly the technique's thesis; A=1/4 -> B=4/4, shipped. personas came back **not-better**: the property generator drew fps from three friendly divisors while the compiler accepts any fps > 0 and snaps to 1/fps, but widening it found **0 failures in 8,000 cases**, so the cheap alternative was sufficient and no field lane is earned - ten of eleven trees have no generator at all, checked at manifest level, which is a searched absence and not an unattempted row. **The forge worker corrected the director twice**: `verified_against: cpp@qt-6.10.0` named a framework rather than the stack (the tree witnesses `CXX_STANDARD 14` at `src/CMakeLists.txt:310`) and a cross-subject link was one level short; it also refused a decorative law citation the spec had mandated and argued why. **0 of 3 fetches.** Index and catalog regenerated under the lock but **left uncommitted** - a live sibling's six uncommitted subjects are in the artifacts. 1 sibling live at claim, a second joined mid-run |
| 2026-09-04 | [copilot-cost-efficiency](2026-09-04-copilot-cost-efficiency.md) `web:github.blog` - "How we make AI coding more cost efficient without sacrificing task quality" | **first-party practitioner account**, unusually design-dense for a blog post: two named engineers on their own harness, four independent A/B experiments on one credit metric, one shipped negative and one rolled-back regression, so the design read (Phase 2d) was run with quote anchors instead of file:line. Routing count 6 decisions / 1 system / 2 unhomed; XL trigger not fired and no handoff possible - an article is not a tree. **Re-scan when** the same vendor publishes on its compressor policy or its recovery-path instrumentation; untriaged rows 1-2 are the two unhomed decisions and both already have a home named | 2225 | 10 | 1 technique + 1 amendment + 1 application | 0 | 1 | 3 caught / 5 untriaged | [[2026-09-04-copilot-cost-efficiency]] - operator focus was **library skills and their design**, and the pick was row 3. **The subject had already drawn the enumeration this extends.** `sibling-floor-ownership` states that the admission funnel "is never run per install, because an install produces no diff for anyone to read" - the case where the diff is *absent*. `rewrite-behavior-pinning` is the mirror: a diff that is **total**. A bulk machine-authored rewrite leaves no line to withhold (`line-earning`), nothing to hold out (`substrate-coupled-expiry`), no per-line origin story, and a review that degrades into a judgement about prose while the property at risk is behaviour. `capability-coverage-contract` stays green through the whole failure, which is the sharpest evidence the gap is real: the capability was still named, and only the guidance about when to reach for it changed. The causal half was folded in rather than banked - **compression deletes hedges before facts**, because hedge tokens carry almost nothing about *what* to do and almost everything about *how strongly*, so an advisory compresses into a mandate and the agent complies with a policy nobody wrote. The source rollback is the instance (independent sub-agents ran strictly sequentially and nothing errored), and the corrective inverts the instinct: the fix was **shorter than the compressed version and less restrictive than either**. **Applied `experiment`, verdict `not-better`, and it is the most useful row of the run.** 12 always-loaded instruction assets across 7 repositories, 152 revisions, 35 bulk rewrites; a modality detector asserted against 3 known positives and 4 known negatives flagged **10 and confirmed 0**, all pairing artifacts. The technique predicted that failure and now carries the measurement. **Two structural facts nobody designed**: 24 of 35 bulk rewrites *grew* the file and not one was undertaken to cut per-turn cost, so the trigger has never fired in this fleet and the population was missing rather than the instrument - the largest asset being a 114 KB method file at 30 revisions never once reduced, a live instance of the accretion `line-earning` warns about; and the registry own skill gate asserts shape only and nothing about behaviour, so the run that landed the technique found its own tree unpinned. **Phase 7.6 ran and zero fired**, mechanically: the fleet map reports `absent: []` because all eleven projects already carry a context for this subject - and the diagnosis generalises, since the pass tests *presence* while this skill lands most often in `llm-agent/*` subjects every repository has, so the lane is inert by construction rather than skipped by luck. **0 of 3 fetches.** 1 sibling live (intake-exo); the gate went red mid-run on that sibling untracked subject - named, not fixed - and index/catalog were left uncommitted |
| 2026-09-04 | [exo](2026-09-04-exo.md) `github:exoharness/exo` @ `7801005e` - "an agent + harness architecture that is fully recursive, able to safely edit all aspects of itself at runtime" | **vendor repository, design-deep**, read as a system end to end. Four parallel design-read workers, one per system; **two independently reached the same finding** (the drain-marker claim/ack split), which is within-source convergence from readers that never saw each other. First run under the v2.5 scored admission gate: `auto=9/3/1`, `fp=0`, both promotion reads flipped their rows, one placement veto caught a good finding whose home was a full category. **Re-scan when** `fork_sandbox` is exposed to a turn-scoped handle (today denied by trait, and that denial is the whole self-experimentation gap); or the disclosed `TODO(storage-rework)` compare-and-swap lands on the agent-scoped stores; or a canary path appears; or 8 weeks elapse (2026-10-30) | 2,003 landing / 62,842 in-tree md / 93,945 lines src (69,289 rust + 24,656 ts), ~31:1 | 33 (design record, 4 systems) | 9 of 12 scored, 0 declined | 3 | 10 | **Routing count fired on one of four systems** (sandbox/checkpoint, 4 of 8 entries unhomed) and **that system alone was handed off, not the repository**: `backend-platform/work-execution/execution-state-checkpointing` forged in-session - 4 techniques + 2 source-tree applications, with the worker rejecting two proposed techniques with arguments, folding a third into one, and adding `resume-mints-a-duplicate` which was not in the spec (a machine resumed twice is two machines that both believe they are the original). The other three systems stayed in intake: three amendments (`termination-and-reaping` two-deadline ladder where a stop's receipt is observable, `operator-tier-code-loading` fourth tier for an agent-written configuration, `declared-verdict-over-inferred-wreckage` absence-inference as the same defect polarity-flipped), one new technique (`agent-memory/durable-store-failure-posture`), four applications of which **three are negative**. **The finding that decided the run**: the trust root - the single load-bearing claim in a self-modifying agent - is stated three incompatible ways (`README.md:31` the event log, `docs/RSI.md:46` the harness, `RSI.md:62` conceding it is a config default), the read-only mount mode exists in the API at `crates/cli/src/main.rs:1700`, and the canonical start mounts the whole tree including the harness crate read-write at `exo.sh:647`. The mechanism was there and the default path did not use it. |
| 2026-09-04 | [system-design-break-order](2026-09-04-system-design-break-order.md) `yt:EaXHfuHRWwg` — "Your App Will Break in This Exact Order" (JavaScript Mastery) | **second-hand survey, tutorial-explainer form** — the canonical single-server-to-sharded ladder, relayed competently with no system of its own; expected yield stated as near-zero for content before the triage table, and that is what it returned | 5,964 | 12 | 1 accepted / 1 lead / 1 untriaged / 10 caught | 1 applied (`experiment`, **not-better**) | 1 | **The catches are the run.** `resilience/scale-investment-timing` owns the source's entire thesis and *refutes its central pedagogical device*: "break it, then fix the piece that broke" is the naive reading `ceiling-as-deadline-not-trigger` names and rejects, because incremental substitution needs runway and runway is exactly what a team lacks at the ceiling. **Accepted row is a correction to that subject about itself** — its opening asserted the bundle "covers thoroughly" four mechanisms it summons; rate limiting, replication (×2) and shedding have subjects, **partitioning has none in any bundle** (no subject slug contains `shard` or `partition`; `data-layer` at 7/10 so there is room). Corroborated by a tree enumeration, not by the video. The XL row — forge the missing subject — was **escalated, not specced**: the gap is proven, but a tutorial explainer cannot author the content. Untriaged row (per-datum vs per-session staleness) had its promoting question executed and it resolved *against* the row: the corpus ejects the per-datum forward deliberately in three places. **Instrument note:** the concept maps never surfaced `scale-investment-timing`; a directory enumeration did, and an absence taken from the maps alone would have been the run's central error. 0 of 3 fetches. 0 siblings live. |
| 2026-09-04 | [rust-proc-macros](2026-09-04-rust-proc-macros.md) `yt:VIsKIzFz_zA` — "Rust's God Mode" (Code to the Moon) | **first-party practitioner account, tutorial-explainer form** — an educator building three procedural macros live. Expected yield stated before the table as **low, 1–2 rows**, and it held. The yield was not the API but the three constraints the author hits while building, one of which he demonstrates as a defect and declines to fix | 3,257 | 7 | 1 accepted of 5 scored (1 caught / 3 untriaged) | 1 applied (`code`, **better**, `ab-paired`) | 1 | **The accepted row is the case `io-free-core` presupposes away.** Its four properties all assume a test can *construct* the input; some types are minted only by the host that supplies them and have no constructor outside it, so "hand it in as a value" leaves the test where it was. New technique `mirror-type-at-the-edge`. The promoting question was executed on it (io-free-core's decision rule and its "when not to use it" both read in full, neither contemplates an unconstructible input) — a mechanism the corpus lacks, not a boundary case, so a technique and not an amendment. **Applied `code`/`better`** against a Next operator-console gate: verdict branches the project's own unit runner can reach went **1 → 4**, with arm A actually run (three of four threw `cookies was called outside a request scope`). **The tree amended the technique before it committed**: the draft said the shim "contains no branches"; the tree kept one guard, correctly, because reading cookies opts a route out of prerendering — so the rule is *no verdict branch*, not *no branches*. **My own premise was corrected mid-run**: `server-only` was not the blocker (vitest already stubs it); the ambient cookie accessor alone was. Rows 1/4/5 are three real mechanisms sharing one home-if-new — compile-time code emission — and `codegen` is emphatically not it (its subject is committed source derived from committed source; all seven of its techniques are inapplicable to an expansion producing no file). **Escalated E4, not forged**: one 16-minute introductory tutorial cannot author a subject. 0 of 3 fetches. 2 siblings live (prompt-assembly/mcp-tools), no overlap |
| 2026-09-04 | [The Harness Playbook](2026-09-04-stencil-harness-playbook.md) `web:stencil.so` — a harness author's postmortem of `omp` plus the architecture of its replacement | **first-party practitioner account in BOOK form, design-dense** — seven chapters, each a system with its forces stated, two appendices (one a complete TLA+ spec). The densest design source mined that is not a repository, and the first where the routing count had to be taken over an article. Container check clean: 21,593 words is real, not a decoded wrapper. **Re-scan when** the two least-built chapters ship as built rather than designed (the Director stack and the session DOM), or a second harness publishes a postmortem of extension-owned state; or 8 weeks elapse (2026-10-30) | 21,593 | 30 | **4 techniques** | 0 declined / 3 deferred | 2 | 3 untriaged / 9 caught | **Routing count 30+ decisions / 6 systems / 15 unhomed — three systems clear the per-system threshold and NEITHER clause fires a forge**, because every proposed home is the techniques/ directory of a subject that already exists (agent-runtime-assembly x4, untrusted-extension-host x2, then singles) and no three share a NEW home. Second run in two days to land that verdict: a first-rate system in this domain no longer produces a subject. A handoff was structurally unavailable anyway — /forge scouts a clone and this is an article. **The four landings are all incomplete enumerations in mature subjects.** `single-loop-authority` holds the continuation authority "to one value per session" and resolves conflict from a "closed set" of three, all of which work by ensuring the second loop does not exist AS a loop — and its stated safe default, `refuse`, is verbatim the source's postmortem anti-pattern; the fourth policy keeps both loops alive under a total order with one consumer per yield, and the one-authority law survives because the stack IS the authority. `guest-execution-bounding` opens by stating its own precondition (the guest runs on the host's thread, so the OS remedy is the host's own death) so the corpus is right where no terminable unit exists and the source is right where one does — a discriminator neither had written, alongside `cancellation-and-finalization`, which concedes the producer "may comply promptly, slowly, or never" and then spends every remaining step on the display. `amortized-compaction-cadence` opens "Two schedules" and both are triggered; the third forks below the threshold, compacts concurrently and splices, taking the stall-free property WITHOUT the per-turn prefix break. `structured-output` treats constrained decoding as a per-call quality choice with no notion of a provider-capped shared capacity where the request that breaks is not the one that asked for too much. **Applied 4/4, shipped 1**: tracklight already degraded the sibling guarantee in-band (determinism, 66 sites) and left schema enforcement on stderr — A=0 -> B=all call sites able to distinguish, shipped green; personas' cancel writes a terminal status over 27 un-abortable spawn sites (2 of 29 bind the handle, both in a unit test) while abort is used 15x elsewhere. **3 rows deferred on V5 contention** with mcp-tools, all three argued and anchored — the next run's cheapest re-entry. **0 of 3 fetches.** 3 siblings live at claim. **Instrument note: the board's `check` contradicted the board's own `list`** — a sibling's claim registered under a shorter path form matched nothing, so `check` returned clear over a contended subject |
| 2026-09-04 | [Claude Code Just Made CLAUDE.md Feel Obsolete](2026-09-04-claude-code-function-hooks.md) `youtube` — Ray Amjad demoing a preview-gated harness feature | **second-hand practitioner review** (a demo of someone else's release, thin first-party half; ~2 of 16 minutes are a course pitch). Expected yield stated as one currency signal, a lead or two and mostly catches — which held, with one method correction: the class rule says "the fetch carries the rest", and here **the shipped binary carried it**. The public docs describe five hook types; the binary's own schema defines six, two of which reach their verdict by asking a model (an "LLM prompt hook type" and an "Agentic verifier hook type", the latter defaulting to Haiku with a 60s timeout). Fetch budget 1 of 3 spent; the decisive corroboration cost zero. Board contention on `agent-runtime-assembly` resolved as an append under the `content` lock, with `check` again contradicting `list`. | 3791 | 12 | 3 | 1 | 7 | **1 technique + 2 amendments**, all three from underneath the headline — the headline itself is a catch on `enforcement-demotion`, which already measures what the video asserts. `agent-runtime-assembly/substituted-result-attribution` (a mutator surface's third power: `rewrite-before-the-gate` models rewrite in full and gives short-circuit one sentence, so nothing makes a gate see the effective *producer* the way it makes it see the effective arguments). Amendment to `enforcement-demotion` — a rule demoted into a model-backed hook changed location, not determinism; adds the destination sort. Amendment to `dated-capability-matrix` — a third axis (session) beside artifact and account, which defeats the **strongest** verification method rather than the weakest. **Row 1 required the superseded-claim carve-out round 19's focus asked for, applied deliberately and argued in the note.** Applied 2 rows (`1c/0e/1s/0t`), shipped 1 to personas, `ab-paired` on a two-arm probe (skills 24→25, commands 59→61, plugins 1→2 at one identical version). One lead, one catch worth recording: the video's best segment is `brokered-egress` verbatim. |
| 2026-09-04 | [Claude Is Not the Best Writing Model Anymore](2026-09-04-authority-hacker-writing-models.md) `youtube` — Authority Hacker Podcast | **dialogue**, hybrid: a first-party account of a blind-preference harness the hosts built (and a plugin splitting brief-building from prose generation) welded to a second-hand vendor-news survey in its last third. Routed per half; expected yield stated as 1-2 techniques plus currency and catches, which is what came out. Fetch budget **0 of 3** — every landing corroborated by training-data convergence or by code read in a fleet tree. 2 siblings live | 13358 | 15 | 4 + 1 XL | 2 | 5 | **1 technique + 3 amendments + a forged subject.** `eval-harness/pairing-schedule` (the comparison graph is a design variable: a bracket finds a winner but cannot rank a field, exposure balances across sessions not within one, connectivity precedes a total order) — found by the enumeration hunt, since `comparison-modes` lists five pairwise pathologies and all five are properties of a *comparison* rather than of the *set*. Amendments: `scenario-design` (prompt elaboration is a measurement decision, declared and version-pinned), `effort-calibration` (a tie is a property of the instrument; where the cheaper tier wins, file the entry under quality not cost), `turn-classification` (a fourth axis: which part of the deliverable a call produces). **E4 escalated and forged in-session:** `localization/craft/translation-quality-measurement`, a golden path and six techniques taking `craft` from one subject to two — the bundle could say what a correct translation is and where translations live, but nothing owned which engine produced them or how that choice is measured. Its authority is shared-task literature, not this video. **Apply: 3 of 4 came back `not-better` because the fleet was ahead of the corpus**, and the strongest of them wrote a second branch back into the landed technique |
| 2026-09-04 | [pgmq-just-use-postgres](2026-09-04-pgmq-just-use-postgres.md) `yt:KZw8et-e6vk` — "Forget SQS, RabbitMQ and Kafka. Just use Postgres." (Better Stack) | **second-hand practitioner review, thin** — 590 words, a vendor dev-content channel demoing a third party's queue extension. Expected yield stated before the table as **one content row at most plus catches**, and the class's "the fetch is the extraction" rule was *not* needed: the finding corroborated corpus-internally and in two connected trees. **The yield came from the source being wrong, not informative** | 590 | 7 | 2 accepted of 7 (1 technique + 1 instrument fix), 0 declined | 1 (`experiment`, **better**) | 1 (tracklight, gates green) | 3 caught / 3 untriaged / 1 lead | **The source's headline claim is false and the corpus refutes it in the file the map named** — a visibility timeout is a lease, not exactly-once delivery, and `guarantee-selection` opens with "Exactly-once delivery across a boundary is an illusion." **The finding was in what that refutation is made of.** `guarantee-selection` names the precise condition where acknowledgment and effect *do* commit atomically — "which holds only when both live in the same transactional store" — spends it entirely on debunking a vendor claim, and closes the door: *"There is no fourth row."* Meanwhile `data-layer/data-access/transactions-and-units-of-work` builds the **mirror of that same property** at full strength and calls it "the strong version": the outbox, where an effect's intent is durable exactly when the data is. **The registry owned the producing half as a mechanism and denied the consuming half as a posture.** New technique `co-transactional-consume` — a mechanism, not a boundary case, so a technique and not an amendment: for a handler whose every effect is local it deletes the persisted claim, the lease, the reaper, the retry counter and the stable-identity requirement, and inverts two of `atomic-claiming`'s rules. Its two costs had nowhere to live in the subject — the transaction held open for the handler's whole duration (a genuine tension with `data-access`'s lock-lifetime rule, recorded as a tension rather than resolved away) and **a concurrency ceiling measured in connections rather than consumers**, which is exactly the boundary the source's proudest segment hides (a 100-worker stress test against an idle store: ~11,100 msg/s on 2 CPU / 2 GB). It also splits a vocabulary the subject had inherited from the marketing it was rebutting: exactly-once *delivery* (impossible) vs exactly-once *effect* (ordinary transactional atomicity, bounded by one store) — the round-19 focus shape, a subject written in the vendor's frame rather than the literature's. **Applied `experiment`/`better` against tracklight and the evidence is a partition nobody designed**: 4 modules whose effects stay in the store carry **0** lease/fence tokens each, 3 whose effects leave carry **23/18/9**, nothing in between, across a dozen write domains, with **no document stating the rule** — 2 of 8 sites classified correctly under the old reading, 8 of 8 under the technique. Shipped the crate doc stating the invariant (`ed9e0d7`, `8be0bb9`, fmt+clippy green, not pushed); no behaviour changed and none should have. **Two catches are the corpus winning and one is a near-miss**: ascent's claim→run→stamp states the escaping-effect dilemma better than most corpus prose ("Write-then-work leaves a failed accept marked done. Work-then-write runs a double-click twice"), and tracklight's own 2026-07-16 scout documents a double-claim bug that is **fixed in HEAD** and was one step from being reported live. **Instrument: the board folded EVERY YouTube URL to one identity** — `normSource` drops the query string and a video's id lives in `?v=`, so this run's first claim was refused as a collision with an unrelated video; it failed in both directions (false collision on every video pair, missed collision between `youtu.be/<id>` and `watch?v=<id>`) and a check that cries wolf on every video teaches the operator to `--force` past it. Fixed with an 8-case assertion that pins the 2026-09-02 repository fold against regression; also narrowed a `catch` that was swallowing `ReferenceError` into a plausible-looking wrong answer. **0 of 3 fetches.** 3 siblings live; gate red mid-run on a sibling's untracked `eval-harness/pairing-schedule.md` — named, not fixed |
| 2026-09-04 | [vibevoice](2026-09-04-vibevoice.md) `github:microsoft/VibeVoice` @ `1541f59` — "Open-Source Frontier Voice AI" | **research-model release, four systems in one tree (long-form TTS with its code withdrawn, realtime streaming TTS, long-form ASR, streaming ASR) plus a serving plugin with two launchers**. Expected yield stated before extraction as 2-4 rows weighted toward design; that held, and **all four landings came from the design read, none from the landing page**. The densest artifact in the tree is a module docstring unreachable from the README. **Re-scan when** the realtime model's streaming-text-input TODO ships; or the streaming ASR path gains data-parallel support, which would mean the affinity constraint was solved rather than declared; or the withdrawn TTS code returns; or 8 weeks elapse (2026-10-30) | 1,284 landing / ~6,600 in-tree docs / ~700 KB Python over 44 files | 15 (10 design + 5 claim) | 4 accepted (`auto=4/0/0`, `fp=0`), 0 declined | 2 applied (both `not-better`), 2 `unapplied by construction` | 0 | 3 untriaged / 5 caught / 2 leads | **Routing count 10 decisions / 3 systems / 2 unhomed — neither v2.2 clause fires, no forge**, and for the good reason: eight of ten decisions home into four existing subjects across two bundles. **2 new techniques + 2 amendments.** `streaming-output/emit-behind-the-revision-window` came from the **refutation hunt** — the golden path's "when not to stream" list closes a non-monotone producer with two remedies and never asks *how far back the revision can reach*; where the reach is bounded, hold the emission cursor at `frontier - reach` and the stream is append-only by construction, with the emitted prefix **binding on the producer's retry** (an inversion of this subject's usual weak-live-tier posture). `voice-io/authored-voice-identity` gains a **fourth specification kind** against its stated "three": a voice shipped as the model's own conditioning state, whose durable/volatile pairing runs backwards — the timbre is durable and there is no specification — and whose safety property is that cloning is *absent* rather than gated. `deferred-operation-fusion/equivalence-oracle-testing` amended: it stands on the discrete-output boundary twice ("nearest should be exact", the label-image argument) without naming it, and **a tolerance on the array is not a tolerance on the result** where a discontinuous selector consumes it — the source's own measured instance is a resampler swap at ~1% RMS that changes words through a greedy argmax. `serving-process-topology/cache-residency-sets-the-balancing-unit`: the subject sizes replicas and never asks whether they are interchangeable, while its neighbour prescribes keeping cache coupling *out* of the balancer — neither covers the regime where the coupling cannot be dissolved. **Both apply rows came back `not-better` with the fleet ahead of the corpus, and both rewrote the landing**: a content-hash fingerprint measured **18.369 ms vs 0.005 ms** on the cache-hit path (3,700x) and produced the better rule (identify the artifact at install, not at use); and the project's agreed-prefix mechanism, which needs no knowledge of the corrector's reach, exposed that the technique conflated *what may be shown* with *what may be acted on*. **Seam scored at triage per round 21's focus and it changed the run** — two rows were known `unapplied by construction` before drafting, verified per project because the fleet map was stale. **Catch: `process-count-as-a-formula` predicted a live defect in the source** (a floored per-worker concurrency constant exported into each of N replicas — 64 x dp, and one-directional, so the documented advice to tune it down is silently ignored). 0 of 3 fetches. 0 siblings live |
| 2026-09-04 | [What is Agentic Testing?](2026-09-04-agentic-testing.md) `web` — Paolo Perrone, The AI Engineer | **second-hand practitioner listicle** — a concept explainer relaying four primaries, with a thin first-party half (five checks run three times each against a stub) and, unusually, **two first-party practitioner comments acting as a dialogue**. Expected yield stated before the table as 1-2 landings and mostly catches, against a corpus already holding a 23-technique `quality-gates` and a `context-starved-executor` that *is* this article's thesis at greater depth. That held exactly: 8 caught, 2 landed, 3 untriaged, 1 lead. Fetch 1 of 3, spent on the one candidate whose entire value was a number. 1 sibling live | 3133 | 13 | 2 accepted (`auto=2/0/0`, `fp=0`), 0 declined | 2 (`1c/0e/1s/0t`, both **better**) | 1 (this registry, gate green) | 8 caught / 3 untriaged / 1 lead | **Both landings came from the source's edges, not its headline — and one came from a commenter.** `quality-gates/renameable-detector-keys`: the sibling of `self-reported-gate-inputs` one step earlier, where the record is honest and complete and the **key the detector matches on** is something the author can change without changing what the check is about. Renaming is then the cheapest way to satisfy "make this pass", the finding disappears with nothing suppressed and nothing deleted, and extending the list buys one round. The distinction that decides the remedy is **accidental gap vs concealable key**; where no invariant key exists (anything turning on reference), the prescription is that the pass state its own predicate. Originated by a practitioner comment ("the agent making the finding disappear — a suppression, a delete, a rename that dodges the detector") converging with the article's own independent thesis that a locator's name is a fragile key — **one root, two signs, two voices**: an innocent rename is a false red, a deliberate one a false green. Amendment to `blocking-by-input-determinism` from the **enumeration hunt aimed deliberately** (round 22 focus item 2) at its own heading "Two advisory-nesses, and only one of them expires": both are about the *input*, and a gate can be non-deterministic while its input is perfectly still because the variance is in the **instrument**. Its practical test is temporal and a sampled judge does not move, it varies — so a fixable gate was being filed permanent, and a statistically-varying check graded safe to block on one run. **Applied both.** Row 1 `ab-paired` against this registry's own purity denylist: arm A names a listed product → 1 violation, red; arm B describes the same product unmistakably and does not name it → 0 violations, `bundle integrity OK` over an equally impure document. No invariant key exists, so detection was not improved and cannot be; shipped the one-line verdict predicate instead — **detection unchanged by construction, overclaim removed**, and the line is additive so eleven sessions' substring greps still hold. Row 2 a three-case `simulation` with the falsifier stated first, across this registry and two consumer trees; all three sort differently under the third bucket and **two had already implemented the prescription independently**, so nothing shipped to them. **Corrected premise:** the article reads a measured 20/40/80 per-language generator spread as setup cost; the primary corroborates the numbers and not the causal gloss, so the row is banked untriaged with the weaker rule the numbers alone authorize. **The peer study owed from round 21 is not discharged** — a 3,000-word newsletter is not a peer system; the debt stays open against a source that is one |
| 2026-09-04 | `youtube:CmmLZeuK4lg` - "Infinite AI Streaming Will Change Content Forever (Minimax FastH3)", All About AI | practitioner build-walkthrough (hybrid: demo half + operating half) | 2,729 | 9 | 2 | 1 | 1 | **The ratio was right and the conclusion was wrong, which is the shape worth mining.** A 15s clip generated in ~13s does not make a stream infinite: what is spent is the **margin** (0.13), buffer accumulates at `(1-rho)/rho` so 60s of depth takes 6.5 minutes to build, and one 13s overrun costs 85s to repay. Two techniques into `media-playback`, a subject that had no regime for a timeline whose tail does not exist yet. **The neighbouring subject's honest self-scoping is what made the hole findable** - `buffering-and-backpressure` opens "The producer being faster is not an edge case, it is the operating condition" and enumerates "exactly two honest responses", all true and all the opposite regime. Applied `code` to a fleet speech service whose certification gate held the source's exact error (mean rtf vs 1.0, p95 unread in the same row) **while the same repo applied the correct rule one file away** for single-request promises; shipped. Second technique came back `not-better` - that scheduler expresses priority as a deadline with an aging bound, which the technique lacked | [[2026-09-04-infinite-ai-stream]] |
| 2026-09-04 | [pi — AI agent toolkit](2026-09-04-pi-agent-toolkit.md) `github:earendil-works/pi` @ `92d8e2d1` — "unified LLM API, agent loop, TUI, coding agent CLI" | **first-party practitioner codebase read as a SYSTEM** — a crash-durable agent harness: immutable entry tree, bound typed values/lists, append-only usage ledger, a 13-leaf operation state machine, an effect gate, 38 invariants, a 30-row race catalog where every race has exactly two legal durable histories, and three test tiers. **Priced before extraction** (round 23 item 1): 990 words of landing page against **325,302 words of in-tree markdown**, including a 27,820-word normative specification and ten ADR-shaped work packages that keep their rejected alternatives. Forecast stated before the triage table was routing count >=3 plus a technique triple or a scoped forge; actual was routing count 3 and **one** technique — right about depth, overshot on volume, because the three homeless decisions scatter into three different existing subjects instead of clustering into one new one, so **the handoff was declined on shape rather than on size**. Fetch **0 of 3**, as the class predicts. The run's centre of gravity turned out to be Phase 7.5 rather than Phase 7: the landing came from an enumeration hunt (`step-position-and-resumability` lists three repairs for the honestly-non-idempotent step and pi's is in none of them), and then **the apply step earned a second landing** — pumper cites `terminal-state-recovery` in its own source comment, adopted it faithfully for the `running` class, and thereby built the exact gap that technique's reachability table invites by saying terminal states need no mover. **Re-scan when** WP08 (named-branch and streaming forks) leaves in-progress status; or the H1 contract-closure debt in `harness.md` §0.9 clears; or per-step replay lands, which `tool-durability.md:485` defers by name; or 8 weeks elapse (2026-10-30) | 990 landing / 325,302 in-tree | 11 | 1 | 0 | 1 | 6 untriaged, 3 catches | [[2026-09-04-pi-agent-toolkit]] — **1 technique + 1 amendment + 1 source-tree application + 1 task row that shipped a plan into a fleet project.** The peer study the operator asked for **already existed**: personas holds a 43-point comparison against omp/omp², itself a fork of pi, so this run produced the delta instead of re-deriving it |
| 2026-09-04 | [weave-router](2026-09-04-weave-router.md) `github:workweave/router` @ `1699cf6` — "Model router for agentic systems" (redirects to weave-os/router) | **vendor repository**, 1,834 landing words against **72,225 in-tree** (~39:1): a 6,262-word configuration reference, an out-of-process policy contract with release gates, a semantics document, a translation-compatibility document, and sixteen per-package module guides each mirrored to a generated `AGENTS.md` with **CI rejecting drift**. Priced before ingest per round 23: *2-5 landings weighted toward design, most homing into a subject forged from a different gateway 48 hours earlier and still single-sourced* — and justified on exactly that ground, since a second independent gateway is the cheapest corroboration that subject will ever get. **Re-scan when** the translation-compatibility rollout leaves `shadow` for `enforce`; or the silent-drop mapping contract on its own "Still to remove" list is removed; or a second in-process router implementation appears, which would give the exclusive-authority technique a non-degenerate instance; or 8 weeks elapse (2026-10-30) | 1,834 landing / 72,225 in-tree | 12 (8 design + 4 claim) | 3 accepted (`auto=3/0/0`, `fp=0`), 0 declined | 3 rows: 1 `code` **better** (shipped), 1 `experiment` **not-better**, 1 `unapplied by construction` | 1 (personas, hooks green) | 3 caught / 6 untriaged / 2 leads | **Routing count 8 decisions / 3 systems / 4 unhomed — the largest system reached three and still did not fire a forge, because its home already existed.** Forty-eight hours earlier this tree would have been a forge job; the Portkey run built `multi-provider-gateway-plane` and this run's corroboration landed *inside* it as two techniques. **`exclusive-authorship-of-a-measured-decision`** merges two design entries into one root — a decision you intend to measure must be the only thing that determined the outcome you record — with three arms the corpus lacked: a per-request fallback is a **survivorship filter attached to the thing it protects** (the source retired its own heuristic because "silent-fallback behavior masked cluster regressions") and the four substitutes that buy availability without it are named, including the operator-lever-versus-per-request distinction that discussions of "fallback" conflate; authority is the **enumerated** suspension of six other writers, with the deliberate exception stated and reasoned; and on a decided-versus-served mismatch the remedy is to **drop the sample rather than correct the record**. **`one-typed-carrier-for-echoed-state`** covers the topology the composer-side subject presumes away — a plane with no transcript, where the client's next request *is* the record: carrier chosen by what a stock SDK is obliged to round-trip, exactly one of them, and the discriminator the source states twice in opposite directions without naming, **defensive redundancy is safe subtractively and unsafe additively**. **Amendment** to `layered-composition` from the promoting question: its door is stated as *enumerable* and a format-translating proxy is a writer that is not a caller; the semantically faithful hoist is the destructive one (~890k cache-creation tokens per turn against a flat 17.5k read), so a required rewrite must be position-preserving, not merely meaning-preserving. **Applied `code` to personas and shipped**: `was_failover` was computed from an enum with exactly one variant that the crate asserts exhaustively — a constant `false` per request over a ladder that downgrades two tiers — measured **0 of 6,163 real audit rows, with `model_used` NULL on all 6,163**. Proof `structural-only` and honestly so: the change compiles clean under the project's own CI feature set but the test binary cannot launch (pre-existing, binary-wide, an untouched neighbour fails identically). **The structural fact nobody designed beats the fix**: the decided and served models are written to the same column, so the comparison is not merely absent but unstorable. The `not-better` row **wrote back into its landing** — the amendment's guard had no number and now has two poles (16.8:1 healthy against ~0.02:1 pathological), so it needs no threshold, only the sign. **Convergence worth recording:** the source's public-repo rule forbids customer names even when a customer's incident motivated the change — this registry's own purity rule, independently derived by a team with the same problem. **0 of 3 fetches.** 2 siblings at claim, 3 by Phase 7; `sozu-rust` reached Phase 7 holding **no declared subjects** over adjacent ground |
| 2026-09-04 | [monty](2026-09-04-monty.md) `github:pydantic/monty` @ `fdd2628` — "A sandboxed Python interpreter, written in Rust, for code written by AI" | **vendor repository**, two systems in one tree (an agent-governed review process; a sandboxed bytecode interpreter). README 392 words / in-tree operating docs ~63,000 — a 160x ratio, the widest this ledger records | repo | 13 design entries | 2 | 1 | 5 | [[2026-09-04-monty]] — routing count met **twice** and fired **no forge**: both clusters landed in homes that already exist, and two of those homes are **one day old and single-sourced**. Landed `instruction-format-follows-program-lifetime` (register-bytecode-execution, forged 2026-09-03 from a JS engine — this is the second independent language runtime into it, and it **refutes** three of the golden path's unqualified claims) and `shared-substrate-check-partition` (quality-gates, #2 on the worklist). Catch: the four-symlink harness topology is already fully modelled by `single-source-topology`, Windows nine-byte failure included. Untriaged with anchors: the divergence register, named-duplication-points as a copy regime with no generator, replace-not-merge ignore semantics, per-transport guarantee enumeration. |
| 2026-09-04 | [Wan2GP](2026-09-04-wan2gp.md) `github:deepbeepmeep/Wan2GP` @ `cdab3128` — "a fast AI video generator for the GPU poor" | **vendor repository / build-walkthrough in repo form**, 8,158 landing words against **76,498 in-tree docs** plus ~89,500 lines of Python. Priced before extraction; ratio only 9.4:1 because this README *is* a long changelog, so the design-content gap is far wider than the number says | 8,158 landing / 76,498 in-tree | 22 (18 design + 4 claim) | 3 accepted (`auto=3/0/0`, `fp=0`), 0 declined | 3 rows: 1 `experiment` **better** (shipped), 2 `unapplied` by construction | 1 (gravity, gates green) | 3 caught / 9 untriaged / 2 leads | **Routing count 9 unhomed / 4 systems; the one system reaching three COLLAPSES to a single idea under the shared-force test, so neither v2.2 clause fires.** That is the third outcome round 24 asked for a word for: *count met, cluster collapses* — distinct from both "count not met" and "handoff declined". The run's method was to take three corpus **absolutes** to a tree that disobeys them, and none survived unqualified. **`write-freshness-gate`** says "writes never refresh marks" and calls it "the whole cure for drift" — true for appends and substring replaces, whose result depends on content the writer did not supply; false for a whole-artifact write, where the writer holds the authoritative copy and a readback returns its own argument. The source's strongest form **refuses literal text** for that operation, so the model never saw the bytes at all. **`catalog-projection-modes`** makes a routing tool self-teaching by answering an unnamed operation with the sub-catalog, and stops — leaving the model holding a name and inventing its arguments, the failing first call the section exists to prevent, one round trip later. The second rung answers the omitted *arguments* with the operation's schema, which is how "constrain in the schema" stays true after compression removed the listing; four independently written tool families implement it. **`generated-shot-sourcing`** says to mint each state from the one before and warns that state 3 from state 1 disagrees with state 2 — true when states **accumulate**, false for independent variations on one subject, where the chain is a pure drift pump; the three topologies (chain, star, pinned bank) now sit in one table and compose along a pipeline rather than competing. **The landing came from Phase 7 and the DEFECT came from Phase 7.5**: gravity was already a pure star everywhere it mints images (references memoized per *theme*, not per frame — undesigned), but `styleRefs` took the newest four approved proofs of up to fourteen, so **the founding proof left the reference set at approval 5 of 14** and late frames were judged against four references sharing nothing with the theme's founding four. Shipped `PINNED_REFS=2` with both bank bounds asserted, plus a paired regression wired into the project's `verify`, asserted against a known positive first. **The apply step then corrected its own landing** — pinning buys origin *retention*, not master *stability* (both arms still see 14 distinct masters), and the corpus text now separates the two properties. **0 of 3 fetches**, as the class predicts. Lead worth more than most of the untriaged tail: **the whole local/open-weights lane is absent from `media-generation`** — searched untruncated, all nine bundle files mentioning an "adapter" mean a *provider* adapter, so the bundle's boundary was drawn at hosted API generation and weights, samplers, quantization and memory placement have no home; `visual-generation` has 2 of 10 slots free today. 0 siblings at claim, **2 by Phase 7**; `index.json` and `catalog.json` left uncommitted because the regeneration referenced a sibling's uncommitted technique five times | [[2026-09-04-wan2gp]] |
| 2026-09-04 | [WikiSkill](2026-09-04-wikiskill.md) `arxiv:2608.27454` — "Compiling Agent Experience into Persistent Knowledge for Skill Evolution" | **paper** (single primary, cs.AI, submitted 2026-08-27). Class prediction stated before extraction and paid twice: *authoritative for its measurement, weak for its framework* — both techniques came from the ablation and the transfer table, and the framework's other three design choices became one lead and two catches. 2 of 3 fetches (the `/abs/` page is a pointer; `/html/<id>v1` rendered because this is a LaTeX submission) | 698 landing / 14,416 full text | 11 | 3 (`auto=3/2/0`, `fp=0`, 0 declined) | 2 | 2 | [[2026-09-04-wikiskill]] — **`routing=n/a`, `directions=n/a`**: a paper is not a tree, so there was no design record, no forge count and no Phase 7.6. Said explicitly because "no count to take" and "count met, handoff declined" are different outcomes. **Home age read before the count** per round 24: `agent-memory` is 15 days old with **9 prior sources**, so nothing was suppressed and this is corroboration into a mature subject. **The absolute that moved** (`absolutes=3/1`): the golden path calls the consolidated layer "the only layer allowed to speak as knowledge" and models it as one layer with one policy; the paper splits it, because the procedures an executor runs on and the accumulated diagnosis used to revise them need **opposite** read policies for the executing agent. Measured 2x2, one model, four benchmarks: the reviser's access is worth **+15.0** (48.7→63.7), the executor's access **costs 2.8–3.4 in both arms**, which is what makes it a role property and not an interaction. New `diagnosis-withheld-from-the-executor`, with boundaries to the two nearest instruments that both say the read path is not neutral and differ in what it damages — `probe-without-write-back` (ranking state; the instrument is a *writer*) and `instrument-exposure-control` (the score, upward, where this one leaves the current number honest and spoils the *next* one). **Two design candidates folded into one technique** per the same round's second focus item: never-roll-back and gate-writes-the-record share a force, and landed as `rejected-revision-leaves-its-evidence` — whose sharpest line the paper does not draw, *store the diff and the number, not the reason.* **`procedure-promotion` declared its own completeness and was incomplete**: "two sources feed promotion … neither is sufficient alone" omits **another agent's experience**, measured across five models and two families (24.3/33.6/**50.5**; 33.9/56.7/**73.7**), running *upward* from a 4B-class author to a 31B-class executor — because an author whose runs never failed at a step compresses it out, so **a weaker author records more of the method.** **All three landings were found in Phase 6, not Phase 3**; the eleven extraction rows produced none of them in the shape they landed in. **apply 3/3, ship 1**: personas' prompt-evolution loop stores `variants_tested` (a count), two fitness numbers and a prose `summary` and **no tested candidate anywhere**, while the tree already holds the opposite design on its newer surface whose fitness loop is deferred — so the surface that keeps the diff does not run and the surface that runs and rejects keeps no diff. Paired on a scratch database **0 of 3 against 3 of 3**, CHECK/FK/UNIQUE/CASCADE exercised; step 1 of 3 on a branch, not merged, `cargo check` not reached. The registry's own skill lane is configured the way the ablation measures as worse and the size is now counted: **83 distinct declared-focus imperatives, 9 carried by the gated file** under strict phrase matching, hand-sampled to **9–25 of 83**, and the worst instance is the technique's promotion-signal clause verbatim — one demand appears **27 times**, its own text counting *sixth* through *tenth deferral*, absent from the gated file. **Two instruments self-caught in one run**: the first apply instrument failed its own assertion (bag-of-words scored a known negative 1.00 over a long file) and the second caught a both-arms-zero tie from a bad schema-marker substitution. Leads: the push-sample/hand-index asymmetry between the store's two readers (**a real corpus gap the class rule refused to land** — the paper measures nothing there) and the monotone bar excluding neutral proposals. Catches where the corpus is ahead: store pruning, which the paper calls future work and this subject models in three places, and retrieval, whose absence is a scope condition on citing the paper. 1 sibling at claim, 0 by Phase 9 |
| 2026-09-04 | [cargo-make — second lens: language craft](2026-09-04-cargo-make-rust-craft.md) `github:sagiegurari/cargo-make` @ `95dcc54` | **practitioner build-tool repository, re-read at the same commit under a different lens** — the architecture run (`cargomake-0904`) mined nine design decisions the same morning and left Phase 2b's *read for reusable engineering* pass empty; this run does that pass and overlaps it in **zero candidates**. The shape of the tree in three counts: **zero trait definitions in 13,145 lines** (the only `trait` token is a doc comment at `completion.rs:16` proposing one), five generic functions, 347 clones against two `unsafe` blocks — a closed-world CLI that abstracts only at its dependency edges, which is defensible and is not the finding. **Re-scan when** a release touches `scriptengine/mod.rs`'s engine resolution (a producer for `Unsupported` would falsify the second landing) or `completion.rs` adopts `CargoMakeError` (closing the third landing's coverage gap); or 12 weeks elapse (2026-11-27) | 13,145 lines non-test src | 8 | **3 techniques** | 0 declined | 0 | 5 untriaged / 0 caught | **The run's own lesson is that a mined repository is not a spent one.** Both notes share a commit and a sweep list and disagree about what the tree contains, because a design read hunts forces and a craft read hunts what a type is being asked to carry: the architecture run recorded `types.rs` as "the real data model" and never opened `error.rs`, which turned out to hold the best engineering in the tree. Landings: **`state-carrier-decides-the-lane`** (`Rc<RefCell<FlowState>>` is `!Send`, so the parallel lane deep-copies and discards — and `FlowState`'s only two fields are an accumulator and a latched decision, so **no field in it is one a copy is correct for**; the time summary silently omits every parallel-forked task and the README's hazard list at `:618-620` names `cwd` and neither of these — the hazard the author designed for is documented, the one an `!Send` bound decided is not); **`shape-with-a-not-applicable-member`** (`EngineType::Unsupported` has **no producer** — every branch of `get_engine_type` maps it onto a real fallback in the next statement — so the exhaustiveness checker *compelled* an arm for an unreachable state and the cheapest arm that compiled was `Ok(false)`, which the caller reads as "no script", making the Shape altitude regress past Call-site into a silent success nobody designed); **`exit-contract-is-a-taxonomy-projection`** (`#[repr(u16)]` with an explicit discriminant per variant, banded, `NotFound = 404`, and a correct narrowing rule that collapses out-of-range to `FAILURE` and prints the real number instead of truncating into another kind's code — the good idea, **plus its own boundary in the same commit**, since `completion.rs` returns `Box<dyn std::error::Error>` and that one peripheral subcommand's failures cannot carry a code). Applied 3/3, shipped 1: the exit-code technique's seam is **this registry's own scripts lane**, where code `3` was carrying three incompatible meanings (CONTENDED/transient, thin-source/permanent, rows-due/not-a-failure) with no declaration anywhere — codes 0/1/2 held across ~30 scripts and broke at the first code a later script added, exactly as the technique predicts. Two `experiment` rows against personas returned **`better` with no change owed** (a union of identical shape to the source's defect, cleared by the producer audit — the matched pair *is* the A/B) and **`not-better`** (a worker boundary carrying a request built at the call site rather than a copied carrier, which bought the technique the precondition its origin concealed). **Index and catalog deliberately NOT regenerated**: an unclaimed session held uncommitted work across `quality-gates` and `agent-runtime-assembly` for most of this run, and a regeneration would have baked it into an artifact committed under this run's name |
