# The source ledger

Every external source [`/intake`](../../.claude/skills/intake/SKILL.md) has mined, one
line each, newest first. (`/deepen` may also write here when it is handed a URL; the
ledger does not care which skill mined a source, only that one did.) One note per run sits beside this file.

This ledger answers one question in one second: **has this been mined already?** A
re-ingested video costs a full extraction round to rediscover declines that are already
written down, and the answer to "did we look at this" is not something to reconstruct
from memory.

## What a source note holds

The candidates a source produced and what happened to each - accepted, declined with a
reason, banked as a lead with a return condition, or caught as already covered. The
declines are the most valuable part. A decline nobody wrote down gets re-proposed every
run forever, and a source class nobody characterised gets over-trusted every run
forever.

## What it never holds

A consumer's paths, repository internals, or the transcript itself. Transcripts are
run inputs; they live in a scratch directory outside the repository and are deleted
when the run ends. A note quotes an anchor, never a corpus.

## Mined

| Date | Source | Kind | Words | Extracted | Accepted | Leads | Caught | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-31 | `github:BurntSushi/ripgrep` @ `3fce3b5` | **first-party practitioner account (repository form)** - one expert maintainer, a decade of operating documents. The class prediction held exactly: corroborates corpus-internally, **0 of 3 fetches spent**, yield concentrated in amendments to mature subjects rather than in new ones | 2875 landing page / ~37000 in tree | 12 | 2 | 0 | 0 | 10 untriaged | [[2026-08-31-ripgrep]] - swept in yield order: GUIDE (6408), FAQ (6409), CHANGELOG (9642), crate API docs, the completion-drift gate, twelve dated benchmark result sets, 110 issue-numbered regression tests, README last. **Landing 1** - `codebase-scanning` models the *sensor* denominator with four measures and gives the *file population* denominator one sentence inside a foreign-tree-only section, so a sweep can report twelve of twelve sensors and zero findings over a tree whose traversal removed a third of the files, and every clause is true. The source's second chapter is what it *doesn't* search - three ignore layers in strict precedence, hidden files, a content heuristic, a link policy, all on by default - plus an escalating repeatable control whose stated purpose is diagnosing exactly this. Boundary drawn against `docs-sync/checked-vs-skipped-denominators`: **its three states cannot see this failure, because the removed files land in none of them - nothing skipped them, nothing enumerated them.** **Landing 2 is a contradiction, which is the best case** - `stake-before-merit` closes on "authorship of the accountability, **not** authorship of the text", and the source's AI policy draws exactly that denied distinction by channel: generated code welcome, generated replies to maintainers hidden without notice. The technique is right that stake is *asked for* at admission and only *tested* in the review round trip it separately calls the expensive half; the reviewer's question there is a probe of comprehension, not a request for text. The asymmetry is structural - **the diff has instruments that do not care who wrote it, the conversation has only the reviewer's attention, which is the scarce server the technique exists to protect.** Enumeration hunt: the technique denied a symmetry, and it denied too much. Both applied to `personas` (15.5% of a security gate's tree silently excluded, falsifier 184 raw -> **0** hand-classified; and a contribution guide that invented the amendment's rule for one field and could not generalize it). **Parallel conditions**: 3 siblings at claim, 5 by Phase 7; `optional-dependency-degradation`, `quality-gates` and `test-harness` all contended, which is why three real candidates sit untriaged rather than verified. Index and catalog regenerated and then **deliberately left uncommitted** - they referenced four sibling techniques absent from HEAD. The gate went red inside the `index` lock on a sibling's half-written subject; lock released before investigating, breakage named and not fixed |
| 2026-08-31 | ICSE 2026 SEIP track page | **reference index — degenerate (no annotations)**: a conference program is a bibliography whose curator sentence was stripped, leaving title+authors only. The substitute signal is the track's own admission rule quoted on the page ("present evidence for the paper's conclusions") — peer review at an industrial-practice track *is* the annotation, and it held: **8 of 8 references read returned something usable**. Enumeration needed two numbers: a raw grep says **150**, two independent structural parses (accepted-papers table, program schedule) both say **74** | 41,712 page / 74 refs, 8 read | 8 | 2 | 5 | 3 | [[2026-08-31-icse-2026-seip]] — **the convergence the wave was ranked on was fake and a real one arrived unranked.** The two benchmark-validity papers are not independent: the second cites the first and relays every validity number from four other papers, so per the tiering rule the pair is **one observation**. The genuine convergence came from two references ranked for unrelated reasons — different authors, institutions, countries, problem domains, neither citing the other — both naming the same missing discriminator in `false-positive-economics`: **the death spiral runs on the author's unilateral power to dismiss, not on the false-positive rate**. One ships a repair gate at precision 0.30 against a 0.19 base rate correctly (machine-generated artifact, no author to bypass); the other measures a mandatory gate at 76% false alarms surviving ten months because a *second* reviewer must confirm any dismissal. Second landing is the enumeration hunt's thirteenth consecutive pay, on an enumeration already extended once: `measurement-honesty` declared "six states, not two" with state 6 *Refuted* defined strictly internally, and a score taken on an instrument the subject was already exposed to passes all six and every technique in the subject. **State 7 `compromised` is invisible from inside the system**, where state 6 was only invisible from inside a single number. Boundary finding: of 74 papers ~46 are single-vendor tool papers in domains this registry does not carry, and `librarian-scan`'s highest-attention subjects appear in **none** of them — this index cannot feed measured demand, only the one seam where both fields stand (measurement of automated verdicts). 13 fetches, all spent by workers. 66 refs ranked and untriaged, never declined |
| 2026-08-31 | `web:postgresql.org/list/pgsql-hackers/2026-08` - a development mailing list month | **reference index (UNCURATED)** - value is entirely the outbound links, but nobody selected them: a chronological firehose, not a bibliography, so the lane's "what the curator included and left out" finding does not exist here | 2,480 (landing page) / **2,680 messages, 578 threads, 255 authors** enumerated across 20 pages | 9 | 2 | 0 | 4 | [[2026-08-31-pgsql-hackers-2026-08]] - **the ingest silently truncated a paginated source**: it returned Aug 1-4 of 31 days, reported `words: 2480`, and nothing in its metadata said "page 1 of 20". The operator authorised paginating before ranking and **it moved the ranking rather than extending it** - the month's strongest artifact (40 messages, 22 correspondents, the highest author count on the list by half again) is not in the first four days, and the truncated triage would have spent its budget on buildfarm attribution and a flag-combination crash and never seen it. Census reported as two figures per the standing focus: **899 threads first, 578 hand-verified** - an unstripped HTML entity (the attachment paperclip) split every thread with *some* attached messages into two, a **36% overcount from one character class**; authors 257 -> 255 on accent variants; and the 4-day sample held **7.5% of messages**, not the 13% its day count implied. Two landings, both from the *disagreements* rather than the claims. (1) `post-landing-repair-density` in `delivery-analytics`: a model was asked to rank landed features by "the number and type of bugs fixed post-freeze" to decide what to revert before shipping - and `revert-linkage` opened with "a revert is the only failure signal available from change history alone" while its own *when not to use this* named the fix-forward population it cannot read. The thread draws the discriminator the technique needed: **scope erosion** was rejected in-thread as "making perfect the enemy of good", **defect tail** got "I'll prepare a revert" from a co-author of the feature being pulled. Its failure mode is in-source too - "Uh, I am confused. We are now considering reverting these?" 25 minutes after the list was circulated as discussion. (2) `reclassification-is-not-repair` in `error-handling`, a **contradicted pick kept**: the patch proposed assigning real error codes wherever the suite reached the internal class, and the senior committer's objection is the finding - "labeling it otherwise is a lie ... blaming the messenger rather than looking for the root cause." `taxonomy-design` owned the closed set and the catch-all and not the opposite member, the one whose entire value is that it never fires. 4 catches against `test-harness` and `dead-code`, both held by siblings all run. **`test-harness` owes a configuration axis** (the corpus has the analyzer form and the runner form, not the suite form) - banked as a lead rather than written, because the sibling held it |
| 2026-08-31 | `tkdodo.eu/blog/creating-query-abstractions` - "Creating Query Abstractions" | **first-party practitioner account** - the maintainer of the library arguing against the abstraction his own users write; reliable for what he built and why at n=1, and it corroborated corpus-internally at **zero fetches** | 3,789 | 10 | 1 | 1 | 1 | [[2026-08-31-tkdodo-query-abstractions]] - ten candidates were **one argument**: four folded into the single landing as its supporting reasoning, one into the lead. `portable-read-definitions` -> `client-fetch-cache`, found by the hunt for the stage a pipeline leaves to a default. The subject owns the cache exhaustively and owns nothing about **where the read definition lives**; two documents brush the seam - the golden path says state all four policies at the construction site *"without reading its callers"*, `prefetch-and-defer` says a prefetch shares the *"same key builder"* - and both presume that site is **reachable**, which it is not when it is bound to a component-scoped primitive. Boundary with `plural-policy-claims` stated in both directions: that technique reads divergence as *claims* to resolve by quantifier, which is right only when the definition is shared; unshared, divergence is drift and no quantifier recovers an intent never formed. Applied `code` on `goat`, **better**: 45 declaration sites over 982 files, 3 questions declared more than once - and **hand-verification changed the conclusion on two of the three**, which is the run's most reusable result. Shipped the verified one (four declarations, three lifetimes -> one declaration, resolved lifetimes unchanged; typecheck 29 before and after, lint clean). **The compiler confirmed the technique**: the two hooks' pass-through options bags could not be composed with a shared definition at all, because their key generic was hand-widened to `readonly unknown[]` against a precise tuple - and one caller had already cast `as any` to get past it. Configurable pass-throughs are not merely inadvisable here, they are untypeable. Second sighting of a banked lead recorded: one key, two different API clients, still unfixed |
| 2026-08-31 | `web:arxiv.org/abs/2604.18071` Architectural Design Decisions in AI Agent Harnesses | **paper** (cross-sectional, 70 projects, corpus frozen 2026-03-23) - taxonomy paper, so its five dimensions and five patterns are the framework half and were treated as marketing; the measurement half carried the run | 16055 (661 landing / 16,055 full text = 24x; the `/abs/` ingest returns the abstract, the `/html/<id>v1` sibling returns the source) | 10 | 3 | 1 | 1 | [[2026-08-31-agent-harness-design-decisions]] - one technique + one golden-path amendment on `measurement-honesty`, **0 of 3 fetches**. The source refutes its own headline statistics: §5.1 defines support/confidence/lift correctly, then Table 10 reports four relationships whose support exceeds the marginal frequency of their own antecedents as published in Tables 4/6/7 - by **2.87x**, **4.34x** and 1.04x, the fourth unverifiable for want of a marginal. Recomputing the highest-lift row from the paper's own conditional gives support 0.31 and lift **2.13** against a reported 3.4, so the ordering it justified is wrong too. Second, independent defect: three of four "compact evidence statements" evidence a metric defined over **binary co-occurrence** with a difference of **continuous mean scores**, so the figure cannot have been computed from the evidence shown. `co-published-numbers-must-reconcile` was written from the identity (support(A^B) <= min(P(A),P(B))), not from the paper - training-data convergence in its strongest form, with the source supplying the worked counterexample. Home found by the **missing-stage** hunt: all six prior techniques are producer-side and the golden path's thesis is that a dishonest number is *unfalsifiable*, so the gap was the inversion - numbers that ARE falsifiable, by their neighbours. The **enumeration** hunt then paid on the same file: "A datum has five states, not two" is a completeness claim and was short by one; the sixth is *refuted* (instrument ran, value well-formed and in range, another co-published number proves it cannot be true), absorbed by none of the five. Applied to `goat` as an `experiment`, **better**: a collection panel computes a completion percentage with a whole-grid numerator over a page-size denominator (its own comment calls the denominator a *proxy*) and clamps at 100%, while the store that owns the data computes the same quantity correctly and needs no clamp. 120 states enumerated from the tree's own parameters; arm A published a well-formed percentage in **all 120**, arm B flagged **101 (84%)**, and the shown figure disagreed with the application's own ground truth in **66 (55%)**. A prediction was falsified en route and the correction is the useful half - the clamp fires in only 9 of 101 and never in the shipped configuration, so the defect is the denominator and the clamp is merely its fingerprint. **Structural fact**: the consumer subscribes to the grid store and reads placed ids off the very state object that also carries the real capacity and the already-correct percentage - the correct denominator was on the object it already held. Ship 0, blocker class **`confirmation`** (triage named registry rows only). 1 catch (the paper independently states Phase 2b's rule - documentation overstates architecture, code settles it), 5 untriaged with anchors, 1 lead |
| 2026-08-31 | `web:arxiv.org/abs/2604.11378` - "From Agent Loops to Structured Graphs" (Hu Wei) | **paper - position paper / design proposal**, the class's weakest sub-kind: it disclaims measurement twice in its own words ("not a production implementation or empirical results"; "they remain untested claims"). By the class rule ("framework papers are the class's marketing") its architecture authorises nothing; what it CAN authorise is a 5-member planning-failure taxonomy, two proved propositions, and a self-declared expressiveness boundary | 739 landing / **21597 full text** (29x) | 14 | 1 | 2 | 2 | [[2026-08-31-agent-loops-structured-graphs]] - expected yield stated as 1-2 amendment-shaped landings before the triage table; actual 1 technique + 1 application, so the calibration held. **0 of 3 fetches** (twelfth consecutive zero-fetch run). `valid-but-degraded-plans` on `pipeline-dag`, **found by the enumeration hunt**: `graph-validation` states its own coverage as a binary - provable at the door, or a named run-time check - and calls the space between them the category that "must never exist". The source demonstrates a *third* class neither half reaches, where the graph is valid, every check passes, **the run succeeds**, and the plan was still wrong. Discriminator: does the defect change the **result** or only the **price**? Price-only defects are invisible by construction, because nothing separates a correct cheap run from a correct expensive one without a second run to compare. The subject's blind spot is in its own boundary prose - every document is written for a graph *the user authored*, where the class is nearly harmless; under a planner it becomes a rate. Corroborated by **training-data convergence, no fetch** (build systems' unused-dependency tooling, `EXPLAIN`, critical-path scheduling all predate the paper). **Applied `experiment`/`better` to `gravity`**: its door validator is unusually strong on the correctness class - checks every reference against a universe of ids, then again against a tighter one, and treats an absent link as a defect after shipping six unlinked records under a green gate - and not one check asks whether an edge that *resolves* is *needed*. The structural fact nobody designed: severity is `declared - missing`, and one class of declared dependency can never go missing, so **5 of 11 nodes can never reach the severe end of the scale** however much real evidence is removed; every differing node carries exactly one inert edge, the predicted signature. The run's own contribution beyond the paper is the **inert edge** rule - any ratio over a declared set whose members have unequal ability to fail is silently capped - now the technique's first decision rule. **Ship 0, blocker named per the declared focus: confirmation + indeterminacy**, two of the four classes and both correct outcomes - the pick named no project, and two fixes are defensible (partition inert edges out of the denominator, or give explanatory nodes the evidence edges the code was built to carry) with nothing in the tree settling which. One answer from the owner selects it |
| 2026-08-31 | `github:remeda/remeda` - TypeScript utility library monorepo | **vendor repository**, mined as a **first-party practitioner account of running agents against a maintained codebase** (the class the tree actually supports) | 240 README / **47025 in-tree md** / 58249 ts | 20 | 4 | 1 | 2 | [[2026-08-31-remeda]] - **the library was not the source.** The README is 240 words; the tree carries 47,025 words of markdown and the densest part documents nothing about utility functions - it is `AGENTS.md`, an `.agents/skills/` tree, three path-scoped rule files, and a `.claude/hooks/` turn-end gate whose comments are paid-for failure modes. Class note for the ledger: **the `.agents/`-shaped tree is a source-class tell** and belongs at position 1 of the Phase 2b sweep, because a repository that instruments itself for agents gives a first-party account of agent operation regardless of what the repository is *for* - here it outproduced every other surface combined. Four landings, none touching the library. `mutating-local-gates` (new, machine-paced-delivery): `hook-hygiene`'s flat prohibition - *"Hooks observe; they never mutate"* - carves out only the editor-on-save loop, and the agent turn boundary is a third position where **two of its three contracts transfer** (review binds hardest, staging does not apply, shared-tree binds and is bounded by scope). `stake-before-merit` (new, same subject): `human-gate-capacity`'s four demand levers **all assume the overloaded party generates the arrival** - unavailable at an outward-facing gate; the filter that vanished was an *accident* (producing a plausible change used to be expensive, so submission carried evidence of investment) and was never written down, so nothing announced its removal; and a merit filter cannot replace it because **deciding whether a change is worth reviewing means reviewing it**. Reaper forms **ranked** in `suppression-hygiene` - our own enumeration presented as flat ("an expiry date, a re-review cadence, or the checkable condition") when the source ranks it and rejects our implied default: a date fires at whoever runs the instrument that day, works exactly as designed, arrives at the wrong person, and the cheapest repair is to postpone it. Plus the sharper half - **the suppression's form can carry its own reaper**, the one case `creation-names-reaper` is satisfied with no clause at all. `build-measurement` gained a **vantage** axis: it labels which build, which variant, which machine, when - and never *whose*. **Applied 2c/1e/1s.** The apply step **corrected its own technique twice**: a managed project's turn-end gate neither mutates nor blocks and still guards re-entry, because exit 2 re-enters the model's loop - so the termination contract is created by the output reaching the agent, and the technique now carries a three-tier ladder. Decisive A/B on the other: **0 warnings vs 63**, same crate one minute apart, only the suppression form changed - 63 of 71 (89%) stale, against a surface that is 0.7% self-retiring. Four of twelve candidates lost to contention, all to one sibling (`release-pipeline`, `quality-gates`, `test-input-generation`, `test-harness`) - recorded untriaged with anchors, not declined. 0 of 3 fetches, eleventh consecutive. 5 siblings live. |
| 2026-08-31 | `youtube:3IyKC5EtNkM` - "9 Ways to do Inheritance in Rust" (Carl Kadie, Seattle Rust User Group) | **first-party practitioner account** (conference talk) with a **live peer-review property the dialogue sub-class does not describe**: the audience are peers who correct the speaker on camera, twice, and both corrections are load-bearing. Hardest possible source shape for this corpus - a language-mechanics talk against upper layers that may carry no language names. | 7358 | 11 | 3 | 2 | 0 | [[2026-08-31-nine-ways-inheritance-rust]] - **the predicted low yield held exactly, and the three survivors were the three about BOUNDARIES rather than syntax.** Six of nine puzzles strip to `nothing` (they are the syntax for a standard OO move). One NEW TECHNIQUE `borrowed-surface` on `module-design`, landed by **merging the operator's two separate picks** once verification found they share a root and the root is higher altitude: `module-depth` already corrects the author's interface denominator *downstream* (with enough callers every observable behaviour is interface) and has **no model running upstream** - that part of your interface is authored in someone else's file, on their release schedule. Two forms: implicit delegation (the source states its own cost - *"the whole reason I gave it a new type was that if I got mixed up I would get a compiler error. And now I'm not going to get a compiler error"*), and a disjointness premise taken from a contract you do not own. The discriminator the source states without generalising: delegation is correct when the wrapper's job is **orthogonal** to the delegated surface, self-defeating when the wrapper's job **is** the distinction. Plus two amendments, both from the enumeration hunt: a **fourth signal** on `seams-and-adapters`, which declared "three signals" and whose *When not to use it* would have told you NOT to own the contract in exactly the case where you must; and the **open-type case** on `settings/typed-accessors`, whose 4-step contract rests on an unstated premise (the type is declared at the accessor) and uses parse failure as the type detector - zero hits for type-mismatch vocabulary across the whole subject, and its `Boundaries` section claims typing rather than scoping it out. `ipc-contract` rejected as the home on its own stated boundary ("no version skew in the field"). **Applied 2/2 `better`, and the tree confirmed the technique in the state it predicts**: a scrubbing wrapper forwards the wrapped type's whole mutating surface, exactly one caller reaches through it, and that caller **hand-rolls the invariant in four lines under a comment explaining why** - the guard has left the type and become a convention held by one author's memory. Same probe line both arms: A compiles clean, B is `error[E0596]`. Not committed - the crate's build script fails before rustc reaches the code, and unproven does not commit to credential code. Second: a settings store with a closed key registry and a real write door where enforcement is nonetheless **per key with nothing counting it** - 58 of 90 key constants reachable inside the validator, and within its own "limits" category 3 of 4, the fourth being a spend ceiling whose sibling is enforced *and* carries six negative test cases. One test over the whole category: FAILED on A, ok on B. **Ship 1.** 0 of 3 fetches. 6 untriaged with anchors, 2 leads (both about source-class calibration: an enumeration that names its own count invites its own refutation, and a practitioner shipping the option they doubt at equal cost). |
| 2026-08-31 | `tigerbeetle.com/blog` (31 posts) | **reference index whose references are all ONE organisation's first-party accounts** — 335 index words against 31 articles (ratio says index), but deduped by author it is 3–4 voices and matklad wrote 13. Phase 2c's *mechanics* apply in full; its *triage signal* (within-index convergence) is **void**, and the same org's repository was mined the same morning, so overlap is depth, not independence. | 335 index / 11 articles read | 11 | 3 | 3 | 3 | [[2026-08-31-tigerbeetle-blog]] — **one NEW SUBJECT plus two techniques, and 3 of 3 apply rows came back `better`.** `test-input-generation` on `engineering-process/build-and-release` (6 techniques, specced and forged same-session): four posts by three authors converge on test-input generation and the corpus held **nothing** — 0 hits for `deterministic simulation`, `swarm test`, `property-based`, and all 56 `fuzz` hits are *fuzzy matching*, which is exactly why the slug map returned confident noise and the hole survived 153 subjects. Home decided on the neighbour's **stated boundary** (`test-harness` opens *'the tests themselves assert facts'* — an explicit exclusion), not on a slug. Its strongest pair: **both clever and naive generators collapse the reachable space, in opposite directions**. One technique was written **against the literature rather than the source** — the post presents swarm testing with no limits and says 'Please steal this'; Groce et al. ISSTA 2012 (the run's one corroboration fetch) states the decisive boundary, so the technique carries both halves. `two-clock-records` on `audit-logging` **corrects a stated rule** — the golden path said *'one clock per ledger'*, right where the ledger originates the fact and wrong for anything learned from elsewhere; found by the asymmetry hunt, since `append-only-design` already owns correction-as-append and records all of it on the *recorded* clock. `toolchain-floor-drift` on `supply-chain` is the mirror of `update-automation-review`: the update you never made, carrying a rare real protocol (top-100 packages, binary search by build, 1.0–1.94, dated). **Applied 3/3 `better`, two of them `ab-paired` experiments**: `personas` declares `rust-version 1.80.0` against a resolved graph whose **effective floor is 1.88.0** (60 of 518 packages above the claim, led by a transitive `time` at patch level) while all 11 Rust CI jobs run `@stable` — and the structural fact the tree could not have been built to prove is that the *same repo* declares a runtime floor of 20 **and runs a job at 20**, so two identical claims are enforced asymmetrically purely because pinning a compiler is an extra argument to an action whose default reads as correct. Second: a property suite running ≥1,000 cases per invariant asserts a violation branch its own generator makes **structurally unreachable** (`media_duration` pinned to 60 while source-end is bounded far below), plus 8 more pinned dimensions. Third: `politicas` independently reached a bitemporal schema and splits enforcement exactly where the technique predicts — record clock `not null`, world clock nullable and unpopulated by the migration's own admission. **Ship 0** — the declared focus (carry a project on every triage row) was missed for the 4th run running, root cause now located in Phase 1, which never reads the focus. 2 catches, 6 untriaged with anchors, 3 leads. 12 fetches, per-reference budget. **WAVE 3** (run `tigerbeetle-w3`, +6 refs read = 17/31): three more landings, **all inside the subject wave 2 created** — which is the evidence its boundary was right, since it absorbed them with no home argument. `liveness-needs-a-quiet-period`: safety and liveness need **opposite fault regimes**, and a safety-only suite does not merely fail to test liveness, it **erases** liveness defects — the stuck component is rescued by the next random draw. Remedy is a phase change inside the run (freeze the fault set, heal only the subset that should finish, keep the rest permanently down, assert monotone progress against a bound); the failure class is two individually reasonable policies that phase-lock, invisible to component testing because neither has a bug. `seed-is-not-a-reproduction`: closes the seed lead wave 2 banked, and produced a **cross-bundle catch** — `game-production/seed-determinism-contract` states the same rule *better* (four terms; the source's 'reproduces at a specific commit' is one of them). Links across bundles are forbidden, so the landing states the **discriminator**: content generation keeps the seed and states the drift because the seed is the artifact's *identity*; a test suite cannot, because the recorded case is *evidence* and exists to survive the fix. Plus a **fourth rung** on the oracle ladder (recorded expected output) as an amendment rather than a competing technique. **Applied 1/3 `better`, `ab-paired`, and the apply refuted my own remedy**: over 50 fixed seeds, the repair this corpus had *already recommended* for that generator re-points **50/50** recorded seeds (structural, not probabilistic — one added draw replaces the sequence); and arm B, included as a mere control, showed **38/50** persisted inputs returning one ULP off, verified at bit level as the JSON library's *default decoder* (its writer is exact, and the language's own parser handles the identical string correctly). Its documented exact-decode opt-in moved that to **50/50**. The technique gained a section: persisting the derived input is necessary and **not sufficient**. Two rows honestly `unapplied` with measured absences (0 fault-injecting lanes; **0 recorded-output assertions across the fleet**). **Ship 0 again, but by a third blocker the focus does not name** — not size (~40 lines) and not confirmation (the tree was authorized): the build could not relink because a running app held the binary, and killing the operator's app is not this run's call. Honest negatives about wave 2's own ranking: row 16 was banded B expecting a docs taxonomy that turns out to live in the *repository*, not the post. 18 fetches total. |
| 2026-08-30 | `github:blue-az/operator-control-plane` - local ledger for claims, evidence and verification across agents | **practitioner build-walkthrough in repository form** (solo builder, 267 commits) | 2639 README / **168969 in-tree md** / 40828 py | 22 | 5 | 2 | 3 | [[2026-08-30-operator-control-plane]] - **the run that changed the method before it changed the corpus.** Triaged first off 2,639 words of rendered landing page; the operator asked whether the repo or only the readme had been read, and the answer was the readme. The tree is 168,969 words of markdown - the README is 1.5% of its prose. `SKILL.md` **1.2.0** adds Phase 2b (clone, pin the commit, sweep operating docs -> instrument -> measurement -> types -> tests -> README last, then sweep again for engineering worth reusing rather than claims worth quoting). Fourteen repository sources audited by the new tell (a `words:` holding one small number, a body citing no file from the source's tree): ten had cloned, one is class-exempt, three had not - two re-runs dispatched. Five landings, and none of the best three is mentioned in the README: `prose-rule-drift` (new, quality-gates - the missing stage one, where the tell is that the forbidden action simply works), `measurement-revision` (new, eval-harness - a re-run at the same n is a second sample, not a correction; pass 3 at n=5 restored pass 1's ratios exactly), an eighth owner in `failure-attribution` (a loop terminating on the first state-changing command defeats every tell in the funnel and lands in the *model* bucket), a `brief-carries-the-session` amendment (the corpus decorrelated reviewers by model family only and never asked what the brief carries in), and a `task-envelope` amendment after the **XL was downgraded on reading the file**. Applied: `prose-rule-drift` -> ascent, experiment, **better** - a documented checker that nothing invokes, 0 violations observed vs **27 across four projects** one invocation apart, which produced the technique's third state (an instrument nobody invokes is still prose). 0 of 3 fetches, tenth consecutive. |
| 2026-08-29 | `youtube:USEk7F9GIZk` - "2 Loop Process for RSI: LLM and Harness" (Discover AI) | **second-hand survey** - single-voice explainer of one unnamed paper, no build, no measurement | 4198 | 10 | 1 | 2 | 6 | [[2026-08-29-two-loop-rsi-llm-and-harness]] - **the class behaved exactly as predicted, and the one finding came from the asymmetry hunt, not from the source.** Six of ten candidates were catches in `eval-harness`, `agent-chaining`, `mcp-tools` and `fleet-orchestration`; two thin leads bank the paper itself. The keeper was the video's throwaway line "if you change the LLM, get rid of your harness": the subject already says the **suite** side of that rule (`unaided-baseline-screening` - the screen expires with the candidate) and `hitl-approval` already says a compensating capability names what retires it, and neither was said where a red case gets its fix assigned. Amendment **held mid-run, then landed** in `failure-attribution` once the parallel session's own section in the same file reached HEAD (`297a7d5`): three of the six owners' fixes are compensations for a *model* defect, a compensation carries the model version as its retirement condition, and on upgrade it is in one of three states - still pays, dead weight, or **now the failure** - so re-ablate the compensation set as a matrix cell instead of carrying the previous winner forward. The lead "harness search is combinatorial, not gradient" got its second sighting from that same pending section. 0 of 3 fetches, ninth consecutive; training-data convergence plus two corpus-internal siblings. |
| 2026-08-29 | `youtube:MvI17TNFW9c` - "AI Creates New Harness by Ignoring 80% of Evaluation?" | **second-hand paper review** (one preprint, arXiv 2608.20169) | 2744 | 8 | 1 | 2 | 2 | [[2026-08-29-task-coevolve-harness-optimization]] - the class predicted itself: the video was a pointer and the paper was the extraction (2 of 3 fetches, first spend in three runs). `eval-harness` gained `discriminating-task-selection` at a **missing stage** - which cells a selection search runs - and the higher-yield half was an **inversion**: `eval-economics` praises the golden set for being frozen and saturated, correct for a regression gate and exactly wrong for a ranking, so the discriminator is the question not the pool. Boundary stated against the same-day `oracle-frozen-during-repair`: the pool is the oracle and stays frozen, the selection moves, from a rule the ranked party cannot write. Two leads (the numbers, and the presenter's doctrine), two catches, three untriaged. |
| 2026-08-29 | `youtube:Z90xlttVx3Y` - "Can Graph Engineering Create System Intelligence?" | second-hand paper review (one framework survey) + **first-party critique** (opinion, not measurement) | 4813 | 9 | 1 | 2 | 4 | [[2026-08-29-graph-engineering-system-intelligence]] - **a critique of a framework is a list of enumerations being challenged.** The paper half stripped to nothing ("system intelligence = graph engineering" is a taxonomy, not a rule) and the critic's counter-cases were boundaries. One amendment: `civic-entity-ontology` had a gate that rejects unknown kinds and a ritual that starts when a kind is demanded, with nothing carrying the one to the other - the rejection ledger is now the ritual's inbox, corroborated from four bundles (`confidence-gates-report-unknown`, `three-state-attribution-that-fails-to-unknown`, `unknowns-as-constraints`, `failure-not-empty-success`). The source argued the opposite (against fixed ontologies); the finding keeps the closed set and gives its refusals somewhere to go. Four catches, the sharpest against `heterogeneous-model-panels` ("two roles are not two opinions" was already "that average with two samples"). Two leads: the survey's own paper list as a future aggregator source, and the critic's real point - nothing in `agent-chaining` or `fleet-orchestration` says WHEN a topology pays over a loop. 0 of 3 fetches, ninth consecutive zero-fetch run. Unattended; picked by impact. |
| 2026-08-29 | `youtube:F1XqqVNBa4o` - "Dual-Memory Optimization for Self-Improving AI Agents" | **single-paper explainer** (paper-aggregator class, n=1) | 5886 | 8 | 2 | 1 | 3 | [[2026-08-29-recuris-dual-memory]] - **the class rule held: the video was a pointer and the fetch was the extraction.** The channel never mentions the ablation table, and the ablation was the whole finding: a verified working state worth +23.9 points, the skill store alone +2.0, and removing the write review costing 13.5 - so `working-memory` (which said the state is "rewritten by judgment" and never named who checks the judgment) got a **propose / check / commit** stage anchored on `gate-sees-target`, and `procedure-promotion` got the sharper scope key (select by current verified state, not by history). Missing-stage shape again; cross-bundle convergence with `no-gate-self-certifies` and `coverage-is-counted-not-claimed`, named not linked. Three catches (trace-attributes-failure, patch-behind-held-out-gate = `oracle-frozen-during-repair` second sighting, workflow-is-not-capability). 2 of 3 fetches, both extraction. Unattended. |
| 2026-08-28 | `web:gamebalanceconcepts.wordpress.com` - Schreiber 10-week balance course (L1/3/7/9) | **first-party pedagogy corpus** (batch, /harvest) | course | 11 | 4 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - cost-curve-object-audit + intransitive-equilibrium-solving landed; reward-cadence + curve corollaries into the thinnest subject. Provenance catch: the four-term equation appears verbatim in L7. Lead: L4-5/8 unread |
| 2026-08-28 | `web:github.com/mxgmn/WaveFunctionCollapse` | **research-model release** (batch, /harvest) | repo+code | 8 | 3 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - three code-corroborated fixes: surviving-seed identity, pinned-seed-can-fail, success-flag-is-not-completeness, per-output-vs-corpus check classes. Lead: backtracking ports |
| 2026-08-28 | `web:github.com/CleverRaven/Cataclysm-DDA` JSON schema docs | **vendor repository** (batch, /harvest) | docs | 12 | 1 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - typed-operator-inheritance landed (relative/proportional deltas, abstract bases, restraint ladder, layer-safe merge); retirement-as-content approved -> spec bank |
| 2026-08-28 | `web:gamedeveloper.com` Machinations intro | **co-author promotion** (class corrected; batch, /harvest) | 3k~ | 7 | 0 | 1 | 1 | [[2026-08-28-game-production-harvest-batch-1]] - runnable-diagram cluster unpicked (untriaged). ATTRIBUTION FLAG: source never mentions tornado diagrams - check tornado-sensitivity-sweeps' citation. Lead: artificial players, three fetch routes failed |
| 2026-08-28 | `web:arxiv.org/abs/2503.18748` RL level balancing | **paper** (batch, /harvest) | paper | 4 | 0 | 1 | 0 | [[2026-08-28-game-production-harvest-batch-1]] - premise corrected: RL edits levels, players are scripted - confirms the corpus assumption. Approved cluster -> spec bank (goal-seek monotonicity precondition; estimator-convergence sim sizing). No human validation in the paper |
| 2026-08-28 | `web:github.com/jeffcampbellmakesgames/unity-asset-validator` | **vendor repository** (dormant; batch, /harvest) | docs | 8 | 0 | 3 | 2 | [[2026-08-28-game-production-harvest-batch-1]] - gate-design cluster unpicked (untriaged with anchors: check-scope ladder, one-ruleset-two-surfaces, contract-reified lookups). Catches on verdict shape |
| 2026-08-28 | `web:derek-lieu.com/essays` (3 of ~89) | **first-party craft corpus** (batch, /harvest) | essays | 9 | 3 | 2 | 1 | [[2026-08-28-media-generation-harvest-batch-1]] - concealment-and-its-tells landed as trailer-structure's 7th technique; mid-climax + opening amendments on escalation; claim-footage pairing on promise-ledger. No provenance echo of the six techniques. Lead: cue-first attribution UNVERIFIED |
| 2026-08-28 | `web:opendoclab.mit.edu` -> APA GenAI documentary standards | **field standard, PRIMARY UNREACHED** (batch, /harvest) | pointers | 8 | 0 | 2 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - PARKED: Drive-hosted PDF egress-blocked; nothing landed from model knowledge. Return: fetch the PDF, then triage audience-disclosure/consent/cue-sheet/archival-marking - real gaps |
| 2026-08-28 | `web:github.com/comfyanonymous/ComfyUI_examples` | **vendor first-party example corpus** (class corrected; batch, /harvest) | repo | 5 | 0 | 3 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - recipe-carrying-renders unpicked (untriaged). Leads: filename-only model pinning; params-below-the-prompt dialect |
| 2026-08-28 | `web:github.com/openai/openai-cookbook` image prompting guide | **vendor repository** (batch, /harvest) | notebook | 15 | 0 | 1 | 4 | [[2026-08-28-media-generation-harvest-batch-1]] - micro-content unpicked (untriaged with anchors). Currency: no negative-prompt param; long-prompts-fine vs prompt-budget-limits' rationale; constraints-LAST divergence. input_fidelity self-contradiction flagged |
| 2026-08-28 | `web:github.com/facebookresearch/audiocraft` | **research-model release** (batch, /harvest) | docs+cards | 10 | 0 | 1 | 2 | [[2026-08-28-media-generation-harvest-batch-1]] - music-family cluster approved -> spec bank (contour-only anchor 0.44, silence-collapse, 32kHz/-14LUFS, provenance-vs-NC-license split). Lead: JASCO temporal conditioning |
| 2026-08-28 | `web:github.com/hkchengrex/MMAudio` | **research-model release** (batch, /harvest) | readme+paper | 7 | 2 | 1 | 0 | [[2026-08-28-media-generation-harvest-batch-1]] - picture-as-timing-brief landed as sound-effect-generation's 5th technique + three defect-taxonomy entries. Text-vs-sync conflict flagged as inference, not author-stated |
| 2026-08-28 | `web:github.com/open-telemetry/semantic-conventions-genai` - GenAI semconv repo | **spec, pre-release** (batch lane, /harvest) | repo | 10 | 0 | 2 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - conventions split to a dedicated repo, all Development, breaking churn active; CONFIRMS attribute-precedence-lists' premise, precedence lists must now span two repos' registries. Return: first tagged release. Token-model cluster left unpicked - untriaged with anchors |
| 2026-08-28 | `web:github.com/Arize-ai/openinference` - OpenInference trace spec | **vendor spec** (batch lane, /harvest) | repo | 9 | 3 | 1 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - declared two-axis identity landed on provider-family-matching; shapes-not-names + cost-name currency on attribute-precedence-lists; no-crosswalk catch confirms two-doors-one-pipeline. Lead: sender-side redaction config |
| 2026-08-28 | `web:github.com/pydantic/genai-prices` - public GenAI price DB | **reference data repo** (batch lane, /harvest) | repo | 10 | 0 | 1 | 5 | [[2026-08-28-llm-obs-harvest-batch-1]] - five catches, zero content, the expected good run for the class: dated append-only history, prices_checked provenance, bundle+hourly refresh, flat variant fields all confirm price-book techniques in the wild. Untriaged: match-expression trees, time-of-day constraints |
| 2026-08-28 | `web:arxiv.org/abs/2306.05685` - founding LLM-as-judge paper | **paper** (batch lane, /harvest) | paper | 10 | 2 | 0 | 3 | [[2026-08-28-llm-obs-harvest-batch-1]] - reference-guided-grading landed as a new judge-contract-design technique (70/30/15 misgrade ladder); golden-set gained difficulty-conditioned agreement + verbosity-inflation fixtures. Swap-order already in the corpus's structural layer. Self-enhancement bias NOT established, by the paper's own admission |
| 2026-08-28 | `web:eugeneyan.com/writing/llm-evaluators` - evaluating LLM evaluators | **first-party practitioner synthesis** (batch lane, /harvest) | 8k~ | 12 | 1 | 2 | 5 | [[2026-08-28-llm-obs-harvest-batch-1]] - panel-of-judges landed on judge-selection-by-spread (externally corroborated ~1/7 cost). Binary-verdict claim CONVERGED with the trace-corpus source and resolved already-covered: anchor for authoring, binarize for kappa. Metric-layer cluster largely already present at body depth - see the batch note's lesson |
| 2026-08-28 | `web:hamel.dev/notes/llm/evals` - trace-driven evals corpus | **first-party practitioner account** (batch lane, /harvest) | corpus | 7 | 1 | 1 | 2 | [[2026-08-28-llm-obs-harvest-batch-1]] - error-analysis-first-taxonomy landed as a new production-trace-scoring technique: saturation rule, guaranteed random review slice, critique-to-few-shot. Tension recorded: capability-ceiling vs spread bake-off. Lead: active-learning loop video |
| 2026-08-28 | `web:habitat-thinking.github.io/ai-literacy-superpowers/plugins` + all 35 `explanation/` children - "ai-literacy-superpowers Concepts" | **first-party practitioner account, documentation form - channel corpus** (one author, 35 pages), wrapped in an **operator dispatch** (mine the batch real-gap-only; add a paired-proof phase to the X lane) | 61142 (after nav strip) | 26 | 4 | 2 | 13 | [[2026-08-28-ai-literacy-superpowers-concepts]] - **four sentinel pages were one missing stage.** Cadence governance, adversarial review, decision archaeology and the challenge-refine protocol each mapped near `hitl-approval` and none inside it: the corpus owns the pause, the rate and what an agent may change, and nothing owns *what a human is shown at the plan gate, in what order, and by which reader that is not the author*. XL spec dispatched (`plan-review` -> `llm-agent/orchestration`, 6 of a cap of 10 verified against `taxonomy.json`) folding the routing rule, the lens priority, the inadmissibility rule and the escalation ladder as four proposed techniques, with the emit/persist/dispose ordering explicitly NOT re-minted. **The asymmetry hunt paid twice in the inverse direction** - the corpus had the instrument and the source supplied the case one step outside its stated domain: `decision-records` reads approval-near-100% as a dead gate, and when the record's subject is an OBJECTION the same six fields read backwards (mostly-rejected-with-rationale is healthy; the why becomes mandatory on every disposition because it IS the engagement); `human-gate-capacity` measures the gate with four numbers, and at the subject's own one-person floor those numbers describe the person who is the queue - amended: count never score, advisory never a gate, local never a persisted judgment, the stop decided before the session; corroborated from training data (ego depletion at d=0.04 in the 2016 23-lab RRR; task-switching cost and vigilance decrement are what is robust), not from the page. **Skill 0.15.0 -> 0.16.0**: Phase 8 gains the operator's paired-proof step before any cross-repo commit (name the measurable; A/B at any scale preferred; before/after under `before-after-outcome-pairing`'s never-invent-the-missing-half rule; closed status set; `unproven` does not commit); corroboration gains *tier sources, never count them* (relays are downstream of primaries, so relay agreement with a stale primary is evidence of its PAST state); Phase 5 gains the unattended real-gap-only rule. 13 catches, all against a mature `llm-agent` / `quality-gates` neighbourhood (read-only emitter, rung-vs-reach = `unmeasurable-criteria`, range-not-point = `preflight-estimation`, structured refusal = `stop-reason-ledgers`, affordance identity = agent identity + human behalf already both retained). 4 untriaged partials with anchors (calibration refusals as signal, path-as-state records, advisory precedence by re-emittability, the rung/reach vocabulary failure). 2 leads with return conditions (the sentinel category; calibration refusals). Zero fetches - tenth consecutive corpus-internal run. No connected tree touched: the dispatch's project half was the skill phase itself |
| 2026-08-28 | `web:addyo.substack.com/p/audit-your-agent-files` - "Audit your Agent files" | **second-hand practitioner listicle + first-party audit half** (hybrid: three papers relayed, one own-setup account) | 3046 | 13 | 2 | 2 | 4 | [[2026-08-28-audit-your-agent-files]] - the routing table's listicle tiebreaker decided the whole run: *items touching this registry's own machinery outrank items about bundle content*, and every picked row was machinery. Subject `agent-instruction-files` was forged four days earlier with six techniques over this source's exact territory, so nine of thirteen candidates were catches or unpicked - and the two that landed came from the one question the subject never asked. **`instruction-freshness` couples the file to change not calendars, and every trigger it lists is repo-side** (stack bump, command rename, restructure, deletion); its audit asks whether a rule's failure mode still exists *in the repo* and never whether the *agent* still has that failure. The subject's own denial hid the gap: rejecting calendar cadence ruled out the only schedule that catches substrate drift, because a model upgrade produces no diff to couple to. Corrective is not the source's "audit on a cadence" but **a second change list** - a model generation lands, the harness ships natively what the file instructed around; both datable. `line-earning` is the same seam from the other side: it asks whether removal changes behaviour then concedes the measurement (*"the honest way to know is the line's origin story"*), which is evidence about the model that FAILED and diverges exactly when the substrate moved. **The instrument was already in the subject at the wrong polarity** - `capability-before-steering` (landed by a parallel session mid-run) isolates a capability gap by checking whether a failure "persists at the top of a fresh, minimal file"; run the same rig with the rule ABSENT and it measures `line-earning`'s second half directly. The subject built one door of a two-door apparatus. **The fetch beat the source twice.** The relay flattens the primary to "instruction value can expire"; the vendor post supplies the mechanism - the expired line is disproportionately the RESTRAINT (its example guardrail was *correct* for older models, removed because newer ones "have better judgement"), so an expired restraint does not idle, it **suppresses behaviour the current model would have got right**, which is `restraint-amplifier-balance`'s loss by a route that subject did not model: a file that BECAME all-cage rather than one authored that way. And expired lines **contradict** - the vendor read its own transcripts and found conflicting directives spanning system prompt, installed capabilities and user request, so two lines can each pass `line-earning` and be a defect as a pair, invisible to any per-line audit. Class lesson worth carrying: **the primary states no removal procedure at all** - it reports the 80% result and offers a tool; the relay's protocol (complete the task on raw model and harness) is the only candidate procedure either source carries, which is why the finding is written from the corpus's own apparatus rather than from either. Second technique `sibling-floor-ownership` takes the half the subject explicitly declined: its opening names the siblings and leaves them to the harness, but they are not the harness's furniture - they are capabilities a person installed one at a time, and the position "paid, advisory, always-loaded" holds for them word for word while being the LARGER half. `creation-names-reaper` is the anchor: install is locally justified, nothing schedules the retention decision, and the admission funnel runs per line on a diff while an install produces no diff. **Phase 4's "one moment" warning fired for real** - a parallel session landed two techniques into this exact subject between the map and the write, and re-reading was load-bearing rather than bookkeeping. 2 of 3 fetches, one wasted guessing a vendor URL. Zero declines; unpicked rows recorded untriaged, the personalization paper flagged as a cross-run convergence candidate against the 2026-08-22 pair |
| 2026-08-28 | `web:blog.google/.../build-with-gemini-omni-1-1-flash` - "Gemini Omni 1.1 Flash lets you build with more control" | **vendor release announcement** (new class) - the marketing surface of a release, WITHOUT the walkthrough half | 1943 | 12 | 1 | 0 | 3 | [[2026-08-28-gemini-omni-1-1-flash]] - **a release announcement is not a release walkthrough, and the difference is measurable.** The walkthrough row says seek it out because a change carries its own motivation - the author says what was wrong before. An announcement is organised around changes it is PROUD of, which inverts exactly that property: it states what is now possible and never what was wrong, because what was wrong was its own last version. The discriminator to carry: **a walkthrough states failure modes; an announcement states NUMBERS, and its numbers are the yield** - the prose around them is the strip test's problem. Every accepted finding here came from a number, none from a sentence. **For this class the fetch is the extraction, for a reason specific to it**: the post said the model analyzes "up to 10 seconds of prior context", which is compatible with sampling ten seconds from anywhere; the API docs say **"the last 10s"** - a trailing window. That distinction IS the finding, and marketing blurred precisely the part carrying the mechanism, because *which* ten seconds is not a selling point. 2 of 3 fetches (the first hit the general video page, which carried no numbers and named the second). Landed one amendment on `generated-shot-sourcing`: the technique said continuity carry extends from the **"whole previous clip"** - wording that entered the corpus ONE DAY EARLIER from run 25 - and the vendor's own doc refutes it. Extension conditions on a bounded window off the tail; the window is a **versioned fact belonging in the vendor ledger** (final-second to final-ten in one release, on a parameter no caller sets); what has scrolled out must be re-supplied, which is chain drift arriving one level down and taking the answer the subject already owns (pin the origin, roll the window); the chain has a **cumulative** ceiling, so extension does not lift the clip cap but re-denominates it - what it buys is a larger unit to place the seam between; and the increments are quantized while authored durations are not, so a derived duration is paid at the timeline in a trim the file's own decision rules already warn about. Also corrected the moving-reference trim from economy to obligation: references cap at 3 clips x 3s, tight enough that a reference is a SAMPLE, and **an unstated span is chosen by the platform - a span nobody chose is not a scoped reference however carefully its negative scope was written.** Three catches, all where the corpus is AHEAD of the vendor: rung-3 anchors (corpus carries the mid-clip glitch and the one-cloth rule; the post demonstrates the feature and states neither), baked audio, and the 360p draft tier. **Self-correction on the last one**: I thought `resolution-as-stage-property` had an upscale-shaped hole in its promotion-is-a-risk-event enumeration - it does not, it already prefers true upscaling of accepted pixels over re-sampling for exactly that reason. Phase 6.1 paid; the correction would have been a phantom fix against my own summary. Operator picked row 1 only, declining the currency reset, the named consumer application and the id-addressed-continuation lead - **no project tree was touched**. 5 untriaged with anchors. **The subject's structural debt is now overdue and this run added to it**: yesterday's note said the conditioning ladder carries five riders and the next pass should rebuild the golden path around a widened ladder "rather than accreting a sixth section" - this run accreted the sixth. Right content, wrong shape; that is a /deepen job. Parallel session live in the checkout the whole run - three untracked techniques appeared under `agent-instruction-files`, the gate was red on their account from Phase 1, and a blind build-index swept them into the software-engineering index (reverted, catalog rebuilt from the corrected state, media-generation + catalog committed alone) |
| 2026-08-28 | `web:github.com/microsoft/AutoSaddler` - "AutoSaddler: Automatic Harness Optimization with Durable Updates from Agent Execution Traces" | **research-code release** (paper + implementation, one squashed commit) - ingested at its README, this class's LEAST reliable surface | 1696 | 14 | 3 | 1 | 1 | 9 untriaged | [[2026-08-28-autosaddler-harness-optimization]] - **the class row is right about reliability and wrong about where the yield is.** It says the README is an advertisement and the least useful file present; the ingest returned ONLY the README and still landed three findings. The property the row should carry: **a research release's README advertises a METHOD, and a method's advertisement is made of its DISTINCTIONS** - a patch taxonomy, a session-type taxonomy, a four-way outcome classification, all claimed as novel and therefore all present, and a taxonomy survives the strip test where a benchmark number does not. Contrast the vendor repository, whose README advertises a PRODUCT and is therefore made of names. The +9.0/+9.6/+10.0 pp table was proposed as nothing. **1 of 3 fetches, spent as extraction not corroboration**, and it made the finding bigger than the source did; the other two corroborated corpus-internally - ninth consecutive run where the corpus was its own second source. **Finding 1 came from one sentence in step 3 of a reproduction recipe** ("run from the external working directory so generated workspaces cannot inherit repository-level agent instructions through Git ancestry"). `agent-instruction-files` models the file entirely from the AUTHOR's chair and sorts loading into always-loaded / loaded-on-touch / per-user - **all three categories describe files an author placed deliberately**, and a directory a program creates has no author and is governed anyway. Confirmed a hole not a seam by the **enumeration hunt, paying a ninth consecutive run**: the golden path's seven failure modes are every one of them *the file's content being wrong*, and not one is *a correct file governing a directory nobody meant to brief*. The primary settled the mechanism and doubled it - files load from the cwd "and every directory above it", concatenated not overridden, **with no boundary at the repository root**, PLUS per-directory files BELOW the launch point loading lazily when the agent reads them. So it is a cone, not a chain, and **downward injection is sharper than the source's upward case**: a third-party tree cloned under the working directory ships its own instructions into context the moment the agent reads anything in it. The source's own recipe clones exactly such a tree and never mentions it. Fetch also yielded the fallback (`absent-guard-is-loud` - a path-exclusion setting protects the machine it is set on) and the verification (the harness can report which instruction files a session resolved), so the isolation claim is asserted against what loaded, not against the layout - the same treatment this subject already gives an enforcement claim. **Finding 2 is a second enumeration denial found in a subject's STRUCTURE rather than its prose**: `line-earning` asks is-it-unreachable and does-it-change-behavior, `enforcement-demotion` asks could-a-program-decide-compliance (yes->gate, no->prose), and **both branches of the demotion sort presuppose an agent that CAN perform the behavior**. Neither asks whether it could have complied at all. What makes it a technique rather than a nicety is corpus-internal cost: this subject rests on compliance falling with instruction count, so **a line written against a capability gap cannot work AND taxes every line that could - the one instruction-file edit that is strictly negative**. Named signature: **a rule restated with escalating force across several revisions is nearly always a capability gap being sharpened as though it were a wording problem.** Landed above the source's altitude - the mechanical discriminator (attempt the behavior outside the agent with exactly the means the agent had) is the corpus's, with `gate-sees-target` because asking the agent reads a proxy and a model will readily accept blame for an impossibility; the two short-circuit tells (uniform-across-sessions = capability, since genuine steering failure is probabilistic; persistence at the top of a minimal file excludes steering by construction) come from the dilution model the subject already owns. **Finding 3 triaged as a technique and resolved as an amendment, the cheaper and more honest landing.** `failure-attribution` says a red case is "owned by exactly one" of six owners - complete for a single-call system, and an agent is not that shape. The finding is not that a row is missing, it is that **the funnel's own tells route tool failures to the wrong owner and the wrong owner is the expensive one**: an agent calling the right tool with wrong arguments because a field name was ambiguous fails the prompt's tell (a person reading only the prompt would NOT make the same mistake) and fails the pipeline's tell (raw output and recorded outcome AGREE), so it falls through to **model**, whose prescribed response is a different model or an accepted limit. **A strictly correct application of the technique produces the exact mis-attribution the technique exists to prevent**, prescribing a model migration for what a rewritten tool description would fix. Seventh owner placed between pipeline and prompt; golden path's "six layers", the two-of-six heading and the resist-all-six line all corrected. **The XL subject proposal dissolved on verification and that is the catch**: the near-empty was misleading (no subject slug matches "harness optimization") but the material decomposes cleanly across two mature subjects - **a method with a paper is not automatically a subject, it is often a route through subjects the corpus already has.** Lead banked on the +9-10pp claim with a return condition; 9 untriaged with anchors, row 4 (the *regressed* cell a shallow reflection loop never computes) flagged as the likeliest real finding on a second look. Zero declines - the operator left rows unpicked rather than rejecting them, so the decline ledger is untouched |
| 2026-08-28 | `web:acceptmarkdown.com/reference` + its eleven named children - "Reference - Markdown content negotiation for AI agents" | **standards index** (new class) - a link hub whose children are PRIMARY standards | 281 (hub) / ~87k (children) | 11 | 2 | 3 | 0 | [[2026-08-28-acceptmarkdown-reference]] - **the class whose hub is structurally too thin and whose yield is structurally high.** `research-ingest` exited 3 on a 281-word page of pure pointers, correctly; everything mined came from the eleven documents it points at. The row it superficially resembles is the second-hand survey - somebody else's links - and it is the opposite: a survey is reliable only for *that* the world moved, while a standards index points at documents that **authorize a golden path outright**, no convergence wait, no second source. Corrective: ingest the hub for its link list, then run the children as a batch; do not judge by `--min-words`. **6 fetches, all extraction, corroboration cost ZERO** - when the children are RFCs the primary is already in hand, which is the inverse of the review/listicle economics the budget was written for. **A hole, not a seam, verified three ways**: `content negotiation`, `Accept header`, `Vary`, `406`, `text/markdown`, `rel="alternate"`, `q-value`, `llms.txt` all return ZERO across 3,389 documents in 8 bundles; `research-map` returned only slug collisions ("content" in `content-drift-and-revision`, "header" in `browser-credential-boundary`), the near-empty signature - so the two nearest real neighbours were opened before anything was written, and both are different actors (`client-fetch-cache` owns the CLIENT's memory cache, not a shared intermediary key; `docs-content-model` names a "machine-readable site inventory" among its projections but gives every consumer the same body, so it has no representation axis). The 150-subject bundle has no HTTP-representation subject at all: a layer, not an opinion, which is what made it `XL`. Subject spec dispatched (`representation-negotiation` -> `integration`, placement verified against `taxonomy.json`: the category is **flat**, 8 of a cap of 10, so a flat add is legal and a subcategory would be wrong) with **five fragments folded in** that each looked standalone at extraction, both rejected placements argued from their own boundary statements, four must-not-absorb boundaries, and an explicit instruction to override the brief. **The hub's two framing errors were worth more than its correct links.** It files `.md` sibling addresses + `Link: rel="alternate"` under a heading citing the *proactive* negotiation section - those are **reactive** negotiation, and the standard makes the choice a real cache trade (proactive "limits the reusability of responses for shared caching"; reactive is advantageous "generally when public caches are used"), so the conflation hides the trade and became proposed technique 1. And it asserts a refusal status is "the right response" when `Accept` cannot be satisfied - the standard says an origin "can either honor the header field... **or disregard** it", making the refusal a policy choice, not a correctness requirement; that correction is written INTO the proposed technique so the received wisdom cannot enter through the back door. Third error recorded as a class signal only: it cites "RFC 9110 SS12.5.1 Proactive Content Negotiation" - SS12.5.1 is `Accept`, proactive negotiation is SS12.1. **A standards index mis-citing its own headline entry is the failure this class walks into, and it costs one grep to check.** **The landed finding was the enumeration hunt, paying an eighth consecutive run** - and the enumeration was stated TWICE in one file, which is the reusable form: `web-scraping` gives its alternatives ladder in the opening ("an official API, a feed, an export, a negotiated data license") and again as a four-bullet section, and neither contains *the same page in another representation from the same server* - the cheapest entry on the list, one request, no contract, no key, same address. When an author reaches for a list from two directions and gets the same set, the gap is in the model, not the typing. The acquire row had exactly two modes (plain fetch / rendering engine), both assuming the browser document is all that is on offer. **The naive reading is wrong and that is what made the technique**: "negotiation replaces scraping" holds only for a representation **authored at the origin** (the feed branch - rules are overhead); one **converted from the same markup on the fly** - the common managed case - is a derived value whose recomputation path is "convert today's markup", so it inherits the redesign it appeared to protect against, relieving the parse stage and buying nothing against the shape-change adversary (`derivation-names-recomputation`). So `negotiated-representation-probe` exists to close a trap that only appears AFTER the probe runs: derived case returns, text looks clean, pipeline gets RELAXED - tripwires loosened, hit-rate baselines abandoned because no selectors remain to count - converting the subject's own shape-change detector into a **shape-change blindfold at the moment the input looks most trustworthy**. Its table makes one row unmissable: derived => every instrument unchanged. Third probe outcome kept from the standard: the server may simply disregard the preference, so read the response's declared type and never your own request (`unknown-is-not-a-value`). **The application named in the dispatch reports a structural fact, not the ratio**: an edge offering publishes a worked response measuring 725 tokens against 12,345 (~17x, doc dated 2026-07-13, one input format, 2 MB origin cap) - an existence proof, not a distribution - but it is BY CONSTRUCTION the conversion arm of proposed technique 5 and not the projection arm, since "no origin changes" is the selling point. **The most widely deployed instance of this subject is the one that structurally cannot make the drift guarantee**, and nobody designed that. Zero declines - a property of the class: primary standards do not produce the half-corroborated middle that generates them. Housekeeping: `build-index --check` was stale on arrival from commit `ccc7798`, regenerated and folded in |
| 2026-08-27 | `web:latticedb.org` + its documentation - "LatticeDB, embedded property-graph database" | **vendor documentation set** (marketing surface + four comparison guides), wrapped in an **operator dispatch** | 1069 (landing) + 1524 (two comparison guides) | 12 | 5 | 4 | 2 | [[2026-08-27-latticedb]] - **the anti-vendor-repository, and the class file needs one amendment.** The row predicted it exactly: the benchmark table was the least useful thing present and every accepted finding came from the docs' honest-boundary pages. But the densest first-party document here is **the page titled after the competitor** - "vs Kuzu" states above its table (not in a footnote) which single row was measured head to head and which are third-party figures on hardware it does not control, then says to treat the rest as "worth investigating on your own data, not a benchmark result", and devotes a section to what the archived rival did better. **For a vendor in a crowded class, seek the page arguing against its own product.** The operator dispatch's three sub-questions routed three ways and only the third paid - second consecutive run confirming split-at-Phase-3. The run's best finding came from the vendor **getting it wrong**: its flagship query computes BM25 relevance and discards it into a boolean filter while vector distance alone orders the result, in the headline example of an engine whose whole pitch is answering the lanes together - so `hybrid-lane-fusion`'s rule that multi-lane convergence is evidence took an amendment naming its unstated precondition (**lanes must be independent**; a lane seeded by another's output counts one signal twice, hardest where the seeding lane was most confident, and diversity cuts pass it because the items genuinely differ). The **enumeration hunt paid a seventh consecutive time**: `retrieval`'s roster is lexical/semantic/recency/always-include - two similarity matchers, a clock and a pin, and **none can surface the item that DISAGREES with the top hit**. New technique `relationship-proximity-lane`: not a peer lane (a function of results, so its budget is a fan-out cap), reach is not path enumeration, per-relation policy, and **owning edges is not the trigger - enumerate the readers of the relation**. The application is negative and the tree proved it: `personas` implements the roster faithfully and holds TWO typed relation stores with no request-path reader - one written by a ledger and read only by export/import/wikilink-render, the other a table plus a **reverse** index built for inbound traversal, a six-name vocabulary including `contradicts`, and an 8-line module ending `Phase 0: stub. Phase 2: traverse, add_edge, contradict_scan` with no writer and no reader. It named the one relation similarity cannot reach, years before anything could consume it. Two more techniques folded into the storage-engine spec (now eight, second reconciliation tree recorded): **a dataset is not a workload** (`politicas` HAD the workload and measured the engine losing; `personas` has the data and no workload) and **a multi-modal engine consolidates seams, not costs** - read its benchmark suite for the class it OMITS, since LatticeDB is strong in the four classes `politicas` measured as "add nothing" and structurally cedes the one it measured as "add something" (43x). Executed cross-repo in both trees on operator instruction; `personas` scoped to a decision record only because its `db` crate is mid-refactor under a parallel session. 2 of 3 fetches, both extraction not corroboration; corroboration cost zero and came from two connected trees - seventh consecutive locally-corroborated run. |
| 2026-08-27 | `web:github.blog` - "How to evaluate LLMs before production" | first-party practitioner account (**hybrid**: numbered how-to wrapped around one shipped system) | 2716 | 12 | 4 | 1 | 3 | [[2026-08-27-evaluate-llms-before-production]] - **the run where every finding came from a sentence in which a forged document claimed its own completeness.** Route-per-half decided the whole triage and held perfectly: sections 1-7 are generic how-to with a listicle's reliability, the first-party half is the sentences about their own classifier, and every landing came from the second half while every catch came from a section heading. 0 of 3 fetches - eighth consecutive zero-fetch run - and the strongest corroboration was sitting two subjects over in the same bundle. The neighbourhood was the corpus's most mature (`eval-harness` at 8 techniques, five `quality-scoring` subjects on judges and gating), so **nothing was a hole and all four findings were too-narrow enumerations**. `scenario-design` listed five ugly-case regions and the source's failure is in none of them: **`distractor` returned ZERO corpus-wide** and the missing region is well-formed, in-distribution, unambiguous, single-feature and non-adversarial - the model evaluates the wrong object because the context holds a more attention-grabbing neighbour than the designated target. The mechanism is the keeper: **curating a scenario set removes distractors as a side effect of tidying it**, so the suite measures *identify the salient item* when the task is *evaluate the designated item*. **Cross-bundle inversion, named not linked**: `recruiting/llm-era-work-sample-design` forbids distractors outright because a decoy hands a judgment probe an answer key - the discriminator is **captured or planted**, and a subject note was created on the recruiting side to record it. Second amendment corrected "captured reality is representative by construction": true of the **input**, false of the **label**, which gets back-filled from a workflow outcome that renders four ground-truth states as one definite value (`unknown-is-not-a-value`). That one corroborated **entirely corpus-internally** - `proactive-nudges/efficacy-feedback` keeps a waved-away prompt "deliberately ambiguous" and `remediation-handoff/evidence-based-auto-close` refuses to collapse a dismissal into a resolution, so the rule already lived in this bundle and had a free pass in the eval lane. `metric-role-contract` landed **above the source's altitude** - the source gives a three-tier table, the durable rule is that its third tier is just more of its second, so: **exactly one metric is optimized and everything else is a threshold**, with the constraint chosen by **irreversibility, not magnitude**, and a breach disqualifying regardless of the primary's gain. Home was contested (gating owns "between two runs"; a metric contract precedes the first run) and the boundary is stated in prose, not linked, because the subjects are in different bundles. `failure-attribution` closed a **missing stage** found by the asymmetry hunt: the subject distrusts a green verdict in two techniques and a golden-path section and had never distrusted a red one. Six owners checked most-upstream-first, and **two of them are not the system** - a label or dataset failure goes red with no defect present, and the fix for either improves the score while moving the system away from correct. Banked lead: `comparison-modes` calls absolute scoring "the only mode that supports a gate (ship if above X)", now the one-metric degenerate case - return on a second independent source or an opened harness. Shared checkout: another session landed 4 techniques, 2 applications and a practices lane mid-run; index/catalog deliberately NOT committed |
| 2026-08-27 | `web:github.com/context-labs/whip` - "whip, a fast coding-agent harness in Go" | open-source infrastructure engine (**engine + operating docs in one tree**) - sub-class: **comparative-design corpus** | 678 (page) / ~15800 (in-tree docs) / ~10300 (rival teardowns) | 16 | 7 | 0 | 0 | [[2026-08-27-whip-coding-agent-harness]] - **the sub-class whose yield is other people's refusals.** The archetype held (seventh consecutive zero-fetch run; the 678-word landing page was install instructions, the source was 15.8k words of design docs plus the tree), but this tree carried something picomq did not: first-party teardowns of FOUR rival implementations of the same product category, each cited `file:line` into that rival's source, plus a PTY probe of five competitors' first paint. **Every "what we should NOT take, and why" section is a discriminator someone was forced to draw** - the research-model-release sibling-instruction property, at a section per rival instead of one per release, and four of six landings came from that half of the tree rather than the engine's own docs. The prediction that high relevance means a high catch rate was reasonable and instructively wrong: the corpus held every *concept* in this source and was missing *stated boundaries* in five places, so every landing came from an enumeration, an asymmetry or a missing producer. Two enumerations paid: `subscription-lifecycle` says its fan-out loop "has one sharp edge" and gives snapshotting, which answers set mutation and not lock ownership - the second edge is the ABBA deadlock between a dispatcher holding the registry mutex and a subscriber parked on the UI queue. `single-flight-primitives` took its second amendment in two runs (picomq added `merge` as a sixth policy; this scopes `join`): join is written for a computation, and pointed at a durable resource the execution recurs while the one-shot completion signal cannot. The missing stage was bigger: the `prompt-assembly` layer table has five layers and every one is *authored*, so the growing tool-use transcript - the only part of the prompt that grows because the system works - was owned by nothing, and `history-compaction` landed with the pairing invariant, the third resume regime `continuation-prompts` does not enumerate (carried and structurally broken), and the reactive compaction path with its once-per-turn guard. The missing *producer*: `mid-turn-steering` models the injection mechanism completely and models exactly one producer, the human - and its sharpest rule ("refuse loudly when no turn is in flight") inverts for machine sources because its own justification rests on the caller having a second door. | 
| 2026-08-27 | `web:github.com/SenteLabsAI/OpenExecutive` - "AI-powered virtual executive team" | vendor repository (**open tree** - the archetype's engine is closed, this one's client is not) | 2723 (landing page) / ~1950 (in-tree operating docs) / 894 files | 14 | 5 | 4 | 0 | [[2026-08-27-openexecutive-virtual-executive]] - **the class variable is not whether the engine is hosted but what fraction of the product is in the tree.** The class entry sends you hunting for the data model in a client's types because the archetype's client is closed; here the engine is hosted and the whole application is open, so the "stated production rules" third is not a page but nine hundred files, and every landing came from code rather than from any document. 0 of 3 fetches - seventh consecutive zero-fetch run for a source carrying its own primary material. **Three of five accepted candidates share one shape: the prose states an absolute, the code knowingly breaks it for a stated correct reason, and the prose never names the reason** - the contradicted-source pattern arriving from inside a single source, and cheaper than usual because both halves ship in one tree and no fetch can adjudicate what the code settles. Landing 1: four artifacts (readme, both environment configs, architecture doc) blame the claim for forcing single-instance while the comment beside the claim says it is safe across processes - the line that forces it is an unconditional startup requeue named nowhere. `terminal-state-recovery` already calls the blanket sweep an anti-pattern in depth and frames it **entirely as verdict correctness**; a grep for instance/scale across it and `lease-renewal` returns two incidental matches. **A subject that models process death in five techniques, silent on the one number process death decides** - amendment, not technique, since the fix is defined in the existing `adopt` verdict's own terms. Landing 2: `layered-composition` orders by volatility and stops at ONE implicit boundary; `context-budgeting` prices compression; `cache-continuity` prices the prefix as a monolith - **nobody owned the cut**, that declared breakpoints are a scarce request-wide budget shared with tools and history, each with its own lifetime. New technique `cache-breakpoint-allocation`; the golden path's layer table already had a *Changes* column and the technique names it the allocation map (cut where the column changes value, not where the topic does). Folded in the correction that *no dynamic content in a cached block* is the wrong discriminator - it bans process-stable computed values and permits a hand-authored string rewritten per request; the real test is **can this value change before this block expires**. The advertised "85% hit rate" is a count with its predicate removed and went in as a caution, not a citation. Landing 3: found by the **asymmetry hunt** - `authentication-and-scoping` models inbound in full, egress gets ONE bullet in `untrusted-result-handling`; both files "cover" the seam and only opening both shows one has a model and the other a sentence. New technique `egress-argument-gating`: two orthogonal scans each blind where the other sees (value-shaped survives unknown field names, key-shaped survives non-string capabilities, and the gateway writes its own residual hole down); strategy chosen by whether the argument surface is enumerable, with **both strategies in one file** and the discriminator stated; the gate is directional so revoking stays permitted; and the sharpest sentence in the repo - **a principal allow-list cannot express a population**, so a share-to-anyone passes a well-formed roster check that found nothing to reject, and must be refused by a rule running BEFORE the allow-list. 4 leads (mandatory segments outside the overridable region; topology from tenancy cardinality; auto-disable of non-portable provider features; agent must not sign as a rostered human), 5 untriaged with anchors, 0 declines. Shared checkout: a parallel session registered its own new technique mid-run, after this run's `build-index`; generated files deliberately NOT committed |
| 2026-08-27 | `web:langchain.com/blog` + `web:github.com/langchain-ai/openwiki` - "Building Self-Correcting Memory in OpenWiki" | **paired single-voice source**: first-party release walkthrough + its own vendor repository | 1559 (blog) / 4739 (repo) | 17 | 8 | 3 | 0 | [[2026-08-27-openwiki-self-correcting-memory]] - **the run whose best finding was the one I got wrong first.** Two artifacts, one vendor, so within-batch convergence is VOID and every landing earned its way corpus-internally: 0 of 3 fetches, seventh consecutive zero-fetch run for a source carrying its own primary material. Mapping our own contribution lane I read the two live `signals/` files, saw `consults` + `deviations`, and concluded there was no channel for a verification verdict - then read the checker, which accepts a third key `citations` (`resolved`/`moved`/`gone`, keyed by slug never by path, counts-only with the reason in the failure message). **The corrected finding is strictly stronger**: the channel is specified, the collector - the lane's ONLY writer - has no code path that emits it, and the scan reads `gone` while discarding the two counts that carry the denominator. Specified, unpopulated, half-consumed. Two runs now say it, so treat it as settled: **structural claims are verified against the authority - the checker, the schema, the taxonomy - never inferred from the data lying around**; data shows what contributors happened to send, only the validator says what the lane is. Eight of seventeen candidates clustered on one neighbourhood - the lifecycle of a claim whose evidence moved - which the corpus covers at file granularity, at artifact granularity, with verdicts in another bundle and by recency decay, and owned nowhere at **proposition** granularity. Landed 4 techniques, 2 amendments, 1 negative application. The best knowledge finding was an **asymmetry**: `docs-sync` measures what a REPORT may claim with total rigour and says nothing about when a DOCUMENT may advance its own review date - the exact field wall 7's detector consumes to choose its query window, so an unearned bump moves the detector's horizon past the changes it exists to find and it reports clean forever. Seam: `docs-content-model` owns the field's shape, `docs-sync` owns queries over it, neither owned the write rule. The contradicted pick was again the best one - a complete-set reconciliation contract that is right, with **omission as its retraction signal**, which makes forgetting and deciding byte-identical. Two picks merged into one technique twice; when the operator picks two rows that are one decision, land one file and say so. |
| 2026-08-27 | `web:github.com/picomq/picomq` - "PicoMQ, durable streams over object storage" | open-source infrastructure engine (**engine + operating docs in one tree**) | 453 (page) / ~14000 (in-tree docs) | 11 | 3 | 2 | 3 | [[2026-08-27-picomq-durable-streams]] - **the class whose promise is in-run falsifiability, and it fired twice.** Engine, ~14k words of design docs and a wire spec ship in one tree, so every claim was checkable against the code implementing it: 0 of 3 fetches, sixth consecutive zero-fetch run for a source carrying its own primary material. The ingest returned 453 words because a repository landing page returns install instructions; pricing the run off that number was the first mistake, corrected in one phase. **Age predicts the reliability of a project adoption claims, not the density of its design decisions** - 15 commits and days old, and it had just finished making every hard choice it will ever make. The yield was a contradiction: the write-path doc describes group commit as self-tuning and knob-free (batch = whatever arrived during the previous flush), while the shipped config carries a 250 ms timer, an 8 MiB cap and a 50-deep in-flight pool - **and the same repo implements the idealised version correctly in its metadata sink**, which blocks for the first command, drains what has queued, commits, no timer. Two group commits, one tree, opposite defaults: the **sibling-systems property**, not specific to model releases after all, with the discriminator visible in the diff - serial flushes self-close, pipelined flushes cannot and must reintroduce a timer. Home came from the **enumeration hunt**, sixth consecutive paying run: `single-flight-primitives` lists four second-caller policies and tells the reader to pick one explicitly, and **merge is a fifth it does not contain** - join returns one shared result, coalesce discards intermediate arrivals as waste, merge carries N distinct payloads through one execution and discards nothing. A designer walking that list for a durable write path finds no correct option. All three findings landed as **amendments, no new techniques**: the merge policy plus a window-closing section; a cross-store **inversion** of the purge ordering rule (payload before reference - one direction leaks a retryable pointer, the other leaks bytes nothing references, and only one leaves a reaper); and lease-cost-as-engineered rather than measured. Two leads banked (stale-view decision classification, awaiting a second sighting to land as doctrine; transfer pre-warm), three untriaged, three caught. |
| 2026-08-27 | `web:kciter.so` - "How to design animation" (Korean) | first-party practitioner account (**methodology essay** - new sub-class) | 3615 | 8 | 4 | 0 | 2 | [[2026-08-27-kciter-animation-design]] - **the class inverts the usual economics: the strip test killed nothing.** The source is mathematics and design vocabulary, not proper nouns, so every candidate survived the cheap filter and the whole cost moved to prior art. `motion` is a 9-technique forged subject that a slug map returns for every animation term - but it is a **governance** system (what the vocabulary permits, who owns the engine, what stops a loop) and the source is an **authoring** method (how a movement becomes a thing worth naming). They touch at one seam and the seam held all four findings. **`research-map` returned a TOTAL empty on `spring physics` over material the corpus covers in depth** - the purest near-empty failure yet recorded: springs are all through `engine-selection` (retargeting from position *and* velocity, the interruption story) and in the consumer evidence file as a real rAF engine. A zero from the instrument is a claim about slugs. The genuine gap was invisible for the same reason - no slug expresses "the contract assumes a timed curve". **Landed as one root plus three amendments, not four amendments**: the preset contract (intent, duration class, easing role, fallback) was forged for one curve started by something and running to completion on its own clock, and each under-served class breaks a *different* assumption - a **spring has no duration** (and `taste-budgets` files it as "one characterful curve", a third incompatible reading), a **composite gesture is more than one curve**, and **value-driven motion has no clock of its own**. New golden-path section states the boundary; the rule is now intent+fallback per gesture, timing+character **per track**, each track declaring a duration class with an easing role OR physics parameters with a **settle bound** - because a gesture with no duration escapes the millisecond entrance cap entirely, which is how the one unbounded gesture becomes the one nobody audits. Its reduction is an **instant settle**, not a shorter curve. `engine-selection` opened "one of **three** engines" - an enumeration, and the missing member is an authored timeline exported as data, whose ownership story inverts every answer in the section (no global switch reduces it because the platform does not consider it animation; its reduced-motion story must be a **second exported variant**); the discriminator is not complexity but **must it respond to input while running**, with the trap that a trigger is not an input. `unprompted-motion-lifecycle` said "for unprompted reveals the answer is **always** one-shot" - correct for scroll-*triggered*, a bug for scroll-*driven*, where reversal is the point; scoped rather than deleted, with a one-question review test (**if the user stopped scrolling, would anything keep moving?**) and the substitution that a driven gesture owes bounded travel but no stop control. New technique `gesture-decomposition` (10th) closes the missing stage - the subject was thorough from "a preset exists" onward and silent on how a movement becomes one, which is the door per-component keyframes return through. **0 of 3 fetches** - sixth consecutive zero-fetch first-party run, and here the claims are mathematics, so no fetch could have added anything. Reusable: **when a contract has mandatory fields, check them against what the sibling technique permits** - two of four fields were unfillable by an engine the subject itself recommends and neither file noticed. Shared checkout: 4 commits landed mid-run and another session's WIP was live; index/catalog deliberately NOT committed |
| 2026-08-27 | `web:scottspence.com` - "Agentic engineering: a practical guide to reliable coding agents" | first-party practitioner account (**guide-shaped**) | 2600 | 15 | 1 | 2 | 8 | [[2026-08-27-agentic-engineering-practical-guide]] - one landing out of fifteen, and the ratio is the lesson. **Every accepted word came from the author's "they also exposed weaknesses" list; every recommendation in the guide was a catch.** New operating note for the class: in a first-party account read the failure list first and the recommendations last - a recommendation is what the author believes, a stated failure is what their system did to them, and only the second is reliably absent from a mature corpus, because the corpus was built from other people's recommendations too. **Guide shape named as a hazard**: it reads high-yield (organised, confident, comprehensive) and is structurally low-yield, since comprehensiveness over known ground is what a mature bundle already has. The picked candidate's headline - put the trust boundary where the agent cannot edit it - is already owned and better (`hitl-approval` has a section titled "The gate lives in the substrate, not the prompt", anchored to `gate-sees-target`, with two corollaries the source never reaches); stopping there would have shipped a duplicate. The finding came from the **enumeration hunt**, fifth consecutive run it has paid: the golden path declares "the subject owns **two flows that are mirror images of each other**", then adds "A third flow" - and all three gate an **action**, while the source's measured failure ("plans needed legitimate amendments more often than I expected", over 173 real harnesses) names a transition none of them covers. The machine's **terms** - scope, route, definition of done - were governed by nothing, assumed static from dispatch. The subject had also **denied too much**: `gate-state-machines` says approval transitions are driven only by a human, correct for verdicts, but applied to the whole harness record it forbids the amendment lane the evidence says is the normal case. New technique `fixed-policy-amendable-plan` splits the governed record by **write authority rather than by content** - fixed tier (allowed paths, forbidden ops, validation commands, stop-and-ask condition) outside the executor's context and read-only to it; the route inside is the executor's to revise, each revision naming the fact that made the old plan wrong - and the split then IS a trigger predicate: inside the boundary amend with no gate, requiring the boundary to move stop and escalate, and the executor may **propose** the new policy but never adopt it. Three of the source's five measured weaknesses folded in rather than becoming separate candidates (over-broad forbidden patterns as the asymmetric-cost section - too broad and every task escalates, reaching gate fatigue by the route that looks most responsible; stale task state as the reaper paragraph; uninterpretable failed runs as "what this cannot do"). Adoption numbers cited in as an **existence proof, not a distribution**, stated in the document so a later reader cannot mistake n=1 for a rate. Row 2 declined as a genuine catch - the corpus's "When a gate is mandatory" (four triggers, explicit complement, "a correct gate map is mostly white space") says the proportionality claim better, and the one non-identical edge (corpus prices over-gating as human attention, source measured task latency) was too thin to carry a technique. **0 of 3 fetches - sixth consecutive zero-fetch run for the first-party class.** Shared checkout: a parallel session ran `build-index` mid-run and swept this run's in-flight technique into its own regeneration, so generated files were left uncommitted by this run |
| 2026-08-27 | `youtube:LeUUxLRdvho` - "Why The Best Software Engineers Focus On System Design" | first-party practitioner account (**interview format**, graded rather than segmented) | 8864 | 15 | 1 | 3 | 0 | [[2026-08-27-best-engineers-focus-on-system-design]] - **the bundle owns every mechanism of scale and no decision about scale.** `vertical scal` returns ZERO across 149 subjects; `horizontal scal` returns one, inside `limiter-topology`. Verified three ways before anything was written: every scale-adjacent subject begins AFTER the sizing decision (`rate-limiting` presupposes a chosen ceiling, `admission-queue` a fixed capacity, `runner-fleet` CI capacity), and the nearest-looking neighbour is not one - `metric-forecasting` states its own boundary as owning "whether the claim may be **displayed** at all", a dashboard discipline that governs drawing a growth curve honestly and says nothing about what to build because of one. Missing stage one under an excellent stage two. Subject spec dispatched (`scale-investment-timing` -> `backend-platform/resilience`, placement verified against `taxonomy.json`: the category is NESTED so a flat add is illegal, `resilience` holds 7 of 10) with **five fragments folded in** that each looked standalone at extraction, the two rejected placements argued, and an explicit instruction to override the brief. **The accepted technique came from a denial, not from the source**: `structure-is-not-delegable` partitions design decisions on whether the outcome is scoreable inside the run, owns the unscoreable half, and hands the scoreable half to `orchestration-to-tool-migration` - whose subject is the LLM tool surface, which decisions a *model* makes in a pipeline, not an engineer choosing between two implementations of one interface. **The class was defined twice in this bundle and owned by nobody.** New technique `scoreable-designs-are-built-not-argued` closes it constructively - what changed is the price of the experiment, not the principle ("measure don't guess" was honoured selectively because building every candidate cost days, so it was reserved for decisions worth days; agent authorship moved the cost an order of magnitude and the threshold moved with it) - carrying three things the source did not say: one harness over substitutable candidates rather than N benchmarks (three benchmarks produce numbers that cannot be ranked but look ranked), the workload choice as the undelegable residue one rung down, and **the cheapness as the hazard** - a number on a two-quarter-lag decision does not inform it, it wins it, which is the mirror image of the subject's existing "taste argument" failure and worse, because it does not look like an opinion. Sibling edited to point back; the partition now has both halves written. **Reusable form: an enumeration that delegates a case to another document is a claim ABOUT that document, and it is checkable in one read** - second time in three runs a "does not apply where..." sentence was the highest-value thing on the page. Class note: the interview format is **graded, not segmented** - the first ~20 min (his own systems) produced everything, the last ~10 (curiosity, learning, motorcycles) produced nothing; read the first third closely and skim. **0 of 3 fetches - fifth consecutive zero-fetch run for this class.** Shared checkout with a live parallel intake run throughout |
| 2026-08-27 | `youtube:zPfxlcVpFgs` - "How I manage 15 AI agents 24/7 as a solo founder" | first-party practitioner **dialogue** - two accounts compared (new sub-class) | 8347 | 12 | 1 | 0 | 0 | [[2026-08-27-managing-15-agents-solo-founder]] - operator picked one row, the doctrine one, so breadth is 11 untriaged and the run is one finding deep. **New sub-class: the dialogue.** Two practitioners with the same job comparing their own systems - where they chose opposite defaults the discriminator is already drawn, for free, no fetch (agent-threads vs public chat: context locality vs adoption legibility); where they converge unprompted, that is the other signal, and both independently said they deliberately ship *less*. Same property as a lab shipping two contradicting sibling instruction docs - **diff the practitioners, not just the transcript.** 0 of 3 fetches, as the class predicts. **My triage read was too strong and verification corrected it downward, which is what made the finding.** I expected "the corpus models machine throughput and missed that the human is the constraint"; `proposal-not-push` says it in its own words - *at machine pace the reviewer is the bottleneck.* The corpus knew. The gap is an **asymmetry, not an omission**: the machine bottleneck gets four measures, distributions, denominators and a whole demand-reduction section; the human one gets one sentence and no capacity model, and everything done about it is per-item (one concern, small, provenance, consistent shape) - **nothing said how many verdicts there are.** So the subject sizes its first server against machine-paced arrival and routes the entire output into a second server whose rate was never written down. Reusable form: **when a corpus states a constraint in prose in one technique and models it thoroughly in another, the finding is the asymmetry between them** - invisible from a slug map or a summary, reachable only by opening the file. New technique `human-gate-capacity`: the merge gate is a server with a fixed rate and it is the only one that cannot be bought, so demand reduction is not the preferred lever but the *only* one. Two things the source supplied that the corpus could not derive - **the overload signatures are opposite** (a machine queue past capacity stalls; a human gate *accelerates*, so rising throughput against an empty backlog is the failure), which let the technique separate a **stall** from a **rubber stamp** by four measures with opposite remedies; and the **independence limit at the subject's own stated floor** - at one person the reviewer is also the dispatcher, so the gate is a check, not an independent review. Enumeration hunt paid a fourth consecutive run: *"Two consequences follow, and the second one is the one that gets missed"* - the third consequence was the finding. Boundary written both ways: `review-queues` owns whether a single decision can be made well, `machine-paced-delivery` owns the rate at which decisions are demanded |
| 2026-08-27 | `web:allthingsdistributed.com` - "DuckDB and the changing physics of analytics" | first-party practitioner account (hybrid: vendor announcement + systems-design essay) | 3583 | 11 | 4 | 2 | 0 | [[2026-08-27-duckdb-changing-physics-of-analytics]] - operator dispatched it at a named weakness ("we default to postgres/pglite"), and the framing was correct, structurally verifiable and understated. **The source originated four findings and authorized none of them** - the class row held exactly, and every landing was authorized by peer-reviewed literature or by a connected project's own measured benchmark. The hole verified three ways: `data-layer`'s four subjects ALL begin after the engine exists (stage zero unowned); `embedded-db` opened by defining embedded as the transactional quadrant only, and all seven techniques are transactional disciplines - a denial that denied too much; and `analytics-store-design` enumerated four portability targets whose 2x2 has exactly one hole. `olap` returns ZERO corpus-wide, while `postgres`/`sqlite` returning zero is the purity floor working - **a concept returning zero is the finding, a product name returning zero is not**. New technique `analytical-reads-off-the-serving-store`: form factor and workload shape as INDEPENDENT axes, threshold is query shape not row count, and the uncounted cost is contention - an analytical read through a single-writer store buys the whole `single-writer-holder-discipline` surface for a workload that never needed exclusion, and **the tell is an operating instruction, not a benchmark** ("run against a COPY when the dev server holds the store", carried verbatim by two trees). `analytical-copy-partitioning` amended in the neighbour bundle: the copy does not have to be a warehouse, every structural rule survives the substitution because they are rules about the ROLE not the topology, so **the fork's trigger drops** - the technique had left only two answers below it. Subject spec dispatched with four fragments folded in. **Corrected my own triage mid-run**: the claim "both projects carry both engines" came from a `grep -r` that swept vendored `node_modules`; a tracked-files survey inverted it to "both evaluated it and neither adopted it", which is the interesting fact - one has a 392-line measured guide, 4 dated cases, cross-engine checksums, negative results, and a rule R3 naming a workload that **still runs on the incumbent**. A decision in a document and a decision in the import graph are different states. 1 of 3 fetches; the canonical primary 403'd twice and the author's own write-up supplied the mechanism |
| 2026-08-27 | `github:herdrdev/herdr` - "the runtime your coding agents live on" | first-party agent-runtime repository (new class) | 607 | 15 | 3 | 0 | 2 | [[2026-08-27-herdr]] - the ingest returned 607 words and they were the wrong 607; every accepted finding came from files the README does not mention. Class carries three first-party documents that check each other: production rules for agents building it (`AGENTS.md`, 290 lines), a skill for agents driving it, and the hooks/recipes that enforce both. **Amends the vendor-skill lesson**: this bundled skill is the counter-example - a *negative* description ("Do not use merely because a task could benefit from a background terminal, delegation, or parallel work") plus a runtime precondition that halts it when unmet. Read a bundled skill for whether invocation pressure produced an ad or a gate; do not assume the ad. All three picked candidates landed in `terminal-multiplexing`. **The golden path's "the ring never blinks" claim fails on the subject's own primary tenant** - a child on the alternate screen has no scrollback to lose rows into, so a byte-faithful ring replays the current grid, not a transcript, and no budget increase recovers it; `bounded-replay-buffers` knew the alternate screen only as a replay-fidelity problem. New technique `occupant-state-detection` merged two candidates once the home contest resolved: `fleet-orchestration/lifecycle-signals` owns the state machine and ranks raw output weakest, and this source's whole situation is the case where its tier one *does not exist* - so the screen-derived substrate lands here and supplies that door. Grounded in 21 per-occupant rule manifests, not the docs; sharpest item is a code comment preventing **the user's own typed text from impersonating a working signal**. 2 of 3 fetches, both missed - the citation was in the tree's vendored emulator bindings all along |
| 2026-08-27 | `youtube:jp0PGmAwK3w` - "This 600M Model Fixes What Whisper Leaves Behind" | second-hand practitioner review (vendor release demo, new class) | 1565 | 10 | 4 | 1 | 1 | [[2026-08-27-s1-mini-transcript-cleanup]] - shortest source ever mined (1.5k words) and a good run, because the operator asked for installation requirements and that forced the primary source. **For this class the fetch IS the extraction**: a demo is organised around a happy path and states no operating constraints, so the control-line grammar, greedy-only decoding, the output ceiling, the behavioral-toggle trap and "empty output is expected" were all in the model card and none in the video. New technique `transcript-normalization` closes voice-io's missing stage - the corpus owned capture->transcript, transcript->command and display-text->speech, but nothing owned transcript->**written text a person reads**, one half of a symmetry it had already named. Its best section is written AGAINST the source: normalize toward a reader, never toward a reasoner - the video's proudest example (clean the transcript before the coding agent) is the one placement where the rule inverts, because a 0.6B normalizer seeing one utterance edits input for a model holding the whole thread. `stt-pipeline` amended: a stage further down gets a different empty (three empties, opposite meanings; the default integration raises an anomaly every time somebody clears their throat). Consumer verdict **do not adopt**, written as a negative application - the tree has exactly two dictation destinations and the reader/reasoner rule disqualifies both, which nobody designed. 2 of 3 fetches |
| 2026-08-27 | `youtube batch:x3Go2UiHZWk+RwynWBXuRaA+H3yU28Mqu9E+oECv0lTU8mo` - 4 AI-editing tutorials, 3 voices | practitioner-tutorial batch (editing lane, operator-dispatched) | 7636 | 9 | 4 | 0 | 4 | [[2026-08-27-video-editing-batch]] - the dispatch batch: 7.6k words -> `cut-compiled-from-source` (NEW, "compiled" had zero prior art - the edit as a declarative composition; the critique pass READS the source instead of watching pixels), transcript-as-derivation-spine on derived-turn-markers (3 authors), plans-as-markers on edit-plan-over-regeneration (propose/approve/apply in the editor annotation channel), generate-to-see-rebuild-natively on anchored-variation-slate. Plus an executed headless-editor probe on the dev machine (boots -nogui, full API, one one-time GUI preference gates external connections) landed as the consumer plan appendix. Shortest source (699w, a builder narrating their own connector) was the strongest |
| 2026-08-27 | `youtube batch:DfdlAwJ4SKw+8tnO15fJWVE+8obne_qS6MY+Whh8vCqboxQ+olXpy9Hu7R8+W-DJnoL1Y18+OF9xcQCQSFc+hpR_4UXGpuo+aTnek5komeE+kv1o8eKOcas+yyZQXxkVP14` - 11 AI-video tutorials, ONE creator | practitioner-channel-corpus (new sub-class) | 22371 | 12 | 6 | 1 | 5 | [[2026-08-27-video-workflow-batch-3]] - the channel corpus inverts batch economics: one voice voids in-batch convergence, so yield ran on corpus-vs-source novelty and cross-run corroboration only. Six amendments, zero new techniques: paired-panel anchors (both rung-3 frames cut from ONE image), the blocking frame as the camera schematic (third member of annotate-not-describe), the built previz plate (motion-plate-library first corroboration + fourth source), board chaining/panel repair/pacing contract (storyboard technique third author), voice minted from either direction, sample-set coherence before capture. Exemplar-conditioned story generation banked. Closes with the consumer-app video-generation integration plan (operator dispatch). 0 fetches |
| 2026-08-27 | `youtube:rKo9iLGjUbs` - "I Built a FREE App That Runs Your Entire Business" | practitioner-build-walkthrough (personal tool, new class) | 5750 | 12 | 3 | 0 | 2 | [[2026-08-27-control-center-dashboard]] - new class, a hybrid whose halves have opposite reliability: the product tour is a demo (strip test deletes all of it), the sentences about the tool he has run daily for months are a first-party account, and every finding came from the second half. The discriminator is cheap - is he describing what the tool does, or what happened to him while using it. Three findings, **0 fetches** (budget 3), all corroborated corpus-internally. `dedup-records-who-carried-it` (grant-source-landscape's 6th normalization-contract clause) closes a seam INSIDE one bundle: `verification-passport`, `majority-rule-doc-consensus` and `funder-portal-resolution` all consume a source count that the bundle's own ingest layer had and threw away one merge earlier - and the source implements it wrongly (counts newsletters that relay each other), so the technique carries the half it lacks: carrier sets are recorded at PUBLISHER granularity or they measure promotion, not truth. `task-envelope` gained the enumeration case - a brief that lists the parts supplies a machine-checkable done criterion by accident, and the model satisfies THAT one: fires on time, on the wrong axis, every surface present and every link dead (probe the leaf, not the shape). `acquisition` gained the per-user-installed rung - the ladder assumes one registered client, so a tool run once per user reaches the ceiling by walking the floor (delegated grant whose precondition is a manual paste), plus a 5th provenance case and the rule that a setup guide must never resolve the provider's trust prompt for the reader. Synthesis banked not landed: candidates 1+2 are independence accounting inflating and deflating; only one half was picked |
| 2026-08-27 | `youtube batch:Hjj-yiDJXTw+LsfcOoCc88I+HMVSCEh72n4+0v534yAyhwg+R8QvtGlVI_E+-ZqilAZOFLE+tZReV23ncuc+lvzPURroPxE+z3uX2YLGudM+VmDpBD_47M0+pYsc01oioKg+Ie0niOVuWzg` - 12 AI-video tutorials, ~5 voices | practitioner-tutorial batch (2nd) | 25336 | 13 | 10 | 0 | 7 | [[2026-08-27-video-workflow-batch-2]] - count voices, not videos (6 of 12 are one creator; same-author repetition is one sighting). Back-to-back batches compound: four landings were widenings of run 24's day-old amendments from independent sources, and the typed-input shape hit its third run in a row and landed as **law `typed-input-owns-its-channel`** (camera path / beat grid / emotion dial). New: `reference-role-map` (every attachment one named role, negative scope, group multiplication) + `storyboard-grid-conditioning` (numbered textless panel grid as one conditioning image; "storyboard" had zero prior art); moving-reference amendment (continuity carry vs scoped choreography transfer); the-import-is-the-lever (grade the anchor, the look rides in); edit passes are lossy (3 practitioners, 2 runs). Mid-run: a parallel session landed `character-identity-continuity`; triage re-based on HEAD. 0 fetches |
| 2026-08-27 | `youtube batch:3rDs6FhFoUQ+6arKHnBNJMI+5dWgZDka3Ww+9Y1A2nw_Veg+83NI19L7fhQ+tYJQusOS2jI+d7J82el-6AU+zYPgz6sOy74` - 8 AI-video workflow tutorials | practitioner-tutorial batch (first batch ingest) | 26578 | 15 | 10 | 1 | 3 | [[2026-08-27-video-workflow-batch]] - first batch run: 8 sources deduped at extraction, and within-batch convergence became the triage signal (every 2-source candidate landed). The yield clustered on the bundle's missing stage - multi-clip sequence continuity - as 2 new techniques (`performance-direction` completes cinematic-language's stack with the performer; `motion-plate-library` restores the layout/animatic split as a sourcing channel) + 6 amendment sections (identity sheet single-face rule, persistent-state variant sheets, voice as second identity surface, default-with-scoped-override style blocks, schematic spatial anchors, briefed match cuts, output-derived extensions, anchor-texture provenance). 0 fetches; vendor-channel tutorials read as first-party accounts wearing an ad - the strip test deletes the sponsor |
| 2026-08-29 | `web:claude.com/blog` batch - "Claude on call: first responder for CI/CD failures" + "The AI-Native SDLC playbook" | batch: first-party practitioner account + vendor doctrine document | 14050 | 15 | 5 | 1 | 3 | [[2026-08-29-ai-native-sdlc-and-ci-on-call]] - one technique in the #2 worklist subject: `oracle-frozen-during-repair` (quality-gates) - the in-task half of the rule `proposal-not-push` already enforced at the merge gate; red-first ordering is the proof of defect, the freeze is mechanical, the release valve is a separate human task. Four amendments: eval golden-set trigger includes instruction files/skills/hooks; self-healing gets a second ladder (magnitude buys invocation, confidence buys aggression, compose by minimum - the source mapped 3σ straight to "propose"); failure-diagnosis rule "diagnose from the measured, not the configured"; gate-laddering places asking controls at stage boundaries. Law lead (2nd sighting): verification is the product when authorship is cheap. 0 fetches |
| 2026-08-29 | `youtube:T6HLeCiUyI0` "The Future of AI (From Harness to RSI)" - Discover AI | second-hand survey (one commentator digesting two papers) | 8162 | 10 | 1 | 2 | 5 | [[2026-08-29-future-of-ai-harness-to-rsi]] - one amendment: `failure-attribution` (eval-harness) gained the missing stage between attribution and re-attribution - the change is an experiment designed before it runs: one harness component per round, model pinned while the harness moves, prediction written first, parent kept. Generalizes the prompt-only rule game-production already carries (`mutation-taxonomy`) to every harness component. Leads: the two primary papers; optimizer architecture vs state as the recursion boundary. 0 fetches |
| 2026-08-26 | `youtube:NYFGCESmikA` - "DHH: Future of Programming, AI, Agentic Engineering, Vibe Coding & Linux" (Lex Fridman #501) | first-party-practitioner-account (podcast interview, new sub-class) | 54597 | 13 | 3 | 0 | 0 | [[2026-08-26-dhh-lex-fridman]] - longest source ever (3.7x prior record), same yield as sources a tenth its size; the sub-class's property: a five-hour conversation leaks the counterexample to its own stated advice (said "be as vague as you can", narrated a textbook tight envelope), and the contrast IS the discriminator. Three amendments, zero fetches: `task-envelope` gained its stated precondition (when done is not knowable the envelope inverts - finish-line constraints free, route constraints spend; discovery's deliverable is the next dispatch's done criterion), `heterogeneous-model-panels` gained the produce-review pair (cross-family sequential decorrelation for routine generation, which the panel rule excludes; corroborated against judgment-guardbands' correlated-judges argument), `fleet-orchestration` golden path gained the operator-medium section (chat couples the one human to fleet latency; work item in, batched decision surface out). 10 untriaged with anchors, incl. the single-owner doctrine's second independent sighting |
| 2026-08-26 | `web:githubnext.com/posts/knowledge-compressor` - "Knowledge Compressor" | first-party-practitioner-account (prototype report) | 4649 | 10 | 3 | 2 | 0 | [[2026-08-26-knowledge-compressor]] - the source's method landed as two techniques that both say the method AS PUBLISHED is unsound: it filters questions the model cannot answer from the article but never those it can answer WITHOUT it, and its require-a-failure guard is only safe after that screen. `unaided-baseline-screening` + `overshoot-and-restore` (eval-harness, first new techniques there since forge) + the recurring-bill/break-even amendment on `context-budgeting`, sharpened by 2026-08-25's cache multipliers. **Zero fetches** - all corroboration from corpus-internal convergence. Then built the corrected method and ran it: `scripts/compression-scan.mjs` + `docs/compression-lane.md` + `docs/compression-brief.md`. Result NEGATIVE and that is the finding - mean repetition 0.94% over ~3,350 docs, top entry document two-thirds irreducible under a real screen; the prototype's 50% came from a synthetic article written to be redundant. Lane default: run the scan, never the compression. Instrument caught its own overlap-counting defect on first read (74.3% -> 15.1%). Law lead banked: *a green result is evidence only where red was reachable*, 4 sightings/3 subjects, return on a sighting outside `software-engineering` |
| 2026-08-26 | `repo:jd-opensource/JoyAI-Echo` @ c796303 | research-model-release (new class) | n/a | 12 | 4 | 3 | 1 | [[2026-08-26-joyai-echo]] - new class: engine AND operating instructions in one tree, so every doc claim is checkable against the code that implements it, no fetch (budget 3, spent 0). The class's signature: a repo shipping two sibling systems hands you discriminators for free - two shot-writers from one lab gave opposite camera instructions, which landed as a boundary at almost no cost. Yield sat in the first-party prompt-engineering artifacts, not the README. `identity-split-from-state` + the `image-prompt-composition` anatomy going three blocks -> four (the restatement law's unstated precondition: a restated block may hold nothing that varies, and the constant only acquires moods when it stops being a style and starts being a subject); camera-as-control-channel discriminator into `movement-motivation`; pin-the-origin-roll-the-recent into `generated-shot-sourcing`, implemented twice at two altitudes in the source; joint-waveform spotting into `music-spotting-against-picture`, whose neighbour's keep/demote/strip enumeration it breaks |
| 2026-08-26 | `youtube:g3X8JauSWTM` - "Stop Building AI Slop - Build High-End Web Apps with AI" | practitioner-tutorial (course walkthrough) | 14649 | 12 | 3 | 0 | 9 | [[2026-08-26-stop-building-ai-slop]] - longest source in the ledger, three findings, none from the curriculum: cross-medium probes on `asset-vs-disposable-render`; `slots-claim-their-subjects` + the "two families" golden-path root (identity-slot convergence with 2026-08-23, landed not banked); structure-vs-restyle amendment on `style-onboarding-from-sample`; class row earned |
| 2026-08-26 | `repo:supermemoryai/supermemory` @ shallow clone | vendor-repository (new class) | 2091 | 16 | 3 | 1 | 4 | [[2026-08-26-supermemory]] - the engine is NOT in the repo (hosted); its ontology leaks through the visualization package's TypeScript types, which is the class's cheapest good artifact. `decay-and-forgetting` gained a third axis (the fact that expires by its own terms; the retrieval term *protects* time-boxed claims), `episodic-capture` gained the batch-shape half of the distiller ceiling at root altitude (3rd sighting, 2 runs). Realized + committed in a connected tree the same run; that tree then corrected the technique - overflow defers, it does not drop. Four catches, all cases where the corpus outclasses a SOTA vendor's own schema |
| 2026-08-26 | `operator dispatch` + `next@16.3.3` release notes / GHSA-p293-qw3h-jr36 + GHSA-2xp9-vwfh-vxw4 | operator-dispatch (new class) | n/a | 8 | 3 | 3 | 2 | [[2026-08-26-next-16-3-3-fleet]] - the dispatch was wrong about its own subject: asked for a performance topic, 16.3.3 is two critical unauthenticated RCEs and its AVIF fix is a REMOVAL. Literal ask unbuildable (denylist). Landed `## When the dependency is the framework` amendment, first `next`-stack application, `scripts/fleet-deps.mjs`, six project commits. The new instrument caught the run own pin-rewrite defect on first run |
| 2026-08-26 | `repo:rohitg00/ai-engineering-from-scratch` @ 39ea8a1 | curriculum-repo (511 lessons, 12-language i18n) | n/a | 7 | 1 | 4 | 3 | [[2026-08-26-ai-engineering-from-scratch]] - new category `localization/craft` + subject `translation-pipeline-topology` (6 techniques) forged same-run by three workers; "comprehension is proven by reconstruction" banked at two sightings; the discriminator (human quality claim) beats either topology |
| 2026-08-26 | `youtube:OzfgOjEfK98` - "Introducing Composer, a section-by-section song editor." | vendor-product-announcement | 76 | 6 | 4 | 1 | 1 | [[2026-08-26-composer-song-editor]] - thinnest source ever (exit 3, correctly); the operator's placement question became the session: `audio-generation` category (3 subjects, 14t), `partial-regeneration-seams`, the four-way audio discriminator, a music engine landed live in a consumer tree; the source itself yielded one catch (its headline is our `edit-do-not-regenerate` law) and one clock |
| 2026-08-25 | `youtube:wa6o-0C9UWE` - "Karpathy Listed What's Wrong With AI Coding" | practitioner-judgment relay | 3470 | 6 | 3 | 0 | 2 | [[2026-08-25-karpathy-coding-file]] - law 13 `silent-state-is-ungoverned` (five-way convergence closes), `restraint-amplifier-balance` technique, machine-checkable finish lines into task-envelope; altitude column added to the skill (0.10.0) on operator feedback |
| 2026-08-25 | `papers: 2607.09510 + 2605.29442 + 2607.21832 + 2606.08091` | commissioned-paper-batch | n/a | 4 | 4 | 3 | 0 | [[2026-08-25-agentic-dev-paper-batch]] - `worker-trajectory-anatomy` technique; measured paragraphs into enforcement-demotion and task-envelope; two independent corpora price fabricated success; PR study read and declined with reasons |
| 2026-08-25 | `repo:Shubhamsaboo/awesome-llm-apps` @ 11a4bc3 | app-aggregator (tutorial monorepo) | n/a | 8 | 3 | 3 | 3 | [[2026-08-25-awesome-llm-apps]] - the periphery beats the apps: skills eval ladder adopted (`scripts/check-skill-triggers.mjs`), cross-field epistemic coherence lands in structured-output; hash-chain audit declined on single-owner grounds |
| 2026-08-25 | `repo:DEEP-JLU/Awesome-Graph-Engineering` | paper-aggregator (~284 papers) | n/a | 7 | 3 | 3 | 2 | [[2026-08-25-awesome-graph-engineering]] - cluster triage, 3 papers read: `coordination-failure-triage` technique, two measured sections in `procedure-promotion`, store-shape paragraph; temporal-KG memory resolved as the corpus outreasoning the paper |
| 2026-08-25 | `repo:freestylefly/awesome-gpt-image-2` @ 6854698 | practitioner-codebase (curated prompt library) | n/a | 10 | 5 | 1 | 4 | [[2026-08-25-awesome-gpt-image-2]] - `verbatim-text-locking` + `screenshots-claim-a-record`, a third dialect in `prompt-dialect-matching`, one node application; the corpus meets the text-capable brief-reading model class |
| 2026-08-25 | `youtube:icM0ewXGvAw` - "19 Claude Code Mistakes \"Pro\" Users Are Still Making" | second-hand-practitioner-listicle | 4465 | 19 | 3 | 1 | 0 | [[2026-08-25-19-claude-code-mistakes]] - three techniques (`task-envelope`, `brief-carries-the-session`, `cache-continuity`), three rust applications, one correction; cross-repo self-check lines in the companion's worker prompts; 16 declined on operator scope |
| 2026-08-23 | `youtube:yCACmFTiCto` - "Turn Claude Into a One Person Marketing Team" | practitioner-tutorial (beginner walkthrough) | 9483 | 8 | 1 | 1 | 5 | [[2026-08-23-one-person-marketing-team]] - mined under `/deepen`; one technique on `evidence-bound-visuals` (synthetic-testimonial defect the source demonstrates), two corpus contradictions, aggregator topology banked |
| 2026-08-23 | `youtube:NUK_TBz46dM` - "Turn Claude Into A Web Design Genius in 3 Steps" | practitioner-deep-dive (technique demo) | 4037 | 6 | 1 | 0 | 5 | [[2026-08-23-seedance-web-design]] - mined under `/deepen`; one technique on `review-iteration-loops`, five catches, one corpus contradiction |
| 2026-08-22 | `repo:onecli/onecli` @ ff7a192 | practitioner-codebase | n/a | 10 | 7 | 0 | 3 | [[2026-08-22-onecli-repo]] - three new techniques, two amendments, two applications |
| 2026-08-22 | `web:linear.app/data` - "How teams build (Edition 01)" | vendor-telemetry-report | 4613 | 7 | 0 | 0 | 0 | [[2026-08-22-linear-how-teams-build]] - none picked; class characterised, 7 untriaged |
| 2026-08-22 | `web:pinglin.tw` - "The Shapes of Agent Memory" | first-party-empirical-study | 13767 | 12 | 6 | 1 | 2 | [[2026-08-22-shapes-of-agent-memory]] - five amendments across agent-memory + eval-harness |
| 2026-08-22 | `youtube:bxp4G-oJATM` - "Difficulty in Video Games" | designer-talk | 2332 | 6 | 5 | 0 | 0 | [[2026-08-22-game-difficulty]] - landed on `research/game-difficulty` + cross-repo |
| 2026-08-22 | `youtube:3MP8D-mdheA` - "How To De-Slop A Codebase Ruined By AI" | practitioner-deep-dive (technique demo) | 2456 | 6 | 0 (+1 dispatched) | 0 | 1 | [[2026-08-22-de-slop-a-codebase]] |
| 2026-08-22 | `youtube:gaDdrDdczO4` - "New Skills! v1.2..." | practitioner-deep-dive (skill-library release) | 2514 | 10 | 5 | 0 | 2 | [[2026-08-22-skills-v1-2-release]] |
| 2026-08-22 | `youtube:u8Im0l_vwqM` - "Inside DeepWiki: How Cognition Builds Wikis for Devin at Scale" | practitioner-deep-dive | 2974 | 10 | 2 (+1 proposed) | 0 | 1 | [[2026-08-22-inside-deepwiki]] |
| 2026-08-22 | `youtube:NC4h5kWH_-A` - "AI News: The AI Agent Race Just Exploded" | mixed-ai-news-roundup | 6958 | 10 | 4 | 1 | 2 | [[2026-08-22-ai-agent-race-exploded]] |
| 2026-08-21 | `youtube:EfGF7QbJItA` - "AI News: The Best Open Model Runs on Your Computer!" | mixed-ai-news-roundup | 6507 | 12 | 1 | 0 | 0 | [[2026-08-21-ai-news-open-model-local]] |
| 2026-08-28 | `youtube:RqcEK7sWesQ` - "You Think This Is Good OOP... It's Not" | **practitioner listicle on design canon** - six textbook class-design mistakes, toy examples, no vendor, no measurements | 3256 | 8 | 3 | 1 | 2 | [[2026-08-28-six-things-mistaken-for-good-oop]] - **the corpus carried every item at module altitude under its own vocabulary**, and the map reported three false holes ("liskov", "mixin", "duplication" - present as substitutability, the contract suite and the wrong-abstraction rule). What the source supplied was the class-hierarchy instance a deliberately mechanism-neutral subject never names, and the run wrote at the corpus's altitude, not the video's. Three amendments on `module-design`, folded from five items: `seams-and-adapters` gains the narrowing adapter (subtyping is a substitution promise; a substitute that narrows the guarantee broke it; fix the interface, not the adapter; inheritance-for-reuse is the accidental form) - the contract-suite section had only ever caught a drifting double; `module-depth` gains the counterpart of the options-bag failure (a variation is data until it changes a guarantee; ask whether the module would be wrong to choose a default) - the existing rule read alone invited a unit per combination; `locality-and-leverage` gains the type-erasure tell. Two catches where the corpus is ahead (god base class = the merged-module test; wrong abstraction = the co-change test). One lead with a return condition: a unit-kind selection rule (classes for invariants, functions for stateless work) that the subject has no place for until a second source converges. 0 of 3 fetches - canon corroborates by convergence. Unattended run; picks by registry impact; no declines; one untriaged aside |
| 2026-08-28 | `youtube:` batch of 12 - PaXuebdY75U, guq_bjGwzdw, SM5Un86ntNU, 8GF828QAyow, _VX6BZDKgrg, edrUbfeSPio, B0kVZvhkaq4, gJED9LCczc0, eDODZfOO7Fc, Cw11MnXArjY, pZRROwTYDqk, Hppz8XfdKHY | **practitioner-tutorial batch, fourth pass at one bundle** - 12 videos, **6 voices** (5 one creator, 3 a second), two Korean-language; operator constraint: real gaps or technique extensions only | 29421 | 24 | 7 | 1 | 12 | [[2026-08-28-media-generation-batch-4]] - **seven landings, all amendments, no new techniques and no new subjects**, which is the right shape for a batch arriving at a bundle runs 24-27 already widened 26 times. Author dedupe mattered most here: the two cross-author agreements (draft-resolution ladder, per-clip generated audio) were both catches, and what paid was the *disagreement* - two independent sources give opposite advice on in-prompt timestamps, so the discriminator is drawn for free (untriaged). Landings: `partial-regeneration-seams` gains the two operations time-based media has and a still does not (**excise an interval, reverse one**) with the defect's extent choosing among them and reversal's hard boundary (time-symmetric motion only - never gait, a mouth, flame, spilling), plus the unaddressable multi-panel sheet (a one-panel fix re-rolls every panel; composite back AND diff the panels you did not ask about); `generated-shot-sourcing` gains rung 3 over an *exact graphic* (a designed graphic is re-synthesised, not drifted - author the anchors as a cumulative single-delta chain; three or more states fall off head-and-tail onto ordered references) and a third cost of a frame anchor (it over-pins **cyclic** motion - pin one member of a symmetric pair mid-stroke, not both); `style-onboarding-from-sample` gains the house-style decomposition (stages fail independently and each is learnable only from evidence of a different kind - **where no channel can read the evidence the stage is hand-authored, and a pre-supplied asset-class decomposition is what makes that tractable**); `reference-shows-only-invariants` gains the ground's *brightness* as an axis hiding inside "plain" (white sweep = an exposure instruction nobody wrote; the tell is a character reliably brighter than the room); `medium-vocabulary-locking` gains the named-work quality proxy (**a proper noun is a content token wearing a quality adjective's clothes** - the render quality genuinely improves, which is what makes it durable; strongest where the subject class overlaps the work's signature subject). 12 catches, one of which **contradicts the source and the corpus wins** (one soundtrack across the edit is what `music-spotting-against-picture` calls wallpapering). 4 untriaged with anchors - the strongest is depth staging as a decision before optics, where "atmospheric perspective" returns zero prior art corpus-wide and `movement-motivation`'s speed-adverb rule may be correctable. 1 lead (currency scoped to a machine's own capability envelope). **Method lesson: an enumeration near the top of a long technique file is not the file's coverage** - a candidate was triaged as a real gap off a four-rung ladder that the same file resolves 200 lines later. Zero of 3 fetches - eleventh consecutive corpus-internal run. Demonstrated-*failure* density predicted yield better than demonstrated-mechanics density: the sources that showed something going wrong produced 6 of 7 landings |
| 2026-08-30 | `youtube:gvPMjz08DDA` - "AI Agent Builds My Game in a Loop Until It's Done" | **sponsored second-hand practitioner review with a build-walkthrough half** - one creator, five games built on a vendor's loop mode, on the vendor's credits | 5045 | 13 | 1 | 2 | 4 | [[2026-08-30-tesana-loop-mode-game-builds]] - the tour half strips to nothing; the operating half's one claim (four-to-five-hour loops came back with logic sound and presentation flat, and the six-to-ten follow-up prompts all went to what no gate could see) landed as `verifier-coverage-review-agenda` on `unattended-build-loop` - a missing run-end stage between the loop and the reviewer: certify each item only up to the rung a verdict reached, hand the rest to the human as a named agenda, print per-gate verdict counts because the static preflight excludes runtime-determined gates on purpose. **Corroborated by a connected tree's recorded runs, not by a fetch**: the perceptual gate was configured, advisory, and returned zero verdicts in 77/77 deciding iterations while 234 features were marked done - the tree made the finding larger than the source. Applied as an `experiment` (A/B over four recorded runs, n=323 features), verdict `better`. 4 catches, 5 untriaged with anchors, 2 leads. 0 of 3 fetches. Unattended run; only the `real gap` row advanced |
| 2026-08-31 | `github:Kevin-Liu-01/Claude-of-Tanks` @ `286bd2a` | **first-party practitioner repository (build-walkthrough form)** - the sub-class tell is a contract document whose sections are numbered AND dated with the owner directive plus the incident that caused each: the release-walkthrough property (motivation preserved) in repository form. Postmortem outranks the lessons file for correction-class findings. | 26,981 landing / ~60,000+ in-tree | 14 | 5 | 5 | 1 | [[2026-08-31-claude-of-tanks-geometry-gate]] - one NEW SUBJECT, `reference-parity-gating` on `game-production/content-pipeline`, with 6 techniques and 3 applications. Found by the enumeration hunt: `generated-mesh-acceptance` declares in prose that the perceptual/'is it the right shape' half is *'a different subject'* and nothing owned it; the bundle had a quality gate for motion and none for geometry against a reference. Techniques: `dual-anchor-scoring` (two authorities measuring different quantities whose conjunction a wrong artifact cannot satisfy - 'you cannot satisfy both without being actually right'), `register-once-from-the-invariant`, `no-average-hides-a-failure` (min/p95/symmetric coverage + the floored-metric rule), `findings-carry-the-correction` (the row payload is the product; no iteration cap because the gate defines done), `instrument-blindness-register` (enumerate what the rig structurally cannot see, one witness per class - four classes, each shipped a defect at a passing score), `defect-cap-bounded-to-its-reach`. Placement overrode Phase 4's HOME IF NEW: `asset-production` sits at exactly the 10-directory cap, so `content-pipeline` by the taxonomy authority - and better on the merits, since these are parity-measurement techniques that transplant across asset types. Applied same-run to a managed tree as an experiment: **not-better**, and it fired the technique's own decision rule - arm B read the same representation with a different parser, so both its catches were parser artifacts; the technique gained `a different parser is not a different authority`. 1 catch (candidate-hash freezing, fully owned by `quality-verdict-integrity`), 1 corroboration-not-discovery (envelope parity 12/12 within 4% of published dims, 0/12 on the perceptual review - cited into L9 rather than landed as a claim), 5 leads (render-bucket-is-not-a-structural-selector; small commits do not contain a shared global assumption; coupled-change proof by input interception; index-blob staging of a tool-written shared ledger; lock/ticket design scars), 3 untriaged with anchors. 0 of 3 fetches spent. |
| 2026-08-31 | `github:tc39/proposals` @ `600a427` | **reference index — but the lane was wrong for it.** 1,603 unique outbound links over ~10,900 in-tree words (ratio says index), yet the ~200 references are language-feature proposals that strip to `nothing` against all eight bundles: a full wave campaign spends ~40 fetches for ~200 honest negatives. **The rows are maintained RECORDS, not pointers** — stage, author, champion, a per-stage evidence column, a dated notes trail, a rationale for the dead — so the table's SCHEMA is the source and the bibliography is decoration. Second reference-index run after `voltagent-awesome-ai-agent-papers`, and the two disagree about where the yield sits, which is the class's real discriminator: **does the curator maintain per-row state over time, or only select rows?** | 1540 landing / ~10900 in-tree | 8 | 2 | 2 | 2 | [[2026-08-31-tc39-proposals]] — 1 of 3 fetches, spent on the process document as EXTRACTION not corroboration, and it inverted the source twice: the tracker's column set turns out to track the entrance criteria exactly, and **"no explicit rule addresses stalled proposals or automatic removal"** is what turned an observation into a technique. Both landings came from one asymmetry — all 14 prior techniques in `quality-gates` take the CHECKER as subject and none takes the item being gated. Measured from the source's own record: 7 of 46 terminated items name owner departure as the sole cause (3 closed the same day — an unscheduled decade-late sweep); 30 of 92 in-flight items silent ≥2 years, all listed active; and the natural experiment that carried the first technique — one board, two obligations, two conventions, 8 of 18 holes legible under explicit markers vs 16 of 29 (55%) unreadable under blanks. |

## Source classes, and what each is trusted for

Written from what runs actually observe, never assumed up front. A class earns a line
here after it has been seen twice, with the incident that taught it.

| Class | Reliable for | Not reliable for |
| --- | --- | --- |
| Designer talk (domain craft, no tooling) | The STRUCTURE of a domain's received wisdom - the decomposition, the named hazards, the shapes practitioners converge on. | Whether any particular studio's version is best, and anything about software. Its findings never route to `software-engineering`; they route to a domain bundle, which may not be on the branch you are standing on. |
| First-party practitioner deep-dive | What they built, what they measured, and what changed when they changed it. Facts about one system, from the person who changed it - no corroboration lane improves on that. | What works in GENERAL. The sample is one pipeline at one company, so a measured result is an existence proof, not a distribution. |
| Operator dispatch | The LOCAL facts: what the operator has observed in the fleet, what the fleet needs, which trees to open. Version numbers, adoption state, felt pain - all checkable against the checkouts, and cheap to check. | The GLOBAL frame. A dispatch is not written against the corpus and cannot know what is already forged, what the gate forbids, or why the subject it names is the wrong altitude. Verify its premises against the trees and its framing against the corpus; the second one is where it fails. |
| Mixed AI-news roundup | That the world moved: a release happened, a price changed, a benchmark was published. | Why it matters, whether it is true, whether it is new. Its claims are second-hand by construction. |

**First-party practitioner account carrying a vendor announcement, first observation
(2026-08-27) - and the run where the corroboration was entirely local.** A senior
engineer at a vendor arguing a systems-design position in the same post that announces
acquiring the team behind the system it argues for. The provenance is not disqualifying
and it is not authorizing: the post's job was to locate a hole, and it located a real one
three levels deep. Everything that landed was authorized by peer-reviewed literature or
by a connected project's own measured benchmark. Two instrument lessons that generalise
past this class:

- **The fetch budget was nearly irrelevant, and reaching for the web was the wrong
  instinct.** 1 of 3 spent; the canonical primary 403'd twice at its own host before the
  author's independent write-up supplied the mechanism. Meanwhile a connected project
  held a 392-line measured decision guide over the exact question - four dated cases,
  five workload classes, cross-engine correctness checksums, and negative results. When
  a first-party account names a decision an operator says the fleet keeps getting wrong,
  **check the fleet before the web**: the claim usually has a home, and the home has
  often already measured it.
- **Survey connected projects with `git grep`, never `grep -r`.** An untracked sweep
  counted vendored `node_modules` and manufactured the claim "both projects adopted this
  engine". The tracked-files answer was the opposite - both evaluated it, neither adopted
  it - and the opposite was the finding. It is also ~100x faster; the untracked sweep
  timed out at 120s on one tree.

The durable finding is one the post could not have supplied: **a decision recorded in a
document and a decision present in the import graph are different states, and only the
second one runs.** The tree that measured a 43x win, wrote it as a numbered rule, and
named the workload is still running that workload on the incumbent - and no gate can see
it, because a document recommending an engine and a script not using it are each
individually valid.

**First-party agent-runtime repository, first observation (2026-08-27).** An
open-source product whose *subject matter* is the same machinery this registry
indexes, so its repository is simultaneously engine, operating instructions, and
enforcement. Three files check each other: the production rules for agents building it,
the skill for agents driving it, and the hooks and task recipes that make both real -
and the third is where a rule's actual conditions live, because every rule worth having
had something behind it. Two things this class taught that generalise past it. **The
bundled skill is not automatically an ad**: this one's description is negative and its
first instruction is a precondition that halts it, which retires the 2026-08-26
prediction in favour of reading for what invocation pressure produced. And **when the
source implements the primitive, its own code is the primary source**: two web fetches
were spent hunting a terminal-behaviour citation that was sitting in the tree's vendored
emulator bindings, stated outright. Not yet a rule; one observation. The third sighting
of a related pattern - **the README is the least useful file present**, now seen in this
class, the vendor repository and the research-model release - is no longer per-class and
belongs in the method's class list once.

**Designer talk, first observation (2026-08-22).** A teacher explaining an established
body of practice - neither second-hand survey nor first-party account. The class has one
defining property for this registry: **it has a placement problem before it has a content
problem.** Its natural home was a bundle living on a different branch, which the mapping
instrument cannot see, so the instrument reported "no prior art" for a topic a sibling
branch covers in two subjects. The corpus is bigger than any one branch and the tooling
does not know it.

**First-party practitioner deep-dive, second observation (2026-08-22) - the row is
earned, and one sub-class is worth seeking out.** Both observations confirmed the split:
authoritative about what they built, silent on generality. The sub-class that outperformed
is the **release walkthrough** - a library author going through one version's changes.
It is organised around CHANGES, and a change carries its own motivation, so the author
states the failure mode out loud because it is the reason the release exists. Three of
five accepted findings came from that structure rather than from the feature described.
A feature demo shows the solution and hides the problem; a release walkthrough shows both.

Also confirmed across both: no corroboration fetch was needed in either run. The 3-fetch
budget binds on second-hand surveys and never on this class.

**First-party practitioner deep-dive, third observation (2026-08-23) - the failure mode
finally showed itself, and the yield profile against a MATURE bundle is different.** The
first two observations inferred the class's weakness from its structure; this one caught
it in the act. A device rule ("if it is a mobile user, serve the still instead of the
video") was stated with total confidence and is the named wrong answer in a subject the
corpus already carries, which requires the page's own measured frame cost rather than a
declaration the device makes about itself. That is precisely the shape the row predicts:
a general recommendation inferred from one system where it happened to work.

The yield profile is the other half. Five of six candidates were already covered - and in
four of those the corpus was not tied but strictly better hedged, holding the same rule
*with its condition attached* (a rights rule, a four-rung ladder, a measurement instead of
a guess). Against a mature bundle this class should be expected to produce catches, not
content, and **a catch is worth recording with the hedge that beat it**, because the hedge
is the reusable argument for the layer contract. Fetch budget was touched for the first
time in this class - not to corroborate what the practitioner did, which still needs no
lane, but to check a claim they made about generation in general.

**Cross-class, second sighting: the candidate a source explains badly is the one worth
the budget.** The 2026-08-21 roundup named a real gap and inverted the rule for filling
it; this practitioner named a real gap (no divergence phase before the review loop) and
inverted its mechanism, calling for more amplitude where the literature says amplitude is
the lever measured not to work. Both runs' single accepted finding came from their single
badly-explained candidate, while every correctly-stated claim was already owned. The
provisional reading, one sighting short of a rule: **a claim a source gets right is a
claim the corpus probably already has**, so the corroboration budget belongs on the
claims that sound wrong rather than on the ones that sound right.

**First-party practitioner deep-dive, first observation (2026-08-22).** Its authority
maps onto the layer contract almost exactly: strong evidence for the SHAPE of a
technique, weak evidence for its universality. So its claims land well as decision rules
with their conditions attached and badly as unqualified assertions - which is a
different editing job from the roundups, not merely a higher trust level. Yield differed
in kind as well as degree: a coherent account of one problem maps onto one region of the
corpus, so the run produced two techniques and a subject proposal rather than findings
scattered across six subjects. At 2,974 words it outproduced a 6,958-word roundup, which
is the first evidence that transcript length is not a yield proxy.

**Mixed AI-news roundup, second observation (2026-08-22) - the class row is now
earned.** Two runs, same channel, and the pattern held in both directions. The class
located four real gaps and was *wrong* about two things the corpus has measured (the
precision-tier rule in run 1, the throughput-buys-thinking claim in run 2), plus a third
it got backwards on agent identity. Yield is not a property of the class but of where
its segments happen to land: run 1 hit model and media subjects and produced one
finding, run 2 hit `llm-agent` and produced four.

The second observation adds something the first could not show: **the class becomes
authoritative in aggregate.** Two independent vendors across two runs shipped the same
"observed repetition becomes a named skill" feature, which met the upper-layer
corroboration bar with no web fetch at all. That only worked because run 1 wrote down
its untriaged candidates - a vocabulary with only "declined" in it would have lost the
first half of the pair.

**Mixed AI-news roundup, first observation (2026-08-21).** The class did exactly what the
row predicts. The one segment that reached a landed finding named a real hole in the
corpus and stated the rule backwards - the primary literature says the compression
*format* dominates the nominal bit count, while the source said take the highest number
that fits. Originating a finding and authorizing one are different acts, and this class
can only do the first. Not yet a rule; one observation.
| 2026-08-30 | `github:laude-institute/headlong` - "Headlong - an open source agent microharness" | **research-model release (harness repo)** - engine + design docs + code in one tree; README is the ad, `design/*.md` are first-party design docs with paid-for corrections recorded as revisions | 1882 | 8 | 2 | 2 | 1 | [[2026-08-30-headlong-agent-microharness]] - two techniques, both corroborated in-tree (0 of 3 fetches): `tiered-history-projection` on `prompt-assembly` (context as a regenerable projection of an append-only record; geometric staircase reaching birth; tiers as an id-index, not testimony; sealed provenance-stamped blocks) with the regime boundary stated on both sides of `history-compaction` - the corpus's "one compaction away from gone" holds only where the message list IS the record; and `engagement-paced-cadence` on `cost-metering` (the stage upstream of every gate: how often a self-driven loop wakes; reactivity never throttled, spontaneity descends exponentially with dwell to a settled cap; a ruminating loop must not score its rumination as work). Both applied same-run to a managed tree as simulations: cadence `better` (the tree already emits the wake-classification predicate and paces on nothing), projection `not-better` (histories fit the tail, no fetch-by-id affordance; the technique gained its adoption gate from the seam). 1 catch (one-mind-many-mouths), 3 untriaged with anchors, 2 leads (fork-test-merge self-improvement; brokered sandbox policy authority). Unattended run; only `real gap` rows advanced |
| 2026-08-31 | `github:tigerbeetle/tigerbeetle` @ `9d2f5d6` | **vendor repository (open engine)** - engine + operating docs in one tree, so every claim is checkable in-run at zero fetch; landing page 421 words against 111,264 in-tree (264x, the sharpest read-fraction tell the ledger holds) | 111264 | 15 | 1 | 0 | 2 | [[2026-08-31-tigerbeetle]] - one technique, `self-paced-intake` on `admission-queue`, landed on **cross-run convergence** with the prior day's Headlong run (0 of 3 fetches): a loop stepping on external events has delegated its work rate to its environment. Home chosen on a missing stage - the subject opens at "arrivals come faster than we execute" and never owned whether an arrival causes a step at all. Applied same-run to `personas` as a simulation, `better`, over three historical repairs; the structural fact was four long-lived consumers, three policies, no rule. 3 catches, 11 untriaged with anchors, 2 leads (a third sighting would make the convergence a law; ARCHITECTURE.md contradicts itself on replication topology and the code settles it). **The run's real output was a corpus finding**: the first triage produced twelve process rows and zero architecture rows from a database, the operator rejected that, and the audit that followed found a **construction frontier** - this bundle builds at the application layer and consumes everything below it. Fifteen builder-position systems concepts map to unrelated subjects by slug collision, four return zero corpus-wide, and the consumer-position control resolves cleanly. Written up in [[../domains/software-engineering]]; a second sighting (2026-08-27, `storage-engine-selection`) proves it is the default rather than an oversight |
| 2026-08-31 | `github:WingedGuardian/GENesis-AGI` - "GENesis-AGI" | **vendor repository (first-party practitioner account in repo form)** - 20,905 LOC memory subsystem shipped with its own architecture docs and ADRs; README is the ad | 5767 README / ~8200 mined | 13 | 3 | 0 | 2 | [[2026-08-31-genesis-agi]] - operator-directed at memory, which was the #1 attention point (56 pts) at scan time. Two techniques on `agent-memory`, both **below the pipeline** where eleven techniques of judgment could not see them: `lane-reconciliation` (a store is a record plus derived lanes with no shared transaction, so a half-completed fan-out leaves an item that passes every quality gate and cannot be recalled - and `coverage-instrumentation` reports it covered, because coverage joins the record) and `probe-without-write-back` (the read path writes, so a scheduled eval obeying `gate-sees-target` pumps the very loop `memory-value-model` bounds, over a fixed set whose expected answers are the items it inflates). Plus a per-class amendment to `health-checks/three-state-outcomes`. 0 of 3 fetches - third consecutive practitioner-codebase run to spend none. Applied same-run to a second managed tree, 3 rows: two `better`, one `not-better` that is independent corroboration rather than a rejection. **Both `better` verdicts amended the corpus from the tree, not the source** - the severity discriminator (does the lane have a reader fallback under it) and the structural-default section (`absent-guard-is-loud`: a suppression flag protects remembered callers, a split write endpoint protects every future one). 1 catch, 6 untriaged with anchors, 2 leads. Ship 0: no project confirmed by the operator. Unattended; only `real gap` rows advanced |
| 2026-08-31 | `github:tt-a1i/archify` @ `5de7275` | **practitioner build-walkthrough (repo form)** - an agent skill for generated diagrams whose operating layer is thicker than its product: 49 dated research rounds, a benchmark with a written fair-run protocol and three dated result sets, and a pre-registered kill experiment with its decision record intact. Landing page 2,051 words against ~130,000 in-tree (63x); a README-only read would have been a 1.6% sample of the ad | 130000 | 16 | 12 | 2 | 2 | [[2026-08-31-archify]] - **a new subject, forged in-session with no dispatched worker**: `machine-authored-documentation` on `codebase-stewardship` (golden path + 7 techniques + 1 application, 0 of 3 fetches). The operator asked whether the corpus covered automated docs creation; it did not, and the reason is the finding: three subjects sit adjacent and each one's own boundary statement excludes it. **Determinism is the discriminator** - `codegen` owns derived source and stands entirely on `derivation-names-recomputation`, which a model author cannot satisfy, because a regeneration diff over a non-deterministic generator is red on every run for reasons unrelated to correctness. So the acceptance burden moves off the derivation and onto the artifact: `three-gate-acceptance` (semantic AND deterministic AND rendered review; a skipped review never combines into a pass), `packaged-surface-evaluation` (measure the extract users install, harness outside the model-visible tree, attempt 1 frozen), `declared-truth-boundary` (the artifact publishes what its derived views do NOT claim; the receipt names counts, never impact), `evidence-without-verdict` (the instrument that gathers evidence for a human judgment reports `pending`, never `passed`), `stale-artifact-on-failed-write` (the atomic write is correct and it is what makes the failure silent), `rescored-baseline-uplift`, `preregistered-kill-criterion`. **The tree's best measurement reverses its own shipped conclusion**: a benchmark matrix published at 10/15 first-pass usable scores 8/15 under the corrected verifier, the post-fix matrix also 8/15, and a third run 8/15 again - the uplift never existed, and only frozen candidates made that knowable. 2 amendments on different axes than their targets (`checked-vs-skipped-denominators` gains the declared strictness tier, where the denominators are all correct and the examination simply asked less; `schema-validation-and-repair` gains the ordinal stop rule for a compositional validator, where the corpus's fixed budget is right for a binary door and wrong for a plateau). 2 catches, 2 untriaged with anchors, 2 leads. Applied same-run to `politicas` as an **experiment**, `better`: a visual detector returns `null` from five places, four of which mean not-measured - and the structural fact outranks the A/B, because the finding record has no skipped type at all, so the conflation is forced by the record shape rather than chosen at the call site. **Convergence with the same afternoon's `genesis-agi` run** on one root - an instrument must spell could-not-run differently from ran-clean - recorded as confirmation of `failure-not-empty-success` across three domains rather than proposed upward |
| 2026-08-31 | `github:herdrdev/herdr` @ `4dd9aa5` (re-run, Rust/backend lens) | first-party agent-runtime repository, **second sweep of an already-mined tree** | 3,304 (AGENTS.md) vs 420 (README) | 9 | 3 | 0 | 0 | 2 | [[2026-08-31-herdr-rust]] - the 2026-08-27 run mined this tree for terminal I/O; the operator's Rust/backend lens re-swept the same commit and it yielded 3 fresh landings from files the first run had left in its untriaged table. **A re-run under an orthogonal lens is not a duplicate** - the first sweep read `src/detect/manifests/`, this one read the justfile, the architecture test and the vendor ledger, and they did not overlap once. Landed: `operation-assertion-gates` (quality-gates) from a 200-line architecture test whose 4 of 7 tests test the scanner rather than the codebase; a third input class in `blocking-by-input-determinism` (deterministic subject, nondeterministic apparatus - the two-class axis mis-grades every timing gate); a `gate-laddering` section on source the compiler removed; and `vendored-fork-ledger` (supply-chain), where the subject's dependency model had exactly two states and **forking is a third that ends every guard rather than failing one**. 1 of 3 fetches, and it hit - the language reference's exact wording (a false predicate means the form 'is removed from the source code') is what let the cfg finding be written as analysis coverage rather than test coverage. Phase 7.5 returned `not-better` on a real 4-arm A/B: the consumer already refuses an empty scope in every checker tested, one of them stating the rule almost verbatim - independent convergence, and better evidence than an adopting tree. Index NOT regenerated: 5 siblings held uncommitted content, so a regenerated index would have referenced files absent from HEAD |
| 2026-08-31 | `github:diegosouzapw/OmniRoute` @ `b7a0c54` | **vendor repository (OSS LLM gateway)** - 13,026 files, ~50 CI gates, 4,207 unit tests; README is the ad (11,691 words) and the yield is in `docs/architecture/`, the middleware dir and a 23-item Hard Rules contract, where failure modes are recorded as issue-numbered revisions | 11691 README / ~34000 mined | 18 | 4 | 0 | 2 | [[2026-08-31-omniroute]] - operator-directed at LLM wrapping/middleware. Three techniques and one correction, 0 of 3 fetches (fourth consecutive practitioner-codebase run to spend none). **Two of the four contradict the corpus, which is the best case**: `resource-denominated-bounds` on `admission-queue` refutes `depth-bounds-and-shed`'s "size the count against the maximal entry" - that reasoning is exactly what produced the source's default cap of 1 and its 503s under ordinary load; and the `priority-and-fairness` amendment refutes the unstated assumption that origins are attested, after the source shipped per-key capacity and removed it (#10110) when unauthenticated callers minted identities to multiply the process-wide bound. Plus `speculative-work-admission` (redundant work is probed and skipped, never queued; release-on-admit; no wait knob) and `quality-axis-separation` on `model-routing` (a 200 measures transport, not answers - the semantic axis is null until an evaluator writes it and never shares a field with the operational one). 3 catches, all the corpus being confirmed independently; 11 untriaged with anchors; 2 leads. **Apply 4 rows, ship 2 commits** - `speculative-work-admission` A/B'd on the real prefetch queue in `goat` across three network tiers and shipped `code`/`better` (3g: 52 dispatches/18 useful -> 30/30; no-op at 4g). The run's best measurement was the arm that failed: skipping speculation indiscriminately served 0/6 high-intent prefetches, which promoted a caveat in the new technique to a boundary |
| 2026-08-31 | `github:calesthio/OpenMontage` @ `cd9f3c1` | **vendor repository (open engine, agent-as-control-plane)** - no runtime orchestrator; every creative decision, stage transition and review policy lives in Markdown skills and YAML manifests, so the prose files are executable and the strongest findings came from tests over Markdown | 943274 | 12 | 3 | 3 | 0 | [[2026-08-31-openmontage]] - three techniques, 1 of 3 fetches spent. Landing page 5,357 words against 943,274 in-tree (**176x**); the first clone silently dropped 2,159 files including the whole `.agents/skills/` tree under a long scratch path and reported success - `git status --short` after a fresh clone is the tell. `delivery-promise-lock` on `production-pipeline-phasing` (classify and lock the deliverable's medium category before the means; enumerate the metric's numerator adversarially, because the cheapest way to satisfy a quality ratio is to reclassify cheap output into it; halt rather than substitute) - home found by the enumeration hunt, the golden path covering only the *probe* that drops a dimension and not the *delivery* that does. `critique-carries-its-fix` on `review-iteration-loops` - the one fetch **corrected the source**: the cited paper is real (arXiv 2604.21718) but its axes are precision/recall/constructiveness, not the source's renamed trio, and the CMU/Harvard attribution is invented; written against the paper, and carrying an explicit cross-bundle discriminator against game-production's `severity-by-consequence` (does the consumer decide, or act?). `capability-coverage-contract` on `agent-instruction-files` - found as an asymmetry: ten techniques covered the direction with an error attached (named implies exists) and none covered the silent one (exists implies named); the source held **both forms of the test in one tree**, which is what made 'enumerate from the runtime, never from a list' evidence. All three applied to `gravity` same-run: 2 experiments + 1 simulation, all `better`, and two of them found the tree had reached the rule independently. 9 untriaged with anchors, 3 leads |
| 2026-08-31 | `github:cline/cline` @ `48d6385` | **vendor repository (open tree)** - no hosted engine to hide: agent loop, provider layer, hub daemon and eval harness all in the tree, so the class's "stated production rules" third is a 6,243-word architecture doc written as design rules with paid-for corrections recorded inline | 1178 landing / 124315 in-tree | 14 | 2 | 6 | 3 | 3 untriaged | [[2026-08-31-cline]] - two techniques, 0 of 3 fetches (tenth consecutive zero-fetch run for a source carrying its own primary material), read fraction ~105x and **zero accepted candidates from the README**. `absent-status-passthrough` on `fleet-orchestration` is a **missing producer**: the subject models the session reporting itself and the sweeper inferring from silence - both observed something - and never the layers between them that observe nothing and are asked for a value anyway (a transport projector, a probe whose identity provider is down, a creation-time seed). Landed as ONE technique from two candidates that shared a root, per the standing critique that the synthesis must come from the skill. **The sweeper is structurally blind to it** - the mislabelled session is alive and recently heard from, so every staleness budget passes; a fleet with both tiers correct still deadlocks. `lifecycle-signals` cites three laws and not `unknown-is-not-a-value`, the one that governs this exactly. `reliability-aggregation` on `eval-harness` came from the **enumeration hunt, eleventh consecutive pay**: the golden path lists the aggregation rules for repeated trials (mean, median, worst-of-N, pass-rate) and the list omits **all-of-N**, the rule a shipping decision rests on; the source's instrument supplies both estimators over one trial set with its own worked example (2 of 3 trials -> any-of-3 ~96%, all-of-3 ~30%). The subject held both halves - `count-carries-predicate` (label the rule) and `metric-role-contract` (name the decision) - and had never joined them. Both applied same-run to `personas`: the eval one as an **experiment, `better`, arms paired** - same trials, **the leaderboard winner flips** - plus an unsought second finding (an unscored metric averages to 0 and sorts as poor performance); the fleet one as a **simulation, `better`**, whose structural fact is the good kind - the auto-kill gate already fails closed on absence, but only because a map lookup returns an optional, so the discipline exists exactly where the type system imposed it and nowhere it had to be chosen. 3 catches (identifier reuse; failure classification, where the corpus is ahead twice over and the source's own regex-over-a-whole-log classifier is the anti-pattern `error-classification-for-retry` warns about; completion-vs-teardown, already drawn at higher altitude as lost-vs-exited), 6 leads, 3 untriaged with anchors, 0 declines. **Phase 4's "one moment" warning fired for real again**: `fleet-orchestration` was clear at Phase 4 and held by a sibling by Phase 7 - technique file uncontended, golden-path list edited under the `content` lock with a re-read inside it. 8 live siblings at claim time; gate was red on arrival from two siblings' untracked in-flight directories and was left alone |
| 2026-08-31 | `github:Aider-AI/aider` @ `5dc9490` | **vendor repository (open engine)** - mature practitioner codebase; landing page 1,156 words against ~9,500 LOC of core engineering mined and ~428k words of in-tree markdown | 1156 README / ~9500 LOC | 10 | 2 | 2 | 2 | [[2026-08-31-aider]] - operator-directed at architecture, code design and performance; routed away from `fleet-orchestration`, `eval-harness`, `retry-backoff`, `prompt-assembly`, `model-routing`, `agent-chaining` and `cost-metering`, all held by the 8 live siblings, which left the repo-map and edit-apply engineering nobody was near. Two landings on `retrieval`, both from **hunting an enumeration and an asymmetry rather than a gap** (0 of 3 fetches - fourth consecutive practitioner-codebase run to spend none). `structural-centrality-lane`: the golden path enumerates its lanes and denies a symmetry ("every other lane is a function of the query", proximity being "a function of results"), and the missing kind is a function of neither - the corpus's own link structure, ranked before any query exists and steered by the query as a personalization vector rather than filtered by it; carries the redistribution move (rank nodes, emit definitions) and the discriminative edge weights. `"pagerank centrality"` returned zero corpus-wide. Plus an amendment to `ranking-budgets`, which argues admission *value* is set-conditional and models admission *cost* as per-item additive in the same file - where the slice is rendered rather than concatenated the greedy pack is unavailable and the cut becomes a bisection over prefix length with a tolerance band, a best-under-budget ratchet and a sampled token estimator. Both applied same-run to `personas` as read-only experiments, both `better`: centrality found the stored edge table at **0 rows against 1,630 nodes** and the tier's `importance` field at its schema default for **93.7%** of the tier (0/10 top-10 overlap between arms), and the budget found a **25.1x** spread in rendered size at a constant count of 20. The centrality arm produced a finding nobody sought - `importance DEFAULT 3` is `unknown-is-not-a-value` in a schema default - which became a section of the technique rather than a footnote. 2 catches (elastic map budget, cache-breakpoint segmentation - both checked by opening the files), 4 untriaged with anchors, 2 leads (disabled-in-place fallbacks as a deliberate negative record; the architect/editor role split). Ship 0: experiments changed no product code and Phase 8 wants explicit confirmation before a cross-repo commit |
| 2026-08-31 | `github:firecrawl/anydoc` @ `261fc25` | **vendor repository (Rust crate + Node/Python/wasm bindings)** - no `docs/` at all; its operating documents are **four release notes under `.github/releases/`**, which the class's sweep checklist does not name. Add that path to the sweep: a repo with no `docs/` is not a repo with no operating documents | 2100 README / ~32000 lines in tree | 13 | 3 | 2 | 2 | 5 untriaged | [[2026-08-31-anydoc]] - operator-directed at Rust depth, a new OCR path, and a `personas` feature. 0 of 3 fetches. **A new subject was forged in-session**: `research-map` returned no prior art for `"ocr"`, `"decompression bomb"` or `"amplification"` across 340 subjects in 8 bundles, so `integration/document-text-extraction` was specced, dispatched to one forge worker, and diff-reviewed here - golden path plus `unreadable-region-refusal`, `screen-then-confirm-detection`, `extraction-yield-bands`, `recognition-boundary-and-escalation`, `structural-amplification-caps`. Read in order the four release notes are **the same defect found four times** (equations vanished; the fix escaped every `$` and broke prices; checkboxes dropped, "a sheet of tick boxes lost its answers"; scanned pages "silently missing"), and the rule that organises the subject is the source's own: a partial extraction errors "whether that is every page or one of a hundred, because output missing those pages would read as complete" - **the threshold is not a fraction, it is whether the consumer can tell**. Two amendments on `error-handling` from the enumeration hunt: `taxonomy-design` states **three** axes "because these are the questions consumers branch on", and the source's `is_fatal()` is a fourth - *recoverability in place* - whose membership inverts where absorption is the default, because **a limit that can be swallowed is not a limit** (relocate the payload into a part the reader treats as optional). Neither amendment touched a golden path, so no `content` lock in a six-sibling checkout. **The spec was wrong once and the worker caught it**: `import-validation` already owns bounded parsing including the derivation-beside-the-number rule, so two of three proposed amplification rules were already covered and only "bounds the input vs bounds the amplification factor" survived - a `research-map` empty for a phrase is not an empty for the concern. 2 catches (the two-channel error code, which `structured-propagation` already states; the cap rules above). **Apply 1, ship 1** - `unreadable-region-refusal` A/B'd in `personas` as `code`/`better`, arms paired on the project's own chunker test target: regions nameable to a caller 0 of 4 -> 4 of 4, tests 8 -> 12, no schema change. The structural fact is the good kind - a recognition command and a text-layer extractor in one binary that **could not be joined**, because the page identity was discarded by the loop that computed it. 5 untriaged with anchors, four of them in subjects live siblings were holding; the best is the source's own bench harness counting position-swap disagreement as a tie and then admitting **unpaired** verdicts at face value into the same win rate |
| 2026-08-31 | `github:VoltAgent/awesome-ai-agent-papers` @ `a2ffba2` | **reference index** - the class's first run. One 175KB README and nothing else: 16,346 words that are almost entirely link text plus a ~23-word curator annotation per row | 16346 landing / 0 in-tree docs | 40 | 7 | 11 | 9 | 366 untriaged | [[2026-08-31-voltagent-agent-papers]] - **382 distinct documents enumerated, 16 read across 2 waves, 366 ranked and recorded** in a companion file so the next pass over this index diffs instead of re-deriving. Signal mix over all 382 annotations quantifies the class's marketing half: 28 claim a negative result (7%), 126 a measurement (33%), **167 propose a framework with nothing measured (44%)**. **Three of 16 annotations were materially wrong** and each would have corrupted a top-3 sample: seeds named as a dominant effect the paper measures as its smallest (~3% vs 6-14.5 points for deterministic implementation choices); "56 scenarios" that are 53 with only 3 quantitatively evaluated; "48,000 scenarios" that are 300 base cases under ~160 conditions with the taxonomy induced from 170 hand-coded traces. Two convergence families landed. **Family A** (3 independent sightings, disjoint authors/institutions/benchmark lineage, earliest predating the others so not a relay): `prompt-safety`'s "the worst fully successful injection yields an embarrassing sentence, not an action" is false where the deliverable IS the prose - the capability ladder holds perfectly and buys nothing because nothing downstream needs to act. Landed as a bounding of its own ranking plus least privilege on the READ side, and the strongest support is internal: the subject calls a secret recalled into prose "the attack proper" two sections above the ranking that ignores it. Plus a fourth payoff class (**persuasive** - prose that works to lower its reader's chance of rejecting it) and a second fencing residual that is a property of a PAIR of spans, which every per-span operation in the subject is blind to - not an inventory gap but a remedy gap. **Family B**: the tool lane's triggers test shape, never truth; `untrusted-result-handling` is one-directional (all mechanisms lower a result's weight, none raises it) and `failover-horizon`'s six unusable-success forms are all parseability. The remedy came from **the baseline column of a paper whose own thesis failed** - agreement across repeated draws, canonicalized, precision 1.0 black-box, priced at n x tokens. **The run corrected its own premise mid-flight and the correction outranks the content**: wave 1 concluded the corpus owns nothing that scores a trajectory, verified by a grep for `ReAct`/`Plan-and-Execute` - scaffold proper nouns that `check-bundles` FORBIDS in upper layers, so the grep measured the purity gate and not the coverage. A manufactured total empty. `worker-trajectory-anatomy` (1,794 trajectories, 63k steps, 20,574-session field corpus) is exactly the missing thing and beats both papers proposing one; the full groundedness build is eight techniques in `civic-intelligence/llm-forensic-gating`. **Root cause and the run's most valuable output: `research-map` matches slugs** - a director test in-run returned six subjects for "evidence conditioned faithfulness" and `llm-forensic-gating` was not among them. Every "we have no material on X" drawn from that instrument is suspect. 2 applications, both **structural-only / unmeasurable** with the instrument named (the personas research-lab tables hold zero rows; the agent layer has no field in which a tool conflict could be recorded). 7 live siblings at claim time held most of the ground this index maps onto, which killed the top-ranked paper of all 382 (its only home is `retrieval`, held) and deferred two landings until the board cleared |
| 2026-08-31 | `github:whatwg/html` @ `778afd9` | **standard repository (living specification)** - the ledger's first, and the top tier of the corroboration table: a primary normative document, so its statements about its own conformance model authorize rather than merely originate. Landing page 302 words against 717,109 in-tree (**2,374x** - a README-only read would have been a 0.04% sample of the ad) | 302 landing / 717,109 in-tree | 13 | 2 | 2 | 2 | 6 untriaged | [[2026-08-31-whatwg-html]] - the class's calibration point is a ratio nothing else in this ledger approaches, and it inverts inside the tree too: **711,540 words of normative text produced zero landings**, while ~120 words of `<p class="note">` and a 5,472-word FAQ produced both. A standard's subject matter is un-strippable by construction; what survives is how the specification is engineered as an artifact. 0 of 3 fetches (eleventh consecutive zero-fetch run for a source carrying its own primary material). `fabrication-economics` on `quality-gates` is the **mirror of `false-positive-economics`** and the enumeration hunt's twelfth consecutive pay: a gate dies of firing on correct content; a *rule* dies of false compliance, which leaves the gate green forever. Where a requirement's satisfaction cannot be verified, the author with nothing true to write is offered two moves and the fabrication is always the cheaper one - **the gate is not fooled, the gate is the cause** - so the fix leaves the detector (which cannot improve; the distinction is not in the data) and moves to the rule's design: a declared inability token, artifact stays non-conforming, verdict channel silent, census counted elsewhere, and the token itself exempt or the pressure has only moved one rule down. `unmeasurable-criteria` states "there are exactly three honest resolutions" over a *missing* value; here nothing is missing and the fourth state is **known-violating, deliberately unreported, separately counted**. Two of its rules invert (a skip must be loud; this token must be silent) and the discriminator is written on both sides. Prior art was read and contradicted rather than duplicated: `generated-from-provenance` already holds "leave a marker, never invent" and says the checker *can* report the marker - the opposite of what the standard requires, for a reason the corpus had not reached. `declared-deviation-register` on `conformance-checking` came from a **corpus-wide empty**: `research-map` returned PRIOR ART: none for `"intentional nonconformance"` and `"why we deviate"` across 341 subjects in 8 bundles, verified by opening the four nearest subjects. The source breaks six other specifications on purpose and records every one in its own normative text, in its own conformance vocabulary, per site, with document + clause + motivation-as-cost + (twice) the upstream thread where the conflict is live, and refs pinned to a revision. It also carries the boundary: one entry marks the weaker "contradicts the spirit" class and one source comment *rejects* a candidate. Six entries over 711,540 words is what a maintained register looks like. The corpus-shaped consequence is arithmetic and lands on `pass-ratio-comparability`: an accepted deviation is a **third finding class**, never a suppression (which inflates the ratio by exactly the failures the team accepted) and never an ordinary failure (which calibrates a team to a permanent non-zero floor). Shared root with the first technique - a true, known violation declared in band and not reported as news - **stated in both files and deliberately not merged**, because an author who could not comply and a maintainer who chose not to have different failure modes and different required fields. **Apply 2 rows for 2 landings, both `better`.** The fabrication A/B ran `ab-paired` over 63 `<img>` elements in `personas`: arm A (the rule teams adopt - is the attribute present?) raises **1 finding, and it is a false positive**; arm B (is the no-content claim supportable?) raises **35, 55.6%** - 30 empty values on a runtime-computed source, 5 filenames used as descriptions. **The run's premise was disproved and the result got stronger**: the tree has no accessibility linter at all (21 hand-written lint rules, none touching the field), and the fabrication is at a clear majority anyway - so the technique gained a measured correction, *the gate is sufficient, not necessary*. The structural fact is the good kind: the one site that documents its decorative intent, using the platform's real remove-from-the-tree mechanism, is the one site arm A reports, and it is correct as written. The register simulation over `personas` found **1 of 8 behavioural conformance claims declared, 0 of 8 enumerable**, and a module header claiming a named RPC protocol whose entry point has no array handling anywhere in either file while the cited spec's §6 requires batch support - probably a correct deviation, which is exactly why a maintainer needs to be able to tell it from an omission. Three of the eight are near misses the register must *refuse*, which is where the boundary earned its place. 3 catches, 6 untriaged with anchors (the change-admission gate among them, **routed away** because `tc39-proposals-0831` held `quality-gates` live and owns the better authority for staged advancement - both landings came from material that source does not carry), 2 leads, 0 declines. Ship 0: both assessments are read-only and neither proposes a few-readable-lines change. **`index.json`/`catalog.json` regenerated under the lock and left uncommitted** - three siblings held uncommitted or untracked content, so a committed index would reference files absent from HEAD (same call as the herdr run) |
| 2026-08-31 | `brooker.co.za/blog` (163 posts; 33 ranked, 16 read in 2 waves) | **reference index (single-author archive)** - the lane's usual economics inverted: every reference shares ONE author, so within-index convergence deduped by author is structurally unavailable and every convergence is n=1. Most of the archive sits **below the construction frontier** (`fair queuing`, `uuid`, `microvm`, `overload`, `utilization`, `best of n` all returned zero corpus-wide) - third sighting | 1685 index / ~14000 read | 33 | 8 | 9 | 8 | [[2026-08-31-brooker-blog]] - **8 landings, 7 applied, 5 of them corrections to what the corpus already said and 3 of those against content sibling runs landed the same morning.** 24 fetches across 16 lanes. The instrument lied three times and workers caught it: `research-map` reported "never heard of this" for `fair queuing`, `utilization` and `queueing theory`, and all three were false - one lived under a slug nobody would reach (`scale-investment-timing` owns open-vs-closed arrivals and the 70-90% knee), two lived in an **application** document, which `research-map` does not rank. **A near-empty from that instrument is not a hole; twice in one run it was material we had written and never lifted to the technique layer.** The source's value is that it writes decompositions, and a decomposition is a probe: 10 of 16 lanes landed their best finding on a sentence where one of our own documents declares itself complete ("three policies", "three ways", "two answers", "per category, not per site", "not an age cutoff"). Its *assertions* landed almost nothing - 4 of 16 lanes found the stated class wrong (position essay, vendor product paragraph, teaching relay, 74-word aphorism) and returned `nothing`. Landed: `queue-cardinality` (new, on a subject whose nine techniques all governed policy inside ONE line; two lanes converged and our own APF application had recorded the mechanism 15 months ago); `admission-hypothesis` (new - the golden path said a cache declares **three** policies and admission's default is invisible); corrections to `reliability-aggregation` (opened with a model over a rate and forbade that move two sections later - needed no source at all), `priority-and-fairness` ("takes nothing from anyone else" is false for throughput share), `taxonomy-design` (never-absorbable is a predicate on category x **arrival correlation** - corroborated by a primary postmortem, not the essay), `retention-and-pruning` ("a query with guards, not an age cutoff" - an in-flight reader is the one reference with no row), `capability-typed-queues` (welded pool and queue, mispriced one); plus `wait-telemetry` gaining the sizing identity and the caller-vs-entry gap. **Apply 7 better / 1 not-better, all experiments on real rows, and four of them amended the corpus from the tree**: a 4.3x regression bounded `queue-cardinality` with a coexistence test; a fleet A/B put honest throughput at 1/min under the *correct* fairness policy, so the global bound protects the resource and abandons the user; a reaper gate supplied a **third** retention mechanism the amendment's enumeration excluded, plus its harm-bound and register-at-the-store-touch clauses; a broadcast-artifact experiment kept 32/32 assertions alive across 6/6 refusals that currently take them all to zero. Measured en route: failures cluster **123x** over independence in an eval corpus (chi-sq 1737), which is direct evidence for the independence correction. 8 catches, 9 leads (one at law altitude), 17 references ranked-and-unread. Ship 0: read-only lane, no cross-repo commit confirmed |
| 2026-08-31 | `danluu.com` - the six PUBLIC 2026 posts, read whole (operator filter: articles published in 2026) | **reference index (single-author archive)** - second sighting of the class in one afternoon, independently reached by `brooker-2026-08-31`. Its defining property: within-index convergence deduped by *author* is structurally unavailable, so a claim recurring across four posts is ONE observation. Unusual for the class in that the author publishes protocol, numbers, and the places he was refuted - including inverting a headline of his own. | 1600 index / **53,978 across 6 refs** | 12 | 5 | 5 | 4 | [[2026-08-31-danluu-2026]] - **100% sample of the reachable references** (6 of 6 public; 3 Patreon-only unreachable), not a top-N, which is what makes the four catches worth recording. 0 of 3 fetches - twelfth-ish consecutive for a source carrying its own measurements. Five landings, four of them in `eval-harness` which a sibling held: `candidate-write-access` (new - the system under test is an agent holding the harness's own shell; two write surfaces, instrument and neighbours; the corrective is structural and inverted - **declare the holdout rather than forbid overfitting**, whose tell is that the VISIBLE score gets worse while the held-out score rises), `resolution-precondition` (new - between-condition spread must exceed within-condition SD before an order is published; measured SD 0.075 against a full best-to-worst spread of 0.069, so every ranking that matrix could produce sat inside the noise of one of its own cells), `repeatability-floor` (new, and the run's best finding because it is a **cross-bundle asymmetry** - the builder side has carried a repeatability floor in `judge-stability` for weeks while the operator side's kappa, trust bar, per-cycle drop and windowed regression have none, so both its detectors fire on unmeasured re-score noise; 23% verdict flip re-grading ONE FIXED artifact, and the half that made it a technique is that repeatability is **per dimension** - 32%/5%/3% across one rubric), `grounding-over-deliberation` (new, `agent-chaining` - a subject that wires steps and never prices them; execution dominates deliberation and N deliberating steps do not sum to one grounding step, with the corollary that a self-produced ground truth is still the candidate's artifact), and a `comparison-modes` amendment naming the self-referential opponent. **Four catches verified by opening files rather than by slug** - `judge-stability` already owns the repeatability floor and the cross-instrument swing, `discriminating-task-selection` already owns the unanimous-cells argument, `retrieval-evaluation` already owns human tuning leak. **Apply 4 rows / 5 landings, three of them real two-arm A/Bs** against a managed project's own measurement apparatus: a 110-byte vacuous agent spec scoring 6/6 identically to the real 2,414-byte one (0 of 6 quality assertions survive candidate write access); a fabricated-citation proposal passing the shipped integrity gate 0-refused and refused by a 20-line source-fidelity arm; and **r = -0.378 between how much of a subject was judged and its deviation rate**, with 0 of 142 judged pairs ever double-judged. Ship 0 - operator's pick named no project, so no tree was edited. |
| 2026-08-31 | `lea.verou.me/blog/2026/` - all four 2026 posts | **channel corpus (first-party practitioner accounts)** - one author, a WHATWG/CSS WG spec editor, so within-batch convergence is structurally void and triage leaned on corpus-vs-source novelty. The class predicted amendments and corroborations, never new subjects; that is exactly what landed | 12768 (4 articles; index page 107, correctly thin) | 13 | 4 | 2 | 1 | [[2026-08-31-verou-2026-blog]] - **2 techniques, 1 correction, 2 golden-path sections, 0 of 3 fetches** (fourth consecutive run landing corpus-internally; this author can only *authorize* claims about the standards process, which no bundle here is about). `inherited-default-override` on `settings`: the subject models every default as a **static constant declared in code**, and a minority of keys default to a **live upstream source the app does not own** - an axis that redefines all three store operations (absent = follow continuously, a write = **detach**, a delete = **re-attach**), so the row's PRESENCE carries meaning independent of its content. Two feedback-free corruptions: writing unconditionally pins a key that was merely following, the instant the target coincides with the source; clearing an override when the source moves makes pinning **unachievable** for anyone whose environment switches on a schedule. Seam, not hole - `setting-kinds` already refuses to store *derived* limits (same freezing failure, opposite remedy: there you store nothing, here the user must be able to detach). `fallback-retirement-condition` on `optional-dependency-degradation`: that subject treats absence as a **stable per-deployment fact**; this is absence as a **closing frontier**, where the fallback is a bet the gap closes and owes a reaper the corpus already demands by law (`creation-names-reaper`) - every fallback in its ladder names a *consequence*, none named a reaper. Where the capability is testable the check IS the reaper; deleting it (proposed on integrity grounds) converts a bounded self-clearing cost into a permanent tax, and suppressing the fallback **disperses** the need into ad-hoc conditionals no upgrade retires. **The correction is the run's best shape**: `adaptive-fidelity-tiers/preference-short-circuits-measurement` had the requirement right (every statement retires a measured default; no permanent exit) and the remedy wrong (a visible third state) - invisible to any concept query built from this source's vocabulary, and only opening the file showed it. **Apply 3 rows / 3 landings, 1 shipped** (goat `facc957`, `code`, 1/2 -> 2/2 viewer environments, arms extracted from real revisions): the embed resolved `auto` **server-side**, where the viewer is invisible. Structural facts: the three-state *model* survived and the subscription died at the **resolution point**; and on personas, 8 seams test the capability not a proxy **8/8** while emitting the branch **0/8** - the check is load-bearing and the emission is not, so conformance tracks consequence rather than diligence. The apply step **amended the technique**: defer a resolution only to a consumer that can perform it. 1 catch (double-keyed caching - nothing in the corpus ever claimed the shared-CDN benefit, so there is no clock to reset), 8 untriaged with anchors (row 8, a locality/composability/scalability invariant triad, is subject-sized), 2 leads |
| 2026-08-31 | `github:TanStack/query` @ `1566c16d` | **vendor repository (open engine)** — mature practitioner codebase; 383-word README against 134,411 words of in-tree docs (497 files), 7,249 LOC core and **105,254 LOC of tests (14.5:1)** | 383 README / 134411 docs / 7249 LOC core | 12 | 3 | 2 | 7 | [[2026-08-31-tanstack-query]] — **three landings, all from hunting an enumeration or an asymmetry, and 3/3 applied to the one fleet project that consumes this library.** Fifth consecutive practitioner-codebase run to spend **0 of 3 fetches**: engine and operating instructions ship in one tree, so every claim was checkable in-run. Four siblings live at claim and **none held any of my four subjects**, so no `content` lock was ever contended — worth recording as the clean counter-case to the usual collision notes. `plural-policy-claims` (new, `client-fetch-cache`): the golden path declares *"Every cache declares four policies"* and says to state all four **at the construction site** — every one assumes a **single declarant**, and a cache keyed by argument rather than call site has a *set* of claims per policy. The code resolves that set with **three different quantifiers over one observer list** — retention by `Math.max` and monotonic, believability by `some`, focus-refetch by `find`-first — plus reference counting underneath, since an attached observer clears the GC timeout so the eviction clock starts only when the *last* claimant leaves. The unifying rule: **the quantifier is chosen by which direction of error is unrecoverable.** `suspension-is-not-failure` (new, `retry-backoff`): *"exactly four legitimate terminal states"* is correct on its own terms, which makes this a missing **stage** not a contradiction — all four are terminal and the ladder has a fifth that is not, **suspended**, where no attempt is made and no budget spent; its nearest neighbour `denied` is attributed oppositely (a breaker judges the dependency, a suspension reports the caller), and the predicate that *resumes* is strictly stronger than the one that *starts* because resuming is the machine's own initiative. `observed-read-subscription` (new, `client-state`): an **asymmetry a slug map cannot see** — the corpus models the **declared** form of subscription narrowing ("subscribe to the narrowest projection you read") and never the **observed** form, where a proxy records the read set; its three guards are each a rule, the sharpest being that an **empty read set means unknown and must fail open** (`unknown-is-not-a-value` — otherwise a never-yet-rendered consumer never renders again), and its defining hazard is that **enumeration is indistinguishable from reading**, so one spread subscribes to everything and the corrective is a linter rather than documentation. **Applied 3/3 `better`, all read-only experiments on `goat`**, with the instrument caught lying twice: a flat constant table let `GC_TIME_MS.STANDARD` overwrite `CACHE_TTL_MS.STANDARD` and reported a **2.0x spread that did not exist**, and a 900-char lookahead invented a divergence between two different keys — both found by opening cited lines by hand, after which the harness was made to assert its own parse and did refuse to print once. Measured: **2 of 7 shared cache keys diverge numerically (2.0x, 3.0x)**, both with the same shape (a prefetcher claiming longer freshness than its consumer, so the prefetch's stated warmth is inert), and the 2.0x case is a **vocabulary collision at the alias layer** — two modules binding the label `SHORT` to different constants under the same table name; the retry classifier **refuses 0 of 17** permanent error codes because it substring-scans user-facing copy while the status sits in a typed field it never reads (`gate-sees-target`), with the classification it needs already present one module away and never consulted; and the project relies on read-observation at **100% of call sites** with the lint plugin **not installed** and the explicit declaration appearing nowhere, its single known defeat sitting in a shared wrapper (blast radius honestly 2 consumers). 2 catches, 2 leads, **7 untriaged with anchors** — including the source's own best idea, that its **eight lint rules are a taxonomy of the type system's blind spots** (key order, referential identity, read set, return-ness, container lifetime), which the operator did not pick. Ship 0 at Phase 8: the triage pick named no project, so step 2 confirmation was absent. |
| 2026-08-31 | `lea.verou.me/blog/2026/` (XL follow-up, same source) | **channel corpus - the banked XL row, built** | - (no new ingest) | 4 untriaged rows folded | 7 (1 subject: golden path + 6 techniques) | 1 | 0 | [[2026-08-31-verou-2026-blog]] - the operator picked the banked `XL` row the same session, which is the outcome the scorecard's declared focus was written to produce. New subject **`dependency-declaration`** in `engineering-process/codebase-stewardship` (placement verified against `taxonomy.json`, the authority - the category holds 6 subjects and no subcategories, so a 7th adds no depth). It absorbs untriaged rows 8-11 as four of its six techniques rather than banking them as leads: `declaration-invariants` (locality/composability/scalability, and each failure's distinct cost), `logical-name-or-address` (**purview** is the discriminator; syntax and immutability are both plausible and both false), `shortcut-is-not-the-substrate` (a convenience binding that becomes the only door, and the extract-first repair order), `declaration-cost-floor` (advanced tools for advanced needs is correct; the defect is a *foundational* capability priced at an advanced tool, plus the inversion where the platform gets designed around the workaround). Two more fall out of the frame: `progressive-resolution` and `vendored-copy-loses-composition`. **The subject borrows rather than mints its central argument**: `module-design/locality-and-leverage` already holds *things that change together live together*, and a unit's dependency list changes exactly when the unit changes - so the locality invariant is an existing law applied to declarations, cited not restated. Applied 1 of 6 (experiment, `better`) with the other five carrying per-technique reasons: personas' plugin mechanism scores locality FAILS 3/3 surfaces, composability FAILS, scalability holds, **0/10 units self-declare**, and the three registries give three different answers to which plugins exist with no stated authority. Ship 0 - the repair is architectural |
| 2026-08-31 | `github:TkDodo/react-query-beyond-the-basics` - "React Query - Beyond the Basics" | **first-party practitioner account, repo form** - a workshop whose content is a *branch sequence*, not a file tree; README is 158 words, the source is seven ordered feature diffs | 158 (README) / ~700 diff lines (the source) | 10 | 1 | 1 | 3 | [[2026-08-31-tkdodo-rq-beyond-basics]] - one technique, `windowing-vs-identifying-keys` on `async-ui-states`, and it is a **stated doctrine corrected rather than a hole filled**. `state-model` named the decision (when does the sticky `settled` bit reset), called it *the subtle decision*, and prescribed "pick one policy per product and apply it everywhere"; `table/loading-and-empty-states` carried the same sentence and `arrival-choreography` the same undefined term - three sites, one primitive nobody defined. The source contradicts it in one line (`previousData?.filter === filter ? previousData : undefined`) and the same distinction independently governs three further subsystems in the same app (scroll reset on page links only, `page: 1` on filter change and never the reverse, and deferring `page` alone). So the corrective keeps the fear and drops the prescription: mixing per *call site* is the defect, mixing per **axis** is a rule the user can state. Golden path gains the `superseded` state and a **fourth** honesty lie; `state-model` gains the matching forbidden edge; `table`'s duplicate line now resolves per-axis and points here. 1 of 3 fetches, and it did two jobs - the primary confirmed the mechanism verbatim (deferral is a property of the *input*, not the response) **and** confirmed the gap (it warns only about *referential* stability, never semantic eligibility). Applied 1/1 as a read-only census over two React trees: instrument **self-asserted and refused to print** on the first run, 2 candidates, **1 survived hand-verification**, seam is a seven-field params bag (3 identifying, 4 windowing) with no retention and a search setter that does not reset the page. `unmeasurable` not `better` - both windowing coordinates are unreachable from the UI, so no gate can see it; instrument named. 3 catches, 1 lead, 4 untriaged. **Class lesson: a workshop repo's word count is a lie about its size - run `git ls-remote --heads` before calling a repository thin** |
| 2026-08-31 | `github:future-agi/future-agi` @ `5b84ef4` - open-core eval and observability platform | **vendor repository (open-core)** - holds **two agent memory systems built to opposite standards**, neither file aware of the other: the internal analysis agent's store is typed, hash-deduped, TTL-pruned and usage-tracked; the product-facing agent's is a flat key-value table where the newest twenty rows win by creation date. The managed one never faces a user | 2676 landing / **42422 in-tree md** + ~2500 LOC memory path | 9 | 2 | 3 | 3 | [[2026-08-31-future-agi]] - operator-directed at memory, which is why it landed: the yield is all in the memory code and the README is about neither system. **Two amendments, no new techniques** - the honest shape against `agent-memory`, the corpus's most mature subject and the librarian's #1 attention point (56pt), which already held 13 techniques. The headline amends `probe-without-write-back` **against its own closing claim**: that technique enumerates the counter's *readers* and rules an under-count "the better failure ... ranked conservatively". Here the usage columns are written by the persistence helper and a point lookup while the bulk read that actually serves the agent writes nothing, so `access_count` is a write counter wearing the usage axis's name - which smuggles back the very "order by last edit" failure `memory-value-model` installed that axis to repair. Absence would have been safer: zeros get noticed, plausible integers do not. Second amendment to `archive-restore-semantics` (other bundle): a same-row existence flag has two populations that fail in opposite directions - the set-level delete bypasses the per-entity soft-delete override (the agent's own delete tool hard-deletes while the human API soft-deletes), and a full unique index never heard of the flag, so a human-deleted key becomes permanently unwritable. **Applied 1 of 2, and the A/B is the run's best artifact**: both audits run on one managed tree with one instrument - arm A (the existing reader-side enumeration) returned **0** findings over four production read sites, arm B (the amendment) returned **1**, a delivery surface arm A's own "suppress new machine callers" default correctly blesses. The apply step then corrected the amendment (a read that hands its selection to a later consumer counts at the consumer). Shipped a comment-only slice: the tree's written memory contract named one caller and had three. Row 2 unapplied - no managed project carries the seam, because two independent trees already implement the branch the amendment recommends. 0 of 3 fetches |
| 2026-08-31 | `web:arxiv.org/abs/2606.10106` - "What makes a harness a harness" | **paper - the definitional subclass**: conceptual analysis over grey literature, **no measurement anywhere in it**. By the class rule ("framework papers are the class's marketing") and the tiering rule (commentary about primaries, not a primary) this is the lowest tier that can still originate a finding, and the expected yield was stated as 1-2 leads before the triage table | 768 landing / 11504 full text | 11 | 0 | 2 | 8 | 0 untriaged | [[2026-08-31-agent-harness-definition]] - **eight catches, zero landings, 0 of 3 fetches** (eleventh consecutive zero-fetch run). The corpus already owns all four of the paper's necessary-and-sufficient conditions, distributed across five subjects, and in six of the eight the corpus is *sharper*: the paper's declared central open problem - "an evaluation that isolates the harness's contribution, controlling for the model, is missing" - is `eval-harness/unaided-baseline-screening`, shipped, with the trigger "scores barely moved when the material under test was removed". Its admission that classifying six known-positive harnesses was "almost tautological" is `quality-gates/gate-liveness` as a practice. Its temporal discriminator is `hitl-approval`'s "review gates output after it exists; consent gates action before it happens". **Three near-empties in the map (`verifier`, `guardrail`, `containment`) were all seams, not holes** - believing them would have minted four duplicate techniques. Second lead is recorded to PREVENT a landing: the corpus's five-way decomposition of "agent harness" by concern is correct, and a subject named for the composition would be a container |
| 2026-08-31 | `web:tkdodo.eu/blog/the-vertical-codebase` - "The Vertical Codebase" | **first-party practitioner account** - a practitioner arguing his own product's ten-year structural decay into a general prescription; reliable for the decay, n=1 for the prescription | 1889 | 8 | 1 | 1 | 1 | [[2026-08-31-tkdodo-vertical-codebase]] - **the thesis was already covered and the yield was in the catch.** The post's argument (group by domain, not by technical kind) is carried in one sentence of `module-design/locality-and-leverage` - "where implementing one feature requires editing in three sibling trees, the grouping is describing a taxonomy of *kinds of file* rather than a taxonomy of *reasons to change*" - which catches the thesis and its detection signature together, stated more generally and without the proper nouns the post is built from. What landed came from the post's closing "Where's the catch?", the section it is least proud of: **private code means teams re-implement**. Found by the enumeration hunt - the golden path lists nine failure modes and **every one is structure being too weak, misplaced or decorative**; there was none for a boundary that *succeeded*, while the subject prescribes hiding aggressively ("hiding is active, not passive") and separately treats re-implementation as a defect fifteen times across the bundle, always fixed by *creating* a shared primitive, and never connects the two. Amendment `The cost that neither payoff prices` on `locality-and-leverage` + a tenth failure mode on the golden path: both payoffs are collected by people who already *found* the module, so leverage (capability per unit of interface learned) begins its accounting one step after discovery; hiding internals is free and is what depth buys, hiding *existence* is a separate act delivered by the same declaration as a side effect nobody chose. Discriminator is the file's own change-coupling test pointed the other way, which is why it is an amendment completing an axis rather than a competing technique - the "no" branch lands on the wrong-abstraction rule already there. **0 of 3 fetches - sixth consecutive zero-fetch run for this class**; corroboration was corpus-internal plus training-data convergence on the autonomy-vs-duplication trade, and was stronger than a fetch because the finding is a gap in *our* model. Applied 1/1 `experiment`/`better`: two arms on a Rust workspace, 232 candidates, 67 invisible to scatter, 52 after filtering forwarding wrappers; **the first instrument design was refuted by hand-verification** (its top hits were private adapters delegating to the public implementation - the single-door discipline, not duplication). Ship 0, blocker `confirmation`. 2 catches, 1 lead, 4 untriaged. Gate found red on a live sibling's file; reported, not touched |
| 2026-08-31 | `github:TkDodo/knip` - knip, find unused files, dependencies and exports | **OSS tool repository** - engine, operating documents and measurements in one tree, so every doc claim is checkable against implementing code at zero fetch | 380 (landing) / 32,535 (in-tree docs) / 28,319 LOC | 21 | 2 | 1 | 4 | [[2026-08-31-knip]] - mined the dead-code tool against the dead-code subject, and both landings are corrections rather than holes. **excess-indicts-the-instrument** (quality-gates): `gate-liveness` enumerates every instrument-failure signal and all of them are *deficiency* signals; there is no excess signal in the subject, while `blocking-by-input-determinism` asserts a large population is about the backlog rather than the check and `ratchet-design` then freezes it - the three compose into a trap where a ratchet defends a misconfiguration forever because the count only goes down. **precision-trades-have-a-direction** (codebase-scanning): false-negative appears **6 times in the entire 155-subject bundle and zero times** in the three checker techniques; wall 2 states that recall failures are invisible and forgiven and uses it to deprioritise recall, when invisibility is the reason recall needs a deliberate instrument. **The apply step refuted the run's own first technique**: on a real baseline (230/982 unreachable, 23.4%, frozen a week earlier) the distribution test fired on 7 clusters at 2.4-4.3x and all 7 were genuine dead code - dead code arrives in whole features, so both hypotheses predict the same distribution; clustering is a sampler and a referrer check is the discriminator, folded in before commit. Second apply `better` on a **zero**: 11/11 shadowing constructs detected from the maintainer doc's own enumeration, converting an unmeasurable baseline property into a measured negative. 0 of 3 fetches, as the class predicted. Ingest returned 380 words (the landing page); the entire yield came from `.agents/`, the blog and one reporter source file - a run that skipped the clone would have mined an advertisement. |
| 2026-08-31 | `github:TkDodo/pacer` @ `e5c2b53` | **vendor repository (open engine)** - hand-written guides are the source, not the README: 836-word landing page against 29,268 words of authored guides (the 87,063-word `reference/` tree is generated and was excluded from the count on purpose), 23,059 LOC core / 12,632 LOC tests | 836 landing / 29268 authored guides | 12 | 1 | 0 | 8 | 2 untriaged | [[2026-08-31-tkdodo-pacer]] - **one amendment, and eight of twelve candidates lost to the corpus.** 0 of 3 fetches, eleventh consecutive zero-fetch run on a source carrying its own primary material. Expected yield was called low before the triage table and was low: this source lands in three subjects already forged deeply (`admission-queue` 10t/8a, `retry-backoff` 7t/4a, `rate-limiting` 10t/5a), and the corpus beat it on jitter, on total-time retry budgets, on cooperative cancellation, on window families, and - most sharply - on `Infinity`-as-default, where `depth-bounds-and-shed` had already written *a bound whose degenerate value switches it off has quietly made 'off' the easiest configuration to ship* and the source's batcher ships all three flush triggers defaulted to never. The landing came from **the denial hunt**: `priority-and-fairness` states *priority orders the wait; it does not skip the gate* and forwards the one exception to `depth-bounds-and-shed`, which duly lists reject-by-class among three choosable shed policies. Both describe it as a policy a designer selects. Neither says what makes it **implementable** - that the gate must evaluate class before depth, and that the natural code shape (cheap length comparison first, class derived afterwards for the insertion sort) hands the designer refuse-newest while they believe they configured reject-by-class. That is `gate-sees-target` at the admission gate, and it diverges from the intended policy only at saturation, the one condition the levels were declared for. **The source refuted itself and that is why the pick was kept**: its guide asserts *when using maxSize, lower priority items may be rejected if the queue is full*, and its code checks capacity at `queuer.ts:410` and derives priority at `:419`, with no eviction path in either the sync or async queuer. Its own suites carry 8 priority tests and 7 capacity tests per package and **zero** that construct both. The same intersection was untested in the consuming project, found independently. |
| 2026-08-31 | `2026-agentic-coding-trends-report` (vendor PDF) | **vendor prediction report** - a class the reference does not carry; sibling of *vendor release announcement* and inherits its rule harder: the numbers are the yield, and here sixteen of seventeen pages are future tense, which the strip test cannot touch because there was never a rule in it. **The instrument failed first and failed LARGE**: `research-ingest` reported 13,029 words at exit 0 over `res.text()` on a PDF container - compressed FlateDecode streams counted as whitespace tokens. Hand-verified: **3,418**. A `--min-words` floor cannot catch this class of failure because binary always clears it. Fixed in-run (`scripts/lib/pdf-text.mjs`, font-aware via `/ToUnicode` or every bold run - where this class puts its numbers - returns mojibake) | 13,029 reported / 3,418 real | 9 | 3 | 2 | 3 | 1 untriaged | [[2026-08-31-agentic-coding-trends-report]] - both knowledge landings came from the two places the report cites measurement, and none from a prediction. `oracle-before-gate` on `hitl-approval`: the subject's four mandatory triggers are all properties of *consequence*, `verifiab*` appears **zero times** across its golden path and eleven techniques, and its rubber-stamp model is a volume model whose every cure reduces volume - so it cannot reach the reviewer who reads carefully and still has nothing to check against. The two axes come apart in the quadrant the corpus explicitly exempts: reversible-but-unverifiable work is white space on a correct gate map and is exactly what practitioners refuse to delegate. The amendment to `before-after-outcome-pairing` is the mirror: its step 5 excludes a unit missing from one side to prevent a delta manufactured by coverage change, and **induced scope has the identical shape and the opposite meaning** - absent because it did not exist, not because nobody measured it - so the rule as written deletes the effect and reports the residue. 0 of 3 fetches (twelfth consecutive) |
