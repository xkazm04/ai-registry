# Applied ledger

One row per Phase 7.5 A/B test run by `/intake` (see
[`.claude/skills/intake/SKILL.md`](../.claude/skills/intake/SKILL.md)). A technique
with no row here has never been tested against a managed project and is, for this
registry's purposes, a wiki page. `/intake apply <technique>` reads this file to find
the oldest unapplied technique. Slugs, modes, verdicts and dates only - never a
project's paths; the seam lives in the project's own `.ai/applied.jsonl`.

Modes: `code` (A/B in the tree behind a flag or branch) / `experiment` (a harness over
the same inputs, product code unchanged) / `simulation` (three real cases from the tree
or its history walked under both policies, with what would falsify the prediction) /
`render` (render-bound findings only, intake 2.9.0: one brief rendered through the
corpus-before and corpus-after instruction, the operator's blind pick is the verdict, the
renders are deleted after it).
Verdicts: `better` / `not-better` (a rejection - the technique gains a condition) /
`unmeasurable` (must name the instrument).

| Date | Technique | Subject | Project | Mode | Verdict | Return condition / note |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-06 | inline-predicate-rung-inference | conformance-checking | (source tree: a containerized LLM toolkit) | experiment | better | A = classify each spec predicate by searching the whole command string for an execution binary; B = split on shell operators, strip `cd`/`env` wrappers, match at the head of each segment. Same 543 commands, same instrument, one change. Execution share **40.0% (A) vs 20.8% (B)** - A overstates by 1.9x, and its pre-wrapper form read 7.2%, so the error is not monotone. Both arms asserted against one hand-written command per rung first; A passes those three and is still wrong on the corpus. Structural fact: 91.6% predicate coverage, 57.5% of all claims proven by a text match, one `ok` for every rung. Return: when a runner reports per-rung tallies natively, this technique describes work already done |
| 2026-09-06 | rule-registry-enumerated-fixtures | conformance-checking | (source tree: the same toolkit's lint suite) | simulation | unmeasurable | Structural-only: the suite's four passes enumerate fixture pairing from the declarative rule registry only, and the one code-defined rule is bridged by a hand-maintained map in the harness - the predicted second-definition-site hole, present. The A/B wanted is *add a code-defined rule with no map entry and watch the harness stay green*, a mutation of a third-party tree this run may not commit. **What would make it measurable, in the project's vocabulary**: export the per-pass rule-id constants into one enumerable union and fail the self-test on any id in it without a fixture; the number then reads off the harness instead of an auditor's directory listing. Return: when that union exists, or when a fleet project grows a multi-pass rule suite |
| 2026-09-06 | rule-registry-enumerated-fixtures | conformance-checking | personas | code | better | Seam: 22 gate scripts reachable from the project's `check:*` registry, **2** carrying a negative control, nothing enumerating them. Shipped an enumerating ratchet plus its own self-test. A = hand count from a directory listing (14 gates, 3 validated); B = enumerated from the registry (22 gates, 2 validated) - the hand count was wrong on both halves, missing 10 gates in subdirectories and counting a test file as a self-test. The new gate's own negative control earned itself on first run: the empty-registry guard sat after the `--json` branch, so the machine-readable path CI reads exited 0 when the parser matched nothing. Gate: self-test 4/4, eslint pre-commit green. Falsifier: the registry is `check:*` only - a gate invoked directly from CI is outside the enumeration, the same hole the technique names. Return: when a second fleet project grows a gate registry |
| 2026-09-04 | consolidation (recency inverts for a state-valued claim restated by its authority: compare instants in code) | agent-memory | personas | simulation | not-better | Seam: the sleep cycle's reconcile leg shows the model id/scope/key/value with no instant and demotes whichever loser it names. Data: the memory-year harness's surviving store (102 cycles, 536 facts, 128 links) joined to ground truth and per-probe verdicts. A = model weighs; B = code closes the older same-(scope,key) row by instant. 128 links, 0 inverted states - the weigher never lost a state change; 11 wrong-old of 95 reversal probes: 5 not yet distilled (latency), 6 distilled, 4 sharing a key (B fires), 2 not (B has nothing to join on). **Condition gained and written into the technique**: compare-instants presupposes a code-owned canonical key, and this distiller minted 160 keys for 44 states (mean 3.6, max 9); the dominant cause was distillation latency, which no ordering rule touches. Falsifier: the four shared-key probes still wrong-old after demoting older rows by instant. Return: a canonical-key step in the compress leg, then a code A/B on the harness |
| 2026-09-04 | decay-and-forgetting (cap demotes the lowest-valued by the shared model, not a column sort) | agent-memory | personas | experiment | better | Seam: the active-tier cap's keep-set (importance, accesses, recency) versus the store's own decay score, both over one persona's 40 real active rows. Measurable: of the rows A archives, how many B ranks above the lowest-valued row A keeps. Production cap 60: A archives 0, arms identical because the operation never runs (mid-state printed). Cap swept on the same rows: 1 of 10, 2 of 20, 3 of 30 - an importance-4 fact read 28 times in a day is 5th under B and 14th under A, behind two never-read importance-5 rows. Direction confirmed on every cap, no row went the other way; magnitude bounded by a store under six days old. Falsifier: a persona crossing the cap whose archived set has no row B ranks above a kept one. Return: first persona over the cap, or age spread past one half-life |
| 2026-09-04 | memory-value-model (unparseable instant is unknown, not new: default age, counted) | agent-memory | personas | experiment | unmeasurable | Seam: the store's decay score defaults an undated row to age 0 and its sweep skips it; a second parser of the same shape sits in the sleep cycle's admission gate. A = age 0 + sweep-exempt; B = one half-life of the kind, counted. n = 807 rows across both stores from the live databases (copied): 807 RFC3339, 0 in the second form, 0 unparseable - the two-format condition is parser tolerance with no rows behind it (the only second-form writes are in a test module). Placement walk on the 40-row active set: a defaulted row ranks 24/41 under A and 40/41 under B - 'ranks first' needs an aged store. Instrument: the counter arm B adds. Return: that counter nonzero in either store, or the store ageing past one half-life |
| 2026-09-04 | deterministic-proxy-gate (tree + toolchain on the gating machine; measure the spread; pin the machine class) | quality-gates | personas | simulation | better | Seam: the blocking bundle-size ratchet (bytes per built chunk vs a committed baseline, tolerance max(1%, 10 KB)), baseline written on a Windows dev box, gated on a Linux runner, carrying timestamp and sizes but no toolchain or machine stamp. Five real baseline revisions walked: 4720 KB / 154 chunks on one bundler major; **held at 4720 KB with the same timestamp across two revisions while the lockfile moved a bundler major and the chunking config changed** (hand-edited, never re-measured); honest re-measure five months later 33380 KB / 1452 chunks. A reads that as tree growth; B names the toolchain bump the baseline never absorbed. The spread B demands was measured from history: the first cross-machine run with zero frontend files changed passed with rows equal at 0.1 KB per chunk against a 10 KB floor. **Condition gained and written into the technique**: for an artifact-size counter under a lockfile the toolchain pin is the whole pin; the machine-class pin belongs to simulated-CPU and hardware counters. Orthogonal defect recorded: the current CI failure is 19 violations with the total under baseline, on rank-position keys that pair unlike chunks. Falsifier: a Windows-written baseline failing on Linux with tree and lockfile unchanged. Return: the first such pair that disagrees, or a re-baseline landing in the same commit as a bundler major bump |
| 2026-09-04 | enforcement-binding (skip resolves to success is a platform fact, not a law of the shape) | quality-gates | ai-registry (fleet audit; personas + pumper ledgers) | experiment | not-better | Seam: every checked-in CI definition in ten fleet trees plus the one second-platform pipeline. Measurable: required-check jobs whose skip would silently satisfy the requirement. 27 job-level conditions in 7 of 10 trees. A (skip = success everywhere): 6, all pumper - live protection requires six contexts and all six carry one schedule condition. B (each platform's documented semantics): 6 - identical, because every conditional-and-required job sits on the platform the old rule described; the second platform's pipeline has zero conditional jobs. Mid-state printed: platform census 10 + 1. **Condition gained and written into the technique**: on a single-platform fleet the platform read is a constant. By-product: kp's committed ruleset binds 3 of 11 declared contexts live, systedo-case 2 of 6 - the technique's original claim confirmed on 2 of 3 declarations. Return: a second-platform job gains rules and a must-succeed setting, or any pipeline moves platform |
| 2026-09-04 | operator-tier-code-loading (load-failure default derives from the declared kind) | agent-runtime-assembly | personas | simulation | better | Seam: the runner hook registry, which distinguishes kinds by type (observers return unit, interceptors a typed decision) and has one load-time policy - log-and-continue - for the single shipped observer; no interceptor ships, so the intercepting path has no policy at all. Three real failure paths walked (built-in observer fails in init; observer names an unknown point; interceptor names an unknown point). A (skip unless operator-marked): 1 of 3 leaves the request chain unguarded for the process lifetime. B (direction from the declared kind): 0 of 3. The tree already argues B at run time in its own module doc. Shape B must handle: the registry is a lazily-initialised static, so a panic there fails closed at first use, not at startup; the fix is a registration step before the runner accepts work. Floor met over failure paths, not hooks. Return: the first interceptor registered at boot |
| 2026-09-04 | host-routes-win (mount order is a security property only on a first-match router) | agent-runtime-assembly | - | - | unapplied | No fleet host mounts a route it did not author, established by search: one in-app HTTP server nests five first-party routers from one boot file; the Rust service has no HTTP framework; the MCP tool speaks stdio. Matching discipline established anyway because the rule is now conditional on it: the in-app router is the specificity family with overlapping registrations refused at build, so mount order is not a security property there and the register-time shadow check is the implementation to write when a contribution arrives. Return: when a fleet host mounts a router it did not author |
| 2026-09-04 | gate-laddering (editor rung 'catches most defects' - refuted absolute) | quality-gates | - | - | unapplied | A claim about field measurement with no fleet seam: no tree records which rung a finding surfaced on or whether it was fixed before the next rung. Instrument: a per-rung fix-rate log (for every finding, the rung it first surfaced on and whether it was fixed before crossing). Return: when a fleet tree's pre-commit or CI lane starts recording finding provenance by rung |
| 2026-09-04 | version-gate-precedes-schema-gate | repo-manifest-standard | ai-registry | code | better | **Shipped.** Seam: the shared taxonomy loader recorded the schema-version mismatch as one finding among many and kept validating the document against the version it knows. A/B on one forward-versioned fixture: arm A 5 findings (four of them instructing the reader to fix keys that were correct for the declared version), arm B2 1 finding. The naive early return (arm B) was WORSE - it returned a truthy object, so the bundle checker's `if (taxonomy)` cross-check would have fired once per subject (191). Shipped arm returns the loader's existing not-usable signal. The correction was fed back into the technique. |
| 2026-09-04 | gate-liveness (could-not-run routing amendment) | quality-gates | ai-registry | code | better | **Shipped.** Seam: the catalog builder drops an unparseable usage contributor file, warns, and then publishes `invokes30d` - a DERIVED number - under a summary reading `usage from N contributor(s)`, where N silently excludes the dropped file. Routing could-not-run to pass is correct here (its green authorizes no claim; validation lives in a separate gate that refuses), but obligation 2 of the amendment - write the blast radius down - was unmet. A/B on a 2-good/1-broken fixture: both arms publish the same total (240); only arm B states the predicate. Console-only change, so no generated-artifact conflict with live siblings. No application document: the node--gate-liveness slot is held by an unrelated tree. |
| 2026-09-04 | fork-to-outlive-the-healed | self-healing | pumper | simulation | not-better | **A rejection with a written falsifier.** Three real failure classes, taken from the worker's own source comment enumerating what its recovery path cannot reach, walked under in-process (A) and process-per-work-item (B): panic - equivalent, B buys nothing; non-yielding wedge - B better (a true verdict instead of a stale-lease label); hard abort - B clearly better (only the child dies, siblings survive). B still loses: the work item holds a live shared spend counter it decrements as it spends, plus a progress reporter and a checkpointer, none of which serialize into an invocation - and a child that spends then hard-aborts consumes budget the parent never saw, reopening the record-precedes-effect window the fork was bought to close. Return condition: when the cost ledger becomes write-ahead (the tree already half has the shape - spend is restored from checkpoints and cost events are recorded), the disqualifier dissolves and the hard-abort case wins. Structural fact nobody designed: the tree already forks one layer in, where what crosses is a request and a response. The technique gained the condition and a placement section. |
| 2026-09-04 | author-declared-include-graph | settings | - | - | unapplied | no seam in the fleet: no managed project carries a configuration format whose documents name their own parents. The registry's clause stamper is the nearest thing and is not it - it inlines one shared fragment into many documents rather than merging many into one, and it already carries provenance out of band in a marker comment. Return condition: when a project grows an extends-shaped config (a shared lint base, a shared pipeline fragment). Source-tree application written against the external tree, which realizes the per-link root and pays the in-band-provenance cost in two places. |
| 2026-09-04 | log-architecture (level-bound-to-side-effect amendment) | observability-telemetry | - | - | unapplied | no seam in the three authorized trees, established by search rather than assumed: every termination site in all three is print-then-exit at an explicit call site (108 such sites in the registry's scripts alone), and neither gate helper accumulates-then-exits - both accumulate and let the caller decide, which is exactly the separation the amendment prescribes. Return condition: when a managed project routes termination through its logging facade. |
| 2026-08-31 | refusal-is-not-failure | optional-dependency-degradation | politicas | experiment | better | **The tree has already mechanised the two adjacent shapes and this one falls between them.** Seam: a custom lint plugin whose `no-silent-catch` bans *empty* catches, and whose sibling `no-silent-null-catch` was written second — its own rationale is this technique's thesis, reached independently and with a price attached: "the loader failure shape that actually bites is a catch that returns a fallback value. The page renders, looks intentional, and a dead database becomes indistinguishable from an empty one. This class of bug cost politicas a day of diagnosis (2026-07-25)." **Arms over the two rules' documented predicates against the extension idiom** `catch (e) { warn(e); return builtin(v); }`: rule 1 does not fire ("any catch with at least one real statement… a fallback assignment" is explicitly exempt); rule 2 does not fire (it keys on `return null`/`return []` and explicitly exempts "a non-empty/substantive value"). **2 of 2 miss it, and the second misses it by design** — the extension fallback returns the default implementation's real output, so it is substantive by construction. Not a defect in either rule: they cover "returned nothing" and "returned an empty thing", and the refusal-override returns neither. Amendment earned and applied: the technique now names both rules and the shape of a third that would catch it (key on the fallback re-invoking the built-in counterpart of what just failed). 4 disables of rule 1 exist in-tree, each carrying a stated reason, so the escape-hatch discipline is already there to extend. Ship 0: the operator's triage answer named no project. Return condition: operator authorization to add the third rule, or a second project showing the same two-rule blind spot |
| 2026-08-31 | destination-guard-integrity | outbound-notifications | personas | code | not-better | **The consumer already implements the technique in a stronger form than the source did, and one call site states the source's defect verbatim as a bug it had already fixed.** Arms: A = the seam as built; B = the seam with the technique applied — B is a no-op, so `not-better` is the honest row. The guard is installed as the client's **own DNS resolver** (`build_ssrf_safe_client`, `core/src/url_safety.rs`), not mounted onto a caller's client, so the validate-then-reconnect window does not exist rather than being closed; the redirect policy re-validates **every** hop. **All three borrowed-client defects the source found while reviewing its own fix are structurally unreachable here** (no mount, no displace, no restore) — a shared process-wide client constructed once. The range table independently covers the exact gap the source had to patch: CGNAT `100.64.0.0/10` is present with its RFC named, alongside documentation ranges, cloud metadata, v4-mapped-v6 and unique-local, and a comment recording that other modules delegate here "so the historical divergent copies cannot drift and reopen an SSRF bypass" — `one-validation-door`, learned the hard way. **The tree corrected the technique.** The guarded-GET helper enumerates three layers and explains why the pre-flight validator is *not* redundant with the connect-time resolver: it covers "everything DNS never sees: an IP literal (no lookup happens, so no resolver can veto it), a non-http scheme, a cloud-metadata hostname that fails to resolve locally but resolves inside a cloud network, and fail-closed on DNS error." The technique as first written implied the pre-flight check was the problem; a resolver-only design has a hole exactly where a literal address is supplied, which is the commonest hostile input. New section added. Ship 0, none owed. Return condition: none — this is a confirmation, and the amendment is the yield |
| 2026-08-31 | transport-selection (library-stdout amendment) | mcp-tools | — | unapplied | — | **No seam in the fleet, stated rather than manufactured.** The amendment governs the process that *is* a stdio protocol server; every managed project sits on the host side, spawning such servers as children. Checked anyway and the host half is clean: every spawn site inspected sets stdout and stderr as separate pipes — streams are never merged at any of the 6 sites — which confirms the reader-side rule `output-normalization` already carries ("the output stream is data; the error stream is logs") and leaves the writer-side rule untested. A simulation was available and declined: walking invented server code would be an opinion with a table around it. Return condition: **the first managed project that ships a stdio protocol server**, or any script in the fleet whose stdout is a parsed data contract and which links a library that can print |
| 2026-08-31 | vacuous-by-evaluation | quality-gates | personas | code | better | **Two real arms, same runner, byte-identical output — the technique's central claim demonstrated rather than argued.** Seam: a 122-line zero-dependency conformance checker wired to a merge gate (`on: pull_request`, exits non-zero on any `fail`). It reports **92%, 0 fail** on the real tree. **Arm A** = a manifest declaring its three path pointers; **arm B** = the same filesystem with the entire `paths:` block deleted. Both score **100%, 0 fail, 0 warn**, and arm B prints `[OK] context index .ai/context-index.json` — a path it never declared. Cause is the defaulting layer directly above the predicate: the reader falls back to the conventional path before the existence check reads it, so three checks labelled as manifest-pointer resolution are in fact filesystem existence checks, and are unfalsifiable with respect to the manifest. Exactly the technique's named pattern (a required-field rule downstream of a defaulting step). **Separately counted, and separately classified**: of the 6 blocking rules, the only one that verifies capability commands actually *work* is guarded behind a flag the CI step does not pass — that is `severity-by-construction`/`gate-liveness` territory, not this technique, and is recorded as such rather than folded in to inflate the finding. Change argued: read the raw sub-key, report a missing declaration distinctly from a missing file. Ship 0: the operator's triage answer named no project, and Phase 8 requires confirmation before a project tree is touched. Return condition: operator authorization; the fix is ~6 lines and the two-arm fixture already exists |
| 2026-08-31 | verification-scope | supply-chain | personas | experiment | better | **The technique's rule fires on a real, uncovered site, and the corpus already holds the reason it matters.** Census of tool-install sites across the fleet's CI: the dependency-policy engine and the advisory scanner are each installed with a lockfile flag and **no version pin** — three sites in this project, and a fourth project references the same pattern. **That flag pins the tool's own dependency graph and leaves the tool's version entirely floating** — every run installs whatever the registry's latest is. That is precisely this technique's "a tool subcommand that downloads is an unpinned dependency, whatever it calls itself", and the site is not incidental: the tool **is** the dependency-policy engine, so this is the exact hazard `dependency-policy-gates` already names — "a pipeline that installs 'the latest' engine against a frozen config has scheduled an outage for whenever upstream renames a config key… Engine floats, policy frozen, gate silently dead." Two techniques, reached from opposite directions (supply-chain scope; gate liveness), converging on one unpinned line. Measurable named: policy-engine installs pinned to a version, currently **0 of 3**. Change argued: add an explicit version to each, bumped as a reviewed diff alongside the policy file. Ship 0: no project was named in the triage answer. Return condition: operator authorization, or the first policy-engine release that renames a config key, which converts this from a finding into an outage |
| 2026-08-31 | lockfile-freshness-oracle | supply-chain | personas | experiment | not-better | **The ecosystem's own strict install already is the oracle, and finding that out corrected the technique.** Arms: A = the pipeline as built (the strict install at 7 sites across six workflows, plus the locked flag on the other ecosystem); B = add the hand-built re-resolve-under-constraint check the technique describes. B adds nothing: the strict install refuses outright when the lockfile cannot satisfy the manifest and never resolves, so it is immune to upstream releases by construction — which is the exact property the technique was built to engineer. Drift census run over the real manifest as the falsifier: **62 manifest dependencies, 0 drift** against the lockfile's root entry, and any nonzero would have failed the install rather than needing a new gate. **Amendment earned and applied**: the technique now opens the seam with "first ask whether your installer already is the oracle", names the case where it is not — a lockfile *compiled* from the manifest by a separate tool, which is the source's own situation and the reason it needed a hand-built check — and adds the decision rule. Without this the technique would have prescribed duplicated logic to every ecosystem whose installer already enforces it. Ship 0, none owed — no change was warranted. Return condition: a fleet project adopting a compile-to-lockfile workflow, where the built-in guarantee disappears |
| 2026-08-31 | hallucinated-reference-sweep | llm-forensic-gating | politicas | experiment | not-better | **experiment, both arms run, verdict split and the split is the finding.** Seam: the project's law-verdict gate checks cited statutes for MEMBERSHIP only (`knownLaws.has(ref)`) and never compares a citation's attributes to the identifier that carries them; `plausible-date` bounds each date globally (1993..today) and refuses to repair, so the law is already held - nothing joins a date to another field of the SAME record. Arm A = gate as it stands. Arm B = the intrinsic cross-field arithmetic, run over 761 of the project's own documents. **B flagged 114, of which hand-inspection left ~1 real** - a statute cited `95/2024` where `95/2004` was meant, which the project's analysts had already found by hand and which membership passes because 95/2024 is itself a real law. `not-better` as an automated gate: in legislative prose the years beside a statute are PROCESS dates (submitted, read, signed), so 'earlier year, later identifier' is the normal case, not the impossible one. Same instrument on preprint-citing generated research: 24 real contradictions, and 0 against the hand-written half of the same repository - so the predicate discriminates where a document has one date and collapses where its subject has a lifecycle. Condition written into the technique. No project file was modified. |
| 2026-08-31 | advancement-evidence-fields | quality-gates | personas | experiment | better | **seam recorded in the project's `.ai/applied.jsonl` (append only; that file is untracked and already holds three other runs' uncommitted entries, so committing it would carry their work).** read-only A/B over a 15-dimension readiness passport. Measurable: dimensions whose evidence state resolves from structured fields alone. Arm A (shipped schema: level + tool(string|null) + skippedByChoice(bool) + free-prose note) 11 of 15; arm B (four-state field) 15 of 15. Second measurable — dimensions with an individually knowable evidence age: **0 of 15** against one collection-level date 5 weeks old. Honest negative reported: the predicted unreadable-blank population is ABSENT (0 of 15 carry neither pointer nor note) — the discipline is the author's, not the schema's, which is the technique's own convention-not-schema hazard observed as benign. **Structural fact outranked both arms**: the schema has no `reason` field of any name, so *absent-with-a-pointer-to-why* — the state the technique calls most valuable — is inexpressible; `skippedByChoice` is a boolean that records THAT a skip happened and never what for. No product code changed; nothing written to the project tree (operator approved a registry-side plan naming no project), so the seam is not in that project's `.ai/applied.jsonl` and is owed. **The seam write found a second, stronger instance the first pass missed**: the same project also carries a TYPED passport model whose 11 per-dimension level unions each begin with `none` and none of which has a not-assessed member, with the field non-optional and the conflation documented as intent. It propagates to a claim - one dimension is derived as a binary over a 5-member union (3 levels unreachable), `none` scores 0 of 18, and it renders to the user as a sentence asserting the absence. An unassessed dimension is published as a definite negative claim about a real project: `unknown-is-not-a-value` at the type level in 11 places. The skip flag has no counterpart there at all, so a reasoned skip is discarded at the boundary rather than merely unstructured. Application document amended with it. Return condition: the countable-backlog claim needs two passport generations under the new schema; this tree has one |
| 2026-08-31 | item-liveness | quality-gates | ai-registry | experiment | unmeasurable | ran the technique's own diagnostic against this repo's two item pipelines (harvest queue, 177 entries all at one status; watchlist, 56 rows) and **found its precondition missing**: both carry a single collection-level date and no per-item trail, so last-touched is not derivable at any price — the technique gained an amendment naming that condition and the schema cost where the cheap version fails. Effect genuinely unmeasurable: the queue is 3 days old, so zero items are stale under any threshold and both arms return the same number; reporting `better` would be reporting the premise as the result. The failure it DOES show structurally is that `entries: 177 / statuses: queued` carries no liveness predicate and nothing will surface the first aged cohort. Instrument named: a per-entry `touched` date, or a join from entry id to the run ledgers that already record dated events. Return condition: re-run when the oldest still-queued entry passes 90 days (~2026-11-26); the 138 banked leads reach it sooner and are the cheaper first test, their dates already derivable from note filenames |
| 2026-08-31 | structural-centrality-lane | retrieval | personas | experiment | better | read-only A/B over the live 1,630-node corpus. Arm A (rank by the curated `importance` field): 5 distinct levels over the 510-node query-independent tier, 478 of 510 (93.7%) holding the schema default, so the tier's top-N is an arbitrary N drawn from one tie block. Arm B (graph derived from item content, no stored edges): 3,523 derived edges against 0 stored, 44 distinct levels, 0/10 top-10 overlap with arm A - arm A's own winner sits at centrality rank 470 of 510. Extends the 2026-08-27 finding on the same tree: the relation store is not merely unread, it is empty, and the curated path never started rather than decayed. Second finding not sought: the default-valued importance column is an `unknown-is-not-a-value` instance, which became a section of the technique rather than a footnote of the application. No product code changed. Return condition: retrieval *quality* needs a labelled query set this tree lacks for the companion path, and per-query personalization is unmeasured - only the uniform restart was exercised |
| 2026-08-31 | ranking-budgets amendment | retrieval | personas | code | better | read-only A/B over 1,105 episode nodes. Arm A (the budget as implemented - a count of 20 items, cost modelled per item and uniform): a fixed 20-item window renders between 1,560 and 39,212 chars, a 25.1x spread at constant budget, over items spanning 2 to 4,918 chars. Arm B (bisect over prefix length, measuring the rendered artifact per probe): bounds the quantity the consumer actually spends. The non-additive term is measurable - tier framing costs 83 chars paid once per non-empty tier, 17.9% of the block over the 20 smallest items and 0.14% over the 20 largest, a 128x swing in fixed-cost share that no per-item accounting can see. The tree gets the harder half right (a shared budget rather than per-lane quotas, documented as a scar it earned). **Shipped**: arm B is now the tree's behaviour - the episode block admits the largest trailing run whose rendered size fits a declared budget, reports what it dropped, and carries four cases. Pre-commit gate (format, secret scan) passed; `cargo check --lib --features desktop` type-checks the changed modules with no diagnostics, but the project's own test runner never executed the committed tests - the crate builds only under a feature combination a second session held the build lock for throughout the run. Return condition: re-verify under the project's suite when the tree is free, and revisit when the companion path has an eval slice that can see answer quality rather than only rendered size |
| 2026-08-31 | reliability-aggregation | eval-harness | personas | experiment | better | A/B in the project's own test runner over one trial set, two prompt versions x 3 scenarios x 3 trials: arm A (shipped mean-composite ranking) returns the spiky version as winner (73 vs 70), arm B (same trials, all-of-N added) returns the steady one - the leaderboard winner flips. Spiky is the technique's third reading (any-of-N 0.67, all-of-N 0.33). Probe deleted, no product code changed. Second finding not sought: an unscored metric averages to 0 and sorts as poor performance. Return condition: adoption needs a pass predicate this grid does not declare - the cheap intermediate (per-scenario spread beside the mean, no threshold) is the change filed first |
| 2026-08-31 | absent-status-passthrough | fleet-orchestration | personas | simulation | better | three cases from the tree. Case 1 already conforms and is the evidence: the auto-kill gate takes an optional state and fails closed on absence - but the optional is a map lookup's return type, not a modelled unknown, so the discipline holds only where the type system imposed it. The 8-member state vocabulary has no 'not stated', and the light-sleep path (which frees a live process) guards on a non-optional field. Bounded: the process scan feeds an operator list, not a reaper, and the reaping ticker never probes. Mode is simulation because the missing vocabulary member is both the defect and the reason it is invisible. Instrument named: adding the member makes transitions out of it countable before any gate changes |
| 2026-08-31 | critique-carries-its-fix | review-iteration-loops | gravity | code | better | **shipped** `b256f24` (main, not pushed). Tree already had the rule's strong form; the gap was one layer on - `store.ts` promoted an abandoned replica's best frame into the style catalogue as an exemplar, indistinguishable from a successful one, and exemplars are what later generation conditions on. A/B over two runs at identical sub-target scores: arm A (score proxy) 3 vs 3 indistinguishable, arm B (settle reason) no-usable-fix vs round-cap, 3 of 3 classified. **An earlier reading was corrected rather than shipped**: the progress-count over-credit is not a defect - that consumer asks 'will more work happen here' and is right to collapse the causes; the technique gained a third-consumer paragraph from it. Return condition: count the real share of abandoned settles, now recordable for the first time |
| 2026-08-31 | capability-coverage-contract | agent-instruction-files | gravity | experiment | better | soundness 19/19 both directions, completeness 7 of 19 named in the planning surfaces; enumerated form writable today because the registry is generated. Second weaker seam checked and rejected rather than counted (deployment capability matrix 0 of 5, but read by product surfaces not by an agent choosing means). Return when the repo declares which unnamed skills are deliberate curation - until then an omission and a decision are the same artifact |
| 2026-08-31 | delivery-promise-lock | production-pipeline-phasing | gravity | simulation | better | `structural-only`: the promise exists and is locked before the means - the hard half, done independently - but carries no rules and is validated nowhere; three consumers only. Phase-state vocabulary uses one token for 'not required' and 'not obtained' and it ranks best in the worst-news merge. Return when a cut assembles mixed motion and slide grammar, which makes the anti-substitution ratio measurable rather than simulated |
| 2026-08-31 | operation-assertion-gates | quality-gates | personas | experiment | not-better | tree already refuses an empty scope in 4 of 4 checkers tested (arm B exit 1-2, never 0), so the rule moves nothing here; technique gained the tree-based-rule condition from this seam. Return when the 21 custom lint rules gain fixture tests and the per-rule suppression count can be re-read |
| 2026-08-31 | vendored-fork-ledger | supply-chain | - | - | unapplied | no seam in the fleet: none of the 7 managed projects carries a patched or vendored dependency (no manifest-level source override in any tree). Return when a project forks a dependency instead of waiting for upstream |
| 2026-08-31 | self-paced-intake | admission-queue | personas | simulation | better | emit the per-drain batch size and read its distribution; the tree counts dropped records but not batch sizes, so proof is structural-only |
| 2026-08-30 | prose-rule-drift | quality-gates | ascent | experiment | better | 27 drift violations across 4 projects found by a checker nothing invokes; fix filed, not shipped - awaiting operator confirmation to touch the tree |
| 2026-08-30 | failure-attribution (8th owner) | eval-harness | - | - | unapplied | when a managed project runs an agent loop whose termination policy can cut a run short; no project currently owns one |
| 2026-08-30 | measurement-revision | eval-harness | - | - | unapplied | when a project revises a published measurement; nothing in the fleet has re-run a circulated result |
| 2026-08-30 | brief-carries-the-session (review brief) | fleet-orchestration | - | - | unapplied | not tested this run - no simulation was run, so no verdict is claimed. Return when a review dispatch can be run twice against one artifact, one arm carrying the producer narrative and one withholding it; the read is whether the verdicts differ |
| 2026-08-30 | task-envelope (specificity ladder) | prompt-assembly | - | - | unapplied | when a managed project dispatches to a weaker-tier model; the fleet routes to one tier today |
| 2026-08-29 | session-registry | fleet-orchestration | personas | code | better | branch `apply/session-registry` reviewed and merged 2026-08-29 |
| 2026-08-29 | fail-loud-classification-default | hitl-approval | personas | code | better | branch `apply/fail-loud-classification-default` reviewed and merged 2026-08-29 |
| 2026-08-29 | incident-promotion | self-healing | personas | code | better | branch `apply/incident-promotion` reviewed and merged 2026-08-29 |
| 2026-08-29 | editor-interop | markdown-vault | personas | experiment | better | ship as the project's next change |
| 2026-08-29 | safe-mode-guarding | sql-console | personas | code | better | branch `apply/safe-mode-guarding` reviewed and merged 2026-08-29 |
| 2026-08-29 | template-anatomy | templates-scaffolding | personas | code | better | branch `apply/template-anatomy` reviewed and merged 2026-08-29 |
| 2026-08-29 | rotation-and-remediation | credential-vault | personas | code | better | branch `apply/rotation-and-remediation` reviewed and merged 2026-08-29 |
| 2026-08-29 | empty-and-degraded-chart-states | data-viz | personas | code | better | branch `apply/empty-and-degraded-chart-states` reviewed and merged 2026-08-29 |
| 2026-08-29 | unpriced-span-accounting | trace-rollup-and-attribution | personas | code | better | branch `apply/unpriced-span-accounting` reviewed and merged 2026-08-29 |
| 2026-08-29 | derived-trace-rollup | trace-rollup-and-attribution | personas | code | better | branch `apply/derived-trace-rollup` reviewed and merged 2026-08-29 |
| 2026-08-29 | first-run-and-quiet-silence | session-resume | personas | code | better | branch `apply/first-run-and-quiet-silence` reviewed and merged 2026-08-29 |
| 2026-08-29 | field-defaults-and-bounds | draft-editing | personas | code | better | branch `apply/field-defaults-and-bounds` reviewed and merged 2026-08-29 |
| 2026-08-29 | stop-reason-ledgers | agent-chaining | personas | code | better | branch `apply/stop-reason-ledgers` reviewed and merged 2026-08-29 |
| 2026-08-29 | event-registry | realtime-events | personas | code | better | branch `apply/event-registry` merged 2026-08-29 together with the repair it demanded: 7 names folded into the registry with payload types, the onboarding listener re-pointed at a producer that exists |
| 2026-08-29 | three-state-outcomes | health-checks | personas | code | better | branch `apply/three-state-outcomes` reviewed and merged 2026-08-29 |
| 2026-08-29 | queue-ordering-and-identity | triage-queues | personas | code | better | branch `apply/queue-ordering-and-identity` reviewed and merged 2026-08-29 |
| 2026-08-29 | suite-partitioning | test-harness | personas | code | better | branch `apply/suite-partitioning` reviewed and merged 2026-08-29 |
| 2026-08-29 | sink-abstraction | usage-analytics | personas | code | better | branch `apply/sink-abstraction` reviewed and merged 2026-08-29 |
| 2026-08-29 | activation-and-funnel-honesty | usage-analytics | personas | code | better | branch `apply/activation-and-funnel-honesty` reviewed and merged 2026-08-29 |
| 2026-08-29 | tombstone-propagation | sync-replication | personas | experiment | better | ship as the project's next change |
| 2026-08-29 | diff-honesty | diff-comparison | personas | code | better | branch `apply/diff-honesty` reviewed and merged 2026-08-29 |
| 2026-08-29 | persistence-and-migration | client-state | personas | code | better | branch `apply/persistence-and-migration` reviewed and merged 2026-08-29 |
| 2026-08-29 | async-race-guards | client-state | personas | code | better | branch `apply/async-race-guards` reviewed and merged 2026-08-29 |
| 2026-08-29 | project-identity-and-joins | multi-project | personas | simulation | better | ship as the project's next change |
| 2026-08-29 | scale-conversion-of-numbers | content-research-grounding | gravity | code | better | branch `apply/scale-conversion-of-numbers` reviewed and merged 2026-08-29 |
| 2026-08-29 | precision-limit-propagation | evidence-bound-visuals | gravity | code | better | branch `apply/precision-limit-propagation` reviewed and merged 2026-08-29 |
| 2026-08-29 | edit-plan-over-regeneration | review-iteration-loops | gravity | code | better | branch `apply/edit-plan-over-regeneration` reviewed and merged 2026-08-29 |
| 2026-08-29 | evidence-grading-ladder | content-research-grounding | gravity | experiment | not-better | when facts[] carries sources[] on the load-bearing rows - or when a gate refuses a `source` string holding more than one publication-shaped token. The seam class is 'render-surface change over an unmigrated data layer'; do not re-run the chip test until the data moves. |
| 2026-08-29 | prompt-dialect-matching | image-prompt-composition | gravity | experiment | unmeasurable | pipeline/build-style-trials.mts re-run dialect-matched - the repo's own 6-style x 5-beat grid, 60 graded cells per policy, scored usable = on-brief AND free of text. The existing 60-cell trial that demoted the caption-class provider ran one dialect's prompt across both classes, so it is the same instrument with the confound removed. |
| 2026-08-30 | engagement-paced-cadence | cost-metering | ascent | simulation | better | upgrade to experiment when the episode store can be queried: count consecutive declared-silence episodes per org; the cheap-probe lever needs no cron change |
| 2026-08-30 | tiered-history-projection | prompt-assembly | ascent | simulation | not-better | technique gained its adoption gate from this seam; return when stored conversations routinely exceed the flat tail (countable from the persisted turns) |
| 2026-08-31 | dual-anchor-scoring | reference-parity-gating | personas | experiment | not-better | a second anchor over the SAME representation is not a second authority - arm B flagged 2 of 4 specs and both were its own parser's artifacts. Seam class is 'gate over a text artifact whose only cheap second measurement re-reads the same document'; do not re-run a static second anchor here. Return when the golden-output eval layer exists and supplies a behavioural anchor. |
| 2026-08-31 | borrowed-surface | module-design | personas | experiment | better | `ab-paired` on a faithful standalone reduction; **not committed**. Same probe line both arms (rustc 1.96.1): A (forwarding operator present, tree as-is) compiles clean and drops a decrypted value un-scrubbed; B (forwarding removed, two scrubbing methods exposed) is `error[E0596]`. Reachable un-scrubbed drop paths: every mutator the wrapped type has -> 0. The tree's own state is the finding - one caller reaches through the borrowed surface and hand-rolls the invariant in four lines under a comment, so the guard is a convention, not a boundary. Return: the product crate's build script fails on a permission-manifest mismatch before rustc reaches this code; re-run when that build is green.
| 2026-08-31 | typed-accessors | settings | personas | code | better | `ab-paired`, committed. Same test (asserts the whole `limits` category and cross-checks its list against the store's own category function so it cannot drift): A FAILED - a weekly budget ceiling accepts a non-numeric value at the write door; B ok. Category enforcement 3/4 -> 4/4. Structural fact: enforcement is per key with nothing counting it - 58 of 90 key constants reachable inside the validator, and the unenforced ceiling's sibling is enforced *and* carries six negative test cases. Return: the other 31 unenforced keys are a backlog; revisit when a key gains a declared type in the registry so the audit can span all 90 instead of one category.
| 2026-08-31 | generated-shot-sourcing (rung-3-two-worlds amendment) | video-assembly | gravity | simulation | better | re-test as `code` when the studio grows a generative-video request path with frame anchoring; today the tree has no motion request anywhere, and the amendment was still confirmed by the cut list's shape - one picked frame per scene, one span per clip, so a clip containing its own cut is unrepresentable |
| 2026-08-31 | reference-role-map (two-reference-seam amendment) | image-prompt-composition | gravity | simulation | unmeasurable | instrument: the foundry verdict grader run over composite calls - which the studio does not yet make; return when a subject reference and a location plate are attached to one generate/edit call |
| 2026-08-31 | authored-voice-identity | voice-io | personas | experiment | better | `ab-paired` on a faithful standalone reduction (rustc 1.97.1), **not committed**. Same probe both arms - add a new engine to the tree's `TtsEngineId`: A (empty `impl TtsEngineId {}`, media-studio guard written `if engine != Kokoro`) **compiles clean** and silently refuses the new engine; B (specification kind declared as a capability, guard rewritten against it) fails with `error[E0004]: non-exhaustive patterns`, forcing the declaration at the moment the engine is added. Capability predicates on the engine type 0 -> 1; specification kinds expressible in the request 1 -> 3. The tree independently CONFIRMED the storage rule (a cloned voice's system of record is its reference wav, not an engine id) and **refuted** the transcript bullet - its cloning engine is zero-shot, so the technique now makes self-containment an engine property. Return: run as `code` when a project tree is confirmed; the diff is small and the project's own suite can see it. |
| 2026-08-31 | on-device-vs-cloud (rights-axis amendment) | voice-io | personas | experiment | better | `ab-paired` (rustc 1.97.1), **not committed**. Measurable: can a surface that ships an artifact ask whether it is permitted to? A: no - installable engines 2, engines whose rights status is queryable 0, surfaces gating on output rights 0, and the one license that IS recorded (`pocket.rs:38-40`, a **non-commercial** packaged export) lives in a module doc comment. B: rights declared on the engine type; the voiceover surface refuses the non-commercial engine while the speak-aloud surface allows it - the per-use rule demonstrated rather than asserted. **Structural fact the tree supplied for free**: `voiceover.rs:61` already refuses that engine, but via `if engine != Kokoro` - the right outcome from an unrelated rule, and the obvious next feature deletes the line and the accidental protection with it. Return: `code` mode when a project tree is confirmed. |
| 2026-08-31 | tts-pipeline (recording-chain amendment) | voice-io | personas | simulation | unmeasurable | Three real cases from the tree; two are void against it as it stands (the curated catalog ships exactly ONE voice, and Media Studio pieces carry a single voiceover), one is live (the merged picker lists user-cloned wavs beside studio voices ordered by servability, not by recording provenance). Acoustic or recording-provenance fields across both catalogs: 0; the nearest field, `grade`, is a training-VOLUME proxy by its own comment. Instrument that would settle it, and the reason this is not a verdict: an acoustic-similarity measure over rendered samples from both engines against a reference bed, plus one usage fact - whether any produced piece has ever mixed engines. The tree has no audio analysis of any kind. Return when either exists. |
| 2026-09-03 | edition-stratified-conformance | conformance-checking | ascent | code | better | ab-paired at n=1 fixture per arm (the file's own vitest suite): under a rubric change the alert glue dispatched once before the guard and zero times after, 38/38 pass; a legacy row with no recorded rubric still dispatches (unknown is not changed). Committed on the project's active branch, not pushed. Structural fact: the outcomes lane already keyed aggregates by rubric version and the alert lane did not. Return: a deliberate re-baselining an org wants to hear about needs its own message class; none exists. |
| 2026-09-03 | capability-subtraction-sandbox (counted-ceilings amendment) | untrusted-extension-host | pumper | simulation | unmeasurable | Three real e2e cases (a trap that fires the hop open, a fuel burn under skip, an instantiation refused for declared memory) end identically under both arms: the counted list (fuel, linear memory, tables, instances) is already published beside each plugin's consumption, and the uncounted list is empty by construction because the plugins import nothing. Instrument that would separate the arms: the linker's import count, currently zero - the first import re-opens the second list. Proof: structural-only. |
| 2026-09-03 | stage-ordered-fuzz-targets | test-input-generation | pumper | simulation | better | Three real cases from the harness that drives the real plugin host, walked under one end-to-end harness (A) and per-stage targets triaged in pipeline order (B): B reaches transform-stage defects on inputs the predicate stage traps on; A cannot. Falsifier: run a transform target alone over the predicate's trapped inputs and count distinct failures - zero means B is a harness with no finding. The tree had already met the masking once (a never-loaded predicate read as a pass) and fixed it by hand. Filed as a direction proposal (a new context), gate skipped (unattended). |
| 2026-09-04 | input-channel-parity | generated-output-grading | gravity | code | better | **The tree held the technique for one dimension and had written its own reasoning for it.** A channel-capability flag already existed on the provider descriptor for reference images, enforced as a routing constraint, because a request field "honoured or quietly ignored" is a failure that looks like a success. The negative prompt has the identical property and had no declaration: a native request field on one adapter, appended to the positive prompt as a sentence on the other. The two dimensions **invert across the same provider pair**, and nobody designed that - it fell out of the reference case being the one whose failure is visible on sight. Declaration rather than routing, per the technique, because both arms do honour it. Paired against HEAD on one counter: unequal dimensions declared 1/2 -> 2/2, recorded on provenance 0/2 -> 1/2; typecheck clean; 41 lines over 4 files; proof `structural-only` (no vendor call). Return: when the grid next runs with a negative prompt, read the recorded channel and check whether a flip attributed to the model survives at prose parity. |
| 2026-09-04 | anchor-removal-ablation | evidence-bound-visuals | (none) | unapplied | - | **No seam anywhere in the eight authorized trees, and that is a state rather than a miss.** The technique needs a generator that fills unobserved regions of a *real referent*; only two projects declare this domain and both condition on **style** references explicitly labelled "not content to reproduce", while the other candidate tree's only 3D surfaces are UI inside a worktree. Not simulated: three invented cases would have been an opinion with a table around it, and the simulation floor requires cases from a real tree. Return condition: when a project grows a reconstruction or inpainting surface over real referents. |

## Backtest waves

| Date | Projects | Pairs judged | Technique C / D / N-A / U | Proposals | Note |
| --- | --- | --- | --- | --- | --- |
| 2026-08-29 | personas, gravity | 150 | 193 / 364 / 503 / 37 | 64 | [[backtests/2026-08-29-personas-gravity-wave-1]] |
| 2026-08-30 | verifier-coverage-review-agenda | unattended-build-loop | pof | experiment | better | file the reporting change (per-feature reached rung, per-gate verdict count, unjudged list in the completion summary) as pof's next harness change; re-run as `code` when the visual gate can start on this machine |
| 2026-08-31 | payoff-removal (persuasive class) | prompt-safety | personas | experiment | unmeasurable | Seam: the research-lab report synthesizer, whose prompt asks in its own words for a narrative Discussion section a person publishes - the deliverable IS the prose, which is the case the technique's `inert` class assumes away. Structural fact, and it is the good kind: the caller passes every hypothesis, experiment and finding for the project unfiltered, and the only limits on the path are `slice(0,40)/slice(0,40)/slice(0,60)` plus per-field truncation - a TOKEN BUDGET, not a read privilege. Meanwhile the finding record already carries `hypothesis_ids`, and the same field is already parsed elsewhere in the app to lay out the project graph. The relation that would scope the report exists, is populated, and is used to draw a picture; it is not consulted at the one seam whose output is published. Nobody designed that. Both arms are expressible (A = shipped predicate, B = filter to findings linked to a hypothesis in scope) and neither ran: the feature's tables hold ZERO rows, so there is no population and no history, and constructed projects would be an opinion with a table around it. Instrument that would settle it: a research-lab project carrying real findings - then A minus B is one query. Also recorded: the type system cannot express the exclusion at all (no visibility/publishability field exists), so arm B is the cheapest available fix rather than the correct one |
| 2026-08-31 | untrusted-result-handling (contested-result arbitration) | mcp-tools | personas | experiment | unmeasurable | Seam: the agent tool-invocation layer of a desktop orchestration app with a full MCP surface. Structural fact by absence, which is the form the amendment predicts: a search across the agent layer for the vocabulary of arbitration - conflict, contradiction, disagreement, mismatch, any spelling - returns NOTHING. There is no branch that fires when a tool returns a result the model's own answer disputes, because the codebase has no notion that such a state exists. Every mechanism the parent discipline prescribes is present in some form and all of them lower or bound what a result may do; the missing one is not a defense but a DECISION, and a codebase does not grow a decision it has no word for. No behavioural arm was runnable: no recorded-run store for tool conflicts, no fixture with a planted disagreement, and - binding - no field in which a conflict could be recorded even if one occurred, so a run that hit the case is indistinguishable from one that did not. Instrument: a replayable tool-call log carrying both the tool result and the model's pre-tool answer, which turns the disagreement rate from a hypothesis into a query |
| 2026-08-31 | prompt-safety golden path (read-side least privilege) | prompt-safety | - | - | unapplied | Same seam as the payoff-removal row and deliberately not double-counted: the golden-path half (capability bounds action harms; disclosure harms need least privilege on the read side) is the general statement of what that tree demonstrates, and applying it separately would report one measurement twice. Return condition: when a second project grows a prose-deliverable seam with a populated store, apply the read-scope rule there rather than re-running it here |
| 2026-08-31 | untrusted-span-fencing (pair-property residual) | prompt-safety | - | - | unapplied | No seam in the fleet. The residual is harm arising from a PAIR of individually-unobjectionable spans, which requires an automatic unconditioned recall path assembling stored context beside a live request. No managed project runs one: the research-lab synthesizer assembles from an explicit project scope rather than by relevance, which is a different shape. Return condition: when a project grows relevance-ranked recall into a prompt - the check belongs at the recall point, where both halves of the pair are visible at once |
| 2026-08-31 | failover-horizon (seventh unusable success + resample agreement) | model-routing | - | - | unapplied | No seam reachable this run. The instrument the amendment names - agreement across repeated draws, canonicalized - is priced at n x tokens and n x latency, and no managed project issues repeated draws of the same request anywhere, so there is nothing to A/B against. Return condition: when a project routes a tool call whose wrong answer costs more than n draws (an irreversible action, a reported figure, a writing tool call), measure the disagreement rate at n=3 before deciding whether the price is worth paying |
| 2026-08-31 | coordination-failure-triage (symptom vs cause judging) | fleet-orchestration | - | - | unapplied | The amendment's claim is about the reliability of an agent judge on a cause axis, and testing it needs a labelled corpus of failed traces with human-assigned root causes to score a judge against. No managed project keeps one. Return condition: when a fleet accumulates failed traces with human cause labels - at that point the measurement is the amendment's own protocol, run locally: score the judge on effect and on cause over the same artifacts and compare |
| 2026-08-31 | failure-attribution (pre-run owners) | eval-harness | - | - | unapplied | Needs a harness whose red cases include environment and dependency-resolution failures, then a re-attribution of those cases under the amended funnel to show they currently land on Model. The fleet's harnesses fail closed on a broken environment rather than recording it as a case, so the population the amendment is about is not collected anywhere. Return condition: when a project's eval harness records a non-starting run as a case rather than as an abort |

A backtest verdict is not an A/B row: `conformant` says the project already realizes
the technique (the seam exists and holds), `deviation` names the seam where an A/B is
owed. Rows in the table above are minted from deviations, one project per technique.

## Unapplied backlog (owed, oldest first)

- `oracle-frozen-during-repair` (quality-gates) - landed 2026-08-29 by intake; candidate
  seams: any managed project running agent repair tasks with hooks in `.claude/settings.json`.
- `gate-laddering` amendment (asking controls at stage boundaries) - 2026-08-29.
- `eval-economics` amendment (configuration in the golden-set trigger) - 2026-08-29.
- `self-healing` two-ladders amendment and `failure-diagnosis` rule - 2026-08-29.

### 2026-08-31 - intake, genesis-agi (memory focus)

- `lane-reconciliation` (agent-memory) x goat - **experiment** - **better**. A: shipped
  write path, 2 of 4 category fixtures diverge record-vs-cache-lane and lose the write on
  reload, 0 detected. B: + read-only reconcile predicate, same 2 diverge, 2 detected,
  0 false positives. Return condition: re-run as `code` when the operator confirms the
  project lane - the fix is one shared key derivation, or an observable no-op path on the sync.
- `probe-without-write-back` (agent-memory) x goat - **experiment** - **better**. Call-site
  enumeration over a rank-feeding counter. A (count on the read path, suppress per caller):
  3 machine caller classes reach the counter, 3 flags required, new readers default to
  counting. B (shipped: uncounted read + explicit fire-once write): 0, 0, default correct.
  The tree's shape fed a new section back into the technique.
- `three-state-outcomes` per-class amendment (health-checks) x goat - **experiment** -
  **not-better**. Zero edits required to reach arm B: the tree's lint ratchet already carries
  a distinct cannot-run exit, refuses partial re-baselining, and asserts a non-empty
  population before trusting counts. Not a rejection - independent corroboration from a tree
  with no connection to the source. Return condition: re-test when a checker here grows a
  finding class whose enumeration can truncate mid-scan rather than fail outright.
- Seams NOT recorded in the project's `.ai/applied.jsonl` for any of the three: that is a
  project-tree write and the operator confirmed no project. Owed on the next confirmed run.
- `evidence-without-verdict` (machine-authored-documentation) x politicas - **code** -
  **better**, shipped. Seam: a design-quality detector's visual engine returned a finding or
  `null` from five places; four meant "could not measure" and one meant "measured and clean",
  and the caller collapsed them with `finding ? [finding] : []`. Three edits - a
  pass/fail/unresolved return, an advisory `contrast-coverage` rule so the denominator rides
  the findings array without becoming a failure, and a tally across both arms.
  Paired proof, both arms running the real exported `runVisualContrastFallback` over one
  stubbed page, 8 candidates: real findings 2 vs 2 and byte-identical, unmeasured candidates
  visible to a reader 0 -> 3, coverage line matching ground truth exactly (5 of 8 measured,
  3 unresolved with the correct reason breakdown). The change alters what the detector can
  say, not what it detects. Committed to the project's default branch, +92/-9, not pushed.
  **Three structural facts, none designed.** (1) The report had no skipped record type at
  all, so the conflation was forced by the record shape rather than chosen at the call site -
  a denominator cannot be added at a call site. (2) The vocabulary already existed one layer
  up: the browser-side analyzer speaks `unresolved` with reasons and its caller dropped them
  at the module boundary - `verdict-survives-boundary`, not an absent vocabulary. (3) The
  same repository implements `checked-vs-skipped-denominators` **exemplarily in a different
  gate** - its doc-sync hook prints four named skip classes at zero and says of its own
  informational block that it cannot fail a build. A discipline is adopted by a gate, not by
  a codebase, and the gate next door does not inherit it.
  And the defect demonstrated itself on this commit: the 77-second `eslint-staged` pre-commit
  hook passed by *not looking* - `.claude/skills/**` is an ignore pattern, so eslint exits 0
  with "File ignored". The detector that gates this project's design quality sits outside
  every one of the project's own gates, which makes the paired proof load-bearing rather
  than decorative.
  Return condition: the live-page rate - how often a selector actually goes stale - is the
  measurement this change makes possible and does not supply; the first real scan after
  `puppeteer` lands produces it. Open: `--no-advisory` still suppresses the coverage line,
  and 31 of the detector's 39 conflation sites are in four untouched engines.

- `speculative-work-admission` (admission-queue) x goat - **code** - **better**, shipped.
  Seam: the prefetch manager enqueued speculative arrivals into a 50-deep queue drained at
  the live concurrency limit, so under congestion a promoted prefetch lands after its value
  window. A/B on the real queue, 86-arrival scroll session, swept across the project's own
  network tiers: 3g 52 dispatches/18 useful -> 30/30 with zero waste; 2g 17/5 -> 10/8; 4g a
  no-op because the queue never saturates. More useful prefetches for less bandwidth - not a
  trade, because the waiting line was converting useful work into waste. The third arm is the
  finding: skipping ALL sources served 0/6 hover prefetches at both congested tiers, so the
  high-intent exemption is load-bearing and the technique gained a boundary section from it.
  Return condition: re-measure with the project's own hitRate/unused counters from a throttled
  browser session - the useful/wasted split rests on a modelled value window.
- `quality-axis-separation` (model-routing) x gravity - **simulation** - **better**.
  Three cases from the tree, decisive one from its recorded history: a frame-planning route
  whose first implementation used "nine roles, nine canned compositions" and produced "exactly
  the deck it deserved" - fast, deterministic, schema-valid, semantically worthless. A single
  outcome-fed quality score rates that maximal. Structural fact: the router has no quality term
  at all (static posture x turn-class ladder), so the failure is unreachable there - but the
  sink seam already exists as the typed descent trail, correct and feeding nothing, which is the
  state in which the shortcut is cheapest. Return condition: becomes measurable when the ladder
  gains a live-measurement ranker.
- `resource-denominated-bounds` (admission-queue) x goat - **simulation** - **unmeasurable**.
  Count bounds (maxSize 50 / maxConcurrent 3) where the scarce resource is bandwidth; the
  project already derives the count from its network tier, which is load-aware-admission done
  right. Whether payload cost varies enough to justify byte denomination is unknown here.
  Instrument named: per-source response size in the prefetch analytics; if p95/p50 exceeds ~10x,
  re-test byte-denominated admission against the count bound.
- `priority-and-fairness` amendment (admission-queue) - **unapplied, no seam**. No managed
  project shards capacity on a caller-minted key: goat's prefetch origins are internal and
  attested (which the amendment says is the safe case), gravity's router has no per-origin
  capacity, pumper's model access is a single-tenant chokepoint. Return condition: when a
  project grows a multi-tenant admission gate that runs before authentication.
- `unreadable-region-refusal` (document-text-extraction) - **personas, `code`, `better`,
  proof `ab-paired`.** Seam: the text-layer chunker counted pages with no text layer into
  a column and discarded which pages they were, at the moment it knew. Same binary ships a
  vision-model recognition command that takes a file. Arms: the same twelve-page input
  (four unreadable) through the project's own chunker test target, before and after.
  Regions detected 4 in both arms; regions **nameable to a caller** 0 of 4 -> 4 of 4;
  digest text `4 page(s) are scanned` -> `pages 2, 5-7 of 12`; readable pages still
  indexed in both. Chunker tests 8 -> 12, all passing. No schema change - the ranges ride
  an existing nullable metadata column the ingest was not writing. Return condition: the
  saving is **available, not realized** - nothing yet calls the recognition path with the
  region list, so re-test the cost arm when it does.
- `screen-then-confirm-detection`, `extraction-yield-bands`,
  `recognition-boundary-and-escalation`, `structural-amplification-caps`
  (document-text-extraction) - **unapplied this run.** Landed with the subject; the run's
  budget is one project per finding and it was spent on the seam above, which is the only
  one of the five with a live instance in the fleet. Return condition: `/intake apply`,
  oldest first - the recognition-boundary technique has the same seam and is the next one
  owed a row.
- `taxonomy-design` fourth-axis amendment and `swallowed-error-prevention` leniency
  amendment (error-handling) - **unapplied, no seam.** No managed project runs a
  recover-by-default reader over untrusted containers: the fleet's parsers either fail the
  whole document (the chunker above) or parse trusted internal shapes. The amendment's
  claim is about components that absorb by design, and nothing in the fleet absorbs by
  design. Return condition: when a project grows a lenient reader over foreign binary
  input, or adopts a third-party one whose recovery policy it must audit.

- **2026-08-31** · `fabrication-economics` · `personas` · **experiment** · **better** ·
  arms paired over one population: 63 `<img>` elements, one parser, two predicates.
  Arm A (the rule as adopted - is the attribute present?) raises 1 finding and it is a
  false positive; arm B (is the no-content claim supportable?) raises 35, 55.6% - 30
  empty values asserting no-information on a runtime-computed source, 5 filenames used
  as descriptions. The premise was disproved and the result strengthened: the tree has
  no accessibility linter at all, and the fabrication is at a clear majority anyway, so
  the technique gained the correction that a gate is *sufficient, not necessary* - the
  pressure comes from the requirement's shape. Structural fact: the one site that
  declares its decorative intent with the platform's real mechanism is the one site arm
  A reports, and it is correct as written. Return condition: when the token is separated
  from the affirmative claim, re-run arm B - the 35 should split into a declared set and
  a real backlog, and the split is the adoption test.

- **2026-08-31** · `declared-deviation-register` · `personas` · **simulation** · **better** ·
  8 behavioural conformance claims to external specifications (after excluding ~200
  incidental format references); 1 of 8 carries a declared deviation, scoring 2 of the
  technique's 4 fields; 0 of 8 are enumerable without reading implementation code, and
  no register exists in any surface. Structural fact: a module header claims a named RPC
  protocol, its entry point takes one line and returns at most one response, and no array
  handling exists anywhere in either file while the cited specification's §6 requires
  batch support - a probably-correct deviation that a maintainer cannot distinguish from
  an omission. Three of the eight sites are near misses the register must refuse, which
  is where the boundary was tested rather than asserted. Return condition: when the
  project publishes a register, re-walk the eight and count entries that survive the
  boundary - a register that admits the three near misses has become a changelog.
| 2026-08-31 | reliability-aggregation | eval-harness | personas | experiment | better | read-only re-aggregation of the bench's own 1,026 recorded trials (342 groups of 3). Arm A (pooled per-trial rate, compounded): any-of-3 100.0%, all-of-3 80.6%. Arm B (observed over the same trials): 95.9% / 89.2%. Replicated on an earlier 486-trial corpus: modelled 97.9% vs observed 80.9%, a 17-point optimism. **The independence assumption is refuted on the data**: groups failing all three number 14 against 0.11 predicted (123x, chi-square 1737 on 2 df); the earlier corpus repeats it at 9.1x. Failure concentrates in a handful of scenarios missed every time (one is 0-of-27) while most pass everywhere. Independence also fails mechanically - 682 of 684 repeat trials read a warm prompt cache, so reps were never independent draws. **Structural fact, strong negative**: the aggregate record has fields for trial count, pass rate and latencies and NONE for scenario count, rep count, or measured-vs-modelled; the header reads `runs`, so 38 scenarios x 3 reps presents as 114 independent measurements. The conflation is forced by the record shape, not chosen at a call site. Elsewhere in the same project a subsystem that does carry a sample count is marked dead and unwired. Return condition: none owed; distinguishing 'same hard case missed twice' from two unrelated misses needs failure attribution the harness does not collect |
| 2026-08-31 | priority-and-fairness | admission-queue | goat | experiment | better | the project's own limiter logic transplanted verbatim with its shipped constants, one interleaved trace, 10 honest callers and 1 attacker. Arm A (per-key capacity shard on a mintable key - left-most forwarded-for hop, and an API key accepted from a query param on a format regex): at 1,000 minted keys, 10,100 admitted/min against a 10/min cap, a 101x overshoot unbounded in a quantity the attacker chooses. Arm B (one global budget, key used only for round-robin ordering): 100/min, fixed. **Attacker share 99.0% under BOTH**, matching the correction's 1000/(1000+M) prediction to the decimal. Fleet sweep: 21 server-side key derivations, 4 caller-mintable, **4 of 4 of those shard capacity** - the forbidden case with no exceptions either way; 2 of 7 projects run per-key limits with no process-wide ceiling at all. **Two amendments came back from the seam**: honest throughput fell 100/min to 1/min under the *correct* policy, so the global bound protects the resource and abandons the user and only an attested key restores their slice; and a per-key limiter in process memory behind an autoscaler degrades in occupancy with no attacker at all, because the replica count sets the shard count. Two other trees had reached the rule independently, one recording the exact bypass in a code comment. Return condition: none owed; sizing the bound in currency rather than requests needs a spend meter this tree lacks |
| 2026-08-31 | queue-cardinality | admission-queue | pumper | experiment | not-better | discrete-event replay of the runner's exact claim loop over its 160 real completed jobs, model first validated against history (aggregate wait within 0.03s over 160 jobs, 160/160 agreement on did-this-job-wait). Arm A (as shipped: 4 fungible permits, one line, 20 work kinds). Arm B (per-kind caps at 1/2/3 - **already implemented in the tree and shipped disabled**). Splitting is **4.3x worse** in aggregate (263.6s to 1124.4s at cap 1), 4.3x worse for the only kind that ever waits, and changes the other nineteen by nothing. Cause measured directly: over the month the queue was non-empty 1,610s of which **zero** held more than one kind; 5 of 159 waiting jobs overlapped another kind, max overlap 0.035s. Every congestion event was one kind fanning out against itself. The technique's own discriminator agrees - the permits are genuinely fungible, non-interchangeability lives downstream in a per-host governor. **The rejection produced the technique's coexistence test**: the cost claim 'splitting costs nothing where servers are non-fungible' holds in the steady state and fails under class-clustered arrivals, a common shape. Structural fact: a disabled feature served as a control arm somebody had already built. Return condition: re-test where coexistence is non-zero - named next seams are gravity's three-vendor imaging router (no queue at all) and tracklight's two-provider dispatch |
| 2026-08-31 | wait-telemetry | admission-queue | pumper | experiment | better | same 160 rows, two mechanisms, two arms each. Caller gap: entry-mean wait 263.7s vs length-biased 952.1s (**3.6x**), service 2.7x, time-in-system 3.0x - with **no starvation anywhere** (every job drained, max wait 20min), which is the amendment's exact claim. Mean 263.7s against median 0.01s: one number overstating nineteen work kinds by four orders of magnitude and understating the victim by 2.2x. Sizing identity over the one saturating window: lambda 0.0416/s, W 868s, so L = 36.1 held, 32.1 of it queue, and lambda*S = **3.99 against a hardcoded 4** - the identity derives the shipped constant to two decimals, validated against direct integration. **The amendment that came back**: the same L over burst/day/month is 36.1 / 1.53 / 0.021, a 1,700x spread, so averaging occupancy over a window no arrival experienced is the caller-vs-entry error in the capacity direction - the two new sections are one error told twice, and the technique now says so. **Structural fact**: both stamps exist in every row as accidents of retry backoff and lease bookkeeping, their difference is the technique's entire stamp pair, and no query, log, gauge or route subtracts them - while the one place that does measure elapsed time covers execution only and its table is empty. The fast-executor-slandered incident, pre-arranged. Return condition: re-read the ratio under a mixed sustained arrival stream - it should fall if the tail is fan-in-shaped and hold if upstream-latency-shaped |
| 2026-08-31 | admission-hypothesis | client-fetch-cache | goat | experiment | better | the app's own route matcher replicated verbatim against its real route surface enumerated from the page files. Arm A (admission as configured: 7 rules, 10 prefetch targets). Arm B (the bet checked - a rule naming a route the app does not serve is a bet on nothing): **5 of 7 rules (71%) and 8 of 10 targets (80%) point at routes that do not exist**, confirmed not rewrites. The product's own hot path admits nothing: the team's e2e journey walks a route matching no pattern, and hover intent routes through the same matcher. Census: 75 registration sites, 41 key families, **0 stating a reuse hypothesis**; 13 families carry more than one lifetime across their own sites (one has 8 sites and 4 lifetimes); three parallel lifetime vocabularies including two different objects sharing a name that map the same label to different values. Free-text-keyed search families (unbounded key population) share the store with hot reference data and are walked by every invalidation. **Structural fact**: the client's default options carry 7 keys and no admission slot; the one policy-taking helper resolves to exactly lifetime and eviction; the upstream library offers no admission hook, so the omission is not local taste. Fleet-wide there is no cache constructor outside this app that could take one. Return condition: the meter exists and is unwired - a prefetch manager tracks hits and a hit rate and **nothing calls its recording function**, so the rate is structurally 0%; wiring it at the read path converts reachability into conversion. No test of any kind covers the prefetch or cache directories |
| 2026-08-31 | retention-and-pruning | versioning-snapshots | pumper | experiment | better | classification of all six readers over the pruned tables against the activity gate the tree already has. **The tree independently built the amendment's first repair and recorded the incident that forced it** - janitors on a bare sleep were guaranteed to land mid-scrape holding the writer lock; its own comment says the fix is 'to stop scheduling and start measuring'. That gate is a **third mechanism the amendment's enumeration excluded** (track whether anyone is reading, not which version each holds) and it is available exactly where the technique had declared the time bound unavailable. **0 of 6 readers carry a lifetime bound**; no request-timeout layer exists in the server at all; fleet-wide 1 bounded reader in ~15. The gate's documented blind spot is the finding: the guard drops at the response head, so a streaming export with 'no row cap' and two cursor feeds spanning requests are invisible while they hit the store the janitor deletes from. Data: 10,782 revisions over 8,546 keys in 7 days, peak 5,843/day, 193 keys already past the floor - machine-rate creation, the condition the amendment names. **Two clauses came back to the technique**: a live-reader mechanism owes a harm bound and a deferral count or it converts data loss into silent no-maintenance; and register at the store touch, not at the response. Return condition: latent with a computed trigger - live when retention is enabled and the corpus ages past the window, or when a walk exceeds the 6h interval (~10s mean page latency at the observed rate) |
| 2026-08-31 | taxonomy-design | error-handling | pumper + gravity | experiment | better | executable arm on gravity's inlined 25KB template ruleset with the project's own 32-assertion regression driver, run in scratch outside every tree; six mutations each a plausible drift in a pushed artifact (out-of-union role, illegal kind, non-integer threshold, empty role, unknown rule name, truncated write). Arm A (newest artifact is the only artifact): **0 of 32 checks run on 6 of 6 refusals**, for every consumer, off one deploy. Arm B (last-known-good, staleness visible): **32 of 32 on all six**, still naming the offending field. Counted arm on pumper: `config.toml` carries 1 syntax path + 29 semantic rules, overwhelmingly resource-exhaustion caps, all terminating out of main() over a git-tracked cluster-wide artifact - instances that stop serving on a bad push: all of them; while a byte-identical sibling TOML in the same tree absorbs at every consumer and stops zero. **The experiment found a failure in the repair**: the truncated case fails in the decoder with no filename while the five validator faults name field and file, so a fallback inheriting the validator's diagnostics handles five classes informatively and the most likely one anonymously - now a clause in the technique, alongside staleness visibility. **Structural facts**: last-known-good is inexpressible without changing a signature (loader takes no parameters, no persisted prior config in the tree), and the pre-migration snapshot is already written in two unrelated trees with **no non-test restore path in either** - one of which implements the exact mechanism a layer down for per-job state. Fleet tally 11 refuse vs ~9 degrade on broadcast artifacts, **no project consistent**. Three trees reached the rule independently. Return condition: arm B's real cost (a decision made under a stale ruleset) needs a counter joining the stale boot path to verdicts reached under it - nobody emits one |
| 2026-08-31 | capability-typed-queues | runner-fleet | - | - | unapplied | **no seam in any managed project**, established by enumeration rather than assumed: 17 workflow files, 60 runner declarations, 8 distinct labels, and **zero self-hosted runners in any tree or any commit in any history**. Every runner is hosted, so the queue is per-label and owned by the platform - no project can merge or split it, and the pooling-vs-queueing decision the corrected bullet governs has never been available to this fleet. 9 concurrency blocks, all workflow-level, none spanning distinct job types. The nearest reachable shape is a needs-barrier over a heterogeneous matrix, which is the same head-of-line mechanism at a different merge point - simulated on tracklight over three real cases including a dated incident where a retired runner image left three consecutive releases cancelled and an installer 404ing for one architecture, and corroborated negatively by personas (10 jobs, 4 runner types, 0 needs edges, with the cost named in-file). Reported as a neighbouring finding rather than as this technique's application, because it tests a barrier and not a queue. Return condition: **the fleet has already named where this becomes real** - politicas marks an adoption point for a self-hosted ingest runner whose 1.6GB store cannot exist on a hosted one; when that lands it will hold two genuinely non-fungible runner classes and the queue decision exists for the first time |
| 2026-08-31 | baseline-ladder (learned rung / elaboration predicate / ablation regime) | agent-memory | - | - | unapplied | Three amendments from three independent papers, all about how a memory number is produced rather than about a store. No seam: applying them needs a project running a memory *comparison* - a ladder with rungs, or a learned recall policy with an assignment that could be pinned. No managed project runs either; the fleet's memory surfaces are single-design and unmeasured. Return condition: when a project grows a second memory design and compares them, these three are the predicates its comparison owes before the number means anything. |
| 2026-08-31 | deterministic-backbone (enforced-vs-honored / tolerance / stored backbone) | judgment-guardbands | - | - | unapplied | Three amendments from three independent papers. The enforced-vs-honored half is checkable statically against any project with an annotation-driven container, and the fleet has one - but the amendment is a rule about what an extractor may be *trusted* for, and no managed project runs a structural extractor whose reach could be measured against it. The tolerance half needs a comparator that admits a margin; a grep of the fleet found none. Return condition: when a project ships a structural extractor or a tolerant value check, both halves become directly testable and the tolerance one is a one-line fixture. |
| 2026-08-31 | candidate-write-access | eval-harness | personas | experiment | better | read-only A/B, no project tree edited (operator's pick named no project, so Phase 8 step 2 confirmation was absent). Measurable: quality assertions in the shipped agent-spec eval lane that gate a property the candidate cannot satisfy by editing the graded file. The graded artifact is `.claude/agents/*.md` - written by the agents that edit the repo - so the graded and writable artifacts are the same file. Arm A: the real spec, 2,414 bytes, 6/6 PASS. Arm B: a vacuous 110-byte spec containing only the literal strings the six regexes hunt for, **6/6 PASS**. Quality assertions surviving write access: **0 of 6**; a spec 95.4% smaller scores identically. Structural confirmation the tree was not built to give: the same project already carries the *neighbour*-surface control (`no-live-mutation`, byte-identical before/after) in its sibling memory-reflection eval, and never generalized it to the instrument surface, because a regex over markdown does not look like something the candidate writes. Honest bound: reachability, not evidence any spec was gamed. Return condition: re-run if the lane gains assertions - the harness transcribes the six rather than importing them (the shipped file is a Vitest module that scans a directory at import time) |
| 2026-08-31 | resolution-precondition | eval-harness | personas | experiment | better | read-only, over 1,031 technique verdicts across 66 subjects in the shipped conformance corpus. Arm A (the ranking as computed): deviation rate per subject, mean 0.440, SD across subjects 0.282, range 0.00-1.00 - a spread that looks trivially resolvable. Arm B (same data, stratified by how much of each subject was judged): **Pearson r = -0.378 between techniques-judged and deviation rate**. Subjects judged on <=6 techniques: n=27, mean rate 0.577, 8 saturated at exactly 0.0 or 1.0. Subjects judged on >=12: n=29, mean rate 0.329, 1 saturated. **A subject looks worse the less of it was judged** - the five subjects at a perfect 1.00 were each judged on six techniques, while the two largest samples (48 and 33) both land at 0.12 and read as the healthiest. Ranking rates over unequal denominators ranks the denominators. The technique's real output here was a **third state**, not a verdict: the within-subject SD is unobtainable from this corpus (0 overlapping pairs), so the precondition's answer is *the check cannot be performed* - distinct from both `resolved` and `noise`, and invisible from a sorted list. Return condition: the confound is measured but three different causal stories produce it (harsher judging of small samples / small samples drawn from worse subjects / sampler stopping early where it found problems) and they call for different fixes - needs the per-subject sampling rule to separate |
| 2026-08-31 | grounding-over-deliberation | agent-chaining | personas | experiment | better | read-only A/B against `runChecks` extracted verbatim (lines 127-188) from the shipped memory-reflection eval. The harness cleanly separates the two step kinds this technique distinguishes: a grounding step (14 deterministic assertions -> `checks.json`) and a deliberating step (`review.md` side-by-side, *'judge each review.md, then apply or discard'*). Finding: **all 14 grounding assertions check integrity, none checks support** - >=2 sources, source exists, not core-tier, importance in 1..5, not double-actioned, no-live-mutation. Measurable: does the gate refuse a well-formed proposal whose declared sources do not support its insight? Two proposals, identical insight text, only `sourceIds` differ. Arm A (shipped): P1 truthful PASS, P2 fabricated-citation **PASS** - 0 of 2 refused. Arm B (+ a 20-line check that recomputes support from the insight text instead of trusting `sourceIds`): P1 PASS, P2 **FAIL**, 1 of 2, no false positive, and it named the memories that actually support the insight. The deliberating step inherits the same blindness and that is the transferable half: `review.md` renders the insight beside *the sources the proposal named*, so the reviewer checks the internal coherence of the candidate's own story - coherent by construction when the insight was written first. Confirming structural fact: the team reached for exactly this move once (`no-live-mutation` grounds a claim against a snapshot the candidate does not control) and not on the citation, because the citation *looks* checked. Return condition: n=2 on constructed rows - the harness needs the app on its test-automation port, not started; a rate at which the reflection pass actually fabricates is unmeasured |
| 2026-08-31 | repeatability-floor | judge-calibration-and-drift | personas | simulation | better | `structural-only`; three real cases from the shipped conformance corpus walked under both policies. The measured fact: 142 judged pairs, 12 workers, 113 `deviation` / 21 `not-applicable` / 7 `conformant` / 1 `unknown` - and **0 (subject, technique) keys judged by more than one worker**. Not one verdict in the corpus was ever produced twice. Nobody decided against measuring judge repeatability; the pipeline partitions work across twelve workers *for coverage*, which is correct for throughput and makes a repeatability floor **structurally unobtainable from the output, permanently and after the fact**. The 79.6% deviation base rate makes it bite: a worker answering `deviation` to everything agrees with the observed distribution four times in five while carrying no information, and with zero overlap there is no way to tell it from a discriminating one. Cases walked with falsifiers: the top-ranked attention subject's 8-deviation count; the 7 `conformant` verdicts (the rarest and most consequential, each one draw from an unmeasured judge against a base rate that makes them unusual); and the evidence strings, where the technique's per-dimension rule predicts the line-locating half repeats far better than the is-this-a-deviation half. Negative form is the stronger evidence - a pipeline designed to prove anything about judge reliability would have overlapped something. Instrument named: overlap ~5% of pairs in the next conform run (~7 extra judgements of 142), no human labels, no new dataset. Return condition: re-run when that overlap exists; until then deviation counts are leads, which is what this bundle already says an uncalibrated judge produces |
| 2026-08-31 | comparison-modes (self-referential-opponent amendment) | eval-harness | - | unapplied | - | **No seam in any managed project.** The amendment names the case where an eval's opponent is an earlier version of the candidate itself - divergence measured as strength, apparent gains of a thousand rating points that survive no external reference. Searched the fleet for a comparison scored against a prior version of the same system and found none: the conformance pipeline's `reconciledToSha` is a staleness marker rather than a scored comparison, and nothing else in the seven trees keeps a self-play or self-referential score. Recorded as unapplied rather than forced into a simulation with invented cases. Return condition: when a project scores itself against a prior version of itself - most likely the first time a managed tree gains a self-improvement loop with a progress metric, which is the exact setup the amendment exists to catch |
| 2026-08-31 | toolchain-floor-drift | supply-chain | personas | experiment | better | `ab-paired`, read-only, no project tree edited (the operator's pick named no project, so Phase 8 step 2 confirmation was absent). Measurable: the gap between the **declared** minimum toolchain and the **effective** floor, being the max declared minimum across the resolved graph. Arm A (the pipeline as it stands): 11 jobs across 6 workflow files select the toolchain with `@stable`, no job pins the floor, no toolchain file in the tree - answer **green, floor never observed**. Arm B (`cargo metadata --format-version 1 --offline`, max over `packages[].rust_version`): declared **1.80.0**, effective **1.88.0**, **60 of 518** packages declaring a minimum are above the claim, the floor set by a transitive date-time crate and its two companions at **patch-level** version - exactly the mechanism the technique names. Eight minor versions is the same order as the ~2-year window the technique cites, which is one tree agreeing with one survey, not a confirmed constant. Structural fact the tree could not have been built to give: the **same repository declares two floors and observes one** - a runtime floor of >=20 with a job that runs at 20, and a compiler floor of 1.80 with every job on stable. Nobody designed that asymmetry; naming a runtime version is the ordinary way to write the job, pinning a compiler version is an extra argument to an action whose default reads as obviously correct. Honest bound: arm B reads *declared* minimums, a lower bound - the true floor can only be higher. Return condition: one job pinned to the declared floor doing a resolved (not locked) check; that is the repair and it was not run here, so nothing was compiled in either arm |
| 2026-08-31 | generator-bounds-the-space | test-input-generation | personas | experiment | better | `ab-paired`, read-only. Applied to a genuinely careful property suite - >=1,000 cases per invariant, 10,000 on a bake, composed generators whose comments state and justify their own constraints - which is the strongest place to ask the technique's question rather than the weakest. Measurable: dimensions the suite reports on vs dimensions it can reach. Arm A (the suite as it stands): green, and structurally unable to report any untested dimension. Arm B (step 1 of the technique - read the generator adversarially, enumerate what it cannot produce, check each against the consuming code): **9 pinned dimensions in ~10 minutes**, 3 of which resolve to code the suite never reaches. The decisive one: `invariants.rs` asserts a violation when a clip's computed source end exceeds its media duration, and the generator pins `media_duration` to 60 while drawing trim 0-5 and duration 0.25-10, so **that violation branch is unreachable across every case in every bake** - the check returns the same answer ten thousand times for a reason unrelated to the compiler being correct. Also pinned: `trim_end` hardcoded 0.0 in a struct that declares the field, transcript fields always None, audio always one source path, loudness measurements correlated with the normalize flag by an explicit comment. Two of the constraints are *documented* choices, which is what makes the case valuable rather than embarrassing - defensible decisions, invisible from the result, and never re-checked when the compiler grows a branch. Honest bound: found by reading, not measurement; nothing counts how *often* a state occurs. Return condition: the step-4 reachability assertions, which the tree emits no counters for - installing them turns this from structural into measured |
| 2026-08-31 | two-clock-records | audit-logging | politicas | simulation | better | `structural-only`; three real cases from the tree walked under both policies. A **confirming** tree, which is why it is the interesting one: this project ingests public records from external publishers, so every fact is learned strictly after it happened, and its authors reached the two-clock design independently - `valid_from`/`valid_to` for world time, `recorded_at`/`superseded_at` for record time, half-open history spans documented at the supersede instant, enumerated writers, no primary key on history because one claim key legitimately has many versions. The structural fact, readable from the DDL without opening a writer: **enforcement splits along which clock the system owns** - the record clock is `not null default now()`, the world clock is nullable and, by the migration's own comment, *'Writers do not populate these yet.'* The clock available for free at the write site is mandatory; the clock that must be parsed out of a source document is optional and empty. A tree that designed *for* two clocks is stronger evidence for that asymmetry than a single-clock tree, which would only show that people forget. It also avoids the unrecoverable failure - unknown world time stored as NULL rather than defaulted to the record time, `unknown-is-not-a-value` in the wild - which is why a backfill is still possible. Cases: a recorded vote (B strictly better; A silently answers a different question), a supersede (A sufficient for the accountability question, insufficient for the analytical one - the case showing the clocks serve different readers), and the migration backfill (**no difference** - the useful negative: adopting the second clock does not repair history, which is the argument for adopting it early). Falsifier stated: if the sources publish only a retrieval date, `valid_from` cannot be populated from them and the split is a property of the sources, not the write path. Return condition: `count(*) where valid_from is not null` against a populated database, which this run did not open |
| 2026-08-31 | inherited-default-override | settings | goat | code | better | **Shipped, `facc957`** (not pushed). Seam: the embed widget's config carries a four-value appearance setting with a distinct `auto` meaning follow-the-viewer, resolved **server-side in two files** to a hardcoded dark constant - one of them documenting the gap in a comment (*could check prefers-color-scheme on client*) rather than closing it. Resolving an inherited default where the source is invisible does not pick a default, it **erases the subscription**. Both arms are real code extracted from their own revisions (HEAD vs working tree), n=2 viewer environments: **1/2 -> 2/2** served the palette their environment asks for. Instrument self-asserted, and its assertions double as regression checks - arms byte-identical on explicit light/dark, and an explicit pin does not move when the viewer's preference changes. typecheck 29 errors before and after with **0 in the changed file**; eslint clean. **Structural fact**: the three-state *model* survived perfectly and the subscription died at the *resolution point*, in two files by two paths - shape and semantics were maintained by different hands and only shape reached the renderer, which is evidence that preserving the model protects nothing on its own. **The apply step amended the technique**: the second resolution point was deliberately left broken, because its consumers render one resolved palette with no media-query block, so returning light there would be a regression - yielding a clause the desk-written technique lacked, *defer a resolution only to a consumer that can perform it*, and where two resolution points share a downstream shape the consumers change first. Return condition: the sibling resolver is repaired once its callers can resolve; and if the embed becomes size-budgeted, price the second palette |
| 2026-08-31 | fallback-retirement-condition | optional-dependency-degradation | personas | experiment | better | Census of capability-conditional fallbacks for **closing** gaps (Worker, IntersectionObserver, ResizeObserver), deliberately excluding `typeof window/document` server guards, which are a *stable* absence belonging to the subject's original lane - the exclusion is stated so it can be disagreed with. Across **8 seams / 4 capabilities / 7 files**: branch tests the capability rather than a proxy **8/8**, check present **8/8**, branch emitted **0/8**. The delta is the third claim alone. Instrument self-asserted twice: a non-zero population is required (a zero would mean a broken pattern, not a clean tree) and the emission probe must fire elsewhere in the tree (it does). **Structural fact, the best result of the run**: the check is load-bearing and the emission is not, so conformance tracks **consequence, not diligence** - omitting the check crashes on the runtime that lacks the capability, so every author writes it correctly and none uses a version/UA proxy; omitting the signal behaves identically everywhere and fails no test, so nobody writes it. Result: 8 correct automatic reapers whose firing nobody can observe, and therefore 8 paths that cannot be deleted on evidence. Ship 0, and the reason is scope rather than permission: the repair is 8 emission sites across 7 files, more than a reviewer reads in one diff. Return condition: once emission exists, read the distribution - a share that will not fall means the check drifted, a share at zero means delete the path |
| 2026-08-31 | preference-short-circuits-measurement (correction) | adaptive-fidelity-tiers | goat + personas | simulation | better | Three real cases, testing the correction's **boundary** rather than its mechanism (the mechanism is exercised by the `inherited-default-override` row above, which is the same rule from the other side). Case 1, the goat embed appearance config - a configuration surface set once via parameters, with no immediate-feedback expectation: the carve-out fires and **three states are correct there**, so the correction declines. Case 2, personas reduced-motion - honored read-only across 24 components with **no override control at all**: the rule predicts two states plus the divergence rule if one is ever added, and predicts nothing else. Case 3, personas theming - fully app-owned custom themes with no upstream source: the rule correctly does **not** apply. In 2 of 3 real cases the rule declines to fire, which is what a well-bounded rule should do and is what was being tested. Falsifier stated per case (e.g. if the environment setting contributed a *third* distinct behaviour rather than selecting between two, three states would be required). Return condition: when any managed project grows an ambient two-state control over an inherited default, the mechanism becomes directly testable there rather than by proxy |
| 2026-08-31 | plural-policy-claims | client-fetch-cache | goat | code | better | **Shipped, `d4995c3`** (not pushed). `ab-paired`. Measurable: keys whose *declared* per-site policy differs from what the shared entry *resolves* to. Arm A — each call site's `staleTime` read as its author reads it; Arm B — the entry's actual resolution (believability existentially by the shortest live claim, retention by maximum). **65 fetch registrations over 50 distinct keys; 7 keys registered from more than one site; 4 divergent; 2 numerically so — 2.0x and 3.0x.** Both divergences share one shape: **a prefetcher claims longer freshness than the component that consumes the data, so its stated warmth is inert** — the entry revalidates on the consumer's shorter clock while the prefetch still pays for the request. The largest shared key (7 sites) is *consistent*, which is what keeps the result honest: divergence is a minority condition a census finds and a reviewer cannot. **Structural fact nobody designed**: the 2.0x case is a **vocabulary collision at the alias layer** — two modules each define a local duration table under the same name, binding the label `SHORT` to different underlying constants (30s vs 60s). The shared constants module is correct and singular; the collision is in two private re-namings no gate compares, which is `one-authority-per-vocabulary` failing one level below where it is usually checked. That is stronger evidence than a careless call site would have been, because the divergence arrived through a mechanism a reviewer would not think to check. **The instrument lied twice and was caught by hand both times** — a flat constant table let `GC_TIME_MS.STANDARD` (600000) overwrite `CACHE_TTL_MS.STANDARD` (300000) and reported a 2.0x spread that did not exist; a 900-char lookahead bled into the next call and invented a divergence between two different keys. The harness now asserts its parse before printing and refused to print once, correctly. Honest bound: this reads *declared* policy, not co-residency — a key whose two claimants never mount together is divergent on paper and harmless in fact, and nothing here separates those. **Shipped**: both local alias tables deleted so the shared constants are the single authority, and the two prefetch claims aligned to what the cache already resolved — **shared keys diverging on lifetime 1 -> 0**, measured by running the census against both revisions with the constant tables asserted in each arm (registration and key counts identical at 65/50, so the change removed a disagreement rather than a call site). Gates: typecheck 29 before and after with 0 in the changed files, `eslint src` 0 errors, the project ratchet matching on all 27 buckets. **Correction to the count above**: a strict explicit-vs-explicit comparison finds **one** divergent key (2.0x), not two; the second (3.0x) is an explicit value against the resolved client default. Both are real divergences in resolved lifetime and only one is visible to a reviewer reading two call sites, which is worth keeping straight. Return condition: a counter at the resolution point recording how many observers were attached at refetch and the spread of their claims (the observer list is exposed; nothing reads it); cheaper first step is a dev-time warning on two explicit lifetimes for one key, which would have caught both cases at write time |
| 2026-08-31 | suspension-is-not-failure | retry-backoff | goat | code | better | **Shipped, `d4995c3`** (not pushed). `ab-paired`. **The good case for the technique's hard half and the bad case for its easy half**, which is why it is worth the row. The tree already gets the asymmetry right by configuration — the first attempt may proceed regardless of connectivity while a *retry* additionally requires the environment to be able to call, so an offline device suspends instead of burning its ladder — and its error vocabulary already mints a distinct offline code separate from a connection the server refused. Both halves of the attribution rule are present. **What is missing is the wire between them.** Arm A — the shipped retry predicate: lower-case `error.message`, refuse on a substring `401|403|404`, else retry to the cap. Arm B — the same decision from the typed fields every error already carries (code, category, status). Run over **all 39 error codes the codebase defines**, using each code's own registered description as the message Arm A actually receives: **Arm A correctly refuses 0 of the 17 codes that must never be retried.** The reason is structural, not marginal — the strings it scans are user-facing copy (*"Sign in to save your rankings…"*, *"This feature requires additional permissions."*) because the message field holds a human description while the status lives in a separate typed field the predicate never reads. **The rule cannot fire** (`gate-sees-target`): every auth failure, permission denial, not-found and validation error is retried to the cap on a ladder reaching 30s. **Structural fact**: the classification the retry decision needs *already exists one module away and is never consulted* — a team that had never classified its errors would have identical retry behaviour. That is the useful negative for the technique: spelling suspension differently in the error vocabulary buys nothing until the retry path reads the spelling, and the failure is invisible because both arms are green. Honest bound: this measures *reachability*, not cost — nothing in the tree records retries by error class, so the wasted-request count is unmeasured; and no arm took a device offline. **Shipped**: the predicate now reads the `category` every error already carries, with the offline code deliberately excluded (the request was never made, so it is a suspension, and the network mode already pauses the ladder there); the message-substring path is kept beneath it for errors thrown outside the hierarchy. Both arms re-run by **extracting each revision’s own predicate and executing it** rather than reimplementing either: **0/20 -> 20/20** permanent codes refused, **0 false positives in either arm**, plus four regression checks that travel with the change (offline not permanent, 5xx still retries, budget still caps, untyped 403 still refused via the fallback). Gates: typecheck 29 before and after with 0 in the changed file, eslint 0 errors, ratchet unchanged. Return condition, still open: count retries by class and outcome so `suspended` is distinguishable from `exhausted` in something an operator reads — the classifier can now fire, but whether it saves anything is unmeasured |
| 2026-08-31 | observed-read-subscription | client-state | goat | code | better | **Shipped, `d4995c3`** (not pushed). `structural-only` on the subscription cost, which the ship did NOT repair — the verdict is `better` on the instrument — both arms read, not run, and the row says so. The stack takes the **inferred** form of subscription narrowing by default (the fetch layer hands each consumer a recording wrapper and wakes it only for fields it read). The question is not whether that default is good — it is — but whether the two things inference needs are present. **Neither is.** The explicit declaration (`notifyOnChangeProps`) appears **nowhere in the tree**, and the lint plugin that ships with the fetch layer is **not installed**: zero references in the package manifest and zero in the lint config. So the project relies on observation at 100% of its call sites with no instrument that can see the defeat. One site defeats it: a shared wrapper hook returns `{ ...query, page, pageSize, … }`, spreading the tracked result during render to merge pagination helpers in — which touches every field and subscribes every downstream consumer to everything, including fields that change on each fetch regardless of the data. **Structural fact**: the defeat is in a **wrapper, not a leaf**, which is precisely the propagation the technique predicts — the defect travels, and the call sites cannot be audited for it because the problem is not in them. **Blast radius measured honestly at 2 consumers**, so the finding is not a large rendering cost today; it is that the project adopted an optimization with a silent failure mode, put the one known defeat in the position that propagates it, and installed none of the three things that would report it. Honest bound: no render counts were taken, so the cost is unquantified, and a field-by-field copy would defeat tracking invisibly to the syntax search used here. **Shipped**: plugin installed, six rules promoted to **error at 0** following the severity policy this project already documents (which requires measuring a rule at zero before promotion). It found **three** real violations, not the one predicted — the spread, plus **two `no-unstable-deps`** this run never looked for: a memoized callback depending on two mutation objects, which are not referentially stable, so it rebuilt on every mutation state change along with anything memoized on it. **The census found one site by hunting one idiom; the linter found a class the census was not searching for**, which is the argument for the instrument over the audit. Both populations fixed, so the rules refuse the next instance. Gates: typecheck 29 before and after with 0 in the changed files, eslint 0 errors, ratchet unchanged. **Correction, and the better finding**: the flagged spread is on an object that is ALREADY untracked — the paginated hook spreads its sibling’s return value, and the sibling has already mapped the tracked result to a plain literal. The real cost is that mapping: it reads **thirteen fields** to build the declared interface, so the wrapper subscribes on its consumers’ behalf to the whole surface including the high-churn ones. Nothing was done wrong — wrapping a data layer behind a stable typed interface is good practice, the loss is an unavoidable side effect of describing a surface, and **no linter will ever flag it** because reading a field you named is what that code exists to do. **The apply step amended the technique**, which gained the section "A wrapper that normalizes the result destroys the observation" and its three ways out. Return condition, still open: convert the wrapper surface to lazy getters and count renders under both arms — a public-interface change, outside what this run was authorized to do |
| 2026-08-31 | mutating-local-gates | machine-paced-delivery | pumper | experiment | better | `structural-only`, read-only, confirming tree — and **the seam corrected the technique twice, which is the row's real value**. The project's sole turn-end gate mutates nothing and, by its own header, *"Nothing here ever blocks"* — and its first executable statement is still a re-entry guard. Why: its exit 2 feeds a reminder to the model, the model acts, acting ends another turn, the turn end re-fires the hook. So the termination contract is created by the gate's output **re-entering the agent's loop**, not by blocking (draft 2) and not by mutation (draft 1). The technique now carries a three-tier ladder — advisory / blocking / mutating — and names the advisory tier as the one most likely to ship unguarded, because it looks harmless. Second, independent corroboration from the same file, for the liveness paragraph: the gate **detected nothing for its entire life** — replayed over all 31 recorded transcripts, **1,136 Edit/Write calls, zero detections** — because its turn-boundary scan stopped at the first user-role entry and **3,837 of 4,187** user-role entries are tool results. It ran green every turn and was byte-identical to a working gate. Structural fact: the repair independently reaches `gate-liveness`'s three-state contract (0 pass / 2 model-actionable / 3 could-not-run, addressed to the operator because *"the model cannot"* fix an instrument), written by a single maintainer who had been burned by exactly that silence — corroboration for the standard rather than an application of it. Honest bound: nothing here exercises the blocking or mutating tiers, the snapshot rule or the patch-as-reason rule; this tree has no fixing gate. The re-entry guard is present but has never been observed firing. Ship 0 — the gate already implements the rule. Return condition: counters on the re-entry and could-not-run paths, both currently unobservable, which is the same failure this row is about one level up |
| 2026-08-31 | suppression-hygiene (self-retiring-form amendment) | dead-code | personas | code | better | `ab-paired`, and the cleanest number this run produced. The amendment claims that where an instrument offers an *assert-the-finding-is-expected* form beside a *permit-it* form, the first is mandatory because only it fails when the cause lapses. This workspace uses the permanent form almost exclusively: **6 self-retiring against 829 permanent** (644 unused-item, 182 lint directives, 3 type-checker) — **0.7%**. Scoped to one crate with no foreign WIP, 32 files, **71** unused-item sites, both arms the same checker over the same sources one minute apart with only the suppression *form* changed: **arm A (permit) 0 warnings — the crate reports clean; arm B (assert) 63 warnings, all "this lint expectation is unfulfilled", across 24 of the 32 files.** **63 of 71 (89%)** guarded code that is no longer unused: correct when written, silently stale since, carried green the whole time. The 8 survivors are load-bearing and stay suppressed under both forms. Non-uniform concentration (9 in one provider module, 7 each in two others, 6 in a queue) — the signature of a suppression copied along a file as it grew. **A cheap grep-shaped proxy** (does the guarded item name appear elsewhere) estimated **123/156 = 79%** tree-wide, within ten points of the compiler and conservative by construction, which removes the usual reason this cleanup never starts: the migration decision does not need the paired build first. **Structural fact nobody designed**: the 829:6 ratio was never decided — the permanent form is older and shorter, each of 835 authors reached for it correctly *at the moment they wrote it*, and the form's cost is invisible at the site, to review, and to the instrument, appearing only in aggregate years later. Honest bounds: one lint, one language, where the assertion form is a first-class feature; counts sites not defects; the reaper-form ranking (dates vs version thresholds) is untested here — the tree uses neither. Experiment reverted in full, tree verified byte-identical. **SHIPPED as a slice, `ba7f613e`** (personas/master, not pushed), after the operator confirmed the lane and set the size constraint. One file, 7 sites, **+2/-7**. Chosen as the *most defensible* suppressions in the tree rather than the worst: all seven carry written reasons and the reasons are true about the product (*'planned API - no Tauri command wires into lenient parsing yet'*) and false about the lint. **The slice forced a correction to the finding**: this crate is a **library**, so the unused-item lint cannot fire on a reachable `pub` item, and **42 of the 71 sites sit on `pub` items** - inert from the moment they were typed, not stale-since-written. The two `pub` entry points here make the whole five-helper private chain below them reachable too, so a carefully documented suppression chain could never have fired at all. So 'stale' has two causes with the same repair and different preventability: `pub`-in-a-library is checkable when the line is written, aging-out is not. This strengthens rather than weakens the amendment - the permit form yields one observable (silence) across three states (suppressing a real finding, one that went away, one that was never possible) and the assertion form separates all three in one compile. Verified by removal, not argument: arm A (attributes present) 0 warnings; arm B (removed) 0 warnings across lib **and all test targets** - a strictly broader target set, which only strengthens the negative. Remaining: 64 sites in this crate, 835 workspace-wide, and the 8 load-bearing ones are untouched - converting those is the change that alters what the build reports, and it is the hard third. Return condition: migrate crate by crate; on the second pass watch whether the load-bearing count stays near 8 — a rising floor means the form is being used to silence rather than to assert |
| 2026-08-31 | build-measurement (vantage-axis amendment) | build-economics | personas | simulation | unmeasurable | Three real cases from the Rust workspace, walked under policy A (the subject's existing scenario axes: which build, which variant, which machine, when) and policy B (the added **vantage** axis — for consumer-visible cost, measure from a consumer). **Case 1, the producer/consumer boundary that already exists**: an engine crate consumed by the application crate. A predicts "the engine check takes 1m19s warm"; B asks what an engine change costs the consumer's rebuild, which is the number governing the inner loop, since nobody edits the engine and stops. Falsifier stated: if the consumer's rebuild after an engine change is dominated by the consumer's own code rather than the engine's interface, the producer-side number is an adequate proxy and the axis buys nothing here. **Case 2, measured rather than reasoned, and honest about testing an adjacent claim**: this run's own two arms are the same crate, same machine, same hour, both "warm" — **1m19s vs 5m33s, 4.2x**, from a change touching only attributes in 32 files. The existing scenario label covered both numbers identically, so the case supports the amendment's *predicate* half and does **not** test its vantage half. **Case 3, the correct decline**: the application binary is a leaf, nothing consumes it, and measuring it from a consumer's vantage is meaningless — a well-bounded rule should decline somewhere, and it does. Verdict is `unmeasurable` rather than `better` because the amendment's core claim — that consumer vantage resolves a delta producer vantage drowns — was reasoned, not run. **Instrument named**, as the vocabulary requires: a minimal crate depending on the engine, timed after an interface change against a body-only change, which separates the two costs the single producer-side number currently fuses. Cheap, and nothing in the fleet has it |
| 2026-08-31 | stake-before-merit | machine-paced-delivery | - | unapplied | - | **No seam in any managed project.** The technique governs arrival at a review gate generated by a population the gate does not control, and its predicate is a property of the submitter rather than the diff. All seven managed projects are single-owner with no outward-facing contribution surface: no project accepts external pull requests, none carries a contributing guide addressed to non-owners, and the one gate that exists is the operator reviewing their own agents' output — which is the *self-generated* arrival `human-gate-capacity` already covers, and the exact case this technique carves out of. Recorded as unapplied rather than forced into a simulation with invented contributors, which would be an opinion with a table around it. Return condition: when a managed project accepts a contribution from someone who does not own it — most likely the first time one of these trees is opened or a second contributor joins the fleet, which is also when the policy has to exist *before* the first submission rather than after |
| 2026-08-31 | consolidation (validated citation is not verified) | agent-memory | personas | experiment | better | Read-only A/B on the live workspace knowledge store, arms paired over one population: the 74 evidence rows whose `refs` resolve to a file in the tree. **Arm A (referential integrity, what the pipeline runs): 74 of 74 pass.** **Arm B (the read-back the amendment prescribes): 14 pass, 60 fail.** The check separates 60 rows arm A cannot distinguish from the other 14. Load-bearing structural fact: the shipped schema already carries a `verified_at` column on that table and **0 of 104 rows have ever had it set** - the store was built with a place to record the read-back and the check has never run. Honest bound on the 60: they are unconfirmable against current source, which collapses paraphrase-rather-than-reproduction with drift-since-recording; the measurement cannot separate them, and that is itself the finding - the read-back is a write-time instrument whose value decays, so a store that defers it turns a checkable claim into an unresolvable one at no visible moment. Return condition: bind the quote by value at write time and the check becomes a set lookup rather than a substring match, removing this application's own ambiguity. |
| 2026-08-31 | consolidation (reachability outranks description) | agent-memory | personas | experiment | better | Same tree, same pass, second arm. **693 memory nodes against 14 edges: 20 nodes (2.9%) participate in any relation, so 673 are reachable from exactly one place.** The partition also barely partitions - of 29 contexts one holds 555 of the 693, and 12 are singletons. Nobody designed this: it falls out of writing nodes with a `context_id` and adding the edge table afterwards, which is the ordinary way a store acquires single-home assignment. Confirms the amendment's claim that description quality cannot compensate, since a title is only read once the reader is already in the right neighbourhood. Return condition: when the project adds a second-home rule (file an item into its runner-up context when the margin is thin), re-run this count as the paired after-arm. |
| 2026-08-31 | 13 further wave-3 amendments (agent-memory pipeline, backbone, absence, routing) | agent-memory / judgment-guardbands / measurement-honesty / model-routing / llm-forensic-gating | - | - | unapplied | Landed 15 amendments this pass; two are applied above and thirteen are not, for one structural reason stated once rather than thirteen times: they are rules about **how a number or a derived artifact is produced** - a ladder's rungs, a tolerance band, a routing-grade summary, a compiled lane's staleness trigger, a coverage instrument's blind spot, a claim-binding split - and the fleet has no project that produces the artifact each governs. No managed project runs a memory comparison, a structural extractor, a tolerant comparator, a compiled navigation surface, or a per-claim citation regime. This is the second consecutive run reporting apply 0 for this reason; see the LESSONS redesign proposal for a `re-analysis` mode whose arms are two readings of one published protocol, which is the only honest A/B this class admits. |
| 2026-08-31 | declaration-invariants | dependency-declaration | personas | experiment | better | Scored a real plugin declaration mechanism on the three invariants. **10 unit directories, 3 central declaration surfaces, 0 of them owned by any unit.** Locality FAILS definitionally - a plugin author cannot state that their plugin exists; someone must edit three unrelated central files on their behalf, which is the technique's own diagnostic (*who has to edit a file when a unit is added*) answered in its worst form. Composability FAILS as a consequence: adding a unit is not a local act, so concurrent authors collide on the registries rather than on the plugins. Scalability HOLDS - only direct units are listed, no transitive closure - and it matters that one invariant passed, because three-for-three would be less credible rather than more. Arm B (each unit ships its own declaration, host discovers): 0 central edits, 10/10 self-declaring, drift structurally impossible. Instrument self-asserted: surfaces readable, id matcher must fire. **Structural fact**: the three surfaces give three *different* answers to which plugins exist and nothing states which is authoritative - no file anywhere enumerates the units, and the nearest definitive list is a directory listing no code consults. 0/10 units appear in all three, 3/10 in none. **Caveat carried into the application rather than buried**: the three surfaces have legitimately different populations, so the raw count is NOT a defect count - the finding is the unresolved disagreement, not the absences. Had locality held, all three would be derived views over one authority and could not disagree. Ship 0: the repair is an architectural change to the plugin mechanism, far beyond a reviewable diff. Return condition: if a plugin manifest is introduced, re-run - the disagreement should go to zero by construction rather than by reconciliation |
| 2026-08-31 | logical-name-or-address, progressive-resolution, shortcut-is-not-the-substrate, declaration-cost-floor, vendored-copy-loses-composition | dependency-declaration | - | - | unapplied | Five of the new subject's six techniques, deliberately not tested this run, with the reason stated per technique rather than as one shrug. `logical-name-or-address` and `progressive-resolution` need a project that *operates* a resolution layer; every managed project consumes one supplied by its toolchain, so the seam is in the toolchain rather than in any tree the fleet owns. `declaration-cost-floor` is measured on an ecosystem, not on a repository - its predicate (what it costs to add the first dependency to an empty project) is answered identically by all seven projects because they share one toolchain, so a per-project row would report the toolchain's number seven times and call it corroboration. `vendored-copy-loses-composition` is checkable statically and was scoped out on budget, not on reachability - it is the strongest candidate for the next `/intake apply` pass. `shortcut-is-not-the-substrate` needs a mechanism with exactly one binding and a refused second context; the fleet plausibly has one, and finding it is a search rather than a measurement. Return condition: `vendored-copy-loses-composition` is directly testable today against any tree's dependency artifacts; the other four need either a project that owns a resolver or an ecosystem-level comparison |
| 2026-08-31 | windowing-vs-identifying-keys | async-ui-states | goat | experiment | unmeasurable | Read-only axis census over two managed React trees - 5,998 files, 380 multi-key object literals - for the shape the technique predicts: a compound query key spanning both classes with no declared classification. **The instrument asserted its own parse and refused to print on the first run** (`a: b || undefined` was yielding `undefined` as a key), which is the sibling's morning lesson paying for itself one run later. **2 candidates in one project, 0 in the other; 1 survived hand-verification** - the other was an options-destructuring pattern, not a key. Confirmed seam: one `useMemo` hands a **seven-field params bag** whole to the key factory - 3 identifying (category, subcategory, search term), 4 windowing (sort field, sort direction, page, page size), nothing marking the split. Against the technique's five consumers: **no retention mechanism of any kind** (no previous-data placeholder, no deferred windowing coordinate), so a *sort* change blanks the grid exactly as a new search does - the forbidden `SETTLED-DATA -> LOADING` edge taken on the windowing axis; the **search-term setter is the raw state setter and does not reset the page**, so the one-directional rule is backwards on the half that matters (the page setters correctly touch only the page); and the surface's loading flag is the disjunction of two regions' flags, so an items-axis change blanks the neighbour too. **Verdict `unmeasurable` rather than `better`, stated plainly**: pagination is off by default with no caller enabling it, and the sort setters exist in a context type with no shipped control calling them - both windowing coordinates sit in the key and are unreachable from the UI, so no gate can see a difference no user can trigger. **Structural fact nobody designed**: the mixed-axis key was built *before any control could exercise it* - the bag is what arrives naturally when a params object is assembled from everything the fetch needs, and the classification is the thing someone has to decide to add. That is the best argument for the technique's central instruction, since the moment the key is defined is the only moment all its components are in one place. **Honest bound**: the census matches field *names* against two vocabularies, so it counts literals, not defects; the zero in the second project is a zero for this instrument, not an absence of the shape. **Ship 0 for a third reason the scorecard's binary does not cover** - not confirmation, not size, but that the verdict never reached `better`. Return condition / instrument named: a rendering test that mounts the surface, changes one windowing coordinate and asserts the content region is not replaced, plus its twin on the identifying axis asserting that it is |
| 2026-08-31 | probe-without-write-back (amendment: the under-count that is not conservative) | agent-memory | personas | experiment | better | **Both arms run on one tree with one instrument (call-site enumeration), which is what makes this a paired result rather than an assertion.** Arm A - the reader-side audit the technique already specifies, whose stated default is that a new machine caller is suppressed until it argues its way in - enumerated four production read sites and returned **0 findings**; all four are consistent with that default. Arm B - the amendment's writer-side audit - found one writer (as the tree's own contract says) with **three** callers rather than the one the contract names, and returned **1 finding**: a projection surface that selects through the same tiered call and renders into a file the consumer "prepends to every turn and re-reads on /compact", never incrementing. Arm A structurally cannot reach it, because a projection job IS a machine caller and suppression IS its correct default; only the writer-side question (did material cross into a consumer's context?) separates a delivery from a warmer. **Honest scope: the defect is staged, not live** - the surface is env-gated with no production caller, so nothing is miscounted today; it sits in the path its own header plans to make the sole injection route, at which point the store's usage signal goes to zero while delivery continues. **The apply step paid back into the technique**: the prepared-run cache reads without incrementing and its consumer increments later, which is correct, and a naive writer-side audit condemns that deferral - so the technique now carries "trace the selection to its point of no return, count once at the consumer". Shipped a comment-only slice correcting the contract's caller enumeration; the behavioural fix did NOT ship and the blocker is neither authorization nor size but **indeterminacy** - a re-read file makes "one delivery" not obviously one increment. Return condition: when the projection gains a production caller, the boundary becomes determinate and the increment is a two-line change with a countable before/after |
| 2026-08-31 | archive-restore-semantics (amendment: the flag's two populations) | entity-lifecycle | - | - | unmeasurable | **Unapplied, and the fleet sweep is the finding.** No managed project carries the seam in either half: one keeps deletions in a separate tombstone table (so the key is genuinely released) and writes explicit SQL with no per-entity delete override to bypass; another already uses partial unique indexes. Two independently built trees, neither of which discussed this question, both landed on the released-key branch the amendment recommends - which is corroboration for the default and is precisely why there is nothing to test here. Recorded `unmeasurable` rather than simulated, because a simulation with no seam would be invented cases with a table around them. **Instrument named**: a schema audit flagging any unique index over a table carrying an existence flag where the index has no partial predicate - it would have found the source's defect in one pass. Return condition: when a managed project grows a same-row existence flag under a non-partial unique constraint, or an ORM-style per-entity delete override with a bulk path beside it |
| 2026-08-31 | seed-is-not-a-reproduction | test-input-generation | personas | experiment | better | `ab-paired`, 50 fixed seeds, read-only. The repair under test is one this corpus had **already recommended in writing** for unrelated reasons - a prior application found the video generator pins `trim_end` to 0.0, making a media-duration invariant unreachable, and prescribed drawing it instead. Arms run the shipped strategy's exact draw order, ranges and pinned fields. **Control: same seed + same generator = 50/50 identical.** **Arm A' (same seeds, one added draw appended): 50/50 DIFFERENT** - not drift, replacement: only the fields drawn before the insertion point survive, and in the first pair the three clips changed source file, start time and every downstream value. Structural rather than probabilistic; it would be 50/50 for any seed. **The unexpected half is arm B**, included as a control on the assumption that a serialized structure obviously round-trips: with default settings **38 of 50** persisted inputs returned differing by one ULP on a float field. Verified at bit level - the writer emits the correct shortest round-tripping text, the language's own standard parser returns the original bits, the JSON library's default decoder returns one ULP off. Its documented opt-in for exact decoding moved the same measurement to **50/50**, so the default is a deliberate documented speed/exactness trade that a project inherits without deciding. The technique gained a section from this: persisting the derived input is necessary and NOT sufficient, and the round trip must be asserted rather than assumed. Honest bound: strategies were reproduced faithfully rather than linked (the project binary was locked by a running instance), so this tests the seed-to-draw-sequence mapping and not the project's compiler; and for THIS suite's 1e-6 frame-alignment tolerance a ~4e-16 error changes no verdict - which is a fact about its assertions, not about the practice. Return condition: **ship blocked, and by neither of the two blockers the focus names** - not size (the change is ~40 lines) and not confirmation (the operator authorized the tree); the build could not relink because a running app held the binary. Re-run and commit when the app is closed |
| 2026-08-31 | liveness-needs-a-quiet-period | test-input-generation | - | unapplied | - | **No seam in any managed project.** The technique needs a suite that injects faults and can freeze them; a fleet-wide probe found no fault-injecting simulator, no chaos lane and no availability contract naming a healthy subset in any of the seven trees. Recorded unapplied rather than forced into a simulation with invented cases - a system with no progress obligation has nothing for the quiet phase to assert. Return condition: when a managed project grows a fault-injecting or chaos lane, or states an availability contract that names which subset must be able to finish. The nearest candidate today is a retry/repair loop, which has the resonance shape the technique targets but no fault schedule to freeze |
| 2026-08-31 | model-based-oracle (recorded-expected-output amendment) | test-input-generation | - | unapplied | - | **No seam in any managed project, measured.** The amendment adds a fourth oracle rung - a recorded expected output, reviewed once and committed. A fleet probe for recorded-output assertions across four trees returned **0 files**: no inline-snapshot, no snapshot-file, no golden-file assertion anywhere. Unapplied by absence rather than by judgement, which is itself the interesting datum - the fleet's oracles are entirely invariant-and-example based, so the rung this amendment describes is not merely unused but has never been reached for. Return condition: when a project adopts any recorded-output assertion, at which point the amendment's five update-path disciplines are directly checkable against it (is updating opt-in, is review a version-control diff, are volatile fields marked ignorable, is the recording adjacent and self-locating, is anything written on a passing run) |
| 2026-08-31 | valid-but-degraded-plans | gravity | experiment | better | **`ab-paired` over the shipped fixture, product code unchanged.** The seam is a findings graph whose door validator is unusually strong on the *correctness* class - every reference checked against a universe of ids, then again against a tighter universe to catch right-shape/wrong-kind mis-wiring, and an absent link treated as a defect after six unlinked records once shipped under a green gate. Every one of those checks asks *does this edge resolve*; not one asks whether an edge that resolves is **needed**, which is the technique's split drawn exactly. **The structural fact nobody designed:** severity is computed as `declared - missing`, and one class of declared dependency has no cascade path, so it can never appear in `missing` while still counting in the denominator. Arms: A = shipped (`declared - missing`), B = same with never-failable edges partitioned out. Worst case run per node, all failable support removed. **A reports the severe verdict for 6 of 11 nodes, B for 11 of 11 - 5 of 11 mis-stated**, every differing node carrying exactly one inert edge, which is the predicted signature. Consequential rather than internal: the surface escalates on the severe count alone, and the same comparison is duplicated in a second file. **Ship 0, and by two of the four blocker classes, both correct outcomes rather than failures** - *confirmation* (the triage pick named no project; a cross-repo edit needs its own authorisation) and *indeterminacy* (two defensible fixes: partition inert edges out of the denominator, or give the explanatory nodes the evidence edges the code was clearly built to carry - its own comment says that union "wounds nothing TODAY", reading as a capability built ahead of its data; nothing in the tree settles which, and shipping either would decide it silently). Return condition: one answer from the owner on whether explanatory nodes are meant to cite evidence in future data - that selects the fix. Separately, the technique's shape measurements (width, depth, edge decisiveness) need a run history this surface does not keep; it stores the current state, not a series, so "an edge never once decisive" is not computable here yet |
| 2026-08-31 | co-published-numbers-must-reconcile | measurement-honesty | goat | code | better | `ab-paired` with a **ground-truth arm**, shipped as `4788b2c` (not pushed). Seam from the project's own `.ai/registry-map.json`, which already joined a collection-panel context to this subject. A completion percentage was computed with a numerator counted over the whole grid and a denominator that is a page size - handed in under the comment "use page size as proxy for grid size" - and clamped at 100%. **The clamp was the tell**: the store's own statistic needs none, because there both terms are counted over the same population. A/B re-run on the project's own test runner over the 120 reachable states (capacities 5-50, page sizes 20 and 50, three page-fill states), each arm scored against the ratio the grid store computes for the same inputs: **arm A 66/120 (55%) percentages wrong, 21 `isComplete` wrong, 9 clamped; arm B 0 on all three.** A five-slot grid with one placed item showed 2% against the store's 20%, and the stats component renders that figure green at >=100, so the clamp was surfacing a false *complete* badge. Shipped: the hook now reads the grid's real capacity from the store it was **already subscribed to**, the computation is extracted so the invariant is pinned by 6 new tests rather than trusted, and the clamp is replaced by a dev warning that **names the pair** instead of hiding it. Gate: typecheck 29 before and after with 0 in these files; suite 275 -> 281; eslint 0 errors and 3 warnings both sides. **A prediction was falsified and the correction is the durable half**: the clamp fires in only 9 of 101 flagged states and never in the shipped configuration, so the defect is the denominator and removing the clamp alone would have changed nothing while looking like a fix. **Structural fact**: the correct denominator was on the object the consumer already held - the hook reads placed ids off a `state` that also carries the real capacity *and* the already-correct percentage, and reached past both. Return condition: **the repair needed a second source of intent** - reconciliation proves something is wrong, never what is right; here the code's own "proxy" comment supplied it, and had the two readings been equally supported the honest outcome was to report the pair and ship nothing. Open: the store patches its matched count incrementally while the full recomputation sits unused in the same file - `derivation-names-recomputation` with the arbiter present but unwired |
| 2026-08-31 | locality-and-leverage (cost-that-neither-payoff-prices amendment) | module-design | personas | experiment | better | **`ab-paired`, two instruments over one Rust workspace, read-only, no product code changed.** The amendment claims a boundary that successfully hides its module's *existence* is paid for in re-implementation, and — the falsifiable half — that this technique's own scatter diagnostic is structurally blind to it, because independent copies produce single-place edits rather than scattered ones. Arm A = change scatter over full history (20,761 files); arm B = one function name defined in >=2 files and >=2 top-level regions with >=1 definition not `pub`. 1,200 `.rs` files, 16,199 definitions. **232 candidates, 165 seen by arm A, 67 invisible to it by construction.** **The first instrument design was refuted by hand-verification and that is the durable half**: arm B's top-ranked hits — six `validate_*` functions and `is_private_ip` — are not duplication at all but private adapters forwarding to the public implementation (`validate_check(pv::validate_name(name))`; `super::url_safety::is_private_ip(*ip)`), which is the single-door discipline `seams-and-adapters` prescribes. Name collision plus visibility is not a detector; the corrected predicate excludes a body calling a function of its own name, dropping 15 of 67. **52 survive**, and three opened by hand split along the amendment's own discriminator: `strip_html_tags` three times — `pub` in `core/src/validation/mod.rs:18`, an identical private copy in `db/src/repos/core/memories.rs:23`, and a hand-rolled `<`/`>` state machine in `engine/src/chunker.rs:357` that decodes no entities and truncates on `>` inside an attribute (waste); `now_ms` three byte-identical bodies, one `pub` (waste); `hours_since` two genuinely different jobs, different signatures and return types (correct divergence, governed by the wrong-abstraction rule the file already had). The discriminator sorting them is the evidence the amendment is usable rather than merely true. Honest bounds: it reads names not behaviour, so same-thing-different-name is invisible and 52 is a floor; the waste/divergence ratio rests on 3 of 52 hand-read; and `pub` is a proxy for findable that a public function in an unpublished internal crate would defeat. **Ship 0, blocker class `confirmation`** — the operator's pick named a candidate, not a project, and Phase 8 step 2 gates the tree write. A secondary **indeterminacy** sits on the chunker copy alone: the weaker stripper may be deliberate, since the sanitizer library in a chunking hot path is a real cost and changing strip semantics moves chunk boundaries and therefore embeddings; the db-layer copy carries no such question, being byte-identical to the public one. Return condition: operator authorization for the tree, and one answer on whether the chunker's stripper is intentionally cheaper — that selects whether one fix or two |
| 2026-08-31 | excess-indicts-the-instrument | quality-gates | goat | experiment | not-better | **The A/B refuted the technique this same run landed, and the technique shipped with the correction in it.** Seam: a committed ratchet baseline freezing 230 unreachable files out of 982 walked (23.4%), measured 2026-08-24 - eleven and a half times the source tool's own self-suspicion threshold of 20, and nothing asked. Arm A = the 230 are debt (current practice, baselined as such). Arm B = bucket by directory, compute each bucket's rate against its own file count, compare to the 23.4% base rate. Re-measurement reproduced the baseline exactly (230/1329/735), so the arms disagree about interpretation and not about the number. Arm B separated the population sharply and by its own rules worked: the single largest contributor sits at **0.9x** (52/259, below base rate - textbook distributed debt) while 7 buckets fire at **2.4x-4.3x**, several at 100% directory saturation. Then every flagged cluster was checked for outside referrers: **all 7 were genuine dead code, 0 were root or resolution errors.** The confound is structural, not a measurement error - **dead code arrives in whole features**, so no-root-declares-this and this-island-is-dead predict the same distribution, and clustering cannot choose between hypotheses making identical predictions. A checker shipping the test as written would have accused the configuration 7 times in one run and been wrong 7 times. The technique keeps clustering as a **sampler** and gains a mandatory second stage - a referrer check per cluster, where only a live rooted importer whose imported files the checker still calls unreachable indicts the instrument. Incidental structural fact: one fully-unreachable 8-file component's only live reference is a **doc comment in a live component recommending it**; nobody designed that, and it warns that a reference found by text search is not a reference. Ship 0 and none owed (not-better). Return condition: a tree whose large finding population is genuinely produced by a wrong root set - the third branch has never been observed and the technique would be stronger for one sighting |
| 2026-08-31 | precision-trades-have-a-direction | codebase-scanning | goat | experiment | better | **ab-paired, 12 exports, project tree never modified.** The strong instrument was unavailable and that is the finding: the project adopted the scanner on 2026-08-24 already at v6 - the version that replaced a typechecker-backed backend with a parser having no type layer, making matching name-based - so its 1329-unused-export baseline was founded **entirely on the post-swap instrument**. There is no pre-swap arm anywhere in its history and the recall loss is unrecoverable from inside the project. This is the technique's nearly-unaffordable-afterwards observed in a tree that did nothing wrong. Fell back to the seeded construct corpus the technique prescribes, built in scratch and run against the exact binary the project consumes. The checklist was **not invented** - the tool's own maintainer-facing doc enumerates the scopes its hand-written shadow detection must register and flags two as ordering-sensitive (parameters and loop bindings are bound before the body whose range a naive implementation keys on); that enumeration IS the test matrix, which is the technique's structural claim made operational. Arms: 1 control (dead export, unique name) + 11 dead exports each shadowed by a different construct - block const, function param, arrow param, catch binding, for-of, for-in, object pattern, array pattern, rest pattern, defaulted destructuring, nested function declaration. **Result: 11 of 11 detected, 0 false negatives.** The documented recall class is real as history and **closed in 6.32.2** - the compensating hand-written scope analysis the technique predicts was written and is complete against its own enumeration. `better` not because a defect was found (none was) but because the prescribed instrument converted a property the project could not measure into a measured negative in ~10 minutes, with a fixture that re-runs on the next upgrade; the alternative was not a smaller number but no number at all. Structural fact: the checklist exists only in a maintainer-facing file - the release notes carry the speedup table and neither they nor the user docs mention the recall class, so a consumer adopting at v6 cannot learn it exists. **SHIPPED 2026-08-31** (same session, operator authorised after the verdict - the blocker class was **confirmation** and one sentence cleared it). Landed as a dependency-free script wired to its own task, generating the fixture in a temp dir rather than in the tree, because a committed fixture of deliberately-dead exports would be walked by the project's own scan and inflate the baseline it protects. **Proven red before trusted green on all three exit paths**: seeded recall loss (one dead export made genuinely imported) exits 1 and names the construct; absent control or absent scanner exits 2. Two controls gate every verdict - the live export must be absent from the report and the unique-named dead export present - because a reporter shape change would otherwise yield an empty set reading as "everything was found", the exact false green the probe exists to prevent. Project ratchet re-run after the change: 3 buckets, 2294 findings, every bucket matching. Not pushed; the operator pushes |
| 2026-08-31 | depth-bounds-and-shed (reject-by-class check-order amendment) | admission-queue | personas | code | better | **`ab-paired`, the engine's own test binary, both arms run minutes apart.** The tree declares three priority levels — the top one documented as healing retries, chain triggers and manual re-runs, i.e. the class that arrives when the system is already in trouble — and a bounded per-origin line ten deep. The depth verdict returns **before the arrival's class is ever derived**, so the class reaches only the insertion search that decides position. This registry's own evidence overlay had recorded the arrangement as *bounded depth with refuse-newest shed*, filed as a policy someone selected from the three this technique offers; it was the policy that falls out of the check order. **The intersection was untested on both sides, and in the source that produced the amendment too** — the depth test fills the bound with uniform entries, the priority tests mix levels at the default depth and never approach the bound, and 0 of 34 cases constructed a queue that was bounded *and* prioritized. Arm A: an urgent arrival at a bound held by bulk work is refused while every bulk entry keeps its position — **33 passed, 1 failed**. Arm B: the class evaluated before the depth verdict, a strict outranking displacing the tail (lowest-ranked *and* newest among its equals, so no separate scan) — **34 passed, 0 failed**, including a case asserting an equal-ranked arrival is still refused, so displacement is not a bypass. The durable half is the split of effort: the comparison was four lines and **revoking the displaced promise was the rest of the change** — the concrete instance of the technique's claim that a design which only sorts has no displacement rule, because sorting makes the easy half look like the whole job. Reusing the existing cancel-while-queued terminal record rather than minting a displacement state is what kept it small. **Ship 0, blocker class `environment`.** The change spans two crates; the library crate compiles and ran both arms, the caller crate's build script fails on this checkout for a missing plugin permission — **pre-existing, reproduced identically with the change stashed**. Shipping the tested half alone would leave the other crate failing on a field it does not yet know about, so both halves stay uncommitted. Return condition: the operator enables that permission, then one build and one commit carries both. Honest bound: the bound here is per-origin, so displacement arbitrates one origin's own line; displacing across origins is a fairness decision this arm does not answer |
| 2026-08-31 | portable-read-definitions | client-fetch-cache | goat | code | better | **`ab-paired`, same read-only census over both arms, 982 files.** Seam: the key vocabulary is centralized but the pairing of key with fetcher and default lifetime is retyped at every consuming site. Arm A 45 declaration sites / 40 questions / 3 declared more than once; arm B (shipped) 42 / 40 / 2. **The totals moved less than the finding did, and hand-verification is why**: of arm A's three, one was real and shipped, one was real and out of slice, one was an artifact of normalizing a spread-prefix key. A fourth - the most serious - is invisible to the instrument entirely because its declarations live in a declarative array rather than a call, and it was found by hand. Shipped: one question declared at four sites with three different lifetimes (300000 twice, a defaulted parameter, 60000) collapsed to one declaration, all four resolved lifetimes unchanged; typecheck 29 errors before and after (all pre-existing), lint clean on touched files. **Ship blocker was hit twice in one row and the two were different classes** - `confirmation` first (the triage pick named no project; asked, authorized), then `size` (making it compile required narrowing two pre-existing pass-through option bags, past the authorized slice; asked again, narrowed, shipped). Structural facts, none designed: the prefetch lane had already invented a standalone `{key, fetcher, staleTime}` descriptor because it could not call a hook, and never connected it to the reading sites - so the tree holds portable declarations for warming and component-bound ones for reading, and every divergence sits on that seam; that descriptor types its payload `Promise<unknown>`, which is the widening argument for a factory per question over a container for all questions; and the compiler refused the configurable wrapper outright. Corrected on verification: a second flagged question is a **maintenance hazard, not a defect** - both sites call the same fetcher, the keys correctly mirror their arguments, and the consumer the prefetch targets passes exactly what it hard-codes. Left alone deliberately. Return condition: the two-API-clients key (second sighting, banked as a lead by the lifetime-census run reaching it from the other side) is the row to pick next, ahead of anything this source proposed |
| 2026-08-31 | post-landing-repair-density | delivery-analytics | tracklight | code | better | **`ab-paired`, same read-only `git log` instrument over both arms, two windows, product code untouched.** Seam: a project that **has never reverted anything** - 266 commits, 0 reverts, 61 fix commits - which is precisely the population `revert-linkage` names in its own *when not to use this* ("a team that reverts readily and a team that fixes forward differ in policy, not in reliability") and cannot read. Arm A = revert-linkage: **0 events** at `v0.0.7` (58 commits after) and **0** at `v0.0.4` (217 after). Not a low number, a structurally absent one - nothing to link, nothing to rank, no risk information at all. Arm B = repair density by feature unit after a declared instant: **17 units** at `v0.0.7`, **30** at `v0.0.4`, ranked worst-class-first then by repairs over surface touched. Arm B produces a ranking where arm A produces nothing, which is the technique's central claim, and it holds. **The severity classing was refuted by hand-verification and that is the durable half**: of the four units the harness promoted to the top, **three were false positives** - `security(supply-chain): secret scanning, dependabot, and the h2 advisory` *adds* tooling rather than repairing a defect; `fix(mcp): ...a panic guard...` was promoted on the word "panic" while *adding* a guard; `fix(tracing): crash-surviving span breadcrumbs` was promoted on "crash" while crash-survival is the feature. Only `security(pii): bound the scrub's walk` survived as a genuine repair. **First number six alarming repairs, hand-verified one, possibly two** - and the error ran toward alarm, so the classifier nominates the teams that write most carefully about hardening their own code. The technique now carries that limitation in its own text: count and density are mechanical, class is not, and the ranking is opened before it is shown to anyone. Honest bounds: feature identity is proxied by conventional-commit **scope**, which is not what the technique asks for (a feature is the commits that landed it) - a scope splits one feature and merges unrelated work sharing a directory, and a repair committed without a scope or folded into an unrelated change is invisible entirely; the harness also cannot see exposure, so a scope with no repairs may be healthy or unused and `git log` does not separate those. **Ship 0, blocker class `confirmation`** - the operator's pick named two candidate rows, not a project, and Phase 8 step 2 gates the tree write. Naming the queue behind it, per the declared focus: clearing confirmation exposes a second blocker the first one hid - **what would ship is not product code but the harness itself**, and shipping it as measured would ship a known-alarming instrument, so the smallest honest slice is the harness with the severity column removed or marked unverified. **SHIPPED 2026-08-31** (same session; the operator lifted `confirmation` and chose the project over this registry's `scripts/`, which cleared `artifact-choice` in the same sentence). Landed as `scripts/repair-density.sh` + a CONTRIBUTING section, commits `685da11` and `a3d5ebd`, **not pushed**. **The severity column was removed before it shipped** - that is the A/B applied rather than a scope concession: what ships prints counts and density and then prints the repair *subjects* for the top units, so the classification the keyword matcher got wrong 3 times in 4 is done by the person who has to act on it. Two choices follow the same rule: it prints the **revert count first**, so `reverts 0` sits above the ranking and the instrument argues for itself in its own output, and it prints both blind spots on every run (unit is a proxy; 4 of 59 in-range commits carry no scope). **Proven red before trusted green on all three refusal paths** - unresolvable instant, empty range, no tag to default to, each exit 2 with a named reason. Gate: core 117 ok, `manifest_guard` 7 ok, `gate_table_guard` 2 ok; `catchup_marker_guard` red on `.ai/registry-map.json` absent from the agent marker - **pre-existing from `4070e17`, reproduced identically with the change stashed**, left untouched and reported. Seam recorded in the project's own `.ai/applied.jsonl` (created this run, 2 rows); `.jsonl` is outside the agent family's declared extensions so it does not widen that red. Return condition: the next release tag passes, and the ranking is read against what actually needed fixing - plus whether scope survives as a feature proxy as the workspace grows |
| 2026-08-31 | reclassification-is-not-repair | error-handling | tracklight | experiment | not-better | **`structural-only`; the fork arm ran statically over the real tree, the detector arm could not.** Seam: a closed `ErrorCode` enum over HTTP in `crates/api/src/error.rs` carrying an internal member documented as "an unexpected server-side failure (store, serialization, I/O). HTTP 500." Enumerating every construction site of that class across the workspace returns **three**, all outside the error module - two task-join errors (`events.rs:364`, `state.rs:163`) and one failure encoding the server's own batch response (`otlp/mod.rs:158`). Put each through the technique's separating question - can a user reach this through a documented interface with values it accepts? - and **3 of 3 answer no**: declaration was right, caller is the bug. Nothing to reclassify, so applying the technique changes no code. **The tree had already taken the *other* branch too, deliberately**: `ErrorCode::Unsupported` exists as a separate member with a comment saying why - "distinct from `internal` so a client (or an operator reading logs) can tell a permanent capability gap ... from a transient outage" - which is the "declaration was wrong" repair, made without the technique existing. And the same reasoning appears once more in the parsing direction: commit `e7fcc7c` replaced an `unwrap_or_default()` in `parse_enum` because "a value the vocabulary did not know was silently coerced into the type's default and handed downstream as if it were a member", drawing the fork per vocabulary - "`Provider::Unknown` and `Operation::Other` are explicit quarantine variants ... Unknown is a value those two vocabularies deliberately have; `status` and `redaction` do not." Its stated reason is this technique's: a corrupt `status` coerced to `Success` reports a failed call as a successful one, and "none of those look wrong in a dashboard." **`not-better` is the correct row and it earned an amendment**: a project whose internal class is already narrow, deliberately split, and constructed only where no accepted input reaches has nothing for the fork to find, and the technique's value there is the detector rather than the reclassification discipline - now stated in its *when not to use this*. **The detector half is genuinely absent and unusually cheap**: the only assertions naming the class are two unit tests of the error type itself (that it stringifies to `"internal"` and serializes into the envelope), **zero** tests assert it never fires across the suite, and no suite-wide sweep exists - so the expensive prerequisite, a stable wire string with a test pinning it, is already built and the sweep is the whole instrument. Ship 0 and **none owed** (not-better). Return condition: build the suite-wide response sweep for the internal wire string and re-run; until that instrument exists the detector claim is asserted and unmeasured, and only the fork half was tested |
| 2026-08-31 | oracle-before-gate | personas | simulation | better | Three real cases from the review surface's own decision functions, walked under A (the queue as built) and B (a verifiability term admitted). **The census, hand-verified by reading every function**: three decision functions (`triageValueScore`, `withinLevelRanges`, `hasLevelFilter`) carrying three distinct terms - impact, effort, risk - all properties of consequence; six sort keys, four of them identity/metadata and two resolving back to the same three terms. **Zero verifiability terms in any ordering, scoring or filtering path.** Meanwhile the row type carries `verifyState`, the store populates it, the backend exposes a writer that also takes evidence, and two surfaces render it as a chip - four references, two plumbing, two pixels, **none a decision**. Nobody designed that; it fell out of building the consequence model first and the verification model second with nothing forcing them to meet. Verdict rests on case 3 (the pending count split by oracle presence), which needs no new data - the column exists and the split is a groupBy. Cases 1 and 2 carry stated falsifiers that depend on how populated the column is. Return condition: **query the stored findings grouped by `verify_state`** - if it is null everywhere the finding is about an unfinished feature rather than a queue ignoring what it knows, and that single count moves cases 1 and 2 from argued to measured |
| 2026-08-31 | before-after-outcome-pairing (induced-scope amendment) | personas | experiment | not-better | The amendment's discriminator run against two real before/after instruments; both returned *coverage change*, so the existing step 5 already governed and the amendment changed no verdict. **Instrument 1**, a generated context-to-subject conformance map, two committed revisions eight days apart: 764 -> 821 pairs, **245 added and 188 removed against a net of +57** - churn four times the net, only 70% of the after side present on both. The discriminator resolves it cleanly: **the context count did not move at all (208 -> 208)**, so no new unit of work appeared and the additions are the matcher's output moving, not the world's. Cuts the other way too: the source tree grew **4,746 -> 5,881 files (+24%)** over the same period, so real induced scope existed in abundance and the map's units did not track it - a reader reasoning 'the tree grew, so new units are new work' reaches the right answer by the wrong route and is wrong on these numbers. **Instrument 2**, a lint ratchet: it enumerates three causes for a *drop* and guards the population with `minFilesWalked`, but has **no ceiling** - a rise gets no such enumeration though it has three causes too, the third being a grown denominator. Real asymmetry, zero cost here: 990 files on 2026-06-01, 996 now. Seam class named so it is not re-tested: **an instrument whose unit population is derived by a matcher rather than enumerated from the tree cannot exhibit induced scope**, however fast the tree grows, because its units are its own output. Return condition: re-run against an instrument whose units are counted tree artifacts, on a tree that is actually growing |
| 2026-08-31 | locality-and-leverage (cost-that-neither-payoff-prices amendment) | module-design | personas | code | better | **Escalated from the `experiment` row above once the operator authorized the tree; the `confirmation` blocker named there was the only thing holding it.** One of the two waste cases corrected: a private `strip_html_tags` in a repository module replaced by the public `personas_core` one it was logic-identical to. **The tree made the case stronger than the census did** - three other files in the SAME crate already called the shared function (a chat repo, a teams repo at two sites, a migration), so one module of four had rebuilt what its siblings imported. **And the asymmetry nobody designed: the copy carried all 11 assertions and the original carried none** - the public module had no test block at all, so the engineer who could not find the shared function rebuilt it *and* tested the rebuild, while the implementation four call sites across two crates depend on went unguarded. That states the cost better than the duplication does. Change accordingly is not a deletion: import replaces the copy, the 11 assertions move to sit beside the implementation they guard, the entity-ordering invariant the copy documented and the original lacked is preserved into it, and the sanitizer dependency - whose only use in that crate was the copy - is dropped from the manifest and lockfile. **Arms on the project's own suite, same inputs both sides: 13 failures before, the same 13 by name after**, all pre-existing and none HTML-related; pass count 866 -> 865 in the db crate and +1 in core, which is exactly the moved test and not a lost one; `cargo check --all-targets` clean, no new warnings. Committed `16a4a978c` on the default branch with a pathspec, **not pushed**. **The third copy was deliberately left**: the chunker's hand-rolled stripper decodes no entities and truncates on `>` inside an attribute, and whether that is a considered trade for a hot path is unanswered by the tree - changing it moves chunk boundaries and therefore embeddings. Return condition: one answer from the owner on whether the chunker's stripper is intentionally cheaper; that selects dedupe or a documented exception. Note the blocker taxonomy earned a data point - `confirmation` was the whole of it, and once lifted the change took one session with no size, indeterminacy or environment obstacle on this case |
| 2026-08-31 | instrument-exposure-control | measurement-honesty | tracklight | experiment | better | **`structural-only`; the census arm ran read-only over the whole tree, no behavioural arm was runnable without live model credentials.** Seam: an LLM-as-judge scoring and benchmark service — a project whose entire business is asserting that two numbers are comparable, which makes it the sharpest available test of state 7. **Two numbers, because the first is not the measurement**: a grep for comparability/reproducibility vocabulary across `crates/` and `docs/` returns **88 matches in 44 files**; hand-classifying every one, **8** are cross-*run* comparability claims and **80** are within-run determinism. Of those 8, the number conditioning on whether the models under test have been exposed to the cases is **0**. Second probe closes it: **zero** occurrences of contamination, memorization, training data or model knowledge cutoff anywhere in the tree — every `cutoff` is data *retention*, every `leak` is PII or dev-mode. **The tree is unusually careful about the axis it does model**: a three-value `Determinism` lattice (exact/best-effort/sampled) folded to its *weakest* member on the stated ground that "a set of runs is only as reproducible as its least reproducible run", contract-vs-convention distinguished, an exactly-reproducible dimension deliberately excluded from agreement statistics so it cannot drag them toward 1.0. Every axis in that lattice describes the harness's own stochasticity and none describes the subject's exposure. Arms: **A** = frozen dataset ⇒ runs comparable (asserted at three sites); **B** = frozen *and* exposure constant ⇒ comparable. They diverge on exactly one population — a frozen dataset whose `source` is `import`, replayed months apart against successive model generations. **The structural fact nobody designed**: `Dataset.source` already partitions `events:recent` from `import` and `DatasetItem` carries `source_event_id` beside an `anonymization` audit — built for privacy, and it is precisely the partition state 7 needs, because traffic sampled from this installation cannot be in any model's training material. The project's privacy design is its contamination defence and nothing in the tree said so. **Shipped** `de0a85f` on the default branch with a pathspec, not pushed: docs-only correction at the three overstated sites, no type or behaviour change, `cargo check --workspace` clean — which is what made it a one-commit slice rather than a schema migration. Honest bound: the stack now *states* the limit where it asserted a guarantee; that is a disclosure improvement, **not** a detection capability. Return condition: a twin-dataset relation plus a run comparison that reads the gap rather than the levels — the service has datasets, targets and a runner and could run the paired probe, but nothing in the data model expresses "these two datasets are twins differing only in exposure" |
| 2026-08-31 | false-positive-economics (author-dismissal amendment) | quality-gates | pumper | experiment | unmeasurable | **`structural-only`; the amendment's second precondition is countable, so it was counted rather than asserted.** Seam: a governed taxonomy registry that several downstream apps key their records on, fed by an agentic proposer that reads scraped material — a gate over machine-generated content, which is exactly the case the amendment carves out. **Two numbers**: a grep for sites setting the registry's `enabled` flag true returns **12**; hand-verifying each, all twelve are compile-time seed entries, unit-test fixtures, or unrelated configuration defaults in other subsystems. **Production paths by which a machine-written proposal becomes live without a human: 0.** The gate is fail-closed at the reader — a record must opt in with an explicit enable, absent-or-false means not live, and a record whose provenance field is missing defaults to *proposed* rather than *approved*. The proposer writes the record with the flag false and emits, in its own result payload, a next-step instruction for a human to review and flip it; it has no path to flip it itself. So step 1 of the death spiral has no one to run through, and the proposer's precision is a **cost** property (reviewer-minutes × proposal volume against the recall of a proposer too timid to suggest a real missing category), not a survival property. **The fail-closed default was written for governance, not precision** — the guarding comment says so — and the precision economics fall out of it for free, which is the structural half worth recording. `unmeasurable` is the honest verdict: the tree already implements arm B, so there is no code change and no behavioural difference to observe, and dressing a confirmation as an improvement would be the dishonest alternative. **Instrument that would make it measurable, named as the method requires**: per-proposal review-outcome telemetry — proposals emitted, accepted and rejected per proposer run, plus reviewer time per accepted entry. The service records new and changed counts and **no acceptance outcome**, so the denominator that would let anyone read the proposer's precision against the base rate of useful proposals does not exist, and no threshold on it can be defended. Scope bound: this is one governed registry with one proposer; the same tree runs approval flows elsewhere whose promotion predicate permits promotion from a validated state without an `accepted` argument, a different shape not examined. Corroborates only the amendment's *second* case (removing the author's power to close removes the engine), never the first, because this gate has no measured precision at all. Ship 0, none owed — no change was warranted |
| 2026-08-31 | the-tree-is-not-the-population | codebase-scanning | personas | code | better | Seam: a dependency-free content-security-policy gate that walks the frontend tree, keeps files by a two-extension allowlist, prunes two directory names, and closes with a line naming fetch targets and hosts and **no population**. Arms over the same root in the same session: **A** = the gate's traversal verbatim; **B** = enumerate first, then classify every removal by the filter responsible. **5,033 examined of 5,955 enumerated - 922 excluded (15.5%) by one undisclosed extension filter.** Neither arm changes which files are examined; that is the point - the claim is that "all allowed" over a silently reduced population is unsizable, and 15.5% is the size. **The falsifier returned negative, which is the more useful half**: scanning all 922 excluded files with a matcher deliberately broader than the gate's own returned **184 raw, and 0 after hand-classifying every one** - 183 are data files whose translated strings legitimately contain URLs, one is a design document, and **no code-bearing file is excluded**. The standing lesson held: an instrument's first number is not its measurement, and reporting 184 as a near-miss count would have been wrong in the direction that flattered the finding. **The structural fact nobody designed**: the gate is sound, but sound *because* every executable file under that root currently happens to carry one of the two admitted extensions - an invariant the project satisfies and does not state, and the 15.5% is an accident of layout (locale catalogs, generated indexes and design notes sharing the directory with source) that will change without anybody deciding it should. **SHIPPED** `1ee1dd43a` on the default branch with a pathspec, **not pushed** - escalated from `experiment` to `code` once the operator authorized the tree; `confirmation` was the whole of the blocker and lifting it cost one session. 24 lines added / 3 removed in one file: a removal tally keyed by the filter responsible, incremented at the two points the walk already discards, plus one clause on the closing line. **Arms through the gate's own npm entry point, same tree, same session: identical verdict, identical 2 targets / 2 hosts, identical exit code** - the denominator is the entire delta, and it reproduces the read-only probe's numbers exactly, which is the check that the probe had reproduced the traversal faithfully rather than approximating it. Project linter clean on the changed file; pre-commit hooks passed. **The shipped instrument reported something on its first run that the probe never thought to ask**: the pruned-directory counter reads **0** - the walk guards against two vendored/build directory names and under this root that guard has never fired, because neither directory exists there. Not wrong, but a reader of the old output would have assumed it was doing work. That is the technique's own last section (a published exclusion set starts carrying information the scan did not set out to produce) arriving on the first run. Return condition retired for the disclosure half; what remains is the invariant itself - the first plain-JavaScript / single-file-component / inline-script file with a network call landing under that root, which the gate will now make visible instead of absorbing |
| 2026-08-31 | stake-before-merit (channel-split amendment) | machine-paced-delivery | personas | simulation | better | **structural-only; no experiment is reachable and saying why matters** - the amendment's subject is what an outside contributor does in a review conversation, and this public repository has had **no outside contributors**: all 8,294 commits and all 17 pull requests are the owner's own. Inventing contributors would be an opinion with a table around it, so three real cases from the tree decided it. **Case 1, the discriminator**: the contribution guide requires a contributor adding a user-facing string to write translator context (what the string labels, space constraints) *in the commit message* because the catalog format carries no comments - and enforces the **translation** half with two pre-commit hooks while the **context** half is unenforceable by construction, because its value is entirely in whether a person who understood the string wrote it. Policy A (authorship of the text is irrelevant) gives that rule no support at all; policy B makes it an instance of the standard. **The project independently invented the amendment's rule for one field, could not generalize it, and left it stated and unargued.** **Case 2**: the same guide mandates translation into thirteen locales, which an individual contributor realistically produces with a machine - so an unqualified restriction on generated text would forbid what the guide requires, and only the channel split resolves it (the locale value is a mandated artifact judged by hooks that do not care who wrote it; the translator context is the reviewer's only evidence a person understood the string). Opposite verdicts on two halves of one commit, which is the work the distinction does. **Case 3, the control**: pull requests #14 / #15 / #16 are real machine-generated batches (one reports 109 findings across 11 waves), merged, and legitimate - **both** policies admit them, which is the check that the amendment has not become the anti-tooling position the parent technique warns against. Surface audited: a 1,198-word contribution guide, a code of conduct, a pull-request template, issue templates, a code-owners file, a security path, a published response expectation - and **zero** mentions of AI, agents, models or generated content. The category is absent, not restricted. Ship 0: the operator's triage answer named no project. Return condition: **the first outside pull request**, which is the only thing that makes a behavioural arm exist |
| 2026-08-31 | human-gate-capacity (size-driven rubber stamp amendment) | machine-paced-delivery | personas | experiment | better | **First application this technique has ever had** - the subject note recorded on 2026-08-28 that its measures "have not been taken on any connected tree". Read-only harness over git history; no product code changed, nothing written to the tree. Population 7,773 non-merge changes over 195 days, **39.8 changes/day**, which is the subject's own thesis arriving as a fact. Measurable: does the size dimension surface a population the item-counting measures report as one healthy number? **Arm A** (the four measures) yields a single repair figure, 62.9% - and two of its four measures do not exist in git history at all, there being no review timestamps, which is itself a finding about what a tree without recorded review events can support. **Arm B** by changed lines: 81.3% repair above the large threshold against 57.4% below, a 1.42x separation. **The control refuted it, and that is the row's value.** Post-merge repair tracks *files touched* on its own and almost perfectly monotonically - 34.0% (1 file, n=2648) / 50.3% (2-3) / 66.0% (4-8) / 73.9% (9-20) / 92.2% (21+, n=1066) - while holding files touched fixed collapses the line gradient and **inverts it in the single-file band** (small 35.2% n=2341, medium 24.6% n=272, large 29.0% n=31). Larger changes touch more files, so a line-based size measure reproduces the files-touched result and reads as if it had confirmed itself; only holding the other candidate fixed shows it. Verdict `better` on the **dimension** (34% to 92% is a population no item count surfaces) with the **unit corrected**: the owning `batch-size-thresholds` table names the unit as an open choice and defaults to changed lines, and in this tree that default carries almost none of the signal it appears to carry. Technique gained a condition paragraph and a decision rule - state the unit, validate it against the other candidate once. **Bound stated rather than smoothed**: the measured population has no human merge gate (changes land directly), so post-merge repair stands in for an unrendered verdict with no way to check that it does - the premise is corroborated, the mechanism is unobserved. Predicate is also near-saturated at 40 changes/day on overlapping files, so only differences between strata are load-bearing, never the absolute rates. Ship 0, none owed - the finding is a registry-side measure, and no project change was warranted. Return condition: re-run against a tree with recorded review events (approval timestamps, per-change verdicts) so dwell and backlog age exist and the rubber stamp is observed rather than inferred |
| 2026-08-31 | tuning-corpus-disjointness | measurement-honesty | personas | experiment | better | **`ab-paired`; both arms run against verbatim copies of the project's real pattern array and the eval's real predicate, no product code changed and nothing written to the tree.** Seam: a shared prompt-injection sanitiser that all untrusted text passes through before being embedded in a model prompt — 13 regex families — graded by a golden eval over a 9-payload fixture corpus. **The corpus is a transcription of the artifact it grades**: every one of the 9 fixture labels is the name of a pattern family (`role-override`, `ignore-previous`, `disregard-prior`, `section-delimiter`, `xml-role-tag`, `heading-override`, `bypass-safety`, `you-are-now`, `zero-width`), one payload per rule. **Arm A** (shipped corpus): scored 9/9, 8 of 9 actually modified. **Arm B** (same shape classes, phrasings the module's own header names as defeating a phrase blocklist — a synonym for the banned verb, a spacing variant of the delimiter, a differently-named structural tag, a rephrased override, an unlisted zero-width codepoint): scored **11/11 — and 9 of the 11 passed through the sanitiser completely unmodified.** A perfect score on payloads the sanitiser never touched, because the *predicate* was written from the same pattern list and cannot fail on an input the list ignores. **The apply refuted the technique it was testing**: the draft covered the fitted corpus and not the fitted predicate; a section was added before commit, carrying the one-line check (can the predicate fail on an input the artifact ignores?). **The tree already holds the neighbouring discipline and it did not help**: an assertion that the pattern list has at least 10 entries, commented "a truncated blocklist is a silent regression" — a vacuity guard on the *artifact*, correctly reasoned, while the corpus and the question went unguarded. The two failures look nothing alike from inside. **Honest bound**: n=9 and n=11 under one sanitiser; it says the eval's score carries no information about unlisted phrasings, not how exposed the application is, which depends on what the downstream model does with an unsanitised override line and no deterministic check here can see. Ship 0 (blocked: confirmation) — the repair is small and belongs to the owners (payloads outside the pattern list, plus the predicate that the sanitiser altered its input at all), and the triage answer named no project. Return condition: retired the moment the eval carries an untouched-rate beside its neutralised rate |
| 2026-08-31 | suppression-hygiene (third rot axis amendment) | dead-code | gravity | experiment | better | **`ab-paired`; same linter, same pinned rule set, two roots, read-only.** Seam: a lint debt ratchet — per-rule warning counts committed to a baseline the gate refuses to mismatch in either direction — whose baseline file opens with its own predicate, including that the population is "the whole repository **minus that config's `ignores` block**". That subtraction is the seam and nothing sizes it. **Arm A** (shipped config, the graded population): **372 files**. **Arm B** (the population the agent-worktree ignore entry removes, measured by running the same linter at the worktree's own root): **305 files, 19 warnings.** One entry removes 45% of the 677 files the arms cover between them, and neither number appears in any output the project produces. **The entry is correct** — it matches, so stale-match cannot fire; its justification holds exactly as written, so no reaper fires; its delegation note is honest, because a foreign checkout is genuinely not this checkout's debt. Every clause of the technique passes while coverage sits at a little over half what the predicate implies. **The tree had already paid for this once**, and its own config comment is the evidence: a second checkout doubled every bucket (608 walked, 309 of them foreign), the ratchet went red, and "what it could NOT say is that the rise was a second copy of the same debt rather than new debt, so the gate read as 'you added fourteen hook violations' and pointed at a refactor nobody needed to do." Reach was never a number, so a reach change and a debt change arrive through one channel wearing the same clothes. **Structural fact nobody designed**: this entry's reach is a function of how many agent worktrees exist that day — one is present now, two would move the subtraction by hundreds of files with no commit touching config, baseline or source, and the ratchet would stay green throughout. **Honest bound**: one reading on one day with one worktree present; it sizes the subtraction and cannot show it has ever silently moved, because no historical series exists — which is the complaint. Arms are also not perfectly matched (arm B's discovery starts from a directory rather than being carved out of the parent walk), close enough to size and not to reconcile to the byte. Ship 0 (blocked: confirmation) — the change is a population line in the baseline the ratchet already commits and compares, and the triage answer named no project. Return condition: retired when the baseline carries files-walked and files-removed per ignore entry |
| 2026-08-31 | model-based-oracle (second-arm amendment) | test-input-generation | tracklight | simulation | better | **`before-after`; three real cases from the tree's own history, no invented cases and no code changed.** Seam: a storage trait with three implementations held to one 1,900-line shared conformance suite, each backend running it from its own integration test. **Policy A** = the single-arm run (the embedded store's test — fast, deterministic, needs no infrastructure, the arm a team keeps when the others are inconvenient). **Policy B** = as shipped, all three. **Case 1 is the discriminator and its commit message states the mechanism**: usage caps were enforced by the trait's default check-then-insert — list rules, read usage, insert, three round trips with no lock spanning them — and **the embedded store overrode that default** with a single locked connection while the other two did not; on the backend carrying production traffic a concurrent burst admitted **8/8 against a cap of 4**, exactly 3 after the fix, every run. Policy A is green there permanently and *correctly*, because the arm it runs has an override making the race unreachable — a single arm is not merely silent, it is actively reassuring. **Case 2**: the trace surface worked on the embedded store and answered "not implemented" on the hosted one until implemented there; policy A cannot distinguish "implemented" from "implemented in the arm I run". **Case 3**: a distinct unsupported-capability error introduced so capability gaps "stop masquerading as data" — a gap is only visible when two implementations are asked the same question. **Falsifier checked, not assumed**: had any case been reachable from the embedded arm alone it would not count, and case 1's commit states outright why it was not. **A wrong first hypothesis, corrected by one command**: I expected the non-embedded arms to be env-gated out of CI (their tests do skip on an unset env var); the workflow stands up ephemeral infrastructure and marks two arms required. **Negative structural fact**: this tree has no designated reference implementation at all — its authority is a written suite, the configuration the technique calls correct — so the amendment's ledger clause separating "our port diverged" from "the reference is wrong" has nothing to attach to. The mechanism transferred; the vocabulary did not. **Honest bound**: historical, so before/after rather than a control; the counterfactual rests on each commit's own account of why the embedded arm was green — explicit for case 1, inferred from the change's shape for 2 and 3. **Ship 0 (declined: no change warranted)** — this tree already implements the remedy, which is why it could validate it. Return condition: a managed project acquiring a designated reference implementation, where the ledger-column half becomes testable |
| 2026-08-31 | oracle-before-gate (descend-an-altitude amendment) | hitl-approval | personas | experiment | better | **`ab-paired`; both arms over the same 13 catalogs at the same instant, read-only harness, no product code changed.** Seam: a localization gate stack of 13 locales × 20,162 keys where nobody in the room reads all 13 languages — the technique's unverifiable column in pure form. The repo already reached for the fourth resolution before the technique named it: it does not gate on meaning, it built oracles at lower altitudes (key parity, value-differs-from-English, section refs, error-registry parity, dead keys) and gates on those. **What it does not do is the amendment's second clause — say what the lower verdict does not cover.** **Arm A** (shipped stack): 262,106 strings, **0 violations**, both gates green, and the checker's own closing line reads *"Every locale renders in its own language"* — a claim one altitude above what it measured, which is that each value differs from English or sits on a 3,688-entry allowlist. **Arm B** (A plus one descent to protected-term survival, parsed from the termbase's do-not-translate section): 10,257 comparisons, **353 candidate violations (3.4%)**, spread **0.3% to 9.9% across locales over one shared English source** — a spread a single rate would have hidden entirely. **The tree had already paid for this exact inheritance one rung down and wrote it up**: its own i18n contract records key parity reporting zero missing across all 13 locales while ~24% of the app rendered raw English, tens of thousands of live strings, silent by deep-merge design. The repair added the value gate and **did not generalize**; the identical inheritance is structurally free to recur one rung higher. **The instrument's first run was wrong in the flattering direction and that is part of the finding**: it harvested the glossary's own "Personas trap" callout — which says lowercase persona/personas MUST be translated — as protected terms and reported **29.6%**, counting every correct translation as a defect. An oracle built at the wrong altitude does not go quiet, it produces confident numbers. Excluding the pair the glossary itself refuses to mechanize (*judge by the call site, never by the spelling*) drops exposure 1,354→789 keys and the rate to 3.4%; those **565 keys are the honest `no-oracle` remainder**. **Honest bound**: arm B is not a meaning check and must not be read as one — 46 of 47 mechanized terms are brand/acronym tokens a bad translation preserves for free; it proves only that an oracle exists one altitude above where the stack stopped, is cheap, fires on live data, and the green did not cover it. Ship 0 (blocked: confirmation) — the repair belongs to the owners and the triage answer named no project. Return condition: retired when the gate stack states its uncovered remainder in its own output rather than closing on a claim about rendering |
| 2026-09-01 | failure-attribution (model-owner two-causes amendment) | eval-harness | tracklight | **code** (escalated from `simulation` 2026-08-31) | better | **`structural-only`; three real cases from the tree, no invented cases, no code changed — no scored run exists in the store to count, which is itself the return condition.** Seam: an evaluation engine whose verdict is one line — `pass = overall >= rubric.threshold && dimensions.iter().all(|d| !d.floor_hit)` — collapsing two structurally different failures into one boolean. Below-threshold is **graded** (continuous, distance to passing is meaningful); floor-hit is **sharp** (a boundary, distance means nothing). The engine **computes both and stores both** — per-dimension floor and floor_hit persist to the score record, and the field's own doc comment names the case as *the reason a high overall can still fail* — and nothing reads them apart. **The stronger fact is a rung down, in the cross-candidate rollup, and it is one character wide**: `acc[i].value += dim.value` (later divided — a mean) beside `acc[i].floor_hit |= dim.floor_hit` (an OR). Averaging the value and OR-ing the boolean is the obvious merge for each and the wrong merge for the pair: after it, a dimension that hit its floor on **every** candidate and one that hit it on **one of five** are the same record — which is precisely graded-versus-sharp, since a trained constraint holds on every sample and a capability near a boundary crosses sometimes. **Two consequences nobody designed**: the merged record can contradict its own documented invariant (`floor_hit` is defined as *value fell below floor*, yet a dimension whose mean clears its floor can carry the flag), and the reliability signal that would recover the distribution does not — agreement is computed over candidates' **overall** scores, never per dimension, so no field counts how many candidates hit a given floor. Per-sample reasoning strings survive deliberately because their tokens were paid for: **the evidence is retained as prose beside a boolean that threw away its own shape.** Cases: the verdict line, the rollup, and the engine's own fixture pinning a safety dimension at 0.2 under a 0.5 floor while quality passes — so the split has data the moment it is computed. **Falsifier stated**: if on a real scored run every floor-hit case also falls below threshold, the causes never separate and the split is a column with no information. **Honest bound**: the split is a hypothesis generator, not an attribution — a sharp failure is equally consistent with a rubric floor set where the task changes character, or an inherited deterministic sub-check; the technique's discriminator remains a re-run at two difficulties, and this tree offers the cheaper upstream signal that the residual is a boundary rather than a ceiling. Ship 0 (blocked: confirmation) — triage answer named no project. Return condition: **one query against the score store** — the schema already holds `run_id`, `case_index` and the per-dimension detail, so nothing has to be built; only rows have to exist. **SHIPPED `26bdda6` on the default branch with a pathspec, not pushed** — escalated to `code` and `ab-paired` once the operator authorized the tree; `confirmation` was again the whole of the blocker and lifting it cost one line, the third consecutive time. **The arms are the test and it ships with the change**: same two constructed cells through the same merge — **arm A** (merge as shipped) reports `floor_hit: true` and `floor_hits: Some(1)` for *both* a three-of-three and a one-of-three dimension, with both boolean assertions passing in both, which is the defect written as an assertion rather than as prose; **arm B** separates them `3 of 3` against `1 of 3`. Verified by reverting the two merge lines and re-running: `left: Some(1), right: Some(3)`. 72 insertions across 4 files of which **6 lines are production logic** — `floor_hits`/`floor_of` on the dimension record, `None` where a dimension has no floor because it cannot hit one, stamped one-per-verdict at the runner's build and summed at its merge. **`floor_hit` and `pass` are untouched: no verdict moves**, only what a stored record can answer afterwards. 34 test binaries green, clippy clean on all touched crates, and the schema comment enumerating the JSON shape moved with the shape. **Two latent facts became documented rather than merely true**: the merged `floor_hit` is an OR that can be true while the merged mean clears the floor — its doc comment defined it as `value < floor`, which quietly stopped holding after a merge — and `agreement` is computed over candidates' overall scores and never per dimension, so nothing else recovered the shape. **One reduction deliberately left in place, and it sharpens the original finding**: the judge computes `floor_hit` from the *mean over samples*, so a minority crossing disappears before the runner sees it. The same quantity is reduced two **incompatible** ways in one pipeline — the judge's mean hides a minority crossing, the runner's OR promotes one — and only the cross-candidate half was repaired. New return condition for the remainder: an engine change reporting per-sample floor crossings, which alters what a single verdict says rather than what a merge preserves about several |
| 2026-09-01 | 9 wave-4 amendments (agent-chaining join + guards + return envelope; panel asymmetry; triage pooling + machine labels; posture label + peer baseline; a11y layer 5) | agent-chaining / fleet-orchestration / plan-review / accessibility | - | - | unapplied | Apply 0, and the reason is DIFFERENT from the wave-3 run's - worth separating rather than merging. Wave 3's unapplied set were rules about how a *number* is produced. These are rules about **structures the fleet does not build**: no managed project runs an event-wired chain that forks and rejoins (the goal-dependency table holds 1 row, so there is no join population to measure), none runs a recursive self-decomposing chain, none convenes a multi-model panel, and none has an accessibility ladder at all - personas ships a React desktop app with no axe dependency and no a11y script, so there are no layers 1-4 to test a layer-5 substitution claim against. Each finding was checked against the tree individually, not waved off as a class. Return conditions, per finding: a project whose chain has a step with two inbound edges; a project that convenes seats from different capability tiers on one question; a project that adopts any automated a11y layer. **Incidental signal worth its own row later**: `accessibility` is rank 3 on the librarian worklist at 46 points and single-stack, and the fleet's one React desktop app has no accessibility tooling of any kind - the demand the scan measures and the absence in the tree are the same fact seen from two ends. |
| 2026-09-01 | repository-landing-document (landing-document-as-router + visual-text-cadence + input-channel-distinction) | repository-landing-document | gravity | experiment | better | **`ab-paired`; both arms measured on one instrument (`scripts/check-readmes.mjs`, written this run), no product code changed and nothing written to the tree.** Seam: a landing document of 2,089 words that routes to **zero** pages while 12,323 words sit in a `docs/` directory beside it — the destinations exist, the router does not. **Arm A** (shipped README): 3 findings — routes nowhere, 90 consecutive unbroken prose lines against a limit of 15, nothing but prose above the fold. **Arm B** (the same content under the golden path): **0 findings** — 581 words, 7 routed pages, 2 reader-directed callouts, longest prose run 8, a routing table above the fold. **No content was deleted**: 1,780 words were relocated into three new pages and four pages that already existed were routed to for the first time, which is the population test's prediction rather than a length cut. **The instrument corrected its own author twice, and that is the finding worth keeping.** First, a hand-count credited one fleet project with 7 figures; they were badge images, and the corrected reading is 7 of 7 projects at zero figures, zero captions, zero callouts, zero routing. Second, the draft counter admitted paragraph breaks and bare headings as breaks and read arm A at 39 lines; reconciled to the closed break set the forged rule actually states, the same document reads **90**, and the fleet worst reads 96 rather than 39 — which invalidated the threshold argument the forge worker had built from the draft numbers and forced a rewrite of the subject's cited distribution (10, 19, 20, 20, 22, 90, 96) and its defence of fifteen. **A threshold argued from an instrument that does not implement the rule is an argument about the instrument.** **Honest bound**: one project, one day; arm B was written by the same author as the rule, so it measures whether the rule is satisfiable and legible, not whether a reader is better served — no reader was asked. **Ship 0 (blocked: foreign WIP)** — the operator authorized this tree, and the tree carried a live 38-line in-flight append in the exact file; a branch would not have carried the uncommitted arm A, and switching that repo's branch under an active session is the incident this method warns about. Arm B is written and measured, waiting only on that in-flight work landing. Return condition: retired when the fleet's landing documents are repaired or the rule is argued down |
| 2026-09-01 | rendered-surface-coupling | docs-sync | - | - | unapplied | No managed project has a single **authored** documentation figure. Checked, not assumed: 6 of 7 fleet trees have zero image references in any markdown file, and the seventh's 22 all sit under `data/artifacts/readable/` — scraped web pages, not authored docs. There is no seam, and a simulation would have required invented cases, which does not count as applied. **The absence is the same fact as the neighbouring subject's motivation, seen from the other end: a fleet that shows nothing has nothing to keep fresh.** Return condition: a managed project growing an authored figure in a maintained document — at which point the technique's input-set composition (too narrow substitutes the picture behind a green gate; too wide fires on every dependency bump) becomes testable rather than argued |
| 2026-09-01 | context-reset-redelivery | agent-instruction-files | - (fleet-wide read) | experiment | better | **`ab-paired` on a live harness; two injectors, one variable — whether the injector re-reads or replays what it composed first.** The instruction floor was edited between two deliveries. Arm A (composes once, caches a digest): `RULE-V1`, then **`RULE-V1`** again after the file said V2. Arm B (re-reads per delivery): `RULE-V1`, then `RULE-V2`. **All four opens produced an identical producer signal** — both hooks ran, both exited 0, both wrote their marker — so nothing on the producing side separates the arm that delivered a stale rule from the one that delivered the current one, which is the technique's claim reproduced as a measurement. **Honest bound written into the application**: the mechanism is proven, the *trigger* is not — a compaction cannot be forced from a headless single turn, so re-delivery was driven by a fresh open, and this harness's per-event behaviour on `clear` and `compact` is recorded **uncovered** rather than inferred from the open that worked. **Fleet position is the useful half and it relocates the exposure rather than removing it**: no project ships a session-start injector at all (the fleet's only hook is a turn-end doc-drift check), so none holds the arm-A defect — and every project's floor is therefore delivered entirely by native re-injection whose reset behaviour no project has verified and none could name. Return condition: any project that grows an injector, or a run that can force a real compaction |
| 2026-09-01 | child-observed-posture (delivery seam) | agent-cli-transport | - (throwaway lab) | experiment | better | **`ab-paired`; the same hook script, same event, same bytes, same exit 0 — the single variable is the stream.** Arm B on stdout: token quoted back, delivered. Arm A' on stderr: **`NO_TOKEN`**, nothing arrived. Producer evidence byte-identical across both (`ran=…, rc=0`), so a host asserting "the hook succeeded" passes in the arm where nothing existed. Writing to the wrong stream is not contrived — it is the ordinary wiring mistake and the exact shape of the field report the amendment is written from. **The negative arm is the half worth paying for**: arm B proves only that today's wiring is right, while arm A' is what shows the instrument is *necessary*, because without the echo arm A' is indistinguishable from success. A second harness was installed and probed in the same lab and did not complete inside a four-minute budget — plausibly an authorization state — and is recorded **not exercised**, never as a negative, since inferring a delivery answer from a run that produced none would reproduce in the measurement the confusion the technique exists to prevent. Return condition: a project that spawns an agent CLI and relies on injected context |
| 2026-09-01 | unmeasurable-criteria (in-path branch) | quality-gates | pumper | experiment | better | **`ab-paired` over four inputs against the fleet's only in-path gate — a turn-end hook, the one check that stands *in* the work rather than beside it.** Arm A is the two-state gate almost every check in the subject is (pass / violation); arm B is the shipped three-state vocabulary. Broken trigger → `3` vs green; missing target → `3` vs green; **standard loaded with zero entries** → `3` vs green; honest clean turn → `0` vs green. Arm A collapses three distinct instrument failures onto the same value as the one real pass, and the checker's own header records that this had actually happened: it "detected nothing for its whole life and looked exactly like a clean repo." **The apply refuted the amendment's own prescription before commit.** The draft told an in-path gate to fail "open, silently and deliberately"; this tree shows withdrawing and going quiet are two decisions and only the first is licensed by the blast-radius argument — the operator is the one party who can repair an instrument and the actor cannot substitute for them. Rewritten to **open to the actor, loud to the operator, on a code of its own**, which is what the tree ships and what `absent-guard-is-loud` requires. Ship 0, `declined: no change warranted` — the tree already implements the remedy, which is precisely why it could validate it. Bound: the arms measure classification, not detection quality; no arm exercised a true positive |
| 2026-09-01 | self-reported-gate-inputs | quality-gates | - (this registry's own board) | experiment | better | **`ab-paired`; an unclaimed concurrent writer simulated under a subject path no live run held, then the same question asked of the record and of the act.** Arm A, the shipped coordination check: `clear: no live sibling holds 1 target(s)`, exit 0 — permits. Arm B, the working tree: 1 in-flight file, contended. The decisive detail is not that arm A is wrong but that it is **unreadable** — that string is byte-identical to what the gate emits when the board is genuinely empty, so an unclaimed writer and an idle repository are the same output: `failure-not-empty-success` sitting inside a gate. **The obvious fix is provably not one**, and it needs no run to see: every "is the board complete?" check reads the claims, and the failure is that there are none, so a second guard on the same input is a second inert guard. The method's own prose already lists running without a claim as an anti-pattern and notes every collision rule "degrades to hope" — a rule written down and never mechanised (`prose-rule-drift`), and this technique supplies the reason it never was. Bounds stated: arm B is a predicate, not a shipped change, and it has false positives (an operator hand-editing, a run's own output) that the shipped form would exclude by asking run — but arm A returns clear in every one of those cases too, and a noisy detector that fires beats a silent one that cannot. **SHIPPED as `92c733b`** (tracklight, main, pathspec, not pushed). The operator lifted the tree question at Phase 7.5 and the change went in at the call boundary with no schema migration: `decide_gate` takes `&Benchmark` instead of `Option<f64>` and refuses the floor when the run was scored against an unfrozen dataset. Second paired A/B, this time on the gate itself and in the tree's own test binary — 8 cases, both arms, same inputs: **3 differ, green 4 -> 2, and 5 of 8 byte-identical**, which is the number that matters, because the golden path's composition doctrine says added rigour may trade a false alarm for a detection and may never disarm. All three differences are verdicts that rested on a comparison against a moving case set, including the legacy scalar path that has no significance testing at all. **Shipping narrowed the claim three ways and the application says so**: it checks one condition rather than the predicate; it does not date the baseline (that needs the baseline stored as a record across four stores); and the frozen flag is a *proxy* — an unfrozen dataset may not have changed, so the predicate produces false refusals, in the direction a gate should be wrong in. The sharpest of the three: **this tree cannot detect judge drift at all**, because the judge is inherited from an immutable benchmark row, so its apparent safety is immutability rather than a check, and a benchmark-update endpoint would acquire the full failure the same day. Full workspace suite run; one pre-existing failure in `crates/core/tests/catchup_marker_guard.rs` reproduced with the diff stashed and left alone |
| 2026-09-01 | baseline-carries-its-conditions | quality-regression-gating | tracklight | **code** | better | **`ab-paired` over real shipped configuration — the model-identity canonicalization table (11 model entries, 7 provider entries), the one surface in the tree where model identity is actually resolved.** Arm A, the shipped normalizer, yields **8** distinct comparability identities; arm B, retaining the dated variant, yields **11**. Five real instruments collapse into two — three `gpt-4o` variants spanning 2024-05-13 to 2024-11-20, two `claude-3-5-sonnet` variants five months apart — and **82% of entries (9/11) carry an explicit date arm A discards**. The collapse happens upstream of every verdict, so no downstream surface can report the distinction was lost. **The apply corrected the technique**: the table is not a defect (its own header argues for it, and for a leaderboard it is right), and what the arms actually establish is that a codebase has ONE identity function while the aggregation surface is the one people look at daily — so a comparability predicate silently inherits the aggregation answer. The discriminator *aggregate on the canonical identity, compare on the measured one, keep both fields* was added to the technique from this result and was not in it before. Second structural fact: the tree records every condition the predicate needs (judge model, dataset ref, baseline, timestamp — one immutable row, no update path) and then narrows to `Option<f64>` at the verdict call boundary; the technique predicted a bare scalar with no conditions anywhere, and a narrowing at the seam is the cheaper repair and the harder one to see. Bound: **the gate's own arm is unmeasurable here** — the verdict function never consults judge identity and the shipped store holds zero benchmarks; the instrument that would measure it is a populated store with two runs spanning a judge change, which this tree cannot produce because an immutable benchmark row makes a judge change a new benchmark. **SHIPPED as `92c733b`** (tracklight, main, pathspec, not pushed). The operator lifted the tree question at Phase 7.5 and the change went in at the call boundary with no schema migration: `decide_gate` takes `&Benchmark` instead of `Option<f64>` and refuses the floor when the run was scored against an unfrozen dataset. Second paired A/B, this time on the gate itself and in the tree's own test binary — 8 cases, both arms, same inputs: **3 differ, green 4 -> 2, and 5 of 8 byte-identical**, which is the number that matters, because the golden path's composition doctrine says added rigour may trade a false alarm for a detection and may never disarm. All three differences are verdicts that rested on a comparison against a moving case set, including the legacy scalar path that has no significance testing at all. **Shipping narrowed the claim three ways and the application says so**: it checks one condition rather than the predicate; it does not date the baseline (that needs the baseline stored as a record across four stores); and the frozen flag is a *proxy* — an unfrozen dataset may not have changed, so the predicate produces false refusals, in the direction a gate should be wrong in. The sharpest of the three: **this tree cannot detect judge drift at all**, because the judge is inherited from an immutable benchmark row, so its apparent safety is immutability rather than a check, and a benchmark-update endpoint would acquire the full failure the same day. Full workspace suite run; one pre-existing failure in `crates/core/tests/catchup_marker_guard.rs` reproduced with the diff stashed and left alone |
| 2026-09-01 | cinematic-language scope clause (`movement-motivation` boundary) | gravity | simulation | unmeasurable | **`simulation` over three real cases from a clean tree (0 WIP), and the honest count is 2 of 3, not 3 of 3.** The tree's `SceneSpec` holds both channel regimes in one struct: overlay `elements` carry position as numbers clamped 0–100 because the project's own vector layer executes them, while `motion` is a free string validated only for length, glyph-asking and self-restatement — because, in the tree's own words, it is "authored here, rendered nowhere". **Nobody designed that as a demonstration of `typed-input-owns-its-channel`; it fell out of which half had a renderer**, which is the structural fact this application exists for. Cases: (1) a developer deciding whether the subject governs this pipeline — under the unqualified golden path the first spatial field they open contradicts it, and the honest conclusion is to skip the subject and with it `movement-motivation`; **better** under the clause. (2) The day the motion channel gains the renderer its comments promise — the clause meets them before they build the executor, when the decision is cheap; **better**. (3) The identity-conditioning probe, where a reference already IS a typed input — governed by another subject, **no difference**, recorded rather than dropped. **Verdict is `unmeasurable`, not `better`, and the falsifier is real**: `movement-motivation`'s `use_when` already names the camera-path case, so a developer who searches rather than reads top-down reaches the boundary without the clause — and cases 1–2 are claims about routing that no instrument in this tree can see. The application also records what it CANNOT do: it holds only the arm where prose is uncontested, so it is evidence about the rule's easy half and none at all about the withdrawal boundary. Ship 0, **declined: no change warranted** — the clause confirms this tree's design rather than asking it to change; gravity and pof were both authorized and neither needed a commit. Return condition / named instrument: **an executor for the motion channel** — at which point the question stops being navigational and becomes checkable in code (does the prose go silent, or do two authorities ship side by side and produce the predicted compromise?) |
| 2026-09-01 | never-the-account-default | generative-provider-auditing | gravity | **code** | better | **`ab-paired` on the project's own gate, and the measurement came from the compiler rather than from a reader.** The technique's audit test — *"ask which model version produced it, and answer from stored data rather than inference"* — turned into a countable predicate: provenance construction sites at which a reader can determine the BASIS of the recorded model identity. Arm A (tree at `e79e04c`): **0 of 5**, gate green, nothing visible. Arm B (`modelBasis` added to `Provenance`, REQUIRED): **5 of 5**, gate green. Making the field required is what turned `tsc` into the instrument — the intermediate typecheck failed at exactly five sites and named them, and **every one of the five error messages displayed a `costBasis` on the same object literal**, so the toolchain printed the asymmetry five times unasked. **The structural fact is better evidence than the change**: `lib/imaging/types.ts` already carried a `CostBasis` discriminator with eight lines of comment arguing from a real incident that *"an estimate presented as a receipt is the error worth spending two lines to make impossible"* — this technique's first decision rule, derived independently, for a number. Immediately above it `model: string` sat required and unmarked. Of the subject's four audited properties, three had their epistemic status modelled in the type (`costBasis`, `cleanup`'s named-miss enum, `reroutedFrom`'s meaningful absence) and **Identity — the property the subject opens with — had none.** All five sites also fill `model` from a caller-side constant, three of them via `env-var || baked-in-default`, which the technique names explicitly as "the same failure relocated". **SHIPPED as `e3b3f09`** (gravity, main, pathspec, not pushed); `lint:ratchet` unchanged at baseline. Bounds recorded in the application: it marks the claim without upgrading it (all five resolve to `requested`, since no adapter reads a model from a response); `undisclosed` is declared but unreached; `tsc` sees construction, not the already-stored records that carry no basis and are indistinguishable from confirmed ones; and nothing yet REFUSES on the field, so the shipping decision the technique asks for is still a human who may not look. Return condition / named instrument: **per-adapter response parsing** — the work that would make a genuine `vendor-reported` reachable and turn 5/5-declared into a real sourcing improvement |
| 2026-09-01 | placeholder-is-not-an-asset (constructed as a third origin) | generative-artifact-gating | pof | **experiment** | better | **`ab-paired` over the 13 authored Items steps in the one fleet project that declares `game-production`.** The measurable: how many steps does each origin policy classify correctly, where correct means the verdict matches whether real work stands behind the artifact. Arm A, the two-valued origin the technique shipped (generated | deterministic stand-in): **2 of 13** — the two generative steps land right and the other eleven, which construct their artifact locally in `produce` from the project's own canon, are classed as stand-ins awaiting a generator run that does not exist for them. Arm B, three-valued (generated | constructed | stand-in): **13 of 13**. **The structural fact is better evidence than the change**: `pof` already runs three origins and names two — `assetHonesty.tsx` states the binary verbatim (`SEED_PREVIEW_CAPTION` = *"Deterministic seed preview — not the generated asset."*) and `stepEvidence.ts:26` enforces it (*"Never a swatch, never a data-derived placeholder"*), yet 11 of 13 steps grade locally computed artifacts on their own terms and never defer. Nobody designed that as three origins; it fell out of building one pipeline over two kinds of producer. **The instrument lied first and the correction is the reusable half.** Arm B is only worth anything if those eleven artifacts are genuinely graded rather than merely present, so this technique's own mutation probe was added to the project: mutate what a gate reads, confirm the verdict moves. First run reported **4 insensitive** (`Attributes`, `Economy` always pass; two generative steps always defer). All four were the probe's fault — it scaled every number at once, which preserves the ratio `Economy` reads against its price curve, and it walked only the top level, so `Attributes` (whose artifact is a single nested `stats` object) was never mutated at all. Corrected to one leaf at a time, recursive, plus key deletion: **13/13 sensitive, 0 insensitive**. A probe that under-reports sensitivity indicts working gates, and shipping the first number would have done exactly that. **SHIPPED as `3de20873`** (pof, master, pathspec, not pushed) — the probe retained as a standing guard, which is what the technique asks for (*"Rerun the probe whenever a gate is rewritten"*), plus the `.ai/applied.jsonl` row. `npx tsgo --noEmit` clean, eslint clean; pof's tree carried unrelated WIP so nothing else was staged. **Bounds recorded in the application**: the three origins are *implicit* here — a consequence of which step frame a step lives in, not a declared property of the asset class, which is what the technique actually asks for; and the probe grades sensitivity, never correctness. Return condition / named instrument: **an explicit per-class producer declaration** — nothing today would stop a fourteenth step landing in the wrong frame, and `StaticStepFrame` and `GenerativeStepFrame` were recently put on one shared `useStepAcceptance`, which is precisely the junction where a two-valued origin read would start deferring eleven finished steps |
| 2026-09-01 | gate-scope-is-not-report-scope | test-harness | personas-web + ascent | **code** | better | **`ab-paired` on two Node trees, same suite, same inputs, one variable - the coverage include set.** Codebase 1 ran its full suite in both arms (20 files/153 tests): arm A, the shipped scoped list, **5 files at 95.74% lines**; arm B (`all: true` + `src/**`), **1,171 files at 4.32%**. 1,070 source files were absent from the report entirely rather than visibly at 0%. Codebase 2 ran its full 782-file suite: **175 -> 1,529 files, 68.98% lines, 490 at 0%** - and it is the instructive arm because it cuts against the obvious reading. Its suite is genuinely broad and the scoped report was *understating* the project; what the scope hid is a **population**, not a bad number. **The structural fact neither team designed**: both include lists carry a comment promising to grow them ("extend as more batches land", "promote them into the gate as their batches grow") - an obligation with no instrument behind it, which is the shape that converges on absent. Codebase 2 makes the ratchet's blind spot concrete: its floors ratchet over three directories of a 1,529-file tree, so a new untested directory lowers no floor and the mechanism built to stop coverage decay cannot observe the most common way coverage decays. **SHIPPED to both** (`c355b79` personas-web/master, `05ef946e` ascent/moonshot-wave-4; pathspec, not pushed). Neither gate weakened - codebase 1's five floors became five per-file thresholds over the same five files, codebase 2's floors were already per-glob - and all thresholds verified passing in arm B. Bounds in the application: the whole-tree number is a report, not a gate, so nothing fails when it drops; making it one needs a second threshold neither project has adopted, and that is the wrong first step. One pre-existing unlabelled flake observed failing twice and passing twice across four runs on both configs, left to its owner. Return condition: either project adopting a whole-tree floor |
| 2026-09-01 | dated-corrections (three-way omission rule) | docs-sync | - (this registry's signals lane) | **experiment** | better | **`ab-paired` over the registry's own recorded consult logs - 104 lines, six connected projects - with the collector's fold logic lifted verbatim and one variable, the window.** The seam is `scripts/signals-collect.mjs`: a rolling `WINDOW_DAYS = 30` (`:32,46`) folded into a **whole-file replace** (`:109`), so a subject witnessed 31 days ago is not marked expired, aged or withdrawn - it is simply not written, and the artifact that replaces it carries no evidence it was there. Omission is the retraction signal, in our own tree, at the lane's only writer. Arm A at the shipped window: 87 pairs, **0 dropped** - the defect is real in code and *not yet observable*, because the oldest consult is 2026-08-23. Moving the boundary into the existing data measures the arrival directly: at 8d, **71 pairs and 16 dropped with no record** (18%), every one a real subject a real consult logged; at 4d, 36 dropped. **The first genuine silent drop lands 2026-09-22**, which is a defect with a computable arrival date rather than a hypothesis. **The asymmetry is the reusable half**: `librarian-scan.mjs` models unknown-versus-zero at *bundle* granularity with visible care ("nobody consults this" and "nobody has told us" are opposite; `DEMAND IS UNKNOWN` is printed explicitly), and at *subject* granularity the same distinction is destroyed - not by a decision but by the write path. One lane, one author, modelled where it was thought about and lost where it was not. **Ship 0, deliberately**: the repair is a schema decision on a lane whose key set is closed and validated, and the prior run against this same source already left it owing an unemitted `citations` key - adding a second key before the first is emitted widens the gap rather than closing it. Return condition / named instrument: **`signals-collect.mjs` emitting `citations`**, after which the aged-out count belongs beside it |
| 2026-09-01 | prose-rule-drift (artifact-rule-enforced-at-the-edit amendment) | quality-gates | pof | **code** | better | **`ab-paired` over one file's 59-commit history with two checks and one instrument.** Arm A, the per-edit view (lines added vs the 2-line append cap): 0 of 26 commits flagged since the file crossed its 200-line cap on 2026-08-19. Arm B, the artifact view (length vs 200): 26 of 26. Calibrated before reading: a known-clean backlog (188/300) reported clean, a known-over external page (2,728/200) reported over, and the file's own seeding commits (+18..+42) tripped arm A. Shipped as an artifact-reading check with the document's stated prune remedy adjacent, wired at the end of the per-CLI gate, plus one prune of 48 disposable entries to exactly the cap; the gate then ran green end to end. Return: when the durable entry kinds alone approach the cap, the prune has nothing left to drop and the cap or the file's shape must change. |
| 2026-09-01 | ordered-lane-blocking | delivery-guarantees | personas | **simulation** | better | **Three real cases from a conversation queue whose drain fires on a `finally` block.** The drain shifts the next queued message on the streaming true->false edge, and the send path raises that edge on success and on IPC rejection alike, so a failed send releases the follow-up on top of a turn the backend never saw (A skips; B blocks and shows *wedged*/*blocked*). A redirect-class message supersedes the head under both. A drained item whose nonce is already accepted returns before touching the flag, so no edge fires and the lane is stuck with nothing on screen (B shows it as wedged). Not a few lines - the store needs a blocked state and the error chip two verbs - so filed as the next change. Falsifier: the classifier's queue class is in practice independent of the running turn. Return: when the queue gains a blocked state, re-run as an experiment. |
| 2026-09-01 | optimistic-write-path (stream-lane inversion + id-less equality amendment) | client-state | personas | **simulation** | better | **Same seam, same three cases, read from the client side.** Local echoes there carry a minted local id, so the id-absence half is met structurally; the stream-lane half is the drain above. Return: same as ordered-lane-blocking. |
| 2026-09-01 | cross-process-exclusion (generation-on-regain amendment) | concurrency-guards | personas | **simulation** | not-better | **The one fleet tree with a real leadership lease, and the regain check changes nothing there - by structure.** The lease carries pid and heartbeat and no generation; three regain paths walked (the non-atomic takeover bounce the existing application already names, upgrade overlap, parallel test instances). Every leader loop re-reads its cursor from the store per tick and the cloud loops gate on a store flag per pass, so nothing derived from the store survives a follower period and a dirtied generation would have nothing to invalidate; the one process-lived state across a bounce is a breaker tally that must not reset. Condition written into the technique: add the check when the first in-memory cache over the shared store appears. Return: that cache. |
| 2026-09-01 | journal-and-durability-modes (name-the-set-from-the-engine amendment) | embedded-db | personas | **code** | better | **`ab-paired` over six store names with one instrument (open under WAL, write, compare the constructed sidecar to the one the engine created).** Arm A, the tree's `with_extension("db-wal")`: 2 of 6 correct - right only while the store is named `*.db`. Arm A', the source's shipped `.wal` suffix: 0 of 6. Arm B, append `-wal` to the full file name: 6 of 6. The tree honored the file-set clause in three places by name and was right by luck; shipped a `sidecar_path` helper through the backup copy, its rotation and the permission pass, plus the regression test the amendment prescribes (three names under WAL, sidecar asserted on disk). `0f4a3b468`, personas, pathspec, not pushed. Return: none; re-check if a store is opened under a non-`*.db` name. |
| 2026-09-02 | multi-client-fan-out | terminal-multiplexing | personas | simulation | unapplied | **Condition absent in every managed project.** The desktop host's attachment set is a boolean (M is 1 by type) and the game tool has no PTY; structural fact recorded in a negative rust application - the host already pays the server-side grid the technique requires and no second viewer consumes it. Return when a second surface renders a session screen or the boolean becomes a set. |
| 2026-09-02 | public-vs-server-env-split (mark the module) | browser-credential-boundary | ascent | experiment | better | **Barrel-only marker sees 0 client value imports; the import graph finds 2 of 118 unmarked db modules reached by value from 15 clients, one reading unprefixed server env.** ab-paired over 385 client roots. Return when the project marks the mode module and splits the pure label - a build before/after then upgrades this to code. |
| 2026-09-02 | one-interface-many-planes (condition) | demo-data-plane | systedo-case | experiment | better | **The condition turns ~42 undifferentiated breaches into 14 expected-shape sites and 6 real mislabels** (sample totals rendered under a live-data pill, sample grounding fed to the model for an owned project). The 2026-08-31 conformant verdict could not see it because every label is honestly read from the session and the numbers beside it are from the other plane. Return as code once a unit test asserts a synced project's totals equal its synced row. |
| 2026-09-02 | import-validation (presence before content) | import-normalization | personas | experiment | better | Fixed seam confirmed 3/3 erased before, 0/9 after (commit named). Enumeration found one unfixed same-shape guard (a credential form's empty field list renders zero fields while the healthcheck reads the same empty list as never-matches, no fixture) and one boundary that collapses option presence into map emptiness. Return when the form declares what an empty field list means, or a second caller sends resolutions to the importer. |
| 2026-09-02 | missing-anchor-degradation | guided-tours | personas | experiment | better | A ledger arm distinguished a first-anchor-removed run (1) from a clean run (0) where the tree's transient flag read false in both; never-strand held with the first anchor removed. Structural fact: a global re-center-by-omission policy and a degradation record the next step overwrites, so a degraded completion persists like a clean one. overlay-precedence owed: neither tour runs a focus trap, so the conditioned rule has no seam. |
| 2026-09-02 | entitlement-lifecycle-revocation (compare state, never event order) | plan-entitlements | ascent | code | better | **Fence moved from the event's subscription snapshot to a fetched subscription; 4/9 resurrecting webhook sequences became 0/9, 35 existing tests green, committed on the project's active branch, unpushed.** Structural fact: the provider's order event ships a status snapshot, not a transition pair, so the technique's cheaper form is unavailable there. Return when a webhook-only deployment without an API token appears (it silently keeps the snapshot fence). |
| 2026-09-02 | safe-mode-guarding (read-shaped writes) | sql-console | personas | code | better | **8 of 9 read-shaped writes passed as read-only before, 0 of 9 after, 0 of 8 plain reads over-blocked; shipped on both guard sides with 10 new assertions, committed on the project's active branch, unpushed.** The envelope half had no seam: the host never wraps a statement in a read-only transaction and the single-statement rule refused the stacked COMMIT without seeing it. Return when the row-limit injector learns row-lock clauses - a confirmed FOR UPDATE now trips it instead of the guard. |
| 2026-09-02 | decode-time-vocabulary-biasing | voice-io | personas | **experiment** | not-better | **`ab-paired`, n=5, the product's own engine binary and catalog model at the product's exact flags; arm B adds the eleven-word spoken-decision grammar as an initial prompt.** Spoken 3/3 resolve to the same decision in both arms (numerals vs words; the parser accepts both); silent 2/2 go from the engine's blank-audio marker to the word `you`. The technique's precondition (a measured confusion) is unmet: a small, phonetically distinct grammar already recognized without help, so biasing has nothing to move and costs the silence gate, which the tree has not built on either side. Return: when the grammar carries entity names, measure their miss rate first, build the pre-decode level gate, re-run |
| 2026-09-02 | stt-pipeline (the engine's empty is not always empty on the wire) | voice-io | personas | **code** | better | **`ab-paired` over five inputs, two of them the verbatim engine output for silence; shipped (pathspec commit on master, not pushed).** The adapter's empty-transcript guard compared string length and the engine prints ` [BLANK_AUDIO]` with exit 0, so the guard had never fired for the case its own comment describes. Fix drops lone bracketed all-caps tags and a short lowercase event list before the join: 2/2 silent inputs reach the typed no-speech path, 3/3 speech inputs unchanged. Crate gate could not compile (build script stops on an undeclared plugin permission before any source builds); arms run as a faithful rustc 1.97.1 reduction with the unit test carrying the verbatim fixture. Return: run the crate's test once the permission is declared |
| 2026-09-02 | stt-pipeline (the utterance cap applies to explicit endpointing) | voice-io | personas | **simulation** | better | Three cases from the tree, one measured: a quick tap (resolved by the marker fix), a normal 3 s answer (identical under both policies), a hold past the engine's fixed 120 s timeout (A loses the whole utterance at release; B closes at a cap in the tens of seconds and transcribes while the hold continues). Falsifier: engine latency not roughly linear in capture length. Filed as the project's next change - the cap belongs in the capture hook with a visible countdown. Return: one 150 s hold before/after |
| 2026-09-02 | probe-design (the-consuming-probe amendment) | health-checks | tracklight | **simulation** | better | **The tree holds an ownership heartbeat and no progress probe, by structure.** The runner renews its job lease from a timer thread at TTL/3, and its comment states liveness must never wait on the work - the ownership rule, built correctly; the API has a health route for itself, claim and renew, and no route that learns a worker exists between jobs. Three cases: an idle worker killed (A: unbounded, first sign is queue age; B: red in three minutes); a worker hung inside a long case (A: lease renews forever, nothing says stuck; B: token stops at the last case boundary, red, lease untouched so the job is not reclaimed); a transient API blip (neither arm changes - B never touches the API). Not a few lines (a token writer and a host probe), so filed as the next change. Falsifier: a legitimate case longer than interval x retries, undeclared. Return: a replayed run with a planted hang read by the probe promotes this to an experiment. |
| 2026-09-02 | installer-authoring (user-owned-config amendment) | packaging | registry (self) | **experiment** | not-better | **`ab-paired` over the link script's managed block in every connected checkout.** A = rewrite the marked block wholesale (as shipped); B = the three-state rule (rewrite only if provably ours, pass if matching, refuse and report on any foreign line). Instrument: classify every line inside the markers as ours or foreign, 7 of 8 checkouts carry the block. Foreign lines: 3, all the same stale comment an earlier version of the script itself wrote; operator lines: 0. A is right in 7 of 7; B refuses 3 and reports an edit that never happened. Not better because the block is a **machine-owned region** and the rule is for values outside such regions - condition written into the amendment's closing paragraph. The seam the rule fits is next door (the manifest's operator-owned skill list on a rename: the script already reports and refuses, and lacks the migrate-with-consent half). Return: a rename map. |
| 2026-09-02 | flake-lifecycle (same-code-means-same-inputs amendment) | test-harness | - | **unapplied** | - | **No fleet project has a scheduled lane over floating inputs.** Both scheduled Rust lanes (tracklight soak, pumper long lanes) pin the toolchain through a manifest the runner cannot override, and each says why in a comment that is the amendment's own discriminator - "a certification judged by a floating toolchain would attribute a compiler's change to the product". The registry's own workflows have no schedule; the two Node scheduled jobs are advisory scans (clock-triggered risk, already owned by scheduled-deep-analysis). The negative structural fact is recorded in the subject note. Return: a project that tracks a moving upstream by design (a packaging repo, a lane pointed at a live service). |
| 2026-09-02 | update-automation-review (release-age amendment) | supply-chain | tracklight, pumper, kp, politicas | **experiment** | unmeasurable | **`before-after` over the bot's own proposal history: 67 proposals, 8 superseded by a later proposal for the same package, 4 within three days, 1 real once calibration removed grouped proposals the bot rebuilds itself** (a library major followed by its first minor forty hours later). Calibration: a merged proposal must not count (did not), a single-proposal package yields no pair (did not), and reading the four fast rows moved the count from 4 to 1. The churn half is measured at ~1 in 67 and is not the reason to adopt the floor; the supply-chain half cannot separate on a fleet history with no withdrawn release. Instrument named: an advisory feed joined to release timestamps, giving each flagged version's age at proposal time. Also found: one tree already refuses all ignores because on this bot an ignore mutes security proposals, and another carries an event-expiring ignore - both now in the technique. Return: the first withdrawn or flagged release the fleet ever pulled. |
| 2026-09-02 | fallback-retirement-condition (when the gap is in something you pin: the release is the reaper) | optional-dependency-degradation | personas | **simulation** | better | **Three cases from the wrapper's own version-floor ledger, structural-only.** The pinned default effort is a policy row, not a workaround (a dated audit would retire it and lose the determinism it buys); the floor advanced from changelogs into a broken window twice, and the ledger's comments say so - source-inspected evidence with no tier label and no re-run of closed rows on a pin move; a scan-disabling flag adopted the day its reason weakened, with no removal condition. Better on 2 of 3. Falsifier: the minted cases are untimed, in which case the re-run misses a per-spawn stall. Return: when the wrapper emits which version-keyed path a spawn took, retirement becomes a traffic share instead of a rerun |
| 2026-09-02 | inherited-default-override (the third column: a default derived from sibling keys; provenance through the validation door) | settings | personas (pumper structural) | **simulation** | better | **Three cases across two trees, structural-only.** Every cross-key rule in the server's validator is guarded by a boolean whose default is off, so no derived value can ever be the trigger - equal under both arms and a structural confirmation nobody designed; the routing cascade fills the model only when nothing explicit exists - equal; the resume path replays a constant effort where the first run derived one from the profile, failing its own stated intent for every profile with a non-default effort - better, with the next change named (re-derive from the same inputs on resume). Not shipped: crosses a provider-trait signature; sibling run live in the tree. Return: a validator that logs, per rejection, whether every named key was explicit - the count that would make the provenance rule falsifiable |
| 2026-09-02 | atomic-downloads (resume-contract amendment) | sidecar-provisioning | politicas | **experiment** | better | **`ab-paired` against a local socket server replaying one failure per case, the same protocol as the source's own tests.** A = the harvester's loop (fetch, `ok`, pipe to the final name, mark done); B = partial name, catalog size pinned before the first byte, contradicting total rejected, 206 offset verified, cut at excess, received-vs-catalog compared, rename last. Predicate: month marked done over a non-artifact. 6 cases, calibrated on a clean transfer both arms pass: A 1/6 (server advertises and delivers 900 against a catalog of 1000), B 0/6. Structural facts: the runtime's fetch already fails a short body (`terminated`) and stops at the advertised length - two of five checks made by the transport, written into the technique as a caution; the index size the loop prints and stores was compared to nothing. **Shipped** `73a9443` (master, pathspec, not pushed). Instrument named for the frequency question - state `bytes` vs on-disk dump size per month - attempted, no harvest state on this checkout. Return: a machine that has run the harvester runs that comparison. |
| 2026-09-02 | transcript-handoff-receipts | voice-io | - | **unapplied** | - | **No fleet project injects text into a foreign application.** The one desktop shell in the fleet owns copy buttons and no paste path; every other project is a web surface whose "clipboard" is a copy affordance inside its own page, which the technique's first "when not" clause excludes. Return: a fleet project grows a dictate-into-the-focused-app or paste-into-another-window path; the desktop shell is the candidate. |
| 2026-09-02 | unsafe-deserialization-off-by-default | supply-chain | pof | **code** | better | **`ab-paired` over the producer's own key set: two archives x two arms.** Four archive readers, three permissive, the newest restricted; the tree's only producer writes seven numeric arrays and one scalar. Producer-shaped archive: 8/8 members load identically under both arms. Hostile-shaped archive (one object member): executed under A, refused under B. Three readers flipped in one pathspec commit (`9788290e`, master, not pushed). Return: an upstream archive arrives with an object-typed member - the restricted loader now refuses it loudly and somebody decides whether it was ever meant to be there |
| 2026-09-02 | deprecation-by-version-arithmetic | release-pipeline | goat | **experiment** | better | **A harness over the tree, product code untouched.** 24 live doc-comment deprecations, 48 ever declared, 24 ever removed, manifest version unchanged since 2025-05-30, oldest live tag 2026-01-11. A (tag, no clock): 0 of 24 reported due, by construction. B (since + removal condition in the unit the project advances - date or importer count, since no version moves): 2 of 24 due (zero-importer modules deprecated 2026-01-18 and 2026-03-19), 22 inside their window with a stated caller count. Carried into the technique: **the operand is whatever the project actually advances**. The project declares no registry domains and has no `.ai/` directory, so the row lives here only. Return: a per-change gate that enumerates tags past their condition - the two zero-importer modules are its first list |
| 2026-09-02 | platform-quirk-absorption (numerics-mode probe amendment) | test-harness | tracklight (+ pumper read) | **simulation** | not-better | **Three real tolerance sites, no mode to probe.** Billionth assertions on a trend fit, a two-tenths assertion on a decay, an epsilon-padded ratio guard - all on general-purpose double with no accelerator, no contraction or fast-math profile, no precision flag anywhere to pin. A (constant tolerance) and B (runner probe publishes the tolerance) give identical outcomes; the probe measures zero deviation and publishes a constant. Condition landed as the amendment's closing paragraph. Falsifier: a lane moved to an accelerator or a fast-math build profile - the billionth assertions scatter first. Return: that move |
| 2026-09-02 | completion-claim-verification | fleet-orchestration | personas | **simulation** | better | **The reporting half is built and the verifying half is absent, by structure.** The bridge parks a session Finished on a declared done-cue (introduced to remove 27 confirmation turns) and the harvest reports only what was declared; nothing between the cue and Finished reads an artifact, while the brief demands a branch - the cheapest decidable leaf (exists, has commits ahead of base), one version-control call, no model turn. Three cases: the 27-turn round (A trusts; B labels finished-unverified on an empty branch and keeps the saving), a continuation cue (unchanged), a question-ended session (unchanged - the tree already refuses to paraphrase). No receipt substrate exists (sessions are observed as terminal text), so only the leaf layer applies. Falsifier: every declared-done session in history has commits ahead. Filed as next change. Return: the history query over session rows and their branches. |
| 2026-09-02 | write-freshness-gate | mcp-tools | - | **unapplied** | - | **No managed project exposes its own write tool to a model.** The one tool server in the fleet publishes feature-matrix and harness tools; the fleet's file edits go through the coding agent's own tools, outside any project's seam. Return: a managed project that grows a write tool over shared artifacts (a document store an agent edits, a config the harness writes). |
| 2026-09-02 | lease-renewal (absent is not lost; teardown is a held state) | job-coordination | pumper | **simulation** | not-better | **The lease is a column on the job row and every write is attempt-fenced, so zero rows has one meaning.** Three cases: a live task reaped mid-run (lost under both arms; re-establish has no branch - the renewal cannot recreate the row it is the lease on); a vanished row (B's letter says lapsed and re-establish, the tree correctly fails closed - the row is the work); a store lost and restored (the flush-eviction shape cannot occur; the same loss takes the jobs with the leases). Condition written into the amendment's closing paragraph. Separate structural fact against the BASE technique: the worker discards its heartbeat's boolean, so a reaped task runs until its fenced completion is refused - the two-way channel is the few-line change owed, recorded in the project's ledger. Return: the lease moves to a store separate from the jobs table. |
| 2026-09-02 | decay-and-forgetting (a review window is not an expiry) | agent-memory | personas | **simulation** | better | **The tree's second belief store has a review pass and no per-item deadline; "aged" is a rank.** Three cases: a time-scoped high-importance fact stays out of every batch while injected (A) vs surfaces at its window (B); a stable low-access fact is re-asked every cycle (A) vs extended once (B); a synthesized insight restarts its clock at merge and preserves no source clock (A) vs inherits the newest source's creation and the earliest source deadline (B) - the merge exists and the structure argues the rule on its own. Falsifier: reviewer keep-rate on rank-selected candidates no worse than on window-selected. Filed as next change (a window column with a write-time clamp, a selector clause beside the rank, an extend action, source clocks through synthesize). Return: keep-rate over the first two review cycles under both selectors. |
| 2026-09-02 | liveness-and-heartbeats (the clock arms at first contact, not at spawn) | subprocess-lifecycle | gravity | **experiment** | better | **One ceiling timer started at spawn is the runner's startup bound, stall detector and executioner at once.** Harness over three child shapes (hang before first byte, slow cold start then work, work then hang) under three policies: a tight ceiling false-kills the working child and detects both hangs at the ceiling; a generous ceiling spares it and detects both hangs only at the ceiling; armed clocks under the generous ceiling spare it and detect the hangs at 42% and 31% of it. Filed as next change: a time-to-first-byte deadline plus an activity clock, and the `timeout` verdict split into never-connected / went-silent. Return: the runner gains the clocks; thresholds need a class profile the tree does not carry. |
| 2026-09-02 | gate-liveness (assert the oracle, not only the instrument) | quality-gates | ascent | **experiment** | better | **The coverage gate repaired its target population the day before and left the oracle at the unit runner's default.** Static census: 313 source modules imported by in-oracle tests; 0 by the end-to-end suite (it drives the served app); 8 scripts executed by unit tests through a spawn, outside the coverage include entirely - tested, no number at all, and 0% the day the include widens. B declares the oracle population and qualifies each zero. Return: browser-side coverage collection through the end-to-end runner, which would make the 490 zero-coverage files measurable. |
| 2026-09-02 | declared-deviation-register (key by identity, never by position) | conformance-checking | gravity | **simulation** | better | **Both of the tree's accepted-finding ledgers key by rule id; no position ever entered, which is what makes the either-direction mismatch rule affordable.** Three cases: an import reorder above an accepted finding (A un-matches and fails the innocent commit, B unchanged); a rule demotion paired by name across two files (A rots in both, B is one search); a deleted file carrying findings (A and B identical - the forced-look clause catches it, recorded so identity keys are not credited with it). Already adopted, nothing to ship. Return: a tool whose waiver file is position-keyed by default enters the fleet. |
| 2026-09-02 | persistence-and-migration (encoding decides the shape change; a version that covered two shapes is retired) | client-state | gravity | **simulation** | better | **The step store's shape policy is honest because its encoding is keyed, and its "every field optional" clause is held by nothing.** Three history cases: the stamp introduced over unstamped records (A and B agree); two new record types under new keys, one with a required field (A and B agree: new key, new payload); the first required field on an existing stamped interface, not yet in history (A silent - v1 covers two shapes; B a bump by rule). Filed as next change: a fifth policy rule and a type-level test that fails when a required member lands without the constant moving. Return: that test exists, or the first such field lands without it. |
| 2026-09-02 | scope-design (canonical-once amendment) | authorization | registry (self) | **experiment** | better | **`ab-paired` over four spellings of one repository address plus one control, read as the run board's SAME SOURCE exit code.** Arm A (the tree's fold: case + trailing slash): 1 of 4 caught. Arm B (fold to host + path, strip scheme / `www.` / `.git` / query): 4 of 4 caught, control distinct. **Shipped** (`run-board.mjs`, `normSource` at the SAME SOURCE comparison only; subject and path claims keep `norm`). Return: when a second identifier family (a mirror host, a fork) needs folding, which is the ledger's question and not the board's. |
| 2026-09-02 | failure-direction (half-success + wrong-minter amendment) | authorization | kp | **experiment** | not-better | **`ab-paired` over the five consumers of the shared fetch hook, predicate "renders the stale value with the error set": 0/5 under A (each consumer's own branch order), 0/5 under B (voided at the hook).** Same pixels both arms; B would violate the product's keep-last-good rule. Condition landed in the row: the void rule is for authority-bearing results, a display value beside an error is legitimate when the error renders first. Return: a counter of "error set while data non-null" on the hook (one line) would say how often the pair reaches a consumer. |
| 2026-09-02 | topology-declaration (mirror's-derived-state amendment) | sync-replication | personas | **simulation** | better | Three cases from the tree: a connector created in one process and called from another within the TTL (one retry, acceptable); a connector *deleted* while the other process is mid-run (routes to the revoked credential for up to 30 s - the revocation the bound was not sized for); a poisoned cache mutex (already handled loud). Better on case 2 alone; falsifier is a proxied-request rate that makes a per-request version read measurable. Return: that counter. |
| 2026-09-02 | lease-renewal (renewal-must-not-queue amendment) | job-coordination | tracklight | **experiment** | not-better | **`ab-paired` with the tree's own parameters (busy-timeout 5 s, TTL 120 s, renew every 40 s) on the embedded store's single serialized writer: a 6 s held write, renewal on the shared writer succeeded after 6.01 s; on a dedicated connection it failed at 5.55 s with the engine's lock error.** The source's reserved-slot remedy assumes a multi-writer pool; on a single writer it converts a survivable wait into a failure. Condition written into the technique with the bound that does apply (longest write < TTL - cadence = 80 s). Return: the server-store half (pool of 5, no reserved slot) measured through the store's own write-lock-wait meter. |
| 2026-09-02 | workspace-ancestry-isolation (author's-chair amendment) | agent-instruction-files | registry (self) | **simulation** | not-better | Three cases: this run reading the source's terms file (treated as data, canary not emitted - both techniques did their job); a visiting agent cloning the registry (no root file to read; no contributions accepted, so the terms half never occurs); the owner's own sessions under a canary (dilution tax, no signal). Precondition absent - one owner, no outside contributions - and that condition is the amendment's last paragraph. Return: a public fleet repository that accepts pull requests. |
| 2026-09-02 | design D1+D2 (metadata travels with the data; applied operations recorded) - handoff `2026-09-02-monai`, no technique slug until the forge run names one | (handoff) | pof | **task** | better | **`ab-paired` on a four-step plan's first step, on branch `intake-monai-0902-v2` (worktree, not pushed).** Seam: four motion-archive readers, none verifying the archive's frame rate or skeleton; the concatenator read `fps` from the first clip only and accepted a 30-fps + 24-fps pair as a 12-frame montage at 30 fps. Step 1 shipped: concat refuses mixed fps, mixed joint counts and missing members, and writes `skeleton`, `sources`, `applied_ops`. Same-fps pair: identical seven arrays under both arms. Mixed pair: A accepts, B refuses. Measurable: readers verifying the contract 0/4 -> 1/4 after step 1, 4/4 at step 4 (~100 lines, 5 files + 1 external edit; plan in the project's `.ai/tasks/`). Gate: the montage builder's dry run on the fixture pair; the contract-check script after step 4. Falsifier (named before the seam was touched): a montage that legitimately mixes frame rates - none in the tree. Return: steps 2-4 |
| 2026-09-02 | advisory-guard-fail-mode | session-continuation | kp | **experiment** | better | **`ab-paired` over four hook payloads (empty; a transcript path that does not exist; an empty transcript; the re-entry flag) through the doc-sync Stop hook.** A: 0 of 2 broken payloads loud (exit 0, empty stderr - a broken trigger read as a clean turn). B (the rule, copied from pumper's hook which already carried it): 2 of 2 loud at exit 3, non-blocking, surfaced to the human; controls unchanged at 0. **Shipped** `0307a014` (main, pathspec, not pushed). Condition named before the seam: the direction inverts for a hook whose instrument failure must block the stop; none in the fleet. Return: if a blocking-class Stop hook is added. |
| 2026-09-02 | advisory-guard-fail-mode | session-continuation | ascent | **experiment** | better | Same arms and payloads as the kp row; A 0/2 loud, B 2/2 loud, controls unchanged. **Shipped** `2f029039` (moonshot/wave-4, pathspec, not pushed). Ledger row in `.ai/applied.jsonl`. |
| 2026-09-02 | deprecation-by-version-arithmetic (public-name amendment) | release-pipeline | personas, gravity | **simulation** | better | Three cases from history: two internal surfaces retired with no window (correct under both policies - no external callers); one earlier row recording a fallback-retirement condition for a surface with a consumer and no clock, where the version operand has nothing to compare and the consumer term is the blocker. Return: a fleet retirement the four-condition gate would have blocked and that broke nobody. |
| 2026-09-02 | fixed-policy-amendable-plan (criterion-amendment amendment) | hitl-approval | pof | **simulation** | better | Three cases from one tree: 81 fact corrections applied against a measured predicate with the disputes retired in the same change and the grading file Director-only (the authority field); "retiring a dispute never moves a grade" (not a goal-weakening tool); the "every recorded dispute is REAL" guard that fires on read (fail closed). The tree already keeps the record the amendment describes. Return: a dispute retired without a measurement in the same change. |
| 2026-09-02 | advancement-evidence-fields (exact-version amendment) | quality-gates | ascent, pumper, kp | **simulation** | better | Three cases: nine phantom follow-ups that would have rendered as closures until "Retired" became its own state (the third state); a contract pass that outlived its run and its source because the verdict had no age (the version binding); a missing block read as a reporter gap until two truths got two fields. Return: a record where binding evidence to its version made a real reading worse. |
| 2026-09-02 | stuck-loop-detection | session-continuation | registry (self) | **task** | unmeasurable | Plan `librarian/impact/2026-09-02-harvest-stuck-loop-task.md` (3 files, ~20 lines, measurable: passes spent after the first repeated failure signature, read from harvest run reports). First step on branch `intake-omc-0902/harvest-stuck-loop` (`5bd80b6`: the harvest stop rule, skill 0.2.0 -> 0.3.0). Instrument that would measure it: the next `/harvest loop` that meets a repeated failure. Not merged; `/intake apply` continues it. |
| 2026-09-02 | continuation-as-state, single-loop-authority, ordered-teardown, compaction-checkpoint, sealed-stage-advance | session-continuation | - | unapplied | - | No managed project builds its own persistence loop, hook registry or multi-stage run; the fleet's harness is the host CLI. Return when a project grows a mode-state file, or the registry's skills do. |
| 2026-09-02 | bounded-projection-of-external-work | agent-runtime-assembly | personas | **code** | better | **Seam chosen to falsify the "one store is the truth" premise: two paired devices, no shared store.** Custody held with a twist (truth split by ownership - originator owns the outbound row, runner owns the note log - reconciled by a resume exchange keyed on contiguous seq; the loop never polls). Delivery did not hold: the runner's model-authored summary and notes were interpolated verbatim into a System-role episode on the originator. Fixed with a marked, delimiter-neutralised report block under the framing (the tree's own framing/evidence split). `ab-paired`, 3 fixtures (benign, device-prefix spoof, early-close spoof): unfenced remote directives 3 -> 0. **Shipped** `502abdc45` on master with a pathspec, hooks green, not pushed; crate does not build here (undeclared updater permission), arms ran as a standalone reduction and a unit test is committed as the falsifier's instrument. Return: `cargo test -p personas-desktop --lib companion::remote_jobs` once the permission is declared. |
| 2026-09-02 | semantic-hook-placement | agent-runtime-assembly | personas | **simulation** | not-better | **Seam chosen to falsify: a single-author closed pipeline.** Three cases (add a compiler, a cross-cutting concern, a reorder) each cost placement classes, a composer and a validator for a freedom the closed stage enum does not have - the enum is the ordering invariant. Condition written into the technique: pays only with more than one author or contributions after the host is built. Return: a per-persona hook or plugin that can wrap a stage. |
| 2026-09-02 | assembly-identity | agent-runtime-assembly | - | **unapplied** | - | No fleet tree records an assembly descriptor per run; the nearest (a design-output fingerprint for stall detection) answers a different question. Return: a per-run assembly record in any project's tracing. |
| 2026-09-02 | operator-tier-code-loading | agent-runtime-assembly | - | **unapplied** | - | No managed agent runtime loads third-party code; the one capabilities manifest in the fleet is operator-tier config for host plugins. Return: the first plugin surface in a managed agent runtime. |
| 2026-09-02 | host-routes-win | agent-runtime-assembly | - | **unapplied** | - | The fleet's services mount their own layers with stated order and reasons and accept no contributed routers. Return: a contributed router in any fleet service. |
| 2026-09-02 | checkpoint-mode-custody | agent-runtime-assembly | - | **unapplied** | - | No fleet project persists conversation state under a mode with a compatibility gate; the companion store is a belief store, not thread state. Return: a project adopting delta checkpoints or versioning its state format. |
| 2026-09-02 | lane-reconciliation (invariant-chooses-write-order amendment) | agent-memory | personas | **simulation** | not-better | Seam chosen to falsify: recall lifts vector hits only through the candidate rows, so a dangling vector is filtered before any consumer sees it and record-first delete is survivable. Three cases from the tree (hard delete with a failed reaper; archive keeping vectors in lockstep plus a bounded idempotent GC; delete-all). Condition landed in the amendment. Re-run if a surface ever serves index hits as addresses without the record join. |
| 2026-09-02 | owner-and-counterpart-scope | agent-memory | personas | **simulation** | not-better | Seam chosen to falsify: one human operator, so one counterpart. Three cases (group-scoped, team-insight, use-case-scoped memories) are all *audience* axes, not a party the memory was formed with; the technique's own exclusion holds. Re-run as a `task` when a second human reaches a persona (shared inbox, multi-user profile). |
| 2026-09-02 | read-set-bounded-links | agent-memory | personas | **simulation** | not-better | Seam chosen to falsify: the model names memory ids in free text and the reflection engine filters them against the fetched pool - validation against the read set is the same property, so the request-local int map buys nothing here. Structural fact kept: the tree's own comment records hallucinated ids as an observed case. Re-run when the model may relate two EXISTING memories rather than derive one from sources. |
| 2026-09-02 | context-hierarchy (subject: per-node-summary-tiers, seeded-descent-retrieval) | context-hierarchy | registry (self) | **task** | unmeasurable | Plan `docs/tasks/2026-09-02-context-hierarchy-descent.md` on branch `intake-openviking-0902`, first step `aa61f50`: a 14-row labeled query set with the flat slug map's baseline (8 top-3 hits, 6 misses; 3 misses are the new subject reachable only via a node abstract). Measurable: top-3 hit rate, B arm = seeded descent over node abstracts; control = a stop-word list, which must lose first. ~180 lines / 3 files. Instrument that makes it measurable: `scripts/research-map.test.mjs` over the fixture. |
| 2026-09-02 | baseline-ladder (judge-and-budget amendment) | agent-memory | - | unapplied | - | No managed project runs a model-judged benchmark (pumper's lanes are numeric bounds judged by code). Return: when a project grows an LLM-judged eval, or the registry's skill bake-off adopts a judge. The source-tree application carries the evidence. |
| 2026-09-02 | declared-process-graph (subject, 6 techniques) | declared-process-graph | - | **unapplied** | - | **No managed project declares a graph of long-lived processes joined by typed channels.** Searched the two Rust services and the spawn-capable web project for bounded inter-process channels, descriptor expansion or typed ports: none. Return: a project grows a multi-process graph with a declared descriptor, or adopts a dataflow runtime; the apply mode is then `task`, sized against the project. |
| 2026-09-02 | edge-queue-policy (subject, 6 techniques) | edge-queue-policy | - | **unapplied** | - | **No fleet tree carries a per-edge bounded queue between long-lived peers.** The nearest shapes (an LRU-bounded client pool; an in-memory span ring) are caches, not edges. Return: a project adds a bounded channel between two processes or a control/data channel that shares one bound - `control-channel-headroom` and `drop-accounting` are the first techniques to test there. |
| 2026-09-02 | fault-signal-propagation (subject, 6 techniques) | fault-signal-propagation | - | **unapplied** | - | **No consumer in the fleet is told in-band that an upstream edge went stale.** The gravity runner (v1 row, `liveness-and-heartbeats`) is the closest seam and it is one supervisor over one child, which is subprocess-lifecycle's ground, not this subject's. Return: a project with two or more cooperating processes where a consumer must degrade on an upstream's silence. |
| 2026-09-02 | data-plane-transport-selection (subject, 6 techniques) | data-plane-transport-selection | - | **unapplied** | - | **No fleet project chooses between a brokered and a direct path per message.** Return: a project introduces shared memory or a peer-to-peer channel beside an existing brokered one; `path-divergence-audit` is the technique to apply first, before any bypass ships. |
| 2026-09-02 | correlated-exchange-over-broadcast (subject, 6 techniques) | correlated-exchange-over-broadcast | - | **unapplied** | - | **No fleet project builds request/reply over a broadcast bus.** The in-app event buses in the web projects are realtime-events' shape (named facts, no answer expected). Return: a project adds a pub/sub transport between processes and needs a reply on it; `orphaned-correlation-on-peer-restart` is the first test, as a `task`. |
| 2026-09-02 | enforcement-demotion (amendment: a hook that prints prose is not a demotion) | agent-instruction-files | kp, ascent (Stop-hook replay over recorded transcripts) | experiment | **better** (for the condition-observed form, on cost) | 1,631 human turns replayed with the hook's own predicate: 122 fires (7.5%); every-prompt delivery would inject 15-25x the words and speak on 1,405 edit-free turns. The compliance side is unmeasured - return when a project adopts a per-prompt prose hook and can count what it changed. |
| 2026-09-02 | procedure-promotion (amendment: when the promoted store is the only durable store) | agent-memory | ai-registry (the harness's per-project memory store, outside git) | code | **better** | Two files on one trigger merged into one: trigger search 2 -> 1 on the same 24-item store; the technique's pairwise vocabulary lint kept four false pairs on both arms and had ranked the true pair 3rd. Return: the store passes ~50 entries, when scoping joins the door. |
| 2026-09-02 | persistent-browser-daemon | agent-browser-control | source tree (gstack clone, not a fleet project) | **task** | better | **The subject's central invariant existed as a comment.** `decideDaemonRestart` was documented "pure and exported for unit coverage" with no test, and the first test could not import its module on one platform (a load-time side effect). Moved to a side-effect-free module, re-exported, 5 of 5 actions pinned plus the budget constant; 3 files / 72 insertions on branch `intake/busy-not-dead-test` (`8f76abb`), green under the tree's runner. Plan and the remaining five-item backlog in `librarian/handoffs/2026-09-02-gstack-source-tree-task.md`. Not pushed, no PR. Return: the operator decides whether a test-only change goes upstream |
| 2026-09-02 | host-contract-compilation | agent-instruction-files | registry skills lane (simulation) | **simulation** | better | Three cases from the lane's own history: six links to one host (equal); the desktop work surface's zip delivery, which the lane has no field for (better); the trigger-collision check that a host with a description limit would need per render (better). Structural-only; written into the source-tree application. Return: the day a second host appears in the fleet, or when the desktop-surface lead fires |
| 2026-09-02 | references-over-selectors | agent-browser-control | - | unapplied | - | No connected project drives a browser from an agent. Return: a fleet project grows the seam, or the next source-tree task (backlog item 3: the ambiguous-ref case the action path does not handle) |
| 2026-09-02 | socket-scoped-surface | agent-browser-control | - | unapplied | - | No fleet seam. Return: a fleet project exposes a local daemon to a remote agent, or a second source states the socket rule |
| 2026-09-02 | effect-classed-commands | agent-browser-control | - | unapplied | - | No fleet seam. Nearest: mcp-tools/write-freshness-gate is applied in personas; return when that seam gains a command registry |
| 2026-09-02 | agent-actionable-errors | agent-browser-control | - | unapplied | - | No fleet seam. Return: the next source-tree task (backlog item 2: `wrapError` has no test pinning any rewrite) |
| 2026-09-02 | stuck-loop-detection (amendment: the interruption that leaves no signature) | session-continuation | source tree (hermes-agent clone, not a fleet project) | **task** | better | The escalation's two critical writes (restart counter; session-store save after suspension) were swallowed by bare excepts; both now warn naming what will not happen - 0/2 → 2/2 reporting. Branch `intake/escalation-arms-visibly` (`0a57be2`, 1 file / 18 lines), compile-verified; the suite could not be collected in-run (missing dependency). Patch + seven-item backlog in `librarian/handoffs/2026-09-02-hermes-agent-source-tree-task.md`; clone deleted. Return: the operator decides on upstream; personas' `restart-class-recovery` proposal is the fleet seam |
| 2026-09-02 | task-local-tenant-scope, fail-direction-follows-deployment-mode, tenant-keyed-cache-evicts-loaded-code, resolve-handles-at-call-time, stamp-ownership-before-the-router, written-inventory-of-what-stays-global | tenant-scoped-agent-runtime | - | unapplied | - | No connected project serves several tenants from one agent process (personas and kp are one-operator/one-org per install by scope). Return: a fleet project multiplexes profiles, or the source tree's backlog item 1 (opposite fail directions of the two resolvers) becomes the next source-tree task |
| 2026-09-02 | observer-and-mutator-surfaces, rewrite-before-the-gate, honest-hook-registry, session-scoped-capability | agent-runtime-assembly | pof (direction proposed), personas (direction proposed) | unapplied | - | Both projects are `candidate` absences whose scope admits the forces; proposals written to their `.ai/directions/` (pof `9aa31407`; personas `f449d7061`), human-gated. Return: a ledger row of `accepted` turns the proposal into a `task` |
| 2026-09-02 | amortized-compaction-cadence, deferred-interface-invalidation | prompt-assembly | - | unapplied | - | No fleet project owns a live transcript window (personas is not the LLM client; politicas' extraction is short-turn). Return: a project compacts a live conversation, or the peer study's T4 test (does a mid-chat persona edit zero the cached prefix on the next resume) is run |
| 2026-09-02 | corruption-class-response | embedded-db | pumper / tracklight (present, scope admits) · personas (direction proposed: store-damage-policy) | unapplied | - | Both durable-store projects are present for the subject; the technique's precondition (a derived index beside canonical structure) needs a registry-map state on the pair before a proposal. Return: `build-registry-map` marks the pair, or personas accepts its proposal |
| 2026-09-02 | webhook-ingestion (relay clause discriminator) + advisory-guard-fail-mode (safe-direction predicate) | webhook-ingestion, session-continuation | - | unapplied | - | gravity is the only candidate for webhook-ingestion and its scope excludes multi-tenant; no fleet hook registry bounds handlers. Return: a project exposes a multi-tenant relay, or adopts a hook chain with timeouts |
| 2026-09-02 | backoff-design (amendment: when the stated schedule does not fit the budget) | retry-backoff | source tree (portkey clone, not a fleet project) | **task** | better | The over-budget stop set `lastAttempt = -1`, the same sentinel as exhaustion, and dropped the stated delay; the handler now returns `skippedRetryAfterMs` and the caller logs it naming the terminal state (sentinel unchanged for consumers). Branch `intake/over-budget-wait-is-not-exhausted` (`cc20ac2`, 2 files); `tsc` error count identical to the pinned base (one pre-existing); the integration suite needs a booted gateway and was not run. Patch + backlog in `librarian/handoffs/2026-09-02-portkey-gateway-source-tree-task.md`; clone deleted. Return: the operator decides on upstream; tracklight's `retry-backoff` proposal cites pumper's `capped_retry_sleep` as the fleet reference |
| 2026-09-02 | strategy-tree-with-inherited-policy, router-versus-candidate-failure, caller-scoped-normalization-strictness, per-provider-stream-framing, adapter-direction-asymmetry, policy-verdict-in-the-status-space | multi-provider-gateway-plane | pumper (direction proposed: router-versus-candidate attribution) | unapplied | - | pumper is the structural peer (engines behind one interface); proposal `2026-09-02-multi-provider-gateway-plane.md` committed `62c0336`, human-gated. tracklight ingests rather than fronts. Return: an `accepted` ledger row, or a second gateway-shaped source for C1's second sighting |
| 2026-09-02 | allowlisted-operator-stream | browser-credential-boundary | tracklight (direction proposed) | unapplied | - | The strongest transfer in the run: self-hosted, holds provider keys, serves an operator dashboard. Proposal committed `cf64295`. Return: an `accepted` row |
| 2026-09-02 | circuit-breakers (one breaker per candidate) + backoff-design (retry-after accept-list; jitter counterexample) + storm-control (fan-in default off) | retry-backoff | tracklight (direction proposed), pumper (present; `capped_retry_sleep` is the reference) | unapplied | - | tracklight's benchmark matrix is the seam for the all-open rule; proposal committed. pumper already realizes the over-budget rule and is cited. Return: an `accepted` row, or tracklight adds a mock provider boundary (zero mock crates today) |
| 2026-09-02 | brokered-egress (priced roster) + failover-horizon (check cadence) | credential-vault, model-routing | tracklight (direction proposed for the roster); nobody for the cadence | unapplied | - | Every fleet project excludes `llm-agent/orchestration` by scope list, so the cadence paragraph is corpus-only and was sequenced last. Return: a project admits model-routing, or tracklight accepts the roster proposal |
| 2026-09-03 | liveness-proof-reclaim | job-coordination | source tree (lightrag clone, not a fleet project) | **task** | better | Dead-process reclaim was disabled off one platform in multi-worker mode with no signal - a reaper absent without saying so. It now warns once, naming the platform; behaviour unchanged. Branch `intake/reclaim-off-is-loud` (`5ecc99a`, 1 file), compile-verified; the suite could not be collected on this host (CLI-framework version mismatch). Patch + eight-item backlog in `librarian/handoffs/2026-09-03-lightrag-source-tree-task.md`; clone deleted. Return: the operator decides on upstream |
| 2026-09-03 | surface-form-identity-and-its-risk, accumulate-then-threshold-merge, recall-passes-with-a-declared-cap, recoverable-fan-out-write | llm-extracted-entity-graph | politicas (peer study; the OPPOSITE identity regime) | unapplied | - | No fleet project builds a surface-form graph; politicas builds the registry-joined one and the study is the discriminator's other side (proposal `entity-id-grammar-door` routes to the civic subject). Return: a project ingests a corpus with no identity authority, or the source loops recall passes to its cap |
| 2026-09-03 | query-decomposition-before-the-lanes | retrieval | politicas (direction proposed: `offline-path-oracle`) | unapplied | - | Missing stage landed in the golden path's pipeline; no fleet retrieval lane decomposes a query today (personas' retrieval is a KNN+FTS pair, ascent/pof list retrieval as candidates without the force). Return: an `accepted` row, or a project indexes at two abstraction levels |
| 2026-09-03 | durable-intermediate-representation | import-normalization | politicas (direction proposed: `durable-parsed-text-sidecar`) | unapplied | - | politicas' contract-dump lane already has a durable filtered IR (the study corrected the seed: three lanes, three answers); the proposal covers the other two. Return: an `accepted` row |
| 2026-09-03 | capability-declared-in-the-type, sequence-token-write-ordering, migrate-from-data-shape | data-access, migrations | - | unapplied | - | pumper and tracklight are single-backend stores; politicas already implements the data-shape decision (`pending.ts`) and pairs it with a marker - a catch, recorded in the study. Return: a fleet project grows a second backend behind one interface |
| 2026-09-03 | resource-denominated-bounds (amendment: the door below the body reader) | admission-queue | - | unapplied | - | pumper's HTTP API has size bounds; whether they sit below the body reader is a registry-map state, not a direction. Return: `build-registry-map` marks the pair |
| 2026-09-03 | allowlisted-operator-stream | browser-credential-boundary | tracklight | **task** (direction gate) | better | Branch `direction/browser-credential-boundary` (`47e4f66`): the operator API refuses to boot unauthenticated unless an explicit override names the consequence; the operator record renders six allowlisted fields, every header redacted. Gate: 223 tests pass, clippy clean. Measurable: reachable-and-unauthenticated configurations ≥1 → 0. Proposal errors recorded: the logging seam it named constructs no record; the loopback falsifier is false (compose and Helm bind all interfaces) |
| 2026-09-03 | circuit-breakers (one breaker per candidate) + backoff-design (over-budget wait) | retry-backoff | tracklight | **task** (direction gate) | better | Branch `direction/retry-backoff` (`74d8e6a`): a stated retry-after honoured inside the call budget and ending the ladder outside it as the typed terminal state; per-target health filtering the benchmark matrix with all-open degrading to trying. Gate: 205 tests pass, clippy clean; a wire-level stub instead of an async mock crate. Measurables: 429 with a 900 s retry-after 3 attempts → 1; a dead target in a 10×60 matrix 60 calls → 3 |
| 2026-09-03 | durable-intermediate-representation | import-normalization | politicas | **task** (direction gate) | better | Branch `direction/durable-parsed-text-sidecar` (`6063577`): a parsed-text sidecar beside each PDF stamped with format version, parser identity and version, and the byte fingerprint; all three must match to reuse. Gate: typecheck clean, 228 tests pass (8 new). Measurable: extractor invocations on the second ingest 23 → 0. Found: the byte cache is write-only, so the network fetch is not saved; only the parse is |
| 2026-09-03 | query-decomposition-before-the-lanes (offline path oracle) | retrieval | politicas | **task** (direction gate) | unmeasurable | Branch `direction/offline-path-oracle` (`6ef2e40`, `1c6b5e6`): a frozen query→expected-path oracle beside the sentinel, nine rows each citing a tree document, explicit `--update` that freezes failing rows only. No local store exists on this machine, so the nine rows are unevaluable here; the instrument proved itself against a scratch store (a mutated hop and a mutated citation both caught). Instrument: a local store plus one `--update` |
| 2026-09-03 | session-scoped-capability | agent-runtime-assembly | pof | **task** (direction gate) | better | Branch `direction/agent-runtime-assembly` (`7b33dba4`, `91ff7fff`): the MCP server's advertised tools resolve from the session's declared topology per request, never memoised, with the env-derived path only as a fallback and the deciding source logged. Gate: typecheck clean, 75 unit tests pass incl. two spawning the real server with the env blank; the 36 contract tests skipped without a live backend and are recorded as unproven. Measurable with the env blank: editor 50 tools, headless 46, web 46 (was 50 everywhere) |
| 2026-09-03 | router-versus-candidate-failure | multi-provider-gateway-plane | pumper | **task** (direction gate) | better | Branch `direction/multi-provider-gateway-plane` (`e569225`): an exhaustive `is_router_failure()` over the error enum (a new variant stops the build) and a second break arm in the fetch ladder. Gate: 446 tests pass, clippy clean, workspace check green. Measurable, measured by disabling the arm and re-running: engine invocations per router-caused failure 3 → 1; the error names its origin. Falsifier fired: a static census finds no producer of a router error on the ladder path today, so the arm is a guard |
| 2026-09-03 | corruption-class-response | embedded-db | personas | **task** (direction gate) | better | Branch `direction/store-damage-policy` (`80052b04a`): a quick check at open, the split on the extended result code (the vtab's own malformed report vs canonical structure), a detach branch for the search index, quarantine as a per-acquire read-only pragma at every pool site, backup rotation held while quarantined, a marker file beside the store because a flag inside a damaged database is unreadable when it matters. Gate: 4/4 new, 907 pass, 10 pre-existing failures read one by one. Measurables on a fixture: canonical writes surviving derived damage 0 → 1; writes accepted after structural damage 1 → 0; backups rotated per boot while quarantined 1 → 0. Owed: search still matches a detached index; no restore path |
| 2026-09-03 | observer-and-mutator-surfaces + rewrite-before-the-gate + honest-hook-registry | agent-runtime-assembly | personas | **task** (direction gate) | better | Branch `direction/runner-extension-surface` (`bb82910df`): observers return unit by signature; interceptors declare one mutation point and return a typed decision; the whole policy path is the continuation body, unreachable except through it; single-use continuation; a pairing test reading the two real emit-site files. Gate: check green with zero warnings; the test binary would not launch on this host (environmental, reproduced on an untouched test), so the 14 tests ran in an isolated crate reproducing the module verbatim - all pass, and swapping the rewrite/gate order turns two red. Measurable: files to add an observer 4 → 2 |
| 2026-09-03 | stuck-loop-detection (the interruption that leaves no signature) | session-continuation | personas | **task** (direction gate) | better | Branch `direction/restart-class-recovery` (`ef6f8382a`): a pure classification of `running` rows on restart, a recovery-state column with a bounded restart count, a clean-shutdown marker that fails toward the sweep, clear-on-completed in the shared status funnel. Gate: 8/8 new, 60/60 executions and migrations, 3/3 marker, desktop check clean, rustfmt clean (the pre-commit hook binary is absent on this host; its step run by hand). Measurable on a fixture in the backup's shape (2,188 rows, 74 running): failed-with-the-marker 74 → 0; returned to the queue 60; surfaced to a human 74; re-admissions bounded at 2. The tree already had a precedent (`Incomplete` from the zombie sweep) the proposal missed |
| 2026-09-03 | renew-from-arrival-not-from-work | cross-instance-cache-lease | tracklight | **experiment** | not-better | Carried to a fenced relay lease that already satisfies it. No diff to make: B is A, reported `structural-only` rather than dressed as a measurement. The tree holds TWO refinements the standard lacked and both were taken upward - liveness and progress are separate doors (a holder alive but stuck computing something to say reads as dead if they are merged), and the renewal carries a generation token the source system has none of. The forging pass had already recorded the missing fence as a deviation and refused to lower the standard; this is the second independent sighting that says the shortfall is real. Technique amended from the project, not from the source. Return when a tree merges the two doors and can measure expiries by cause |
| 2026-09-03 | renewal-beats-a-tuned-timeout | cross-instance-cache-lease | pumper | **simulation** | not-better | Independently implemented: a job heartbeat plus a stale-after reaper, and - beyond the standard - config validation that REFUSES a deployment where the stale threshold does not exceed the heartbeat interval. The source system states the 4:1 margin in prose and checks nothing. An inverse finding: the fleet project enforces the invariant the technique only describes. Return condition for promoting the check into the technique: one more independent tree that validates the ratio rather than documenting it |
| 2026-09-03 | invert-the-default-for-exposed-surface | extension-trust-boundary | pumper | **simulation** | not-better | The tree runs two extension surfaces off one directory-scan mechanism, and the network-exposed one (dynamic modules listed over HTTP) already defaults to None - discovery disabled entirely - while the executed one is bounded by fuel, memory and concurrency caps inside a sandbox. The rule fires correctly and finds nothing to change, which is what a confirmed default looks like. The residual gap is narrower than the technique: the executed surface has a global on/off, not a per-module allowlist. Return when a module set is operator-supplied rather than deployed with the binary |
| 2026-09-03 | export-terms-not-ratios | metric-surface-contract | tracklight | **experiment** | not-better | The ingest status surface already publishes shed, timeout and admitted as counters, with a source comment naming the admitted counter 'the denominator for a shed rate', and states that the counters are process-local and reset on restart. It also DECLINES a scrape endpoint on the record, as the smallest thing that answers 'are we shedding'. So the rule is satisfied and the obvious direction - add a metrics endpoint - is refuted by the tree's own stated reason. Two independent systems reaching the counters-not-a-gauge rule is the run's cleanest convergence. Return when a second consumer needs a window the fixed surface cannot express |
| 2026-09-03 | dev-variant-design (ladder amendment) | build-economics | pumper | **simulation** | better | The amendment claims the 'default to lite' frequency argument INVERTS when the variant ladder ships to a different constituency. Confirmed in the tree: the crate declares the EXPENSIVE variant as the default and library embedders opt down explicitly, for a documented linking reason. Three real cases walked - the server (wants the default), an embedding sibling project (opts down), the test harness (opts up) - and all three are decided by constituency, not by cost. Falsifier stated: a tree whose default is cheap while its dominant caller is a long-lived service would refute it. None found |
| 2026-09-03 | no-unrestorable-state-at-a-suspension-point | job-coordination | pumper | **experiment** | not-better | The technique names a regime the corpus had not modelled: work destroyed between two operations, with no notification and no reaper on its side. The tree answers it across all twelve applications that checkpoint, by a shape convention - the progress marker and the accumulators it gates live in one atomically-saved blob, so replay from the last durable snapshot re-derives both together. Arm A (the contract as prose on the checkpoint sink) 12 of 12 compliant, 0 violations; arm B (the same contract declared and checked) the same 12, 0 additional findings. An enforcement proposal against a fully compliant population detects nothing and adds a declaration obligation to twelve call sites. Structural fact: the safety is real, the compliance is total, and the mechanism is a convention the runtime cannot inspect - the sink accepts an arbitrary state value and no test asserts the shape. Return when the checkpointing population outgrows one reviewer, or when an application first checkpoints an accumulator its own marker does not gate |
| 2026-09-03 | when-the-sender-is-not-yours (cross-boundary-propagation) | tracing | tracklight | **code** | better | Branch `intake/trace-id-bound` (`4cf35ef`, 3 files, +126): a 128-character bound on `trace_id`/`span_id`/`parent_span_id` enforced in the shared ingest policy, so both front doors and both ingest paths pass it; reject, never truncate. Gate: `cargo test --workspace` all green (40 suites, 0 failed), clippy `-D warnings` clean, check clean, fmt clean; the same-morning pin on opaque ids passes unmodified. Arm A was PROVEN not asserted - a temporary negative control asserting a 100 KB id returns verbatim passed on the untouched tree, then was reverted. Measurable, caller-controlled bytes through one identifier field: 2,097,079 -> 128 on the single door (~16,000x), 8,388,522 -> 128 on the batch door (~65,000x). The asymmetry that made it a finding: the same codebase already bounded every other caller-supplied identifier (project id 64, prompt ident 128, both tested at the boundary) and had simply never pointed the discipline at the trace fields. **The tree corrected the technique**: its opaque ids are a tested product feature, so the grammar half does not transplant and the bound is the whole defence - the amendment now carves identity fields from free-form state. Limit recorded: a bound caps volume, not content |
| 2026-09-03 | recorded-interaction-fixtures | test-harness | pumper | **experiment** (peer study, 42 points) | not-better | The technique did not improve this tree; **this tree improved the technique**, which is the row's whole value. pumper's cassette layer already declares its own fidelity as a three-valued grade that is a fact about the app's code rather than the cassette (`Full`/`Partial`/`Unreplayable`), carries the grade and its reason on the result so a partial replay cannot read as full determinism, and refuses an unreplayable job at the door instead of running it live under a replay label. The technique had the seam-as-precondition rule and no way to say how much of a green was actually recorded; it now carries the grading and the refuse-rather-than-degrade rule, cited to `count-carries-predicate`. Genuine gap found in the other direction and left for its owner: cassettes are retention-exempt long-lived plain text with no sanitizer layer, storing response headers from authenticated fetches - ranked #2 in the study, not carried as this run's proposal. Return when a sanitizer layer lands, or when a second tree grades replay fidelity |
| 2026-09-03 | catalog-projection-modes + tool-identity-vs-tool-name + sanctioned-session-state | mcp-tools | pumper | **unapplied** (structural, via the peer study) | - | All three were carried against the fleet's only MCP server and none has a seam, each for a stated reason rather than for lack of looking. Catalog projection: pumper publishes 8 tools in the shipped default and 19 at most, pinned in order by its e2e suite - 8 of a shared 128 budget is a rounding error, so the mechanism the technique exists for has no force here. Tool identity: no rename-stable identifier and no telemetry keyed on tool identity, so nothing is losing correlation yet. Sanctioned session state: single-process, no affinity, no replicas. Two things the study did confirm upward - read-only gating already happens at BOTH listing and dispatch with the don't-list-and-refuse rationale written down (the source only implements it), and the run's one proposal came from this pass instead: all 19 tools advertise `additionalProperties: false` and nothing enforces it, so a typo'd argument key produces a plausible wrong answer today. Return when pumper's tool count approaches a host budget, or when it renames a published tool |
| 2026-09-03 | similarity-keyed-admission | client-fetch-cache | pumper | **simulation** | not-better | Both of the tree's severe shipped incidents in its near-dup gate happened at a CORRECT metric and a CORRECT cut-point; the defects were in what the reject arm also skipped - outbound-link enqueue in one, the sink write and revisit-cadence advance in the other - so a precision review would have returned "both knobs fine" while paginated subtrees went uncrawled and templated records froze indefinitely. Seam class: a similarity gate whose reject arm is co-located with unrelated bookkeeping. Technique amended from the project: enumerate the reject arm's side effects before tuning either knob, and lift the scope predicate into a named pure function. Clause 4 (measure at the threshold you ship) independently corroborated verbatim by the tree's own post-mortem - it had tested the gate at distance 0 and shipped 3. Return when a tree runs a similarity gate whose reject arm is already side-effect-free |
| 2026-09-03 | similarity-keyed-admission | client-fetch-cache | politicas | **experiment** | not-better | Precondition absent. The seam is a memo keyed on the first 256 chars of a statement, but the cached parse reads only those same 256 chars, so the key is exact on its own domain - precision 1.0 by construction, one knob not two, and the eviction hazard cannot arise (clear-at-cap, not recency). Harness over 145 real SQL literals, 19 of them longer than the window: 0 prefix-collision groups with divergent true keys, 0 keys that change when the window is unbounded. Seam class: derived-key memoization of a projection-scoped pure function. Technique amended from the project with the projection boundary. Do not re-run this test against that class |
| 2026-09-03 | similarity-keyed-admission | client-fetch-cache | tracklight | **experiment** | not-better | No seam: the only near-dup mechanism is a normalization equivalence class (case-fold + whitespace-collapse + digest) that explicitly rejects embeddings and edit distance with its forces recorded in the tree. Carried back as the canonicalization boundary - identity after a total function, so none of the technique's hazards transfer. Return when a resemblance key appears |
| 2026-09-03 | failover-path-liveness | model-routing | politicas | **experiment** | better | The tree declares 90 distinct named fallback sites, lint-enforced so a silent catch is impossible, and ALL 90 classify exceptional (catch to report to null). Against them: 5 liveness assertions in 2 files, 5.6% coverage. Zero ambient fallbacks at the loader boundary. An env census of 29 flags found exactly one touching a safety net and it DISABLES the gate rather than exercising the fallback. The classification step is where the value sits: 90/90 exceptional means this fallback fleet is, by construction, the class for which the project's own observability is uninformative |
| 2026-09-03 | failover-path-liveness | model-routing | pumper | **simulation** | better | Three cases from history - a dead peer of three serving a deterministic third of egress from exactly the address the fabric existed to stop using; a peer answering a profiled fetch logged-out with HTTP 200 into stored dataset revisions (the failover SUCCEEDED, so asserting the final response passes and only asserting the transition catches it); and a retryable-502 amplification costing four proxy attempts before the local ladder started. The tree's own doc comment already states the technique's core claim - both counters stay 0 when the fabric is off, because nothing is being substituted - which is a two-sighting corroboration. The technique adds the ambient/exceptional split the tree lacks |
| 2026-09-03 | failover-path-liveness | model-routing | tracklight | **simulation** | better | Three of the last fifteen commits touching the engine crate are fixes to substitute paths and none of the three added a counter, so a fourth is invisible today. Determinism is stamped BestEffort unconditionally, so the one persisted field that looks like a liveness signal cannot say whether a retry fired. Contrast case in the same tree: the revenue FX fallback IS instrumented and withdraws a capability while rows sit at it - the project owns the pattern and never applied it to the engine. Fault injection unreachable: both provider URLs are hard-coded literals with no base-URL override |
| 2026-09-03 | fallback-retirement-condition (amendment) | optional-dependency-degradation | politicas | **experiment** | better | A clean inversion witness. Applying the naive rule (retire on zero usage) to this tree retires 90 incident paths, every one of them the last line of defence for a civic-accountability surface, and KEEPS the single genuinely dead path - a capability shim that exists only so a hand-built test store keeps working. Policy A gets this tree backwards on every single row |
| 2026-09-03 | fallback-retirement-condition (amendment) | optional-dependency-degradation | pumper | **simulation** | better | Four call sites of one summariser fallback emit a byte-identical output string: two ambient (engine unavailable, no model configured, and an empty summary meaning the diff was judged immaterial) and two exceptional (unparseable prose, budget exhausted). A retirement instrument reading that share would average two retirable-on-evidence triggers with two where zero is healthy, into one number meaning neither. The tree's other fallback counter pre-empts exactly this misreading in prose at one seam and has no instrument at all at the other |
| 2026-09-03 | vocabulary-source-selection | tracing | politicas | **experiment** | better | The tree adopts the vendor ENVELOPE vocabulary and refuses the leaf vocabulary, scrubbing by value shape and never by attribute name - a case the technique did not cover. Carried back as a new section: where an unmatched attribute leaks a value rather than breaking a query, name matching is fail-open by construction and the right answer is to depend on neither vocabulary. Separately, the one instrument the project owns for borrowed-attribute skew asserts against a transitive package it does not declare, under a caret range on a different package, so the version can move without a deliberate act while the assertion still passes |
| 2026-09-03 | vocabulary-source-selection | tracing | pumper | **simulation** | better | The extraction that produced this technique is the tree's own design, written out in a module header: one in-house neutral lineage model rendered into two adopted external vocabularies. The discriminator held exactly - the project controls both ends of its own model and neither end of the two catalog specs, so the in-house layer sits where both ends are controlled and the adopted vocabularies sit at the boundary. Its golden byte-identical aspect tests are a STRONGER instrument than field-wise absence assertions: any extra, renamed or reordered key breaks them, so absence is covered for free |
| 2026-09-03 | attribute-precedence-lists (amendment) | multi-provider-event-normalization | tracklight | **experiment** | better | The run's only defect carrying a dollar figure. Two builder-side readers of the same provider's usage block, in one crate, disagree; the cost path subtracts cached from input, encoding one vendor's semantics against a provider whose input EXCLUDES cache reads, so the real input tokens saturate to zero; and the largest priced class in the tree's own fixture has no field anywhere. The same response prices at 48%, 108% and 43% of true cost depending which of three doors it entered. Borrowed-spelling coverage measured 9/40, with the three money-carrying lists at 0/9 - the inversion the amendment now warns about, and the reason it now says to audit list length against the price book rather than against the convention's table |
| 2026-09-03 | escalation-adjudication | document-text-extraction | politicas | **experiment** | better | Over the project's real cached corpus of 141 bills: 111 take the first tier, and 59 of those 111 (53%) also carry the second tier's markers - two competing structures in one document, resolved by tier order alone and never by evidence. One document carries 160 first-tier and 150 second-tier markers. A majority of the corpus reaches the adjudicator with two candidate structures and the adjudicator has no adjudication in it |
| 2026-09-03 | structure-saturation-guard | document-text-extraction | politicas | **experiment** | not-better | The saturation cut-point never plateaus over 141 real bills: 2% demoted at 0.95, 13% at 0.90, 50% at 0.80, 79% at 0.70 - a 0.10 move in the knob swings policy by 37 points of the corpus. Cause: the extracted text is the whole PDF, and a large non-operative explanatory tail dominates the largest-inter-marker-block metric on essentially every document, so the instrument measures a document-boundary artefact rather than saturation. The guard is not wrong, it is downstream of a boundary decision the tree has not made. Seam class: structural segmentation over a composite document with no operative/non-operative extraction. Positive by-product: the one bill that flips under every cut-point tried is a real shipped defect the tree had closed with a bespoke special case, so the idea holds where the instrument does not. Return when the tree gains an operative-text cutter |
| 2026-09-03 | band-calibration-by-construction | document-text-extraction | politicas | **experiment** | not-better | Already-covered form: there is no arm B. The tree's precision harness already derives three bands from constructed predicates rather than thresholding a score, and already excludes the unjudgeable class from the denominator rather than folding it into the low band, with a measured 6.3% proxy false-positive rate over 574 edges. The technique holds and is already this seam's practice. Seam class: proxy-verdict banding with an explicit unjudgeable class |
| 2026-09-03 | metric-removal-is-a-staged-pipeline | metric-surface-contract | tracklight | **task** (direction gate) | better | Branch `direction/metric-surface-contract` (`ce57202`): a deprecation policy with a three-stage release axis and an escape flag that takes the version it escapes FROM, so it expires by construction; a marker on the shared contract row that renders into both the schema document and the capabilities endpoint from one edit; and a gate diffing the served surface against a committed baseline. **Both proposal falsifiers checked before building and neither fired** - the three SDKs version independently of the server, and the capabilities endpoint answers which surfaces a backend implements, not which promises a version still keeps. The gate was proven RED first by deleting an unmarked field, then restored. Gate: check, clippy, fmt and all 40 test binaries green; the TypeScript client suite fails and was verified pre-existing by stashing the change and reproducing on the untouched base. Measurable: fields leaving the surface unmarked per release, unknown (no instrument) to 0 (produced by the gate); marked elements 1. Worker overrode the 3-file size estimate to 10 with an argument the director accepted - the marker must reach two renderings that this tree generates from one table, and collapsing it would have meant the hand-kept second list the contract crate exists to prevent. Only stage 1 machinery built; stage 2 refusal deliberately not built while nothing occupies it. Return when 0.2.0 actually removes the marked field |
| 2026-09-03 | permission-gate-vector | native-shell-integration | personas | simulation | better | Three real connector-readiness paths walked under one boolean vs a blast-radius split: the tree collapses a capability whose absence kills the feature and one whose absence only degrades it into a single readiness string, so a degrader denies the whole surface. Return when the readiness detail can be joined against actual connector usage and the killer/degrader split becomes countable |
| 2026-09-03 | capability-presence-contract | native-shell-integration | personas | simulation | better | The sidecar engine's absence on a non-primary platform is reported with the same shape as a denied grant; grant-state and presence are one axis in the tree. Return when a non-primary-platform sidecar build ships - that release is the test |
| 2026-09-03 | non-stealing-overlay | native-shell-integration | - | unapplied | unmeasurable | No always-on-top, hit-test-suppressed or transparent window anywhere in either tree; every overlay is in-DOM. Instrument that would measure it: a focus-ownership probe around a dismissed floating surface. Return when the desktop-awareness feature grows a floating capture indicator over another application |
| 2026-09-03 | layout-resolved-input-synthesis | native-shell-integration | - | unapplied | unmeasurable | Nothing synthesizes input into a foreign application in either tree; the only native input path is read-only clipboard access, and the one paste path targets the product's own surface. Instrument: a layout-swept paste harness. Return when a companion action delivers text into the externally focused application |
| 2026-09-03 | native-owned-stream | native-shell-integration | personas | simulation | not-better | The health-monitoring stream lives in a view that is not hidden and whose host bus has never been observed throttled, so relocation adds a hop, a serialization boundary and a second reconnect policy while removing no failure. Condition landed in the technique: the throttling must be demonstrated, not assumed. Seam class: a stream whose consumer is the visible view itself |
| 2026-09-03 | unattended-caller-attribution | voice-io | personas | simulation | better | Three real local-surface request paths walked with and without a producer-raised attribution surface; the tree's machine-facing speech path has no unsuppressible indication of which caller spoke. Return at the first request for a headless or scripted speech mode |
| 2026-09-03 | caller-scoped-voice-binding | voice-io | personas | simulation | better | The tree's voice resolution has no per-caller binding and no terminal error rung; an unmatched explicit id degrades silently. Return by posting a nonexistent voice id to the running local service |
| 2026-09-03 | render-acceptance | voice-io | personas | experiment | not-better | Six real renders on the installed engine, product code untouched: the two degenerate inputs that engine class can actually reach both pass the detector, and the longest legitimate internal gap across the set was 160 ms (the technique's own named false-positive case, a paced list, at 140 ms; a render at the caller's maximum accepted length at 160 ms), so an honestly tuned bound lands at >=300 ms and fires zero times in either arm. Cause: the engine predicts durations before rendering and has no stop token to miss, so the technique's generative precondition is false for the whole engine class. Condition landed in the technique. Seam class: fixed-schedule synthesis. Return by re-running the detector against an autoregressive engine's corpus |
| 2026-09-03 | change-rate-partitioning | sidecar-provisioning | personas | simulation | better | The tree's runtime engine install is one payload with file-existence as its entire freshness predicate, so a core change and a runtime change are indistinguishable and both re-fetch whole. Return when a second runtime version ships |
| 2026-09-03 | adoption-time-packaging-audit | packaging | gravity | experiment | not-better | Instrument asserted against known positives first (a linter package with three source-reading files; a compiler package that reads its own source), then swept a real closure of 326 top-level entries / 464 manifests: zero absolute-path resolutions in three of four principal dependencies, zero imported computed-name dynamic imports, priced shim cost zero. Arm B predicted nothing the existing post-hoc bundle check had not caught. Cause: the packaging mode is not a source-stripping freeze, so most of the enumerated constructs have no exposure. Condition landed in the technique. Seam class: non-freezing bundler. Return when the repo adopts a dependency under a freeze transform |
| 2026-09-03 | unsatisfiable-set-empirical-gate | dependency-declaration | gravity | simulation | not-better | The repo's consistency gate is green and no two mutually exclusive published bounds could be named; the bypass would have removed a working guarantee for a weaker one. Condition landed in the technique as an entry test - name the two packages and versions that cannot both be satisfied, or the set is satisfiable and this does not apply. Seam class: satisfiable constraint set. Return when two exclusive upstream constraints can be named |
| 2026-09-03 | signing-and-trust (attestation-does-not-nest) | packaging | personas | simulation | better | The release workflow's attestation enumeration covers the payload and not every wrapper it produces; walked against three real release-asset shapes. Return by running the widened enumeration on the next release |
| 2026-09-03 | deletion-blocked-until-dependents-confirm | declarative-resource-lifecycle | source tree (kube-rs clone, not a fleet project) | **task** | better | The marker-removal patch tested only the marker's index, so a delete/recreate/re-mark race under one name removed the NEW record's marker. Patch construction factored into two pure functions; a `test` on the record's minted identity prepended when present; four unit tests where the module had none. `cargo test -p kube-runtime finalizer` 4 passed, clippy clean under pedantic deny. Branch `intake/deletion-marker-uid-precondition` (`b839c0a`, 1 file, +147/-51), patch + backlog in `librarian/handoffs/2026-09-03-kube-rs-source-tree-task.md`; clone deleted. Return: the operator decides on upstream (needs nightly rustfmt) |
| 2026-09-03 | cross-source-precedence-chain, parse-failure-keeps-identity, told-that-not-why, keyed-queue-with-earliest-wins, per-key-exclusion-under-a-global-cap, error-policy-as-a-separate-function, drain-a-derived-queue, desync-is-a-state, atomic-swap-at-initial-sync, completeness-barrier-with-a-warm-queue, one-stream-fanned-out, initial-read-strategy-behind-a-gate, ownership-edges-that-enqueue-the-parent, per-field-write-ownership, synchronous-gate-before-persistence | settings, error-handling, convergence-loop-and-requeue, watch-cache-and-resync, declarative-resource-lifecycle | - | **unapplied** (source-tree applications only; witnessed at `rust@1.89`) | - | No fleet project runs a control plane, replicates a remote change log, or boots against several partial config sources; the nearest seams are pumper's scheduler (`keyed-queue-with-earliest-wins`, proposals `requeue-policy-function` and `target-key-exclusion` waiting at the gate), personas' admission queue (`admission-key-exclusion`), and both projects' error classification (`parse-failure-keeps-identity`; proposals `error-source-chains`, `typed-failure-classification`). Applied when the gate accepts |
| 2026-09-03 | price-tables (a published end date is not staleness; a computed key outlives its number) | cost-metering | ascent, systedo-case | **code** | better | ab-paired. A: move the model constant alone. ascent's shipped-defaults test fails `no built-in rate for default model` (1 failure, predicted verbatim by its own comment); systedo's model-pin gate reports 1 finding naming the constant. B: add the rate row, extend the dated promo guard to cover both rows on the shared expiry, correct the computed-key rate. Green both sides (45 assertions / full suite 2176; 8/8 pin tests, pin check clean). The instructive half is what A did NOT fail - systedo's rate-row existence test stayed green while the rate under a computed key still described the previous model, understating it 10x (0.075/0.3 against 0.75/3.75). Return: when a third project books the same promotion, check whether the current-spend vs cross-model-comparison split still predicts which rate it carries |
| 2026-09-03 | effort-calibration (a version successor is a re-measure trigger) | model-routing | gravitone-gcloud | **task** | unmeasurable | The tree had already refused this bump in writing - a dated live-roster measurement, the newest model excluded on a 503-under-load property, and the promotion rule "not by assuming it settled" in the file header. It had also already measured the term the release note describes: hidden reasoning at 12.8x the visible answer on a mid-tier flash, 25.1x on a larger model, billed as output. Landed a priced-but-not-promoted rate row and the pass the promotion owes, with the two questions it must answer. Instrument that would resolve it: one live roster pass with the repo's own key, which the tree already knows how to run - third-party spend this run had no standing to authorize. Return: when that pass runs, or when a fleet project gains a scheduled re-measure trigger rather than a manual one |
| 2026-09-03 | cache-value-not-handle | concurrency-guards | ai-registry | **code** | better | ab-paired, 5 fixtures / 2 arms / 1 predicate ("did the sibling get the lock?"). The run board's lock reclaim decided breakability from the lock's own age alone and never read the holder's run record, which the same board maintains with a heartbeat - wrong in both directions: a live, heartbeating session holding the index lock past its 900s ttl had the lock broken by a sibling (both then regenerate over each other's half-written files), and a session that died one second after acquiring held it for the remaining 899. B makes reclaim a judgement about the owner (`ownerGone \|\| pastTtlAndQuiet`), preserving the wedged-owner backstop. A 3/5, B 5/5, three controls byte-identical. An earlier B draft was WORSE than A for a live run holding a short lock without beating; the fifth control caught it. Return: when a hold needs a real owner-side timeout - the reclaim deadline currently sits above an owner timeout that was never written down |
| 2026-09-03 | post-merge-regeneration | codegen | ai-registry | **simulation** | better | structural-only. Three real cases from `librarian/sources/index.md`, not invented: three separate runs each regenerated the derived index and then left it uncommitted because it had absorbed a sibling's in-flight files. The policy does not transplant unchanged - this repo commits straight to trunk and has no merge point - but the adaptation is already present by accident: ~8 of the last 60 commits touching the artifacts are generated-only, produced ad hoc by whichever session noticed the collision, with no rule naming it. Return: build the counter that would settle it (how often a committed index described a tree no session had); it is a smaller job than adopting the policy |
| 2026-09-03 | truncated-verdict-space | hitl-approval | kp | **simulation** | not-better | structural-only, and the most useful row of the run. kp truncates its automated verdict space on the ADVERSE side - a fairness-cleared reject is downgraded to a hold and routed to the human gate ("the pass no longer produces a rejection"), while the admitting verdict is auto-applied with actor `system`. The technique as landed described the mirror image. Walked over three real entry shapes, applying it as written would move the human gate onto the reversible direction (a wrongly advanced subject meets the next gated stage anyway) and leave the irreversible one automated - the exact inversion the technique exists to prevent. Condition landed as an amendment: polarity is not part of the mechanism; truncate the verdict the system cannot take back. kp also contributed a second mechanism the technique lacked - re-derive the truncation at the APPLY boundary, independently of the decider, and count the downgrades. Return: when a fleet project gains a count of automated advances a later human stage reversed, the walk becomes a measurement |
| 2026-09-03 | two-tier-extension-format, isolation-tier-independent-extension-api, pluggable-isolation-runner, canonicalizable-privilege-declaration, grant-change-consent, declared-schema-extension-storage, per-callback-failure-policy, origin-signed-record-with-index-as-cache, verdict-bound-to-the-exact-revision, split-admit-state-and-redact-authority, fail-closed-trust-dependency, signal-that-only-subtracts, publisher-declared-verification-floor | untrusted-extension-host, decentralized-artifact-distribution | - | **unapplied** (source-tree applications only, 6 of them; witnessed at `node@22.16`) | - | No managed project hosts third-party code or distributes artifacts to strangers - the forces, not the features, are absent fleet-wide. Return: when a fleet project grows an extension surface, or ships to an audience it does not control |
| 2026-09-03 | operator-defined-tables, localized-record-identity, expand-deploy-contract, dynamic-table-set-migrations, counted-set-snapshot, measure-apply-split, context-starved-executor | data-access, migrations, quality-gates, ci-execution-trust, test-harness | - | **unapplied** | - | No fleet project runs a rolling deploy over an operator-defined schema; the fleet is single-owner with no external contributors, so the untrusted-measurement force is absent. `localized-record-identity` was checked against kp and is unapplied for a real reason: kp is multi-locale but its CONTENT is single-language (it stores a recipient's locale on their record, `edge/schema.sql` has no locale column). Its `comms-translator` independently draws the same boundary the technique's decision rule names - convergence, not adoption, and cited into the technique. Return: when a project stores a content record that exists in more than one language |
| 2026-09-03 | liveness-and-readiness split (health-checks: one process, two answers) | health-checks | tracklight | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`549a15d`): `/health/live` takes no state, `/health/ready` round-trips the store, chart gains a startup probe; readiness green on a severed store 8/8 -> 0/8, liveness stays green. cargo test workspace 36/36, clippy -D warnings clean. Merged `8b2d09e` |
| 2026-09-03 | chart-policy-gate with must-fail fixtures (deployment-contract) | deployment-contract | tracklight | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`cfd71ae`): a Rust test-driven chart policy guard (10 policies, 17 must-fail fixtures) wired into CI; chart at branch point 6 of 10 red -> 0 of 10, every red fixed in the chart. Merged `8b2d09e` |
| 2026-09-03 | parse-failure-keeps-identity + structured classification (error-handling) | error-handling | tracklight | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`a63d7a7`): provider failure class carried in event metadata from the ingest boundary, responder stops re-deriving it; wrong verdicts from message-reading 8/20 -> 0/20, fallback rate 1.0 -> 0.0, counters at GET /classification. Merged `8b2d09e` |
| 2026-09-03 | liveness-and-readiness split (health-checks) | health-checks | kp | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`ad6c4449`): readiness at /api/health, liveness a socket probe, startup probe added, the no-probes policy requires distinct endpoints; probes observing the database 0/2 -> 1/2, test:deploy 20 -> 21. Container-with-unwritable-/data arm not run (no local image). Merged `cb5aec93` |
| 2026-09-03 | deployment-config-as-code: read every template (deployment-contract) | deployment-contract | kp | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`0d1b8955`): check-chart reads all templates (5 -> 9) with an unreviewed-template policy in both directions, a pod-identity policy and a ServiceAccount with the token unmounted; a rogue template exit 0 -> exit 1. test:deploy 21 -> 23. Merged `cb5aec93` |
| 2026-09-03 | disruption budget + rotation checksum (deployment-contract hardening) | deployment-contract | kp | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`9411a8cc`): PodDisruptionBudget minAvailable 1 and a secret checksum annotation; rotated secret gives a byte-identical pod template before, a rolled pod after; policies 12 -> 16 each with a must-fail fixture, test:deploy 25. Drain not run (no cluster). Merged `cb5aec93` |
| 2026-09-03 | per-key-exclusion-under-a-global-cap (concurrency-guards: target key) | concurrency-guards | pumper | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`c60900d`): one in-flight run per target key in the claim statement, all six enqueue doors mint the key, a held-by-target gauge; peak concurrent runs per target 2 -> 1, different targets and NULL keys still peak 2. cargo test workspace green, clippy zero warnings. Merged `3f1b291` |
| 2026-09-03 | error-policy-as-a-separate-function (convergence-loop-and-requeue) | convergence-loop-and-requeue | pumper | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`515b06c`): requeue_after(error, attempts, stated) replaces the ladder inside Storage::fail; engine calls against a Retry-After 600 origin in one minute 3 -> 1, attempts on a config error 3 -> 1, default arm pinned byte-identical. Stated wait only reaches raw-HTTP apps (fetcher drops it) - recorded. Merged `3f1b291` |
| 2026-09-03 | structured-propagation: typed causes (error-handling) | error-handling | pumper | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`9b15ca1`): Http/Browser/Parse/Config carry a typed cause with a guard test; flattening sites in the governed crates 18 -> 3 (all pinned, one !Send); the study's 186 was workspace-wide. Merged `3f1b291` |
| 2026-09-03 | per-key-exclusion-under-a-global-cap (concurrency-guards: admission key) | concurrency-guards | personas | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`692acccab`): admit refuses a second in-flight run for one (persona, schedule source) before the depth verdict; concurrent runs per key 2 -> 1, different triggers and keyless operator runs still both Running; only schedule fires are keyed (webhook/polling carry no event identity). engine 1065/0. Merged `df5390adc` |
| 2026-09-03 | parse-failure-keeps-identity: class minted at the raise site (error-handling) | error-handling | personas | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`081fb9acd`): failure class column written at three minted raise sites, readers stop sniffing prose, migration e18; minted sites carrying a class 0/3 -> 3/3. The 40-of-43 replay is unmeasurable here (the named backup is not on this machine) and stays the proposal's claim. Merged `df5390adc` |
| 2026-09-03 | chart-policy-gate with must-fail fixtures (deployment-contract) | deployment-contract | gravitone | **task** (direction gate, kube-rs round, after onboarding) | better | Branch `direction/kube-rs-round5` (`fb64b9b`): ten text-level policies over the chart with a fixture per policy; chart at branch point 8 findings in 5 policies -> 0, fixtures 13/13; wired into the service CI job. The director executed it (the worker lane was overloaded) |
| 2026-09-03 | disruption budget + rotation checksum (deployment-contract hardening) | deployment-contract | gravitone | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`99baeca`): PodDisruptionBudget minAvailable 1 and a secret checksum annotation; pod template identical across rotation before, rolls after; PDB objects 0 -> 1 |
| 2026-09-03 | fleet-wide admission signal (admission-queue) | admission-queue | gravitone | **task** (direction gate, kube-rs round) | better | Branch `direction/kube-rs-round5` (`5e47813`): /metrics gains a fleet block summed over the headless peers Service and the scaler reads fleet.queued instead of one pod's sample; 5 unit tests, a silent peer counted missing never zero; the ten-poll flip needs a cluster and stays the proposal's claim |
| 2026-09-03 | liveness-and-heartbeats (late-tick amendment) | subprocess-lifecycle | personas | **code** (intake-chatterino2) | better | ab-paired, vitest fake timers: A 2 failed / 1 passed (entered reconnect on one late failure), B 3/3 (re-probes once after a 3 s grace; a second failure still reconnects; on-time failure untouched). Branch `apply/late-tick-skip` (`3cb8e9c`), merged `--no-ff` into master (`4382980`) after tsc, eslint and lefthook ran green. Revisit if the grace masks a real outage in the field. |
| 2026-09-03 | approval-snapshots-with-guarded-update | test-harness | pof | **experiment** (intake-chatterino2) | unmeasurable | 17 of 17 snapshot-touching commits also changed source, 0 snapshot-only; no gate sees a snapshot change and there is no CI workflow. Instrument: a pre-commit or CI check that fails on a marker-less `__snapshots__` change, counted over the next 20 snapshot commits. Row committed `ae704fb`. |
| 2026-09-03 | config-backup-and-restore | settings | personas | **simulation** (intake-chatterino2) | better | Three cases from the tree (the migration branch that tolerates residue for want of a restore path; canonical damage at boot; the founding bad-migration case). Direction proposal written and committed (`.ai/directions/2026-09-03-backup-restore-surface.md`, status proposed). Return: when accepted and the fixture-damaged boot reaches the restore state, re-grade as task. |
| 2026-09-03 | applied-defaults-ledger | settings | personas | **simulation** (intake-chatterino2) | **not-better** | Idempotent replays over structure already own the property and add a per-boot re-check the ledger would skip; seam class: schema migrations as idempotent replays. Technique gained the boundary section "When a replay already owns the property". Return: when the project ships a user-overridable default set (shortcut rebinding, presets); the persisted UI store is then the seam. |
| 2026-09-03 | per-callback-failure-policy (uniform non-fatal amendment) | untrusted-extension-host | personas | **simulation** (intake-chatterino2) | better | Observers are non-fatal by signature (panic guard at emit), interceptors keep per-registration refusal; the mute is not owed while observers cannot emit. Return: when observers can be supplied from outside the tree or call back into the runner. |
| 2026-09-03 | crash-capture (restart flag outside the store amendment) | error-handling | personas | **simulation** (intake-chatterino2) | better | The crash/quit discriminator is already a marker file beside the store; no auto-restart exists to carry a policy. Return: when restart-class-recovery grows an auto-restart, the marker file is the seam and this is re-graded as code. |
| 2026-09-03 | capability-subtraction-sandbox | untrusted-extension-host | - | unapplied (intake-chatterino2) | - | No fleet project loads third-party code into its own process through a scripting runtime; the one extension surface (personas) is in-tree compiled code. Return: when a project grows an in-process scripting surface. |
| 2026-09-03 | safe-mode-registration | untrusted-extension-host | - | unapplied (intake-chatterino2) | - | Same seam absence; the direction candidate for personas was not proposed because its falsifier fired (extensions cannot break boot). Return: when a fleet desktop app loads operator- or third-party-supplied code at boot. |
| 2026-09-03 | capacity-packed-subscription-pool | realtime-events | - | unapplied (intake-chatterino2) | - | No fleet project multiplexes topic subscriptions over push connections with a per-connection cap. Return: when one does (a fleet feed over a capped websocket API). |
| 2026-09-03 | immutable-model-cached-layout | chat-transcript | - | unapplied (intake-chatterino2) | - | Fleet transcripts are reconciled by a UI framework, not painted; the technique's forces (one message in several panes, layout on the UI thread) are absent. Return: when a transcript surface paints its own rows or shares one message model across panes. |
| 2026-09-03 | virtual-filtered-channel | chat-transcript | - | unapplied (intake-chatterino2) | - | No fleet transcript shows one channel under two filters at once. Return: when a fleet feed grows a filtered or merged view that must outlive the source's retention. |
| 2026-09-03 | typed-filter-language | search | - | unapplied (intake-chatterino2) | - | No fleet project has a user-authored filter expression language (fleet filters are structured UI facets or SQL behind a safe-mode guard). Return: when one grows a text filter language over a hot path. |
| 2026-09-03 | subscription-lifecycle (synchronous re-entry amendment) | realtime-events | - | unapplied (intake-chatterino2) | - | Fleet event buses dispatch asynchronously; the deliberately synchronous transforming subscriber has no instance. Return: when a synchronous hook surface that can publish lands (personas' runner observers cannot emit today). |
| 2026-09-03 | live-prepend (cursorless catch-up amendment) | feed | - | unapplied (intake-chatterino2) | - | No cursorless catch-up seam located in the fleet this run (the cloud event stream was not read). Return: when a fleet feed reconnects to a transport with no cursor, or the next run reads personas' cloud worker stream. |
| 2026-09-03 | guard-input-custody | agent-runtime-assembly | ai-registry (self) | code | better | ab-paired on the repo's own purity gate: planted stack identifier failed (1 violation, exit 1) with the selector present, passed green (0 violations, exit 0) with one frontmatter line deleted; after the change both fail. Custody of the domain-to-profile mapping moved into `check-bundles.mjs`; the bundle index key must now agree with it and absence fails. Controls: no behaviour change when the declaration is correct, no false positives across 8 bundles. Ceiling: enforcement and governed content still share a trust domain - only CI holds the committed state at a boundary contributors cannot write. |
| 2026-09-03 | selection-over-noise | ai-registry (self, `skills/dojo`) | code | better | ab-paired on the loop's own recorded ledger geometry (4 gated cycles, 3/6/3/3 = 15 pairs, judges disagreed on 6; accuracy calibrated from the ledger's mean agreement 0.625 -> p=0.75 exactly). Under the null that both judges are equally good, the written rule ("one judge leads in >= 3 of 4 gated cycles") pins a winner 22.8% of the time; adding a resolution precondition on the aggregate margin takes it to 5.8%. 200k trials, deterministic seed, instrument asserted first (perfect vs coin-flip trips the rule 95.1%). Both conditions shipped into `judge-protocol.md`; dojo 1.1.0 -> 1.2.0. Structural fact the arithmetic did not carry: retiring the loser destroys `gemini_agreement`, the only between-judge instrument that could reveal a bad pin, so a close pin now keeps the loser on a sampled fraction instead. Return: when the ledger passes ~40 discriminating pairs, re-read whether the precondition still binds. |
| 2026-09-03 | monitor-inside-the-loop | personas | experiment | better | ab-paired on 15 planted cases through the project's real predicates, imported unmodified: 11/15 graded as constructed, 4 misgrades, every one a false positive, all from `contains_value`'s head-token substring fallback ("We run Postgres 14" grades as containing `Postgres 16`; "Redis was removed" as containing `Redis 7`). All four land on the superseded/expired/distractor probes the suite exists to measure, and a false positive marks a wrong answer correct. Arm A was the repo as found - no calibration set anywhere under `evals/`, grader recall unmeasured and reported as nothing. Shipped `memory_year/checks/judge_calibration.py` in the package's own idiom on branch `apply/judge-calibration` (foreign WIP in that directory), not pushed; it lands red on purpose and the predicate was deliberately left alone, because narrowing it changes grading across every cached score. Return: when a key and budget are available, plant cases for the model grader at `judge.py:113-114` - the half whose recall the technique most wants measured is still unmeasured. |
| 2026-09-03 | guard-input-custody | agent-runtime-assembly | personas (evals/memory-year) | simulation | not-better | Confirmation, not refutation - do NOT count toward the two-rows-demote rule. The harness's confinement is an injected clock that cannot be placed out of reach (backends load into its own process), so the tree refused to call the convention a guard and built `checks/clock_purity.py`: replay one scenario at two base dates, require identical rendered recall, normalising ids and instants first. The technique gained a section from this seam - "when placement is impossible, detect the escape differentially" - with the normalisation condition and the honest limit. Structural only; falsifier stated in the application (introduce a clock-reading backend and confirm the check fails). |
| 2026-09-03 | scenario-design (seeded-environment amendment) | eval-harness | personas (evals/memory-year) | simulation | not-better | Confirmation, not refutation. A fully fabricated world where every fact has ground truth - the exact shape the amendment warns about - that does not seed the stage away: expired facts generate an `expired` probe class with gold UNKNOWN, the consumer prompt permits abstention on purpose, the judge carries a distinct verdict for stating a superseded value, and the class reaches the scored timeline. The UNKNOWN filter in `run.py` is scoped to unaided-baseline screening, where excluding it is what keeps the class in the score; recorded because that is the cheap misreading. Return condition: a coverage count per probe class would say whether the class is large enough to move a ranking. |
| 2026-09-03 | config-backup-and-restore (accepted direction, Phase 7.7) | settings | personas | task | better | Branch `direction/backup-restore-surface` @ `c14d0edf4`, **merged 2026-09-04** as `7b263292f` (--no-ff) after the sessions holding `db/src/lib.rs` and `damage.rs` committed; it was held one day under Phase 8's other-session exception rather than merged green on the day, which was the right call - master gained 10 commits in between, including a damage.rs rewrite the branch interacts with. **The gate was re-run on the merged tree, not inherited**: 924 passed / 13 failed against master's own 933-test suite failing the same 13, so +4 passing and 0 new; the one failure the branch report had not listed was confirmed pre-existing on master in a separate worktree first. Both conflicts were append-only JSONL and resolved by union - master's simulation row (another session predicted the change would help) now sits beside this task row, and all three acceptance rows survive including that session's correction that the branch predated its run. Gate: `cargo test -p personas-db --lib` 919 passed / 13 failed vs a stashed-tree baseline of 915 / 13 - same 13 keychain-environment failures both runs, delta +4 passing, 0 new. Mutation-checked: neutering `apply_pending_restore` turns the main test red, so the gate measures the change rather than its compilation. Three worker overrides, all accepted on review: restore is deferred to the next boot via a marker (a pool of twelve is already open by the time any surface returns), the proposal's "failed init_db has nowhere to send the operator" was half wrong (init_db returns Ok with a read-only pool on quarantine, and header damage fails earlier in r2d2 than the quarantine path), and no UI was built. Return condition: the offer reaches a human only through the boot log and a Rust call - the dialog is a frontend-shaped follow-on direction, and this is its precondition. |
| 2026-09-03 | k-anonymity-cases-and-sources (+ strict-ingestion-lenient-consumption's operator obligation) | federated-benchmark-sharing | tracklight | code | better | ab-paired on the project's own suite. The floor's *enforcement* half was correct and its *disclosure* half absent, while the consent envelope beside it (`projects_included`/`projects_excluded`) was disclosed properly - which is what makes it an oversight rather than a decision. Arm A: a 3-case bucket at floor 5 and an empty store both return `entries: []` with no other output - byte-identical, so "held back" and "nobody measured" are indistinguishable. Arm B: the same two inputs return `buckets_withheld` 1 and 0; a mixed input (6-case + 2-case, floor 5) returns 1 entry + withheld 1, confirming the count reports suppression rather than totals. Distinguishable states 0/2 -> 2/2. Gate `cargo test --workspace` green, all 20 suites, none moved. The load-bearing decision is what the count is NOT part of: `digest_sha256` hashes an explicit field list, and adding it would make every existing instance re-push unchanged data once - a second test pins that two digests differing only in the count hash identically. Commit `384b713` on `main`. Return: no surface renders the field yet (API only, no CLI/UI), and if a hub ever needs to distinguish two contributions whose entries match but whose suppression differed, the count enters the hash and everyone re-pushes once. |
| 2026-09-03 | join-breadth-follows-the-wrong-match-cost | multi-provider-gateway-plane | tracklight | code | unmeasurable | Confirmation from an independent tree, not a change - do NOT count toward the two-rows-demote rule. The seam is `crates/core/src/collective/aliases.rs:1-13`, and it already holds the technique's conservative setting with its own argument written out: "an identity absent from the declared table passes through unchanged ... and rows never merge on *family* - a leaderboard that merged `openrouter` into `anthropic` would publish a number nobody measured." Reached from the unwindability axis (a silent false merge in shared data cannot be undone by any single contributor) rather than from the technique's cost-direction axis, which is why the two arguments are worth keeping side by side. No arm to compare: measuring would mean reverting the project's existing behaviour, which is not warranted. Return: when a *presence*-shaped join appears in this tree - the endpoint-identity direction accepted this run is exactly that surface, and its rows will be the first ones where breadth is a live choice. |
| 2026-09-03 | content-addressed-contribution | federated-benchmark-sharing | tracklight | code | unmeasurable | Confirmation, not a change. `crates/core/src/collective/contribution.rs:93` already gates a repeat push on `digest_sha256` of the digest body - "the gate a repeat push is skipped by" - so the idempotency property the technique derives from content addressing is present, reached independently. The technique's *naming* half (a path derived from content, so concurrent contributors cannot collide and an open proposal is extended rather than duplicated) does not apply here: this federation posts to a hub endpoint rather than proposing changes to a shared store, so there is no path to derive. Return: if this project ever gains a store-based contribution transport, the naming half becomes live and the duplicate-path policy the technique asks for is the thing to write first. |
| 2026-09-03 | refuse-rather-than-emit-a-sentinel | modelled-performance-estimates | tracklight | code | unmeasurable | Confirmation from an independent tree of a technique forged this run. `crates/core/src/collective/merge.rs:10-15`: when fewer than half a row's cases carry a known variance, "no CI is published at all (an honest 'insufficient variance data' marker) rather than a fabricated number", and a null variance means "no interval", never a synthesised one. That is the technique's domain test applied to an interval rather than to a rate, and it was reached without the technique. No arm: the alternative is fabricating intervals. Return: the technique's *provenance ladder* half is not confirmed here - this tree's merged rows carry spread and a low-confidence flag but no evidence-class field distinguishing a measured input from a modelled one, which is the endpoint-identity direction's territory. |
| 2026-09-03 | absent-degrades-malformed-fails-fast (third-state amendment) | optional-dependency-degradation | personas | code | unmeasurable | Confirmation of the amendment's first half only. `src-tauri/core/src/url_safety.rs:428` and `src-tauri/engine/src/healthcheck.rs:1622` both reject `http://0.0.0.0/` outright, and `src-tauri/src/commands/fleet/pairing.rs:196` filters `ip != "0.0.0.0"` when choosing an address to advertise - so "a wildcard bind is never a connect target" is already enforced, in a tree that faces it for real over LAN pairing. The amendment's *second* half - that a fallback attaches to a defaulted value and never to an operator-supplied one - was not checked in any tree this run and remains unapplied. Return: grep the three authorized trees for an env-var read whose explicit value falls back on failure; that is a small, mechanical hunt and it was cut for run budget, not for merit. |
| 2026-09-03 | upstream-identity-before-inventory | multi-provider-gateway-plane | tracklight | task | better | Accepted direction, Phase 7.7. The seam is benchmark target resolution: a self-hosted OpenAI-compatible endpoint reaches the collective merge under a provider string the operator typed, which is the family-merge `aliases.rs` refuses deliberately, arriving through an unguarded door. Proposal `.ai/directions/2026-09-03-upstream-endpoint-identity.md`, accepted at the operator's multi-select gate, executed in-session by an Opus worker on worktree branch `direction/endpoint-identity`. See the branch's own `.ai/applied.jsonl` row for the gate output, the measure, and which of the proposal's three falsifiers fired. Return: falsifier 1 is a usage question the tree cannot answer and the owner did not answer at the gate - whether anyone benchmarks self-hosted endpoints at all decides whether this direction was worth its size. |
| 2026-09-03 | named-members-over-a-uniform-collection | multi-provider-gateway-plane | (none) | unapplied | - | No seam in the three authorized trees. The technique governs an *inventory* fan-out over a heterogeneous adapter set whose members are consumed by different match rules and carry different failure costs; tracklight fans out over providers on the request path (where the technique says a uniform collection is correct), and pumper's engine set is pluggable but single-purpose. Return: when a fleet project grows a second inventory source for one concern - the shape is two sets from one upstream that must not be merged because one is matched by substring and the other by equality. |
| 2026-09-03 | capture-locally-publish-separately | federated-benchmark-sharing | (none) | unapplied | - | tracklight already splits capture from publication (runs land in its store; `POST /contribute` is a separate verb over them), so the technique's timing rule is satisfied structurally and there is nothing to test. Not recorded as a confirmation row because the split predates any contribution feature and was not a decision made against these forces. Return: when a fleet project adds an opt-in export whose decline path is destructive. |
| 2026-09-03 | strict-ingestion-lenient-consumption | federated-benchmark-sharing | (none, beyond the k-anonymity row) | unapplied | - | The technique's *asymmetry* half has no seam in the authorized trees: none of the three compiles pooled third-party data into a build artifact, which is the stage where leniency is owed. Its *operator obligation* half is what the tracklight code row above delivers. Return: when a fleet project embeds contributed data at build time - the question to ask is whether one bad merged row can fail everyone's build. |
| 2026-09-03 | scoped-calibration-fallback / one-ratio-then-a-capability-cap / provenance-travels-with-the-value | modelled-performance-estimates | (none) | unapplied | - | The fleet map classifies this subject `out-of-scope` for all three authorized projects, and correctly: none of them publishes a performance number derived from a model of the hardware it runs on. This is a fact about the fleet rather than a miss. Return: if `gravitone` (an Arm TTS/STT service that sizes work against real hardware) is ever authorized, it is the one tree in the fleet whose forces match, and `one-ratio-then-a-capability-cap` is the technique to try first. |
| 2026-09-03 | fallback-retirement-condition (least-tested-path amendment) | optional-dependency-degradation | (none) | unapplied | - | The amendment predicts that within a family of alternative paths the fallback carries the weakest tests, and gives an inspection signature (siblings that take an injectable root vs one that reaches for the live path inline). Checking it costs a grep per family and was cut for run budget. Return: run the signature over the three authorized trees' capability probes - it is the cheapest apply owed by this run and it is owed. |
| 2026-09-03 | provider-family-matching (third-posture amendment) | multi-provider-event-normalization | (none) | unapplied | - | The amendment tells a reader to check what a family matcher's output *claims* before reusing it. tracklight's matcher feeds a priced total and is already in the correct posture (see the join-breadth row above), so the amendment's warning has nothing to catch there yet. Return: the endpoint-identity direction accepted this run introduces the first presence-shaped identity in that tree; re-read the amendment against it once that branch merges. |
| 2026-09-03 | model-based-oracle (normalise-then-round-trip amendment) | test-input-generation | (none) | unapplied | - | No managed project feeds a structural generator into a rejecting consumer; the fleet's round trips are network stubs and serialisation checks, not print-parse fixpoints. Return: when a project grows a generator over its own syntax - the personas execution lab or the pumper plugin envelope are the likeliest seams. |
| 2026-09-03 | object-shape-representation / engine-string-representation / embedded-tracing-collector / register-bytecode-execution / guest-execution-bounding / engine-host-contract / native-guest-interop / standards-layered-runtime / guest-language-introspection (9 subjects, forge wave forge-boa-0903) | backend-platform/language-runtime | (none) | unapplied | - | No fleet project embeds a language runtime; the nearest seam is pumper's WASM plugin host, which is a host over a foreign engine, not an interpreter with its own object model, and it was applied against the security amendment instead. Return: when a project embeds an interpreter or grows a plugin runtime with its own value representation - then `guest-execution-bounding` and `engine-host-contract` are the first two to apply, in that order. |
| 2026-09-03 | endpoint-sealed-continuation-metadata | prompt-assembly | (none) | unapplied | - | No seam in any of the four authorized trees: none multiplexes providers *within one conversation*, which is the technique's precondition. Verified with a grep over provider continuation vocabulary (thoughtSignature, encrypted_reasoning, reasoning_details, providerOptions) across pumper, tracklight, personas and pof - empty - and the instrument was then **asserted against a known positive** (anthropic|openai) before the absence was believed, per the standing rule. Return: when a fleet project switches model or provider mid-conversation and replays a stored assistant turn. |
| 2026-09-03 | elision-to-a-refetch-pointer | prompt-assembly | tracklight | code | better | Seam: the tool server's resource read emitted `to_string_pretty(&body)` as a second content item beside the rendered Markdown of the same body, unbounded. Payload-bearing fields are now elided to `<elided: N bytes - fetch via get_event>` and the over-budget form serializes compact. Paired on one fixture (200 spans x 400-byte payloads): 206,644 -> 43,628 bytes, 4.7x. Gate: `cargo test -p lighttrack-mcp` 58 passed, clippy -D warnings clean. Committed on main. **Undischarged and pinned by a test:** arm B does not reach the 24,576-byte threshold, because elision bounds payload per item and cannot bound an unbounded item count. Return: when spans are capped, tighten the residual assertion and re-measure. |
| 2026-09-03 | additive-input-at-the-call-boundary | agent-runtime-assembly | pof | simulation | unmeasurable | Seam: the harness loop has no input door at all - the creative direction is a start-time option and `pof_harness_control` exposes exactly two verbs, pause and resume. A working mid-run path exists and nobody designed it (pause, hand-edit the config that resume rehydrates from, resume): unrecorded, and it races the pause. Three cases walked both ways. **Instrument named, per the rule:** the harness already emits `harness:paused` and writes run-meta.json, so counting paused-then-resumed against paused-then-restarted (and areas replanned when restarted) would measure work discarded per intervention. Return: when that counter exists, or when an operator reports discarding a plan to change direction. |
| 2026-09-03 | indeterminate-closure-on-interruption | agent-runtime-assembly | personas | simulation | not-better | **The technique lost, and the fix travelled into the registry.** Picked because the tree looked like it had the named anti-pattern (a boot sweep writing `failed` plus a prose marker, re-parsed by a string-matching classifier in *both* Rust and TypeScript); that reading was wrong - `restart_recovery.rs` already replaces it with a three-class boot classification, and the string arms are backward compatibility for a measured 74 of 2,188 historical rows. Decisive case the technique missed: a run that kills the process on every resume always looks fresh, so honest closure plus re-admission is a boot loop. Four mechanisms read out of the tree into the technique (classify rather than declare; reuse the existing liveness threshold; count involuntary interruption on its own key; clear the mark on completion, not on the attempt). Return: when the tree instruments its class distribution, re-read whether the 30-minute window and the cap of 3 are calibrated. |
| 2026-09-03 | same-value-still-conflicts (mergeable-plane amendment) | transactions-over-a-replicated-log | (none) | unapplied | - | The amendment states where the technique's rule inverts: a plane contending on a *document* with a store-owned content merge treats a stale base as a merge input, not a refusal - and still writes an attributed record for a merge landing identical bytes. No fleet project runs a mergeable multi-writer document plane; checked across the four authorized trees. The nearest thing, tracklight's ingest idempotency (`same_logical_event`, api/src/events.rs:233), is squarely in the *main* rule's regime and already discloses the duplicate to the caller (`duplicate: true`) rather than swallowing it - a confirming read, not a seam. Return: when a fleet project grows a multi-writer document store, or a second source shows one. |
| 2026-09-04 | unit-classes-are-open | cost-metering | tracklight | experiment | better | **The tree taught the corpus while the technique caught the tree.** Seam: `clients/rust/src/extract.rs:64` (the Gemini branch reads three keys), `crates/core/src/event.rs:164` (`TokenUsage` = input + output with cached and reasoning declared as SUBSETS; `total()` sums two fields), `crates/api/src/otlp/semconv.rs:70-91` (four key groups; an unmapped `gen_ai.usage.*` attribute is dropped). **Arms through the real extractor** on a payload of the documented agentic shape: A (as it stands) 2,000 tokens vs B (payload's own total) 157,000 - a **98.7% under-report produced silently**: no error, no `None`, no counter. Second pair: the unit saving a ledger would report for the mode (>99%) against the vendor's own published figures for it (-88% tokens, -66% cost, no surcharge) - arm A measures its own blind spot, not the optimization. **What the tree knew that the technique did not** (round-9 focus 2, executed deliberately): `crates/core/src/unpriced.rs` stores `cost_usd = NULL` rather than a conservative declared default, ranks unpriced pairs by traffic, and tells the operator every number over the window is a FLOOR until priced - stronger than `price-tables`' default-plus-counter, because a default invents a number and a floor states a bound. The technique carries the tree's rule. Full fix ripples through the canonical `TokenUsage`, the OTLP groups and possibly three storage backends, so it is a plan with its first step landed (`.ai/tasks/2026-09-04-unit-class-residual.md`, branch `direction/unit-class-residual-0904`, not pushed; branch forced by another session's WIP in the client's test and diagnostics files, checkout returned to `main`). Return: when the residual field exists, read the count of ingested events carrying unmodeled units - it is 0 today only because the schema has nowhere to put it. |
| 2026-09-04 | outcome-conditioned-cost | eval-harness | personas | code | better | **The comparison inverted, and the tree's own test pinned the half that was right.** Seam: `src/features/agents/sub_lab/libs/evalAggregation.ts` - the three quality metrics each carry a scored-sample counter (and a comment saying null scores are excluded), while `totalCost`, `totalTokens` and `totalDuration` sum over every sample and `avgDuration` divides by the unconditioned `count`. One leaderboard row, two denominators, neither disclosed; the record type's `status` and `errorMessage` fields were never read. Arms on a fixture where one variant crashes cheaply on one of two samples: A reports it 25% cheaper on `totalCost` (0.30 vs 0.40), B reports it **45% dearer per completed sample** (0.29 vs 0.20) and completing half as many. Cost figures with a disclosed denominator: 0 of 3 -> 3 of 3. The existing test `counts duration and cost across all results regardless of null scores` still passes - the technique requires the unconditioned primary to survive, and it did. Gate: vitest 7->9 passing, `tsc --noEmit` clean. Commit `ed5fd8058`, pathspec, not pushed. Return: when the record gains a task-level pass predicate, re-condition on the verdict rather than on the presence of a score. |
| 2026-09-04 | treatment-election-rate | eval-harness | personas | simulation | unmeasurable | Seam: `src-tauri/engine/src/test_runner/scoring.rs` stores `tool_calls_actual` as `None` when `output.tool_calls.is_empty()`, so an agent that ran and elected nothing is indistinguishable from a sample with no recorded call channel - an observed zero laundered into an absent record. **The structural fact the tree supplies:** the same file already solved the adjacent case, nulling `tool_accuracy` for sandbox scenarios because the real-call channel is empty *by construction* and the metric is degenerate - but not for the channel being empty *by the agent's choice*, where the judge still returns a low number and the cell reads as bad tool use rather than as no tool use. Nobody designed that asymmetry; it fell out of which emptiness had an obvious cause. Three cases walked from the tree's own scenario shapes. **Instrument named, per the rule:** the comparison becomes runnable when that field stores `Some([])` for a completed run that called nothing. Return: at that point, re-run as an experiment over an existing lab run and count how many low-tool-accuracy cells are zero-election cells. |
| 2026-09-04 | caller-differentiated-capability | mcp-tools | pumper | simulation | not-better | **Nothing to change, and the tree paid the corpus for the visit** (round-9 focus 2). The runtime already ships the technique at both its rules: `crates/core/src/config.rs:1740,1747` grants two different tool lists by execution mode (`WebSearch`+`WebFetch` hosted; exactly `mcp__pumper__fetch` self-hosted), subtracted by a stated invariant - `crates/core/src/agent_tools.rs` calls an unattributed fetch "precisely the state this feature exists to end" - and every refusal names the surface ("not callable from an ordinary MCP client"). No arm B; nothing committed. **What the tree knew that the technique did not:** the gate enumerates FOUR verdicts, not two, because an expired grant and an unknown token are different facts - collapsing them would make a leaked-token report indistinguishable from an ordinary late call, and both look like a guard working correctly. The technique gained "The refusals are a channel, and they must not collapse either." Second structural gift: the token's life is enforced by Rust drop semantics rather than by a sweeper, so a narrowed grant cannot outlive the work it was narrowed for - a property the technique asks for and cannot obtain without deterministic destruction. Return: when a fleet tool needs narrowing at the *argument* level rather than by removing a whole tool, which this pattern cannot express. |
| 2026-09-04 | fluent-syntax-bounded-grammar | mcp-tools | (none) | unapplied | - | **No seam in any authorized tree - the corpus outran the fleet.** The technique's precondition is a tool whose input is a *notation* (a command line, a query language, a glob) rather than a schema, supplied by a model and parsed by the project. Established across all eight checkouts at their real layouts. **The first search behind this row was void and the row is corrected from the second:** it globbed `src/ crates/ src-tauri/` over a SIX-project list. **grant and gravity were never in the loop at all**, politicas and kp have none of those directories (searched: nothing), and pumper's `clients/`, LightTrack's `clients/` and goat's `app/`+`lib/` were missed - so four of eight trees contributed zero files and two more were partial. Re-run per project against the directories that exist, vendor code excluded, the conclusion holds - no project exposes a model-supplied notation it then parses. **But the original evidence sentence was wrong and is withdrawn:** shell invocations reachable from an app-supplied argument DO exist (`personas/src/lib/harness/executor.ts:146,167-169` spawns a CLI with the prompt in argv under `shell: true`; kp's `spawnPython` and `repo-scan-run` pass argv arrays with no shell, which is the safe shape). Those are the technique's *inverse* - passing a payload out through a shell rather than accepting a notation in - so they are not this seam. Recorded as a lead instead: **three** fleet projects spawn the same coding CLI and personas is the outlier two-to-one - grant and gravity both route the prompt through stdin and both wrote down why (grant: "the command line is static flags only - the prompt is written to stdin, never the argv/shell"), while personas puts it in argv under `shell: true`. Not a refutation and not an apply miss: one of this run's four landings simply has no tree to meet it, which is the state the round-9 focus asked be reported rather than logged as a zero. The source tree's own realization is recorded as an application instead (`mcp-tools/applications/node--fluent-syntax-bounded-grammar.md`). Return: when a managed project exposes a query language, glob syntax or command line as a model-supplied argument - then apply at that seam and measure malformed-call rate against a structured-schema alternative. |
| 2026-09-04 | compression-hardens-deferred-decisions | prompt-assembly | personas | simulation | better | **The seam was picked on a premise the tree refuted, and the refutation was the finding.** Expected a large standing layer (836 lines, ~500 of framework text, 27 numbered rules) with no behavioural tests; the project runs a **ten-fixture behavioural bench over that exact prompt**, whose rubric cites its rules by number. A first instrument — does any test file mention a rule’s identifiers — returned 100% in both arms and could not discriminate, because vocabulary overlap is not a behavioural assertion. What the bench measures instead is this technique’s mechanism directly: a rule set written almost entirely in the categorical voice scored **mean 0.48**, high band **0.285**, the round-cap dimension **0 or 1 on all ten fixtures**, and the fully-specified control drew **four** questions where the design says zero. Its own prescription — *select questions by information value, not by template* — **is this technique’s decision rule, reached independently seven weeks earlier from a regression**, which is stronger corroboration than the source the technique was written from. Three recorded cases walked both ways, with the falsifier stated: if the delegation raised the cap dimension while asked-before-assuming fell, the hard enumeration was doing real work. Return: when the bench next runs, read the two deltas — only arm A has ever been measured. |
| 2026-09-04 | escape-hatch-usage-as-the-safety-metric | tool-result-economy | personas | code | unmeasurable | A 4,000-char cut on a long-term-memory payload emits head-plus-ellipsis. The material genuinely stays addressable at its source and the reading agent can fetch it, so the recovery path exists and **the model is never told** — making the recovery rate zero *by construction*, and "did not need it" indistinguishable from "could not reach it". The format has no room for either fact; this is not an omission that happens to be missing. The neighbouring `context-budgeting` bottom-rung notice rule is implemented elsewhere in the same tree and absent here. Arm B keeps the head byte for byte and adds the dropped count against the total plus the source location; the paired test separates the arms on 2 of 2 assertions. **Compiles clean under the project feature set; the lib test binary will not launch on this machine (`STATUS_ENTRYPOINT_NOT_FOUND`, 0xc0000139) before running any test**, so no gate saw it — left on a branch, not merged, not pushed. **Instrument named:** the same targeted test on a box where the binary launches; separately the behavioural half needs a re-read counter that does not exist today. |
| 2026-09-04 | classify-before-you-respond | contested-acquisition | politicas | code | better | **The finding named a seam that turned out to contain a defect.** Every ingest adapter keys its retry on two literal status codes (`res.status === 429 \|\| res.status === 503`) and treats everything else as not-a-pressure-signal, so three states were one: the host DECLINED us, the resource is ABSENT, and the host is BROKEN. In `lib/ingest/sources/monitor.ts:fetchJsonWithRetry` the `throw new Error(!res.ok)` fired *inside* the try, so the loop catch that exists for network errors retried it. Measured with an injected counting fetch on `MonitorClient.fetchPeriods`, same instrument both arms: **arm A issued 3 upstream requests for a 403 and 3 for a 404, identical to a transient 503; arm B issues 1**, with pressure and broken still at 3 - 2 classes improved, 2 unchanged, 0 regressions. Retrying a public civic-data host that just declined is what `web-scraping/scrape-scheduling` names as how a pause becomes a ban. 209 ingest tests pass, typecheck clean, both lefthook hooks green (doc-sync satisfied through its own dismissal channel, not bypassed). Shipped: `7eac8cb5` on `master`, not pushed. **Structural fact:** `dataor.ts:49` and `smlouvy.ts:331` carry visually identical retry loops that `return res` instead of throwing, so the three adapters behave differently at the one point that matters and nothing in the tree names the difference. Return: when the refusal vocabulary is shared across all three adapters, or when a live request log exists to say whether any upstream has actually throttled this client. |
| 2026-09-04 | read-triggered-reconciliation | markdown-vault | personas | code | better | **The seam contained the technique's own first two failure modes, live.** The project keeps a context index whose records carry `reconciledToSha`, written by the `touch` subcommand on every reconcile - and `check`, the pre-push gate, computed staleness **entirely from the uncommitted working diff** (`git diff --name-only HEAD` + `--cached`) and never resolved the stamp at all. That is "the stamp nobody resolves" and "the gate that only sees the change in flight" together: it warned while an edit was in the working set and went silent permanently the moment the edit landed, so drift was detectable during exactly the one window in which it is not yet drift. Arm B resolves the stamp and compares against the module's own history. Same command, same repository, both arms: **A = 1 warning, `--strict` exit 0; B = 2 warnings, `--strict` exit 1.** The added warning is not a near-miss - the recorded stamp is seven hex characters `git cat-file -t` rejects as "Not a valid object name", and behind it the root module's CONTEXT.md was last committed **2026-06-11 with 4,215 commits touching 8,788 files since**, none of it ever visible to arm A because none of it was ever in a working tree when the gate ran. Second branch verified separately against a resolvable stamp (reports 4,198 commits since, context untouched in any). 14 lines, no new dependency, lefthook green (eslint ran). **Two defects introduced and caught inside the change**: `git rev-parse <sha>^{commit}` is not portable through `cmd.exe`, where `^` is the escape character - the ref arrives as `<sha>{commit}` and **every VALID stamp reads as unresolvable**, caught only because the resolvable branch was exercised deliberately instead of inferred from the failing one; and the git helper inherited stderr, so a healthy run printed a bare `fatal:` above its own verdict. Shipped `43988bd6d` on `master`, not pushed; applied row `4f1e59263`. **Fleet reach: 1 of 8 projects carries this seam** - personas is the only one whose context index carries a reconcile stamp. Return: when the gate must prove the context is CORRECT rather than merely considered at a commit - it measures drift in commits, not in meaning, and `touch` re-stamps without checking that anyone re-read anything. That needs a content check the project has no instrument for. |
| 2026-09-04 | advertised-support-is-not-evidence | remote-capability-probing | tracklight | code | better | **The seam collapsed two capability states into one, and the collapse routed work to a broken device.** `crates/agent/src/inventory.rs` enumerates what a device can run from its filesystem and returns an empty vector for BOTH "I read the library and it holds nothing runnable" and "I could not read the library at all" (`let Ok(namespaces) = read_dir(root) else { return found }` discards the io::Error). The cloud reads an empty advertisement as **no filter**, so a device with a mistyped `actions_dir` leases every action type in the fleet. The collapse is deliberate for ROUTING and was not changed - the wire value is bit-identical and the pre-existing back-compat test passes unmodified. What was fixed is the operator's first diagnostic surface: the startup banner, whose stated job in its own comment is to answer "why is nothing being picked up" without reading the cloud's logs, could not name the likelier of the two causes. Arm B adds `unreadable_reason()` on the reporting side only, and the banner names the path, the OS error and the config file. **Measured on the crate's own gate, same instrument both arms: distinguishable causes for an empty inventory 1 -> 2; tests 22 -> 23; `cargo fmt --check` clean; `cargo clippy` clean.** Shipped `462d0f1` on `main` with a pathspec, applied row `1d78064`, neither pushed; the tree's uncommitted `clients/rust/` WIP was left untouched. **Structural fact, and nobody designed it:** one line above the inventory call, `main()` probes the engine binary and refuses to start with a message naming both the path AND the config file it came from. Two capability checks a line apart - one fatal and actionable, the other silent - because they were added at different times. That asymmetry is better evidence for the technique than the code this run then wrote. **Fleet reach: 1 of 8.** `gravity` already implements the discipline and states the rule independently in `lib/capabilities.ts` ("'the model is reachable' and 'this app can drive it' are different claims and only the first is measured") - recorded as convergence rather than converted into a manufactured change; its text router was examined at `router.ts:281` and cleared, because the adapter appends the schema instruction itself when the schema is not natively expressible. `pumper` is guest-sandbox ground a prior run covered; the other five have no seam. Return: when the cloud gains a way to distinguish "advertises nothing" from "could not enumerate", move the distinction onto the wire and revisit the M18 no-filter default. |
| 2026-09-04 | indeterminate-closure-on-interruption | agent-runtime-assembly | pumper | task | better | **The amendment predicted a defect in a tree that had already reasoned carefully about this exact family, and the prediction held one level down.** The amendment: an interrupted metered generation is closed as indeterminate and its spend reconciled, never assumed zero; where a committed partial exists it rides inside the closure. The seam's own doc comment names the anti-pattern ("the anti-pattern is one silent $0 that reads identically to 'no call was made'"), distinguishes three cases with typed classes rather than strings, and carries a test asserting the COST travels in a field and is not smuggled into a message. The residue is the same defect one level down: the amount for an unmetered timeout is a summable `0.0` and the *unknown-ness* lives only in the detail string beside it. **Falsifier run in-session rather than deferred** - if any consumer branched on that string before summing, the finding was cosmetic. `ledger_event()` has exactly ONE production consumer and it passes the amount straight to the meter without reading the string, so the spend ledger understates by the whole timeout population. That grep also shrank the size estimate from "3-5 files" to three. **A second, narrower gap:** the abandon path states "a killed run produces no envelope", true of the wait timeout and false of the drain timeout, which is reached only after the process has already exited and where the sibling branch 25 lines below was already fixed once to stop discarding a printed envelope. Plan, measurable, gate and size committed to the project; **first step not taken** - that tree held another session's uncommitted work in three files, so no branch was opened. Return: when that tree is clean, open `direction/unmetered-timeout-metering` and start at the ledger event's timeout arm. |
| 2026-09-04 | two-cancellations-and-a-synchronous-door | durable-agent-operations | pumper | simulation | not-better | **The strongest row this run produced, and it is a rejection.** The technique says a caller's cancellation and the operation's durable cancellation must not share a mechanism. This tree already implements the split - a guarded durable write for work with no live executor, a registered per-job token for work in flight - and it did not arrive there by reading anything. Its own test module records the incident that taught it: the graceful-shutdown drain fired each running job's cancel token to mean *suspend*, and the operator's delete door fired **the same token** to mean *stop*; the worker read both as suspend, so an operator's cancel inside the drain window silently became "run it again after the restart" while the door answered `{cancelled: true}`. That is the technique's predicted failure mode, in production, with a regression test over it. A second doc comment records the mirror case - without the durable write a parked job would be uncancellable, because the door would find no registered token and tell the operator the job was already terminal, "which is the opposite of true". **Verdict not-better because the technique would change nothing here** - and the row's value is the opposite of a miss: this is a **second independent sighting** of the mechanism, reached from a job runner rather than an agent harness, which is the convergence bar the corroboration table sets for a technique. The synchronous-admission half is NOT corroborated here and remains single-sourced. Return: when a second tree is found where the two cancellations still share one mechanism, apply there and measure operator-visible lies per drain window. |
| 2026-09-04 | close-is-a-controlled-crash | durable-agent-operations | pumper | simulation | not-better | **The tree takes the opposite stance deliberately, and the reason is the exception the technique already names.** The technique says shutdown writes no cancellation and no terminal state, so reopening finds exactly what a power loss would leave - one recovery path, exercised by every shutdown. This tree drains instead: shutdown fires each running job's token to mean suspend, and has two dedicated test modules pinning termination and drain semantics separately. That is not a defect. The technique's own stated exception is work holding a fact that lives only in the dying process, and the worker generalized it during drafting to "the repair is usually to make that fact durable rather than to grow a second recovery path". Here the fact is the guest app's opaque in-flight checkpoint: the host does not own the state vocabulary (see the `total-restart-point-by-reference` row), so it cannot know what a crash would cost, and suspending is cheaper than recovering from an unknown. **Verdict not-better, and the condition is now evidence rather than assertion** - the technique's exception was written from one system's reasoning and this is a real tree standing in it. Return: if that host ever owns its guests' state vocabulary, the drain becomes a second recovery path and this becomes a live finding. |
| 2026-09-04 | intent-mints-the-identity | durable-agent-operations | pumper | simulation | better | **Same seam as the amendment row, read from the other direction.** The technique says the identities an uncertain effect's output will occupy are minted in a durable intent commit *before* the effect, so a crash during it is distinguishable from a crash before it and a recovered record occupies the slot the real one would have. This tree does the reverse for its metered engine calls: nothing is reserved before the process is spawned, and the spend is recovered *afterwards* by parsing the exited process's stdout for a cost field - a recovery that works precisely when the process finished, and yields nothing when it did not. That is why the timeout path has no amount to write and falls back to a marker. **Arm A** (today) cannot tell a call that spent from a call that never started except by class-tagging the error. **Arm B** (a row written before spawn carrying the attempt's identity, settled or reconciled after) makes the three states distinct in the ledger and gives the drain-timeout case somewhere to put a recovered envelope. Three real cases walked: a clean run (identical), a non-zero exit with a printed envelope (identical - the tree already recovers this one), a timeout (A writes a summable zero, B writes a reserved attempt with an unknown outcome). Better on 1 of 3, neutral on 2, no regressions. **Falsifier:** if the engine's provider never bills a killed call, arm B buys only bookkeeping - the tree's own cost handling and the registry's spend subject both say otherwise. Same task plan as the amendment row; not separately shipped. |
| 2026-09-04 | total-restart-point-by-reference | durable-agent-operations | pumper | simulation | not-better | **The technique meets a host that deliberately cannot satisfy it, and naming why is the result.** The technique says the runtime replaces its operation state with the complete current state after every transition, keeping it small by holding ids rather than content - which is what makes crash states enumerable. This tree's durable execution hands a re-claimed attempt its last persisted checkpoint, clears it on completion, and discards a poisoned blob after a bounded number of failed restored attempts. That checkpoint is an **opaque blob owned by the guest app**, not a leaf of a vocabulary the host owns; the host's own durable state is a status plus an attempt counter, which is a position, not a total state. **Verdict not-better, and the condition belongs in the technique:** T1 presupposes that the runtime owns the state vocabulary. A plugin host that delegates state shape to guest code cannot enumerate its crash states no matter how it stores them, and telling it to write a total state would be telling it to stop being a plugin host. The tree is not doing it wrong; it is outside the technique's domain, and the technique does not currently say so. **This is the one finding of the run that should amend a technique landed by the same run** - deferred rather than written, because a condition established from one tree is a lead, and a second sighting should settle whether the boundary is "plugin hosts" or the narrower "any host whose durable content is opaque to it". Return: on that second sighting, or when a fleet project owns an agent state machine end to end. |
| 2026-09-04 | settlement-order-is-not-placement-order | durable-agent-operations | (none) | unapplied | - | **No seam in any authorized tree.** The technique's precondition is several effects running in parallel whose results must enter one record in a fixed order that is not their completion order. Checked across the three authorized trees at their real layouts: the job runner fans out across jobs but each job's record is its own, so there is no shared ordered record for outcomes to contend over; the observability trees ingest events that are already ordered upstream. Not a refutation and not an apply miss - the corpus outran the fleet on this one. **Precondition shared with `recovery-prefix-enumeration`?** No - these are two distinct preconditions and are reported as two, not folded into one number: this row needs *parallel effects into one ordered record*, that row needs *a closed durable-state vocabulary the runtime owns*. Return: when a managed project runs parallel tool calls into a single transcript - then apply at that seam and measure repeated side effects per crash-during-batch. |
| 2026-09-04 | recovery-prefix-enumeration | durable-agent-operations | (none) | unapplied | - | **Blocked on the same precondition as `total-restart-point-by-reference`, and that is the useful way to say it.** The technique enumerates test cases from the durable state vocabulary - for each leaf, construct it, close, reopen, drive, assert the next transition, and treat every half-completed recovery prefix as its own case. It presupposes a closed leaf set the runtime owns. The one authorized tree with durable execution stores an opaque guest-owned blob, so there is no leaf set to enumerate; its resume tests are per-feature (checkpoint restore, poisoned-blob discard, parked-job resume) rather than per-state, which is the correct shape for what it actually holds. **Two unapplied rows, ONE precondition** - reported that way per the standing focus, because three rows behind one precondition is a different fact from three rows behind three, and this one has a single return condition that would unblock both. Return: when a managed project owns an agent operation's state machine end to end - the same trigger as the `total-restart-point-by-reference` row. |
| 2026-09-04 | presence-decides-precedence | settings | goat | code | better | **The seam contained a defect, and the defect had a security half.** An enrichment health endpoint probes four external sources; one probe resolved its credential as `TMDB_API_KEY || NEXT_PUBLIC_TMDB_API_KEY` while the fetcher it reports on (`TMDBFetcher.getApiKey`) reads only the first. Set just the public name and the endpoint reported the source **configured** while every real enrichment call threw `TMDB API key not configured` - the gate did not read what the gated code reads. Three artifacts in the same file already disagreed with that line: its own doc comment (*Requires TMDB_API_KEY*), its own error string, and **four sibling probes (Twitch x2, Spotify x2) that each read exactly one name** - the dual read was 1 of 5, which is a line that drifted rather than a house style. Four paired tests through the exported `GET` with `fetch` stubbed, same instrument both arms: **A = 2 of 4 failing, B = 4 of 4 passing.** The fourth test is the sharp one - arm A does not merely report the public value usable, it **spends** it, putting `NEXT_PUBLIC_TMDB_API_KEY` into the outbound `api_key` query parameter, so a bundle-named variable was a working way to supply a secret and the fallback is what kept it alive (`public-vs-server-env-split` is the convention it re-crossed). Full unit suite 285/285 across 13 files; typecheck adds no new errors (29 pre-existing in an unrelated store module, confirmed untouched; one error the new test DID introduce - Map iteration needing downlevelIteration - was found and fixed before commit). Shipped `323b1bd` on `main`, not pushed. **What it does not do:** it fixes one call site rather than introducing the single resolver the technique asks for - the project has no configuration module, every route reads `process.env` inline, so "resolve once" has nowhere to live - and it emits no deprecation notice, so an operator still holding the public variable is not told it stopped working. Return: when the project grows a config module, move the resolution into it and add the presence-attached notice. |
| 2026-09-04 | credential-attachment-at-the-hop | stream-proxy-hop | gravity | simulation | not-better | **A rejection, and the run's most useful row: the amendment was refuted by the second tree it was walked against and had to be corrected.** As first written it said the credential-route ranking collapses to the cookie for requests **the browser issues on rendered content's behalf**. Gravity serves access-checked run artifacts to an `<img>` gallery (`app/api/foundry/file/route.ts`), states this exact protocol fact in the route's own header comment - *an `<img>` cannot carry an Authorization header* - and takes the **query-string** rung instead, with the argument written down: the client composes the URL per render (`foundryClient.ts:67`, nothing persisted, so the credential does not outlive the string) and the value is *already a public bundle credential*, so a URL discloses nothing the bundle did not. Under the amendment as written, applying it here would have made the tree WORSE - a second credential transport, a second invalidation path, and cross-site obligations, to hide a value published by design. **The technique was not deleted; its discriminator was corrected** from *content-issued versus app-issued* to **who owns the URL string, and whether it outlives the credential**. Three real cases now land on three answers: gravity on the query string, flatnotes on the cookie (its URLs live in saved user markdown and must keep resolving), and kp outside the question entirely - its hand-rolled markdown renderer enumerates a closed tag subset with **no image syntax at all**, so no content-issued request can be generated, and that fell out of an injection-safety decision with the credential question never considered. kp's case earned its own line in the amendment: **run the enumeration even when you expect it to be empty.** `structural-only`: no behavioural arm ran anywhere - gravity needed no change, flatnotes is not a managed project, kp has no seam to instrument. **Instrument that would upgrade it:** a checker emitting the count of guarded routes reachable by a request the application does not issue; no fleet project has one. Return: when such a checker exists, or when a managed project stores an app-authored URL to a guarded asset inside user content. |
| 2026-09-04 | terminate-from-outside-when-you-cannot-count | guest-execution-bounding | personas | code | better | **Independent convergence, and the apply vocabulary has no cell for it - which is the row's second finding.** The hypothesis was that a desktop app driving external CLI tools under async timeouts leaks detached children wholesale. **Refuted on the hot paths, and the refutation is the corroboration**: this tree had already found the mechanism on its own and documented it in the mined source's own terms - `cli_process.rs:590` says the guard exists for *the engine ceiling dropping the runner future when `tokio::time::timeout` fires. Without it the underlying CLI keeps streaming - and billing the user's API account - until the desktop app restarts*, and `session/cli.rs:214` states flatly that without it the runtime **detaches** the child. Two systems, unrelated problem domains (a wedged browser worker; a runaway subscription bill), same mechanism, neither aware of the other. So arm A's semantics were established by the project's prior investigation rather than asserted by this run, which is what makes a structural count sufficient evidence here. **What was missing was coverage, not the mechanism**: 16 files guard with `kill_on_drop`, 5 more kill explicitly on the timeout branch and reap under a bounded second wait (equally correct, used where the child handle is still in scope), and **8 sites had neither** - a bridge action, two version probes, an outbound HTTP tool test, a build-tool invocation, and one pipeline step. The pipeline step is the one a file-level scan cannot see: its child is spawned *inside* the async block the 300s ceiling drops, and the only explicit kill is on the user-cancellation branch. **Two sites failed the scan in the opposite direction and running the check rather than trusting it is what caught them** - a verification-command runner and a context-generation scan both look unguarded by grep and both kill explicitly; one carries a comment reasoning that dropping the future makes *the buffers* safe and says nothing about the child, which is the exact reading error the technique exists to prevent, in a codebase that then got the behaviour right anyway. Measurable: unguarded spawn-under-ceiling sites **8 -> 0**. `personas-engine` checks clean; the desktop crate's build-script failure (`Permission updater:default`) was **proven pre-existing** by stashing the seven changed files and reproducing it identically, rather than assumed. Shipped `a659dff78` + `b5fac8fed` on master, not pushed. **What no gate saw:** the behavioural half. Proving the child is *dead* after a fired ceiling needs a test that spawns a sleeper, lets the ceiling fire and asserts the process is gone; the desktop lib test binary will not launch on this machine (STATUS_ENTRYPOINT_NOT_FOUND), so the structural count is what moved. Return: when that test binary launches. |
| 2026-09-04 | soft-budget-under-the-hard-cap | fleet-orchestration | pumper | code | better | **Shipped.** Two independent bounds on one agentic loop, neither derived from the other: a literal step cap and a request parameter published as the run's total turn budget. The falsifier was RUN, not argued - a scripted arm with a 200-turn request stopped at the step cap having charged 96, and 104 requested turns were never spendable with no field saying so. Arm B derives the ceiling and reports it: measurable 0 -> 2 result fields naming the surplus, 55 tests green, linter and formatter clean. The tree then taught back: its own result-contract test refused the two new keys until the published output shape declared them - the same derivation rule one altitude up, enforced. Return condition: none. |
| 2026-09-04 | parallel-dispatch | fleet-orchestration | pumper | task | better | **Convergence on two of three rules, a gap on the third.** The amendment landed this run says a requester that cannot survive the wait is refused into its own context with the number rather than queued. This tree reached the elected-vs-cut distinction independently - a stop-reason vocabulary separating a real finish from four truncations, with the technique's own justification in its doc comment - and reports the cut to the CALLER. It does not reach the AGENT: three step prompts carry a stopping condition and no count while three ceilings terminate the loop from outside it. Banked as a plan with its measurable (cap-fired fraction) and its falsifier (if the fraction does not move, the budget line is prompt noise - revert, do not tune). Return condition: a branch and a metered A/B; one file, ~40-60 lines. |
| 2026-09-04 | probe-the-decision-not-the-artifact | eval-harness | pumper | task | better | **The hardest precondition was already met, independently.** The technique requires the decision to be observable in typed state before a probe is even possible, and most trees must add that record first; this one emits a closed verdict vocabulary per tier escalation and says in its own comment that consumers should branch on it rather than parse the trail. What is missing is a suite: the only eval scores the extracted artifact, and a wrong tier costs more than a mediocre extraction. Frozen fixtures and recorded transcripts make the probe free. Return condition: label the fixtures, stub the metered tier to panic; step 0 (check the label distribution) can kill it. |
| 2026-09-04 | escape-hatch-usage-as-the-safety-metric | tool-result-economy | pumper | simulation | not-better | **Refuted at two of three real sites, which is the honest result.** The amendment says a bounding stage's failure path is correlated with the input it bounded, so the involuntary fallback is largest where the transform was most needed. Two ceilings in this tree already cap their fallbacks below the trigger, as the amendment prescribes, and a third states the amendment's disclosure rule in the tree's own words - a refusal is loud, a truncated one is not - and refuses rather than truncating, which is stronger than what the amendment asks. Residue is one uncapped, unmarked fallback whose blast radius is a CLI envelope. Return condition: a counter separating involuntary fallbacks from elected ones, instrumented nowhere. |
| 2026-09-04 | deliberation-as-an-elected-turn | fleet-orchestration | - | unapplied | unmeasurable | **No seam in any managed tree, and not a rejection.** The technique governs where a model-dispatcher's reasoning sits and whether it is durable. The nearest project hands a prompt to an external agent process and receives text, so the deliberation happens inside a transcript it never sees; owning the placement would mean parsing another tool's turn stream, which is a different subject. Return condition: when a project runs its own dispatcher loop rather than shelling out to one, or when the engine surfaces the per-turn transcript. |
| 2026-09-04 | negative-claims-are-pinned | docs-sync | personas | `code` | **better** | `ab-paired`: same mutated landing page through both arms - the promise paragraph rewritten into a plausible benign replacement. Arm A (the project's three prose-adjacent checks) detected **0 of 4** claims, all exit 0; arm B **4 of 4**, exit 1. Falsifier run uncapped rather than assumed: all 7 references to the page across `scripts/` opened, none reads its content. Built stronger than the source - the checker REFUSES a pin with no dated `reviewed`, 2 of 8 tests on that branch. Structural fact: the promise lives in **exactly one document**, which the scope enumeration surfaced and nothing else could have. Shipped `23070dba` + `2b40ab1e` (+ `900b4151` applied rows), master, not pushed |
| 2026-09-04 | prose-as-an-execution-surface | docs-sync | personas | `simulation` | not-better | `structural-only`: swept all 8 checked-out projects - **zero** documented credentials in process arguments, one bare install line naming an unsquattable first-party package. Population claim **confirmed** (the app emits actionable install hints from compiled source, outside every doc gate's scope); vector **absent** because the fleet owns every namespace it names - the technique's own stated disqualifier, so no amendment is owed. Sharper return condition than the technique carried: **the vector is created by a rename**, not by a naming choice, so the check belongs in a rename runbook, not a standing gate |
| 2026-09-04 | translations-drift-against-the-product | docs-sync | - | unapplied | unmeasurable | **No seam in any of 8 checked-out projects**: none ships prose documentation in a second language. One carries UI message catalogs, which is `hash-pinned-translation-pipeline`'s population and not this one's. Instrument that would measure it: a project's own capability registry read against a localized landing page. Return when **a project ships a landing page in a second language** |
| 2026-09-04 | zero-depth-admission | admission-queue | gravitone | experiment | not-better | **The technique lost on the number that matters, and the loss rewrote it before it landed.** Seam: admission is a bounded semaphore sized `workers + queue_max`, acquired non-blocking, 429 beyond it — so zero depth is `queue_max=0`, an existing env knob, and the arms differ on exactly one variable with no product-code change. Paired on one seeded arrival schedule (90 callers over 1.5s, 3x capacity, deterministic 100ms model, project's own fake-model rig driving the real admission path): at the shipped caller deadline, depth-32 completed 41 and abandoned 21; depth-0 completed 24 and abandoned 0. Pre-registered falsifier fired. Sweeping the ignored variable located the boundary: caller-deadline/service-time of 1/3/5 -> zero depth wins 20-2, 24-7, 24-12; 10/15 -> queue wins 26-24, 43-24. `depth-bounds-and-shed`'s existing wait-time formula predicts the crossover (30 at ratio 15; the tree ships 32), so the technique's proposed structural selector was strictly worse than the arithmetic the subject already had. Rewritten to the ratio before commit. Seam class named so nobody re-runs it: a synchronous caller whose deadline spans many service times. |
| 2026-09-04 | admission-vocabulary (two-arms amendment) | admission-queue | (source tree) | experiment | not-better | Not applied at the fleet peer: it runs a real queue and emits all three verdicts, so the amendment does not bind at its seam. Tested instead against the source tree, which is the shape the amendment is about, and it **fails the amendment's second test while passing the first**: two arms are correct there (`queued` unreachable by construction), but the refusal is the bare object `{"error": "overloaded"}` at both hops — no reason class, no retry hint. That is the amendment's own prediction: dropping `queued` is free, dropping the reason is the original outage by another road. Witness appended to the existing `rust--admission-vocabulary` rather than given its own file (stack+technique filename collision). Return: when a fleet project runs a zero-depth gate of its own. |
| 2026-09-04 | remediation-derived-bounds | admission-queue | gravitone | simulation | unmeasurable | Seam: the deployment-plan generator derives queue cap, scaler target and replica ceiling in one file, and the scaler's input IS the capped queue depth — so the cap sets the controller's gain. Evaluated by importing the generator and running its own constants across the replica counts it emits autoscaling for: cap-per-replica equals the scaler target, so gain == replica count while the ceiling always asks for 3x; they cross at 3 replicas, and the 2-replica plan (the smallest fleet it will autoscale at all) needs an extra polling interval, ~30s of continued shedding. **A first reading claimed the ceiling was unreachable; the tree refuted that** — an iterating controller converges geometrically, so a clipped signal costs convergence time, not range. Technique corrected before commit. `unmeasurable` because whether 30s matters depends on burst shape. **Instrument named:** a control-loop simulation stepping a scaler on the polling interval against a burst replayed through the fake-model harness, versus an unclipped-signal control; both halves exist in the tree, the loop joining them does not. |
| 2026-09-04 | refusal-without-release | admission-queue | gravitone | simulation | not-better | **The technique's own exclusion applies, and the tree is the confirming case.** Three real conditions walked both ways (cold start with the model loading; a scale-out where new replicas are unready while existing ones saturate; graceful shutdown, which is correctly terminal). All three are correlated server-side refusals, so the technique's premise holds — but its cost argument inverts: callers here are stateless HTTP requests whose re-establishment is a reconnect, not a socket plus handshake plus session hydration, and the refusal already carries a predicted wait and a retry-after. Holding would buy nothing and leak sockets. This is the boundary the technique's closing section draws (`whether refusal or holding is the expensive act is decided by whether the caller's context survives the refusal`), measured against a tree that sits on the release side of it. Return: when a fleet project terminates long-lived stateful connections. |
| 2026-09-04 | healer-death-as-promotion + declared-verdict-over-inferred-wreckage | self-healing | gravity | code | better | **Shipped.** The engine computed its terminal give-up correctly and spelled it distinctly in the health body, and two of its own docstrings promised this "lets the process supervisor replace the replica" - but the deployment could not read it: readiness branches on a status code three not-ready states share, and liveness was a TCP connect that a worker-less process with a bound port passes forever, so an exhausted replica left the rotation and was never replaced. The chart's own policy gate made it durable: `probes-distinct`'s remediation text prescribed the socket probe. Fixed with a liveness path failing only on the terminal state, plus a new chart policy with its must-fail fixture. Paired over five engine states, three arms: correct replacements 0/1 -> 1/1, false 0 -> 0 (the naive fix - liveness on the readiness path - would be 3, two wrong). Gate green: 43 service tests, 14 chart policies. Committed, not pushed. Return: the verdict still dies with the pod, so a deterministically broken model re-spends the full restart budget in every successor - `consume-once-mode-handoff` has no seam there yet. |
| 2026-09-04 | consume-once-mode-handoff | self-healing | (none) | unapplied | - | The cross-death channel disciplines (unlink on read, authenticate by shape, derived read bound, out-of-band door). No fleet project persists a failure verdict across its own process death; the nearest, the speech service above, holds its give-up in memory and relies on an orchestrator whose crash-loop backoff paces a repeat without ever classifying it. Return: when a fleet project needs a mode decision to survive its own restart, or when the row above is extended past the probe boundary. |
| 2026-09-04 | far-side-oracle | test-harness | (none) | unapplied | - | Deploy the oracle onto the system the product effects, reporting substrate rather than product vocabulary. Checked across the twelve authorized trees: only one drives a system it does not own, and it already has two of the three properties - it reads the far side's filesystem for its growth measurement and skips-with-a-reason when the far host is absent - lacking only a deployed agent for far-side *runtime* facts, which its bridge currently answers for. A confirming read, not a seam. Return: when that project needs to assert an in-editor effect its bridge cannot witness. |
| 2026-09-04 | bypass-is-a-versioned-policy | signed-artifacts | (none) | unapplied | - | The signature exemption as one pure predicate with a deny default and a refusal table. No authorized fleet project verifies signatures on artifacts it installs - checked per project, with the instrument asserted against a known positive in the source tree first. Return: when a project grows an install path for third-party or self-published artifacts. |
| 2026-09-04 | updater-chain / two-baselines (amendment) | release-pipeline | (none) | unapplied | - | Rehearse the update from a synthetic baseline as well as the shipped one; they localise faults differently and the synthetic lane structurally cannot catch a break in the old reader. No authorized fleet project ships a self-updating client. Return: when one does, or when a second source shows both lanes run together. |
| 2026-09-04 | tool-schema-design (amendment: transport rungs) | tracklight | code | better | Audited the generated MCP catalog: 64 tools, 203 top-level params, six schema keywords in the whole published surface (type/description/enum/required/properties/items) - no numeric or string bounds anywhere. Measurable: published params carrying a server-enforced value constraint reaching neither the schema nor the description. A=1 (`create_benchmark.baseline_score`, rejected outside 0.0..=1.0 because run means are normalized, published as "the mean a run must not fall below" with no unit - a caller thinking in percentages sends 85 and reads an undiagnosable 400), B=0. Gates green both arms (21 contract + 58 server tests), pinned tool contract unmoved. Shipped to main as `ad6234a`, not pushed. **The tree was ahead of the registry** and became a source: it demotes constraints to prose as house style AND carries the reason with the bound ("clamped to 4..=90 - below the evidence floor a trend cannot be presented"), which the mined source does not - that discipline is now in the amendment. Structural fact: the contract pin covers names/types/required and not descriptions, so the demoted rung has no drift guard. Return: when a check can name a server-enforced bound automatically - both halves are already generated from declarations, so the tree could support it. |
| 2026-09-04 | foreign-config-replay | pipeline-authoring | personas | task | better | Two independently authored answers to "which checks apply here": a local hook config with per-job globs, and a workflow set that references it nowhere. Measurable: locally-enforced checks with no remote counterpart. A=3 of 11 (a secret scan whose own comment argues CI catching a leak is already a leak, so the local hook is the ONLY enforcement; the translation-completeness check written to close a hole that hid ~24% of the app behind a green report; an agent-context freshness check), B=0 by construction. Instrument expands npm wrappers transitively - a first version grepping workflow files reported 9 gaps and was wrong about 6, caught by asserting two known positives and a known negative. First step shipped on branch `intake/foreign-config-replay` (commits cad975c58, 9fcd024b0): a parity checker that ships RED deliberately, plus the plan for the remaining steps. Not pushed. Return: when the three gaps are closed or exempted and the checker is wired into a gate; scope parity (not just presence) is step 4 and unstarted. |
| 2026-09-04 | single-source-topology (amendment: the format edge) | agent-instruction-files | kp | simulation | not-better | The amendment adds a second link/copy boundary inside one checkout, for hosts that differ in file format rather than location. Its discriminating question - could one file satisfy both readers? - was run against the only fleet project with two substantial instruction files (1,427 + 2,405 words, two hosts, one checkout). Answer: yes, both read plain markdown, so the tree belongs in the link regime - and it is already there, correctly: the second file is a bridge that points at the canonical one and cites rather than restates its doc-sync obligation. The amendment therefore recommends no change to any fleet tree today. Recorded as a confirmation of the routing question rather than a defect. Return: when a fleet project adds a host needing a different rule-file format or frontmatter vocabulary. |
| 2026-09-04 | host-api-import-budget | untrusted-extension-host | tracklight | experiment | not-better | No fleet project hosts foreign code under an execution limit, so the technique was tested from the other side, against its own escape clause ("the API is already dependency-free - the right fix is upstream"). Seam: the client library a consumer imports. Measurable: runtime dependencies at the extension-facing entry point. A (as-is) = 0 - `dependencies: {}`, `peerDependencies: {}`, global fetch, and the entry's own header states zero runtime dependencies. B (with the stub substitution) = 0 plus a second implementation to maintain. So the escape clause fires and the substitution is correctly declined; the tree confirms the technique's upstream-fix preference. Return: when a fleet project executes user-supplied code under a wall-clock budget, or when a client acquires its first runtime dependency. |
| 2026-09-04 | fault-localizing-metric-set | metric-surface-contract | tracklight | experiment | unmeasurable | Seam: the ingest admission path and the alerts relay. The front door CONFIRMS the technique - `admitted_total`, `shed_total` and `timeout_total` are three producer-side outcomes at one hand-off, exactly the emitting-side rule. The gap is the duration split: no queue-delay, dwell or wait quantity exists anywhere in the crates, so a backlog and a slow consumer are indistinguishable - and the relay's own source comment names that confusion ('the wrong half of the system: the fleet is what changed, not the app that enqueued the work'), which is the defect arrived at independently. Unmeasurable rather than better because both arms need a behavioural run. Instrument that would decide it: the technique's own validation - replay a recorded relay incident against the current surface and against the surface with the wait/work split, and count which localizes the stage. Return: when such an incident is on record, or under a load run against the relay. |
| 2026-09-04 | three-state-outcomes (amendment: remediation per state) | ai-registry | experiment | better | Seam: `scripts/check-bundles.mjs:505` skips every absolute URL by an explicit guard, then reports "11016 links checked" - so the corpus's `verified_on` fields assert citation resolutions no gate can see. Measurable: dead external citations surfaced. A (gate as it stands) = 0 of 172 checked. B (three-state sweep, `scripts/check-citations.mjs`, advisory, never deletes) = 1 genuinely gone (an era-learn.eu funding page cited by a grant-format application, 404), 20 unverifiable (18x 403, 1x 405, 1x 429). Deleting on any non-2xx would remove 21 to retire 1 - a 95% false-deletion rate that would take the best-defended publishers first. **The apply step corrected the landing before it shipped**: v1 of the sweep reported 6 gone, 5 of which were `${...}` interpolations and regex fragments pulled out of fenced code blocks - the corpus's own `checker-false-positive-discipline` ("never pattern-match a language you have a parser for"), a 6x error in the alarming direction, narrowed rather than deleted. Return: when results are stored across runs, making the persistence predicate computable and promoting the check off advisory. |
| 2026-09-04 | gate-liveness (amendment: the observable is absence) | ai-registry | code | better | Seam: `.github/workflows/knowledge.yml` had `push`, `pull_request` and `workflow_dispatch` and **no `schedule:`** - no workflow in the repo had one. Measurable: whether a gate decommissioned from outside (withdrawn runner, a pinned action that stops resolving) is distinguishable from a quiet week. A = no, indistinguishable; a change-triggered workflow has no heartbeat and its last run is only evidence about the last change. B = a weekly cron gives every job a dated run in CI history, which is the amendment's obligation 2 (a surface that decays visibly) at the cost of one line. Also added the advisory `citations` job, gated `if:` to schedule/dispatch so it adds no requests to the merge path. YAML validated, 7 jobs parse. **A hypothesis was refuted en route**: the path filters looked like they might omit `taxonomy.json`, which `check-bundles` reads - they do not, it lives under `knowledge/`, and no finding was written. Return: when a provider-side inventory reconciliation is possible (obligation 3, which no repository-local check can satisfy). |
| 2026-09-04 | query-transparent-overlay | native-shell-integration | (none) | unapplied | - | Exclude a reading overlay from the accessibility layer rather than making it input-transparent. No authorized fleet project ships a native process that places a surface over foreign applications and queries their accessibility trees; the subject's own applications are the only trees that could witness it. Return: when a project grows a screen-reading or element-picking surface. |
| 2026-09-04 | observer-perturbation | native-shell-integration | (none) | unapplied | - | The instrument's presence changes the measured system - a fullscreen opaque surface trips the observed application's occlusion optimization and reparents its accessibility provider out of the queried subtree. The general form (an instrument sharing a screen, a lock or a device with what it measures) may have seams the specific form does not; a targeted sweep for it was not run. Return: when a project's instrument shares a resource with its subject, or on the next observability round. |
| 2026-09-04 | unexported-capability-ladder | native-shell-integration | (none) | unapplied | - | The rungs for reaching a capability a platform does not export, and the rule that ends the ladder: look for the queried subsystem's own opt-out before building a scoped replacement for its interface. No authorized fleet project reaches an unexported platform entry point. Return: when one does, or when a project spends more than a day working around a subsystem it is querying. |
| 2026-09-04 | completion-authority-arbitration | terminal-multiplexing | (none) | unapplied | - | Promotion to a protocol tier must revoke the heuristic tier's authority to conclude, not merely outrank it. No authorized fleet project runs two evidence tiers over one stream. The generalization (a lower-ranked rule that can fire while the higher-ranked one is silent has been made the default) is the part likeliest to have a seam elsewhere and was not swept for. Return: when a project drives a subprocess whose completion it infers from more than one signal. |
| 2026-09-04 | readiness-edge-detection | terminal-multiplexing | (none) | unapplied | - | Wait for the readiness edge, not the protocol's first sighting; emit a readiness marker only after the mode it advertises is enabled. No fleet project probes a third-party occupant for an injected protocol. Return: when a project's capability probe passes on evidence emitted during startup. |
| 2026-09-04 | capability-revalidation | terminal-multiplexing | (none) | unapplied | - | A capability probed once and trusted forever is the failure; plus the bounded acknowledgement window that keeps a run from waiting on a terminator it can never reach. Return: when a project caches a capability probe across a session rather than per operation. |
| 2026-09-04 | injected-hook-reconvergence | terminal-multiplexing | (none) | unapplied | - | An injected hook that loads before user-controlled configuration must re-converge each cycle, by assert-and-repair rather than reinstall. No fleet project injects configuration into a user-owned shell or editor profile. Return: when a project ships an integration script sourced alongside a user's own config. |
| 2026-09-04 | patching-mechanism-ladder | supply-chain | (none) | unapplied | - | Four patching rungs chosen by the dependency's shape, with mirror shadowing's three structural rejections. Checked the fleet for a vendored or patched dependency: none of the authorized projects carries one, so there is no divergence for a ledger or a mechanism to govern. Return: when a project needs a change upstream will not ship in time. |
| 2026-09-04 | signature-preserving-patching | supply-chain | (none) | unapplied | - | The patching mechanism is chosen against the distribution channel, not only the code: start-up modification yields a valid signature over modules that no longer determine behaviour. No fleet project modifies a dependency at either time, and only one ships a signed artifact at all. Return: when a project signs an artifact containing a modified dependency, or when heuristic malware detection quarantines a build nothing in the source explains. |
| 2026-09-04 | in-place-document-patch | settings | (none) | unapplied | - | The stored document is the source of truth and the runtime object is patched from it, so unrecognized members survive a round trip and observers keep their references. Swept the fleet for a settings store that round-trips a user-editable document: the projects with settings persist typed records through a schema, which is the regime `read-batching` correctly governs instead. Return: when a project's config file is hand-edited by users AND written back by the app - the two conditions together are what make replacement lossy. |
| 2026-09-04 | composite-condition-verdicts | pipeline-dag | (none) | unapplied | - | Three-valued composite operators, and the negation row: a `none-of` folded as the boolean inverse of `any` returns true exactly when the engine could not find out. Swept eleven fleet trees for a condition tree with all/any/none composition, instrument asserted against a known positive first; the two candidates were false (an i18n label for a filter button, and enum mirroring across a process boundary). No fleet project composes conditions that can fail to answer. Return: when a project grows a rule engine whose inputs can time out or be unsupported. |
| 2026-09-04 | contributed-document-admission | prompt-assembly | (none) | unapplied | - | What an assembler owes when it injects instruction documents its team did not author: identity from location not self-declaration, a declared capability as precondition never grant, invalid entries listed with diagnostics and never injected. No authorized fleet project injects third-party instruction documents into a prompt - the registry itself does, but it is the tooling and not a managed consumer. Return: when a project admits operator-installed instruction documents, which is a plausible near-term direction for two of them. |
| 2026-09-04 | execution-emitted-inverse | goat | code | better | Shipped `f3c9594`. Seam: `createUndoableOperation`'s rollback in the drag-operation layer, whose own comment said the displaced item could not be restored "without more info". The apply step refuted the comment - the plan builder already captures the displaced item at the instant it is still on the grid, so the information was in scope and the inverse was simply written in a different function from the mutation, which is the drift the technique names. Measurable: pre-operation state fields restored by undo of a displacing assign. A=1/4, B=4/4, ab-paired on the real modules with an identical fresh store. Return: when a second project's hand-written inverse claims information loss - check whether the forward path already captured it. |
| 2026-09-04 | field-captured-operation-traces | personas | experiment | not-better | Shipped `25233f43d` (the generator widening, not the lane). Seam: the render-plan compiler's property suite. The compiler rejects only `fps == 0` and snaps every boundary to `1/fps`; the generator drew from three friendly divisors. Widened to a 4:1 mix with the full accepted range and ran 2,000 cases per property - **0 failures**, and arm A's space is a strict subset of arm B's, so 0 vs 0 over 8,000 cases. The identified bound was real and closing it revealed no defect, so no field lane is earned here: the cheap alternative (widen the generator) was sufficient. Ten of eleven fleet trees have no property-testing dependency at all, checked at manifest level with the instrument asserted against a known positive - a searched absence, not an unattempted row. Return: a user-reported render defect that does not reproduce from a generated composition. |
| 2026-09-04 | rewrite-behavior-pinning | agent-instruction-files | fleet-wide (7 repos) | experiment | not-better | Swept 12 always-loaded instruction assets across 7 repositories: 152 revisions, 35 bulk rewrites (>=25% of lines replaced). A = the review that shipped all 35; B = a modality-delta detector asserted first against 3 known positives (hedge->mandate, hedge deleted, permission->imperative) and 4 known negatives. Mid-state 10 flagged, **0 confirmed** - every candidate a pairing artifact of a prose-to-table restructure or a re-wrapped line, the top-scoring one matching a sentence against its own continuation. A lexical pin is not gate-grade, exactly as the technique predicts (a text check re-implements the per-line funnel it exists to escape), and the technique gains the amendment with the number, because the lexical pin is what everyone builds first; the bias runs the wrong way too, since the strongest inversions rewrite the most words, so every threshold that quiets the detector also blinds it. Structural fact nobody designed: 24 of the 35 rewrites GREW the file and not one was undertaken to cut per-turn cost, so the trigger has never fired in this fleet - the population was missing, not the instrument - and the largest asset is a 114 KB method file at 30 revisions never once reduced. Return: when a fleet instruction asset is first rewritten to reduce its per-turn cost, and then with a behavioural arm (the same task under both revisions, the permissive branch as the assertion) rather than a lexical one. |
| 2026-09-04 | durable-store-failure-posture | agent-memory | personas | experiment | better | Seam: `record_phase` — a read-modify-write over one durable JSON column that parsed it with a tolerant default, so a corrupt-but-recoverable payload was replaced by a one-element array and the call still reported success. Measurable: bytes of the unreadable payload surviving the next write. A=0 of 35, B=35 of 35, on a real store with three observation points (seeded / corrupted / after) because a two-point pair ties when the corruption never lands. Product fix shipped on `master`; the paired Rust test is committed beside it but **could not be run** — the crate's build script fails on a pre-existing Tauri capability error (`Permission updater:default not found`) before any code compiles, which is why the arm is `experiment` on a faithful replication rather than `code`. Return: when that build script is fixed, run the committed test — it is the same assertion. |
| 2026-09-04 | termination-and-reaping | subprocess-lifecycle | (none) | unapplied | - | The amendment splits rung 2 into a claim deadline and a completion deadline, and applies only where a polite stop's *receipt* is observable. Searched: no fleet project has a cooperative stop protocol with an acknowledgement — children are spawned with a deadline and killed when it fires, so rung 1 has no ack to split. The project closest to the shape records a clean-shutdown marker, but it is consumed at the next *start* to classify the previous exit, not by a running child to acknowledge a stop request. Return: when a project gains a drain protocol whose request is claimed by the child. |
| 2026-09-04 | operator-tier-code-loading | agent-runtime-assembly | personas | simulation | unmeasurable | The fourth-tier amendment governs a runtime whose extension configuration is written by the agent. Structural read: personas has no dynamic code-loading surface at all — no `libloading`, no `dlopen`, and its doctrine assets are `include_str!` at compile time — so it is already on the rule's correct side by construction (deny code entry, extend through data). A and B are therefore the same tree and no arm exists. The accepted `runner-extension-surface` direction does not change this: it adds compiled-in dispatch hooks, not external code loading. Instrument that would make it measurable: a project that loads operator- or agent-supplied runner code at runtime. Return: if that direction is ever widened to load external modules. |
| 2026-09-04 | declared-verdict-over-inferred-wreckage | self-healing | personas | simulation | better | Seam: `boot/recovery.rs` states the pattern outright — "absence of the marker IS the crash signal" — gating one recovery sweep on a clean-shutdown marker consumed at startup. Three real cases from the tree: (1) first-ever launch, where no marker has ever been written, is classified as a crash; (2) an OS-initiated termination (upgrade, shutdown) writes no marker and is classified as a crash though it is not one; (3) the tree's own comment records that **four sibling sweeps still "declare blind"** because widening an absence-signal "would only make their wrong verdicts rarer, not righter" — which is the amendment's prediction in the author's own words: you cannot safely widen a signal you cannot decompose. Under policy B (the start records *why* it started; a start with no reason is unknown rather than a crash) cases 1 and 2 resolve and the gated sweep's precision rises. Falsifier, stated: if the four blind sweeps stay blind under B, the blocker is row classification and not the signal — and the tree says it is, so B is an improvement in precision, not the unblock. Return: when a start-reason is threaded through and the four sweeps are re-examined. |
| 2026-09-04 | runtime-bound-checkpoint | execution-state-checkpointing | personas | unapplied | - | **Corrected within the run by a second reader.** The first filing asserted no fleet project captures execution state; the peer study found `src-tauri/engine/src/git_checkpoint.rs` - 272 lines implementing checkpoint, snapshot, rollback and fork, backed by a migrated table, declared `pub mod` - with **zero call sites** (verified independently: 0 occurrences of `git_checkpoint::` outside its own file). The seam exists and is unwired, which is a different row from an absent seam and a far better return condition. No arm today because nothing calls it. Return: when the module gains its first caller - that is the moment the fingerprint rule applies, before the first restore ships, which is the cheapest time it will ever be applied. |
| 2026-09-04 | restore-semantics-belong-to-the-format | execution-state-checkpointing | (none) | unapplied | - | Same absence as above: one capture API over backends whose restore semantics differ requires more than one backend, and no fleet project has a second. Return: as above. |
| 2026-09-04 | resume-mints-a-duplicate | execution-state-checkpointing | personas | unapplied | - | **Corrected within the run**, same catcher as the row above: the unwired checkpoint module implements `fork`, so the fleet does hold a fork-a-running-state primitive on disk. It has no callers, so nothing is duplicated yet and there is no arm. This is the technique whose window closes first - identity divergence has to be designed into the fork path before its first caller, because retrofitting it means reasoning about instances already minted. Return: when the module gains a caller, or when the appliance lane ships an image that boots from identical state on two sites. |
| 2026-09-04 | the-record-outlives-the-rewind | execution-state-checkpointing | personas | unapplied | - | **Corrected within the run**: the unwired checkpoint module supplies `rollback`, a second reset axis, so the state matrix would not collapse to one column as first filed. Its rows are already visible - a rollback of the working tree resets code while leaving the local database, the run ledger and the companion event history untouched, and that asymmetry is exactly what the technique says to publish rather than discover. No arm while the module has no callers. Return: when it gains its first caller, at which point the matrix is owed before the first rollback ships. |
| 2026-09-04 | coverage-assertion-gate (proposed, not landed) | scale-investment-timing | ai-registry | experiment | not-better | Tested whether a golden path asserting its neighbourhood coverage needs a gate, since the bundle checker verifies links and structure but never a prose coverage claim. A detector was asserted against a known positive (the uncorrected sentence in `HEAD`) and 411 known negatives, and the mid-state was printed so both arms provably ran. **Population: exactly 1 across 412 subjects / 4,653 concept documents** - the sentence this run had already fixed by hand. A gate for a population of one is over-building, and the verdict comes from the subject under test: over-building fails silently and generates no incident. **The verdict changed the landing** - it prevented a checker rule this run would otherwise have shipped. Return: when a second unbacked coverage assertion is found by any run, the population is 2 and the gate is worth re-pricing. |
| 2026-09-04 | stage-ordered-fuzz-targets | test-input-generation | pumper | task | better | Direction accepted at the 2026-09-04 gate and executed in-session on worktree branch `direction/test-input-generation`, **merged** after review (`14b01cb`, `--no-ff`). Two generated-input targets beside the existing end-to-end harness, one per hook stage, each asserting its own stage oracle instead of an end-to-end outcome. Measurable: distinct transform-stage failure classes reachable behind a predicate that answered nothing - **0 by construction before, 2 after** (`hook_malformed`, `hook_trap`) over the 117 of 300 envelopes the predicate stage stopped, a set no end-to-end case can reach. Gate re-run independently by the director: 2 passed, fmt clean, clippy zero warnings. **Size did not hold** - 346 code lines against a stated ~150, and the overrun is honest: the builders, `wat` fixtures and host constructor already exist in the sibling e2e file as *private* helpers a second test module cannot call, so ~180 lines are re-declared scaffolding. The worker declined the shrink-by-dropping-a-target remedy because dropping the transform target would still have missed the budget while deleting the measurable. **The bound worth carrying forward**: the tree's existing `wat` fixtures ignore their input, so the envelope axis is load-bearing for the transform oracle and near-inert for the predicate one (discard fraction exactly 0.000) - input-reading fixtures are the owed follow-on. Return: when those fixtures exist, re-run and see whether the predicate target's discard fraction moves off zero. |
| 2026-09-04 | recovery-path-as-loss-signal | prompt-assembly | tracklight | experiment | better | The seam is this registry's OWN prior landing: an elision shipped into the tool server at `crates/mcp/src/resources.rs`, whose 4.7x was measured with `arm_a.len()` against `arm_b.len()` - the transform's own output, which is exactly the boundary this technique forbids. Nothing in the crate counts recovery: by its own module doc it never touches the DB, and its only per-request trace logs the JSON-RPC method rather than the tool name, so the read that elided and the call that recovered are the same two strings. Mid-state over the complete unit of work: break-even at a **67.5% recovery rate** (0 recoveries 21.1% of arm A, 135 recoveries 100.0%, 200 recoveries 138%); on turns rather than bytes arm B is worse from k=1. Both readings stay open because the rate is unobserved. Structural fact nobody designed: `elide_payloads` tests for a map keyed `event` - a SHAPE test, not a size or class test - so the elision never fires for the event resource kind at any size, which is also the only reason the break-even terminates. Return: when the stdio trace carries the tool name and the elided branch, the rate becomes a grep over the server's own stderr - it counts 2 of the technique's 5 recovery-family members and any rate from it is a floor. |
| 2026-09-04 | elision-to-a-refetch-pointer | prompt-assembly | tracklight | code | better | **Shipped `7c746eb`.** The classing amendment tested against the same seam found the threshold is a hybrid (pure size outside, class-correct elision inside, and NO size rule inside the class) and that the missing inner rule is a live defect: the marker is ~44 bytes and fields of any size were elided, so a wide trace of small payloads rendered LARGER than the un-elided compact form it replaced, while still reporting the content gone. Measured in the implementation itself, not a model: 400 spans x 4-byte payloads, un-elided compact 56,228 -> rendered **85,828**, +29,600 bytes (+52.6%); with a 64-byte floor, 56,228 -> 56,228, +0. The control holds - the case elision was built for is unchanged at 206,644 -> 43,628 (21.1%), still past the 4x floor its own test asserts. 59 tests, clippy -D warnings and fmt --check clean. **The arms are byte-identical at BOTH endpoints and diverge only across the middle** - the false tie the mid-state rule exists to catch - and a byte-faithful reimplementation calibrated at one point had the direction right and the magnitude wrong, so the paired run in the real language is what earned the commit. One sub-claim came back `not-better` and was kept: the lossless-first ordering has no purchase where the trigger already measures the compact form, because "lossless suffices" and "the trigger did not fire" are then one predicate (swept 1-200 spans; the set of bodies that are lossy though lossless would have fit is empty). Second defect recorded and not fixed: the budget is decided on `compact.len()` and paid in `to_string_pretty`, overshooting by up to 9.4% at 24-26 spans. Return: re-measure the crossover against a captured real trace rather than the synthetic fixture. |
| 2026-09-04 | consumer-coupled-decoration | prompt-assembly | tracklight | experiment | better | The three sites named in the brief were **rejected**: per-payload truncation markers, not per-item decorations, so the multiplier collapses from items x payloads x sessions to payloads x sessions and `context-budgeting` already owns them - the two constructs look identical and only the multiplier separates them. Two real decorations found. The elision marker passes the audit (consumer, use and failure all named at the site) and fails the GRANULARITY bound instead: attached to every field regardless of size, it is ~16,400 bytes of markup standing in for 2,400 bytes of content on a small-payload body, 7:1, inside a transform whose purpose is to shrink. Table-cell padding in the render crate has no nameable consumer on the tool-server surface **by the crate's own statement** (it concedes Markdown renderers re-align it), and is wrong-granularity: built for the CLI surface, attached in the shared primitive, paid by both. A clean pass was recorded as a control - an audit that only ever finds defects is not being run as a form. Landings 2 and 3, orthogonal audits by the technique's own account, converged on one four-line diff, which is stronger evidence for the audit-before-compressor ordering than either alone. Return: when the padding is re-sited to the CLI client, or when a render-crate byte-length test exists to size it - no byte assertion exists anywhere in that crate today. |
| 2026-09-04 | bounded-projection-of-external-work | agent-runtime-assembly | gravitone-gcloud | experiment | better | Grouping wins where completions co-arrive in one commit, which is how this tree actually delivers (the store restores up to 50 events and the effect runs once over all unread): 3 parallel completions 3 utterances/2800ms -> 1/0ms; 10 unread 9 utterances with **1 never spoken** -> 1 with 10/10 delivered. **The mid-state is the whole finding** - arm A's queue peaks at 8 on the commit, the 11th announce overflows and sheds an item whose key was already claimed at enqueue, so that completion is silently never voiced; endpoints alone read "9 vs 1" and hide the lost item. The set-key arm is a live defect here rather than hypothetical: because the unread list is cumulative, a set key changes on every add and re-speaks everything already said, 6 repeat deliveries against 0. Instrument certified by transcribing the project's own announcer policy and mutating it - 7/7 properties on the faithful copy, 1/7 with the dedupe guard deleted, 4/7 with the queue deleted (correctly still passing the two that do not depend on it). Structural check enumerated the optional payload half: **8 branches across 7 call sites, all matching the discriminator**, and classifying the one site an earlier merged row had hidden forced out a sharpening - the discriminator applies per FACT, not per notification, since one message may carry one fact and point at another. The token claim was explicitly refused: the reader here is a person on a serial speech channel, so only the wake term transfers. Return: when the bell gains a bounded collection window - grouping alone TIED arm A on staggered arrivals and only separated once a 1200ms window collapsed the commits. |
| 2026-09-04 | recovery-path-as-loss-signal | prompt-assembly | politicas | unapplied | - | Precondition fails: the listing caps at `getDenikData.ts:487` and `readReviewAudit.ts:66` truncate with no recovery path, so there is nothing to instrument. Notable as a near-miss the technique explains: politicas instruments the transform FIRING, which is precisely the compression-ratio-style signal this technique says cannot measure need. Return: if a cursor or pointer makes the dropped rows fetchable. |
| 2026-09-04 | recovery-path-as-loss-signal | prompt-assembly | systedo-case | unapplied | - | Precondition fails: `compactEvidence` (`extract.ts:296`, a 140-char cap into an LLM prompt) and `digest` (`validation.ts:409`, lead-plus-closing with the middle elided) both discard. Both nevertheless SATISFY the technique's rule that a transform whose recovery cannot be instrumented should ship conservative, and the code comment argues for it in those terms. Return: if evidence or article bodies become addressable by id. |
| 2026-09-04 | consumer-coupled-decoration | prompt-assembly | systedo-case | unapplied | - | `patternPromptLine` (`extract.ts:315`) is the one confirmed per-item prompt-payload decoration outside tracklight in the fleet, and it PASSES the audit - its consumer is named at the site. Recorded because a searched pass is evidence the audit is a form rather than a defect hunt. Return: if the pattern line gains a second consumer or the prompt shape changes. |
| 2026-09-04 | elision-to-a-refetch-pointer | prompt-assembly | pumper, ascent, personas, kp, pof, goat, gravitone, gravitone-gcloud, personas-web | unapplied | - | Searched absence across all nine remaining trees, instrument asserted against tracklight as a known positive first (14 matches, 2 files, 573 files searched). The first sweep returned all-empty across six projects and was a **false negative from an over-specified regex**, caught only because the known positive was asserted. Return: when any of these grows a payload cap with a pointer back. |
| 2026-09-04 | mirror-type-at-the-edge | module-design | politicas | code | better | Arm A was run, not predicted: a probe asserting all four verdicts of an operator-console gate on the project's own unit lane reached **1 of 4** — the branch returning before the host read passed, the other three threw `cookies was called outside a request scope` at the same line, so reachability stopped exactly at the host-bound read. Arm B mirrored the request-scoped cookie jar to the two optional strings the verdict actually reads, moved all four verdicts to a pure function in a directory the unit lane collects from (`app/**` is not in its globs), and left the ambient read in place: **4 of 4**, 6 assertions, typecheck clean, full lane 221 files / 3040 tests. Measurable: branches of the decision the project's own runner can reach, **1 → 4**. The tree amended the technique — the shim keeps one guard deciding *whether to touch the host*, because reading cookies opts the route out of static prerendering and a branchless shim would have changed an unconfigured route's rendering mode under cover of a refactor; "no verdict branch" replaced "no branches". Return: if a second verdict-bearing `return` appears outside the core, the extraction has been undone — that, not the branch count, is the check. |
| 2026-09-04 | tool-schema-design (error channels: route by actor) + one-validation-door at the tool surface | mcp-tools | pumper | **code** | **better** | **`ab-paired`, one test, both arms, same harness — and the test was written and run BEFORE the change, so Arm A is measured rather than asserted.** Seam: `crates/server/src/mcp/mod.rs` `tools_call` handed arguments straight to handlers, never checking them against the `inputSchema` the server publishes for that tool — 28 advertised constraint keywords across 7 tools. A = `search` with a misspelled `sort` key returned `isError:false` and ran with the key silently dropped (a confident answer computed without the argument supplied, and no signal in the result, the logs, or the caller's context); `query_dataset` missing its declared-required `dataset` also accepted. B = both refused in-band (`isError:true`) with each violation named by JSON pointer and an instruction to re-read the schema; the refused request provably never reaches the index; the valid call is unaffected. Gate: 506+9 tests pass 0 failed, clippy `-D warnings` clean. Shipped `1158645` on `master` with a pathspec, not pushed. **The tree corrected the finding and that is the durable half:** the first reading was "no validation", which is false — the handlers DO validate by hand, and `search` already refused a bad `sort` enum and a blank query IN BAND, i.e. the corrected registry rule implemented before this corpus wrote it down. The real defect is that the set of constraints *enforced* lived in the handlers while the set *published* lived in the schema, and the two had drifted; `additionalProperties` and `required` were in the second set and in no handler. That drift is invisible to any reviewer looking at one side. Honest limits: the validator is compiled per call (free at this scale, a cache-invalidation question at another), and an `inputSchema` that fails to compile is warn-logged and SKIPPED — a deliberate fail-open that silently degrades that one tool back to Arm A, tolerable only because the schemas are literals in the binary rather than operator input. Return: a tool whose schema stops compiling, or a handler rule that duplicates a constraint now enforced at the door |
| 2026-09-04 | suspendable-request-classes | mcp-tools | — | **unapplied** | — | No fleet seam, stated rather than manufactured. No managed project's tool server implements elicitation or any pause-for-a-human round trip, so there is no call that could suspend and nothing to partition. Checked across all five fleet MCP surfaces rather than assumed. Return: the first fleet tool server that asks its caller a question mid-call |
| 2026-09-04 | sealed-continuation-state | mcp-tools | — (source tree only) | **simulation** | — | `structural-only` against the standard itself (`spec--sealed-continuation-state`). No fleet server mints a continuation — the tool servers are single-process or hold no cross-round state. **The finding was a placement gap, not a coverage gap:** the corpus already held the full mechanism (HMAC envelope, 32-byte key floor, 600s TTL, domain-separated bind tag so the principal never appears on the wire, constant-time compare) inside a DATED APPLICATION about one SDK's transport behaviour, framed there as "echoed back as an ordinary argument" — the words of the very rule it is an exception to. An application can carry a mechanism its subject never learned, and nothing structural surfaces it, because applications are read as evidence for techniques rather than as candidates to become them. Return: the first fleet server that pauses a call for human input across instances |
| 2026-09-04 | enumeration-without-a-scope | mcp-tools | personas | **simulation** | **better** | `structural-only`, and the verdict rests on convergence rather than on a measurement. Two independent systems reached the same shape: the standard shipped a task list endpoint with a documented scoping obligation, then DELETED it when sessions were removed ("there is no other natural scope a server can define unilaterally"), and personas' peer-agent surface implements per-item get/cancel with an ownership comparison on every call, a not-found refusal that never names the owner, and **no list endpoint at all** — with the anti-enumeration reason written down in its own design notes. Neither read the other. `better` because the alternative design was tried, shipped in a released revision, and withdrawn. Return: a fleet project that grows a list over per-caller objects |
| 2026-09-04 | deprecation-by-version-arithmetic (amendment: the declarer must own the runtime; the consumer must step smaller than the window) | release-pipeline | — | **unapplied** | — | No fleet project publishes a specification or wire contract whose implementations it does not control, which is the regime the first half of the amendment governs. The second half — a consumer who jumps the whole window in one upgrade was never warned, however correct the window was — is checkable in any project that publishes a deprecated symbol with a named removal version, and no fleet project currently carries one. Return: the first fleet project that deprecates a public symbol with consumers it cannot see |
| 2026-09-04 | constrained-decoding-is-a-shared-budget | structured-output | tracklight | **code** | **better** | `ab-paired`. The dialect half was already built there - one caller schema rendered into three provider dialects, with an overridable API base so the same model is reachable by several routes. The budget half has NO seam: one claimant only, which is the case where the rule is genuinely unnecessary. The structural fact nobody designed: the result type already degrades the sibling guarantee in-band (a three-state determinism enum read at 66 sites, explicitly weaker when sampling knobs were rejected and retried without them) while schema enforcement rode on a stderr line - so a caller could not tell provider-enforced syntax from prose that looks structured. Measurable: call sites able to distinguish the two from the value they hold, A=0 -> B=all. Shipped with a proof test; both blocking gates green, 127 engine tests pass. Return condition: a second independent party can contribute a constraint to the same request, at which point priority-and-shedding applies |
| 2026-09-04 | cancellation-needs-a-terminable-unit | subprocess-lifecycle | personas | **experiment** | **better** | `structural-only`, and the tree contains **both sides of the discriminator**, which is better evidence than a tree that simply got it wrong. The agent-execution canceller runs the full ladder - mark cancelled, kill the child OS process (its comment names the stake: stop API credit consumption), then await the held handle under a bounded 5s grace so metrics finish writing. The background-job path has no unit to terminate and writes the same terminal status anyway: 29 guarded-spawn sites, 2 bind the handle and **both are the same unit test**, so 27 production tasks are un-abortable and reaped is structurally zero - and the job entry type had no slot for a handle at all (six fields, none of them one) - a detail this run asserted from a second-hand report and had corrected by the executing worker. The two paths differ in exactly one property, whether a unit exists whose death the host survives, which is the technique's ordering rule (placement decides cancellability) showing up as an accident of which spawn helper a caller reached for. Instrument asserted against the helper definition as a known positive, both binding sites opened individually, and the primary path's kill verified at source after a peer study corrected an earlier over-broad reading of this row. Order of work: stop asserting a reclaim on the path that cannot perform one, THEN close the gap - (2) without (1) leaves the window where the claim is still wrong. Return condition: when the ancillary spawn sites retain handles, so signalled-vs-reaped stops being a constant zero |
| 2026-09-04 | ordered-yield-composition | session-continuation | — | **unapplied** | — | The shape is present in one fleet project - 18 restated checks of two mode flags across 8+ files, which is the exact anti-pattern the technique replaces - but those modes gate PERMISSION rather than a candidate yield, and claiming the mechanism applies without establishing that would be an overclaim. Return condition: when a fleet project grows two behaviours that each want to hold a session open past the model's own stopping point |
| 2026-09-04 | speculative-compaction-splice | prompt-assembly | — | **unapplied** | — | No fleet project runs a conversation-window compaction it owns; the compaction hits in the nearest candidate are memory records and review summaries, not transcript spend-down against a provider window. Return condition: when a project compacts a transcript against a context window it has to stay inside |
| 2026-09-04 | dated-capability-matrix + substituted-result-attribution | personas | `code` | **better** | `ab-paired` | Two headless spawns of the same agent CLI, one machine, one account, one reported version (2.1.260), minutes apart, differing only in one environment variable on the child: the init payload reported skills 24 vs 25, slash_commands 59 vs 61, plugins 1 vs 2; tools (30) and agents (5) did not move. The project's probe module claimed it reported what is available "on this machine + account" - two axes where three decide, the third being what the session's remotely-fetched flag payload switched on at startup. Corrected the scope claim with the measurement, and added a cache-replay marker so a stored capability set is not readable as a fresh observation; the tree already stamped freshness at probe time rather than serve time, so half of substituted-result-attribution was satisfied before the technique existed. Gate green (crate type-check, generated binding regenerated from the Rust type, formatter clean); shipped. Return condition: a payload that moves the two arrays the feature gate actually derives from, at which point that gate is itself session-scoped and belongs on the matrix's degrade branch |
| 2026-09-04 | enforcement-demotion (model-backed hook amendment) | fleet-wide (3 hook-carrying projects) | `simulation` | **unmeasurable** | `structural-only` | The amendment says a rule demoted into a model-backed hook has changed location, not determinism. Audited every settings/hooks file tracked in twelve fleet projects, with the instrument asserted against known positives first: three projects carry hooks, all three are the deterministic `command` type, **zero model-backed hooks fleet-wide** - so the amendment is preventive here rather than corrective, which is a real measured negative and is why the verdict is not `better`. Its live content is the destination sort applied to the doc-sync hook those three projects share: a pure decision function with three exit states that decides by program on a **path-coupling proxy**, so the trigger is program-decidable while the verdict ("this change deserved a doc update") is a judgment. That is precisely the case the amendment's step 2 newly routes. No instruction file over-claims it - all three name the gate without calling it enforcement - so nothing needed correcting. Instrument that would make this measurable: the proxy's false-positive rate over recorded firings (the corpus already knows the firing rate, 122 of 1,631 turns, but not how many firings were true positives). Return condition: when that rate is computed, or when any fleet project adopts a `prompt` or `agent` hook type |
| 2026-09-04 | pairing-schedule | eval-harness | tracklight | `simulation` | **not-better** | `structural-only`. The technique told a disconnected comparison graph to report components rather than a leaderboard; this tree publishes exactly such a graph (a pooled model leaderboard whose contributors each measure on their own private cases — disconnected by construction and permanently, because the privacy floors that make buckets publishable are what stop cases travelling), where suppressing the order means shipping nothing. Its answer is to keep the order and widen the interval with a between-source variance term; its own module records the defect that replaced — naive pooling made five contributors who disagreed produce a NARROWER interval than five who agreed. **Not-better here means prior conformance and then some, not refutation**, and the finding ran backwards into the corpus: the widened-interval branch was written into the technique, discriminated by whether the ranking is an internal selection or the deliverable itself. Return condition: when a pooled row can carry a case-overlap figure — the interval measures disagreement, not incomparability, and two differently-distributed corpora that happen to agree still publish a narrow interval |
| 2026-09-04 | scenario-design (elaboration-axis amendment) | eval-harness | pumper | `simulation` | **not-better** | `structural-only`. Prior conformance. The amendment says how much prompt scaffolding a scenario carries is a measurement decision that must be declared and version-pinned. This eval declares the scaffolded setting explicitly (the production prompt verbatim, with the reason the frozen snapshot does not move what is measured), and guards the pin with a test named for the failure it prevents — a changed prompt fails the eval rather than passing silently. Nothing to adopt. Return condition: the suite proves only the scaffolded half, so it cannot answer a model-selection question, which needs the bare setting it deliberately does not run |
| 2026-09-04 | effort-calibration (tie-instrument amendment) | model-routing | tracklight | `simulation` | **not-better** | `structural-only`. Prior conformance, and the strongest corroboration the amendment has. The amendment argues a tie is a property of the instrument before it is a property of the tiers; this tree selects its judge on **spread** — the gap between the score it gives good answers and bad ones — on the stated grounds that a narrow-spread judge cannot separate quality from deflection at any threshold. Measured, not reasoned: the cheapest judge had respectable error and the worst spread; the dearest failed by being generous in the middle, passing a factually wrong answer. Same-family grading is routed to randomized-order pairwise explicitly. The half this tree does NOT carry is what happens when no better judge is available — naming the instrument beside the tie, and filing a cheaper tier win under quality rather than cost. Return condition: when that caveat line gains an instrument name |
| 2026-09-04 | turn-classification (fourth-axis amendment) | model-routing | ascent | `simulation` | **better** | `structural-only`, **shipped** (row in the project ledger, `22ee13ca`, branch `moonshot/wave-4`, not pushed). The tree argues the distinction and then declines to route on it: its text module exists precisely because the non-scan surfaces need free-form judgment and their own schemas, and the same header says it is not a second provider path so that which-provider has exactly one answer. So three real classes — the scan assessment leg, the org-memory write gate, the interactive chat turn — resolve one model id (`resolveBedrockModel`) and one global reasoning budget (`thinkingBudgetTokens`, zero-arg), and raising the dial for the chat turn pays for reasoning on every scan call. Predicted small rather than a refactor because the classes already exist as separate functions. **Falsifier:** enumerate the callers of the text seam — if the gate and the chat turn share one entry point with no caller-distinguishable site, the class would have to be inferred from content (forbidden) and the honest answer is two classes. Return condition: the split ships as structure with one model in the table until a blind-preference instrument measures the tier, which this tree has no harness for |
| 2026-09-04 | co-transactional-consume | delivery-guarantees | tracklight | **experiment** | **better** | `structural-only` for the tree, and the tree is the technique's confirming instance rather than its patient. The discriminator ("does every effect of this operation land in the store that holds the row?") was run as a classifier over 8 write paths in one crate and scored against two policies on the same input: under the subject as it stood — three postures, stable identity and a dedup barrier required of every accepted event — **2 of 8** classify correctly, because the 6 local paths carry none of that and are nonetheless right; under the technique, **8 of 8**. **The partition is exact and nobody designed it**: `alerts`, `admission`, `price_fill`, `collective` carry **0** lease/fence tokens each (one transaction takes the row, does the work, commits, so a crash rolls back effect and claim together and leaves nothing to reclaim); `jobs` (external model judges), `relay` and `relay_lease` (an action handed to a device) carry **23 / 18 / 9** — a persisted claim, a fence the holder renews, and a completion write conditioned on still holding it. Nothing sits between, across a dozen write domains, and **no document in the tree stated the rule** — which is the strongest available evidence the discriminator is forced by the substrate rather than chosen, since a convention would have drifted and a written rule would prove only that someone read it. **No behaviour changed and none should have**; shipped the crate doc that states the question, both answers and the prohibition, because the silent-revert risk the technique names was live and undefended (one added network call moves a local path across the line into the group with none of the machinery it now needs). `cargo fmt --check` + `clippy -D warnings` green on the crate; `ed9e0d7`, `8be0bb9` on `main`, not pushed. **Honest limit**: this confirms the discriminator and measures the corpus's fit; it does **not** price the technique's cost claims — every local handler in this tree is short, so the held-transaction and connection-ceiling penalties never bite. Return condition: a tree that pushes a slow or externally-paced handler into the local group, which is what would measure where the shape stops paying |
| 2026-09-04 | bound-and-measure-the-tool-roster (direction, from the harness peer study) | mcp-tools | personas | **task** | **better** | Accepted at the Phase 7.7 gate, executed in an isolated worktree, merged after the director re-ran the gate (`orchestration::mcp::handlers` 6/6, `http_engine::` 19/19 with 3 live-API ignored, `prompt::` 29/29). A persona can now declare its roster positively; the HTTP path INTERSECTS the declaration with the existing security allowlist so it can only narrow, never reopen; and roster size plus serialized bytes are recorded per run beside the existing latency and cost telemetry, so the correlation the source measured becomes one query rather than a belief. **The worker overrode the spec and was right**: it used the existing free-parameter list instead of the new column the study named, because the tree already routes two other execution settings that way. **Drive-by defect found and fixed**: the HTTP branch returned above the only site that persisted a config snapshot, so every remote run had stored a NULL execution config. Return condition: the latency delta is now READABLE from stored data but not yet MEASURED - that needs accumulated real runs |
| 2026-09-04 | athena-report-tool-defect (direction, from the harness peer study) | eval-harness | personas | **task** | **better** | Accepted at the Phase 7.7 gate and merged. A fifth verb on the existing MCP channel gives agents the bug-report path the source calls AutoQA, with the schema enum GENERATED from the existing failure taxonomy so advertised and stored vocabularies cannot drift, and a non-member token rejected rather than coerced. Agent-authored text is treated as data: flattened, stripped of control/bidi/zero-width characters, then role-prefix and markdown leaders peeled in a FIXPOINT loop - the same shape `prompt-safety/output-sanitization` requires - and a structure-only input sanitizes to empty and is rejected. **The study's named reader had drifted**: the promoter it pointed at is gated behind an env var the tree's own audit records as shipped-but-never-armed, so writing only the audit row would have produced exactly the write-only channel the direction forbids; the worker promoted through the same ungated door three existing production sites already use. Return condition: the source is candid that the signal is noisy - a triage pass is owed once reports accumulate |
| 2026-09-04 | make-cancellation-mean-cancellation (direction, this run's own technique applied at project scale) | subprocess-lifecycle | personas | **task** | **better** | Accepted at the Phase 7.7 gate and merged. Built in the order the application document argued: the honesty half first (a cancel that was *requested* is now distinguishable from one that *reclaimed*, and the terminal row refuses to say the job stopped when nothing proved it), then the gap half (a handle slot, auto-registration, and execution.rs's own ladder - signal, bounded grace, abort), plus the two counts the technique names. Six user-visible cancel paths converted; five deliberately not, each with the reason recorded at its definition, and all of them honestly reporting `Requested`. **Director reproduced the worker's mutation check**: flipping the no-handle arm to `Reclaimed` turned exactly one test red and nothing else, so the gate measures the behaviour rather than the compile. **The worker corrected this ledger's own earlier claim** that the job entry already carried a handle slot - it did not, and since the entry is cloned while a handle is not, the slot needed shared interior ownership. Return condition: the 2s grace is a constant and nothing has measured what these workers need to flush |
| 2026-09-04 | wire-run-checkpoints (direction, raised by the earlier exo run) | execution-state-checkpointing | personas | **task** | **better** | Accepted at the Phase 7.7 gate and merged; a 272-line checkpoint module with ZERO call sites is now wired into the auto-run loop at the wave boundary - the only moment the loop drains and no agent is mid-write - with a baseline capture and typed gap rows where a capture could not be taken. **The worker refused the spec's named function with evidence and was right**: the spec said to call `checkpoint_stage`, and the module's own doc at `git_checkpoint.rs:103` says not to use it inside a repository the user owns because it switches branches. Using the snapshot path instead exposed a real hole that was then filled rather than forced: the snapshot had **no matching restore**, and the only rewind on offer would detach HEAD onto a dangling stash object - so a non-disruptive capture had a disruptive undo. Return condition: the spec's own falsifier (abandon-after-stage-1) needs accumulated runs to instrument, and the survives-the-reset matrix is stated in prose but not yet published as the matrix its technique asks for |
| 2026-09-04 | authored-voice-identity (fourth kind: **materialized**) | voice-io | gravitone | **experiment** | **not-better** | The project already implements the amendment's inverted storage rule - voices are pre-exported ~10 MB embedding artifacts fingerprinted by their bytes, not by any specification. The A/B substituted a content hash for the shipped `mtime:size` fingerprint over six artifact-delivery scenarios: arm B correct 6/6, arm A 4/6 (one stale-audio case where a metadata-preserving copy installs new bytes under an unchanged fingerprint, one needless invalidation where an image rebuild resets mtime over identical bytes). **Still `not-better`, and the measurement is the reason**: the fingerprint is computed inside the cache KEY and therefore before the cache LOOKUP, so hashing taxes every request including the hits - **18.369 ms vs 0.005 ms**, a 3,700x increase on the exact path whose stated purpose is "the difference between an instant demo replay and another full render", on a CPU-only Arm target. The tree forced the better rule, now in the technique: **establish the artifact's identity when it is installed, not when it is used**, and make the installer responsible for moving the cheap identity. The project ALSO refuted the amendment's consent paragraph outright - it stores a content hash of the source clip in the consent receipt, stamps it forward onto every rebuild, and has a one-directional consent-laundering guard on export; the paragraph was rewritten around that. Return condition (seam class, so nobody re-runs it): a per-request cache key over a large identity artifact - close it at the write side, not the read side |
| 2026-09-04 | emit-behind-the-revision-window | streaming-output | gravitone | **simulation** | **not-better** | Three real cases walked from the project's conversational path, which solves the same problem with a different instrument: the prefix two successive decodes agree on, cut at a word boundary, under the standing rule that a speculation is invisible until the turn is confirmed, with a normalized continuation check before anything speculative may be heard. **Agreement needs no knowledge of the corrector's reach**, which is precisely the case the emission cursor cannot serve - and this project's reach is unknown, so the technique's precondition is unmet here. The row improved the technique rather than the project: it had conflated *what may be shown* (needs the guarantee) with *what may be acted on* (a heuristic plus a gate, because unshown work is discardable), and a section was added separating them. Return condition: a fleet seam whose revision reach is **bounded and nameable** |
| 2026-09-04 | equivalence-oracle-testing (discrete-selector tolerance) | deferred-operation-fusion | - | **unapplied** | - | **`unapplied by construction`, and known at triage rather than discovered at Phase 7.5** (round 21 focus item 1). No fleet project has a context for this subject - verified per project against all eleven `.ai/registry-map.json` files, because the fleet map was stale. Landed anyway: the amendment corrects a standing corpus claim and is corroborated by code read in the source tree. Return condition: a fleet project grows a lazy array pipeline, or any path where a tolerance-checked transform feeds an argmax or a threshold |
| 2026-09-04 | cache-residency-sets-the-balancing-unit | serving-process-topology | - | **unapplied** | - | **`unapplied by construction`, known at triage.** No fleet project has a context for this subject - verified per project. Landed anyway: it corrects an assumption the subject's own application records as a fact of its stack ("any API server to route requests to any engine core"), and the source holds both regimes in one repository over one model family, separated by a single engine flag. Return condition: a fleet project grows a model-serving path, or any service whose replicas accumulate per-caller state |
| 2026-09-04 | renameable-detector-keys | quality-gates | ai-registry | **code** | **better** | Seam: the bundle checker's own purity denylist — a list of literal product names guarding a rule about transplantability, whose comment already called itself "a FLOOR: extend it when a leak slips past". That describes an *accidental* gap; the key is renameable, which makes it something else. **Paired A/B, same document, same instrument, measurable = reported purity violations.** Arm A (a listed linter named) → 1 violation, exit non-zero. Arm B (the same product described unmistakably and not named — identical referent, identically non-transplantable) → **0 violations, `bundle integrity OK`**. A first arm B tripped on a *different* listed word, which is the honest measure of the bypass cost: the floor catches careless evasion and took one revision to walk around, by a writer not trying hard. **No invariant key exists** — reference is semantic and a static matcher cannot separate "describes a product unmistakably" from ordinary prose, which the project had already concluded (its real transplant test is an agent read the checker cannot run). So detection was not improved and cannot be; what shipped is the technique's prescription for exactly that case — the verdict now states its predicate in one appended line beside the two disclaimers already there. **Detection rate unchanged by construction; predicate coverage absent → present.** The line is additive, so every existing substring match on `bundle integrity OK` still holds — checked first, because eleven sessions grep it. Gate green, committed with a pathspec, not pushed. Return condition (seam class): **findings-per-thousand over time is not collected and the checker keeps no history**, so a drift in the denylist's fire rate would be invisible — that instrument is what would make the diagnostic runnable |
| 2026-09-04 | blocking-by-input-determinism (third variance: the variant instrument) | quality-gates | ai-registry, personas, pumper | **simulation** | **better** | Three real checks in three trees, each sorted twice — once by the two advisory-nesses, once with the third bucket — **with the falsifier stated first: if the two-way axis were sufficient, these trees should have filed their variant instruments as permanently advisory and stopped.** None did. (1) This registry's judged transplant test: unamended reads "a model" and files it permanent; amended sees a still input and a varying judge, so the work is *inside the gate* and nameable. (2) A consumer's scoring-card benchmark: seeded composition by hash of the cell id, no wall clock, no random, no network, the schema half split into a separate deterministic contract test whose own header says *no LLM, no fixtures, runs in milliseconds*, and the judged half gated by a script that asserts — the unamended reading would have written off a gate that demonstrably exists. (3) A third tree's long-lane certifier, whose header states in one sentence that long lanes judge statistically and that the unit of value is the trend across runs rather than the verdict of one, with a four-member vocabulary where *could not see* and *could not run* are never counted as a pass and a sibling flake checker aggregating under an explicit independence predicate — **here the unamended axis is not merely pessimistic but unsafe**, since the input is the tree and it would say this may block on one run. `structural-only`: nothing behavioural was run, and **no consumer code was changed because none was warranted** — two of three trees had already reached the amended prescription, which is the strongest evidence for it and the reason this row ships nothing. Return condition: a repeat harness over any judged lane in the fleet — run the same input past the same judge N times and compare the verdict distribution — which no tree currently has |
| 2026-09-04 | generated-supply-margin | media-playback | gravitone | **code** | **better** | Seam scored at triage from the fleet map and then **verified against the tree rather than trusted** (round 22's check): the project computes a realtime factor *and* a p95/p50 spread, and its certification gate compared `server_rtf_mean` against exactly `1.0` while `lat_p50_s`/`lat_p95_s` sat unread in the same level row - the source's own error, encoded as a production gate. **The structural fact is better evidence than the fix and nobody designed it:** the same repository already applies the technique's rule one file away, widening a single-request deadline promise by the measured spread and refusing to widen it from an unmeasured one, while the durable signed capacity certificate applied none of that rigour. Two layers, same numbers, opposite standards. **Paired A/B, same level row, project's own test suite as instrument:** at 2.5x mean with p95/p50 = 4.0 the old bar passes and the new one refuses at 0.625x, asserted together in one test so the disagreement is the assertion. Admitted-but-unsustainable band was `1.0 <= rtf_mean < spread`, i.e. all of `[1.0, 4.0)` at the engine's own `_MAX_SPREAD` clamp - the entire declared certification range. 127 passed, `test_certify` 44 -> 46. Pathspec commit on the active branch, not pushed. **Honest limit:** the ledger is empty and the one documented figure is 4.26x, so this prevents a future miscertification rather than correcting an observed underrun; the threshold is `1.0` and the service targets marginal Arm hardware, which is the whole band. Return condition: `loadtest` records per-request rtf percentiles, making the spread a measurement of synthesis time instead of a proxy from request latency at concurrency 1 |
| 2026-09-04 | committed-buffer-steering | media-playback | gravitone | **simulation** | **not-better** | **The technique's central claim has no seam in this fleet and is recorded unapplied**: no managed project runs a producer feeding a playback timeline that third parties steer. Return condition: a project grows one. Its *queue* half does have a seam, and the project's version is strictly better - priority expressed as a **deadline** (`t_enqueue + horizon` per class, computed once at admission) rather than as a class, which collapses preemption and a starvation bound into one number, plus a per-class floor on caller-supplied urgency because a request-body priority field is "a starvation weapon costing one JSON field". The technique as written would have let a busy instruction stream starve the background producer indefinitely while every scheduling decision looked correct. **Both rules were written back into the technique as an amendment**; nothing was changed in the project, which is why this ships nothing. `structural-only`: one scheduler read, not three cases under two policies, and labelled `simulation` on that basis rather than promoted. Seam class recorded so a later run does not re-run it: **a request scheduler is not a committed-buffer timeline** - it orders work that has not started, so it has no produced-but-unplayed content and the central trade cannot be exercised there |
| 2026-09-04 | single-source-topology (the link regime's platform precondition) | agent-instruction-files | kp, politicas, systedo-case, gravitone-gcloud, ascent, personas-web | **experiment** | **not-better** | **Paired, and the fleet is the arm that wins.** Arm A, the source's own checkout: `git ls-files -s` reports mode `120000` on the second vendor's filename, `core.symlinks` is `false`, and the working tree holds a **9-byte regular file containing the text `CLAUDE.md`** - 0 of 2,399 words delivered to any agent that opens it by name, silently, with the file present, readable, non-empty and containing a plausible path. Arm B, six fleet repositories shipping both filenames: every one uses a reader-resolved import directive instead of a link, and **all seven directives resolve**. Nothing shipped because nothing is broken here - the fleet had already voted against the corpus's stated default without a rule telling it to, which is why the amendment landed as a refutation of "drift is not representable... nothing can differ" rather than as advice. **kp is further ahead than the source**: `.ai/manifest.yaml` declares canonical + projections with the rule "each must NAME the canonical path", and `npm run guidance:check` (539 lines, CI on every push/PR) fails on a projection that never names it; its `resolveIncludes` leaves an unresolvable include as raw text precisely so `projection-not-pointing` catches the consequence. Return condition (seam class): a fleet repo adds a filesystem link to its index, or a contributor machine without symlink privilege joins a repo that has one |
| 2026-09-04 | inside-out-invariants (who supplies the input decides whether halting is the lesser class) | test-input-generation | tracklight | **experiment** | **not-better** | Seam: a network-facing Rust API service, which is the trust boundary the amendment is about. Instrument: count `unwrap`/`expect`/`panic!`/`unreachable!` occurring **before** any `#[cfg(test)]` boundary, per file, across the API crate - and the instrument was **corrected mid-run against a known positive** before its answer was believed, because the first pass counted whole `tests_*.rs` files that carry no inline `cfg(test)` marker and reported 177 sites in the top file. Corrected result: **4 non-test sites in the entire crate**, and all four are caller-guaranteed internal invariants (`and_hms_opt(0,0,0).unwrap()` on midnight, `expect("just pushed")`, `expect("slot filled")`) - exactly the class the amendment's two-regime rule permits. The project already satisfies the rule. Nothing shipped. Recorded because the *absence* is the finding: the amendment predicts a defect class this tree does not have, and a later run should not re-test it. Return condition: a fleet service grows a hand-rolled parser over untrusted bytes, or any `debug_assert` appears on a request path |
| 2026-09-04 | swarm-feature-sampling (mandatory features, and the unit a coverage claim is quantified over) | test-input-generation | - | **unapplied** | - | **`unapplied by construction`, known at triage rather than discovered at Phase 7.5.** No fleet project runs randomized or property-based testing at all - verified by grepping every `Cargo.toml` in both Rust projects for `proptest`, `quickcheck` and `arbitrary`, which returned nothing, so there is no generator anywhere for a feature draw to partition. Landed anyway: the amendment corrects a claim the corpus states in two places (`swarm-feature-sampling`'s portfolio rule owes the quantifier consequence and does not pay it; `blocking-by-input-determinism` states "a gate is the all-of-N position by definition", which forbids the merged-campaign gate as written), and it is corroborated by a two-simulator implementation read in the source tree with its feature classification published. Return condition: any fleet project adopts property-based or simulation testing, at which point the MANDATORY column is the first thing to write down |
| 2026-09-04 | ratchet-design (the second severity split, and the retention case) | quality-gates | personas-web | **experiment** | **not-better** | Seam: a named-allowlist ratchet (`scripts/i18n-encoding-baseline.json` + `check-i18n-encoding.mjs`), which the corpus already carries an application for. **Measured: 14 buckets, every one at zero, total 0 - and the file is kept.** That is the retention case the amendment now sanctions, found in the fleet rather than argued: an emptied allowlist retained because it *states* the standard and makes the next violation a one-line diff. It is the **third independent instance** (with `ipc-contract`'s deliberately-empty registration baseline and the source's empty `KNOWN_PREEXISTING_VIOLATIONS`), which is what promoted the retention case from a disagreement into a rule - `ratchet-design` instructs deletion, a landed application records the opposite with a reason, and the amendment names both endings instead of resolving it by majority. The amendment's **other** half is `unmeasurable` here and the row says so: with an all-zero baseline there is no allowlisted population, so the blocking/advisory population split cannot be exercised. Nothing shipped. Return condition: a fleet ratchet carries a non-empty allowlist, at which point the advisory-count half becomes testable |
| 2026-09-04 | instruction-freshness (the file agrees with itself) | agent-instruction-files | tracklight, pumper, kp, politicas, systedo-case, gravitone-gcloud, ascent, personas-web | **experiment** | **not-better** | The check the amendment adds is mechanical, so it was run rather than argued: extract every restated version floor (`node`/`rust`/`python`/`MSRV`/`toolchain` followed by a dotted version) from all three candidate guidance surfaces in eight projects and look for one fact carrying two values. **Result: one project states one floor once; no project states any floor twice; zero contradictions.** The fleet has no instance of the defect. The defect is real and is recorded from the source, where the instruction file states `MSRV 1.93.1` in its header and `MSRV 1.91` in its Code style section - one file, two answers, and no arbiter inside the trusted layer because the reader is told to follow the file over its own investigation. Nothing shipped. **Honest limit on this row:** the scan covers restated *version floors* only, which is one of the three classes the amendment names; path names and command names were not scanned and a contradiction there would not have been seen. Return condition: extend the scan to command names, which is where kp's own guidance checker already found real drift |
| 2026-09-04 | in-flight-is-a-position (a third durable position value between pending and complete) | job-coordination | personas | **task** | **better** | Seam: `src-tauri/src/engine/execution.rs:563-618` (`requeue_persisted_executions` selects on status alone and passes `continuation: None` unconditionally) against `src-tauri/db/src/repos/execution/restart_recovery.rs:145-155` (the classifier writes a third position value, `recovery_state=resume_pending`, and `RestartSweep.resume_pending` is documented as "Now queued: the existing re-admission path will pick these up"). **This is round 23s refinement, confirmed: the tree has the INSTRUMENT and not the RULE.** It paid a migration (`e17_restart_recovery`) to record the in-flight position - after measuring 74 of 2,188 executions carrying a blind restart marker nobody observed - and never taught the consumer to read it. The re-admission doc comment enumerates the four fields it reconstructs (status, persona_id, use_case_id, input_data) and omits `recovery_state` and `claude_session_id`, while the same comment records that it runs AFTER the classifier: **the enumeration predates the second population it now serves.** Consequence is concrete, because personas has no tool-level idempotency (`tool_call_id` read, defaulted to "", echoed, never stored or compared): every re-admitted run replays every side-effecting tool call. Plan, measurable, gate and falsifier committed to the project at `.ai/directions/2026-09-04-resume-pending-consumes-its-pointer.md` (`273dab501`), applied row at `782db2a37`; **not pushed**. Proof is `structural-only` **by necessity and the row says why**: `execution.rs` is 2,795 lines with zero `#[cfg(test)]`, the local Rust lane runs `--lib` so the binary crate is uncovered there, and `MockProtocol` is unfinished no-op stubs - there is no seam that drives this path without a real LLM, so the plan names a pure classifier-to-re-admission contract test as the honest first step rather than an end-to-end one. Return condition: the falsifier query (`SELECT COUNT(*) ... WHERE recovery_state=resume_pending AND claude_session_id IS NOT NULL`) returns a non-trivial count, or `recovery_state` is surfaced in `src/` at all - today it appears nowhere in `src/` or `bindings/` and `list_unresolved_recoveries` has zero call sites |
| 2026-09-04 | exclusive-authorship-of-a-measured-decision | multi-provider-gateway-plane | personas | **code** | **unmeasurable** | The project's manifest says it tunes routing from evidence, so the technique's third rule was testable against its own record — and the record could not answer. `was_failover` on the BYOM provider-audit entry was computed from the engine axis, on an enum with exactly one variant that the crate itself asserts exhaustively, so the expression was a constant `false` evaluated per request while the model ladder underneath it downgrades two tiers. Arm A over the project's own history: **0 of 6,163 audit rows flagged a substitution, and `model_used` was NULL on all 6,163** — two of three substantive fields constant, the third empty. Arm B (chain index) flags every advance. Shipped three lines plus a paired test over the real chain; `rustfmt` and `gitleaks` pre-commit green, and the change **compiles clean** under the project's own CI feature set. Proof `structural-only`, not `ab-paired`: the test binary cannot launch on this machine (`STATUS_ENTRYPOINT_NOT_FOUND`), which is pre-existing and binary-wide — an untouched neighbouring test fails identically while a sibling workspace crate passes. **The structural fact nobody designed** is stronger than the flag: `set_launch_model_info` and `set_model_used_actual` both write the same column, so the decided and served models are not merely uncompared but *unstorable simultaneously*. Return condition: when that column is split in two. **Corrected after the peer study re-read the spawn loop:** the first reading took 0-of-6,163 as hidden substitutions; `break 'failover driver` fires on spawn SUCCESS (`runner/mod.rs:1899-1903`), so the chain advances only on a process-launch failure that a different model cannot fix, and the project's own docs say the ladder is reachable only when changing the model cannot help. Nothing is being hidden, so the verdict is `unmeasurable`, not `better` — the fix removes a `gate-sees-target` violation with no traffic behind it. **The correction is the better finding:** a constant-`false` flag cannot distinguish 'the substitution mechanism never fires' from 'it fires and we do not record it', and those call for opposite responses (delete the ladder, or fix the record). The audit that would have shown the fallback was inert was itself inert. Instrument that would make it measurable: a second provider re-entering at `failover.rs:747`, or a post-spawn advance condition |
| 2026-09-04 | layered-composition (the last writer is not always yours) | prompt-assembly | personas | **experiment** | **not-better** | The amendment's own prescribed guard was run rather than argued: compare cached-read against cache-creation tokens over recorded runs. Result **15,830,327 read against 941,777 created — 16.8:1**, the healthy pole; the pathological case the amendment is written from sits at ~0.02:1. Nothing to ship, and the project passes. **The row wrote back into the landing**: the guard was unusable without a number and now has two poles two orders of magnitude apart, so it needs no careful threshold — only which side of 1 the ratio falls on. Return condition: when a fleet project puts a format-translating proxy between its assembler and a provider |
| 2026-09-04 | one-typed-carrier-for-echoed-state | multi-provider-gateway-plane | — | **unapplied by construction** | — | Verified per project rather than assumed: the technique's precondition is a plane that sits in the request path and keeps *no* record, so the client's next request is the record. No fleet project has that shape. The nearest peer persists `claude_session_id` on its own execution rows, which is the technique's first "when not to use it" clause verbatim — a plane that can hold the state should hold it. The one project serving an API-compatible surface carries no per-turn continuation state to round-trip. Return condition: when a project grows a format-translating hop between a client it does not control and a provider |
| 2026-09-04 | peer-study directions x4 (VibeVoice study; decode-parity, artifact-preflight, identity-at-install, degenerate-recovery) | voice-io | gravitone | **task** | **better** | All four ranked features accepted at the Phase 7.7 gate and executed in the same session on two branches split by FILE OWNERSHIP rather than by proposal (`direction/vv-audio`, `direction/vv-artifact`), because the four share `app.py`, `stt.py` and `engine.py`. Both merged `--no-ff` after the director re-ran each gate in the worktree; both `.ai/applied.jsonl` conflicts resolved by union, never by choosing a side. Combined post-merge gate green across 261 tests spanning both workers' code. **Both workers overrode their briefs and both were right.** The artifact worker refused "read the stored hash instead of stat-ing": three of four installers do not stamp, so an unconditionally-trusted stamp serves a re-cloned voice's previous bytes forever — the shipped design is stat + stored hash, believed only while the file matches the stat it was taken at, with `stamped/metadata/drifted/builtin` reported to the caller. The audio worker refused the peer's retry semantics: text repetition cannot separate a stuck decoder from a recording of a chant, so agreement across two independently sampled decodes now ENDS the ladder and returns the transcript, where copying the peer would have 500'd the verify route for anyone synthesizing a refrain. **Measured (worker's instrument, better than the director's): the two sides of the identity comparison were 0.568 apart for one recording uploaded at 16 kHz vs 48 kHz; now 0.0007** — compared to each other rather than to a reference, because a check whose answer comes from the function under test certifies its own blind spot. Return conditions: T-D1 (does the identity SCORE move) still needs `sherpa-onnx`, which is not installed; three of four installers still unstamped (`voices.create_voice`, `voices.derive_voice`, `ingest.commit`), each one line but needing a second transaction because the row commits before the file moves; a fifth resampler found in the operator CLI (`diarize._main`) and named in the register rather than taken |
| 2026-09-04 | no-wall-clock-in-the-advance-contract | job-coordination | pumper, personas | **experiment** | **not-better** | Instrument: enumerate every wall-clock site in each project's job/execution path and classify each as *executor-side* (races the effect, at the place the time is spent) or *contract-side* (a budget passed into the call that advances durable work). **pumper: 3 sites, all executor-side.** The drain deadline (`crates/server/src/worker.rs:262-312`, a `clean_window` plus a `grace`) bounds how long *shutdown* waits — the host owning termination, which the technique explicitly sanctions. The job timeout (`worker.rs:815-861`) is raced against the app future in a `tokio::select!` with `biased;`, so it fires concurrently with the work rather than being checked between transitions, and produces a fenced terminal verdict through the normal `fail()` door. `worker.rs:881` fires cancel tokens ahead of the drain deadline — cancellation, not a budget. **personas: 2 sites, both executor-side**, bounding a spawned CLI subprocess (`src-tauri/src/engine/runner/mod.rs:62`, `core/src/limits.rs:15`), with a test asserting one must *exceed* the child's own stall cutoff (`runner/mod.rs:3465-3466`) — two timeouts coordinated across a process boundary, which is fine precisely because both sit at the effect. **Zero contract-side sites in either tree.** The finding is structural and is why the row is `not-better` rather than `unapplied`: **the anti-pattern requires a runtime whose advance call is separable from the effect's execution**, drivable by an external scheduler. Neither project has one — pumper's worker owns its job's process for the job's whole life, personas' runner owns its subprocess — so the mistake is not merely absent, it is unexpressible. Nothing shipped. Return condition: a fleet project grows a durable runtime whose advance step can be invoked independently of the effect (an alarm-driven or serverless job driver), at which point the invariant is the first thing to write down |
| 2026-09-04 | race-catalog-with-two-histories | concurrency-guards | pumper | **task** | **better** | Seam: the job lifecycle is deliberately concurrent and **this tree already holds the reasoning without the artifact.** Four correct two-history statements exist as prose in source comments — cancel-vs-timeout "resolved exactly once, here, because it CONSUMES this run's cancel intent" (`crates/server/src/worker.rs:1066-1070`); shutdown-suspend vs operator `DELETE`, where "an operator's `DELETE /jobs/{id}` inside the drain window outranks the drain" (`:1076-1079`); `fanout_owns_outcome`, the staleness fence "that the inline version got for free" (`:1174-1190`); and the boot sweep and lease reaper unified into one verdict function citing this registry by name (`crates/core/src/storage.rs:1223-1231`). Against that: **one** race-shaped test module (`worker.rs:2718 mod fanout_fence_tests`) and one either-order assertion (`scheduler.rs:1137`). Nothing enumerates the set, so nothing detects a row going missing — **and one already has.** `finalize_fanout` fires irreversible effects *after* the completion write, under its own comment "A webhook is irreversible once sent" (`worker.rs:1262-1264`), while both recovery paths select `WHERE status = 'running'` (`storage.rs:1234`, `:1332`); a crash mid-fan-out leaves a row that is terminal, unleased, unreaped and correct beside half-delivered effects, producing no stuck row, no alert and no error. Plan committed at `.perfect/Perfect/directions/job-race-catalog.md` (`4071643`), applied row in the same commit; **not pushed**. Proof `structural-only`: the missing row is proven by reading two query sites, but constructing the interleaving needs the commit-gating harness the plan proposes. Measurable: durable races with both outcomes stated *and* tested — today 1 of ~8, with 4 stated-but-untested and ≥1 neither. Return condition: the table exists and the post-terminal fan-out window has a mover |
| 2026-09-04 | step-position-and-resumability (total state, never a journal; no hot path infers from an absent value) | job-coordination | pumper, personas | **experiment** | **not-better** | Instrument: look for a recovery or execution hot path that folds a history or reads meaning out of an absent row. **pumper satisfies the rule outright.** The job row *is* the total state — status, attempts, `heartbeat_at` — replaced in place, never a journal; both sweeps select on positive predicates (`WHERE status = 'running'`), and `reap_stale` handles missing stamps with an explicit fallback chain (`COALESCE(heartbeat_at, started_at, created_at) < ?`) rather than treating absence as staleness. The `job_stages` migration even writes the rule down for its own readers: "`NULL` means *this job never reached that stage*, which is deliberately distinct from 0, and readers must render NULL as unknown, never as zero." **personas inverts it deliberately and safely, which is the interesting half.** Its boot recovery treats the *absence* of a clean-shutdown marker as the crash signal (`src-tauri/core/src/shutdown_marker.rs:54,72`, written last on exit at `src-tauri/src/lib.rs:2192`) — an inference from absence, and a legitimate one, because the absence is manufactured by a positive write on the good path and the fail direction is conservative ("fails toward sweeping"). That is the boundary the amendment's prohibition needs and does not yet state: **inferring from absence is safe exactly when some party positively writes the presence on the healthy path and the ambiguous reading is the cautious one.** Recorded here rather than re-amending on one sighting. Nothing shipped in either tree. Return condition: a second fleet project uses absence-as-signal, at which point the boundary has two sightings and belongs in the technique |
| 2026-09-04 | shared-substrate-check-partition | quality-gates | politicas | **experiment** | **better** | Seam: a design-review agent brief that excludes the mechanical detector's whole domain in one sentence, names no rule from it, and inherits no verdict - while capping its own output at 8 ordered fixes. Measured over the real rule set: the detector defines **66 distinct rule ids**, and **39 of them** name a condition the reviewer's own TYPE/MATERIAL/density row also classifies (predicate: rule id contains a token from that row's vocabulary). Arm A (as shipped): 39 of 66 ambiguous - no way to tell a comp-fidelity call from a value-in-band check, and each ambiguous finding spends one of 8 capped slots. Arm B (patched): detector verdict inherited as a premise, shared surface named, split stated as comp-character vs value-in-band, plus a 5-entry not-reasons list -> ambiguous set 0 by construction. Gate reached: the project's blocking commit-msg doc-sync gate, 0 drifted of 83 checked; no gate failed. Behavioural rate NOT measured - the tree holds no recorded reviewer returns and running the agent needs screenshots, an approved comp and a quality card this run did not have. Return: when recorded returns exist, count the fraction of the 8 slots landing on detector-owned rules, both arms. |
| 2026-09-04 | instruction-format-follows-program-lifetime | register-bytecode-execution | - | **unapplied** | **n/a** | No seam in any managed project: all twelve fleet trees searched per-project with `git grep` for opcode/bytecode/interpreter and every hit is lockfile or prose noise - no project embeds a language runtime, which independently confirms the same statement made in this subject's note on 2026-09-03. Return condition: when a managed project embeds or vendors an interpreter, a bytecode VM, or any compile-then-execute stage whose programs are generated per request. |
| 2026-09-04 | generated-shot-sourcing (anchor-series derivation topology) | video-assembly | gravity | **experiment** | **better** | Seam: `lib/themes.ts:181` (`styleRefs`), consumed at `app/_phases/frames/useFrames.ts:142` and by the alternatives path. **The tree was already a star and nobody designed it that way** — references are memoized per THEME, not per frame, and no generation conditions on a sibling plate's output, so no path exists along which one frame's error reaches another. The defect was the property the star silently depends on: `styleRefs` sorted approved proofs NEWEST-first and took `SEND_REFS=4` with nothing pinned. Instrument: `pipeline/style-ref-stability.mts`, a paired replay of one theme's approval history through both selectors, same input, both arms printed; asserted against a known positive FIRST (with `PINNED_REFS=0` it fails and names the exact eviction, `[proof-14, proof-13, proof-12, proof-11]`). Measurable — the approval at which the founding proof leaves the reference set: **arm A = 5 of 14, arm B = 14 of 14**. Second measurable, distinct masters over the sequence: **A=14, B=14, UNCHANGED** — the honest half. Shipped `PINNED_REFS=2` (both bank bounds asserted: pinned < window, ≥1 rolling slot) and wired the regression into `npm run verify` as `check:style-refs`. **Gates: PASSED tsc --noEmit, lint:ratchet (374 files, 0 errors, at baseline), check:frames-prompt, check:style-refs; NOT RUN npm test (playwright, needs a running server) and npm run build.** Commit `eb2f272` on main, not pushed. **The apply step corrected the landing**: the amendment implied the pinned-bank rule buys master STABILITY; the measurement says it buys origin RETENTION only, and the corpus text now separates the two. Return condition: master stability is still unsolved — freezing the reference set per SHEET, so every frame of one sheet demonstrably shares a master and the sheet records which, is the change that would make the star's guarantee true, and it needs somewhere to store the frozen set |
| 2026-09-04 | write-freshness-gate (whole-artifact write may refresh the mark) | mcp-tools | — (source tree) | **unapplied** | **n/a** | No seam in any managed project: six fleet trees searched per-project with `git grep` for an agent-facing write tool (`name: "write|edit|patch"`) and the count is **0 in all six** — the fleet consumes MCP (clients and thin HTTP proxies; personas' two servers at `tools/*-mcp/server.py` publish 8 tools each and write no files) but no fleet project is an MCP *publisher* with a file-write surface, which is what this technique governs. Applied instead against the **source's own tree** per v2, as `applications/python--write-freshness-gate.md`: it grants `readback_required: false` on exactly the operation that refuses literal text and moves bytes the server already holds. Structural fact that beats the fix: **one helper produces the evidence for every write on the plane** (`shared/deepy/filesystem.py:509-520` returns sha256, counts and heading facts), so a literal `write_text` — including `mode: append`, precisely the case the amendment forbids refreshing — comes back carrying identical proof. The discriminator is therefore NOT visible in the evidence, and a server granting the license by inspecting the result would grant it to appends. Return condition: when a managed project publishes an agent-facing write tool over shared artifacts |
| 2026-09-04 | catalog-projection-modes (the argument-schema rung) | mcp-tools | — (source tree) | **unapplied** | **n/a** | No seam in any managed project, and the technique's own discriminator says why: *is the ceiling imposed by someone you do not control?* No fleet project publishes a compressed catalog — personas' two MCP servers expose 8 tools each, one tool per operation, well under any host budget, which is the case the technique explicitly tells you not to compress. Applied against the **source's own tree** as `applications/python--catalog-projection-modes.md`, where the rung appears at **four independently written tool families** (`:1309/:1313`, `:1364/:1369`, `:1490/:1497`, `:1461`) — within-source convergence across call sites. Structural fact recorded as a gap in the tree rather than the standard: every io action declares an `access` tier (`filesystem.py:753+`, `"write"` on delete/zip/unzip) so the per-operation blast-radius classification **exists and is correct**, and never crosses the wire as a behaviour annotation — the honest annotation set the technique asks for is already computable there. Return condition: when a fleet project's tool catalog runs against a host ceiling it does not control |
| 2026-09-04 | rejected-revision-leaves-its-evidence | agent-memory | personas | **task** | **better** | Seam: the prompt-evolution loop's cycle record stores `variants_tested` as a COUNT, two fitness numbers, a `promoted` flag and a prose `summary` — three of the technique's four required fields, plus the one field it argues against, and **no tested candidate anywhere**. So `variants_tested = 5` is five of nothing in particular: it cannot be joined to a candidate, a score or an outcome, and the breeder cannot avoid re-proposing what the evaluator already rejected. **The structural fact is that the tree already holds the opposite design on its newer surface** — the Director-commissioned experiment row carries the candidate prompt, its source and a provenance snapshot, under a comment saying every row is "auditable back to the observation that motivated it" — but that surface's `running`/`concluded` states are marked *reserved for a deferred canary-fitness loop*. **The surface that keeps the diff does not run; the surface that runs and rejects keeps no diff.** Nobody designed that; it fell out of two features landing at different times, which is what makes this coverage of an existing context rather than a new capability. Proof `ab-paired`: both schema arms applied to a scratch SQLite database with `foreign_keys ON`, arm A read from `git show HEAD:` rather than a hand-edited copy, the same three candidates offered to each for one completed cycle with `promoted = 0` and `variants_tested = 3` — **A recovers 0 of 3, B recovers 3 of 3**, with the `outcome` CHECK rejecting an unknown value, the FK rejecting an orphan, `UNIQUE(cycle_id,variant_index)` rejecting a duplicate and `ON DELETE CASCADE` reaping the children, each exercised rather than assumed. **Arm A applying is the load-bearing half**: an earlier run returned 0 on *both* arms because the schema's `{{TRIGGER_TYPE_CHECK}}` placeholder — a CHECK member list — had been substituted as though it were a whole predicate, so neither DDL parsed; a tie there meant nothing had run, caught only by printing the mid-state. Shipped step 1 of 3 on branch `intake/evolution-variant-provenance` (`337f07ff4`, 3 files / 147 insertions, verified in `HEAD`), **not merged and not pushed** — a table nothing writes to is half a feature. **Gate reached: the DDL, paired. Gate NOT reached: `cargo check`** — the change sits inside a `const &str` in a worktree and no Rust was compiled. Deliberately **no `reason` column**, which is the technique's specific prescription against the existing `summary`. Return condition: steps 2 and 3 of the project's own plan — a recording function inside the cycle-completion transaction so a cycle cannot reach `completed` with its variants missing, then the breeding loop handing its scored candidates to it, which is the only step that touches behaviour |
| 2026-09-04 | diagnosis-withheld-from-the-executor | agent-memory | registry skills lane (+ personas, conformant) | **experiment** | **better** | This method is an instance of the loop the technique describes, configured the way the ablation measures as worse: Phase 1 instructs the **executing** run to read the diagnostic layer's latest declared focus before extracting anything, and the run's own outputs are the evidence Phase 11 revises the method with. Instrument: every numbered imperative inside a declared-focus block, scored present iff some four-consecutive-word phrase of it appears verbatim in the gated method file. **The first version of this instrument failed its own assertion** — bag-of-words overlap scored a known negative at **1.00**, because the method file is long enough to contain almost any single content word somewhere, and it would otherwise have reported a comfortable number. Phrase-level, asserted against 3 positives and 2 negatives: **56 blocks, 83 distinct imperatives, 9 carried by the gated file, 73 not, 1 undecidable.** Strict matching understates carriage, so 73 is an upper bound and was hand-sampled twelve deep in both directions — eight likely-absent all genuinely absent; **two of four likely-reworded had in fact landed** (a focus item asking that a category listing be read before believing a map result is in the file as a placement veto counting the category before scoring; one asking that forces be mapped rather than the concern is there as a rule that a proposal cite forces and never features), a third partially. So carriage is **9 to roughly 25 of 83**. **The worst instance is the technique's promotion-signal clause verbatim**: one demand to close a named gap "in writing" appears **27 times**, its own text counting *sixth*, *seventh*, *eighth*, *ninth*, *tenth deferral*, and the gated file mentions it **zero** times — ten rounds of the loop telling its executor the same thing through the only channel with no gate on it. Read as an access problem that is discipline failure; read as a promotion backlog it is a ranked, actionable list whose priority order repetition already wrote. `before-after` rather than paired because a control is impossible — the same 83 items cannot be delivered to a run that did not read them — and **the missing half was not invented**: no estimate is offered for mining quality under the withheld configuration. A live instance arose inside this run and changed its result: the mid-state rule that caught the both-arms-zero tie above lives in the diagnostic lane and **not** in the gated file. **The same technique was checked against personas and that tree is conformant** — its evolution engine writes nothing into the memory store the executing persona reads, and its only read of execution history is a `COUNT(*)` for the trigger; verified rather than assumed. Nothing promoted into the method file by this run: that lane's bar is three-runs-confirmed, and a mid-flight method edit is the one change a fleet of parallel readers cannot absorb quietly. Return condition: the third confirmation, at which point the recurring imperatives graduate under a version bump |
| 2026-09-04 | procedure-promotion (third capture source: another agent's experience) | agent-memory | personas | **experiment** | **not-better** | The amendment's third source is **unexpressible in the shipped loop, not merely absent** — which is why this is `not-better` rather than `unapplied`. The genome a cycle mutates is derived by `PersonaGenome::from_persona` from the **same** persona (`src-tauri/src/engine/evolution.rs:244`), so every candidate is self-distilled by construction; and a variant's `source_persona_id` is stamped with a freshly minted `evo-<uuid>` (`:323`), a generated value occupying **exactly the field the amendment asks to carry an authoring lineage**. The ingredient exists one layer down — `team_memories` shares recorded experience across the personas in a team, created from approved manual reviews — but that is shared *memory*, not a promoted *procedure*, which is the distinction the host technique exists to draw: a recalled procedure is advisory and a promoted one is a capability. So the tree can already move one agent's episodes to another and cannot yet promote across that boundary. Nothing shipped; the fix is one field but it is meaningless before the loop can seed a genome from another persona's episodes. Return condition: cross-persona genome seeding lands, at which point `source_persona_id` is the first thing to fix and "name the authoring agent, not only the episodes" is the assertion to write |
| 2026-09-04 | exit-contract-is-a-taxonomy-projection | error-handling | ai-registry (this repo's `scripts/` lane) | **code** | **better** | Seam: the scripts lane is exactly the consumer class the technique is about — hooks, CI steps and skill procedures that receive an integer and nothing else. Measurable: **distinct incompatible meanings sharing one exit code.** Arm A carries three on code `3`: `run-board.mjs` (4 sites) means CONTENDED, transient, retry is meaningful; `research-ingest.mjs:400` means source-too-thin, permanent, retry can never help; `upstream-check.mjs:668` means rows-are-due, which is not a failure at all. A caller doing `cmd \|\| retry` loops forever on a thin source and treats due upstream rows as an error. **Codes 0/1/2 held a consistent convention across ~30 scripts with no declaration anywhere and broke at `3`** — the first code added by a later script — which is the technique's prediction about where an undeclared vocabulary fails, confirmed rather than assumed. Landed `scripts/lib/exit-codes.mjs` (the single declaration; the three live meanings on `3` recorded as `KNOWN_COLLISIONS` rather than renumbered, because changing a live code breaks callers mid-flight — the mechanism is that the NEXT meaning cannot be added without a decision) and `scripts/check-exit-contract.mjs` (the gate, which **asserts itself against a planted positive before reporting**, and whose first run correctly flagged its own self-assertion fixture as a real exit site — fixed by building the literal via concatenation). Proof `ab-paired`, same tree, same injected drift (a new script exiting `4` with a novel meaning): **arm A (`check-bundles`, `check-skills`) returned `0`, `0` — blind; arm B (`check-exit-contract`) returned `1` with file and line.** `build-index --check` read `1` in **both** arms from a stale index caused by this run's own new knowledge files, so it is a constant and not a signal; the confounder was confirmed by re-running it with the probe removed rather than assumed away. Baseline census: 134 exit sites across 42 scripts. Committed `45aeba4a`, verified in `HEAD`, **not pushed**. Return condition: when a script needs a fifth meaning — the gate will refuse it, and whether the collision ledger or the vocabulary grows is the decision the mechanism exists to force |
| 2026-09-04 | shape-with-a-not-applicable-member | invariant-placement | personas | **experiment** | **better** | Seam: `src/api/liveRoadmap.ts:95`, `LiveRoadmapFailureKind = 'offline' \| 'timeout' \| 'http' \| 'schema' \| 'unknown'` — **the same surface shape as the source's defect**, a failure taxonomy ending in a member meaning none-of-these. The producer audit clears it: `classify()` at `:123-133` reaches `'unknown'` as the final fallback for any unmatched message, so the member has a producer, is reachable, and is meaningful to a consumer deciding what to render. This is the honest exception the technique carves out, and finding it was the point. **The A/B is the matched pair, not a before/after**: cargo-make's `EngineType::Unsupported` (`scriptengine/mod.rs:43`) and this union are *identical at the type declaration* and *opposite in verdict*, and nothing separates them except the producer audit — no producer there, a producer here. Arm A (read the type) cannot distinguish them; arm B (one grep for producers) distinguishes them in one pass. That is the technique's discriminating power measured rather than asserted, and it is the strongest form available for a rule whose whole claim is that the surface tells you nothing. **No change is owed to personas** — a `better` verdict on a technique that correctly recommends doing nothing. Return condition: when a fleet union gains a member the audit finds unreachable, at which point the silent-success arm is the thing to look for |
| 2026-09-04 | state-carrier-decides-the-lane | module-design | personas | **experiment** | **not-better** | Seam: `src/features/agents/sub_executions/libs/comparisonDiffWorkerClient.ts`, a real `Worker` boundary where everything crossing is structure-cloned — the technique's canonical trigger shape. It predicted accumulator loss and **found none**, for a reason worth more than a confirmation would have been. The module does hold accumulators — `lineCacheEvictions`, `jsonCacheEvictions` and two LRU caches at `:27-47` — but they live on the calling side and are written when the reply resolves, while `postMessage` carries `{ id, kind, left, right, chunkSize }`: **a request assembled at the call site, not a snapshot of state the caller already had.** That is the technique's own primary repair (*move the accumulator to a sink that outlives the branch*) reached by default rather than by correction, which is what a worker boundary designed from the start usually looks like. **The row bought the technique a precondition it did not have** and which its cargo-make origin concealed, because there the carrier pre-existed the lane: it triggers on an existing shared carrier copied in order to cross, **not** on a payload built for the crossing, and a seam failing that precondition needs no field classification and must not be reported as a clean result. Landed as the section *"Where it does not apply: a payload built for the crossing"*. Return condition: a fleet seam where an existing shared object is cloned to cross a worker or process boundary — the precondition is now the filter that finds it |
| 2026-09-05 | capability-deferred-release | engine-binding-surface | pumper | **code** | **better** | **The tree refuted the reading that picked it, and the refutation is what made the row worth landing.** Seam: `crates/engine-browser/src/lib.rs`, the `Handle::try_current()` Err arm of `RenderScope::drop`, which warned and abandoned the tab. Expected an unguarded destructor; found a project that had already reasoned about this exact family — it aborts the capability-free half unconditionally before consulting for a runtime, and states its failure modes in comments rather than hiding them. What it lacked was the third answer between *perform* and *abandon*: record the debt, settle it where the capability provably exists. `BrowserEngine::acquire` was that checkpoint already — it is `async`, so a runtime is entered by construction, and every subsequent render passes through it. Landed `PendingCloses` (queue beside the engine, `Option` as the closed state so a late arrival discards deliberately, `AtomicUsize` hint so the hot path is a relaxed load when nothing is owed) plus the drain. **Measured 0 of 1 against 1 of 1** on closes observed after a runtime-less drop, on the crate's own recording harness, with the old behaviour as arm A INSIDE the same test — so the instrument is shown to distinguish rather than merely to pass (`selfcatch`). 32 tests green against a baseline of 31; clippy clean. Branch `intake/rustyv8-capability-deferred-release`, not pushed: ~110 lines is past a one-sitting read. Residue, named rather than shrugged: a runtime that is *shutting down* still takes the successful arm, so spawn succeeds and the task may never be polled. Return: when the engine grows a teardown hook the drain can hang on. |
| 2026-09-05 | enforcement-placement-and-reconciliation (client-side seat; any pre-provider seat prevents) | usage-limit-governance | tracklight | code | better | All three SDK seats already met the four honesty rules under one shared contract fixture; the missing worst-case sentence was written into the project README. Paired prediction run refuted the corpus's own bound: a fail-open seat's staleness bounds how long a refusal stands, not spend; spend past the cap is bounded by the next response (reachable) or the provider ceiling alone (unreachable). Return: a client seat built fail-closed, or the rust client gaining the auto stale refresh its README promises |
| 2026-09-05 | lifetime-cap-rotation | stream-proxy-hop | ascent | experiment | not-better | Rotation excluded twice by its own rules: the client cannot resume (single POST, no resume cursor, no replay window) and the turn is bounded one layer below the route by a single cross-leg deadline under the cap. Paired through the real loop: per-call ceilings only reached the cap by moving one env variable; the cross-leg deadline held at every latency. Condition written into the technique: bounded by construction is a sum, not a term. Return: token streaming or a client resume path ships, or the loop budget moves toward the cap |
| 2026-09-05 | file-scale leak is session length, not line count (golden path) | agent-instruction-files | kp | experiment | not-better | 373 recorded transcripts and 1,296 commits replayed through the project's own hook predicates, each event tagged with the instruction-file version live at its timestamp. Line count: no decrease (size null replicated). Position: nearly every dropped rule fell at the FIRST owed event of a single-event builder session, so the session axis has no spread; the Stop hook fired hundreds of times on the Director surface and never on the subagent surface that made most edits. Condition written into the golden path: single-event builder topology has no axis to leak along; the lever is a gate on the builder surface. Return: a subagent-stop hook or a per-commit ordinal in the doc-sync record |
| 2026-09-05 | reconnect-storm-hygiene (reset on stable, not on open; clean close is reconnectable for a hand-rolled reader) | stream-proxy-hop | personas | code | better | The tree already reconnected on a clean close and reset on a stable connection; the technique found the reset lived in the clean-close arm only, so a stable connection ending in a read error kept the cap rung (first retry after a ten-hour session: 30s under A, 1s under B; ab-paired, n=4 replayed close sequences from the tree's own bug-hunt note). Code on a project branch, not the default branch: every desktop lib-test binary on this machine fails to load (predates the change); tests green in a verbatim harness. Return: the project gate green on a machine whose test binary loads, then fast-forward. Standing deviations for a task row: one error string for four causes, no attempt limit, status stays connected through a post-clean-close backoff |
| 2026-09-05 | pending-beliefs-live-apart (rule 3: the proposer consults rejections by whole-claim identity) | agent-memory | personas | **code** | **better** | `persist_proposals` split out of `run_consolidation`; before each insert it checks `companion_consolidation_item` for a `rejected` row with the same `(scope, fact_key, proposed_value)` - the value is part of the key so a rejected "home_city = Brno" does not silence "home_city = Prague". Paired test `a_rejected_proposal_is_not_asked_again` with arm A inside it (pass one inserts 1/0; reject; pass two 0/1 (inserted 0, skipped_rejected 1); a new value 1/0). Arm A re-run with the check disabled: red at the rule assertion - inserted 1 / skipped 0, 0 passed / 1 failed. Gate: consolidation::tests 14 passed / 0 failed with the check on; the lib-test exe loads once the documented runner's comctl32-v6 manifest is embedded post-link; rustfmt clean. Branch `intake/utopia-pending-rejections`, commit `50bdbb5fc`, merged `--no-ff` into master as `41752ff97`, not pushed. Return: the check is exact-string on the value; when the operator reports a reworded re-ask, add the normalised-hash rung |
| 2026-09-05 | self-declaring-spec-debt (amendment: the change that closes a gap moves its label) | practices | ai-registry (the registry itself) | **experiment** | **better** | 17 `docs/subject-proposal-*.md` status lines checked against `knowledge/software-engineering/index.json`: **5 lagged** (companion-identity, companion-runtime, conversation-orchestration "dispatched" since 2026-08-23; module-design "proposed" since 2026-08-22; repository-landing-document "proposed" since 2026-09-01). Moved in this run with the original line kept in place. Arm A = 5/17 stale; arm B = 0/17 after the change. Return: re-run the same check at the next librarian sweep - the practice predicts it will be non-zero again unless the forge's Phase 4 moves the line itself |
| 2026-09-05 | migrations (amendments: the merge is a third road; a nullable column is a query audit) | migrations | personas | **experiment** | **unmeasurable** | Merge road: personas migrations are Rust modules registered in one list, so a duplicate is a compile error and the boundary does not apply. Nullability: 22 `<>`/`!=` comparisons against bound parameters found in the Rust tree; the ones read are on primary keys and NOT NULL columns (`tier != ?`, `id <> ?`), none verifiably against a nullable column without the schema in hand. Instrument that would measure: a schema-aware audit that joins each comparison's column to its declared nullability. Return: when personas makes an existing column nullable |
| 2026-09-05 | record-axis-predicate (and its "three absences" section, which this seam contributed) | audit-logging | politicas | **simulation** | **better** | `features/shared/provenance/asOfLens.ts` is an independent implementation: one pure module both receipt surfaces read, six closed states, tests that a day resolves to its END and that three "we don't have it" states must not share a sentence. Three real cases (a day before the migration epoch; a derived family that cannot be replayed; a malformed day) under the technique as first written (present-or-absent at T) versus with the three-absences section: better for the section, added to the technique; `next--record-axis-predicate` written. Nothing in politicas changed. Return: when the store's `asOfNode`/`asOfEdge` gain a both-directions test, read it against the technique's test clause |
| 2026-09-05 | live-attachment-delta-resharing | prompt-assembly | personas | **experiment** | **better** | Read-only replay of `companion_turn` block hashes: 65 consecutive pairs, arm A (re-render every block) 11,708,577 chars vs arm B (send only changed blocks) 1,662,815 (14.2%); constitution/plugins/mode_addenda unchanged on 92-97% of turns, observability on 7.7%, and observability sits third of eight in the composed order. Next change filed in the project ledger: reorder the two volatile blocks below the stable six; measurable is cache_creation vs cache_read per turn over >= 30 live turns. |
| 2026-09-05 | context-ownership-regimes | prompt-assembly | personas | simulation | unmeasurable | Three real cases (the session-id reset lever, the cache series, the wipe path): the companion is agent-held for the conversation and projection-regime for memory; policy A (client lanes) has no list to act on. Instrument: a stateless endpoint path in the companion. |
| 2026-09-05 | history-compaction (amendments: lossless-lane trigger; provider-anchored count) | prompt-assembly | kp | simulation | **not-better** | Voice-intake fast thread: 48-turn cap with a drop-count marker, no tool results, no attachments, turn-denominated window with a 4,000-char utterance bound. The lossless lane has nothing to fire on; the anchoring half has no estimator to displace. The amendment now carries the precondition. Return: a token-denominated trigger or attachments quoted into the transcript. |
| 2026-09-06 | control-failure-is-not-a-datum-state | measurement-honesty | personas | **code** | **better** | Seam: `scripts/test/athena-model-bench.mjs` `report()` — infra failures (timeout / rate limit / CLI error) filtered out run-wide at `:495`, disclosed ONLY as a global count in the report header. Measurable: **cells whose report attributes their own excluded runs.** The defect is the technique's exact prediction: exclusions are not uniform across cells, because a slow or overloaded cell times out on its HARD scenarios and keeps its easy ones — so its accuracy is computed over a self-selected subset and compared against `o-base` scored over all of its own. Fixture: 14 of 20 `s-low` attempts lost to infra. **Arm A printed `### s-low — CERTIFIED (all classes + latency)` on six surviving runs, winning every class and 67% on latency; arm B prints `INCONCLUSIVE — control failed, re-run (14 of 20 attempts excluded)` and names the non-random survival.** Predicate `0 -> 1` cells attributed. Proof `ab-paired`, same fixture through both arms, mid-state printed so a tie could not be mistaken for a result. **The predicate was asserted before either arm ran** — two known negatives (a global header count and a bare per-cell row must both score zero) and three known positives — and it **self-caught once**: the first boundary regex missed a prose-form attribution, which would have under-counted arm B. The verdict changes rather than gaining a footnote, because a caveat printed under a green headline is read as the headline; `maxExclusionRatePct` joins the other gates so the threshold is declared rather than implied. Committed `62482a90` on `master` with a pathspec, lefthook pre-commit green (eslint + gitleaks), **not pushed**. Return condition: when a real bench run trips the 20% gate — the open question the fixture cannot answer is whether the threshold is right, and the first cell it blocks is the evidence |
| 2026-09-06 | baseline-species-degradation | diff-comparison | pumper | **simulation** | **better** | Seam: `crates/apps/connector-api-watch/src/lib.rs:292-303`, a watch that diffs a fetched document against `prior_markdown`. Mode is `simulation` rather than `code` for a stated reason: the change is a few lines, but verifying it needs a Rust workspace build whose cost exceeds this run — that is the mode table's *cost of the experiment exceeds the run*, not an absence of seam. Three real cases from real trees. **(1) This seam already gets the hard half right**: `ChangeKind::Changed` gates out first-seen records and `let Some(prev) = prior_markdown else { break }` refuses to emit an event with no baseline, so it never fabricates a diff against empty — the technique's rung-three refusal, reached by design. **(2) It fails the naming half**: three distinct silent exits (`kind != Changed`, no-prior, and a hash flip on whitespace/reorder noise) all `break 'connector` with no record, so an operator asking *why did the watch not fire?* cannot tell a first-seen record from a noise-only change. The technique asks the refusal to name every failed precondition; here it names none. **(3) The source tree** carries the same rung with the label attached, which is what makes case 2 legible as a gap rather than a style choice. Predicted outcome: adding one trace per exit takes distinguishable refusal reasons from 0 to 3 at no runtime cost. **Falsifier:** if the three exits turn out to be reachable only in mutually exclusive configurations, an operator could infer which one fired from context and the traces would be noise — a reading of the surrounding `'connector` block would settle it. Return condition: **the code row** — when a build is affordable in-session, this becomes `code` with distinguishable-exit count as the measurable |
| 2026-09-06 | constraint-and-tool-channel-exclusion | structured-output | kp | **code** | **better** | Seam: `pipeline/jobfit/gemini.py:443-445`, the single seam every model call routes through. It already excluded a grounding tool and a response schema by construction (`if use_grounding: tools` / `elif response_mime_type`) — the technique's rule, reached independently and without contact — and its JSON extractor's docstring draws the downstream consequence ("with grounding enabled there is no response_mime_type, so the model returns JSON embedded in prose"), with a whole tolerant-parsing ladder built for that case. **The gap is the half the technique insists on: the shed was invisible to the shed party.** `analyze_document` (`:772-778`) passes BOTH `response_mime_type="application/json"` and `use_grounding`, so whenever grounding is on the constraint is dropped and the caller cannot observe it — the silent form of the "drop the constraint, keep the tools" repair the technique names as insufficient. Measurable: **shed events a caller can detect.** Arm A **0 of 1**; arm B **1 of 1** with three no-shed controls correctly reporting enforced. Same four tests through both arms: A fails 4/4 because the observable does not exist, B passes 4/4, wider slice 135 passed / 1 skipped. **The mid-state was printed** (zero occurrences of `schema_enforced` in the arm-A source) so a tie could not be read as a result. `GroundedAnswer` gained `schema_enforced`; committed `0301c4f9` on `main` with a pathspec, commit-msg hook green, **not pushed**. Return condition: when a provider is added that emits tool calls through a structured field the grammar never touches — there refusal-at-assembly would block a working combination, and the flag becomes the wrong shape |
| 2026-09-06 | unadopted-extension-point | dead-code | kp | **experiment** | **not-better** | Seam: `pipeline/jobfit/llm/base.py`, `TextProvider` — a provider abstraction with three concrete adapters, the same surface class as the source's defect. The technique's own census (AST, call sites counted against the receiver, declaring module excluded) run over `jobfit/`: **14 methods, 0 with zero call sites**, every one overridden 1-68 times. There was no unadopted surface and the decision table would have had no rows. This is **absent, not unexpressible** — the instrument ran, produced a number, and the number is zero; nothing is owed to make it measurable. **The instrument self-caught once and the catch is the row's substance**: the first pass reported three dead methods, all of them private helpers called from inside `base.py`, which the technique excludes as the *declaring module* — correct for public surface, wrong for intra-module helpers. Re-running against base.py's own call graph found all three live at `:366, :372, :382, :471`. Without that check the row would have reported a 21% dead surface on a healthy abstraction. **The technique gained the boundary this row bought**: section *"Where the population does not exist"* — the discriminator is a ratio, not a size, and the strongest tell of the defective case is members carrying overrides but no callers (providers specializing behaviour nothing invokes), which is exactly what separates the source's eleven from kp's zero. Return condition: a fleet abstraction where the census returns members with overrides and no call sites |
| 2026-09-06 | capability-feature-gating (amendment: flag and gated code with different owners) | build-economics | personas | **experiment** | **better** | Seam: `scripts/check-command-registration.mjs` answers the orphan question over the **union** of every cargo feature and says so itself — duplicate names are cfg-gated variants "and are not a finding". Correct for that question, and structurally unable to express the per-configuration one. Measurable: **frontend-reachable commands absent from the built variant.** Same definitions, same registrations, one change — evaluate the guarding `#[cfg(...)]` against a feature set instead of collapsing it. **Arm A reports 0 for every variant** (1627 registrations, 1627 registered, 0 orphaned); **arm B reports 4 / 4 / 72 / 97** across the four `tauri.*.conf.json` build configs. The amendment's generalisation is what made this applicable: the ownership split need not cross an organisation, only a **decision** — here cargo features decide what registers and the frontend's imports decide what is called, and nothing joined them. **One of the four is live**: a command registered only under a `test-automation` cfg that the production feature set does not include, called from three shipped chat modules each wrapping the call in a silent catch — so the invoke fails in every production build, the rejection is swallowed by design, and the list it populates is permanently empty with nothing logged. Recorded in the new gate's baseline rather than repaired: the repair is a product decision belonging to the owner. Shipped a new gate + two-sided baseline (a rise fails, and so does a silent drop) + a 20-assertion self-test, wired into the project's `check` chain; unmodelled cfg predicates return null and fail loudly rather than being assumed satisfied, which would hide exactly what the gate is for. **Verified red on both a rise and a silent drop, then green on restore** — a gate only ever seen passing has not been tested. Committed with a pathspec on the project's active branch, lefthook pre-commit green, **not pushed**. Two instrument self-catches, both now recorded in the application: a naive bracket match over raw source over-counted registrations (1,634 against the true 1,627) because the file carries an unbalanced `[` inside a comment within the handler list, and the feature name has to be read from the raw text at the same offset because masking blanks string literals. Return condition: when the frontend tier and the cargo feature set are actually coupled by an assertion, the per-variant counts become defect counts rather than an exposure surface |
| 2026-09-06 | turn-classification (amendment: no call site to ask) | model-routing | — | **unapplied** | — | No fleet project routes per turn by inferring a class from a human's input, so the amendment has no seam to test. The two nearest are near-misses in the instructive direction: `personas` selects a model tier × reasoning effort explicitly in a config panel, and `tracklight`'s agent asserts a model per action in its `action.toml` — both are the caller-asserts contract, one with a human in the caller's chair, which is the arrangement the amendment says is *correct* where it is available. Reported `unapplied` rather than simulated because the method forbids invented cases and there are no real ones: walking a policy the tree does not implement, over turns it does not classify, would be an opinion with a table around it. **Return condition:** when a fleet project adds a model choice made from the user's own input rather than from an asserted class — the first candidate is any chat or command-palette surface that grows a cheap tier. Noted for a future row against the technique's *existing* rules rather than this amendment: `tracklight/crates/agent` defaults an action's model to a literal model name and types `ActionSpec.model` as a model string rather than a class, which is the decision rule *class names survive roster changes; tier assignments do not* |
| 2026-09-06 | enumerable-domain-decoding | structured-output | kp | **task** | **better** | Seam: `pipeline/jobfit/gemini.py:452-453` sets `response_mime_type` and nothing else - `response_schema` occurs **0 times in the whole repository** - while the pipeline repairs the domains downstream at `automation.py:909` (confidence 0-100), `appmaster.py:165-173,672-682` (scope_rung 0..2), `matching.py:126` (potential_score) and `calibration_drift.py:65`. Instrument added this run (`pipeline/jobfit/decoder_domain_gap.py`, 13 tests green) walks `AnalysisResult.model_json_schema()` through `anyOf`/`$ref` for bounds/enum/const and scans for clamp and validator sites, excluding tests and itself: **153 exported fields, 1 with a declared domain (0.7%), 17 repaired in code.** **Structural fact nobody designed:** the schema is complete and machine-readable and has exactly one consumer - the TypeScript generator at `codegen.py:153`; the model boundary, the one place where sending it would prevent all 17 repairs, is the one boundary it does not cross. The loop closes on itself: the model emits out-of-domain values because nothing told it the domain, and nothing tells it because the clamps make the symptom survivable. **The run's premise was wrong and the instrument caught it** - the domains are not declared-and-unsent, they are not declared at all, so they reach neither the model nor the generated TypeScript; that promotes the technique's derive-don't-retype rule from footnote to load-bearing and adds a step ahead of the obvious one. `task` rather than `code` because adoption is 6-9 files / ~120-180 lines across the models, the adapter and the clamps. Plan at `.ai/tasks/2026-09-06-declare-model-value-domains.md`; first step landed (instrument + tests, asserting a known-present clamp so an empty scan cannot read as a closed gap); commit `b933fb81` on `main`, **not pushed**. The tree already implements the shed-is-visible rule for the mime-type constraint under grounding, so a response schema inherits that flag rather than needing a second one. **Return condition / falsifier the plan names:** before sending the schema, count how often each clamp actually changes its input on recorded analyses - a clamp that has never fired makes steps 2-4 churn and shrinks the item to a deletion |
| 2026-09-06 | budget-includes-the-callers-own-cache | admission-queue | - | **unapplied** | - | No seam: a fleet-wide scan for a free-capacity read (`freemem`/`availableMemory`/`free_memory`/`mem_get_info`/`totalmem`) returns **0 files in all 9 checked-out projects**. The technique needs an admission gate that divides into a remaining-capacity figure behind a pooling allocator; the fleet's queues wait on a pool rather than sizing against its headroom, so there is no denominator to read wrongly. **Absent, not unexpressible** - the instrument ran and returned zero, and nothing is owed to make it measurable. Reported `unapplied` rather than simulated because walking a policy over an admission check no project makes would be an opinion with a table around it. **Return condition:** when a fleet project sizes admitted work against measured headroom rather than against a connection or worker count - the first candidate is any local-model or media-encode path that must decide a batch size before it starts |
| 2026-09-06 | constraint-injection-for-unreachable-tiers | test-harness | - | **unapplied** | - | No seam: no fleet project carries a capacity or capability ladder whose low rungs are unreachable from the dev machine. **This absence was a self-catch** - a grep for `MAX_*_TIER|FORCE_TIER|SIMULATE_|_SIMULATED` returned 3 files in kp and would have manufactured a seam; reading them showed a rate-limit constant (`app/api/interview/simulate/route.ts:26`), that constant's contract test, and a vendor endpoint URL. There is no ladder, so there is nothing to inject a false capacity into. **Return condition:** when a fleet project grows a declared rung table whose rows change behaviour - the nearest plausible one is a local-inference or offline mode with a memory-dependent path, which would need the probe interception point before the rungs, not after |
| 2026-09-06 | branch-provenance-gate | quality-gates | - | **unapplied** | - | Unapplied **by the technique's own scope rule**, which says its value collapses toward zero on a single-owner repository because there are no other proposal branches to be polluted by. Establishing that took two instruments and the first one lied: `git log -200 --format=%ae | sort -u` reported **5 committers in `ascent`**, which would have been the seam. Reading the identities showed `loop@ascent.test`, `land-test@ascent.invalid`, `deps-test@ascent.invalid` and `wt-test@ascent.invalid` - that project's own harness authors - leaving one human. Every fleet project is single-owner, and the defect class (a proposal carrying another proposal's commits) cannot occur without a second contributor. The source tree is the contrast and carries the application: thousands of forks, and this gate is the **only** push-stage hook it has - chosen ahead of a formatter, a test runner or a type check. **Return condition:** when a fleet repository takes commits from a second non-synthetic committer identity on an origin branch |
| 2026-09-06 | first-byte-commits-the-verdict (`failover-horizon` amendment) | model-routing | pof | **code** | **better** | Seam: an SSE consumer whose reader loop broke on stream end and did nothing - status 200, nothing thrown, no verdict ever set, surface renders "running" forever; the producer reaches it when its generator returns without yielding a terminal event. **Paired A/B, one harness, one stubbed stream**: arm A called the completion handler **0 times** (test fails), arm B **once with a failed status** (passes). A control case carrying the terminal event returns success in **both** arms, which is what isolates the defect to the missing-terminal case rather than to the harness. Two neighbouring suites pass in arm B (13/13), one of which drives the same hook with a never-settling fetch and would have caught a branch firing on a merely pending stream. Typecheck and lint clean. `generalised=no`. Return: when a second stream consumer is added - the requirement is per-consumer and nothing enforces it across the five routes |
| 2026-09-06 | tolerance-is-priced-against-a-residual (`similarity-keyed-admission` amendment) | client-fetch-cache | - | **unapplied** | - | No seam. Query `cosineSimilarity\|cosine_similarity\|nearestNeighbo\|semantic.cache` over `*.ts,*.tsx,*.py,*.rs`; population **8 fleet projects**; count **1 file**. Reading it - the step a count cannot do - shows a resemblance **matcher** that routes a step to a candidate and then still does the work, not a cache serving a stored answer in its place, so no false-hit rate is being purchased and there is no residual to price. **The broad form of the query returned 136 files in one project and all 135 extras were the bare word `embedding` in unrelated contexts**; reporting 136 as the population would have manufactured a seam. Return: when a fleet project builds a cache that serves a stored answer on a resemblance match |
| 2026-09-06 | projection-covers-the-record | markdown-vault | (this registry) | **experiment** | **better** | Seam: the registry's own mirror — the index projects each concept document into slug, `use_when` and law statements and **drops the body**, and the map instrument reads that projection. Paired, both assertions run first: known positive `markdown vault` 2 owning rows / 3 total; known negative `zzqqxxwv` 0/0; **arm A (index) 0 owning rows out of 40 returned**, arm B (body-reading mode) 2 of 40. The probe term is carried in the body of two subjects and in no slug or `use_when` anywhere. **The failure shape is the finding**: not an empty result, a full one with the concept's owner absent — and the instrument's own header told readers to reach for the body-reading mode "before believing any empty", while neither recorded failure (2026-08-31: six rows; this one: forty) was ever empty. Shipped the corrected guidance plus a printed footer naming what the matching did not read, varying by the flags the run actually passed. Return: when the index carries a body digest, at which point the two arms converge and the footer is redundant |
| 2026-09-06 | unregistered-is-stronger-than-refused | authorization | pumper | **code** | **better** | Seam: a process-constant switch enforced the strong way — the actuating tools are withheld from the tool listing when it is off, so a generated client cannot spell them — with the gated vocabulary then spelled **three times** by hand: the block deciding what is advertised, the guarded dispatch arms deciding what is reachable, and the refusal arm deciding what gets a readable error naming the switch. They agree today; nothing makes them agree tomorrow, and the directions are not equally loud — a tool advertised behind the gate whose dispatch arm loses its guard stays **callable while the switch is off**, silently, and it actuates. **Paired: 0 of 4 drift directions detectable by anything in the repo before, 4 of 4 after**, plus 2 cannot-check guards so a parser whose shapes have moved reports that rather than reporting clean. 10/10 green, whole node lane 34/34, wired into the justfile and the CI job that already runs it. Instrument asserted against the live source and cross-checked from another layer: its ungated set matches what an end-to-end test pins in Rust from a running server. `proof=ab-paired`. Return: when the three sites are replaced by one authority the others derive from — the checker then becomes ceremony and the row says to delete it |
| 2026-09-07 | derived-expectation-needs-an-evidence-floor | conformance-checking | (this registry) | **code** | **better** | Seam: three gates here derive their evidence by pattern-matching source rather than by loading or running what they describe. `check-citations.mjs` **already carried the floor** - `if (urls.length < 50) die(...THE EXTRACTOR IS BROKEN)` plus known-alive/known-gone probe controls - arrived at independently of this technique, which is its strongest corroboration and the reason the row is a coverage fix rather than a new idea. The usage-lane and signals-lane gates had neither, and are the harder **must-not-match** shape where a clean sweep and a dead pattern set produce identical output: the count each publishes says how many files were read, never whether the five patterns can still fire. Four arms, population = the one real contributor file carrying a planted Windows absolute path, decay modelled as pattern drift: **A** as shipped RED (leak caught, named); **B** drifted **exit 0, "usage lane OK" - leak shipped**; **A'** control added, still RED (no false positive on the healthy path); **B'** control added + drifted, exit 2 naming the dead pattern. Both gates now assert one fixed positive per pattern, in pattern order, before reading a contributor file, and both stay green on the real tree. **The calibration is the row's most useful line**: the first known-positive was planted through a shell argument that ate its backslashes, so it contained no path separator and the gate correctly passed it - an instrument agreeing with a broken fixture, i.e. this technique's failure one level up, and invisible without running the calibration. Unexercised: the technique's per-case clause (separate *derived nothing* from *derived and matched*) needs a multi-population gate; both shipped gates have one. Return: when a third lane needs the leak table, at which point the table and its controls are hoisted together - never apart - or when a gate here grows a per-case empty |
| 2026-09-07 | capability-declared-in-the-type (presence-vs-declaration amendment) | data-access | kp | **code** | **not-better** | **The `not-better` is the finding, and the seam was chosen to falsify.** kp sits on the *excluded* side of the amendment's new boundary: its provider adapters subclass a base whose `complete_document` raises a typed refusal, so every adapter has the member whether or not it can honour it, presence is uninformative **by construction**, and kp correctly keeps a declared matrix - commented "DECLARED per adapter, never probed at call time". The amendment predicts exactly that and nothing in kp is improved by switching channels; the boundary is confirmed from the side it excludes, which is the half a source-tree-only reading could not supply. What the boundary *did* direct is the assertion kp was owed and lacked - the technique's own third one, which it calls "always missing": `subtype="missing_capability"` appeared once in the source and in **no test**, against five other subtypes that are asserted in tests. Shipped (`3f253853`): two tests derive both directions from `PROVIDER_CAPABILITIES` rather than listing providers - a row without the capability must refuse by name, a row with it must not inherit the base - with a floor on the adapter/matrix join so a collapsed population cannot pass vacuously (the run's other landing, applied to itself). Negative control run and restored: adding `CAP_FILE_INPUT` to the anthropic row, which inherits the refusing base, turns the pair red and names the defect; 12 pass on the real matrix. Note the CRLF trap - the control's Python restore rewrote line endings on a file whose content was unchanged, and `git checkout` was needed to leave the sibling's tree clean. Return: when a fleet project defines an adapter interface as a plain record of optional function fields with no base class, which is the side of the boundary no fleet tree currently sits on |
| 2026-09-07 | self-trained-capture-filter | agent-memory | (none) | **unapplied** | **unmeasurable** | **The absence was measured, not assumed.** No fleet project carries a learned pre-filter in front of an LLM judge. The one memory system, personas, took the opposite route the technique's closing section prescribes for stable noise: its sleep-cycle gate is a stated-rule screen — an interval floor, a pressure threshold in characters, a minimum staleness — every skip returning a human-readable reason, consuming no outcome of the consolidation it gates. Its dedup is by content hash and stable keys, never by embedding similarity, so the `add_bar <= match_bar` invariant has nothing to bind to either. The technique's "when not to use it" clause therefore predicted the fleet's actual choice and is confirmed by it, which is the only thing this row can honestly claim. Instrument that would make it measurable: a screen-rate counter paired with a sampling-lane disagreement rate — neither exists in any tree read this run, including the source's, where screened items are dropped with a debug log and no counter. Return: a fleet project adds a screen whose decision derives from its judge's past output, or personas' sleep cycle gains a pressure term computed from prior cycle yields |
| 2026-09-07 | decay-and-forgetting (the-test-enters-through-the-door amendment) | agent-memory | personas | **experiment** | **better** | Seam: whether a memory system's governance tests assert the *mechanism* or the *door*. Same instrument both arms — count non-definition references per governance function — calibrated first against a live pair in the source tree (`fuseResults` 4, `applySearchBoost` 4) so a zero is a reading. **Arm A** (source, tests construct the governor): `runRecallLifecycle` 0, `recordAccessAndMaybeTransition` 0, `applyLifecycleBoost` 0, `getStaleMemories` 0, `createUserScope` 0, `buildAdmissionStats` 0 — **0 of 6 have callers, and the suite is green**, because `test/smart-memory-lifecycle.mjs:88-100` constructs the decay engine and tier manager directly and asserts correct transitions. **Arm B** (personas, tests drive the door): `spawn_reapers` 2, `reap_recorded` 1, `drain_ledger` 4, `record_owed` 7, `resolve_reaper` 4, `record_attempt` 5, `gc_archived_memory_embeddings` 7 — **7 of 7**. The causal link is readable rather than merely correlational: `every_delete_door_records_the_owed_cleanup` enters through `memories::delete` and asserts the ledger row, so a lost caller fails by construction, and the module stands in plain tables for its feature-gated vector tables so the door test still runs in the build where the real mechanism is compiled out. Two worker claims were re-checked by the director; one was corrected (`getStaleMemories`'s two hits are a declaration and its implementation). **Ship 0 and the reason is the verdict**: personas already implements both halves, including recording a debt on all three skip paths rather than no-op'ing, and the amendment was written from its arm rather than proposed to it. Return: a fleet project hosts a lifecycle sweep inside an optional feature and its skip path records nothing |
| 2026-09-07 | relevance-floors (substitute-path-score-space amendment) | retrieval | personas | **experiment** | **better** | Seam: what a retrieval fallback writes into the field the healthy lane also writes. **Arm A** (source): `scoreLexicalHit` = `min(0.95, 0.72 + queryLen*0.02)`, monotone in the query and carrying no information about the match, so every substring hit with a ≥2-char query scores ≥0.76 — clearing the 0.75 bar that grants both a fusion floor (`retriever.ts:1353-1363`) and full protection from the reranker (`:1564-1576`), two guarantees calibrated for a sigmoid-normalized BM25 needing raw ≈5.49. The lane is labeled degraded; both thresholds run upstream of the consumer and never read the label. **Arm B** (personas): the optional semantic term composes multiplicatively as `value * (1 + w * similarity)` with absence mapped to similarity 0 — the multiplicative identity — so an un-embedded, off-topic or below-floor memory scores exactly its value score and a partially embedded corpus ranks byte-for-byte as the value-only path, a property the module states as load-bearing. **The apply step revised the technique before commit**: the amendment first said "emit no score", which is ambiguous about what the combining step does with a missing value; arm B supplied the sharper rule — an additive blend contributes zero for a missing term, indistinguishable from a measured zero, so the absence must be the identity of whatever combines the lanes. Ship 0: personas is already on the correct side and is the source of the rule. Return: a fleet project adds a scored fallback behind an optional lane, or the source's FTS-less path gains a fixture asserting what its floors admit |
| 2026-09-07 | baseline-ladder (cap-inside-an-arm; the truncation boundary) | agent-memory | personas | **code** | **better** | Seam chosen to **falsify**, per the standing focus: the amendment claims under-spend is *not* the signature when a cap truncates inside an admitted item, so the seam picked was the one that should expose a truncation the under-spend check misses. **It refuted the hypothesis.** The truncation branch exists in the compiled-truth arm but cannot fire at this scenario's scale (6 pages, mean 988 chars, against a 6,000-token budget). What the same seam showed instead is the plain form of the corpus rule, unnoticed across four published arms: realized context spend of **15%, 27%, 32% and 54%** of one shared budget - a 3.5x spread in what the arms actually read - with the realized number and the declared budget both already printed in every report and never subtracted. **Arm A** (reports as published): 0 of 4 arms flagged. **Arm B** (same four runs, same renderer, utilisation computed): 3 of 4 flagged, and the three flagged are exactly the three lowest-accuracy arms (0.835 / 0.866 / 0.784) while the only unflagged arm is the highest (0.892). Re-rendered from each run's own header and answers - no model calls, no judge, no re-run - so the two arms differ by the two added lines and nothing else. The 50% line is the technique's own wording ("leaves most of it on the table"), not a tuned threshold; the 54% arm passing is the discriminating case. **Ship: personas `61fbf9b8e`.** Untested and named: whether raising the write-verdict arm's `neighbours=3` - its actual binding constraint - moves its 0.784, which needs a paid run. Return: when a ladder run compares arms whose binding constraints differ, which is every run to date - each backend knows whether it stopped on budget, on item count or on store exhaustion, and still does not report it |
| 2026-09-07 | timezone-anchored-period-dates / anchor-re-derived-not-chained | subscription-billing-periods | kp | **task** | **better** | Seam: the allowance window vs the PAID window. kp keys its monthly allowance ledger on a UTC **calendar** month at two call sites, while the payment provider charges on the subscription's **anniversary** - and kp already stores the provider's true period, reading it only for expiry and display. The two coincide only for a subscription anchored on the 1st. **Instrument calibrated before it was believed**: two known-answer cases (an anchor on the 1st MUST yield one window; a mid-month anchor MUST yield two), both separated before any measurement was read. Predicate = distinct allowance keys emitted across one paid period, correct answer 1, over 10 representative anchors. **A** (HEAD, calendar-month key): **9/10 over-grant** - two allowances inside one paid period. **B** (anniversary key, day clamped to the target month's length): **0/10**. **B0, the control** - the first draft of B, comparing against the raw anchor day: **1/10**, broken on the month-end anchor, which is the clamp drift `anchor-re-derived-not-chained` exists to describe; the technique predicted its own violation and the control is what the correction was worth. The error direction is an **over-grant**, the invisible half of the pair `plan-entitlements` names. **Shipped the instrument, not the fix** (kp `66ee5f0d`, project hook green, not pushed): re-keying changes the persisted allowance period format and needs a cut-over decision - a mid-period re-key must not hand a customer a third allowance - so the B arm is a committed plan in the project's own task lane and the pinned `9` goes red the day it lands. **Second independent sighting in the fleet**: ascent computes its monthly allowance from a UTC calendar-month start with the same independence from the anchor; not separately A/B'd (one project per finding per run), but two projects with one hole is what makes this a technique rather than an anecdote. Return: when the planned re-key lands and the pin flips to 0, or when a third fleet project meters a monthly allowance against a provider-anchored period |
| 2026-09-07 | unspent-budget-is-a-defect | generative-provider-routing | gravity | **code** | **better** | Seam: `lib/imaging/budget.ts:215` (`budgetStats`) against `lib/imaging/router.ts:64` (`PLAN`). **The finding is an enumeration the tree wrote about itself**: its header block says "A ceiling that never reports its own activity is only half a limit. **Two things** used to happen silently here and now do not" - uncounted refusals, an invisible window reset - and both are overspend-direction. There is no third, and a third fits the premise exactly: a window that spends almost nothing on the fallback vendor while `google`, the entry the tree's own 60-cell grid put first at $0.052/usable against leonardo's $0.110, is never called. `spendByAxis().byProvider` already held the raw fact but flat across capabilities, so a provider serving one `recognize` call reads as "called" for `generate` too - the data existed and the **verdict** did not, which is `unmeasured-is-not-pass` at the window level rather than the call level. Added `budgetFloorUsd()`/`FLOOR_VAR` with `floorUsd`+`underFloor` on `budgetStats` (reporting only - never read by `assertWithinBudget`, so declaring a band cannot change who is refused; and `rows > 0` is required, because an idle window is arithmetically under any floor), `reachByCapability()` excluding `failed` rows (reached is not served), and `unreachedPlanTops()` in the router where `PLAN` lives. **Measurable named first: run-level dispositions the meter can distinguish.** Same two windows both arms - thrifty (one $0.0257 leonardo call), healthy (40 x $0.045 google). **Arm A** (pre-change health fields: refusals, bookedFailed, unpriced, over-ceiling): `false` / `false`, **indistinguishable, 1 disposition**. **Arm B**: `underFloor=true`/unreached=1 vs `false`/0, **2**. The arm-A control **ships as a committed test**, which is what keeps arm B evidence rather than a restatement of its own implementation. 21/21 pass (4 new), `tsc` and `eslint` clean. **Seam-to-falsify fired for the third round running**: the per-capability reach test was written to check the technique and instead found a live second instance nobody was hunting - dev plans `ollama` first for `recognize` at $0 explicitly so a box with a daemon never pays, so a cloud-served recognition leaves that top uncalled, and the per-call elimination trail cannot see it because no single call departed from the plan. **Ship: gravity `ae79916`** (+ `e1c31ec` correcting a guessed hash in the row), not pushed. Untested and named: the floor is a **declared band and nobody has calibrated one against real traffic** - this run proved the instrument fires on constructed windows only. Return: when an operator sets `IMAGING_BUDGET_FLOOR_USD` on a real production window, read whether `underFloor` fires on runs a human would call thrifty |
| 2026-09-07 | part-cut-planning | image-to-3d-input-gating | pof | **simulation** | **better** | Seam: the missing stage between a defect class and a consolidation step. In that tree the word "part" has exactly one meaning and it is a **defect** - the critique stage carries an over-budget part count as a finding code beside floater fragments, and the finishing stage is declared to **resolve** it because its runner joins every part back into one object. No stage anywhere authors a part list, so the split that budget division and assembly both consume is never anyone's decision. Parts are what the generator did to the asset, not what the pipeline asked for. **Three recorded in-tree cases, not invented ones.** (1) Four independent rolls of one prompt at prop asset class: 0/100 each, 16-50 floaters and 35-56 substantial parts every time, 20 credits a roll - policy A discovers the fragmentation after paying for it and the tree's own conclusion is that re-rolling buys nothing but the bill; policy B commissions named closed regions so a failing part costs one part's credits. (2) The decimation pair - 1,482,446f / 2 components / 1 floater grades warn, its decimated game mesh 46,791f / 17 components / 16 floaters grades fail - policy B makes the shattering part identifiable and re-commissionable alone, **and is explicitly NOT claimed to reduce the floater count**. (3) 10 of 52 fail, every one on floaters, none on the part count: **policies A and B are identical here and the row says so** - a cut plan does not touch the dominant defect, and reporting the case that shows no difference is the point. **Falsifier named**: if planned per-part commissioning costs the same or more total credit per accepted asset than one whole-subject generation plus its re-rolls, the prediction is wrong - measurable from the job stores the pipeline already keeps, and it is the arm to run first. Ship 0: the next step is running that arm, not editing code. Return: when a part-list artifact exists on the image side |
| 2026-09-07 | texture-pass-must-consume-the-bake | mesh-finishing-for-engine-readiness | pof | **simulation** | **not-better** | **Seam chosen to falsify, per the standing focus - third consecutive round it has paid, and this time it refuted the run's own technique rather than its hypothesis.** The technique's central procedure step is *probe the texturer with a strongly-baked part and see whether the returned colour follows it*. In this tree that step is **unrunnable, not uninformative**: the re-texturing module documents, ground-truthed against the provider's own SDK reference in its module header, that the texture operation accepts a prior generation task id **and nothing else** - an externally finished mesh, precisely what the finishing bench produces, cannot be submitted at any price. **Arm B does not exist.** The structural fact nobody designed for: generator-textured and bench-baked are **disjoint product lines** - an asset either comes back coloured from the generator and never receives a bake (the bench's UV mode has a branch for exactly that input, re-packing the islands textured parts already carried rather than re-projecting and discarding authored seams), or it is baked by the bench and has no path back to that texturer at all. Nothing crosses. **The rejection is what the row is worth.** The technique gained a new section, "When the round trip does not exist at all", stating the condition under which it did not hold: where a texturer only textures its own output, the routing key is still normal salience but is applied at **commissioning** time to choose which line the asset enters, not after finishing to choose a texturer - and choosing wrong is a commitment that costs the whole asset to undo, not a defect a later pass repairs. What the tree does **not** contain is any statement of that consequence: the bench-baked line has no budgeted colour engine downstream of it. Ship 0: no arm can run, and the only cheap change was a doc comment with no measurable. Return: when the pipeline adopts a texturing engine that accepts user-supplied meshes, the probe becomes runnable and a paired arm is owed |
| 2026-09-07 | gap-directed-elicitation | agent-memory | personas | **experiment** | **unmeasurable** | Seam: `evals/memory-year/memory_year/world.py` - `World.generate`, where every fact is `_new_fact()` immediately followed by `_emit()` across all seven construction passes, with no assertion guarding the invariant because nothing had ever violated it. **Seam chosen to falsify, and it was not falsified** - fourth consecutive round the falsifying choice paid. The technique's sharpest claim is that the entire apparatus which ranks memory designs is blind to knowledge nobody transacted, and this harness was the place to disprove it. Instead: the module docstring names the property as a virtue for an unrelated and correct reason (*"probes are derived from the world, never from observed queries, so the fixture set cannot feed on itself"*), and that same property means **every probe asks about knowledge the store was told**. The nearest existing class is `distractor` (8 probes): a key never stated **and not true**, graded UNKNOWN, so a store is right to disclaim it and penalised for inventing an answer. Its exact inverse - **true, and never stated**, where UNKNOWN is wrong - did not exist. So no arm on the nine-row ladder has ever been asked a question whose answer was never in the event stream, none can lose a point for lacking an acquisition path, and **the 0.89 ceiling is a ceiling on witnessed knowledge** - sound as measured, narrower than the word "memory" suggests when cited. **Measurable named first**: held-out facts backed by zero events, and whether enabling the class perturbs any existing arm's inputs. **Arm A** (`held_out=0`): 176 facts / 3,571 events / 194 probes / 10 classes, **byte-identical to the generator at the previous commit** on all three lists, asserted field-by-field. **Arm B** (`held_out=8`): 184 / 3,571 / 202 / 11, event list and every non-held-out probe identical, **0 of 8 held-out facts referenced by any event**. Proof `ab-paired`. Two design decisions carry the inertness and both are the kind that are wrong if written the obvious way: a **separate RNG** (a single `rng.choice` in the main stream would have shifted every later draw and silently produced a different world under the same seed) and **appended last** (creating the facts where they logically belong would have renumbered every downstream id). `unmeasurable` rather than `better` because no arm has been **scored** on the class - that needs a paid run over 202 probes - and the method forbids the word without naming the instrument that would fix it. The instrument is named and now exists. **Prediction recorded before the measurement**, because a prediction nobody wrote down cannot be wrong: every current arm scores at or near zero on `held-out`, including the leader. Ship: personas `565e54569` on `master`, lefthook green, not pushed. **Falsifier, and it is the first thing to check on the paid run - not the score**: if a capture-only arm scores well, the class is **leaking** (the answer is inferable from surrounding events the store did see), which would mean the pool is guessable rather than that acquisition is solved; reading it the other way turns this into exactly the flattering measurement `coverage-instrumentation` exists to refuse. Named limit: `held_out` defaults to 0, so the gap stays invisible unless asked for - deliberate, since it is what keeps the nine published rows comparable, but it does preserve the failure the class was built to expose as the default. Return: when a paid run scores the arms on the held-out class |
| 2026-09-07 | scene-partition-is-the-gated-unit | image-to-3d-input-gating | pof | **code** | **better** | **Seam chosen to falsify, and the first arm refuted the hypothesis — fourth consecutive round the focus has paid.** Only the technique's *residue* half had a seam: the project generates one asset per submit and its input gate demands exactly one subject, so the partition half is honestly unapplied. The residue seam is a packaging collector that already implements the idea independently, with a docblock reading "Reported, never dropped: a package that silently omits a mesh looks identical to one that never had it" — a strong falsifier, written by someone already burned. Arm one drifted the extension list, an ordinary refactor, and was **caught**, 5 of 18 tests red; the enumerated formats are well covered. That failure produced the correct arm: the unresolved channel is only reachable by a string that already matched a serve-route regex, so two producing steps emitting four artifacts in unenumerated formats gave a **byte-identical report — files=6, unresolved=1, suite 18/18 green** — four real artifacts in no field at all, indistinguishable from steps that produced nothing. After the residue channel, **unresolved=5**, full suite 269 passed 1 skipped, shipped with a test that is red without the change and a negative control (prose, bare directories, engine object paths) reporting nothing in both arms. Per-case coverage cannot reach this class, because a format nobody enumerated has no case. **Unplanned convergence with `derived-expectation-needs-an-evidence-floor`**, landed the same day from an unrelated source by a different run. **Return condition:** the floor reaches *a reference I saw* and stops short of *a reference I expected* — when the project grows a brief-side manifest declaring what a step owes, the technique's step 1 becomes testable and a step that produces nothing becomes detectable |
| 2026-09-07 | four-term-difficulty-decomposition | difficulty-design-and-adaptation | pof | **code** | **better** | Amendment applied, not the base technique: the estimator of the one unsettable term errs in a *knowable direction only if it plays*, so a non-playing author leaves an unsigned residual. Seam is `threat-score.ts` (weighted sum of an archetype's stats into a danger number), whose weights the project's own audit records as hardcoded by a generator, and it is a **second seam in a tree the existing application already covered at a different one**. Both arms over the same twelve-archetype three-tier roster - shipped vector, and an equally defensible one weighting defense level with offense on the argument that survivability compounds. **Cross-tier ordering identical** (tier separation dominates, so "boss vs grunt" never sees the weights); **within-tier ordering flips in all three tiers, always the same pair, six of twelve archetypes moving** - and within-tier is exactly what the peer-band linter and encounter budgeting read, so the fragile half is the consumed half. **Structural fact nobody designed**: every pre-existing assertion in the module was weight-*independent* (damage outranks health, a score is positive, empty is zero), each passing for any vector where damage outranks health, so a suite that looked like coverage pinned no shipped value at all - unguarded by construction, with a green suite saying otherwise. Shipped the corrective the technique prescribes and **explicitly not a retune**: provenance recorded as unestimated with the reason the easing reflex does not apply, plus characterization tests whose own comment says they do not claim the order is right, only that changing it is a decision. **The first calibration of that guard FAILED and the failure is the useful part** - a single weight nudged 0.05 left all 11 tests green, so the guard did not protect what had just been claimed for it; re-run against the perturbation actually measured (the full reweighting) both new tests go red, and the shipped comment now states the reach honestly as a change of *stance*, not drift. 50/50 green across the balance lane after restore. **Ship: pof `a87224d9`**, not pushed. Return: when someone plays the game and reads the within-tier order, change the weights and the expectation in one commit |
| 2026-09-07 | entitlement-exhaustion-is-not-ill-health | cross-provider-benchmark-operations | tracklight | **experiment** | **better** | `proof: structural-only`, ship 0. **Seam chosen to falsify, and it mostly succeeded - the row is worth more for what it refuted than for what it confirmed.** That runner already renders provider-side absence separately from money-side absence and comments on the distinction in this technique's own terms, feeds its breaker on **generation** failures only so a bad judge hour cannot indict a healthy target, fails open when every target is indicted at once, gates a truncated run as unverified with a test named for the trap, and tests the leaderboard's winner **paired on the cases both targets completed** so a short column cannot win by skipping hard cases. **The coverage half of the technique lands as already-covered and the application says so rather than claiming it** - a team adopting against a runner of this shape adopts one rule, not six. What survives is missing *structurally*: the engine defines **eleven typed error variants**, one of which is exactly this technique's discriminator (it carries the wait that did not fit, the budget that remained, and the attempts spent, with a doc comment insisting it be kept distinct from ordinary exhaustion), and one layer up the comparison runner reduces every generation outcome to a **boolean** before the breaker or the report sees it. Eleven causes upstream, two absence classes in the artifact, and the variant that would have made a third possible is discarded at the assignment. Not an oversight about the distinction - the same tree draws the harder version of it two fields away - but a type collapsing at a layer boundary one line before the consumer that needed it, invisible to a reviewer reading either file alone. **Ship 0 for a recorded reason, not an excuse**: another session held uncommitted work in the file carrying the collapse, which is the one case the standing authorization does not cover. Not proven and named: frequency - nobody exhausted a real plan against this runner, so what is shown is that the artifact *cannot express* the distinction, never that it has misled anyone. Return: when that session's work lands |
| 2026-09-07 | repairing-check-erases-its-evidence | invariant-placement | pumper | **experiment** | **better** | Seam **chosen to falsify, and the first seam refuted the claim** - a *declared* threshold's fixtures already straddle it, because a named config field advertises both of its sides; that narrowed the technique to repairs against a **derived** quantity. Second arm: a half-open contract, a closed assertion and a dead clamp - the one-bit maintenance error the clamp exists to survive put **50.0% of seeds on the excluded bound** (n=1,000,000) with the unmodified test still passing. Mutation reverted, nothing shipped: the one-character fix is the tree owner's and the tree carried another session's uncommitted work. Return condition: an application from a tree where the repair actually fires in production. |
| 2026-09-07 | generated-file-hygiene | codegen | personas | **simulation** | **not-better** | Amendment applied, not the base technique: an artifact that is *seeded* rather than owned, where deleting an in-file marker transfers authorship permanently. Seam chosen to falsify and it succeeded on all three cases. The tree carries 68 generated-marker files and two committed exception lists guarding a binding exporter. **Case 1** - an orphan binding whose Rust type was deleted: the file exists so a marker fits, but it is a *contract artifact*, which the amendment itself forbids adopting; disqualified by its own third constraint. **Case 2** - four listed types carrying the export derive whose file was never written: **an ownership bit cannot live in a file that does not exist**, so the mechanism has no place to put the information at all. This case was new to the amendment. **Case 3** - the drift the amendment predicted does not occur, because both lists are **two-sided**: the checker fails on an unlisted violation *and* when a listed name stops being one, so a stale entry turns the gate red rather than accumulating. Both constraints went back into the technique, which is now narrower and true: the absent-artifact case it did not have, and the correction that drift is a property of *one-sided* lists rather than of lists. **Ship 0** - nothing in this tree should change; the amendment survives for the artifact class it was written from (editorial pages that always exist and are meant to be adoptable), and this tree has no such artifact. Return condition: a fleet project growing generated editorial docs with a history of clobbered hand-edits - that is the seam with an instrument for the maintenance cost this test could not see |
| 2026-09-07 | performance-direction (the zero-beat performance; frozen is a number) | cinematic-language | gravity | **experiment** | **better** | `proof: ab-paired`, ship 1 (`577524b`, ledger `5a8550e`). **Seam chosen to falsify, and the pre-check said what a CAUGHT arm would teach**: had the clip briefed "almost still - the camera does not move" measured at the frozen floor, the finding would have inverted into a boundary on the directive (it collapses to still at this scale, and the anti-freeze negative did not prevent it). It did not. The lane's three-poster judge had picked against that clip as "the same frozen wide with nothing advancing"; a consecutive-frame luma-delta ruler, asserted first on the advancing clip (8.547) and on a looped-still floor through the same codec (0.000, max 0.0025), put it at **0.210** - two orders above frozen, a third of its own undirected baseline (0.558). The clip obeyed; the instrument was blind. Shipped: a stdlib-plus-ffmpeg ruler with the calibrated floor written beside the number, a `motion` pre-filter in the lane's measurement script writing `measures.json` beside the readbacks, and a self-test case pinning frozen / not-frozen / unmeasured under the CI gate that already runs the file; 7 cases green; the shipped command reproduces the table. The global negative was not changed - a prompt change with no paired proof. Return: run the arm the ruler now permits - the same near-still spec with and without "no motion" in the negative at one seed, predicate the frozen rate against the floor - and decide whether anti-freeze belongs on every request or per shot |
| 2026-09-07 | generated-shot-sourcing (rung zero: the still that is moved, not generated) | video-assembly | gravity | **simulation** | **better** | `proof: structural-only`, ship 0 with the reason stated. Three real cases from the tree - the explainer fixture whose spec validator already refuses to move text, the read-only shots lane for promotional cuts, and the "still built to move" deck - walked under the ladder as it stood versus rung-zero-first. The tree is rung zero by construction and says so in its own words ("a clip in this app is AUTHORED, never rendered"), reached with no sight of the source. **Structural fact**: the decision to generate at all is made there by the *absence* of a renderer - nothing on the frame can say "stays a still by choice" as distinct from "no render seam yet", so the day one is built the rung-zero frames and the not-yet-rendered frames are indistinguishable. A field, not a policy, and it is missing. Not shipped: a field with nothing to read it is the authored-but-unread shape the tree's type file was scoped to remove. The existing `react--generated-shot-sourcing` application was extended, its citations re-resolved at HEAD before `verified_on` moved. Return: when a render seam is built, add the rung-zero field before the first clip renders and re-test as code - explainer frames should stay at rung zero by choice, trailer shots should climb with zero-beat direction for their set-dressing figures |
| 2026-09-07 | gate-liveness (a scoped population passes the floor test) | quality-gates | ai-registry (self) | **code** | **better** | `proof: ab-paired`, ship 1 (`check-skills.mjs`). **Seam chosen to falsify, and the pre-check said what a CAUGHT arm would teach**: the easy seam was the source's own changelog linter, which would only have restated the landing. The question was aimed at our own checkers instead - *does this registry carry a derived exemption whose premise is unchecked?* - and a caught outcome (nothing here has the shape) would have refuted the mechanism's reach and demoted the amendment to a claim about someone else's tree. Not caught. `check-skills.mjs` scoped version discipline to `--since <ref>`; with the flag absent it printed a correct, distinguishable `version discipline: NOT run` and exited **zero** - a derived population empty by default. CI passes the flag; every local invocation that authorizes a commit does not, and **this skill's own Phase 11 says to run `check-skills.mjs` before committing a skill change and has never mentioned it**. So the registry violated in its own tooling a rule the registry publishes, and `gate-liveness` already held the sentence that convicts it: a could-not-run routed to pass is wrong where the green authorizes shipping. Paired on a seeded unbumped `SKILL.md` edit - arm A `exit 0, skills lane OK` (defect escapes), arm B `exit 1` naming the skill and the frozen version; clean tree still green (no false positive), `--since HEAD~3` unchanged (CI path intact), unresolvable ref still FATAL exit 2. The flagless default now compares working tree and index against `HEAD`, and the report carries its predicate rather than a bare count. Not proven and named: whether any skill actually shipped unbumped under the old default - the arm shows the gate *could not have seen it*, never that it happened. Return: when a skill change lands with `--since` in local use, re-check that the two populations still agree |
| 2026-09-07 | (instrument, not a technique) board address fold | - | ai-registry (self) | **code** | **better** | `proof: ab-paired`, ship 1 (`run-board.mjs`). Not owed by a landing - found executing round 37's open board-address focus item, which had passed trivially twice for want of a live sibling. `norm`'s docstring claimed it folded `knowledge/x/y/z.md` and `x/y/z` to one token; it never did, and `touches` compared raw tokens, so the two dialects the method itself produces never met - **Phase 4 claims a subject slug, Phase 7 checks the index file address**. Measured against a live sibling holding `.../quality-gates`: slug-claim vs file-check **clear**, path-claim vs slug-check **clear**, subject-doc vs a technique nested under it **clear** (the doubled subject leaf breaks the prefix relation), identical spelling CONTENDED. The only pair that worked is the one the method never produces, so every `check` any run ever made against a held subject returned a false all-clear. Paired before/after on the same probe: detection **1/4 -> 4/4**, sibling subject in the same category correctly still clear. The fold self-asserts on every invocation over the cases measured broken. `normSource` in the same file was fixed twice because it was tested against real spellings; this fold was asserted in a comment and never tested. Return: none - this is the assertion |
| 2026-09-07 | imported-material-conformance (whole subject: 6 techniques) | imported-material-conformance | - | **unapplied** | - | **No managed project has this seam, and the absence was measured rather than assumed.** The fleet is web applications plus a 2D imaging studio; none carries an asset import edge. The media-generation project was searched with its own instrument, **asserted against a known positive first** (71 files for the control term), and the surface query returned exactly one file - which, read rather than counted, is an *audio* prompt containing the word "metallic". A count concealing a same-word match is a failure this registry has already paid for, so this is `unapplied`, **not** a simulation: three invented cases would be an opinion with a table around it. **Return condition:** when a managed project grows an asset import path - a delivery crossing a format boundary into a renderer - run `surface-conformance-swatch` against it first, because it is a test rather than an opinion and it fails loudly. |
| 2026-09-08 | elision-to-a-refetch-pointer (class by producer at the Read boundary) | prompt-assembly | personas | **code** | **better** | `proof: ab-paired`, ship 1 (`scripts/build/guard-whole-read.mjs`, commit ca95c49e3 on master). **Seam chosen to falsify**: the source's mechanism is a size-only block; the replay of 30 days of transcripts could have shown that the size rule was right (large reads mostly generated) and it showed the opposite - 175 of 208 fleet-wide large whole reads are source, 84% of a size rule's blocks. The class-based guard blocks 23 of personas' 43 (16 harness-overflow, 7 generated-artifact), recovers ~870k of ~1.19M tokens, 0 blocked reads followed by an edit. The size-only shape is `not-better` on every project measured. Return: what the agent does after a block (targeted read, grep, subagent, override) is unmeasured; `WHOLE_READ_GUARD=off` more than weekly in transcripts falsifies the class list. Candidates by measured rate: ascent 1 in 3, systedo-case 1 in 3; kp 1 in 12 but nearly all source - measure before installing |
| 2026-09-08 | enforcement-demotion (amendment: the sort applies to procedures) | agent-instruction-files | personas | **code** | **better** | `proof: ab-paired` via the same guard: the procedure "sort a read by class, block two classes over a threshold, fail open loudly" landed as a 30-line script with a 15-case decision table and no CLAUDE.md paragraph. Arm A is "nothing owns it" (no prose rule existed either): 43 whole reads in 30 days. The decision table ran in 256 ms without a model, which is the amendment's claim. Return: a blocked read a later step needed whole (n=7 generated-artifact reads in the sample - small) |
| 2026-09-08 | inherited-default-ownership | agent-instruction-files | kp | **code** | **better** | `proof: ab-paired`, ship 1 (one bullet in `.claude/CLAUDE.md`, commit on kp's active branch). **Seam chosen to falsify**: the harness's push-on-finish default is documented for background agents, not for the `-p` lane the arm ran in; the control could have stayed local and refuted the lane. It pushed (bare remote 1->2); the treatment with one policy line committed and did not push (2->2) and said why. Return: when a harness release changes the unattended terminal action again, re-run both arms. |
| 2026-09-08 | hook-hygiene (amendment: when the author is an agent, the bypass is not the author's to take) | quality-gates | kp | **code** | **better** | `proof: ab-paired`, ship 1 (`permissions.deny` x4 + `$CLAUDE_PROJECT_DIR` hook path in `.claude/settings.json`, same commit). **Seam chosen to falsify**: deny under `bypassPermissions` is undocumented, so the control and the treatment could have both run. They did not: A ran `git commit --no-verify` (`[main 4fd113b] probe`), B was refused twice; the in-tree dry-run probe against kp's own settings was refused before running. Return: a release that documents or changes deny-under-bypass; the row's fact is dated 2.1.263. |
| 2026-09-08 | sibling-floor-ownership (application: a repository-linked catalog is still the floor) | agent-instruction-files | fleet (12 trees) | **experiment** | **unmeasurable** | `proof: structural-only`, ship 0. Replay over 1,792 transcripts / 39,739 tool calls: 193 skills loaded, 28 ever invoked (25 of the fleet's own). The instrument that would decide a prune is the technique's held-out trial - one representative task per repository with `.claude/skills/` emptied and then present, scored on the repository's gate. Return: when that trial runs, or when the harness's own unused-skill report is captured per repository. |
| 2026-09-08 | inherited-default-ownership + hook-hygiene (amendment) - fleet unification | agent-instruction-files, quality-gates | fleet (12 trees) | **code** | **better** | Ship 12: the kp change (8fa6a693) generalised to every Wolf checkout in one pass - `permissions.deny` for `--no-verify`, force-push and each repo's own gate escape hatch (`*_SKIP_GATE=1`, `LEFTHOOK=0`, `HUSKY=0`; systedo-case also denies `git push*` because its own rules table says never push, and that row moved from `partial` to `blocking`), plus one git-policy block in the file each repo's agents actually read (canonical or conventions section, never a machine-owned region). tracklight's pure-template `.claude/CLAUDE.md` (loaded every session beside the real file) was removed. goat gitignores `.claude/`, so its deny block is machine-local and only the guidance line is committed. Verdict inherited from the paired arms above; not re-run per repo. Return: a repo grows a second bypass spelling, or a release changes deny semantics. |
| 2026-09-08 | gate-liveness + false-positive-economics (a gate that cannot go green on the author's machine) | quality-gates | ascent | **code** | **better** | `proof: ab-paired`, ship 1 (ascent d1b1ac88). Found while pushing a merge to master: the delivery contract's pre-push gate (`npm run verify`) refused on two failures CI had never seen, and the CI evidence was the control — the `verify` job is **green** on origin/master while only `e2e` is red, so the local red was a false positive, not a defect in master. Both causes were line endings on a Windows checkout: a `#!/usr/bin/env node` shebang with CRLF is an "Invalid or unexpected token" to the test runner's ESM transform (the test file failed to LOAD, reporting 0 tests), and a source-text assertion regexed on a bare `\n`. Arms: the same two test files red before, green after (`2 failed` -> `2 passed (16 tests)`), then the full gate green end to end. The repo's own `.gitattributes` already carried the rule and the reasoning for `.githooks/*`; the fix extends it one directory over. **A gate whose green is unreachable on the one machine the contract calls the real control is the shape that gets routed around** — and this repo's history already carries a "record the gate bypass" commit. Return: another executable class outside `scripts/*.mjs` grows a shebang, or a second source-text assertion is written with a bare `\n`. |
| 2026-09-08 | generated-shot-sourcing (amendment: when the cap stops binding, the beat floor starts) | video-assembly | gravity | **unmeasurable** | `proof: structural-only`, ship 0. **Seam chosen to falsify** — the shot decomposition, where `shots.ts:529` literally computes `holdS = round1(beatS / n)`; a caught outcome was defined first (if the tree already enforces a floor, the amendment restates a solved problem and demotes). It was caught, twice. `shots.ts:294` declares a measured `FLOOR_S = 0.5` and `shotCountFor` caps the count by it before dividing, so the quotient cannot go below the floor; a probe over the full parameter space (5 roles x 0.5-40s, n=400, with a known-positive assertion that fired) found the clamp reachable in **1 of 400** pairs. The clamp is near-unreachable because the tree *derives* beat counts from a script clock instead of letting an author enumerate them, and `shotPrompt.ts:5` prompts one shot per request — the model is never asked to cut. The corrective is not a change this tree could adopt; it is what the tree already is, reached independently. Refutation relocated the finding: the exposure is **the enumerating surface**, not long durations. Instrument that would measure it: a beat-enumerating prompt builder with a duration parameter, plus the review's `unmeasured` verdict wired to the seconds-per-beat quotient. Return: when a project grows a surface that sends one prompt enumerating several beats into one fixed-duration generation. |
| 2026-09-08 | engine-choice-on-decisive-terms (new technique) | voice-io | kp | **experiment** | **better** | `proof: ab-paired`, ship 0 (the only change identified is a **direction**, not coverage — an engine adapter creates a capability kp's harness scope does not name, and it is the decision the operator asked to be advised on). **Seam chosen to falsify**: kp already implements the metric the technique argues for, three weeks before the technique existed, so the pre-defined caught outcome was "already fleet practice, the corpus is behind and the landing's home is wrong". It was *partly* caught and the finding survived in altered form. Arms scored by kp's own `wer.py` over the same 3-utterance set (one utterance is the verbatim ground truth + recogniser output from a live Czech call in the project's test suite; arm B is constructed and labelled): aggregate WER **0.167 vs 0.278** prefers the engine that fabricated two skills, decisive-term recall **0.667 vs 1.000** prefers the other — the metrics invert. Sharper: on the decisive utterance **both arms score an identical 0.231**, so the aggregate cannot separate them at all. **Negative structural fact, the run's best artifact**: the harness speaks one provider's realtime websocket protocol directly and has no engine adapter — it holds the right instrument for choosing an engine and cannot aim it at a candidate, an excellent *regression* instrument and no *choice* instrument. Two self-catches during the seam hunt, both flattering and both caught: a fleet-wide `git grep -lin` parsed as `--lin` returned five empties reading as "no STT in the fleet" (exposed by asserting against a known positive), and a grep for `from .audio` wrongly concluded the seeded degradation probes were unwired (`session_runner.py:24` imports them as `from . import audio`; they are fully wired) — that second correction **removed** a finding and left the fleet ahead of the corpus on noise robustness. Return: when kp grows an engine seam, or when a second fleet project acquires a recognition path. |
| 2026-09-08 | cache-continuity (amendment: where the provider keys the effort decides what the flip costs) | model-routing | personas (seam) / Claude Code harness (arms) | **experiment** + **task** | **better** | `proof: ab-paired`, ship 0 - **seam exists, sized as a task, first step not taken** (schema change across 4 files; foreign WIP in the tree). Paired headless on Claude Code 2.1.263: turn 2 resumed at the same effort read 33,353 / wrote 59 ($0.0070); resumed one effort step higher read 23,997 / wrote 9,415 ($0.0425, 6.1x) - effort is keyed in the system layer of the prefix. **Seam chosen to falsify**: the vendor sentence "preserves the prompt prefix" would have inverted the technique's rule here if it were a property of effort; it is a property of that vendor's API. Structural fact: personas has two resume doors and only the companion's is pinned; `fleet_wake_session` spawns a bare `--resume`, so a plan row's `--effort`/`--model` are dropped at wake. Plan: personas `.ai/tasks/2026-09-08-fleet-wake-carries-model-and-effort.md` (commit f27ea0764). Return: `/intake apply cache-continuity --project personas --mode code`; measurable is `cache_creation_input_tokens` on the woken turn (~9.4K today) |
| 2026-09-08 | instruction-freshness (amendment: the reader can change while the file does not - a model successor is a coupling trigger) | agent-instruction-files | ai-registry skills lane (linked into the fleet) | **simulation** | **better** (prediction) | `proof: structural-only`, ship 0 - no gate in the lane sees instruction compliance. Three recorded cases: the 2026-09-01 nine-skill two-model bake-off (winner differed in 5 of 9; three `model:` pins added with no trigger firing), the 2026-09-08 harness default switch (no file changed, every floor now read by an untuned reader), and the pins themselves (check-skills admits `model`/`effort` as keys and never reads the value - no audit class covers them). Return: the instrument is a per-skill gate outcome recorded with the model that ran it; the first skill re-run after a model change can then be compared paired |
| 2026-09-08 | swr-design (amendment: what counts as a failed revalidation - could-not-look is not looked-and-found-nothing) | client-fetch-cache | tracklight | **code** | **better** | `proof: ab-paired`, ship 1 (tracklight `4199407`). **Seam chosen to falsify**: `journal.ts recover()` documented "Never rejects" as a deliberate choice, and an unlistable journal directory returned `[]` while the caller returned `0`, byte-identical to a clean start. New test fails before (0 diagnostics) and passes after (1 for a file-at-the-path, 0 for ENOENT); suite 43/43; the 6 tsc errors are pre-existing in a file not touched. What survived: ENOENT stays a genuine empty - the amendment's own boundary. Return type unchanged; the failure is spelled through the client's existing rate-limited diagnostics, as dropped events already are. Source-tree proof in `node--swr-design`: structure check removed -> a rewritten upstream serves zero entries as `fresh`; as shipped -> previous entries as `stale` with `lastSuccessfulAt` unchanged. |
| 2026-09-08 | outcome-branched-cache (amendment: a served-from-cache response advises with what it has left) | public-verdict-badge | ascent (history `da162859`) | **simulation** (structural) | **unmeasurable** | `proof: structural-only`, ship 0 - **no live seam**: goat's cached route computes per request, gravitone's cache hits are content-addressed and immutable, no other fleet route emits `s-maxage` over a memoized value. The corpus's own exemplar carried the gap before it was removed: `cache.ts` 15-min report TTL under `CACHE_RESOLVED = s-maxage=600` on every hit, no `Age` -> worst-case 25 min against a declared 10. Recorded in the application as its structural fact; the same read dated all three badge applications (feature removed 2026-08-29). Instrument: any fleet route that memoizes a lifetime-bearing value behind an edge directive; return when one appears. |
| 2026-09-08 | engine-choice-on-decisive-terms (follow-up: the seam the earlier row said was absent) | voice-io | kp | **task** | **better** | `proof: ab-paired`, ship 1 (branch `feat/stt-engine-adapter`, commit `25bb37a0`, **not merged, not pushed** — kp's `main` carries a sibling session's uncommitted work, which is why the build was done in a worktree). Closes the negative structural fact the same-day row recorded: the harness held the right metric and could not aim it at a candidate engine. Built the technique's own closing requirement — a recogniser protocol one method wide (`transcribe(pcm, lang) -> str`, deliberately narrower than the provider's conversation session), a command-driven adapter so an offline open-weights engine is a drop-in **without the project taking a dependency on any model runtime**, a registry where adding a candidate is a registration or one env var, and a bake-off that hands every engine the *same* synthesized bytes under the same seeded degradation, optimizes decisive-term recall and demotes aggregate WER to a threshold that gates but never ranks. Unreachable engines are reported, never dropped; an engine that dies mid-run discards its partial scores rather than being ranked on the utterances it survived. 25 new tests; **project gate green at 2,646 tests, exit 0**. Verified end to end with real synthesis in both languages through real subprocess engines, and **that run found a defect no unit test had**: formatting the audio path into the command template before splitting let the shell-style splitter eat the path separators on Windows, so every candidate reported a missing file for a file the harness had just written — split first, substitute per token, both directions now pinned. Return: when a local open-weights engine is actually registered and scored, or when the selection set grows a speaker axis (accent, disfluency, in-sentence language switching) that a one-language-at-a-time synthesizer cannot vary. |
| 2026-09-09 | number-formatting (amendment: the unit belongs to the branch that has a value, not to the column) | status-vocabulary | tracklight | **code** | **better** | `proof: ab-paired`, ship 1 (tracklight `4e4cad6`). **Seam chosen to falsify, and it half did**: this crate was on record as already careful about absence, and three of its four unit-bearing optional renders (`benchmarks.rs:78`, `collective.rs:293`, `compare.rs:249`) plus `schedules.rs::every` are correct **by construction**, because `Option::map` puts the unit inside the `Some` branch — so the concatenate-after-the-branch mechanism is a *format-string* failure, not a universal one, and the amendment says so. What survived is the mechanism the idiom does not protect against: with `map` you write the `None` branch by hand, and the temptation is to decorate it to match the column. `traces.rs:196` rendered an absent latency as `+—` two lines below a bare `—` for its own absent cost — arm A printed both on one row (`· @0ms +— · 0/0 tok · —`). A=2 distinct absent tokens, B=1. New test fails on A, passes on B; the pre-existing test asserted only the present branch (`@0ms +120ms`), which is the enforcement gap the amendment names. `-p lighttrack-render` 10/10, `--workspace` green except one pre-existing unrelated failure (`guidance_guard`, a projection file untracked and absent at HEAD). Not pushed. |
| 2026-09-09 | caller-differentiated-capability (amendment: subtraction holds within a surface, not within a host) | mcp-tools | pumper | **code** | **better** | `proof: ab-paired`, ship 1 (pumper `531ee02`). **Seam chosen to falsify**: the MCP feature doc asserted "an agent cannot ask its way past the operator's rail" - the amendment's claim under test. A caught outcome would have taught that the rail belongs at the engine's admission path, a stronger rule than the amendment states. **Not caught**: `clamp_budget` is private to the MCP module and `enqueue_app` takes the ceiling as an argument, so the same input through the REST door (`validate_budget_usd`) returns unclamped - and an *omitted* `budget_usd` gives the tool caller the operator's rail and the REST caller **no ceiling at all**, both doors on one unauthenticated listener. Structural fact better than the change: the tree already documents the asymmetry in its own refusal text (`routes/jobs.rs:53` sends a rejected caller to enqueue over MCP for a real ceiling), so the authors knew and only the doc's summary did not say so. REST deliberately NOT clamped - the operator is inside the trust boundary, which is the technique's own reasoning. Shipped: one paired characterization test, `mod jobs` widened to pub(crate) so it can reach the other door, doc sentence qualified. **The test was shown to fail** (perturbing the REST door to clamp turns it red) and the perturbation reverted. Gate 507 passed / 0 failed. Not pushed. |
| 2026-09-09 | ambient-selection-is-not-an-argument (new technique) | mcp-tools | politicas | **simulation** (structural) | **not-better** | `proof: structural-only`, ship 0 (`e279878`, ledger row only) - **and this is the more useful of the two rows**. Seam chosen to falsify: `live-wrap.mjs`, whose single-seat application is a live browser and whose ambient state is which page is open. **Caught, and it refuted a clause of the technique landed forty minutes earlier.** politicas already holds obligations 1-2 (the browser passes `$event.pageUrl` in; the tool never reads the current page itself) and holds obligation 3 more precisely than the technique stated it: the technique demanded an unconditional typed refusal on an absent reference; `live-wrap` refuses only when the absence could change *this* answer, computing from the source range the caller did name whether any pending edit can reach it (`manualEditMayAffectWrap`), refusing by name with a hint if so and proceeding silently if provably not. **The corpus was corrected, not the tree** - obligation 3 rewritten in the same run: the refusal is owed where the missing reference could have changed the answer, as a predicate over the real target rather than a policy about the argument. Applying the technique as written would have made this tree worse on the majority of calls where no ambient state is in play. Return: when a fleet tool surface accepts an ambient reference it reads itself rather than receives - none does today. |
| 2026-09-09 | motion-sampled-under-a-frame-budget (new technique) | visual-style-locking | pof | **experiment** | **not-better** | `proof: ab-paired`, ship 0 (`d823bffe`, ledger row only) - **the tree was ahead of the corpus and the corpus was corrected instead**. Seam: `src/lib/anim-critique/filmstrip.ts`, the fleet's only frame-sampling seam, chosen to falsify - a caught outcome would corroborate by convergence while refuting the technique's claim to be new. **Caught, and it refuted the technique's central arithmetic.** The technique treated the frame budget as the whole story (resolution = budget / span, "sample more" implied); the tree's own module comment states the sharper rule - *the sampling is part of the instrument*, and a judge scoring TIMING on a strip whose spacing the sampler made uneven is grading the sampler. Probe reimplemented the tree's index arithmetic and **asserted itself against the worked example in that file's comment before reporting** (10 of 14 -> indices 0,1,3,4,6,7,9,10,12,13, gaps 1,2 - it does). **31 count-inversion pairs** across nine ordinary capture lengths where a *smaller* cap is uniform and a larger one is not (4 of 10 = clean stride 3; 5,6,7,8,9 of 10 all mixed), so a larger sample is routinely the worse instrument. Structural fact nobody designed: an even-as-possible pick is uniform only when `(available-1) % (kept-1) == 0`, so where `available-1` is **prime the only uniform picks are 2 and all** - six of nine lengths probed, including the 14-frame capture the file names, which makes the tree's default cap of 10 the only kind of number available rather than a sloppy one. Corrected in-run per round 44's focus: the technique gained the spacing constraint, the divisibility rule, the count-inversion finding, and the declaration discipline the tree already implements (kept-of-available not a bare count; published gaps; `available`/`uniform` reported as **null rather than guessed** when the caller supplied the paths). Two decision rules became four. Return: when a fleet tree samples frames for a model *without* reporting the sampling - none does today. |
| 2026-09-09 | unknown-metric-sinks-never-flatters | keyword-metric-reliability | systedo-case | code | better | **Shipped** `cfeec5bc` on the project's active branch, not pushed. Seam chosen to falsify: the paid-search keyword adapter mapped an absent competition index to 0 while the second-platform adapter beside it documented 50 for the same absence, so two sources disagreed about what "unknown" means. A = absent -> 0 -> ease 1.0: an unknown-difficulty term scores 100 vs 80 against a reported-mid term at equal volume. B = absent -> 50: both 80; a reported "0" stays 0. Pinned by 3 tests; 15/15 with the sibling adapter and economics tests; typecheck clean on the seam. What a caught outcome would have taught: if the platform never omits the index on returned rows, B is a no-op - the fixture is the API shape the adapter already tolerated, so the arm was at risk. Return: when the Planner path is exercised live and the unknown-share of returned rows is measurable |
| 2026-09-09 | (founding wave: 180 techniques across 30 marketing subjects) | marketing bundle | systedo-case + seo-agent (source trees) | application (structural) | - | 80 source-tree applications, each `verified_against` at the witnessed version (`node@24`, `react@19`, `prompt-pipeline@2026.09.06`) with a structural fact and the deviation recorded; per intake v2 a repository run's apply step is the application against the source. The unapplied backlog is every marketing technique without a row here; the source note names the first four seams to take (`platform-conversions-are-self-attributed` on the two-network blend, the demo ad fallback's fabricated claim, the money-page rhythm checker's missing leak order, the doorway generator's missing per-area gate) |
| 2026-09-09 | motion-plate-library (amendment: built plates bank the scene) | video-assembly | gravity | **experiment** | **better** | `proof: ab-paired`, ship 0 - the code change is a **direction** (a typed move schema plus a renderer), not coverage, so it waits for a ledger row; the applied row shipped (`851abc8`, not pushed). Seam chosen to falsify, and the pre-check said what a caught outcome would teach: if the studio's six preset plates could be re-rendered from their records, the amendment's premise died. **Caught.** The spec pins a seed, a size, a frame rate and an engine, so the plates *are* reproducible and the claim as first drafted was wrong. The correction is the landing: a sampled plate is reproducible, never **editable** - "the same move a third slower" is a fresh roll, not a change - and only a built plate's scene admits the edit. Paired arm over the same six plates, instrument asserted against a known positive and a known negative first: arm A (the tree's own authored-clip predicate, non-empty string) 6/6; arm B (the technique's demand that a built blockout state a magnitude) **0/6**. Structural fact nobody designed: every element and text block is typed to four numbers and motion sits beside them as free text, and motion is also the only dimension with no renderer. Second independent reading of the file the 2026-09-07 run reached from `generated-shot-sourcing`. *Return condition*: when a typed move schema or a motion renderer exists, re-run arm B and read the verdict from the spec validator rather than from a predicate. |
| 2026-09-11 | contract-version-is-provenance | billing-revenue-normalization | kp | code | better | **Shipped** `4d373026` on `main`, not pushed. Four wire boundaries each decided the provider's dated API contract independently and all four named none, so the contract followed a Current that rotates quarterly. Seam chosen to falsify: not "does the header get sent" (a confirmation) but the control - *an unpinned gateway must send bytes identical to before*, asserted as an exact header key set. A caught outcome would have meant the retrofit moves every live deployment's contract on deploy, which would have blocked the ship and owed the technique a boundary about retrofitting pins. It held. Same test file both arms: A 0/7 predicates satisfiable (the provenance surface does not exist), B 7/7; 67/67 billing tests, typecheck green - and typecheck caught one defect the strip-only unit runner missed. **The pin itself was deliberately not made**: an unknown or removed version is a 404 on every billing call rather than a degradation, and the live version table could not be established, so recording the observed version shipped instead and is the prerequisite. Return condition: the first observed-version log line from a deployment |
| 2026-09-11 | logical-name-or-address (amendment: a versioned address binds strangers) | dependency-declaration | ascent | experiment | better | **Shipped** `05e98064` on the active branch, not pushed. The provider pins its API contract by import path; ascent reaches the client directly and through a framework adapter that writes its own specifier. Paired on one question: declared (package.json) 1 revision, resolved (lockfile) 2 revisions across 3 copies. A control family measured by the same instrument reports agreement at 1, so a clean reading is reachable and the split is a fact about the tree rather than the script. Not yet a live defect - the revisions are adjacent patch releases and package version moves independently of contract version - and saying so is the result. No gate wired: the right threshold is >1 revision carrying a *different contract selector*, which nothing in the tree can compute today. Return condition: a fleet import path gains a dated contract selector |
| 2026-09-11 | contract-version-is-provenance | billing-revenue-normalization | tracklight | simulation | unmeasurable | ship 0, deliberate. The strongest structural fact of the run and nobody designed it: `RevenueEvent` carries `fx_book_version` (`crates/core/src/revenue.rs:81`) and no field for the provider's contract version - provenance discipline applied thoroughly and stopping exactly at the boundary of what the operator controls, which is the corpus's own asymmetry standing in code. A pure webhook consumer: it makes no API calls, so the contract its parser is exposed to is chosen in *other repositories*. The field is implementable and was not added, because **no instrument in this tree would show a difference** - the tests assert against fixtures with no version in them. Instrument that would make it measurable: a normalization test over two fixtures of one event type captured either side of a rotation. Return condition: a rotation produces a second-contract delivery anywhere in the fleet; the kp change is what will make that capture possible |
| 2026-09-14 | rendered-string-extraction (walk every container shape; count units against an independent count) | copy-quality-gates | kp | code | better | **Shipped `8450dbb1`.** Seam: the catalog gate's `flatten()` stored arrays as leaves and every content check early-returned for non-strings, while the contract said the dash ban "is gated". A = shipped gate: 8,505 "keys" per locale reported, 0 dash findings, no string count. B = array-aware walk addressing `a.b[i]`, list-length parity, an independent recursive count that must match: 8,553 strings per locale, 34,212 equal across 4 locales, **4 dash findings** (en em dash plus a banned prose en dash in cs, de and fr, all in one transcript string), 0 new syntax/placeholder/list-length findings in the 62 array strings; recast to 0. 9/9 negative-control fixtures. The Director's own pre-fix measurement had looked for the em dash only and found 1 - the gate running every rule found 4. |
| 2026-09-14 | enforcement-at-the-write-seams (verify delivery by load telemetry) | copy-quality-gates | ai-registry (fleet rule delivery; kp probe) | code | better | **Shipped `84f4b6aa`.** Seam: the installer that puts always-on knowledge rules into every fleet project wrote symlinks to generated files outside each project, and its `--check` verified link targets. A = symlinks: paired headless probe with a load-telemetry hook on harness 2.1.270 - throwaway repo real file loads, out-of-project symlink does not, hard link does; kp with four rule symlinks loaded 0 of 4, every project's external-include approval flag false and never prompted, `--check` green. B = installed copies with a content-drift check: 35 entries replaced across 11 projects; kp re-probe loaded 4 of 4 and the session quoted the localization card verbatim. Skill-directory symlinks loaded in both arms. Owed: the check sits at no seam, the probe is manual, the installer's byte-exact .gitignore compare rewrites CRLF checkouts. |
| 2026-09-14 | layered-mechanical-gate + enforcement-at-the-write-seams (pre-push gate that blocks only new fingerprinted errors, skips loudly without the checker, stays out of CI-mirroring aggregates) | copy-quality-gates | kp, systedo-case, personas-web, ascent, gravitone, politicas, goat, gravitone-gcloud | code | better | **Shipped on each project's active branch, never pushed.** A = no English copy gate in the fleet: 0 of 13 trees ran a prose linter; four products held em-dash policies that contradicted each other, and the two that gated it did so for part of their copy only. B = the `native-copy` contract (US English, em dash banned, counted before declared) + fingerprint baseline + a pre-push step in each tree's existing hook system: in all 8 trees a planted em dash exited 1 naming its key and the revert exited 0. Debt recorded, not swept (operator ruling): politicas 865 baselined errors, personas-web 276, systedo-case 100, kp 78, gravitone 40, ascent 29, gravitone-gcloud 9, goat 0. Adoption surfaced six checker defects fixed the same day (`native-copy` 1.1.0 and a crash exit code): logging-call and aria-hidden text read as copy, dash-joined titles skipped (7 banned dashes unseen in three trees), catalog siblings counted as one page, baselined findings burying the one new error, a crash reported as a finding. Conditions the technique gains: a new hook system beside an existing hook installer would have silently disabled personas-web's i18n checks - extend the installer; inline shell in a lefthook job fails on Windows - call a script; a fingerprint over a whole long-form string means touching one word re-opens every baselined finding in it. |
| 2026-09-14 | reference-role-map (the reference pipeline as a whole: style anchor, invariants-only sheet, empty plate, role-mapped keyframe composed with late-admitted references, anchored motion) | image-prompt-composition | (render proof: local image and image-to-video models, no project seam) | render | better | **First `render` row (intake 2.9.0).** Operator blind pick, pair "method": the pipeline's head-anchored clip over a text-only clip of the same brief - same generator, seed, length. Discrimination 68.98 against a seed floor of 9.30 (7.4x). The text-only arm was polished generic anime with the wrong costume, an unasked crowd and no plaza; the pipeline arm held the sheet and the bible. `grader: operator`, n = 1 pair. Every stage of the pipeline was already a catch in this run, so this row corroborates rather than lands. Renders deleted after the verdict. Return: a managed project with a still-to-motion path adopts `render-triage.mjs` for its next consistency lane |
| 2026-09-14 | generated-shot-sourcing (rung 3: head-and-tail anchors cut from one cloth) | video-assembly | (render proof: local image and image-to-video models, no project seam) | render | unmeasurable | Pair "keys": head anchor only vs head + authored tail key pose, discrimination 26.54 vs 9.30 (2.85x). Operator picked head-only. **The losing arm broke the technique's own rule**: two derivations of the tail from the head (an edit; a composition with the head admitted at 0.3) both copied the head's pose, so the tail was composed from the head's recipe at a different camera angle - not one cloth - and still showed the arrow nocked. The clip reached the tail pose by mid-clip and held it (prose "releases the arrow" lost to the typed tail). Consistent with the rule; rung 3 done properly was never tested. Instrument that would measure it: a follow-through tail key cut from one cloth with the head, against head-only |

| 2026-09-14 | mount-references-across-features (mountedness is a reference count each feature acquires and releases) | render-mount-pipeline | personas | code | better | **Shipped `d0709a8f2` (+ ledger `a96e814dc`), not pushed.** Seam chosen to falsify: a virtualized trace waterfall whose rows carry expand toggles, where focus is the reader's position; the deck rail was declined because its keyboard cursor is one global listener and could only confirm. A = window alone decides existence: focused row scrolled 2,000 rows away, window moved (<200 rows created), focus on body. B = focus holds its own claim keyed by span id, rendered set = window UNION focused row: same window and bound, focus retained; release test passes. ab-paired n=1 fixture, suite 88/88, eslint and tsc clean. Structural fact: the same file had already settled one mount conflict by deleting the feature (entrance animation off on the virtualized path). |

| 2026-09-14 | capability-to-vendor-plan (no surface outside the routing layer names a vendor and gets it) — deviation D1 of pof's accepted local-first model-routing direction | model-routing | pof | task | better | **Accepted at the 2026-09-14 operator gate, executed and merged in session.** pof `e4ea8062` (decision), `e92dbfe2` (worker, isolated worktree), `579b4f2d` (ledger verdict in the project's own vocabulary), merge `0c941c02` `--no-ff`; not pushed. Measurable: runtime imports and calls of the vendor vision factories outside the provider layer, 4 lines in one route → 0. Director re-ran the gate before merging: 62/62 vision + verify-animation, typecheck exit 0. **The gate offered BUILT work as decidable for the second recorded time** — step 1 of the proposal had been on master since 2026-09-08 and only its `status:` line was stale. **Refused rather than faked:** the proposal's arena (step 2) — no local vision model is installed, so the local eye is absent from the multi-frame plan and does not declare the capability; owed with a return condition. One deliberate behaviour delta: an unconfigured default eye now re-routes to a metered rung instead of returning 502, with the elimination on the answer. |
| 2026-09-14 | budget-shapes-output-not-just-caps + import-scale-derivation + structural-scorecard (the commissioning contract, applied to agent-scripted construction) | asset-class-poly-budgeting | pof | experiment | better | **Shipped `3f1a67c3` on master, not pushed.** Seam: pof's construct-vs-generate rule routes hard-surface assets to scripted construction and promises topology on the hard edges and polygon economy, while handing the constructing agent no numbers. Seam chosen to falsify that promise, and a caught outcome would teach that construction needs the contract - it was caught. Four independent agents, same two briefs (stairs kit piece, two-handed warhammer), one harness asserted against a planted control first. A (look and feel only): 33-235 shells, 58-394 intersecting shell pairs, 4-8x the triangles, invented sizes (7-10 m tile footprint, 1.74-1.83 m hammer). B (target extent, triangle request, closed-surface rule): one closed shell each at the commissioned size, 2.3-4.2k triangles; both B agents caught their own length errors. The engine's importer matched the harness triangle count on one mesh. `proof: ab-paired`, structural only, n=2 per arm per brief. Perceptual verdict NOT reached (turntables refused by the gate twice, operator rejected the proxy-hold pair, engine capture 7 of 16). Return: a perceptual verdict in a usage context, then a registry placement for the route |
| 2026-09-14 | headless-dcc-capability-limits | mesh-finishing-for-engine-readiness | pof | experiment | better | **Shipped `3f1a67c3`.** Live GUI editor session on the engine version pof pins. A = quit after the capture call: 0 files (call returned, effect absent). B = a busy-guarded, frame-driven loop launched as the project start-up script, waiting in seconds and verifying each file: files saved. Also ground-truthed: an asset import re-enters a slate post-tick callback (a nested level change crashed the editor within 200 ms), the run-a-script launch flag exits after the script returns, two signatures. `proof: before-after`. NOT solved and recorded as such: an unattended run saved 7 of 16 captures. **Withdrawn before landing:** "headless spawn is fatal" - a third probe spawned in the default world and after a new level; the corpus application stands and the earlier crashes belong to an unisolated sequence. Return: a second automation surface re-entering a callback from inside an operation, which would promote the re-entrancy amendment |
| 2026-09-14 | anchored-model-review | copy-quality-gates | personas-web | code | unmeasurable | **Shipped `4445215`.** Seam: native-copy 1.2.0 review mode on the money pages — 165 units, 1,784 words (hero, compare, pricing, FAQ, download, footer, feature pages; the meta description; three blog posts). Checklist 72 binary questions over 65 rule IDs. Three fresh reviewers: raw 35 / 28 / 31; the veto suppressed 4, **all four the veto's own defects** hiding two findings the panel agreed on (a fix keeping an unflagged word read as a synonym swap; a keyless unit unvettable) — fixed in native-copy 1.2.1; kept 90 in 41 clusters, 28 at 2-of-3, 75% of those by all three, 31.7% of clusters single-reviewer. 11 language defects repaired in 10 units; 17 agreed findings escalated as claims or terms (10 need a fact). Checker 276 → 270 errors, new 0; baseline 601 → 596. **Condition the technique gained:** idempotence is two numbers — repair stability (0 findings on repaired text, 0 flip-backs here) and panel recall (4 new findings on untouched text) — because merged into one "nothing new" target a stable repair reads as a failed one. Unmeasurable because no human verdict or gold set exists. Return: the owner rules on tier names, the telemetry and cloud claims and the termbase; a human pass on the escalations; a gold set for the reviewers' false-positive rate |
| 2026-09-14 | prompt-context-contract | translation-pipeline-topology | personas-web | code | unmeasurable | **Shipped `b3fe23f`.** Seam: the guide translation subagent prompt. Field coverage against the contract's ten fields: A 2 present / 3 partial / 5 absent; B 5 / 2 / 3 (surface and audience added, siblings stated, plural categories derived from locale data, the action stated; glossary moved to a pointer to a new `docs/i18n/glossary.md` seeded only from lists the tree already carried, with three conflicting tier-name sets marked UNRESOLVED for the owner). Stale facts removed: "8 files" over a 10-file list with one category file unlisted (11 real), 102 topics expected against 116 (now read from the manifest), Hindi called right-to-left, one tier-name set imposed as do-not-translate, shorter-than-source fields allowed silently (now flagged with a reason). **Condition the technique gained (`ffa7d8ff`):** a file-batched prompt cannot carry per-unit occurring terms, so it attaches file-scope terms or splits at section boundaries, and records which. Unmeasurable because no translation run was scored; instrument owed: a guide sync under A and B scored for terminology and plural errors. Return: the next guide sync |
| 2026-09-14 | source-identical-value-audit + non-translatable-value-classification | translation-pipeline-topology | personas-web | code | better | **Shipped `7c05f33`** (read-only report, non-blocking, outside CI, 6/6 tests). Seam: the key-parity coverage gate that prints "xx: 100%". A = pass/fail (exit 1 on a sibling's unrelated block; three locales "100%"). B = counted identity after whole-value exclusion (15 per locale: 14 accepted terms, 1 number): identical remaining 87–93 in the seven non-Latin locales, 107–179 in the six Latin ones, 1,475 total at HEAD; raw identity reproduces the survey's 101–194. **Two conditions the techniques gained (`ffa7d8ff`):** (1) the every-locale intersection is legitimate only if every locale was sent the keys — here 66 of its 81 keys were untranslated FAQ and use-case copy from sections never dispatched; (2) "excluded values stay identical, no false positives" holds only for the pipeline's own exclusion set — against a copy gate's accepted-terms list it fired 7 times on a correctly transliterated brand, and one hit exposed a real split-sentence fragment (a Hindi postposition attached to the fragment holding the name). Return: the owner decides on a ratchet with a reviewed allowlist |
| 2026-09-14 | rig-preset-and-bone-remap-binding (preconditions on a generated mesh: welded weight solve, parts separated across bones, rigidity verified per vertex) | mesh-finishing-for-engine-readiness | pof | experiment | better | **Shipped `aff0e23d` on master, not pushed.** Seam: pof's rigging knowledge covers biped-vs-creature rigger choice and glTF seam splits before a remesher, and says nothing about whether a generated mesh can be weighted at all. Live Blender 4.2.1 rigging of one pof character mesh by an independent agent, four attempts (the fourth operator-approved over budget). A = automatic weights on the import as delivered: failed to find solution, 27,001 of 27,001 vertices unweighted. B = the same solve on a copy welded within 1 mm with weights transferred back by position: solved (6 of 19,645 unweighted), original topology and UVs kept. Measured separately: a fist-to-thigh fused strip stretched ~500 edges past 2x; a post-hoc split thinned it to 475 and moved weight damage into the axe haft; the rigger's written rigidity claim failed a per-vertex probe (733 of 765 axe vertices below 95% hand weight). `proof: ab-paired` for the weld step, structural for the rest; four animation agents then hit the same haft defect independently. Return: a second fleet mesh rigged, or a parts-generated mesh rigging clean, which would promote the upstream rule into the registry |
| 2026-09-14 | session-capability-conjunction | prompt-safety | personas | simulation | unmeasurable | Committed `cccd993f1` (applied row only, not pushed). Seam chosen to falsify: the credential proxy's scope enforcement might already cut exfiltration; it bounds destination, not direction. Structural: the only refusal-capable hook judges a request record with no execution id; connector credentials enter the CLI env at spawn; tool roster undeclared by default; shell network calls bypass the proxy. Return: execution id on the request record + tool-result leg classifier + replayed injection fixture counting refused in-scope writes under A and B |
| 2026-09-14 | motion-sampled-under-a-frame-budget (amendment: when the extractor can run instruments, the budget binds only the naming) | visual-style-locking | gravity | code | better | **Shipped `1f80ac6` on main, not pushed.** Seam chosen to falsify: the multi-frame style readback asks two readers for a closed palette category over the same eight frames. A = different categories for 6/6 sources. B = a measurement over exactly those frames (pick reproduced 6/6, rerun byte-identical) settled 4/6; the other 2 sit where the categories overlap, which is the landing's own boundary found in the tree. Sampling now declared: an even stride for 2/6. Upstream experiment on a planted-truth clip: tool-using extractors (GPT-6 Astra x2, GPT-5.6-Sol, Opus 5 x2) got 5/5 cuts in 4 runs and 11/11 colours in all 5; vision-only arms got 1/5 cuts and 0/2 holds; the engine made no difference. The selftest case caught a hue bin edge before the commit. Return: a render pair of a recipe written against the measured palette vs the readback |
| 2026-09-15 | self-conditioned-supply | media-playback | pof | simulation | unmeasurable | Committed `3a6f6d92` (applied row only, not pushed). No managed project runs a live self-conditioned producer (gravitone's conversational service is turn-based). Seam chosen to falsify the offline form of the reserved-seam rule: pof's melee combo was three separately generated clips concatenated with only root XZ re-anchored, and it exploded on retarget while each clip retargeted clean. A caught outcome (a one-take continuation with an instruction switch failing the same way) would bound the technique's splice paragraph. Case 2: a vision critic scored requested rest frames as "static holds" beside a real freeze, where the held-frame discriminator predicts a pixel-only gate cannot separate them. Case 3: gravitone discards a speculative reply when the caller resumes, where clock ownership predicts discard and the tree agrees. Return condition: ARDY reinstalled locally, producing arm B through `autoregressive_step` with a prompt switch beside the concat arm, plus a seam-continuity probe (joint velocity at each seam vs within-clip p95) |
| 2026-09-15 | reduced-motion-mechanics (amendment: a universal reset does not match snapshot-transition nodes; framework ownership removes the call-site gate) | motion | personas-web | experiment | better | Committed `cd5f97d` (applied row only, on the active branch, not pushed). Seam chosen to falsify: the tree's own reduced-motion block copied verbatim into a react@19.3.0 `<ViewTransition>` harness (Playwright 1.62.1, Chromium 151), in case a real block carries an html-level or inherited rule a synthetic reset lacks. A (verbatim) = entering card at 51,153,153 frozen at 125/250 ms, positive-control element animation collapsed. B (+ `::view-transition-group/old/new(*)` rule) = 0,128,128. Preference off: both arms identical. Not shipped because the tree has no view transitions and pins react ^19.2.8, so nothing in it can see the change. Return condition: the tree adopts `<ViewTransition>` or `@view-transition`; add the rule in the same change. |
| 2026-09-15 | relevance-floors (amendment: a floor stated in converted units inherits the conversion) | retrieval | personas | experiment | better | Committed `cf54b904b` (applied row only, not pushed). Seam chosen to falsify: the companion floor derives cos >= 0.15 from the same identity the source misapplied. Standalone crate on the lockfile pins (rusqlite 0.38.0, sqlite-vec 0.1.6): the library returns the root, so the documented maths holds; the existing kind-scan test passes on a squared index too, a known-pair pin does not. Return: the pin run under the ml feature in personas. |
| 2026-09-15 | subscription-auth-selection (amendment: when the child reads untrusted content, the strip inverts into an allowlist) | agent-cli-transport | athena-everywhere | code | better | **Shipped `1bf65ad` on main, not pushed.** Seam chosen to falsify: an allowlist could starve the CLI. Paired probe through the real transport with two planted secrets: 86 vars / 2 seen -> 22 vars / 0 seen, child starts in both; ruff, format, mypy, non-provider suite green. The local sandbox still inherits: owed as its own change. |
| 2026-09-15 | origin-validation (amendment: when the guest has no origin) | embedded-preview | personas | simulation | not-better | Committed `cf54b904b` (applied row only, not pushed). Seam chosen to falsify the rule: the report print srcdoc frame has no sandbox, and it is host-authored, escaped, script-free and needs same-origin to print. It returned the authorship discriminator the amendment states. Return: none; re-check if reports render markup. |
| 2026-09-15 | embedding-lifecycle (amendment: the role is a fifth input the stamp cannot see) | retrieval | systedo-case | simulation | unmeasurable | Committed `aab65a29` (applied row only, not pushed). Pattern RAG embeds query and library with no task type on an asymmetric model and caches on text alone; kp's symmetric CV-to-ad cosine is the control. Instrument owed: labelled queries from the three query builders over one tenant library, recall@5 per arm. |
| 2026-09-15 | foreign-harness-history-folding | prompt-assembly | personas | experiment | better | Committed `c9e9fd8bd` (applied row only, on master, not pushed). Seam chosen to falsify the awareness branch: the CLI session awareness reader keeps text blocks only and renders them into a persona's system prompt. If that block still leaked the foreign idiom, "project to text for awareness" would be wrong. Paired, qwen3.8:27b, 10 seeds per arm: A raw replay of foreign tool turns = 10/10 first responses called a tool absent from the roster; D the tree's render shape = 0/10 (shell tool first 10/10); control with roster names = 0/10. gemma4:12b = 0/10 in A, B and C. No code change: the tree already sits on the winning arm, and its skip is documented as noise, not as this reason. Return condition: awareness gains tool-call content, a second transcript adapter, or a path that resumes a task on a different engine |
| 2026-09-15 | running-total-or-increment | usage-event-ingestion | ascent | code | better | **Shipped `7dc3a545` on the active branch, not pushed.** Seam chosen to falsify: the day-bucket store could have shown the technique redundant (correct under the exporter's delta default); the session store, written specifically to avoid adding running totals, inverted it. Same two-report fixture through the real parsers and upserts: A delta = session 500 / day 1500, A cumulative = session 1500 / day 2500; B = 1500/1500 and 1500/refused-and-counted. Both stores' own tests used single-report fixtures, which cannot see the encoding. Return: a second provider lands in the same ingest (its default must be pinned), and session rows written before the commit are undercounts that need a cutover label or re-ingest |
| 2026-09-15 | direction: source-keyed-attempt-rollup | delivery-analytics | ascent | task | better | Merged `963d44e3` (branch direction/aecoach-rollup, deleted). T2: rows mixing sources 1 -> 0; unit economics folds back per repo so merged changes are counted once. Operator gate 2026-09-15 |
| 2026-09-15 | direction: care-share-contract | measurement-honesty | ascent | task | better | Merged `768c4823`. Share payload validator refuses 15/15 bad payloads; typed empty reasons replace a blanket "not shared". Not yet wired (waits for C3) |
| 2026-09-15 | direction: care-band-floor-on-sharers | people-analytics-ethics | ascent | task | better | Merged `768c4823`. T6: 3 sharers -> 2/2 others recoverable; floor on sharers (5) -> suppressed. Return: at exactly 5 the middle quartiles are real values; revisit when C4 fills bands |
| 2026-09-15 | direction: guidance-signal-census | scoring-rubrics | ascent | task | better | Merged `54d77637`. Stuffed 4001-char guidance file scores the grader max 56 vs real median 34; no signal above 85% on the 55-report layer |
| 2026-09-15 | direction: stop-rewarding-guidance-length | scoring-rubrics | ascent | task | better | Merged `54d77637`. Rubric r17 -> r18; T4 boundary +3 -> 0; mock bench replay exact level 7/10 -> 8/10, MAE 0.3 -> 0.2. Return: a live bench run (needs server + token) and the 4 stored reports that drop a level |
| 2026-09-15 | direction: mentor-intake-counter | adoption-measurement | ascent | task | better | Merged `6a57af7f`. Count-only local sensor; T1 on fixtures; real logs (read-only): 349 interactive sessions, 8,106 programmatic excluded, plan mode 0%. Return: subagent files unread, skill vs built-in slash command indistinguishable in logs |
| 2026-09-15 | direction: self-reported-count-ceiling | measurement-honesty | ascent | task | better | Merged `4a75ef85`. T5: 1,000,012 -> 15,012 invokes30d, 1 contributor clamped and named |
| 2026-09-15 | direction: prompt-egress-redactor | prompt-safety | ascent | task | better | Merged `4a75ef85`. 16 secret shapes x 6 prompt builders: 96 -> 0 reaching prompts; a JSON-escaped quote leak through a memory tool found mid-build |
| 2026-09-15 | golden-set-agreement-measurement (amendment: self-consistency is a spread only where the draws can differ) | judge-calibration-and-drift | tracklight | code | better | **Shipped `d6c0324` on main, not pushed.** Seam chosen to falsify "pinned samples never split": rubric judge samples were pinned (temperature 0, one seed) while the generation half already unpinned. Paired live, local seeded model, 4 ambiguous cases x 5 samples x 3 runs: pinned 3/12 case-runs split (one case, reproducible, stamped exact); unpinned 7/12 across 3 cases, stamped sampled. Engine 204 green, fmt, clippy. Return: stamp exact only on a verified replay. |
| 2026-09-15 | fit-and-report-on-disjoint-labels | judge-calibration-and-drift | tracklight | simulation | unmeasurable | Three real cases: the default judge chosen by spread on a 12-item set, batch-method adoption on a calibrated set, rubric revision from promoted events; one golden set per rubric does all three jobs and nothing expresses a split. Instrument owed: a second disjoint labeled round for one rubric. |
| 2026-09-15 | decompose-then-verify | grounded-answer-scoring | athena-everywhere | experiment | unmeasurable | Committed `9a97a3a` (applied row only, not pushed). The production judge prompt is byte-identical whether tool results support or contradict the reply; both scenarios' grounding criteria are scored without evidence. Instrument owed: per-turn tool-result capture in the browser leg. |
| 2026-09-15 | decompose-then-verify (direction: evidence entry point + grounding dimension kind) | grounded-answer-scoring | tracklight | task | unmeasurable | **Merged `8d1bfa5` (branch `3cf08ba`) on main, not pushed**, after the 7.7 gate accepted it. Gate green: core 291, engine 212, runner/api/store, fmt, clippy. Size crept: +1032 lines against ~350 proposed. Return: a labeled grounded golden set with an unsupported-claim stratum, for a paired kappa against the holistic dimension; runner callers still owe evidence wiring. |
| 2026-09-15 | the-decomposer-is-inside-the-instrument | grounded-answer-scoring | - | unapplied | - | No fleet project has a claim decomposer outside the just-merged tracklight kind. Return: its first labeled decomposition check. |
| 2026-09-15 | bidirectional-claim-matching | grounded-answer-scoring | - | unapplied | - | tracklight has a reference slot but no multi-claim reference case. Return: a benchmark with multi-claim references. |
| 2026-09-15 | per-source-attribution | grounded-answer-scoring | - | unapplied | - | No fleet judge received passages as separate blocks until `8d1bfa5`. Return: runner evidence wiring lands. |
| 2026-09-15 | question-blind-relevance | grounded-answer-scoring | - | unapplied | - | Seam exists (athena matrix criterion 1) but no non-model similarity instrument. Return: one exists beside that judge. |
| 2026-09-15 | gate-state-lifetimes | session-continuation | athena-everywhere | code | better | **Shipped `e106230`, not pushed.** The ticket owns the budget by design, but the meter lived per invocation, and a park or resume replays the handler. Paired test at a limit of 10: A completed at 12 spent, B fails `budget` on resume carrying 6 calls. Seam chosen to falsify: the failure-path charge could not leak there (the meter dies with the failed invocation), and the lifetime defect surfaced instead. Return: none. |
| 2026-09-15 | boundary-applied-stop | session-continuation | athena-everywhere | code | better | **Shipped `60108d0`, not pushed.** The round budget is known before the last batch is dispatched. A ran 8 tools, and the 8th result was never read. B ran 7 and answered the 8th as not executed. The seam could have falsified deferral and refined it instead: defer when a reasoning call will read the batch, refuse when none will. Return: none. |
| 2026-09-15 | host-routed-approval-round-trip | agent-cli-transport | - | unapplied | - | athena's CLI harness removes the child's native tools and denies leftover prompts, so it has nothing to route (structural confirmation of the discriminator). personas keeps native tools under skip-permissions; that change is a direction, carried by the 2026-09-15 peer study. Return: the owner's ledger row on that direction. |
| 2026-09-15 | fold-only-acknowledged-evidence | prompt-assembly | - | unapplied | - | No fleet project folds history under context pressure (per-project grep across eight trees). ascent's serializer cap is a producer cap, the stated "when not". Return: a project grows an in-turn fold. |
| 2026-09-15 | summary-evidence-gate | prompt-assembly | - | unapplied | - | No fleet project admits a model-written history summary into a standing prompt layer. athena's job summary is a final answer, not compaction. Return: a project compacts history with a model, or athena's matrix transcripts record tool-result text so an identifier gate can replay against the judge. |
| 2026-09-15 | gate-state-lifetimes (direction: resume-inherits-spend-bound) | session-continuation | personas | task | unmeasurable | **Accepted at the 7.7 gate; falsifier fired, nothing built.** A resumed spawn carries no spend bound, and the gap is real, but the live data never reaches it: 88 resumed spawns, none on a capped persona; 0 of 41 retry chains over cap. Record landed `2cef228a3` (branch `7cb83e142`). A build also needs a chain-cost query (monthly spend is the wrong number). Return: a capped persona is resumed, or a chain exceeds its cap. |
| 2026-09-15 | circuit-breakers (direction: consecutive-failure-pause) | circuit-breakers | personas | task | unmeasurable | **Accepted at the 7.7 gate; falsifier fired, nothing built.** No schedule slower than 14h had 3 consecutive failures in 90 days. The falsifier query found `persona_executions.trigger_id` empty on all 598 runs, so the existing rate monitor has never counted an outcome (coverage fix dispatched separately). Record landed `8ef682b08` (branch `15b75b55d`). Return: trigger outcomes are recorded and a 3-in-a-row streak appears. |
| 2026-09-15 | host-routed-approval-round-trip (direction: permission-round-trip-spike) | agent-cli-transport | personas | experiment | better | **Accepted at the 7.7 gate; spike only.** The installed CLI (2.1.272) documents print-mode delegation of permission prompts to a host tool. Paired T4, same prompt and model: arm A (skip-permissions) ran 1 command and wrote the marker; arm B (a deny tool on a stub server) ran 0, and the CLI listed the call as denied. Untested: tool crash, hang or malformed reply. The build needs its own decision. Return: the build's crash, timeout and malformed-reply arms show the CLI failing open, or a CLI release removes the delegation. |
| 2026-09-15 | gate-state-lifetimes (coverage: trigger outcomes the monitor never counted) | session-continuation | personas | code | better | **Branch `8d5bc36c0`, landed on master, not pushed.** Found by the consecutive-failure falsifier, whose query had to join on the input payload because `persona_executions.trigger_id` was empty on all 598 runs. Every event-bus run passed `None`, so the schedule failure monitor never counted an outcome. The fix passes the resolved trigger's id (the column is a foreign key). Paired: `trigger_outcomes_in_window` counted (0,0) before and (1,3) after. Gates: core and db tests, app-crate check, rustfmt. The event-bus loop itself is compile-checked only. A second site (`daemon/runtime.rs`) passes raw `source_id` for every source type. Return: none. |
| 2026-09-16 | lens-versioning-as-invalidation (amendment: injected context, evidence or steering) | aaa-craft-rubric-authoring | pof | experiment | better | **Seam chosen to falsify.** Read-only pass over the live verdict store with pof's own `stepContentHash`: 152 bound panel verdicts, 143 with siblings, 131 with a sibling updated since judgment. As built 0 staled, full binding 131 (92%), per-dimension (coherence weakest) 13. Drift is an upper bound (updated_at, no projection digest stored). Row `bba723dc`, not pushed. Next change filed: stamp the sibling projection digest on each verdict. Return: re-run the arms with exact drift once that column exists. |
| 2026-09-16 | compaction-horizon-breakeven | prompt-assembly | ai-registry | experiment | not-better | **Seam chosen to falsify.** The break-even rule replayed over 166 real sessions of this checkout's coding-agent harness: 8 threshold compactions at a window near 1M tokens, each repaid within 0.8–2.2 requests against 34–791 remaining; the gate declines 0 of 8 at ratio 12.5 and at ratio 3. What it refuted: that the economic test bears on threshold compaction at all. The technique now states it governs early, boundary-triggered compaction only. Return: a fleet harness that compacts at plan boundaries. |
| 2026-09-16 | summary-evidence-gate (amendment: quote-only receipts) | prompt-assembly | ai-registry | experiment | better | **Seam: the registry's own review contract, "open one cited line".** Arm A verifies 1 anchor per document by hand. Arm B, `scripts/check-anchors.mjs`, verified 63 of 63 anchors across the three new applications - after its first pass found 39 of 39 unresolvable on the director's first draft (mechanism-relative paths, line numbers from filtered reads) and 4 of 17 guessed in the source note. Floor: the gates stayed green. Briefs and the intake review step now require root-relative `path:line "quote"`. Return: none. |
| 2026-09-16 | graded-span-grounding | structured-output | ascent | experiment | better | **Seam chosen to falsify:** an admission gate where a fuzzy grade would inflate a score. The exact verifier checked raw commits and files while the prompt showed bulleted subjects and defused fences. Battery over ten bench snapshots: faithful copies of the rendered view 0/147 -> 147/147 verified; fabricated near-copies 0/267 admitted in both arms. Model arm, 11 producer runs and 13 claims: 10 -> 10 (the one live bullet copy is under the length floor). Fuzzy LCS alternative rejected: it accepted a span straddling two commits at coverage 0.8, density 0.36. Commit `84226a65` on the active branch, not pushed. Return: none. |
| 2026-09-16 | recall-passes-with-a-declared-cap (amendment: blind passes, positional merge) | llm-extracted-entity-graph | langextract (source tree) | experiment | better | **Seam chosen to falsify the first repair.** The library's own merge function against type-and-span keying and declared nesting, on four pass sets (one is the library's test fixture). Wrong outcomes 2/4, 1/4 and 0/4. Type-and-span keying kept the fixture's relabel, which is why the landed rule declares nesting and records conflicts. No fleet project runs multi-pass extraction. Return: when one does. |
| 2026-09-16 | risks-gate-not-confidence (amendment: code-detected inbound risks) | speed-to-lead-and-assisted-reply | systedo-case | code | better | **Seam chosen to falsify:** the floor, ordinary inbound messages that the pattern detector might hold. Paired over the tree's own texts with the model assumed to obey the payload: adversarial corpus auto-approved 12/12 -> 0/12; the nine in-tree customer messages 9/9 -> 9/9. What it refuted: nothing in-tree; three phrasings written for the check (forgotten password, "reply only by e-mail" cs/en) do hold, and the application says so. Unit suite 4141 pass, tsc clean. Commit `42c1f54f` on master, not pushed. Return: replay one auto channel's real traffic and read the hold rate. |
| 2026-09-16 | session-capability-conjunction | prompt-safety | personas | task | unmeasurable | Discord inbound poller: any non-bot author runs the full persona and the output is posted back to the channel (legs A, B, C in one context); plan also covers unfenced Twin draft inputs and a read-only template beside send/modify OAuth scopes. Latent: 0 personas with a Discord channel configured, 2 logged messages from 1 author. First step not taken (sibling WIP and a same-day cargo build). Return: author allowlist on a branch, gate `cargo test -p personas-desktop discord_poller`, measurable executions per unlisted-author message 1 -> 0 with the owner's reply path held. |
| 2026-09-16 | unspent-budget-is-a-defect (amendment: the substitution that makes no call) | generative-provider-routing | pof | code | better | **Seam chosen to falsify the row.** The tree routes a request to a computed path when the subject's geometry is better computed than commissioned - so if it recorded its diversions, the claim that a path substitution is structurally invisible dies on a real system. What a catch would have taught was stated first: cut the amendment's third section and rescore 2/0/2, below threshold. It did not catch. Of four observable routing outcomes the three ending in a paid call each carry the decision as structured data on the 202; the one that saves carried it as prose in an error body, and the suite asserted the spending outcomes on typed fields and the saving one on a substring. Target: machine-readable routing outcomes 3/4 -> 4/4. Floor declared first: router verdicts and refusal text unmoved, suite green - arm A failed the new assertion while the five standing tests passed (negative control), arm B 6/6, 1,693 pass / 3 skipped, tsc --noEmit clean. Commit `8affd56b` on master, not pushed. Return: price a diversion - the count landed, the saving is still unquantified and this subject has no consumption band for a computed capability. |
| 2026-09-16 | owned-push-as-its-own-kind | zero-budget-channel-planning | systedo-case | code | better | **Seam chosen to falsify.** The newsletter sits low on fit in three of the five project-type packs and the query queue can run dry before reaching it, so the defect might have been unreachable in practice - a CAUGHT outcome would have demoted the finding to a lead about taxonomies in general, and that was stated before the arms ran. It did not catch: two packs mis-deal, and in the content-site pack the newsletter is ranked second by fit, so the mis-deal takes the second-best query rather than a leftover. Target: target search queries dealt to a channel a searcher cannot arrive on, 2 -> 0. Floor declared first: no query may be lost, and the project's gate must not move - total plan rows 42 -> 42, total queries dealt 18 -> 18 (redirected, not dropped), `test:unit` 4,144 tests / 0 fail on both arms, `tsc --noEmit` clean on both. `carriesContent` was not touched; the exclusion follows from the taxonomy, which is the same by-construction property that had propagated the fold error. Commits `e18be2c6` and `a297ac6e` on master, not pushed. Return: the technique's second half is unapplied - the push cadence ceiling is still a chosen number because nothing in the tree reads what a send produced; replace it with a measured ceiling when a complaint or unsubscribe rate per list exists, and show no cap until then. |
| 2026-09-16 | indistinguishable-members-do-not-rise | invariant-placement | ascent | code | better | **Seam chosen to falsify, and it was not caught.** The tenant boundary here is that a scoped read is ANDed with the org *id*, never the *slug*; both were plain strings, the gate stated the rule in a comment and then returned both together, so one destructure handed every consumer two interchangeable values. 40 functions took the id, 181 took the slug, one resolver sat between them, neither was branded. The pair is carried and matched on rather than computed with, so a caught outcome would have narrowed the technique to behaviourally-silent pairs only. Injected the identical one-token confusion into both arms. Target: is it refused? A - typecheck exit 0 with **0 errors** and **70 of 70** route tests green, nothing in the tree could see it, because a slug matches no row and the read returns empty rather than throwing. B - **TS2345** naming the boundary. Floor declared first: the project's own outcome must not move - typecheck clean with no injection on both arms, full suite green under B at **970 files / 12,599 tests, 2 pre-existing skips**. Both held. **The affordability number is the point**: branding the door's output plus 13 consumer parameters cost 4 typecheck errors and 2 one-line edits across 221 surfaces, because a branded string is assignable *to* the primitive and not from it, so the altitude rises one consumer at a time and unmigrated callers are no worse than before. **A second finding came from the instrument, not the source**: the negative artifact was written as a test file first and stayed green with both brands deleted - that tsconfig excludes every test file from the typecheck, so it sat on no rung and was certifying its own exclusion. Renamed out of the test glob, deleting either brand now fails with TS2578; seen red at birth, then restored. Branch kept rather than the active branch, which belongs to an unrelated ADR and carried foreign WIP; built in a worktree, gate run there, not pushed. Return: when the remaining 207 surfaces are migrated, or when a rename makes the two identifiers distinguishable by name alone, re-check whether the brand is still buying anything. |
| 2026-09-16 | indistinguishable-members-do-not-rise | invariant-placement | - (source tree) | experiment | better | The mined tree is the technique's negative instance twice over, in both shapes the technique names, and neither is reachable by any test it has. Measured rather than quoted, on a standalone probe against two checker majors (the version the repo pins and the next): arm A, a function whose third and fourth parameters are transposed against the contract it is assigned to - **no error**, because parameter names are not part of structural compatibility; arm B, the same transposition with the two numbers carrying distinct nominal brands - **error**, and the compiler names the swapped pair itself. One variable, two arms, the diagnosis printed in B and absent in A. The second instance inverts measurably: a status union whose value table is cast into it member by member rejects the literal comparison against the value the table really produces and accepts the one the declaration permits and nothing produces, dead forever. **Structural fact nobody designed**: the tree's one consumer of the transposed pair swaps it back at the throw site, against a constructor whose own order is the opposite, so the rendered output has always been correct and the pair of defects is now load-bearing - repairing either end alone starts reporting positions off by a line. The only test asserting those fields covers a different code path that never touches the transposed function. That compensating pair is a shape no deliberately-broken example would produce, and it wrote the technique's central warning. Return: when either end is repaired upstream (the rescan condition on the source note names both), re-read whether the corpus rule still needs the compensating-pair clause or can cite a fixed tree instead. |
| 2026-09-16 | misconfiguration-never-reaches-a-fallback | optional-dependency-degradation | kp | code | better | **Backlog wave 1, unit c07; four independent source notes (gbrain, squid, weave-router, semantica).** Seam chosen to falsify: kp documented dropping an unknown provider id as deliberate, so a stale preference never wedges the app, and its speech-to-text allowed list is a data-residency control. A caught outcome would have been a retired id or a writer emitting ids outside the registered set; pre-checked and refuted (id lists unchanged since introduction, onboarding only writes ids the status endpoint returned). Target: of 14 misconfiguration cases, how many fail to refuse naming the variable - A 14/14 (11 served silently with zero log events, including a one-letter typo that served the paid cloud engine with no fallback recorded), B 0/14. Floor, tolerance 0: 12/12 valid configurations identical, registry tests 30/30 in both arms (positive control: B's tests against A's code fail 2/30), typecheck clean (positive control: injected error exits 2). Shipped `8cf4b738` on kp main; re-run there under kp's own unit runner, 30/30, typecheck 0 errors. The proof covers the unrecognised-name door only; the runtime-fallback and stand-in doors rest on their source notes. A voice-io application that cited the drop as good design was corrected in the same landing. Return: if a provider id is ever retired, check it was mapped by name rather than re-admitting a general drop. |
| 2026-09-16 | reading-is-not-running | supply-chain | personas | experiment | better | **Backlog wave 1, unit c30; three independent source notes (ai-engineering-coach, knip, rodin-worldgen).** personas' static-scan lane describes itself as read-only and spawns an unused-code analyzer in the project root. Replayed on an archive copy of personas HEAD with three planted marker statements, positive-controlled by direct load first. Target, markers that fire at an unapproved byte state: A (as-is) 2/3; B1 (digest over the named config files) 1/3 - **the falsifying seam refuted B1**, a marker in a local module imported by an unchanged build config still fired, which is why the landed rule hashes the local import closure; B2 (config files plus local import closure) 0/3 with one unneeded refusal (a lint rule the analyzer reads statically). Floor, tolerance 0: 1,042 finding rows set-identical across A, B1 and B2, and 0 false refusals over 2 unchanged re-runs. Structural fact: A's findings were identical with and without an executed marker, so the scan gives no signal that code ran; A also crashed loading one config and silently lost its inputs, which a static read would not. Not shipped: the product change needs an approval store, IPC and a review queue - filed as personas' next change, not a diff. Return: when personas gains a digest-approval store for executing content, re-run the three markers against it. |
| 2026-09-16 | loss-site-inventory | agent-memory | personas | code | better | **Backlog wave 1, unit c06; four source notes (future-agi, whip-coding-agent-harness, joyai-echo, openwiki-self-correcting-memory).** Seam chosen to falsify: the reviewer's long-term-memory read path, where a 4,000-character cap on a small coaching history might never fire. It half did not (0/60 states under light load) and exposed a larger unmarked loss than the sources named: a 3-note window beside a digest of notes past a folder cap of 12 leaves notes 4-12 in neither view, silently, in 204 of 240 swept states; the cap removed the digest entirely in 38 of 96 states that had one, leaving a bare ellipsis; all notes unreadable told the model its memory was empty. No stored reviews existed on this machine, so the sweep drove the real render and condense code verbatim with review sizes set to the prompt's own output limits. Target, losses named in-band with kind and count: A 0/2,059, B 2,064/2,064, 0 false notices. Floors, all held: no-loss states byte-identical; newest-note characters kept B >= A; B at most +400 characters (max +311); crate check clean; module tests 9/9. Arm B broke two floors on its first version (notice-first ordering cut the newest note sooner; wording grew +487) and was tightened with the floors unchanged, which wrote the technique's placement rule. **Not shipped:** the in-crate test binary would not launch in a fresh target directory (STATUS_ENTRYPOINT_NOT_FOUND), and personas master carries a sibling's staged change in a file this branch touches. Branch backlog/c06 kept. Return: ship when the crate tests run green on the branch and mod.rs has no foreign staged work. |
| 2026-09-16 | downstream-check-over-stage-accuracy (not landed) | structured-output | pof | code | not-better | **Backlog wave 1, unit c33; three source notes (boundary-software-factory, handy, stefan3d-free-ai-level). The rule lost.** Seam: pof's scene decomposer, where a live run listed stairs and a bush as props and the shipped fix was an upstream prompt sentence. Replayed 30 recorded replies from pof's primary vision model over pof's two real scene images. Target, non-prop rows reaching the solver per call: today's prompt with the parser unchanged 0.0; the pre-fix prompt plus a downstream word check 0.7. Floor, real props kept per call: 0.0 vs 0.5. The check caught 5 of 12 leaked scenery rows and passed 7 synonyms the list lacked, as predicted before running: a check that only re-reads the stage's own output knows less than the stage and cannot substitute for upstream accuracy. **Seam class for the next run: a re-reading check.** The unrefuted branch - a check fed information the stage never had (a real diff size, set-level statistics) - is banked unmeasurable with its instrument named; no fleet project has that seam. The replay also surfaced an unconfirmed pof lead (n=1): the shipped empty-answer sentence dropped the scene's one real prop in 10 of 10 calls. Branch backlog/c33 kept, not shipped. Return: when a project routes work by a predicted size and records the real one, measure the substitute branch. |
| 2026-09-16 | nested-run-is-not-its-parent | agent-runtime-assembly | personas | code | better | **Backlog wave 1, unit c11; three source notes (herdr, deer-flow, awesome-llm-apps). Half the rule was refuted before the other half was confirmed.** A pre-registered experiment gave a headless command-line agent child a complete fake parent identity in its environment (session id, messaging socket and token, pid, nesting marker): full inheritance and a full scrub both scored 0 effects reaching the parent, with a positive control proving the socket listener caught 2/2 test connections. The refutation branch fired, so on this runtime environment scrubbing buys nothing today, and the partial-strip arm was skipped as unable to discriminate. The same experiment found the real seam: every nested child fired the user-wide fleet hooks, and personas' hook receiver resolved an unknown session id to the most recently active row in the same directory - chosen to falsify, since a second pass that never matched a stranger would have killed the receiver half. Target, nested-run events attributed to the parent over a 5-event sequence: A 5/5 (tool events into the parent's operative memory and subagent counters; the child's Stop flips a still-working parent to Idle), B 0/5. Floor held: a clear re-exec keeps its row, a hook with no session id still falls back to the directory, a new row still binds on first start; fleet tests 273/273 under personas' own rust test runner. Shipped `a39315049` on personas master (rustfmt and gitleaks hooks ran; commit holds only the 2 files; a sibling's staged work in another file left staged). Residual risk stated: a session whose clear or resume start event was missed now has unknown-id hooks dropped instead of guessed. Side finding: this loop's own headless arms fired the operator's user-wide hooks, so before this ship any arm run beside a live fleet session was attributed to it. Return: if a dropped-hook symptom appears after an app restart mid-session, the start-event rebind needs a second trigger. |
| 2026-09-17 | waiver-names-its-finding | quality-gates | kp | experiment | better | **Backlog wave 2, unit c13; split verdict, this row is the half that held.** Two source notes (omniroute, awesome-agentic-patterns). kp had already adopted a named waiver grammar on 2026-09-05, so a code arm would only rebuild shipped work; instead all 11 historical waiver trailers, in 4 recorded pushes, were replayed through the grammar before and after, with findings from the rules at HEAD in every arm so the waiver logic was the only variable. Target: collateral waivers (findings downgraded that no trailer names) A 7, B 0; attributable waivers A 0, B 8. Floor, at most 2 intended waivers lost and each reported: 1 lost (a trailer that predates the grammar and does not parse), reported by the gate as unmatched. Positive control: a seeded diff with a secret, a focused test and a free-prose trailer - A still collaterally waived the focused test, B blocked both. A sub-claim failed its floor: requiring a linked issue on every waiver would reject all 8 legitimate matched waivers (0 of 11 carry one), so the technique scopes issue links to standing entries. Structural fact: nothing re-reads a passed range, so a route-auth finding the blanket silenced in one push is still undeclared at HEAD - banked as a residual for the owner, not shipped. Not shipped: kp already applies this. Return: when kp's gate grows a replay of history, re-run the 4 pushes. |
| 2026-09-17 | waiver-names-its-finding (coverage-removal ban, not landed) | quality-gates | kp | code | not-better | **Backlog wave 2, unit c13; the refuted half.** The source rule said a waiver may never cover a deleted test or an added skip. Arm C on branch backlog/c13 (b94f2222, not shipped) made test-deletion, test-skip, focused-test and skip-baseline findings unwaivable. Target, illegitimate coverage waivers removed: none moved - under the named grammar there were already 0, since all 7 collateral coverage waivers in A were removed by the grammar itself. Floor fell on both parts: 8 of 8 named historical coverage waivers blocked, all legitimate on reading (4 deleted tests whose subject was deleted, replacement named; 4 conditional skips with a reason), and kp's own gate test file went from 68/68 to 5 failures, every one asserting that these findings can be waived by name (positive control: a broken assertion turns the file red). **Seam class for the next run:** an absolute ban layered on a waiver grammar that already names its target. What the ban really guarded against was the blanket, so the technique keeps it as a scope rule - coverage removal waivable only by exact file name. |
| 2026-09-17 | uncontrolled-games-beside-the-win-rate | eval-harness | tracklight | code | better | **Backlog wave 2, unit c18; one uncovered residual of a four-member unit.** Three members were already owned across six files, and the unit's first half (never count a verdict from only one order) was already enforced by tracklight's judge, so the measurement went to the residual. Seam chosen to falsify: the pairwise runner, where either adding the split moving a win rate, or the seam already reporting per target, would have killed it. Target, per-candidate disclosure of excluded games and forced ties: A 0 fields - a candidate that failed to generate on 3 of 4 cases ranked first at a perfect 2-0-0 over 2 of its 8 scheduled games; B excluded (3, 3, 6) and forced ties (2, 2, 0). Floor, tolerance 0: win rates 0.500/0.300/1.000 and rank order identical; runner crate tests 197 -> 198 on the fork, clippy and fmt clean, two positive controls. Re-proven on current tracklight main with a private target dir (198 passed), then stacked with unit c04's schedule fix (202 passed, the 4 new tests accounted for). Shipped `5787612`. Harness fact: a shared cargo target on this project is overwritten by sibling builds and produced a false failure. |
| 2026-09-17 | scope-before-similarity | agent-memory | ascent | code | better | **Backlog wave 2, unit c22; two source notes (memory-lancedb-pro, supermemory).** The corpus owned the read side of ownership scoping and one sentence of the write side. Seam: ascent's reflection verb clustered memories on text similarity alone, and the apply door checked only the org, wrote every rollup shared, and took the namespace from the request. Target, scope-crossing cases accepted (three similar notes across two namespaces; shared plus private; two authors' private notes; an apply spanning namespaces; a private member in a shared rollup; one author superseding another's private notes): A 6/6, B 0/6. Floor held: same-scope grouping and supersede still work, an author's own private rollup stays private (A wrote it shared), memory suites 210 -> 219, full suite green, typecheck red under an injected error. **Director review caught an ADR breach the gate could not:** the branch added the new error to ascent's root db barrel, which ADR 2026-09-07 freezes; nothing imported it (the route matches the error by name), so the export was dropped before shipping. The rollup-compaction technique appears forged from this same file and carried its blind spot; it now points at the new technique. Shipped `bcdec50c` `15e8ea5d` `a576a035`, stacked with c19 and c04 on ascent's active branch: typecheck 0, eslint 0, 12,610 tests passed. |
| 2026-09-17 | graded-span-grounding (amendment: citation tier) | structured-output | ascent | code | better | **Backlog wave 2, unit c19; two source notes (awesome-game-security, inside-deepwiki).** The summary-compounds-loss half was already owned for agent history; the citation half was open - no file said a citation that resolves proves nothing about whether its target is primary. Seam chosen to falsify: ascent's claim verifier awards 6 points for two guidance files stating the same command. Target, a model claim citing a guidance file and its own generated projection: A awarded 6 points on an in-sync projection and on a drifted one, and the deterministic detector counted the drifted projection as agreeing although its own comment said generated projections were excluded (the code excluded only byte-identical duplicates); B awarded 0 on all three. Floor: two independently written files that agree still award 6 in both arms; full suite green, typecheck 0. Scoped to agreement: a disagreement between a source and its own projection is real evidence of drift and is still admitted. Shipped `be3780d3`. |
| 2026-09-17 | marker-certifies-what-was-done | delivery-guarantees | ascent, tracklight | code | better | **Backlog wave 2, unit c04; four source notes (awesome-game-security, find-me-a-freaking-job, openwiki-v050, cline-desktop). Both halves in code, in two projects.** The corpus ordered the marker write only for the crash case; this is the success path. Half one, ascent's skills report: after a 200 the drain set the offset to the file size at drain time while the batch was capped at 500. Target, events lost after two runs over a capped batch, lines appended during the report and a torn last line: A 13, B 0. Floor: a small spool delivered once, a failed report drains nothing, 0 duplicates, existing hooks tests 15/15; the three target tests were red on A as a positive control. Half two, tracklight's sampling schedule: a window was skipped when any dataset carried the watermark name, and datasets are created before their items and frozen last, so a failed item post or scrub left a partial dataset that captured the window forever. Target, windows wrongly skipped: A 1, B 0; a frozen dataset still skips and no match still builds, and a deliberately broken predicate turned both floor tests red. **Both trees carried comments claiming they were already safe**, and both were false on the success path. Shipped tracklight `c3563c9` and ascent `a77877d7`, each re-proven stacked on its project's current branch. |
| 2026-09-17 | falsifier-carrying-rules (not landed) | agent-instruction-files | pof | blind-ab | not-better | **Backlog wave 2, unit c14; two source notes (sozu, firstmate). Split, and nothing landed.** The rule half - cite the commits that exist only because a rule was skipped - was refuted by experiment: at the pinned commit none of the 3 cited hashes existed on any branch (the feature branch was rebased twice before merging) and the dead citation survived ten later edits; the arm given the hash list recovered 4 true positives against 3 without it, but through commit subjects and a widened diff search, not the hashes. The decision half split: its retiring observation is countable from history (a positive control recovered both pre-tripwire gaps), but whether the tripwire flips the decision is unmeasurable because it has never fired - banked with the replay instrument named. The combined draft tied its control 4/5 to 4/5 in a blind A/B at a real pof site; its one measured gain was the query still finding its own origin row. **Seam class for the next run:** an instruction-file convention a strong model already follows unprompted. Also found: a commit the source counted as the rule being broken quotes the rule while applying it. |
| 2026-09-17 | server-readiness-on-the-spawn-path (not landed) | agent-cli-transport | pof, kp | experiment | not-better | **Backlog wave 2, unit c35; two source notes (stencil-harness-playbook, find-me-a-freaking-job). The unit rule - every loaded tool costs wall clock, in boot and in generation - did not hold.** Same prompt replayed through the local agent CLI, fresh session per run, arms interleaved, correctness checked every run, target and floor declared first with four addenda each written before its arm. Roster half refuted: 30 built-in tools to 0 (about 20K prefix tokens to 2.6K) left generation at 176 vs 171 tok/s, 90 tools to 29 at 174 vs 173; the host defers server tool schemas, so 61 extra server tools added about 1.9K input tokens. Boot half refuted at the fleet seam most likely to confirm it: pof's judge spawn loads a node stdio server, a down localhost server, a plugin server and four account connectors, and stripping all of them saved 0.36 s (median 10.3 vs 10.0) against a 2 s target; kp's inherited connectors sit unauthorized in headless mode and cost nothing measurable. An apparent inversion (stripped arm slower in 5 of 6 rounds) was machine load, shown by a four-arm control that put every arm within noise after load dropped about 3x. What held was the positive control, not a target: a server whose initialize waits 8 s delayed the first request by 6.0 s loaded and quiet - banked as its own residual. A kp code arm (strict tool config on generate mode) was written and withdrawn before any arm ran: kp generates in a neutral folder that loads only the no-cost connectors, and its shim path erases an empty tool-list argument. **Seam class for the next run:** a roster-size rule on a host that defers tool schemas. Side finding outside the unit, reported to the operator: a public repo outside the fleet had a database service-role key committed in a tracked tool config. |
| 2026-09-17 | loss-site-inventory (ship) | agent-memory | personas | code | better | **Ship record for the 2026-09-16 wave-1 row, which was held.** Both holds cleared: the crate tests, which would not launch under a raw cargo test in any target dir, ran through personas' own rust test wrapper (16/16, then 52/52 after rebasing onto current master - a first re-proof silently ran the old base because the hook shim had rewritten the lockfile and the rebase refused; caught by checking the rebase's exit code), and the sibling's staged work in the shared module file was committed by its owner. personas' rustfmt pre-commit hook then refused the ship: the worker's worktree had no lefthook on PATH, so formatting never ran. Formatted the three leaf files (no out-of-line module declarations, so no sibling file was reformatted) and put the module declaration in sorted order by hand. Shipped `86487a0c8` with gitleaks and rustfmt passing; the sibling's unstaged edits in five other files untouched. |
| 2026-09-17 | existence-authority-written-last | data-access | gravitone | code | better | **Backlog wave 2, unit c23; two source notes (tigerbeetle-blog, rowboat). Split: one half held, one half was refuted.** The unit said write the referent before the reference and the authority last, as if those always agree. Seam chosen because the two halves predict opposite outcomes there: gravitone's voice ingest exported each embedding straight into the voices directory, calibrated, then registered the row. The row points at the file, so referent-before-reference calls that order correct; but the roster lists files and the engine loads voices by path, so the file is what makes a voice exist, and authority-last predicts leaks. Target, phantom voice files with no registry row across three scenarios (registry write fails after export; cancel while the export child runs ahead; a roster read during calibration): A 4, B 0. Floor, zero new failures across the 13 test modules covering ingest and voices: identical counts on both arms, plus the new write-order module. Positive controls: all three new tests fail on A at the phantom assertion, and each scenario asserts its mid-state (the injected failure fired once, the child wrote both files, the file was in place when the engine loaded it). Why nobody saw it: the rollback journal records a voice only after its row is written, so neither the failure rollback nor restart reconciliation ever saw these files, and the existing rollback test's fake export failed without writing a file. The project's other two creation paths already staged the file and moved it into place after the row; only ingest had the wrong order. The refuted half is stated as a condition rather than a slogan: the two orders are one rule only when readers find things through the reference, and the corpus already held the inverted case (secondary-index-before-primary). Unmeasured and stated as reasoning: a row briefly without its file holding an emotion slot. Fleet ship held pending the wider gate: the repo's per-module loop was 104/108 green on B, with one failure that also fails on A, one module cut off by a timeout and one that looked hung - both being run to completion on both arms. |
| 2026-09-17 | existence-authority-written-last (ship) | data-access | gravitone | code | better | **Ship record for the wave-2 c23 row, which was held for an unresolved gate.** The worker's wider run left two modules unaccounted: one cut off by a 200 s timeout, one reporting 10,805 s and looking hung. Both were run to completion on the branch AND on main: the slow one is 82 tests OK in 910 s on main and 914 s on the branch (a genuinely long module, not a hang), the suspected hang is 10 tests OK in about 5 s on both, and the one failing module fails identically on main. Green on the branch, nothing new. Shipped `1d577db` on gravitone main; the active branch had no work in the touched files. |
| 2026-09-17 | preparation-is-the-staleness-window | concurrency-guards | kp | code | better | **Backlog wave 3, unit c25; three source notes (openwiki-v050, lago, openviking), all three re-verified in their own repos and all three corrected.** Seam chosen to falsify: kp's outbound lifecycle mirror checks consent while building the record, then crosses an awaited host re-vet and a signing-secret decrypt before POSTing candidate PII to a third party - if any later gate re-read the entry, the rule would have been theatre here and the external-effect half would have been not-better. Target, three deterministic probes: A 1/3 passing (the one that passed was the control, so one leaked POST), B 3/3 with 0 leaked bodies and a terminal ledger row. Floor, tolerance 0: the ATS-adjacent suite 68/68 across ten modules on both arms, the happy path still delivering with its idempotency key unchanged, typecheck clean. Three positive controls, including a mid-state assertion inside the window (not anonymized at record-build, anonymized before the fetch resolved) so a tie could not mean nothing ran, and the falsifier verified red on arm A before the fix existed. What the seam taught that the sources did not: the gate has three answers, not two - an expiry landing inside the window makes the prepared body OVER-disclose, so the refusal is 'the verdict that produced this payload no longer holds', and refusals need a retry class (settled goes to dead-letter, transient rebuilds masked). Corrections that changed the landing: one member's own instance verifies AFTER a cheap re-doable write (a skip license), so it landed as a third placement rather than as the unit's single rule; another member omitted its source spec's inversion (where the inputs determining a lock set are already stable, compute first and lock once), without which the technique would have recommended an unlocked window; the third's idempotency key is per attempt row, not per obligation, which is what proves the re-read narrows rather than closes. Shipped kp `13914f2f`; re-run on kp main: 17/17 on the two touched modules, typecheck 0 errors. Return: revisit if a receiver is added whose dedup keys on the obligation rather than the attempt. |
| 2026-09-17 | gate-liveness (amendment: a canary enrolled in the judged population) | quality-gates | pumper | code | better | **Backlog wave 3, unit c32; split - the unit's rule was covered, the residual landed.** The unit's 'a guard can be present, registered and inert' half is COVERED four ways (swallowed-error-prevention, honest-hook-registry, trigger-matching, instrument-per-orphan-class), which also closes a standing lead in one source note that claimed the corpus had no home for it. The residual is what the population's own machinery does to an enrolled prover. Seam chosen to falsify: pumper's lane certifier already plants reds inside its own fixture suite, so if enrolling a canary needed no inversions this would have been asserting a mechanism the corpus already had. Target, canary states read correctly out of 4: A 1/4, B 4/4. The one state A got right it got right for the wrong reason (a missing artifact is cannot-see for any lane, not knowledge of canaries). The finding: **a canary that stopped catching its own planted breach read as PASS, exit 0, whole run green** - enrolling a liveness prover without inverting its reading manufactures the false green the technique exists to refuse, and adds a green row while doing it. Floor, tolerance 0: existing suite 23/23 -> 34/34, and certify/health/report output byte-identical to HEAD on a five-lane non-canary fixture set, exit codes for a population with no canary unchanged, sibling harness gates green (93 tests total). Positive controls: an inverted assertion turned the suite red then was reverted, the planted fixture was asserted to really breach its bound, and the not-caught fixture asserted not to. Shipped pumper `37f2a12`; re-run on pumper master with its own runner: 93/93. Return: when a second project enrolls a canary in a scheduled population, check whether the retention inversion is needed there too. |
| 2026-09-17 | inspection-coverage-is-a-verdict | prompt-safety | ascent | code | better | **Backlog wave 3, tension unit t01 (4 members); the clustered discriminator was covered, the residue was not.** Four candidate discriminators were declared before measuring. The clustering pass's guess (observe-only versus enforcing) is already the corpus's in four files, and it is insufficient alone: a real enforcing guard in the fleet correctly fails open (a telemetry seat that has not yet spoken to its server must never read as over budget). What survived head-to-head is that coverage is a separate output from the verdict, plus the refinement that voids the usual cost argument - the constrained party chooses how often the guard cannot decide, so an undecided state reachable on demand has no fail-open budget. Seam chosen to falsify: one field on ascent's passport surface already reported its own coverage hole while four siblings asserted from the same bounded read (a reserved file quota, a per-file byte cap, a displacing byte plan). Had any test depended on the blocker minted from unread evidence, the project would have priced it deliberately and the verdict would have been not-better. None did. Target: block-severity claims minted from evidence the snapshot does not hold, 6 -> 0 across 3 cases; unassessable info findings 0 -> 6. Floor, tolerance 0: the three fully-observed cases byte-identical in finding id, severity, level and score; suite 457/457, typecheck and lint clean; three positive controls including the new assertions verified red against the pre-change code. Shipped ascent `4d49eef2`; re-verified on the shipped commit in a clean worktree (typecheck 0 errors - the main checkout shows 4 errors from a sibling's uncommitted work in an unrelated feature). Residual reported, not fixed: the production score still prices an unread read as a deficiency (the same conflation one layer down). Not measured by this arm: the enforcing ladder (refuse, confine, escalate), which rests on corroboration. Return: when the scoring layer gains an unassessable state, re-measure whether the score still penalises coverage. |
| 2026-09-17 | unreached-decisions-pin-nothing | test-harness | kp | code | better | **Backlog wave 3, unit c16 (4 members, 3 source notes); one landing, one member covered.** Seam chosen to falsify: two kp tests named for pinning a locale decision are served from a prompt cache with a bogus interpreter command, so the value they assert is derived above the cache lookup while the argument carrying the locale to the engine sat inside the block a cache hit skips. If the mutations had been caught, the cache-hit stamp would have been a sufficient witness and the claim would have been wrong here. Target, mutations of the decision detected by kp's own runner: A 1/4, B 4/4. The one A caught was the historical defect itself; a literal substituted at the argv construction, a literal at the call site, and deleting the flag entirely were all green - re-run against the whole suite, 8091 tests, 8090 passing, the single failure a load-dependent process-tree test in an unrelated module. So the defect the file exists to prevent could be reintroduced at the crossing and kp's gate said nothing. Floor held: unmutated arm B green on the eight files naming the value (65 vs 63 tests), full suite 8092/8093 with the one failure a performance test that re-ran green alone, typecheck zero diagnostics, lint clean, production argv byte-identical asserted by deep equality against the literal it replaced. Three positive controls, including the instrument catching its own first draft (it counted 4 where the module's comments said 1). What it refuted: a negative control chosen from the code the test reaches passes this class with flying colours, which is exactly why the existing negative-control technique is blind to it. What it returned unhunted: kp already solves this correctly one module over and says so, so the repair is the project's own idiom. Shipped kp `73672577` (7/7 on the touched module after the ship). A member's source claim is backwards and is corrected in the subject note: all four of its cited instances are defences, not defects. Return: re-run the reach probe when the reasoning lane gains a spawn-capable test lane; the caller-side half is a source-text pin until then. |
| 2026-09-17 | speed-figure-names-its-endpoints | measurement-honesty | personas | code | better (endpoint half) / unmeasurable (kind half) | **Backlog wave 3, unit c17 (2 members); split, one draft.** Seam chosen to falsify: a bench publishes a column named p50 first-token whose stamp is the first streamed fragment of any kind, with a silent fallback to the first complete message. The falsifier was that every recorded stream's first fragment is a visible text fragment, which would have made the name accurate. It nearly held: 22 of 24 recordings had both arms identical. On the 2 that reasoned first, the first forwarded fragment is an empty envelope carrying zero characters, 2,055 ms before the first text (understating those runs by 19.7% and 24.1%) - **and both fires landed on the two highest-effort cells, 0 of 12 on the low-effort cells**, so the stamp's end event tracked the axis the cells are compared on. That changed the landing from label-the-metric to a figure whose end event can differ between arms cannot be compared across them, with the repair in the definition rather than in withholding the contrast. Target: stamps not taken at the first visible text fragment A 2/24 -> B 0/24; published stamps carrying their end-event class A 0/24 -> B 24/24. Floor: turn text byte-identical 24/24, totals and usage identical 24/24, new test 16/16, untouched control gate PASS, lint exit 0. **A pre-declared noise control was wrong and is reported as wrong:** the declared band (same-cell rep-to-rep spread, median 4,817 ms) sizes variation in an absolute arrival time across independent runs, not a paired within-stream difference; the correct paired null is the 22 non-thinking recordings at exactly 0 ms with zero variance. Read against the paired null the effect clears; read against the mis-declared band it does not. The kind half (a speed headline footnoted to system throughput) is **unmeasurable** here and landed as a reader-side rule on corroboration: no fleet site publishes both a throughput and a per-request latency figure from one change. Instrument named. Two fleet sightings corroborate the content, including a repo that retracted its own speedup figure because the basis did not transfer. Shipped personas `19c485e2f`. |
| 2026-09-17 | reserved-space-must-be-unusable | repo-manifest-standard | tracklight | code | better | **Backlog wave 3, tension unit t02 (3 members); the two sides landed as one rule with the discriminator stated, and the side the clustering pass called a contradiction was confirmed rather than refuted.** Discriminator declared before measuring: a reservation is worth what a writer loses by using the reserved space today. Refused at the door reserves; inert and non-conforming reserves; accepted, working and merely labelled experimental is a release with a disclaimer. Corollary: reserving by enumeration protects only names already invented, reserving by shape protects the ones nobody has invented yet. Code arm on a benchmark target field documented free-form, out of which the service had already taken two names by enumeration five weeks apart. Target, reserved names a caller can write and have read as host policy: A 2, B 0. Floor held: capability reachable, unreserved names still the caller's, zero new failures across five crates. Positive control: with the refusal stubbed out both new tests failed and the stored record carried the captured key, witnessed at the real door; a second control shows a neighbouring unreserved name still reads as the caller's. History arm for the third rung, rows read not tallied: three fleet production adoptions of a host's experimental namespace (one shipped as a cache-correctness fix and removed two weeks later), against 21 compatibility aliases in the host's own reader plus 7 deprecated renames - nine of which keep the same name after promotion, so the label bought nothing but the alias table. What the seam returned unhunted: the shape half is **no longer reachable there** - the two names live in stored documents across three backends and one already carries a boot-time migration created when the key moved once before, so late reservation is priced at one migration per key against one comparison at authoring time. That is what turned reserve-by-shape from a preference into the technique's timing rule. It also exposed a live defect the finding predicts: a number a caller writes under the recurrence key becomes a schedule row at the next boot, and paid recurring runs follow. Shipped tracklight `5503ed1`, re-proven on current main with a private target dir: core 293+, contract 24, api 461, mcp 59, runner 202, clippy clean. The repo's OpenAPI baseline was deliberately left alone (regenerating it produced a 950-line pre-existing drift the repo refreshes only at a release). Correction to a source note: it claims a corpus technique recommends the opposite marker assignment to must-ignore-unknown; both put the marker on the writer. The real delta is that the marker is a shape checkable without an owner registry, and that the unmarked remainder is the host's. |
| 2026-09-17 | ceiling-before-consent | authorization | personas | code | better | **Backlog wave 3, unit c10 (2 members); the unit's mechanism half confirmed, its provenance half refuted.** Seam chosen to falsify: personas' pairing ceremony, where a legitimate paired client could need a scope a server-owned ceiling must refuse - which would have killed the rule at the issuance stage. It did refute the unit's wording ('a scope set is never taken from the constrained caller'): the hiring bridge legitimately requests a privileged mutating scope, and a second path in the same tree takes the resource id from its caller and composes the handle itself, correctly, because the minter's own grant is the ceiling. **Provenance is not the defect; the absence of a server-owned ceiling to intersect with is.** Confirmed on the mechanism: with no ceiling, a five-scope request (including the scope that authorizes use of every stored credential and minting further handles) reached a **real minted origin-bound key**, and the project's own architecture doc asserts twice that paired keys never get it - documented, believed, reviewed and false. A route gate's comment claiming only the issuer's own path holds a marker scope was equally false: a request naming it was minted verbatim. Target, privileged or unrecognised scopes a pairing client can mint onto its own key: A 5/5 (pending view and minted key), B 0/5. Floor, tolerance 0: every scope the fleet's real clients pair with still pairs (read, build, per-persona execute), the unattended bridge still receives its marker scope because the minter adds it after the intersection, a request that drops a scope still succeeds; no new failures against a baseline re-run on the same lane (10 db and 3 engine failures pre-existing and identical, one flaky-under-load gate passing in isolation on both arms). Positive controls: the instrument was run against unchanged code first and went red with the five-scope list printed, the floor assertion passed in the same run, and the filter reported a non-zero test count rather than an empty selection. Shipped personas `9acaaaab2` rebased onto current master. **Behavioural change a legitimate caller would notice, and it is why this row names it:** an out-of-ceiling scope is now silently dropped before the approval modal (no fleet client asks for one), and the approval entry point refuses instead of minting when handed an out-of-ceiling list. The operator's own privileged minting lane is unchanged - the ceiling is per lane. **Left for the owner, not decided by measurement:** the exact membership of the pairable set. The landing includes broad execute and excludes the test marker; narrowing pairing to read plus per-persona execute is a one-line policy change the architecture doc's own example would support. Queued as its own item rather than chosen here. |
| 2026-09-17 | inherited-state-provenance | fleet-orchestration | personas | code | better | **Backlog wave 3, unit c38 (2 members); split - one member's statistic refuted, the mechanism's residual landed.** `brief-carries-the-session` models loss in one direction (the dispatcher knows, the worker does not) and its inventory of what a fresh worker gets ends with 'That is all' - an enumeration that denied too much. It never reaches state that arrives **structurally**: a retried worker placed back in its own previous attempt's directory and branch, a half-drained store, a predecessor's output. The worker sees the artifact and not its provenance, and its default reading (the tree is as the repository left it) is wrong, so the earlier attempt's work reads as stray, finished or foreign. Seam chosen to falsify: the orchestrator's retry path re-attaches a retried step to its previous attempt's worktree and records `reattached` in its own event log. It could have refuted the finding, because that same function already carries one handover fact into the brief (a base-branch mismatch, with a comment saying it must be said to the worker and not only in the envelope) - if the resume fact had reached the worker by any channel, this was a catch. It did not: a first attempt and a resumption produced **byte-identical briefs and envelopes**. Target, resume facts reaching the child's own channel as 4 assertions: A 0/4, B 4/4. Floor: the project's lane green as before (4 pre-existing failures in untouched modules, unchanged) and a first attempt's brief byte-identical to the legacy composer's output - both held. Positive controls: an existing 'the brief says X' test proves such an assertion can pass on arm A, the new tests were run on arm A first and were red, and the byte-identity test passed on both arms so a change that rewrote every brief would have been caught. The refuted half: the source's '85% of orchestrator-worker projects against 20% of single-agent ones' does not reconcile with the paper's own tables - 85% x 13 plus 78% x 9 plus 20% x 21 is 22 projects against a table counting 16, and the two delegation patterns alone exceed it. Either the prose predicate is not the table's category, and it is never stated, or the numbers are wrong (the registry already recorded the same defect in another table of that paper). The statistic must not be quoted; the mechanism survived on the fleet's evidence instead. **Harness fact for every future personas Rust gate on this machine:** its test databases are created in the SYSTEM temp dir and stale ones are swept, so two concurrent personas test runs delete each other's databases - a first full-lane run returned 236 environmental failures, and 4 with a private TMP. Set a private TMP as well as a private target dir. |
| 2026-09-17 | terms-travel-permission-does-not | job-coordination | tracklight | code | better | **Backlog wave 4, unit c01 (3 members).** The rule: the terms a queued unit will be JUDGED by are frozen at submission and travel in its record; only what can stop or narrow the work is read live at execution. Seam chosen to falsify: the benchmark gate, which deliberately reads the benchmark row's CURRENT `judge_model` and `rubric_id` when reporting a verdict about a run scored earlier - and argues for the live read in its own prose ('so a benchmark that switched judges does not inherit the previous one's calibration'). If that argument had held, the rule would have lost here. It also enumerated what it believed was 'the one condition it can actually read off the run', which is the incomplete-enumeration tell. It did not hold, and the distinction the measurement forced is the technique's central one: reading the current configuration to decide WHETHER TO RUN is correct; reading it to decide WHAT A FINISHED RUN MEANT is attribution of a result to conditions that never produced it. Target, gate verdicts attributing a run's pass/regressed to a configuration it was not scored under: A 2 (one passed, one regressed, both judged by a model the benchmark no longer names, both returned with no caveat), B 0 (both `no_baseline` with a caveat naming the drift). Floor, tolerance 0: all eight pre-existing `decide_gate` cases return identical statuses and caveats, `no_baseline`-from-absence gains no caveat, `partial` is not relabelled - held. Positive controls: the new test was red on arm A first, the runner was shown naming a failing test rather than an empty summary, a known-passing case was reported green by the same command, and a private `CARGO_TARGET_DIR` kept a sibling build from supplying the verdict. Shipped tracklight `9e12534` (466 api tests, all three gates PASS). Return when a unit's frozen terms and its live ones disagree in a direction the record cannot express. |
| 2026-09-17 | refusal-names-a-reachable-remedy | quality-gates | kp | code | better | **Backlog wave 4, unit c15 (2 members); split - one member covered, the residual landed.** Precision decides whether a finding is true; a separate property decides whether the author can act on it. The remedy is a refusal's third part, the only part a reader executes, the only part written by hand, and the only claim a gate makes that nothing verifies - because the author's own installation is the one where it works. Seam chosen to falsify: kp's pre-push hook, whose announced skip names a remedy script by path. It could have refuted the residual, because kp ALREADY enforces the covered half (`check-guidance.mjs` finding `dangling-command` catches an `npm run <x>` no manifest defines); if the residual were satisfied everywhere in practice, nothing was owed. Census over twelve repositories sharing one toolchain: of 68 runnable remedies named in gate output, every one pointing inside the shipped tree resolved and every one pointing outside it did not. Six of the seven failures were **one sentence copied into six repositories**, while each of those six declared the true location in its own manifest - so the fix is to resolve the remedy where the message is built, not to correct six strings. Target: executable-remedy references printed by kp's push gate that resolve from no root a reader has, A >=1 -> B 0; and the number any existing kp gate can detect, A 0 -> B all, with the new rule going red on a seeded bad remedy through `npm run guidance:check`. Floor, tolerance 0: every pre-existing assertion in the checker's suite still passes (40 nested assertions), `guidance:check` exit 0 on the real tree in both arms, the hook's behaviour unchanged in BOTH installation states (checker present -> `copy:check` runs and blocks on new errors; absent -> non-blocking announced skip, exit 0), eslint clean. Positive controls: a fixture naming a non-existent script was flagged and one naming a real one was not, a `.json` path was not read as `.js`, the gate was shown going red on a seeded remedy, and the new unit test failed against the un-amended checker first. An earlier instrument version produced 72 rows of prose-about-data-files noise; v3 narrowed to printed output and end-anchored the extension match. That narrowing is in the technique as its two exclusions. Shipped kp `82265890`. Return when a gate's remedy is correct in the author's installation and absent in the consumer's for a reason the manifest cannot state. |
| 2026-09-17 | stamped-instants-name-their-clock | issuance-policy-ladder | tracklight | code | better | **Backlog wave 4, unit c37 (2 members).** Every rung of an issuance ladder is a difference from *now*, and the clock that produces *now* is the one input to the composition nothing checks. Seam chosen to falsify: a key-rotation path that expresses retirement as an expiry clamped to the present. A stamped retirement is the better-engineered option on its face - one field, one code path, durable across restarts where a scheduled job is not - so if clock corrections were rare or the comparison sites monotonic, the rule bought nothing and the verdict was `not-better`. Target, rotations surviving an 8-day backward clock step: A 0 of 3, B 1 of 3; outcomes that are a function of the stamping clock: A 3 of 3, B 2 of 3. Decision rule declared before the arms: B >= A+1 on the first. Met. **Both of the worker's arm-A predictions were optimistic in the same direction, and that is the finding**: the clock reaches into a third path whose deadline reads as operator-chosen and is `min(old_expiry, now + grace)`, so a skewed clock moves it too. The technique's 'a deadline clamped into the past is not a short window, it is a retirement' sentence exists because the measurement produced it, not because it was predicted. Floor: 461 pre-existing api tests green on both arms, tolerance 0. Positive controls: the instrument went red on arm A with the counts in the panic messages; mutating the revocation back to an expiry stamped at `now` returned the arm-A numbers and red again, so the green came from the rule and not the harness; every figure quoted is the test binary's own `test result:` line. Shipped tracklight `a6c814e` after a rebase past two intervening commits, re-proven on the new base: 466 api tests, all three project gates PASS. |
| 2026-09-17 | parallel-dispatch (amendment) | fleet-orchestration | kp | code | better | **Backlog wave 4, unit c27 (2 members); split - the depth half is covered, the residual landed as an amendment.** The slot cap counts a **stock** and releases it on every confirmed stop, so it cannot also be the run's budget: a run that plans, issues a batch inside the cap, waits, plans again and issues another spends without bound while breaching nothing. The degenerate case is what settles it and it is in the landing: **a loop that dispatches one worker at a time, sequentially, can never reach a ceiling of any size, so no ceiling bounds it.** The subject's own 'a broadcast of forty against a cap of eight is a rolling wave by construction' is the same sentence read from the other end - the rolling wave is admission the cap declines to count. The run total is reserved at admission rather than booked at completion (a total read from settled work admits one cap's worth of extra sessions before the first returns to be counted, and every individual admission was legal). **The overflow rule inverts, which is why this is an amendment and not a restatement**: the cap queues what it refuses, a run total cannot - the queue would hold the run's own excess and the run would never end - so its overflow defers to the next run and is counted. Deferral is safe only under a condition the cap never needs: the admitted work must leave the work list, or the same prefix is re-admitted forever while every run reports a full, successful budget. Measured on a scoring sweep: 12 paid forks became 4, with the tail recoverable across passes; shipped kp `bb5b74f1` after a rebase, 8102 unit tests green and `tsc --noEmit` exit 0. Return when a door serves independent arrivals rather than a caller that authors its own next batch - the total there is ceremony that cannot fire. |
| 2026-09-17 | admit-on-the-expanded-cost | admission-queue | pof | code | better | **Backlog wave 4, unit c21 (2 members), both halves better.** A door that bounds transferred bytes has bounded the transfer. The subject already answered expansion with a multiplier folded into the budget; that holds while the multiplier is a property of the system and **inverts the moment the arrival supplies it**, because then no fraction of the budget is small enough. Seam chosen to falsify: if the cost dimension were unreadable without paying the expansion, or if one legitimate delivery were refused, the existing exemption covered the case and nothing was owed. It did not fire, and returned three things it was not hunting. **Bytes do not even ORDER deliveries by cost**: 615 KB carrying 30,700 units beside 3.1 MB carrying 4,867, a 34.5x spread over sixty real deliveries - a byte cap is not a coarse proxy for the real bound, it is uncorrelated with it. The project already owned the cost number and enforced it *after* the expensive load. And the overflow half is language-bound: the seam's language saturates to infinity and still refuses, so it was run in fixed width, where the product form admitted 256x its budget on two dimension pairs that division and a wider type both refused - which is what proves the difference was the wrap and not the policy. Target: cost-bomb refusals at the door A 0 of 4 -> B 4 of 4. Floor, tolerance 0: all 60 real deliveries still admitted with 0 unreadable, the existing seam suite 14 green, the whole module area 1,190 green, typecheck and lint clean. Positive controls: the new test file was red 5 of 7 on arm A first; budget boundary admitted at the budget and refused at budget+1; the parser was checked from a different layer than itself (34 of 60 files land at the project's own authored budget, a number the parser knows nothing about); the overflow arm carried a wider-type control and an honest non-wrapping bomb, both refused. **A gate that did not go green, stated rather than absorbed:** the project's full suite is red at HEAD, 10 failures in 4 files, identical on pristine HEAD and on arm B (9,507 -> 9,515, the +8 being the new tests). Pre-existing. Shipped pof `a27ddd2d`. The technique's third state - a cost not measurable at the door but DECLARED there - is the part the corpus's own two-way enumeration had no room for. |
| 2026-09-17 | schedule-parity-by-realized-cut | windowed-inference-over-oversized-inputs | gravitone | code | better | **Backlog wave 4, unit c24 (2 members); split - the unit's rule as written is covered, the residual landed.** The covering instruments all assume the two paths are co-resident: one shared shape, one conformance test, one interface. An offline path that has already finished and an online path that runs per request cannot be. There is no observable, the two paths start from different representations so a shared *function* is impossible in principle, and - this is what the seam caught - **importing the one authority fails when the authority is stated as a policy.** Seam chosen to falsify: two functions twelve hundred lines apart naming the same clip, one enumerating its own completeness and excluding the segmentation because 'it is derived from the text', the other folding a segmentation version into a durable digest because 'seams are audible'. Both sentences written down, both confident, contradicting each other. Two assertions that pull in opposite directions had to hold on one arm. **Arm B1 IS the unit's rule applied literally - share the policy - and it scored exactly what doing nothing scored (1 of 2), moving the failure from one assertion to the other.** The policy is too coarse (two identical policy strings cut a ragged input differently) and too fine (one policy change renames every result, including the ones whose seams never moved). B2, keyed on the realized cut, scored 2 of 2. Floor, tolerance 0: 109 modules run one per run, 103 green and 6 non-green - all six environmental, re-run on the arm-A baseline with identical results and identical causes (two need an absent optional dependency, verified by asking the import system rather than by reading the error). Cache hits and determinism green. At the topology the product ships, the new key component reduces to the body's own length, so the correction bites only where the defect can fire. **The positive control found the real gap:** dropping the text from the cache key entirely left the determinism module green at 14 of 14 - the one module that names the key asserts voice separation and never text separation. The floor is real but not where a reader would look for it. Reported to the owner, not repaired. Shipped gravitone `1a04e2c`. |
| 2026-09-17 | own-writes-are-not-triggers | convergence-loop-and-requeue | pof | code | better | **Backlog wave 4, unit c26 (2 members); split - the loop half is covered for a job whose medium carries an author, the residual landed.** A convergence pass reads a space and writes to a space and the two are routinely the same one. Two of the subject's own properties look like they cover this and do not: **convergent idempotence is written about the WORLD**, so a pass that changed nothing outside and still stamped a last-observed field has satisfied it and produced a trigger; and **coalescing cannot remove a trigger** - a self-trigger arrives after the pass completed and finds no twin to collapse into. A third property makes it a trigger-layer problem: the subject reduces every arrival to a bare key *before* the queue, which is the deletion of the only field that could say 'this one was mine'. So the exclusion runs at the boundary or it cannot run at all. Seam chosen to falsify: the watcher already carries a debounce whose own comment names the agent's writes as the thing being coalesced. If debounce plus change-type detection collapsed a self-write to nothing, the corpus's existing coalescing discipline sufficed and nothing was owed. **Arm A returned 2 echoes** - the debounce merged the two writes into one batch and removed neither. Target: own emitted files reported as source changes, A 2 -> B 0. Floor, tolerance 0: foreign-origin events still delivered 2 of 2; an unclaimed hand-authored file still emits; a later foreign edit to a path the door had written still emits. Positive controls: the same test file byte-identical fails on A and passes on B; a claimed path whose bytes differ from the claim still emits, so it is not a path mute. Remedy is the write-door claim rather than an origin filter, because a filesystem records that bytes changed and nothing about who changed them - matched on content, consumed once, expiring with a reaper. One instrument note that cost an arm: a dynamic import used as a capability probe is resolved statically by the project's bundler, making the shared test file unloadable on arm A; the control was moved out before either arm ran and the amendment is dated in the declaration. Shipped pof `c1b006ad` after a rebase past a sibling landing, re-proven at 1,197 passed. |
| 2026-09-17 | causes-beside-the-finding-count | codebase-scanning | politicas | code | better | **Backlog wave 4, unit c29 (2 members); the predicate half better, the DIRECTION half refuted by measurement.** Identity dedup asks *is this the same finding?* and is built so the answer is no whenever the rule, the site or the matched content differs. That is correct and it is one axis. The report's total runs on a second axis the key cannot see: three correct findings, one remediation. Seam chosen to falsify: one report in the lane already printed the rule ('2 bill-level relations explain 9 candidate pairs that the pair-by-pair method would have read as independent findings') while its sibling consumer absorbed one row of that result as a hard-coded pair of identifiers - the asymmetry the unit predicted. If mechanical attribution over the real backlog had moved nothing, the constant was complete and the landing became 'a hand-maintained exclusion suffices for a finite backlog'. **It refuted the unit's stated direction.** The shipped number, 76, was wrong in *both* directions at once: it presented 76 rows as 76 independent findings, and it had already deleted the seven rows of the one cause it knew about, so that fact was counted nowhere. Mechanical attribution RAISES the count to 77. A collapse-by-cause does not reliably lower a total - subtraction is not attribution, and the direction of the gap is the diagnosis rather than the correction. That is now the technique's central section instead of a prediction. Target: rows with no cause attribution A 76 -> B 0. Floor: 83 of 83 rows retained with tolerance 0 lost (arm A had kept only 76), the (scope, pair) set asserted identical, the project's own gate green - 222 files, 3,048 tests, 0 failed; typecheck 0 errors; lint clean; the doc-sync hook satisfied. Positive controls: arm A's reconstruction reproduces the backlog number the committed artifact actually shipped (76), an empty register makes arm B report 83 causes and 0 explained so the collapse is visibly switchable, and an inverted assertion went red. Over-collapse risk found and bounded: one artifact appears against four others across four scopes, and collapsing on the shared participant would have destroyed four real findings. Only the stated, computed relation separates the cases - that is the technique's boundary section. **Member 4-076's statistic is not corroborated by its own source note and is not quoted in the landing**; the shape is stated instead. Shipped politicas `d73126e`. **Do not run the formatter in that repo** - it has no config and is not a dependency, and it reformatted three files to an 80-column default against a ~120-column style. |
| 2026-09-17 | release-level-by-reader-reach | release-pipeline | kp | code | better | **Backlog wave 5, unit t03 - the first TENSION dispatched, and it resolves without picking a side.** Two sources disagreed: one held that additive changes take a patch because new features pose no risk to existing code, the other that even a new optional field is breaking within a frozen contract. The discriminator is not a property of the change, it is two properties of the READER. **'Additive' names a new NAME, which only a consumer that writes that name can reach, and a new MEMBER of an existing shape, which arrives through a channel every existing consumer already reads - opposite blast radii under one word, which is why the argument never converges.** Then: what does an existing reader do with a member it has never heard of? It cannot exist (a derived surface, reached at build time), it falls into a branch that acts (reached at run time, silently wrong, no gate), or it is ignored. The level is the level of the loudest reader class present. Seam chosen to falsify: one contract read by BOTH binding modes in the same tree - an exhaustive dispatch closed by a never-typed assertion, and a nested ternary whose last branch is a real label. Six arms. A new exported function nobody imports: 0 errors, 29/29, zero of 15 inert readers reached, so the lowest level is genuinely right and **the blast-radius premise is sound**. A fifth union member emitted by the producer: an immediate hard build break on the derived reader. Same word, same release, opposite outcomes. **The ordering half is refuted**: the repair produced only a test failure while the member addition produced a compile failure - the addition was the louder change, not the quieter one. And the acting reader rendered the new member under an unrelated label with the whole project typechecking clean at exit 0. Floor, tolerance 0: typecheck 0 errors, 29 -> 31 tests, eslint clean, both known keys still resolving to their own labels. Positive controls: a planted type error was reported in the file under test; the probe extracted the panel's REAL ternary by brace-matching on every run and aborts loudly if the anchors move; both known keys round-tripped. Negative control on the floor: deleting one handler from the exhaustive dispatch reddened it, so the guard is load-bearing. **A claim in the tree is wrong and the measurement shows it**: a comment credits the exhaustive dispatch with preventing the panel's silent fall-through, citing a dated bug. It does not - it guarded one of two readers of the same vocabulary. Shipped kp `545cad4a`. |
| 2026-09-17 | command-audit-by-position | mcp-tools | personas | code | better | **Backlog wave 5, unit 1-059. A verified command-execution bypass, found by the falsifying seam.** A character denylist over a command string is a proxy for a parse: what decides whether a token executes is WHERE it sits, and a scan over the text cannot see position. Seam chosen to falsify: the host's own gate for user-configured server launch commands - maximally pattern-based, a 16-character metacharacter denylist over the whole string plus a runner allow-list on the first token. The rule predicts it is wrong in both directions; wrong in neither would have refuted the rule. **Confirmed with commands actually executed on this machine**: an allow-listed runner's own code-string flag hands its operand to a shell, so a single bare word runs an arbitrary program with ZERO metacharacters for the scan to see. Same class, all admitted by the live gate across four other runtimes and a container entry point. And the `=`-joined form of the one flag the URL scan exists to catch walked straight through it, because the scan is a prefix test over each whole token while the flag name comes first - and the identical bytes in value position are the case the scan was built to permit. Corpus: 41 commands, every one from live usage - five real config files, a desktop config, fleet scripts, hook commands, the gate's own tests, and two terminal runs. 21 must-allow / 20 must-catch, labelled by syntactic role before any arm ran. Target 10/20 refused -> **20/20**, floor 19/21 real commands admitted -> **20/21**, with all 6 live-config rows admitted in every arm. **The target rose while the floor rose**, which is what proves the catch was not bought by over-refusing. **The rule's literal form lost.** Deleting the character set - which is what 'pattern matching cannot make that distinction' prescribes - gave the best floor of all three arms and re-opened all three transport-class holes, two of which an executed experiment recorded in that very file had already demonstrated. So position is a property of A PARSE, and a transport that re-parses does not inherit it; the character rule is demoted to a fact about the executor and kept for those transports, not deleted. The positional model also failed once on its own terms: with one runtime's verb missing from the entry-spec table, the audit shifted one position left and admitted a remote code reference the denylist had caught. An incomplete position model fails exactly like a denylist, and that is now a named section. Positive control: a should-panic assertion proves the corpus can go red against the shipped gate in the same run that reports the verdict. Negative controls: emptying the code-string tables reddens the target; an always-refuse function reddens the floor at 0 vs 19. **Gates that could not run, stated rather than absorbed:** clippy was not run (a second full cold profile build); the worktree hook shim rewrote the lockfile and installed a partial dependency tree, both reverted and never committed. One clause of the rule - heredoc bodies are data - has no expression at this seam and is untested. |
| 2026-09-17 | evidence-outranks-a-liveness-claim | fleet-orchestration | ai-registry | code | better | **Backlog wave 5, unit 6-001; split - the derive-from-records half better, the 'carry no status field at all' half refuted.** A stored liveness claim may never outrank a record the work itself wrote, and an expired staleness budget is an *unknown* rather than a death. **The measurement found a live defect in this registry's own coordination board and the fix is the code arm.** A run reports at transitions and the long silences are the work, so the moment its report looks oldest is the moment it is deepest in the work - which is exactly when it takes the commit lock. Replayed: `lock` broke a lock taken one second earlier on heartbeat age alone, and `check` told the same sibling the holder's paths were `clear`, in the same second. A 2026-09-03 guard for precisely this was never reached, because the reclaim predicate short-circuited on the first term. Eight scenarios, 12 assertions, ground truth the physical fact no arm can read. Verbatim board 0 of 7 on the assertions where its stored claim is false; the rule applied literally 4 of 7 but then LOST two it had been getting right and broke the floor - with no budget and no probe nothing can ever say *dead*, so a crashed entry is outstanding forever and reclaim never terminates. Its total was indistinguishable from a deliberately broken control that answered 'clear' to everything. **Removing the claim is not the rule; demoting it is.** The narrow crash the technique concedes: a process that dies between writing its entry and doing anything else leaves one record and no evidence - all three designs are wrong identically, and only the budget clears it. Shipped as `931d1c0f` with 16 assertions, two of which carry a replica of the old predicate and require it to answer the opposite, because a regression test whose subject is a deleted branch has no positive control otherwise. Both directions asserted on the same run: a fix that made everything live fails the floor rows, one that made nothing live fails the regression row. Reclaim now terminates SOONER - through the ttl branch at 900s rather than the 45-minute window. Two instrument bugs the controls caught first: the board prints its break warning on stderr and exits 0, so a stdout-only capture made the break invisible; and a shared reset across arms let the first arm acquire the broken lock, making the later arms read as deaf. |
| 2026-09-17 | decidable-in-a-window | quality-gates | tracklight | code | better | **Backlog wave 5, unit 7-001 - dispatched BECAUSE it was marked refuted, and the refutation stands.** `prose-rule-drift` heads a section 'the discriminator is artifact versus intent' and states that rules about artifacts a parser can see are mechanised, **full stop**. That is false, and the file's own text already knows why: it says elsewhere that most gates read the tree and *that is what makes them cheap*, then attributes the sort to artifact shape instead of cost. Both directions re-verified before measuring: the corpus does say what the row says (quoted), and the row reconciles with its primary source including its figure - unlike wave 4's, which did not. Artifact held constant so both guards judge the same population, with only the decision procedure's reach varying. Window scan: 68 lines, 1 pass, 0.15s, **6 of 6 exact on precision with zero collateral**. Reachability guard: 163 lines (2.4x, inside the declared bound), 4 iterations, 0.37s - and **1 of 10 on precision**, because it keys on bare names and resolves every name to every definition, marking 3,273 of 3,959 functions 'reachable from a handler'. 79.5% of its findings sit in a suite whose only callers are three integration tests. It computes the name-sharing closure and calls it reachability. Negative control on the floor: deleting the traversal cut findings 161 -> 21 and lost a genuine 2-hop positive, so the floor is real - **but 12 of the 21 survivors are the same false positives**, so the part that looks like analysis can be removed without removing the noise. The arm that armed the prescribed repair: the corpus's own discriminator predicts 'mechanised' for all 14 conventions in the tree's standing documents and is wrong on seven; the row's test predicts what a scan can decide 14 of 14. Not one guard in the tree computes a graph. The case that looked like a refutation confirmed it instead: a guard for a genuine whole-program dataflow property does not compute it - it pins a two-file allowlist and greps one literal assembled at runtime, and its author knew, because the test asserts the allowlisted files still exist since 'a guard that passes because its subject moved away is worse than no guard'. That is now the technique's mitigation. **Nothing shipped to the project's main and that is the right outcome**: both guards are measurement instruments, neither earned a place in the gate set, and wiring an unproven guard into a blocking lane is the failure the subject exists to prevent. Branch `backlog/7-001` at `79bbc3f` keeps them. The correction the row owes the tree: three local rules there have no guard either, and one is drifting now - a stated ~300-line file cap with 159 of 592 files over it, largest at 9.7x, and no checker reading a file's length. |
| 2026-09-17 | addresses-before-compression | agent-memory | (none) | experiment | better | **Backlog wave 5, unit 7-002; the claim's null replicates and its stated CAUSE is refuted.** The claim was that consolidation bought no accuracy at about 50 sessions *because the reader could still hold the store*. That logic predicts consolidation pays once the reader cannot. Measured at read budgets of a quarter and a tenth of the store, it did not pay - it **lost 2.0 and 4.0 points**, and a length-budgeted merge lost 4.7 and 5.0. Size and fit are not what '50 sessions' was a proxy for, so the rule as written must not land. **A no-fact-loss merge does not compress.** 28 items to 10 is a 64% cut in items and a **7% rise in words**; only 2 of 10 families shrank at all, and restricted to families of three or more they grew 4.1% - which bounds `rollup-compaction`'s 'three is the smallest size where compression genuinely wins', now amended to say it is a rule about when a summary is worth writing. The finding is a sign flip on one variable: withhold the store's always-loaded index and the SAME merge, same cap, same questions goes from -2.0 to **+3.6**, with non-overlapping ranges both sides. What a merging pass spends is **addresses**, not words. Instrument: 22 questions whose answer keys were verified against real trees BEFORE the questions were written, graded by deterministic substring match against a frozen key, no judge model, three strata pulling opposite ways (recall, discrimination needing two items held apart, and abstain where the correct answer is 'not in store'). Controls: an oracle arm scored 21 of 22 so the questions are answerable and the grader works; a no-store arm scored 4 of 22 with **0 of 14 on recall**, so no answer comes from priors and the gate can go red. Abstain held at 4 of 4 in every cell. Corroborates untriaged item 7 of the same note as a side effect: at the 25% budget the merged store's retrieved bodies carried 11 of 14 answer keys against the unmerged store's 8, and scored LOWER. A coverage-only instrument would have reported a win. Independently replicated at year scale by an existing harness in the fleet - 365 simulated days, 3,571 events: verbatim beat every distilling arm, and the distilling arms held 3 and 4 stale answers against verbatim's 16. **Limitation stated:** the full-read cells saturated at 22/22, so the whole-store null is a ceiling result that can show a loss and not a gain; every informative comparison here is budgeted or decomposed. Why not `code`: the one project with a real consolidation seam has 14 tests in that module and zero answer-against-known-answer assertions anywhere in its tree - arm B could ship and every test would stay green. A gate nothing can fail is not a gate. |
| 2026-09-17 | path-class-confounded-with-size | delivery-analytics | (none) | experiment | not-better | **Backlog wave 5, unit c12 - four independent sources, and the load-bearing half of all four is refuted.** They converge on routing review depth by the paths a change touches, with security globs forcing the top tier regardless of size. They do not reduce to one blog post - four artifacts, four authors, four domains, no shared citation. But **they converge on a DESIGN, not on a result**: not one reports any measurement that the routing worked, and there is a common upstream that explains the convergence without validating it, since path-based required review has been a platform default for a decade. The unit's weight is four for 'practitioners build this' and zero for 'it predicts risk'. 6,299 first-parent changes on two trees, repair linked by shared file in a 14-day window with hub files excluded. Unconditionally the table stratifies well - 3.20x and 2.19x top-to-bottom at p <= 0.001. **Size-matched against other code, the union of the genuine security globs came in BELOW what its own size mix predicts on both trees** (1.14 against a null centre of 1.25, and 0.91 against 1.02), and the best-powered single glob returned 1.12 against 1.24. At equal review budget the table lost to sorting by files touched: 16.2% of budget bought 24.7% of harm where size bought 27.9%. The bottom class is the wrong comparator and using it is what makes a weak taxonomy look strong: a bottom class of docs, tests, config and assets always shows a large gap, because the finding underneath it is that code is riskier than prose. **The null is POWERED, which had to be shown separately:** a planted class with the same n and the same size distribution carrying a real 1.15x lift turned the same size-matched test red 20 of 20 trials, and at 1.3x red 20 of 20 even at the smaller tree's n=58. Two globs did carry signal size-matched and neither is a security glob - a migration directory and a long-lived-session module. **What predicted repair was irreversible state and long-lived state, categories the security-review vocabulary these sources use does not contain.** One of the two was also a glob false positive, matching a build-session subsystem rather than authentication. The half that HELD (lesson 11's opposing assertion): a non-code auto-approve tier waves through 21.3% and 16.8% of changes while releasing only 7.6% and 4.6% of harm. **An auto-approve tier drawn on non-code paths is defensible from history; a top tier drawn on security paths is not** - the opposite of the emphasis these sources are written with. Qualified: in the smallest size bands the non-code class out-repaired the top class, so it is a budget decision and not a safety proof. Two disclosed instrument corrections: a first strict overlap label killed the positive control because overlap fraction is anti-correlated with commit size by construction; and reading the matched rows showed the raw label was a co-location signal, which is why hub exclusion was added from the data rather than from theory. Member 8-049 is mis-clustered (severity of a finding, not depth of a review, and its second half negates the unit's rule) and 9-002's row promotes its source's design rationale to an observation. Nothing shipped: no gate change can re-run the past's defects, so the rule's falsifiable core is predictive and history is the only place ground truth exists. |
| 2026-09-17 | cross-context-promotion | agent-memory | (none) | experiment | better | **Backlog wave 5, unit 1-101; the mechanism better, the headline refuted.** The claim: a personal skill performed about as well as a borrowed one, a generic one built from many beat both, and personalization looked promising only where a preference recurred. **The figure does not exist.** The source was fetched: no scores, no n, no benchmark, and its own caveat that the experiments used a simulator. Worse, **the relay inverts its primary** - the paper it links (206 real sessions, 13 developers) reports other-user skills UNDERPERFORMING personalization, which the listicle flattened into 'about as well as'. So the mechanism was measured and no figure quoted. The corpus note owes a correction, and its 'LLM-simulated developer' caveat also overshoots: the histories were real, only the future-task evaluation was simulated. Seam: 32 real per-context stores, 830 items, 428,759 words of body and 23,508 words of always-loaded index - the strongest personalization case on the machine, since this wave's own worker brief is assembled out of it, so a win for personalization was `not-better` and it half was. It also closes a return condition the corpus wrote for itself on 2026-09-02: 'the index passing roughly fifty entries'. It fired sixteen times over and nothing was watching. Retrieved through a real product's unmodified ranker, so no scorer of the worker's is in the loop; queries mechanical (every two-term pair from the later item's own filename with its project's tokens removed, 57 pairs over 12 hand-confirmed re-learning events). Local store 1 of 12 coverage, 11 of 12 locality. A size-matched OTHER context's store: 2 of 12 - **personal and borrowed are indistinguishable once size is held**, and the declared arm that picked the densest other store turned a size effect into an apparent provenance effect. All 32 pooled: 6 of 12. **Pooled and gated on cross-context recurrence - 15% of the items - 9 of 12, beating the pool it was cut from.** **The literal rule is catastrophic as stated** (lesson 9): the gated tier retained 0 of 8 singleton probes that the local store kept all of. 707 of 830 items exist nowhere else. So recurrence across contexts is an **admission bar for a shared tier and never a retention bar for a local one** - and only the two-tier store passes both axes, at 15-27% of the pooled size. Controls: 20 of 20 self-retrieval, a nonsense probe returning zero rows, and a floor control that stripped the five labelled siblings of the worst case and took it from 5 hit pairs to 0. A similarity pass's largest component - 30 items across 11 contexts - was rejected on READING as a topic rather than a lesson. Boundary condition found: 3 of 12 confirmed families were unreachable from every probe in every arm because two contexts named one defect in disjoint vocabulary, so the cross-context count is a **floor on recurrence, never a census**. The worst case was learned separately in five contexts over two and a half months. And the store has already converged to contradiction in two places - one surface recorded POST-only in one lane and read-capable in three others - which is the shape of a self-written per-context store with no shared tier. |
| 2026-09-17 | (none - premise refuted) | cost-metering | (none) | experiment | not-better | **Backlog wave 6, unit 1-043. The falsifier fired on the premise, so nothing lands.** The claim: an expensive-processing switch is set per input PART, so one request mixes modes across its parts and its cost is not characterized by a single mode. True of the vendor's API and **false of this fleet** - the rule has no consumer here. Census over 13 fleet projects and 26,122 tracked source files, with the target declared first (at least one site where two parts of one live request can carry different cost modes): **0**. Four instruments - 318 literal element-array sites of which 195 carry a non-content element key, dynamically-built parts, map-built parts, and the one part type that actually takes the switch, which appears in 0 of 13 projects. **The positive control is why this number is trustworthy, and it caught the instrument dead.** The census had to independently re-find three known multi-element request sites. First run: **0 of 3** - the scanner counted the outer bracket into brace depth and never reached an element key at all. After the fix, 3 of 3 with file:line. Without that control this unit would have returned a confident zero from a scanner that saw nothing. A negative control planted a synthetic per-part mode on a real call site and the classifier named it. A blind-spot control earned its place: the literal-array instrument missed a real multi-part request that the dynamic-build instrument found. **The fleet's one multi-part media request refutes the premise on the only candidate it has**: N clips become 2N+1 parts in one array, and its cost lever is set once at REQUEST level, applying to every part - the vendor's own design. **And the unit's prescribed repair is already live in that tree** (lesson 9): a sibling path labels a whole batch on the cheap model, escalates only the low-confidence items to the expensive one, and records the model per item plus an escalation state. Arming the rule literally would have changed nothing, because the tree got there first. A real defect found in passing and **already covered by two existing techniques** rather than a corpus gap: that same path charges every call through one counter keyed on PROVIDER, so a cheap call over 8 items and an expensive call over 3 land on one blended attempt-count that is published into every job's result. `usage-ledgers` already requires one row per metered call with the concrete model as served, and `price-tables` already names a blended per-call rate as the defect. It is an unapplied technique, and it would be wrong on a one-part request too. Home hint overridden: the routing subject's own golden path draws the seam as routing decides / failover retries / **metering bills**, so any landing would have amended `cost-metering/usage-ledgers`. The row's own nearest-prior-art pointer is also wrong - a per-part input property is not an override, with no consumer, no precedence and no reaper. |
| 2026-09-17 | oracle-before-gate (amendment) | hitl-approval | (none) | experiment | not-better | **Backlog wave 6, unit c28 (2 members). Both halves COVERED, and the residual I declared came back refuted - in the opposite direction, under every matching.** Half A, bound the loop: covered five times, and one member is a design **the corpus refuses by name** - `stuck-loop-detection:14-21` says a maximum attempt count 'is wrong in both directions' and keys the stop on failure identity instead. Its 'exhaustion warns rather than fails silently' is stated better at `:39-44` ('never a bare stop', hands upward a root-cause hypothesis), and three retry techniques already own exhaustion as a distinct recorded ending. Half B, move what the human approves: covered three times over, including the sentence that answers the falsifying question directly - `hitl-approval.md:170-173`, 'a summary written by the gated party is the fox describing the henhouse door', and `oracle-before-gate:96-104` already licenses descending an altitude while requiring the remainder be recorded rather than inheriting the verdict. **The residual: I predicted the remainder is where repair concentrates. It is not.** Two mainline histories, 400 commits each, one observation per changed source file, hub files and bulk changes excluded. Size-matched, the slice no test reached was repaired at **0.57x and 0.25x** the covered slice; matched on size AND the file's own churn, 0.84x and 0.49x - never above 1.0, at p<0.005 against a 200-shuffle permutation null, 0 of 200 shuffles below the observed value on both trees. About half the raw gap is churn: a file with tests is a file under active work, and active work has more chances at a fix. **The half that survived is sharper than the one I predicted:** the slice the proxy CAN see is not safe either - those file-changes were green at merge and repaired within a fortnight at 26.6% and 12.3%, which is 180% and 423% of the remainder's rate. And the proxy's differential component carries no signal once churn and size are matched. Both components fail to discriminate while the artifact renders as complete. Power stated rather than assumed: a planted 1.3x lift at the same n and band mix turned the test red 15 of 20 and 13 of 20; 1.15x, 8 and 6 of 20; a planted null, 1 and 0 of 20. Label permutation collapses the ratio to 1.00, so the test can both see an effect and decline to see one that is not there. Calibration disclosed: back-dating agreement 11 of 20 and 18 of 20, with **every** disagreement in the direction that over-counts the remainder, so the headline share is if anything too high. Instrument correction disclosed: the first coverage label missed tests that read a sibling file and assert on its text; adding that form moved one tree's remainder share 51.8% to 47.4%. Landed as one bounding section plus one decision rule on `oracle-before-gate`, because the count's implication - that the remainder is the risky part - is not supported and the technique should say so. What the instrument cannot reach is stated in the landing: repair history only sees harm somebody found, which is precisely not the drift that technique warns about. Two ledger corrections from the worker: `6-043` is marked `refuted` and should be `covered` (its claim is faithfully stated in its source; what is true is that the corpus refuses the design), and its home hint points at transport retry when the owner is continuation. |
| 2026-09-17 | the-narrowest-view-owns-the-number | codebase-scanning | ascent | code | better | **Backlog wave 6, unit c20 (3 members); split three ways - the rule as written COVERED, a residual landed, and one member mis-clustered.** The rule 'a sampled read must publish a coverage statement' is in the corpus twice producer-facing, including the sharp half at `context-budgeting.md:74-81`: the notice belongs in the PROMPT, because the party that needs to know is the model about to reason over the truncated corpus, and a log line informs only the operator, who is not the one reasoning. **The residual is where the figure is COMPUTED, and the seam was chosen so that publishing it could turn out to be ceremony - which is exactly what one arm showed.** Arm B1 applied the unit's rule literally: it printed a correct coverage statement and moved no consumer at all - same coverage 0.85, same blend, same silent caveat. Without that arm the landing would have claimed a win the prescribed repair does not deliver. Arm B2 admitted the same fact as a TERM in the figure the judgment's weight is computed from: coverage 0.85 -> 0.38, the model's weight on every score 0.51 -> 0.228, and a partial-read caveat that had stayed silent on a 45% view fired. The 17 dropped files were the deployment manifests, the project's own capability manifest, an API doc and the whole end-to-end tree. Floor: 2,003 tests across 126 files green, typecheck and lint zero bytes of output, all read from logs rather than from an exit code after a pipe. Positive controls: the coverage number is live (10 displaced files move it 0.85 -> 0.64 under unchanged code), the prompt assertion can fail against a fitting fixture, and **7 of 10 new assertions are red against pre-change HEAD** while the 3 that pass are the agree-assertions, which is their purpose. **Negative control found the floor's blind spot** (lesson 10): forcing the window to show ZERO files reddened six assertions elsewhere - so the floor catches the catastrophe - and it was completely silent on the live defect at 14 of 31. A floor that sees total failure and not partial. The pair asserted on one path: separate when the view narrows, AND a byte-identical prompt when it does not. An always-on banner passes the first and fails the second; the null change passes the second and fails the first. **A sub-rule found only by shipping it:** a scope statement about untrusted material is a first-party claim and must sit OUTSIDE the fence that voids that material's authority. The obvious placement - a clause on the sampled-files label - puts the one sentence the model most needs to obey inside a fence it was just told to evaluate rather than follow. Nothing in either tree said this. Shipped ascent `f674b1ee`. Home overridden: the unit's home field was a PRIOR-ART pointer mistaken for a home assignment, which is lesson 5's purest failure mode. |
| 2026-09-17 | (none - claim refuted on the fleet) | audit-logging | (none) | experiment | not-better | **Backlog wave 6, unit 1-094. Both halves refuted, including the residual the worker found itself - and the residual was killed by a scorer it had not tuned.** The claim: containment maturity does not imply accountability maturity. Four independently derived scorings of 13 fleet trees all put the correlation above the pre-declared 0.40 ceiling - 0.587 and 0.527 by hand on two passes, 0.449 mechanical capped, **0.753** mechanical repaired - with 14% discordant pairs and only one tree in an off-diagonal quadrant. Containment predicts accountability here. **The positive control is what makes the refutation trustworthy rather than a null:** a third axis, the read surface over the records, correlates 0.821 with accountability against 0.358 with containment. The instrument can see a correlation and distinguish two axes; it simply did not find independence. Confounds reported rather than used as rescue: partial correlation given commit count falls to **0.343**, so about a third of the co-movement is tree maturity - one owner's habit - and the rest is real. The claim is refuted on THIS population, 13 single-owner trees, not on the source's 70-project cross-section; what it shows is that the claim does not generalise to the population the corpus is written for. **The residual - that the slack is one-sided, containment exceeding accountability but never the reverse - was refuted by its own pre-declared held-out test** and is recorded with the numbers that killed it so a later run does not rediscover it as new. The worker separated the three possible readings of a failed floor and answered each: arm 1's floor failure means the claim lost; arm 2's P1 failure was an instrument fault, repaired, and the repair STRENGTHENED the refutation; arm 2's P2 was a mis-declared floor that would have failed on noise either way, so its failure is not counted as evidence. **Two instrument faults worth carrying, both nameable and neither a judgment call.** A match count capped at 12 over trees of 872 to 11,016 files is a presence test, not a measurement - under the cap a tree whose 12 `actor` matches were game-engine scene objects scored identically to a tree with 254 real sealed-decision attributions. And masking by FILENAME removed 92 files from a high-accountability tree and moved not one signal: its hash-chained ledger lives in files named for the domain, not for auditing, so a filename-scoped ledger sweep would have reported that tree as having no trail. **The row's third figure is arithmetically impossible at its own N.** 5% of 70 needs 3.5 projects; 3 prints as 4% and 4 prints as 6%. The other two reconcile exactly. Per the technique this very source already landed, a failed reconciliation is a finding about the pair - so no figure from this row may ever be quoted. |
| 2026-09-17 | class-before-inspection | tracing | tracklight | code | better | **Backlog wave 6, unit c36 (2 members); one half better, the other refuted TWICE, and the seam returned a live leak.** Confirmed: a run record is secrets-bearing by CLASS, because its four surfaces - instruction text, returned text, tool-call arguments, and the environment those calls named - are all free-form content assembled by somebody else's code. A content scan cannot clear one: it recognises only shapes someone enumerated, so a clean result means 'none of the shapes we know are present' and is read as 'nothing sensitive is present'. A scan is a volume reducer and a second fence, never the classifier. **Refuted, and arm B1 is why it is refuted rather than unproven** (lesson 9): the three-sink tiering implemented exactly as proposed scored PRECISELY what arm A scored - same assertion, same panic, same stored row field for field. Three sinks do not fix an assignment made per door; they are three ways to be wrong at a door that never consulted the authority. The failure was never 'one log level too coarse', it was 'the class recomputed at each writer'. **Refuted a second way, by reader count:** product-code readers of the hashed-payload tier in the one fleet implementation: **0**. No query, endpoint, dashboard or diff consumes it. The digest that does answer the question people ask - which instruction text ran - is a NAMED digest of one field carried in the metadata tier, computed by the producer over what it actually rendered and declared exempt from the scrub, and it backs a whole public endpoint. A whole-payload digest as its own tier answers 'did this change', which nobody asks. **What the seam returned, beyond the claim:** an owner configured never to persist payloads had them persisted in the clear - and because the same call that applies the class writes the receipt, the stored rows carried a well-formed stamp naming class `none` for an owner whose configured class is `drop`. The posture endpoint that exists specifically to answer what the database holds rather than what the configuration says reads those rows as an ordinary cohort. **The per-door decision forged its own receipt**, so the one instrument built to catch this reported the door's choice as the owner's policy. That is in neither source. Target was a pair pulling opposite ways: separate when the class moves (a drop-policy owner's server-built record persists no payload; a hash-policy owner's persists a digest and no plaintext) AND agree when it does not (a none-policy owner's payload stored verbatim, with usage, cost, status, error, name, tags, action type and version, the prompt fingerprint and the parentage identical under all three classes). A 466-test floor plus integration, private target and TMP dirs per the fleet gotcha. Arm B2: **468 passed, 0 failed**, clippy with -D warnings exit 0. **Two negative controls, and the second is the finding.** Breaking the shared enforcement point reddens 4 tests, so the floor is real for that path. Restoring the literal at the exempt door alone reddens **only the new test** - and that 466-test floor includes a dedicated suite for that very door, asserting its PII scrub and its fingerprint survival, which cannot see it ignoring the owner's class. Home resolved from no home field at all, with four neighbourhoods read and rejected by a stated discriminator (what a sink may hold, which the corpus owns exhaustively, versus how an artifact is classified, which it owned nowhere). Re-proven on tracklight's own gate script before shipping: format, clippy and the full workspace suite all PASS. Shipped `3c49cae`. Second landing: `llm-call-telemetry-model` states the one-function rule correctly and its writer enumeration is short by the door that matters - 'both front doors' counts the surfaces a CALLER can reach, and a server-built record is exempt with an argument whose every clause is true and none of which covers the payload class. |
| 2026-09-17 | (none - the design family loses) | agent-memory | personas | code | not-better | **Backlog wave 6, unit 6-002. The trigger refuted on real data, the direction confirmed, and the whole design family still loses to the shipped seam - so nothing lands and the worker said so itself.** The claim: a write path charges `weight - 0.1` on each correction, so ten unattended corrections drive a claim to zero and a well-maintained record becomes indistinguishable from an untrusted one. **The trigger does not exist at real scale.** Deepest supersedes chain in a recorded year of one person's use: 3 in ground truth, 5 at the write door, histogram {0:106, 1:39, 2:24, 3:7}. Zero subjects reach ten, so the charge bottoms out at 0.6 and zero is unreachable. At the depth actually reached, the penalty arm landed ONE probe from the no-penalty arm - inert. **The direction is confirmed and the row understates the harm's shape while overstating its trigger.** The penalty arm is the only one that improves the never-corrected rows' mean rank while worsening the corrected rows' - the inversion made numeric. And it is not indistinguishability: the most-corrected row sat at 0.6 against 10.7 for the most-repeated untouched row, distinguishable in the WRONG direction by more than an order of magnitude. **The falsifying question answered: mis-attached, not worthless.** The write door one call earlier already emits the label that separates the two causes - an explicit change versus the same item said better versus cannot-tell - and a flat subtraction discards a label already computed. Arming that label recovered 42 of 92 probes against the flat penalty and gave up nothing on the never-corrected half. **But the 'record was wrong' branch is argued, not measured**: the fixture's world only emits lifecycle changes, so nobody pricing churn can label it on this data, and that is recorded as a rescan condition. **The finding that outranks the unit:** no weighted arm beats plain cosine (80 of 95 answers in context, against 60, 61 and 65). A standing the write path spent a year accumulating - 1,117 bumps - is actively harmful when multiplied into similarity: 78 of 92 corrected probes demoted, 20 answers lost, none gained. Queued as its own row. Paired per-probe counts rather than means, which is what exposed it. Positive control: a recency arm moved the never-corrected rows' rank 10.9 to 47.3, so the instrument hears a ranking change. **Negative control against the floor:** cutting the weight multiply out made every policy collapse to exactly arm A's numbers while still computing its 0.6, so every difference is attributable to the weight reaching the ranking and nothing else. Determinism: 1,026 of 1,026 cache hits, zero model calls. A declared-pair trap the worker caught: every weighted arm LOWERED the stale-value count, which reads as hygiene, and the answers-in-context count fell with it - the lane returning less, not returning better. A single target assertion would have scored this a win. Gate stated rather than absorbed: the harness's own clock-purity check did not run and is not green - it replays at a shifted base date, so a date-carrying prompt misses the content cache and issues real model calls; aborted after 22. |
| 2026-09-17 | sink-reversibility-decides-the-commit | streaming-output | personas | code | better | **Backlog wave 6, unit 6-003; split - the exactly-once clause COVERED, the irreversibility asymmetry landed.** The corpus asserts irreversibility of emission AS SUCH, universally, and never of the sink; nothing decides the commit policy from the sink's class; and nothing covers one producer feeding sinks of DIFFERENT reversibility, which is where the cost lands. The falsifying question was answered by reading: no modelled capability carries retractability, so the rule survives. Measured on 397 real execution logs, 182 MB, 80,919 output lines: **222 lines ended at the reader's own marker and all 222 were unparseable** - records destroyed, 220 of them tool results, lengths clustered at 63,266 to 65,533 against a 65,536 cap. The producer mutated the record IN BAND to satisfy an append-only sink, and the entire cost landed on the other sink. **The worker's first instrument failed its own control and would have reported 80,689 destroyed records instead of 222.** Cause: a SECOND, independent in-band mutation in the same irreversible sink from a different stage - a log redactor rewriting a key-value form. So the project's own documented claim that these logs are the exact bytes the parser was handed is **no longer true**, and any future measurement off them must undo the redaction first. That is the most useful thing in this unit. **The correct policy already existed in that tree at exactly one site** - a credential path that sanitizes, re-checks that the result still parses, and keeps the original if not, with a doc comment explaining that the unguarded form wipes a ledger. Known, written down, never generalized to the two sinks that needed it. A two-sighting inside one repository, which is why the landing is a mechanism rather than an opinion. Target pair on arm B: a clipped record surfaces, AND an unclipped one is byte-for-byte the old classification and display. Floor: the 13 pre-existing failures identical to a paired baseline, and **1,337 passed against the baseline's 1,330** - the +7 are this unit's own tests. Positive control: the arm-B expectation run against arm-A code went red for exactly the claimed reason. **Negative control turned TWO gates red**, the new one and a pre-existing window test, so the floor is real in both directions. Shipped personas `91b684dfb`. **What shipped is the mechanism plus the assertion pair, NOT the production fix** - the runner's call site still uses the in-band form and lives in a heavier crate, so the 222 are not yet recovered in production. Home overridden: the unit's hint named the surface a program owns and rebuilds, which is one instance of the sink class rather than its owner. |
| 2026-09-17 | paired-sites-localize-the-fault | invariant-placement | tracklight | code | better | **Backlog wave 6, unit 1-078. Six rules, six verdicts - four covered, one refused, one landed.** The unit proposed a new subject with a density floor; the subject is refused for want of a design record, and the floor is refused on evidence. **R1, the density floor: not-better on mechanism, and explicitly NOT refuted on statistics.** The fleet has almost no variance to measure - 3,134 of 3,176 production files carry zero assertions, 9 meet the floor. The natural experiment is a reference tree that applied the rule in bulk in three commits: 1,463 assertion lines across 55 production files. Difference-in-differences returned 2.97 against a null centre of 1.02, p=0.032 in the WORSE direction - **and the worker declined to report it, because its own positive control could not detect a halving** (p=0.73 at 0.50x). A test that cannot fail is not a floor, which is this corpus's own rule applied to the worker's own arm. **What is powered is the mechanism, and it needs no statistics.** Within hours of that campaign landing, two FIX commits repaired it: four assertions asserting a bound the kernel does not honour, which killed a worker and hung CI to a 60-minute timeout; and five snapshot bindings that broke the optimized build, which the debug test lane structurally could not see. And the campaign shipped a helper documented in-tree as existing to keep an assertion a no-op while still compiling in every profile - **`proxy-metric-counts-its-own-satisfiers` found realized in the code**, an assertion written to be trivially true so a density count would hold. **R2 is the landing, and its value is not what the rule claims.** Four arms on a write/read codec pair. Arm A detected the real defect and could not say which side - so the pair buys LOCALIZATION, not detection, and the rule does not say so. **The worker's own write-side assertion, written to the rule, was wrong about the code**: the reader splits at the first separator, so only the leading component must be separator-free, and the rule applied literally REFUSED A LEGAL INPUT - the same failure class as the reference tree's fix commit, produced independently by following the rule. Only the residual's form refuses the illegal construction and accepts the legal one. **A third rule found by measurement:** 'keep assertions on in production' is two rules. Whether a check RUNS is a severity decision the corpus already settles by who supplies the value. Whether a check is PRESENT is not a decision at all - a stripped check still compiles its operands, so a paired snapshot can make the shipping profile the only build that refuses the module. Measured: neither fleet Rust project compiles the shipping profile on a blocking rung, so that class is invisible before merge in both. R5's unconditional form is refused by the corpus BY NAME: at a trust boundary, stopping is the outcome the adversary selected. Negative control: deleting the write-side guard turned a named test red with its attribution string, so the floor is load-bearing. Shipped tracklight `ed2ffa1`, re-proven after a rebase past a sibling unit's change to the same crate tree - format, clippy and the full workspace suite all PASS. Two corpus corrections owed and recorded: the application that reports this tree's assertion density presents it as a property of the tree, and it was **three months old** when read - 1 site in January, 1,101 in September; and that same application reads the campaign as clean on arrival, which it was not. |
| 2026-09-17 | one-version-decays-to-a-floor | supply-chain | (none) | experiment | better | **Backlog wave 7, unit 2-011; split - the proposed SUBJECT refused, one technique landed.** The claim argued from four subjects each explicitly scoping away from framework adoption across a fleet. **One of the four exclusions is false**: supply-chain names dependency resolution in scope in its own charter sentence, and a fifth subject the claim did not know about routes update discipline into it by name. Three of the four candidates said to land in the unowned seam had already landed INSIDE supply-chain in that same intake run. The method's own subject test: **1 unmodeled decision against a floor of 3**. Arming the prescribed repair literally (lesson 9) would have produced a subject with ~5 technique slots of which 4 are prose relocated out of one forged technique - 80% relocation - while stripping supply-chain of an amendment its own subject note records as belonging there. The corpus's own promotion condition for this lead (a second framework in a different ecosystem) is **unmet** 22 days on. **The absence instrument failed its own control first, and that is why the absence is trustworthy.** A broad multi-word `--prose` query did not return the file the worker had just read, because ranking is dominated by single-word slug overlap and the carrier fell outside the top N. Re-derived from a narrow distinctive body phrase: exact hit. Five narrow absence queries then agreed, and an independent grep over bodies agreed - so the gap is real, and it is one technique wide, not a subject. The residual measured on the live fleet: a coordinated upgrade is an EVENT and being on one version is a STATE. Equality has no owner because N checkouts are N resolvers with no shared lock, so coherence is dissolved by ordinary work in members that are not in contact. **Measured: 8 of 10 manifest-bearing projects carry the framework, 6 at the swept version, and 2 sit below the floor 22 days after the sweep** - one of them declaring a range that can never resolve into compliance. Across the same fleet, 37 shared dependencies resolve to more than one version, which refutes the opposite error: nobody held equality for a single dependency for three weeks. **The instrument that would have caught it exists and was retired on 2026-09-04 for having no caller**, after one recorded use on the day it was written. Home overridden: the hint's entire golden path contains one incidental mention of `version` and none of `dependency`. |
| 2026-09-17 | parallel-dispatch (amendment) | fleet-orchestration | (none) | experiment | better | **Backlog wave 7, unit 1-106; split - the ceiling half better, the bucketing half refuted as a remedy, and the row's headline false as written.** 'The concurrency ceiling is supervisory, **not** machine' fails: the machine ceiling does bind, and first - 11 simultaneous runs against a configured cap of 10, and a peak of 15 live sessions on a door whose live-session ceiling was never switched on. What survives is stronger and is the release asymmetry: **a cap on a stock that RELEASES places no bound whatever on a stock that does not.** Ten slots cycling all day deliver an unbounded pile to one person. Over three weeks of one real single-operator fleet: supervisory stock peaked at **34**, stood at 10+ on 8 of 10 active days, and on **5 of those days the machine ceiling did not bind once**. 636 runs passed through ten slots. The release test is what makes it a mechanism: of the holds that closed, **47.6% outlived completion by more than an hour** and 7.9% by more than a day; 22 never closed; and the five longest all closed at ~168 hours, which is a weekly sweep and not a verdict - so where the release IS observable it is usually an expiry, which must stay a distinct outcome from a decision. **The prescribed remedy is the breach signature, not the fix** (lesson 9). Bucketing halved the verdict count - 190 items in 97 acts, 0.51 acts per item - so as a BEHAVIOUR the claim is confirmed. But **18 of 28** multi-item verdict acts spanned more than one severity class, the one batch a review surface must not offer, and the largest covered 28 items across 4 severity classes and 7 work streams **8 minutes after the stock peaked at 34**. Without arming the prescription this loop would have landed a rubber stamp as a remedy. The borrowed 'four or five concurrent things' figure is refused on **construct**, not arithmetic: it is a working-memory capacity number from recall tasks measuring passive retention over seconds, and a multi-hour work stream held in a folder is the external representation that literature says removes the limit. The corpus already refuses this class of borrowing elsewhere. No figure quoted; a per-person derivation from the discharge pattern replaces it. **And it found the same defect in this registry's own method file**, which justified a wave size of 8 as the subagent cap of 20 divided by a fan-out of 3 - a bound on the director's attention derived entirely from the process table, `limits-are-derived` satisfied in form and violated in substance. Corrected in the same commit: 8 now stands as an observed working figure and says so. Controls: the concurrency instrument reads 1 for serial and 20 for fully overlapping intervals; the lag detector read a real 0.0027s resolution; the three series come from three different tables and the fourth arm from a different system entirely. |
| 2026-09-17 | activation-is-a-separate-mutation | agent-runtime-assembly | (none) | experiment | better | **Backlog wave 7, unit 1-118; the reach classification better, the claim's implied consequence REFUTED IN THE OPPOSITE DIRECTION, and the residual is the landing.** Census of 277 installed skill entries across 63 trees: 118 prompt-class, 150 execute-only, **9 execute-and-persist**. Positive control from a different layer: the user-wide settings file carries 7 hook events firing on every session. Negative control: two of four persisting patterns returned zero members, so the classifier can report zero, and the persisting share is 3.2% - it discriminates rather than rubber-stamping. **The falsifier fired and the claim had the direction backwards.** Review surface per component, fleet-wide: hook bodies **12/12 tracked (100%)**, hand-written instruction documents 23/24 (96%), **installed instruction copies 3/49 (6%)**, activation values **0/14 (0%)**, installed payloads 116/274 (42%). A hook escaping the review an instruction gets is false here - hook bodies are the best-reviewed artifact on the machine. The property that predicts visibility is **hand-authored versus installed**, and within an install the reviewable component is the pointer, not the deciding one. **The decisive control:** a hand-built hook-integrity checker whose `--check` mode is documented as 'the one that has teeth' returns **0 with bodies present, payload present and activation unset** - it prints that the activation is unset and then prints that everything is fine, because the activation comparison sits outside the findings array and can never contribute a block. Two other negative controls went red correctly (bodies absent, payload absent), so the floor is real and this is a specific hole rather than a dead gate. Arming the claim's own repair (lesson 9): 'review the install' puts a reviewer in front of a diff that exposes 12/12 bodies and 0/14 activations - **exactly what doing nothing exposes**. It moves the reviewer's attention, not the instrument's reach. The landing also names the install-step idiom that converts the mitigation into the thing it mitigates: a bootstrap step written to swallow its own failure so container builds survive it, which makes 'a working checkout implies a working gate' false. Not-fatal is correct and must stay; silent is not, and was never required. Declined experiment stated rather than run: measuring an ungated push directly would require mutating live harness state. Owner resolved from no home field, with four neighbourhoods read and rejected by a stated discriminator. |
| 2026-09-17 | addresses-not-atoms | agent-memory | (none) | experiment | better | **Backlog wave 7, unit 9-116; BOTH clauses of the claim refuted, and the residual landed.** The claim: recall precision scales with fact granularity, not count, and the discriminator is fact atomicity. **The confound was the whole claim.** Holding total words AND piece count fixed and moving only where the cut falls: atomicity predicted **0.0** at the loose budget and **-2.0 against itself** at the tight one, where the arbitrary word-offset cut beat the fact-boundary cut 16.0 to 14.0 on non-overlapping ranges. The entire effect is piece count. And specifically **reach**, which one arm isolates: atomizing only the items the budget already reached raised the count 61 to 174 and **lost** 1.3 points, below the whole-document arm, the only arm to also drop on the unanswerable stratum. Fineness spent inside existing reach is a loss on every axis; fineness that extends reach gained 2.7 to 3.7. Opposite signs of one variable. The mechanism is pinned in the prompts rather than inferred: **a fact-boundary cut yields a piece that is grammatically complete and epistemically partial** - the qualifier lives in the next sentence, and an arbitrary cut sometimes keeps two neighbouring facts together where a fact-boundary cut never does. So the cut is what creates the complementarity. **The reach is paid for in abstention, and one total cannot show it:** wrong answers 1.7 -> 4.7 while honest 'not in store' collapsed 6.3 -> 0.7. Three points of score moved up and three answers moved from silence into confident error. Probes written to be unanswerable scored 4 of 4 in every arm, so the guessing is on questions the store does answer. Above an always-loaded index **every arm scores 21.0 identically** and granularity stops existing as a variable - the same boundary the sibling merge technique found from the other side. **The worker retracted its own declared control in writing** when the shredded 8-word arm scored best rather than worst, and replaced it with a blindness check derived from a different layer, which went red in the other direction. It also detected the measurement substrate shifting mid-run - the store grew 24,335 to 25,003 words and one gold file changed, because the director edited that memory the same day - and re-ran its own baseline rather than reusing the prior wave's figure. Corpus correction: `recall-injection`'s 'prefer fewer, stronger items' is size-confounded and now names its predicate. |
| 2026-09-17 | the-broad-root-is-the-unaudited-one | codebase-scanning | personas | code | better | **Backlog wave 7, unit 2-008; better, and the half the claim gets wrong is the interesting half.** The three rot classes are **ordered against their detectability**. Of 11 declared root patterns on one repository: 6 duplicate another, 1 is 99.5% subsumed, 1 is trivially broad (225 files, 93.8% of its directory), 3 contribute unique files. **Seven of the eight rotten ones are INERT** - dropping the duplicates, dropping the near-duplicate, mistyping the sole real root, and planting a nonexistent pattern each left the finding set byte-identical across 2,210 rows. The eighth moved **195 rows** (+148 findings, +112 unused files) and is **the only class the tool's own hint channel structurally cannot emit**. Paired assertion on the same arm set: every inert removal byte-identical, AND the broad removal changed 195 rows. The first alone is passed by classifying everything inert; the second alone by deleting the whole array - which one control did, moving 7 rows. **A positive control failed as designed and its failure is a measurement:** emptying the entire hand-written root array moved 7 of 2,210 rows and left the unused count at exactly 317, because autodiscovery already supplies every real root. So that control could not certify the instrument; narrowing the population field instead took 2,210 to 189, which is what licenses every delta. Precision hand-verified rather than extrapolated: 11 of the repair's 112 new findings sampled, 7 with no reference anywhere, 4 invoked by path string from documentation - **64%**. The literal repair was worse, adding 39 script test files that are dead by import and alive by runner, a false-positive class the refined repair removes. Second surface: 43 declared patterns across 8 separately maintained contracts, **0 empty and 0 duplicated**. So the rot appeared only where a hand-written declaration and autodiscovery BOTH supply roots - 8 of 11 patterns rotten there, and eight single-author declarations clean. **An inclusion surface rots this way when it has two authors.** **The worker's own first classification was a fabricated result**, caught by reading rows rather than the tally: passing an array index as a glob matcher's second argument made every element after the first match, reporting 42 of 43 patterns redundant. The tell was a coverage ratio above 1.0. Shipped personas `f30c6073c`. Home overridden to codebase-scanning, which the hinted subject's own text assigns it to. |
| 2026-09-17 | release-level-by-reader-reach | release-pipeline | kp | code | better | **LEDGER CORRECTION to the wave-5 row above.** That row recorded `kp:545cad4a` as shipped and it was not: the branch was proven, the row was written, and the fast-forward was skipped. Caught 2026-09-17 while a wave-7 unit sent the director into that project for an unrelated reason, by testing the RECORDED COMMIT against the branch rather than trusting the branch name - `backlog/c15` and `backlog/c27` are both absent from main as branches while their content is present under cherry-picked SHAs, so branch ancestry is the wrong instrument and would have reported three false alarms. Re-proven on current main before shipping: 8,104 unit tests with 1 failure, `tsc --noEmit` 0 bytes, eslint 0 bytes. **The one failure was first called a regression and that call was wrong** - a process-tree abort test failed 2 of 2 in isolation on the arm, which looked deterministic, and then on plain main it passed 3/0 on one run and failed 2/1 on the next. It is flaky under machine load, t03 touches two files in an unrelated feature directory, and the earlier proof of a sibling unit had simply caught it on a good roll. Now genuinely in kp main at `545cad4a`. |
| 2026-09-17 | occurrence-is-not-a-type | invariant-placement | goat + personas | code | better | **Backlog wave 7, unit 2-007; the taxonomy better, the implied prescription refuted.** The falsifying question was whether this is a list or a mechanism, and the answer is mechanism **because the test refuses things**: applied to all 33 hand-written lint rules in a 13-repository fleet it admits **9 (27%)**. A filter that admits everything is a label. The test: state the term without naming where the value came from, when it was read, whether anybody consumed it, the order it was written in, or which of its parts were touched - if you cannot, no type holds it. Two families fail it: occurrence properties (identity, capture time, consumption, read set, lifetime) and **terms about the annotation itself**, which the fleet supplied and the source did not name. Four of the source's five blind spots are independently sighted across three unrelated repositories against unrelated libraries; the fifth is inference-algorithm-specific rather than general. Distribution over the 33: 19 style, 5 arrangement (already routed by an existing technique), 9 type-invisible, and **0 rules standing in for a term a type could have held** - the reassuring half. The sharpest sighting states the claim in its own header: a value read at import time is frozen forever, and the keys exist, are translated and are referenced, just at the wrong time. **Refuted: type-invisibility does not imply a rule is needed.** Two repositories on the same container library, same term: one at **0 violations over 64 opportunities with no rule**, the other at **18 of 49** while 35 sites in that same tree supply the comparison correctly. What type-invisibility guarantees is that the **compliance rate is unobservable** - neither team could tell 0/64 from 18/49 from any surface either would have checked, including a green typecheck. That is the real argument, and it is stronger than the one the unit made. Invisibility proved rather than asserted: repairing all 18 left the typechecker at **39 errors before and 39 after**, identical codes at identical sites, none at a selector, the only delta a one-line offset from an added import - re-verified by the director on both arms. **Sledgehammer control (lesson 11):** the same rule with the freshness predicate deleted reports **280 findings of which 262 are correct narrow reads - 94% false**, from a three-line deletion, and both variants run in under a second, so cost never separated them. Positive control caught a harness defect first: the seeded-violation run returned 0 of 3 because flat config was passed as an object, and without it the arm would have read as a clean zero. Shipped goat `84db23c` (rule + 18 repairs) and personas `b6ac5376e` (rule at error, `eslint --no-cache src` exit 0, 0 findings, negative control confirmed by unwrapping one real selector and watching that one error appear). **Gate liveness stated, not absorbed:** goat's `eslint src` **cannot start on either arm** - its config imports a lint plugin that is not installed, so it exits 2 before reading a file, on main, unmodified. The director re-verified this and also found `tsc --noEmit` at 39 errors on main. Arm B's zero is a rule result, **not** a gate result, and both dead gates are queued. |
| 2026-09-17 | line-volume-confounded-with-edit-grammar | delivery-analytics | (none) | experiment | better | **Backlog wave 7, unit 2-002; the diagnosis better, the prescription better with a stated condition, and the source's own MECHANISM refuted on its sign.** Corpus: 9,366 session records, 21,840 mutating calls, 11,791 file-edit chains - and both grammars belong to the **same producer**, so model, operator, repositories and period are held constant and grammar is the only variable. Over 2,030 verified chains: whole-file rewrite inflates the net change **6.89x**, ranged replace **1.19x**. Spread **5.8x** against a declared target of 1.5x; the +/-10% floor failed decisively. A naive per-call diff does not repair it either, at 6.76x. **The source says ranged replace UNDER-counts because it shows only insertions. It over-counts**, by 1.19x headline and 1.22x baseline-free, with 17.7% of its payload being restated anchor context booked as written. Both grammars over-report and only the magnitude differs - and the sign error matters because it sends an implementer to correct the wrong side. **No correction factor exists:** rewrite inflation rises with file size (1.84x, 1.94x, 2.20x by band) while ranged replace is flat at 1.21x over 99.7% of its population, so knowing a producer's grammar does not let you normalise its number. The prescription is computable on **43.4%** of real chains and 84.2% accurate where computable - 50.3% of chains were not under version control at all - so the landing is a three-rung ladder ending in **publish no volume figure**, and a baseline-free arm was built to survive that limit. **Two instrument faults, both caught by reading rows rather than the count.** A baseline-fidelity control first returned 0 of 297 with every mismatch off by exactly one line - a trailing empty line the blob comparison drops; the count alone read as a total refutation of baseline recovery. And the first pass pooled brand-new files with rewrites of existing ones, which **inverted the entire result** - rewrite read 1.09x, below ranged replace - because a new file cannot be over-counted by construction. Stratifying produced the 6.89x. Controls: the rename unwrapper fired 3,005 times on real rows and is reported as load-bearing nowhere in the baseline path; a new-file control returns 1.02x, so an instrument manufacturing inflation would have shown it there; 172 real pure-deletion edits reported positive payload against zero added lines. The worker's own declared expectation was off by 6 through its arithmetic and it **amended the declaration rather than the code**. The rule had already shipped as a comment in one fleet project from this source's own intake run, with no corpus statement anywhere. Amendment landed: `batch-size-thresholds` now says a stated unit NAME is not a stated unit. |
| 2026-09-17 | configuration-axes-cross-the-ladder | test-harness | personas | code | better | **Backlog wave 7, unit c34 (3 members); one unit plus one mis-clustered orphan, and one half honestly unmeasurable.** The clustering call was made first: all three members' MATRIX halves are one claim, but `3-026`'s second half - immutable sessions inside a mutable manager - is an ownership/observability design sharing no mechanism with 'the axis you vary is not the axis you report'. It is marked **unmeasurable** with its instrument named, and `ab_verdict: better` explicitly does not cover it. **Third wave running with a mis-clustered convergence member.** The falsifying question answered honestly: only **two** fleet suites vary two or more axes in one run, so the rule buys something in one of two places and the landing says so rather than pretending to a population. Every Playwright config in the fleet is single-browser, so the literal claim has no seam and was measured on conditional-compilation feature sets instead. Prior art found in **three** bundles, each about one specific axis pair and none in the home subject; four nearest neighbours in that subject read in full and all correct-but-blind, one proving the residual by its own boundary section. The residual: the suite's configuration space is a product, the reported result is one cell, and **no instrument defined inside a cell can see a missing cell.** Measured: the cell CI reports compiles **1,340** tests and runs 0 of the 12 in the module under test; another cell compiles 1,189; both together 1,352. **Target T2 is what stopped the sledgehammer** - 1,189 < 1,352 means the cells are not nested, so 'just run the richer shape' trades one blind region for another, and without that assertion the landing would have prescribed switching cells. 25 gated test functions at four sites; one file is a trap, holding a second ungated test module so a file-level count over-reports. **The negative control went somewhere unpredicted.** A deliberately broken gated test left the reported cell at 3 failures - **none of them the broken test** - and clippy green. Those 3 are pre-existing at unmodified HEAD, verified by reverting. So the floor was red throughout for three unrelated reasons and its failure list never contained the injected break: a red floor is not evidence the axis is covered. **The prescribed instrument's self-test caught two defects in the instrument, both the same shape - a scanner reading a DESCRIPTION of an invocation as an invocation, always failing toward clean.** It read its own doc comment's example and reported the gap closed; then it read its own test fixture and emptied the finding in one line. Both now excluded by name with the reason at the exclusion site, and that failure became a clause in the technique. Shipped personas `2c1df0186` - self-test green, eslint clean, and the project's own negative-control gate reports the new gate ok while holding at its pre-existing failure, verified against a stashed HEAD. Not shipped: the missing test cell itself, whose wall-clock cost is the owner's call; the commit makes that deferral explicit and dated instead of invisible. **The fleet's best statement of this rule is in a project, not the registry** - one harness holds `incoherent`, `blocked` and `flaky` as distinct verdicts, with the rule that the cross product is not the matrix, and the corpus owes that a home. |
| 2026-09-16 | write-back-sink-class | agent-instruction-files | gravity | code | better | **The seam hunt was the second source, and it refuted my first reading.** Two agent-written logs are named together in one contract line under one append-only rule, and their collectors aggregate differently: one sums rows into per-subject counts with no key, the other dedupes on (ts, bundle, claim). So one must stay installation-local and the other should travel, and nothing says which. Across the nine projects holding both, the count-aggregated log is shared in 2 and local in 7; the identity-aggregated one shared in 6 and local in 1. **Both projects sharing the count-aggregated log had written down a reason, and the two reasons contradict each other** - each is correct about one of the two files and false about the other, and each was applied to both. Shipped to the one project whose sharing carried no stated reason, so the arm tests the rule rather than overturning a judgment. Target: rows a fresh checkout inherits and re-counts as its own, 83 -> 0. Floor declared first: this installation's own collected output unchanged - 281 consults over 7 bundles, every per-bundle block byte-identical once the generation stamp is excluded, because the collector reads the working tree and not the index. Commits `e7b47e0` and `6b0dc23` on main, not pushed; the first was a pathspec commit that silently discarded the staged untracking, so the second lands it index-only. Return: the double-count itself is inferred from the collector's arithmetic, not observed - re-verify when a second installation clones any of these repositories, or if either collector gains or loses its identity key. Two known-misclassified projects untouched: one holds a dated owner decision plus live WIP in both files, one is on the cheap side (4 stranded rows, not a corrupted count). |
| 2026-09-16 | write-back-sink-class | agent-instruction-files | - (source tree) | experiment | unmeasurable | The mined tree's own agent configuration: 8 write destinations across 23 config documents, every one outside the shared artifact and none named anywhere in the same corpus as a read. Three are the policy file's closing line (mistakes / missing capabilities / environment discoveries, one clause, one ignore decision - and one of the three is a claim about the product rather than about the checkout). Four are a security-review runbook's machine-readable scan outputs, discarded, whose only surviving summary is a hand-maintained prose block dated months earlier - the half something read is the half that lasted. `unmeasurable` with the instrument named: the damage lands in a second installation and this tree has one. Return: when a second installation exists, or when any sink path appears in the tracked file list. |
| 2026-09-16 | one-accessor-per-fold-direction | authorization | ascent | code | better | **Seam chosen to falsify, and the first candidate refuted the stronger reading.** The hunt was for a verdict wider than its branch; the first hit computes permission by negation over a four-valued charge type, which is exactly the warned-against `!allowed()` shape - and it is correct there, because the enum is closed and its three non-denied members are homogeneous. Stated in the application as a refutation rather than dropped. The live defect was one function on, in the lane-aware generalization written explicitly to settle the shape before anything depends on it: a lane ABSENT from the allowance map (not metered on this plan) and an explicit null (metered, included without limit) both answered one value, in a line whose own comment names them as two facts, and the test above it asserted the distinction in a comment then asserted the value that erases it - an assertion that passes whether or not the two are distinguished. Target: outcomes distinguishable for the two empty inputs, 1 -> 2 (the new predicate fails arm A with `expected 'unlimited' not to be 'unlimited'`, 54/55, and passes arm B, 55/55). Floor declared first: no billing path changes and the scan lane stays byte-identical - the standing table-wide equality test holds, full suite 12,597 passed / 2 skipped / 0 failed, `tsc --noEmit` clean after the return type widened. Cost stated: one test's expected value moved for non-scan lanes on metered tiers, admissible only because no production consumer reads the result yet, which is the whole argument for doing it now. Commit `21b2bebe` on the project's active branch, not pushed. Return: when a caller outside the tests reads `LaneCharge`, re-check that `unmetered` is not folded into permission by a later `!== "denied"` - its members are no longer homogeneous, because `unmetered` is a claim about the plan's model rather than about this call's cost. |
| 2026-09-16 | one-accessor-per-fold-direction | authorization | - (source tree) | code | better | The mined tree is the technique's positive instance rather than a seam: a four-valued policy answer whose type exposes `allowed()` and `denied()` as non-negating named folds, each documenting which uncertain values it absorbs, ranked in prose (`If in doubt, use this popular method` against `Avoid this rarely used method`), with a third accessor for the uncertain case and an `implicit` flag recording whether the answer was matched or derived from the negate-the-last-rule fallback. `better` is read against the counterfactual the tree itself names: its one implicit conversion is to the enum rather than to bool, and its own TODO asks for that to go. Structural fact worth the row: the pair exists because the same answer type is consumed both by authority-bearing directives and by directives merely selecting between two authorized behaviours, so no single kernel-side fold is correct - which is the precondition `failure-direction` relies on and does not state. Return: when the implicit-conversion TODO is resolved, or if a bool conversion is ever added, re-read whether the two sentences still hold the line without the compiler's help. |
| 2026-09-16 | seek-stable-composition-authoring | video-assembly | systedo-case | code | better | **Seam chosen to falsify, and it widened the rule instead of confirming it.** The source's static analyzer flags a relative operator (`+=`) with a second concurrent writer; the seam picked was a marketing demo with a second writer and **no relative operator anywhere** - it animates to an absolute target from whatever the value currently holds, which is implicit in exactly the same way, so a checker written to the source's rule passes the file. A caught outcome would have cut the technique's general form back to the source's syntactic one; it was not caught. The demo's Replay called a reset that *animated* two counters back to their idle figures over 400ms and started the climb on the next frame, so the climb inherited the in-flight return rather than the idle value. Target: distance the counters travel on replay against a first run - rank climb 33% -> 100%, visibility 40% -> 55/55 (100%), measured headless on the project's own easing constant, three arms. Floor declared first: every arm must still settle on the shipped endpoints (rank 1, visibility 67) so the fix moves the starting point and not the claim - held in all three. Typecheck and lint green, commit `b95a43a4` on the project's active branch, 17 insertions / 5 deletions, not pushed. Return: when a lint rule exists that flags an implicit-start second writer with no relative operator, re-check whether the general form can be enforced rather than only stated. |
| 2026-09-16 | seek-stable-composition-authoring | video-assembly | - (source tree) | experiment | better | The mined tree carries the technique's mechanism and its own defence, so the row tests the claim rather than the tree. Measured at the timeline-library layer, headless, no browser, two arms one variable (the entry path): a relative value with a second concurrent writer diverged on **45 of 90 frames** between a sequential walk and two workers splitting at the midpoint, with a seam step of 26.67 against a normal per-frame 1.67 - the snap at a chunk boundary the source describes. Explicit endpoints: 0 of 90. **Control**: a single-writer relative value, 0 of 90 - the arm that earns the discriminator, since had it diverged the technique would have had to ban relative values outright. First harness returned identical numbers on all arms and that was not a tie: the ESM import hoisted above the DOM shims and the animation engine never ran. Structural fact nobody designed: the tree enforces one invariant at three altitudes (composition analyzer, runtime drawing backend, encoder frame placement), each refusal written up locally in its own platform's deployment guide and never as a doctrine. Return: when the frame-adapter interface leaves experimental v0, re-read whether the contract's stated obligations still match what the analyzer enforces. |
| 2026-09-17 | rebase-the-varied-input | eval-harness | personas | code | better | **Seam chosen to falsify:** the harness already injects a clock and ships a purity check for exactly this defect, so a working check would have made the technique a restatement. It did not work. A full-history copy that renders the wall clock on every recalled line passed 46/46 under the shipped check, because every date was masked before comparing. B rebases each date onto its replay's base (fixture-literal dates compared as written) and adds `--self-test`. Target caught 0 -> 46/46; floor full-history, none, raw-retrieval, hybrid-verbatim PASS 0/46 on both arms. Second defect at the seam: the id pattern's word boundaries were literal 0x08 bytes, so id erasure never matched. Shipped on branch `intake/clock-purity-rebase` (`3c606cf51`), because the shared checkout was mid-merge; not merged, not pushed. Return: re-run the 4 model-bound arms' clock purity when the sim bridge is up; their earlier PASS rests on the old mask |
| 2026-09-17 | observation-clock | agent-memory | personas | simulation | unmeasurable | The consolidation prompt prints each episode's instant but never states now or requires grounding; its rule 5 uses "today". No arm could see it: 12 of 3,571 replayed events carry a relative reference, all one template ("X is Y from today, not Z"), consolidated at 23:00 the same simulated day, so every ladder arm ties on this axis. Instrument named: a scenario with relative references dated by ground truth, a probe class asking for that date, and a multi-day consolidation lag or a backfill; then current prompt vs now-stated-and-grounding-required as a paired arm |
| 2026-09-17 | observation-clock | agent-memory | - (source tree) | simulation | unmeasurable | Structural, not executed: the live extraction prompt states the rule three times; the builder defaults the observation date to now; neither call site passes one; the public write call raises on the parameter as a hosted-tier feature, and search refuses a reference date the same way. Instrument: a stub model recording the rendered prompt, fed a message dated a year ago through the public write path |
| 2026-09-17 | instrument-per-orphan-class | dead-code | personas | experiment | not-better | **Seam chosen to falsify:** a private desktop app with no exports field or plugin surface should have no members of the new published-names class. Target = unused wire-facing exports (218) with a caller outside the TS universe: a by-name grep across Rust said 1, the real join against the bridge's method table said 0 (the hit is a duplicate the bridge re-implements; knip was right). Floor = the report unchanged (317 files / 1,152 exports). The precondition held where it could have failed. Structural: the Rust -> JS bridge direction had no join; shipped one (`2ad1cedc8`, 33/153/0, `--self-test` injects a miss), npm script + pre-push job. Return: a fleet project that publishes a plugin or library surface - re-run the class there |
| 2026-09-17 | shared-resource-arbitration | concurrent-vcs | - (the fleet's worktrees) | experiment | unmeasurable | 52 worktrees across three checkouts, five sessions live; process census found one editor extension host and zero per-worktree language servers or watch-mode checkers. CLI agent sessions spawn no per-checkout daemon (resident cost is per session: 58 runtime processes, ~24.7 GB). The shape already runs here as the shared-install junction with the worker's test run as the probe. Instrument: a worker profile that runs an LSP-backed check per worktree and keeps the server warm; then the same census before and after consolidating behind one server |
| 2026-09-18 | outcome-conditioned-cost | eval-harness | personas | code | better | **Seam chosen to falsify:** if the harness charged its own failures to the arms, its published ladder would carry them. Two paths could. The visible `error` verdict stays in the scored denominator and is zero on all 13 runs; the silent one was a judge whose failed value-extraction fell back to matching the raw reply with no mark, which flips a correct answer to stale on exactly the replies extraction exists for. Target: degraded verdicts visible. Floor: no verdict moves. HEAD vs fix over the cached answers of all 13 runs - 2,158 judgments, 0 verdict changes, 0 degraded, 521 of 522 judge calls from cache; positive control fired on a throwing and an empty extractor. **Refuted:** the worry that the ladder silently absorbed judge outages. Return: the same ownership split for the designs under test - a store's writer timing out is the arm's failure |
| 2026-09-18 | stale-served-versus-stale-answered | agent-memory | personas | code | better | The harness reported only the wrong-old rate, which cannot say whether the store served the superseded value or the reader repeated it. Added a served-rate counted with no model in the loop, printed beside the answered rate. Target: the split is reported. Floor: no verdict moves. Re-ran one arm over identical replayed contexts - 194 of 194 consumer calls from cache, 0 of 194 verdicts changed, new section reports 92 served / 6 answered. Return: report the pair for every arm; a low-served, high-answered arm is a prompt problem no store fix reaches |
| 2026-09-18 | baseline-ladder | agent-memory | personas | experiment | better | A released self-hosted memory engine driven as a *service* rather than reimplemented as a policy, so a published number could be re-run instead of cited. 361 day-documents, 3,571 events, 194 probes, four model-budget windows. It tops the ladder (0.90 mixed read, 0.92 curated) at the highest write cost measured (~4.1 writer calls per ingested day). Its paired read differs on 5 of 194 probes, all five favouring the curated read - but three are grader strictness, so the honest reading is "not worse, cheaper". Return: the additive arm (extraction, no rewrite) is the missing neighbour that would price rewriting itself |
| 2026-09-18 | handoff-figures-carry-their-population | agent-chaining | personas | code | better | **Seam chosen to falsify:** if chains never fanned out, the path-carried total would equal the cascade total and nothing would move. The cost ceiling (documented as halting "the whole cascade") compared the parent path's running total, while the breadth guard beside it counted every trace row of the chain. Target: halt a fan-out whose siblings sum past the ceiling. Floor: chain 53/53, traces 6/6, crate failure set identical to HEAD (13 unrelated). A: 1.20 spent vs 1.00, next link fired. B: halts `budget_exceeded`; single-path control fires. Shipped `1ce23dde3` (not pushed). Return: in-flight (reserved) spend is still unseen; the live summary still shows the max over paths as "spent by the chain" |
| 2026-09-18 | one-server-many-registrations | mcp-tools | personas | experiment | unmeasurable | The reflect parser keys MCP tool counts on the registration segment of the name; its own expression, run on both forms, splits one product into 2 keys. Live: 0 plugin-scoped calls in 10,294 on this machine, so latent. No fix: the plugin segment joins two names with a separator either may contain, so parsing cannot recover the server. Instrument: `rg -o mcp__plugin_` over the transcript store returning a row; then build B from the plugin manifest's server keys |
| 2026-09-18 | capability-coverage-contract | agent-instruction-files | ai-registry | code | better | **Seam chosen to falsify:** had the weekly CI cron run `upstream-check.mjs`, the intake skill's "reads these on a clock" would be true; `knowledge.yml` (Mon 06:17) runs integrity and currency only, and no other scheduler exists. False forward claims in the registry's skills: 1 before, 0 after (intake 2.11.1). Floor: intake passes check-skills. A grep of 7 fleet trees found no other unbacked cadence claim. Return: when the cron wrapper is built, the sentence gains its mechanism |
| 2026-09-18 | live-catalog-edit-is-a-lifecycle-event | plan-entitlements | kp | code | better | **Seam chosen to falsify** the draft's claim that a code catalog escapes the bulk downgrade; it did not, and the technique was corrected before landing. `275141050` capped every paid tier where it had been unlimited, live at deploy, and rewrote the pinned tests to derive from the catalog. Target: narrowings surfaced before merge. Floor: 0 flags on widening, adding or retiring transitions. A: 0 of 5 transitions flagged. B: acknowledged-baseline detector over 6 versions flags that transition only (8 caps, 4 tiers). Mutation on HEAD red on a narrowing, silent on a widening. Shipped `83085b027` (not pushed). Return: hosted `billing_state` rows for 2026-08-16 would count who was actually narrowed; an enforced effective date needs the allowance ledger keyed on the paid period |
| 2026-09-18 | pack-existing-vs-smart-unwrap | mesh-finishing-for-engine-readiness | pof | render | unmeasurable | **Seam chosen to falsify** the re-pack rule for a generator's own atlas. 7 meshes: incoming p95 1.42-1.58, projection after reduction 1.12-1.17, re-pack after reduction 1.54-1.80 (2x-off share 0.4%-3.2%); island-scale averaging barely helps. Blind operator pair at a 1024 bake (character, prop), discrimination 7.9 vs 0.0: tie, and the prop was unusable on both arms. No upper-layer change. Shipped pof `ca5eaf23` (calibration record plus row, not pushed). Return: a 2048+ bake with texel-scale close-ups or an in-engine hero view, or a learned-seam provider layout through the same grade |
| 2026-09-18 | stated-distribution-over-closed-labels | generator-uncertainty-scoring | personas | experiment | not-better | **Seam chosen to falsify**: a memory-admission verdict with withheld neighbours and cross-project distractors built to induce a wrong destructive label. Target: wrong destructive verdicts. Floor: exact-label accuracy within 2 points. A discrete 80/80, 0 destructive, 338 output tokens; B distribution 80/80, 0 destructive, 795 output tokens, 49 of 80 saturated. It refuted the use case at this seam: nothing to rank, pure cost; the boundary is written into the technique. Return: a verdict case class with measured discrete error above 5% |
| 2026-09-18 | stated-distribution-over-closed-labels | generator-uncertainty-scoring | personas | experiment | better | A strict yes-or-no model grader, 120 recorded pairs, borderline label from the harness's own strict-vs-lenient disagreement (7). Target: AUROC of 1-abs(2p-1), 0.82; band 0.2-0.8 flags 7.5% and holds 4 of 7. Floor: p>=0.5 agrees with the strict verdict on >=95%, 97.5%. Filed as next change, not shipped: it moves the instrument published rows were read through. Not hunted: the strict call reproduced its recorded verdict on 100 of 120 identical pairs. Return: a dated re-grade of a fixed pair sample, n>=2 reruns per arm |
| 2026-09-20 | catalog-projection-modes | mcp-tools | ai-registry | experiment | not-better | **Seam chosen to falsify** the source's headline proposal, and it refused the simple story twice. Question: should this registry gate its own capability catalog behind a per-request selector? Measured catalog: 70 packages, 40,003 chars, ~10,001 tokens, in the standing prefix. Target: per-turn prefix cost. Floor: capability reach (arm A is exact - every package is named). Three placements priced with this bundle's own break-even arithmetic, swept over prompt 20k-400k, r 0.05-1.0, w 1.0-2.0. A (keep) = `prompt x r`. B1 (per-request, rewritten in place) loses everywhere a cache exists - 112,686 vs 10,000 at a 100k prompt, and the penalty grows with prompt size; it wins in exactly one regime, `r = 1`, i.e. no prefix cache at all, which is the discriminator the amendment now states. B2 (delete from prefix, inject the selection last) saves 813 base-units per turn, FLAT across every prompt size - a fixed block yields a fixed discount. Prize is 10% of the face value the source quotes. Verdict `not-better` on the two-number rule: the target moves 8% of A while the floor gives up the selector's miss rate, and the only published figure for a selector at this catalog size still mis-picks about one turn in fourteen. No change shipped; the finding is that the tokens a standing catalog occupies are the cheapest in the prompt. Return: a selector measured against this registry's own capability set, which would move the floor in either direction |
| 2026-09-20 | admissibility-before-fit-and-effort | zero-budget-channel-planning | systedo-case | experiment | better | **Seam chosen to falsify**, with the pre-check written first: if the tree's channel list came from a closed catalog the finding would not apply here and the technique's own procedure step 1 would become its boundary; if the list is open the gate has a live seam. The list is open, and the tree confirms it structurally - the sole normalizer coerces `category` against a closed eight-value set, coerces `effort`, clamps `fit` to a range, and length-bounds `name` and nothing more. Three of four descriptive fields constrained to a domain; the one that says what the channel IS takes any 120 characters, so an inadmissible channel is not rejected but filed quietly as `partnership` or defaulted to `content`. Arms over the tree's own curated catalog (23 real channels) plus one constructed enrolled link supply. Target: enrolled supply flagged for the gate's question, 1 of 1. Floor: curated channels refused, 0 of 23 - all 23 still reach the plan and zero were even questioned. The floor was genuinely at risk: three curated channels sit on the boundary, including the cooperate-with-the-businesses-next-door channel the golden path itself names as legitimate. None was taken, and the discrimination is carried by the conjunct rather than the keyword - of the 23, exactly three promise links at all and every one is `medium` effort. Ship 0: the gate's output is a question and the tree has no advisory field, so rendering it is a direction rather than coverage and waits for the owner. Return: a run against the live generator with a grounding that invites an enrolled supply, which would measure how often the open door is walked through |
| 2026-09-20 | announcing-versus-silent-breakage | release-pipeline | personas | code | better | **Seam chosen to falsify, and the first candidate refuted the hunt.** Looking for a dependence on an order nothing promised, the spend rollup turned out correct: two queries with no ordering clause, then an explicit sort on the full grouping key across both ledgers - a TOTAL comparator, with a comment explaining why the sort lives there. No bug. So the arm became the falsifiable question - if that comparator went non-total, would anything notice? A CAUGHT outcome would have said a well-written suite is already the technique's third aid and the rule is redundant. Six shipped tests, four comparator arms, a perturbation flag modelling a different engine's arrival order, and a known-positive control. Target: tests failing when the sort goes non-total while row count, values and multiset all hold - predicted ~0, **measured 0 of 6 on two independent weakenings** (dropping the origin tiebreak, dropping the ledger tiebreak), each with a provably different emitted sequence. Floor: arm A green AND the perturbation provably firing - both held, the total comparator is byte-identical under a perturbed arrival order and the weakened one is not, so a green arm is not a harness that never ran. Control: dropping the day term failed 1 test, proving the rig can see an ordering change at all. Five of the six tests assert by keyed lookup and the sixth reads one row; the module's single ordering assertion covers one comparator term of three, one-sidedly (under the control with rows reversed it passes by luck). Shipped `3dd60c22d` on master, not pushed: comparator extracted as a named function carrying its totality contract, plus one perturbation test verified to pass on the shipped comparator and fail on all three weakenings. Proof is `ab-paired` on a standalone copy, NOT a green suite - that crate's `cargo check --lib` is red at HEAD for an unrelated pre-existing build-script reason, and the row says so rather than upgrading. Return: the technique's second aid (a consumption-pattern probe) is unbuilt - 41 unordered aggregations in that tree, 13 reaching a sequence, 12 never examined |
| 2026-09-20 | normalization (many-cohorts amendment) | scoring-rubrics | ai-registry (the join instrument itself) | code | better | **Seam chosen to falsify.** The change can only ever LOWER an admission bar, so recall was never the thing at risk and an arm aimed at it would have been theatre; the falsifiable question is precision, and a CAUGHT outcome - recall bought by admitting junk - would have killed the amendment outright. Target: pairs the context map can see, fleet-wide over 13 checkouts and 997 contexts, **4208 -> 4689 (+481, +11.4%)**, with **461 of 481 recovered pairs `use_when`-grounded** rather than lexical. The named prediction held: the subject a project's frame-composition file cites BY NAME in its own comments, with two human-gated A/B cycles recorded against it, rejoins that context at 732.3 after being absent from the whole map. Floor declared first and held: **0 pairs lost**, contexts with no map at all unchanged at 6, max pairs per context unchanged at 9, and the corpus-wide lexical-only share **improved** 6.18% -> 5.99% - the recovered pairs are better grounded than the population they joined. Contexts above the nominal TOP cap go 35 -> 41, a pre-existing retained-verdict merge behaviour present in BOTH arms and named as out of scope. **The measurement refuted itself once and that is the reusable half**: a first pass reported 11 then 17 pairs LOST, which is structurally impossible for a bar that only lowers - two sibling intake runs had committed to the corpus between the arm-A build and the arm-B build. Interleaving the arms per project, seconds apart, returned 0 lost. In a shared checkout a paired measurement must interleave its arms or it measures the neighbours. Return: the TOP cap / retained-verdict interaction is unexamined |
| 2026-09-20 | failure-not-empty-success (law) | eval-harness | gravity | code | better | Originated by USING a fleet instrument rather than by reading it - the run's own render proof went through that project's paired video A/B harness, and its annotator was down. The harness cached a readback entry whose every tag held only an error, its resume guard is `key not in readbacks`, so the failed read was permanent and invisible, and the run still printed `4/4 read back`. Paired on that same cycle with the annotator genuinely unreachable, so both arms ran the real path. Target: entries cached as complete while holding zero successful reads, and whether resume retries them - HEAD **4 cached / 0 successful / reported `4/4` / 0 retried**; fixed **0 cached / reported `0/4` / 4 retried**. Floor: one seeded successful entry is still cached, still skipped on resume (3 in the work list, not 4), and still counted `1/4` - the success path is byte-unchanged. Shipped `24a3c78` with a pathspec on the project's active branch, not pushed. Return: the guard that should have refused before the loop ran did not, and was left alone - a second row when someone asks why the preflight passed with the engine down |
| 2026-09-20 | vision-model-grading-schema | generated-output-grading | pof | experiment | not-better | **Seam chosen to falsify** the fourth-remedy amendment, and it refuted it at this seam for the reason the amendment's own saturation caveat names. Seam: `src/lib/anim-critique/parse.ts` clamps six bare integer dimensions and `score.ts` thresholds them at `passAt`/`warnAt`; the input gate runs `passAt: 7`. CORRECTED 2026-09-20: this row claimed no self-hosted serving; that was an instrument failure (`git grep -l` with `-n`). pof DOES carry a local eye and the free branch IS reachable - see the measured re-run below. Target: false passes caught by a borderline band. Floor: false fails introduced (0 allowed - the gate guards image-to-3D credits). The tree's own measured benchmark decides it: 40 scored calls per model over two blocks, and **every error across every model in both blocks was a false PASS**, scoring a confident 10/10 on concepts whose arms are buried in drapery or hair. A band around 7 cannot rank a saturated 10, so the mechanism has nothing to work with here - this is the corpus's characteristic failure (confidently, stably wrong scores high), arriving as a measurement rather than as a caution. No change shipped. Return: a dimension whose errors sit NEAR the threshold rather than at the ceiling, or a self-hosted grader in the tree, which would move this to the free branch and make recording the distribution costless |
| 2026-09-20 | vision-model-grading-schema | generated-output-grading | pof | experiment | not-better | **The measured re-run of the row above, whose structural premise was wrong.** pof DOES carry a self-hosted eye (`src/lib/vision/providers/ollama.ts`, `OLLAMA_HOST` + `OLLAMA_VISION_MODEL`); the earlier row's "no self-hosted serving" came from a `git grep -l` run with `-n`, which silently collapses the result. Corrected and re-tested against the real thing. Instrument probe first: the local daemon returns per-token `logprobs` with `top_logprobs`, so the free branch is real - but NOT on the configured model. `qwen3.8:27b` returns exactly 1 logprob token for a 33-token answer (text-only and with an image alike); `gemma4:12b` returns the full sequence. 18 labelled concept images (10 single-subject creatures, 8 maps/wireframes/sheets, labelled by construction), pof's own `buildInputGatePrompt` verbatim, one call per image, both arms read from the SAME response - zero added cost, ~4s per image. Target: does borderline-ness rank the gate's errors. Floor: arm B's argmax must equal arm A's stated integer. **Floor held 18/18.** **Pre-declared falsifier (saturation) did NOT fire** - median pMax 0.705, 1 of 18 above 0.99, so the band had room. **The hypothesis failed anyway, for the other reason:** borderline-ness ranked the gate's own errors at AUROC 0.542, a coin flip, and the probability-weighted mean REDUCED good/bad separation against the stated integer (0.675 vs 0.700). Both false passes were held confidently - a UI wireframe, unusable as a mesh input, scored 9 with 0.64 of the mass on 9. This is the subject's characteristic failure (confidently wrong scores high) measured in the vision lane. Landed as the correlation admission test plus two traps: a 0-10 scale makes `10` two tokens and reads as `1` (it produced an apparent 8-point correction that was pure artifact, caught before it was believed), and swapping the resident grader failed all 10 correct inputs at the unchanged `passAt: 7`. No pof change shipped; the experiment is read-only. Return: a field whose errors are NOT confident, or a grader whose borderline AUROC clears chance on labelled cells |
| 2026-09-20 | conform-target-is-not-a-remap-row | mesh-finishing-for-engine-readiness | pof | code | better | **Seam chosen to falsify, with the caught-outcome value written first:** if `rig-presets.ts` already separated the two kinds of target, the technique is a catch and the registry landing demotes to an amendment. It did not. Measured arm A with the row count as a known positive - `metahuman` declares 584 bones and 5 IK chains, carries 0 mapping rows, and leaves 10 of 10 required chain endpoints unmapped, which by the corpus's own remap rule is five limbs that will not animate. It is not: the row is a conform target whose table is empty BY CONSTRUCTION, and the number is an artifact of asking a remap question. **The structural fact nobody designed** carried it - a census of the field's readers returns three, each blind differently: the renderer guarded the section on `mixamoMapping.length > 0` so the one preset with no mapping displayed no mapping problem; two sibling suites flat-map every preset's rows, so a preset contributing zero rows cannot fail an assertion built from them; and the armature builder ignores the field, emitting the same 11 bones for presets declaring 67, 584 and 25. Law L13 with the absence load-bearing in two contradictory directions at once. Shipped `a0878ba1` (not pushed): `kind: 'remap' \| 'conform'` plus `checkPresetBinding()` branching on it, returning `unmappedChainBones: null` rather than `[]` because an empty array would claim "checked, nothing missing". Target 0/10 -> 10/10 correctly reported, plus 20/20 now verified on the two remap rows that had never been checked either. Floor 1,195 tests passing, `tsc --noEmit` exit 0. Known negative in the new suite pins arm A at 10 of 10. Return: when a fleet tree can read an exported rig's joint ORIENTATIONS - the conform row's real obligation (joints inherited, not estimated from the source mesh) is prose with no instrument behind it |
| 2026-09-20 | hybrid-lane-fusion (the item-scoped retirement predicate the blind-predicate enumeration misses) | retrieval | personas | code | better | **Seam chosen to falsify, with the caught-outcome value written first:** if the retirement filter bound on every lane, the finding is refuted here and demotes to a lead about labelling versus exclusion; if it binds on one lane only, the corpus's enumeration is missing a class and there is a shippable fix. **Caught.** Retirement in this tree is always a demotion and never a delete; the keyword lane gates on the column in SQL; the vector lane's index has no such column and its scan joins nothing, so the kind lookup that feeds the lane ranker resolved a kind for every hit including retired rows - and the two lanes are unioned under an explicit "strict superset" discipline, so the superset included the store's own retirements. **The project's own ledger was read first (the 2.13 rule) and it changed the framing:** a 2026-09-06 row fixed this exact neighbourhood on the WRITE side (procedural rules retired 0 of 133 to 68 of 131) and witnessed a superseded rule being the one answered from. The question had been asked and half-answered; the stated difference is that the write side is now correct and nobody had asked whether the read side binds. **The structural fact sits twelve lines from the defect:** the same function already re-imposes the SESSION predicate for episodes with a comment naming the rule and explaining why the vector lane needs it. An isolation predicate is an argument the caller passes, so its absence is visible at the call site; a retirement predicate is a column another subsystem sets on another clock, and that subsystem asserts in four comments that retrieval already filters it - including a test named "aged out means retrieval-ineligible" that asserts only that the column was set, which passes identically on a build where no read path consults it. Target: retired rows admitted to the vector lanes, **2 -> 0**. Floor: live rows admitted, **4 and 4**, tolerance 0, held exactly. Control: the keyword lane over the same store admits 0 retired and 2 live in BOTH arms - the known positive and the asymmetry in one number. The retired row the old arm admitted was the strongest topical match present, which is the shape of the damage: the lane surfaces it because it is on point, and only the column it did not read says the claim was withdrawn. Fixed at the kind lookup rather than per lane, because the ranker skips any id whose kind did not resolve - one predicate covers three lanes and every lane added after them. Shipped `18df2ee0b` and `c9e5a661d` with a pathspec on the project's active branch, not pushed. **A compile check of this run's own was vacuous and the feature list caught it:** the changed function is gated on the build feature that supplies the vector lane, and the first green check did not enable it - a green that could not have failed. Re-run with that feature, and again with all targets for the added read-side test: both clean. Return: run the added test once the tree's test binary can launch (a pre-existing condition recorded twice in the project's own ledger); and the four comments asserting retrieval filters the column are now true and were not when written |
| 2026-09-20 | admission-is-not-a-tier | model-routing | tracklight | experiment | not-better | **Seam chosen to falsify, with the caught-outcome value written first:** the target tree already ships an endpoint-probe module, so it is the place where this technique is likeliest to be re-derivation of known practice — had the probe covered admission, the registry landing would have been withdrawn. The predicted defect was the liveness surface: the probe caps every rung at 2s on its own client while the generation client gets 120s, so a runtime loading a large model should probe as unreachable while being perfectly usable. **Measured and refuted.** Known positive fired (an origin with nothing listening hit 2.04s, so the timer works); arm A (idle) returned 0.00s on all three rungs; arm B, taken *during* a 17 GB model load, returned 0.03s / 0.01s / 0.00s while the concurrent generation completed in 6.9s, so the floor held and the endpoint was genuinely usable throughout. This runtime serves metadata off a path independent of the model loader; the 2s cap is well chosen and a run that had assumed the defect would have loosened a bound doing its job. **What the seam returned instead is better than what it was aimed at.** The probe resolves identity from metadata routes only and never issues a generation — correct for its stated purpose of attributing a benchmark row, and exactly why "which request fields will this endpoint accept" is answered nowhere before a run. It is answered during one: the deterministic entry point catches a bad-request rejection and re-issues the call schema-less **and non-deterministic**, dropping the pinned temperature and seed together with the schema, while the judge path documents determinism as the property that makes a verdict *a measurement*. So the function whose name promises determinism can silently stop being deterministic on a rejection about a field that has nothing to do with the answer, and the stored row is shaped identically to one drawn under the pin — the admission failure loud on stderr, the degraded contract it leaves behind silent in the artifact. No code shipped: carrying the degradation into the outcome is a type change across an engine crate with several call sites, not a diff a reviewer reads in one sitting, so it is filed as the project's next change. Return: when the outcome type can carry the contract a row was drawn under, the measurable is the share of stored rows whose determinism is knowable from the row itself — currently zero for rows that took the fallback, and indistinguishable from 100% |
| 2026-09-21 | response-reservation-sizing | prompt-assembly | tracklight | task | not-better | **Seam chosen to falsify, with the caught-outcome value written first:** this tree sets its own output ceiling deliberately rather than taking a platform default, so it is where the technique is likeliest to be re-derivation — had the ceiling been derived from the observed distribution, the landing would have demoted to an amendment about defaults. **The technique's headline is refuted here and its precondition confirmed.** The engine's 4,096 is a chosen ceiling with its reasoning written beside it, and the escalation to 64,000 at the top two effort levels is justified by intermediate reasoning being billed against the same cap. Batch input is bounded at ~50k tokens against a much larger window, so the reserved room never competes with the input side: the recurring cost this technique exists to recover is **zero** in this tree, which is the technique's own "when not to use it" clause arriving as a measured result rather than a hedge. **What the seam returned instead is the run's best finding.** Two crates size the same response and neither reads the other: the runner's packer derives a per-dimension output cost from a measured failure and enforces a 16,000-token response budget when deciding how many cases share a call — its comment states the enforcement exists because overrunning the response loses every case in the batch at once — while the engine puts 4,096 on the wire below xhigh effort. A **3.91x overcommit**, arithmetic over two constants at 220 tokens/dimension/case: at 1 LLM dimension the packer packs 72 cases where the wire fits 18, at 2 dimensions 36 against 9, at 3 dimensions 24 against 6, at 6 dimensions 12 against 3. The failure produced is verbatim the one the packer's comment says the enforcement prevents. **Structural fact nobody designed:** the request type carries system, turns, schema, tools and tool choice and has **no output-ceiling field at all**, so the packer's computed budget has no channel to the request — the engine's constant is not a lazy default but the only thing the type permits, and a reviewer reading either crate alone sees a careful, correctly derived number. This sharpens `limits-are-derived`, whose stated failure is a formula that no longer tracks its input: here the formula is computed, tracks its input faithfully, and is derived from the wrong input. Scope stated so the finding is not larger than its evidence — binds only on the bare-API provider path, below the top two effort levels, at batches past the per-dimension line; at xhigh/max the 64,000 ceiling closes the hole, which is why it has stayed open. No code shipped: the fix is a request-type change threaded through provider paths with differing ceiling semantics, larger than a diff a reviewer reads in one sitting. Filed as a task in the project's own directions with both options, the measurable (no batch planned whose projected response exceeds the ceiling it will be sent with), the floor (xhigh/max batch sizes unchanged, no new truncation on any path) and the gate; first step deliberately not taken, because a test asserting the invariant is red until the fix lands and committing a red test to a shared main is not a run's call. Return: when the request type can carry a caller-supplied ceiling, the measurable is the share of planned batches whose projection exceeds the wire ceiling — currently every batch above the per-dimension line, and invisible from either crate alone |
| 2026-09-21 | operator-tier-code-loading (amendment: the tier rule is about authority, not about code) | agent-runtime-assembly | fleet (6 projects) | experiment | not-better | The amendment says a configuration key whose value a standing permission carve-out consumes acquires that carve-out's authority, so it belongs to the startup tier even though it names no code — and that the technique's mechanical test, which writes a *code-naming* key from the untrusted tier, returns green on a runtime compromised this way. **The apply asks whether the fleet has the shape, and the answer is a clean structural no.** Read every managed project's harness settings with one predicate — permission rules scoped to a path — across six trees. Result: **zero allow rules in all six**; every project carries only deny rules (3 or 4 each). The attack the amendment describes needs a standing *allow* carve-out whose scope a configurable key determines, and this fleet grants no standing allows at all: every write is either denied outright or approved interactively per call. So there is nothing to harden and the technique cannot improve these trees — `not-better` for the honest reason rather than the flattering one. **The structural fact is that nobody designed this defence against this attack.** The deny-only posture is a general stance about interactive approval, adopted for unrelated reasons, and it happens to close the entire class: a carve-out that does not exist cannot have its scope redirected. That is worth recording precisely because it is incidental — the protection would disappear the first time a project adds one path-scoped allow rule to stop being asked about a scratch directory, and nothing in any of the six trees would flag that as a security change. Known negative carried: the predicate was run against the deny lists too and returned rows there, so an empty allow result is an absence in the data rather than a broken reader. Return: when any managed project adds a path-scoped permission allow rule, re-run the predicate and test whether the key that scopes it is writable from the repository tier |
| 2026-09-23 | action-catalog-single-source (the pin holds per producer; every executor arm claimed by some producer) | companion-runtime | personas | experiment | better | **Seam chosen to falsify, value stated first:** if a per-producer check flagged any of the 56 model-accepted kinds, or cleared all 5 extra executor arms, it adds nothing over whole-executor equality and the landing demotes to a lead. Neither. A (executor arms must equal the accepted list) flags 5; B (a producer column: the model's grammar, its connector arm, five host paths that file approvals) flags 2 of those 5 and 0 of the 56 - the other 3 are claimed by real non-model producers (the kp bridge, the night plan, the connector write path). The 2 unclaimed differ: a fallback kept for rows filed before the kind became auto-fire (0 such rows of 120 live), and an arm with no producer since the day it was added, which a frontend composer prompt still teaches the model to use and the dispatcher rejects as unknown; two project docs disagree about who files it. Taught-and-accepted held per producer (55 approval-gated op docs = 55 accepted; the 56th taught as system-filed). **Structural fact:** the list guard deleted in a dead-code sweep had recorded that kind as dead in a comment, and the comment went with the list. Controls: named, last and block-bodied arms found; an auto-fire kind not found in the accepted list; the catch-all not read as a kind. No floor (no test ran). Refinement banked: the producer column wants a *retired* value. Return: when the dead arm is removed or gains a producer, and when the fallback's last row drains |
| 2026-09-23 | action-catalog-single-source (label surfaces exhaustive when shipped with the table, tolerant when not) | companion-runtime | personas | experiment | better | **Seam chosen to falsify, value stated first:** the surface ships in the same binary as the table, so the skew channel is a stored approval row outliving a release; retired kinds in the store would put a condition on "cannot be out of step". The switch has 36 cases; 22 of 56 accepted kinds and 25 of 59 carded kinds have none and render as a title-cased slug - the plausible guess the technique rules out; the file header promises an unknown key the code never uses. In operation 18 of 120 live rows (5 of 16 kinds used) carded through the fallback. **Falsifier survived:** rows do cross releases (8 pending 44 days), but 0 of 120 carry a kind outside the table, and approvals are excluded from the data export, so none arrive from another install. A hides the 22; B (a generated exhaustive union) makes them 22 build errors. Controls: a named and the last case found; a neighbouring function's case not read. Floor not run. Return: when a kind is retired while rows of it are pending - the first live test of the tolerant arm |
| 2026-09-23 | action-catalog-single-source (undo, first-run default and uncalled restore are each half of a reset) | companion-runtime | personas | simulation | better | **Seam chosen to falsify, value stated first:** if any sequence of existing controls reaches the known-good layout, "half a reset" is refuted; the candidate was removing widgets one by one. Three real cases from code and the live store. (1) The dashboard's restore swaps with one backup slot - its own test asserts the swap swaps back - so after two bad compositions it trades one for the other; it is dead code awaiting a command never added. (2) The operator's cockpit was composed weeks before the deterministic default shipped; the default renders only while no composition exists, so it has never been shown and no control reaches it. (3) The falsifier: removing every widget saves an empty composition, not none, so the panel shows an empty grid labelled as composed - a third state, neither default nor empty-state prompt. **Structural fact:** the fallback checks "no composition", not "no widgets", so the one reachable path cannot reach it. Falsified if an empty list rendered the default or a reset command existed; neither. Return: when a reset command lands, whether an operator who has composed reaches the default in one action |
| 2026-09-23 | host-seam-contracts (the embedded store's throwaway instance is its own best double) | companion-runtime | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** if the whole store is reached through the handle a test opens, the rule holds; if part is reached another way, it gains a condition. **Caught.** The database half confirms it outright: 104 throwaway-real-database opens across 28 companion files, each a unique temp file with production pragmas and schema, 0 hand-written store doubles. The markdown brain half is resolved from a process-wide environment variable that falls back to the operator's real home when unset. One test binary holds three writer populations to it - the brain module's lock, a data-portability lock, and 7 voice-engine tests with no lock, making 14 writes of which 7 clear it; the brain module's comment records two of the three. Controls: a guarded writer classified guarded, a lock-free writer unguarded; destructor-held locks corrected by hand. Floor not run (the tree's test binary does not launch here, per its own ledger). This row does not show the race happened - the sibling identity row found its consequence on the install. **Condition written into the technique:** the throwaway instance is a sufficient double only while the whole store is reached through the handle; the half found through process-global state needs its location on the handle, and until then one lock for every writer. Return: brain root on the handle, or lock-free writers 7 -> 0 |
| 2026-09-23 | host-seam-contracts (package boundary as the cheapest direction check; the data-layer edge it cannot see) | companion-runtime | personas | experiment | better | **Seam chosen to falsify, value stated first:** trivial host reaches would make "cheapest check" a claim about a cost with nothing to cut; a refused set dominating the data-layer edge would make the technique's caveat unfounded. 25 of 152 companion files reach modules only the host package has (command handlers 16, app state 6, browser bridge 6, notifications 3, host-local engine 2, web build 2), all production code; 15 more reach only the UI framework, which an extracted crate may take (the pre-run estimate of 41 counted those as host). **The caveat measured:** 100 of 152 reach the data layer directly - an acyclic edge the build keeps after extraction. A: 0 reaches visible to the build now; B: 25 refused, 100 unseen - the build would see about a quarter of what the direction rule is about. **Structural fact:** a four-crate workspace already exists, split for build parallelism, and its manifest keeps the companion in the host as "what's left", so the import count is the only instrument, as the technique says. Controls: the dispatcher's reach into command handlers found; a pure utility shows none. No floor (no build ran). Return: an allowed-edges test over the dependency graph, or the companion leaving the host crate |
| 2026-09-23 | anchored-identity-diffs (self-model diffs sit outside blanket consent; name who resolves each door in every mode) | companion-identity | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** if the autopilot already deferred the identity op, the gap claim dies; if realized unreviewed writes came through the autopilot, the rule is confirmed on traffic; if they came from somewhere the door list cannot reach, the door test gains a condition. The third. Structural audit, a sentence-scoped parse of the shipped law against the resolver: of 7 ops the law says never auto-fire, 1 agrees and 6 conflict (the identity op, both dev-mode ops, fleet broadcast and redirect fire with no human; fleet kill on its own gate); the whole-content identity mode fires too. Controls: credential reconnect and browser ops defer; an ordinary fact write fires. B (unconditional defer for the identity op) takes the identity conflict 1 -> 0 and leaves the other 55 ops unchanged (floor held). **Target on real traffic, autonomous mode on:** A 26 unreviewed self-model writes across 4 days, 0 via the autopilot (0 identity approvals of 120; 0 approvals of any kind in 66 turns since the allowlist went); B 26 - the rule moves none. The live self-model has been a data-portability test fixture literal since 2026-09-20. **Structural fact:** the brain root is process-global; of 6 files that set or clear it, 3 take no lock and 1 a private one, so tests write the operator's real brain, and a door list over product call sites finds 4 doors and cannot see this writer. The fixture match proves the last write; the rest is inferred from timing and the lock census. **Condition written into the technique:** enumerate doors over every process that resolves the document's location. Falsifier: an identity approval auto-resolved under autonomy once test writes stop. Return: rerun after one lock or an injected root, on an install with identity proposals under autonomy |
| 2026-09-23 | constitution-self-model-split (keep the product's grammar out of the person's law; fingerprint the shipped text; the law's upstream is part of the law) | companion-identity | personas | experiment | better | **Seam chosen to falsify, value stated first:** bumps dominated by durable law, or backups holding the person's edits, would demote layering below the ladder's rungs. Neither. Over git: 57 version bumps in 143 days; lexically 21 grammar-only, 32 mixed, 4 law-only, and reading the 4, 3 are feature procedures - at most 1 of 57 is durable law. Known positive: a one-op-line bump classifies grammar-only. **Target:** 20 template commits changed the text without a bump, 17 of them op lines - grammar A delivers only at the next bump and B ships with the binary (17 -> 0); person's file rewritten per bump A 57, B at most 1. **Floor held, and refuted the landing's worst case on this install:** 39 of 39 backups byte-identical to a shipped text, so 0 amendments displaced in either arm - nothing was written, and there is no in-product law editor. A fingerprint would have made all 39 silent; 3 re-fired over already-current text (stamp in the database, file in the home folder; inferred). Backup result discarded, failure path unobserved. Upstream: both dev-mode ops fire under autonomy, so the law's source is writable without a click. Falsifier: a backup matching no shipped text. Return: an in-product law editor, or any install with an amended file |
| 2026-09-23 | disk-truth-db-index (the document is written first; index-only state judged by what its reset would falsify) | companion-identity | personas | experiment | better | **Seam chosen to falsify, value stated first:** the tree argues that claiming the index first is "the safe direction"; had that held, document-first would owe a condition for stores with no rebuild. It did not. A = the order extracted from source by script (row claim, document, importance stamp; three writes, no transaction); B = guard read, document, claim plus stamp in one transaction. A fault-injection **model** over every boundary with one caller retry, reading the backlog list, the retrieval gate and the document status. Fault points leaving a surface false after retry: no rebuild A 2/3, B 0/3; with a rebuild A 1/3, B 0/3. Floor (no-fault identical, double resolve refused) held. Controls: no fault agrees everywhere in both arms; a document failure in A leaves the item retrievable, pending, and refused on retry - permanent. **Structural fact:** the "index" is two tables written either side of the document, and the retrieval exit is last, so the stated safety never existed. The reminder counter is index-only and no rebuild exists, so "the rebuild names what it resets" is unmeasurable. Not hunted: 2 documents with no index row are test-fixture promises a rebuild would import. A model of the extracted order, not an executed run. Return: when a rebuild from disk lands, check it names what it resets and what it adds |
| 2026-09-23 | instance-upgrade (the stamp is the merge base; the out-of-date offer must be seen to fire) | templates-scaffolding | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** if adopted recipe use cases carried nothing an adopter can edit, or edits survived remove-then-adopt, the merge base is ceremony here; if the offer cannot fire, the accept path is never reached. Prior: one earlier row on this subject in the project, on a different seam. The tree's own adoption mapping ran over the 132 shipped recipes and 9 real content revisions from history: deterministic 132/132 once the minted use-case id is masked, so a base could be regenerated, but the stamp holds id and version and no answers (278 bindings across 119 recipes dropped). Edit surfaces reaching an adopted use case: 3 (one with a UI caller). A (remove-then-adopt) loses 27/27 walked edits; B (regenerated base, three-way) keeps 27/27; both deliver the upstream change; 0 conflicts. Controls: with no edit A == B; the disabled toggle comes back enabled under A. Tuned parameters do survive A through an existing-keys-win merge, and by the same rule a revised default never arrives. Trigger rows keyed to the old use-case id are neither removed nor re-keyed on re-adopt, though the confirm text says they are removed. **Caught:** the offer fires for 0/132 at HEAD. Over 32 commits of the seed bundle, 783 payload changes moved the version 0 times (the only version ever shipped is 1.0.0), because the boot seeder rewrites built-in content in place without a bump; the technique's fire test exists and passes by hand-setting a fixture version. Only 1 of 9 revisions changes the copied instance; 2 change the input schema, read by reference. **Condition written into the technique:** the fire test moves the version through the publisher's own content-change path, not a hand-set fixture. Floor not run. Return: when a content change bumps the version through the seeder or corpus, re-count offers fired and edits kept |
| 2026-09-23 | readiness-prerequisites (the adoption gate reaches only what is absent at adoption; later health belongs to run time) | templates-scaffolding | personas | experiment | better | **Seam chosen to falsify, value stated first:** if a credential failing after adoption turned the adoption gate red, the reach split is wrong and run-time health redundant; if the run-time half were missing, the technique names a gap nothing fills. Prior: the project's 2026-09-03 simulation recorded its native resolver unified so adoption and promote stop disagreeing. The tree's adoption gate over the 38 published templates: 27 carry a credential-gated question, 63 in all. Controls: an empty vault blocks 63/63; a present credential clears 63/63. A present-but-failed credential turns the gate red 0/63 - the gate's only input is a set of service types. The run-time half exists and re-reads live usability (fields present, last check not failed, success newer than the last field edit); credential edits recompute bound personas. **Falsifier did not hold.** **Refinement written into the technique:** the reach is set by the predicate the gate reads, not only when it runs - a presence-only gate also passes a credential already failed at adoption, so the adoption gate should read the run-time predicate. Also found: 13 of the 63 blocked questions carry a template default that answers them, so the gate lets them through with no credential. Floor not run. Return: when the adoption gate reads the run-time usability predicate, re-count failed-at-adoption passes (target 0) |
| 2026-09-23 | readiness-prerequisites (a browse badge names the question it asked; Ready means the gate passes on the default path) | templates-scaffolding | personas | experiment | better | **Seam chosen to falsify, value stated first:** zero Ready cards over a blocking gate would make the stated-question rule ceremony here. The tree's browse badge and adoption gate over the 38 published templates on the default path (defaults and auto-answers applied): empty vault, 5 cards read Ready and 2 are blocked by the gate; a vault built so every badge reads Ready, 38/38 Ready and 4 blocked. The safe direction is also present: 12 of 33 not-Ready cards would pass the gate with an empty vault. Controls: a synthetic card with one required vault question and no connectors reads Ready and is blocked; a template with no gated question reads Ready and passes. **Structural fact:** badge and gate read two different category vocabularies - the badge a primary-category map where messaging always counts as ready, the gate each connector's own tags - and the gallery's readiness sort and filter inherit the badge. **Falsifier did not hold** (4, not 0). The second arm's vault is constructed, not a user's. Floor not run. Return: when the badge asks the gate's own evaluator a stated question, re-count (target 0) |
| 2026-09-23 | integrity-and-provenance (timing absence is a fourth state, pending, bounded) | templates-scaffolding | personas | experiment | better | **Seam chosen to falsify, value stated first:** if the no-digest window could never paint at start-up, pending is a display for a case that does not occur. The tree's start-up order replayed against its real verifier over the 38 published built-ins: rows from the last session are painted before the seed pass runs (deferred behind idle), and expected digests exist only once that pass computes them. Before the digest, 38/38 read verified and a tampered row reads verified too; after it, tampered reads untrusted and untouched verified. **Falsifier did not hold:** the window appears on every cold start with the gallery mounted, folded into the success mark. **Not hunted, the mirror:** the pass registers new digests before its write lands, so after an update stale rows are accused until the refetch - 18 of 37 built-ins upgrading from a 2026-06-01 build, 1 of 38 from 2026-07-01, 0 from 2026-08-01 - and a failed write is swallowed, so the accusation lasts the session. Two timing absences, folded opposite ways; one bounded pending state covers both. **Structural fact:** a build-time checksum of every catalog entry already ships in the binary, over the entry rather than the seeded payload. A replay of the verifier's calls, not a rendered gallery. Floor not run. Return: when a pending state or a build-time expected payload digest lands, re-run (target 0 verified-without-evidence, 0 accused-by-timing) |
| 2026-09-23 | catalog-as-data (the door serves two reads, offer and resolve) | connector-catalog | personas | experiment | better | **Seam chosen to falsify, value stated first:** if no identity lookup can hold a gated row's key, moving the predicate changes nothing and the rule has no reach here. **Caught, in part.** The only gated row (1 of 135) is a global-probe singleton with no credential fields, none of its type is seeded, and the zero-config form cancels rather than minting one - so 10 of 17 identity lookups (9 credential-keyed, 1 constant-keyed) can never lose it, and the application's "an existing credential loses its row" did not hold (corrected). The loss travels through declared references: the build session offers all 135 rows to the model unfiltered, so a persona can name the gated connector on an install with no vault; the back end answers "needs a vault" while the front end loses the row. A (predicate on the only list): 7 of 17 lookups return nothing for the gated key - 6 declared-reference reads and the design flow's mint pre-check, which then hits the store's duplicate-name refusal where it should have reused the row; rendered as a raw slug, generic icon, "Connector not installed" for a shipped row, category "unknown", and an add-key click that does nothing. B (unfiltered resolve read plus offer-only selector): 0. Controls: non-gated key differs 0; gated key with the plugin ready differs 0; forced credential reachability ceiling 17. Floor (gated row absent from every offer surface) not measured - the selector has 0 callers. **Refinement written into the technique:** which resolve readers are at risk depends on what can name a gated row; credential-less gated rows lose declared references, sharpest where a generator is offered every row outside the door. Not hunted: rehydration flips the gate without refetching the door. Return: a gated row with credential fields ships, or the offer readers move to the selector |
| 2026-09-23 | schema-driven-forms (no probe is a third outcome, not a green and not a lock) | connector-catalog | personas | experiment | better | **Seam chosen to falsify, value stated first:** if a no-probe credential reached no green anywhere, the rule's warning about boolean projections is moot here. **Caught.** 115 rows declare fields, 13 have no probe, 0 of those 13 carry an explicit no-probe declaration; 3 end Verified through a desktop install check that reads no field, 10 end Unverifiable. The main form gates Save behind a test for every row, and on a row with no recipe the no-recipe result's compatibility boolean (true) opens it - neither skipped nor locked. The save requests no server re-probe, so the credential stays untested (0 of 10 consumers draw it as passed) until the daily sweep, which stores the unverifiable state and the boolean as true. After that, A: 7 of 10 consumers draw it as passed - three visual (a graph node stroke, a failover check icon, a playground banner reading "Verified"), two ordering (failover rank, list health sort), two back-end texts (a build tool test "passed", key rotation "verified healthy"); 3 of 10 read the state. B (consumers read the state token): 0. Controls: a verified record drawn as passed by 10 of 10, a failed one by 0. Floor (verified credentials still drawn as passed) held in the model. **Structural fact:** state and boolean are persisted side by side, and each stored fallback builds a result from the boolean alone - the credential-vault subject's fourth narrowing, sighted independently. Return: the boolean stops being persisted true for unverifiable, or all 7 consumers read the state |
| 2026-09-23 | catalog-as-data (minting is a checked act: the store refuses a used key) | connector-catalog | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** if two labels that normalize alike were admitted as one identity, the rule is confirmed on a live store; if the second mint is refused, the gap the research pass predicted does not exist. **Refuted.** The tree's own slug function gives the same key for "My API" and "my-api" (and "Stripe"/"stripe"), and the second mint is rejected by an exact-name existence check inside an immediate write transaction at the store door (added 2026-06-17), with no schema uniqueness constraint behind it. Both runtime mint paths pass that door, so a lookup by key meets one row. A: 0 colliding mints admitted; B (a uniqueness constraint): 0. Controls: a distinct pair does not collide; an exact duplicate is refused. **Residue:** the shipped-row seeder inserts keyed on id, not name, and bypasses the check; 1 of 135 shipped names lies inside a user mint namespace, and the reverse order is unchecked. Pre-check-era duplicates are unmeasurable without opening the store (not opened). **Condition written into the technique:** where every runtime mint passes one door checking the key in the same write transaction, a schema constraint changes nothing for those mints; the exposure is writers keyed on another column, closed by keeping shipped names out of minted namespaces or checking the name there too. Return: a shipped name enters a mint namespace after users could mint it, or a second runtime mint path appears |
| 2026-09-23 | catalog-lifecycle (the lifecycle has a front end: surfaces derive from the stage) | connector-catalog | ascent | experiment | better | **Confirmation row, not independent evidence:** the landing's application was written from this tree. **Seam chosen to falsify, value stated first:** any surface offering an action for the planned row, or reading an id instead of the stage, refutes "the stage decides". None on the page: 3 of 3 surfaces (connect action, first-action copy, badge) read the stage, 0 test a literal id. A (the dispatch before the fix, from history): 1 of 2 available rows had no connect surface. B (HEAD): 0 of 1 planned rows get a surface, 2 of 2 available rows get exactly one. A component test pins the planned case; the pre-fix id test over the same rows reproduces the bug. Floor (available rows' surfaces unchanged) held. **Structural fact, off-page:** the generic ingest write door accepts any source string, including the planned row's identity, and checks neither catalog nor stage; its downstream effect was not hunted. Return: the planned provider ships, or a record lands for a source that is not available |
| 2026-09-23 | health-probing (verdict and attempt stored as two facts; only a verdict writes the verdict fields) | credential-vault | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** the tree already stores the two facts, so the live risk is ordering: if the verdict path leaves the attempt standing, "render both" shows a could-not-check beside a verdict that already answered it, and the rule owes a clause. **Caught.** Write sets extracted from source: 0 of 3 verdict writers clear the attempt (the single engine writer, the gateway sweep, the renderer's post-probe patch); the attempt fields have 0 readers outside their writer. Over 120 outcome sequences (length 1-4), 40 end unreachable and 80 in a verdict. A (tree, verdict rendered alone) hides the attempt in 40 of 40 and shows 0 of 80 stale; B as landed with a presence reader hides 0 of 40 but shows 50 of 80 stale - the floor fell; B with a reader ordering attempt against verdict time, or with the verdict clearing the attempt: 0 and 0. Controls: the unreachable branch is detected writing the attempt; synthetic null patches are detected as clears. **Structural fact:** the gateway sweep persists through its own path with no unreachable branch, so a connection error there is still a failed verdict; the metadata patch already deletes a key sent as null, so the clear is one line. A model over extracted write sets, not an executed run. **Condition written into the technique:** the next verdict clears the attempt, or every surface orders the two by time; every verdict path owes the clear. Return: when a reader of the attempt lands |
| 2026-09-23 | health-probing (which answers are verdicts: classify from typed signals, not the status line) | credential-vault | personas | experiment | better | **Seam chosen to falsify, value stated first:** if any catalog provider answered a revoked key with a quota-shaped 403 or a 5xx, the partition would need a per-provider override and could not be drawn from the protocol. Not falsified on the providers read. Shipped catalog census: 115 probe recipes over 113 connectors; the probe reads only the status line (no headers, no body); 0 recipes read a rate-limit header; the one recipe declaring an expected status carries a key the parser never reads. Providers documented to answer spent quota with 403 cover 4 recipes (2 via a remaining-quota header, 2 via a body reason); 5 more probe the same provider's identity endpoint where the quota answer is undocumented. Over 9 documented answer shapes from 3 providers, A (status line) is right on 2 and B (429 and 5xx unknown; 403 with zero remaining quota unknown) on 5. Floor, real rejections kept as failed: 2 of 4 in both arms. Revoked keys: 401 at two providers, 200 at the third - none quota-shaped, none 5xx. Controls: 2xx verified in A; 401 and a bare 403 stay failed in B; 503 unknown in B. **Not hunted, and written into the technique:** one provider answers a revoked token with HTTP 200 and an error flag in the body, so both arms draw it green - the status line fails in the success direction too; B also misses quota signals carried in a body. Evidence is provider docs, not recorded answers. Return: once the probe reads headers and body, rerun on a recorded rate-limited answer and a recorded 200-with-error answer |
| 2026-09-23 | credential-vault golden path (one sensitivity predicate for every surface, default secret) | credential-vault | personas | experiment | not-better | **Seam chosen to falsify, value stated first:** zero disagreements would make the rule insurance, disagreements putting real secrets on screen would confirm it, and disagreements running the other way would say the authority is wrong, not missing. The third. The storage predicate was ported and agrees with 19 of 19 of its own unit-test cases; the renderer side has 5 surfaces masking on a password type and 0 reading the field's sensitive flag. Over 196 catalog fields in 115 connectors: 151 agree, 0 masked on screen but plain on disk, 45 sealed on disk but shown plain across 36 connectors - 36 of them sealed by an explicit sensitive flag on identifiers (ids, domains, emails, ports, paths, model pickers) and 9 by the name backstop, each the identifier half of an id-and-secret pair whose secret half is already masked; one public key the schema marks non-sensitive is overridden by the backstop with a warning per save. Target, secrets shown plain: A at most 1 (a judgement call on one consumer key), B 0. Floor, non-secret values masked on the confirm screen: A 0, B 44 - fell. Controls: a known sealed-and-plain field flagged; its secret sibling not. **Structural fact:** the disagreement is the seal over-classifying - the backstop matches key and token as substrings of identifier names. **Condition written into the golden path:** one predicate is only as good as its classification; correct it first, and a name backstop stays a floor for the seal, not the screen's rule. Return: once the catalog declares its identifiers non-sensitive, recount |
| 2026-09-23 | navigation-model (the back gesture belongs to whoever draws it: a same-URL entry carrying the location, restored on traversal) | app-shell | kp | experiment | better | **Seam chosen to falsify, value stated first:** if traversal to a same-URL entry cost a server round-trip, or Back still left the workspace, the entry-state route is not the cheap fix the application names and the host rule loses its recommended form. Neither fired. The installed router's own restore reducer executed in node over synthetic states: traversal and the push patch at an unchanged URL with the same stored tree, 0 server fetches; negative controls (entry tree differing only in page search; a different route) 1 each. The push patch keeps the caller's state and adds the router's key; traversal preserves custom state. **Caught:** the router's copy keeps only its own two keys, so any replace carrying null state erases the recorded location, and its own soft-navigation replace discards custom state as well. Workspace history writers: 4 through the shell's navigate door with null state, 3 direct null replaces, 6 framework replaces, 3 already carrying the entry's state. Back presses landing right over 4 real sequences (n=8): A 1 right, 7 leaving the workspace early; B as specified 3 right, 5 wrong tab, 0 leaving; door carrying entry state plus inbox cleanup writing the adopted tab 7/8; framework replaces moved onto the door 8/8. Floor (the rail switch writes nothing to the address) holds by construction; e2e not run. **Structural fact:** the arrival inbox's cleanup is itself one of the erasers, so a deep-linked first entry cannot hold its destination as written. **Refinement written into the technique:** an entry-state location lasts only as long as every writer that replaces the entry carries it forward. Falsifier: a traversal between two door-written entries that issues a request. Return: when the door carries entry state, re-count the writers that still pass null |
| 2026-09-23 | lazy-section-loading (keep-alive is an optimization over restore, bounded, with explicit resets for what should not survive) | app-shell | kp | experiment | better | **Seam chosen to falsify, value stated first:** an empty hidden-state census would make keep-alive free here and the bound and resets ceremony; a tab that refetches and blanks on return would mean keep-alive preserves nothing. Neither held. Census over the 24 tab chunks' import closures, 9 of 9 detector controls passing: transient open/menu flags 40 across 14 of 24 tabs (21 counting shared components); live mic/voice media in 2 tabs; duplicate identity 0 cross-tab static id or test-id literals of 39, and 0 of 10 e2e id selectors hit two tabs; text-, label- and placeholder-based e2e locators (60) match hidden nodes and were not measured; document-level style hazards 0. State preserved: 22 of 24 tabs load in effects; 18 keep last data on screen while reloading, 4 blank first. Target, surfaces surviving a return: A 0 of 24 (every return remounts and replays the entrance), B at least 20. **Caught:** the installed React's hide path runs passive-effect cleanups and its reveal reconnects them, so under that primitive mount-keyed logic re-runs on every reveal and unmount-keyed cleanups fire on every hide - the technique's "mount-time initialization runs once" bullet was backwards for it. The voice interview's cleanup ends the call and beacons a completion, and a ref surviving the reveal marks it finished, so the kept surface would show a dead call. **Correction written into the technique:** lifecycle timing depends on the primitive; audit teardown-on-unmount before keeping anything alive. **Structural fact:** the shell has no restore path, so an evicted tab comes back exactly as every tab does today. No gate run. Falsifier: a kept tab whose reveal shows state its hide already tore down, with no reset. Return: a bounded keep-alive branch with the voice reset, and the text-locator e2e run twice-mounted |
| 2026-09-23 | catalog-as-sync-key amendment (normalization fails in two directions; equal digests must imply equal content) | knowledge-registry | ai-registry | code | better | **Seam chosen to falsify, value stated first:** the registry's own digest is the one tree that could refute the finding on the spot - a sound normalizer there would have made the source's incident a curiosity about somebody else's canonicalizer. It did not hold. `hashBundle` fed each file's relative path and then its bytes into one stream with no delimiter or length prefix, so the path/content boundary was unrecoverable: `ab.md` holding "X" and `ab.m` holding "dX" both produced `sha256:f29ce5a2284c36dc`. Neither file is contrived and no adversary is needed - any edit moving a character across the name/content boundary collides. Target, colliding bundle pairs: A 1, B 0. Floor, the review ledger the same function decides currency for: A 95 reviewed / 25 stale / 0 invalid, B identical. **The floor shaped the fix and the first attempt failed it:** length-prefixing closed the collision and changed every digest, stranding 120 stored records; reporting them stale was false drift, and reporting them not-comparable was honest but lossy - it collapsed all 120 into one state, discarding the 25 genuinely stale and the 95 genuinely current alike. A target that moves while the floor falls is not-better, so the superseded function is retained and selected by each record's own prefix, dated by the condition that no old-prefix record remains. **Structural fact nobody designed:** this registry already published *skill* content hashes as `sha256-n1:`, a prefix naming the normalization, while the *bundle* digest shipped as a bare `sha256:` - the discipline existed, was written down in the form of a prefix, and was applied to one of the two digests; nothing detected the asymmetry because both looked individually correct. **Second structural fact:** the stability guard asserted four insensitivity properties (line endings, lone CR, filesystem ordering, content-sensitivity) and none of injectivity - a guard built entirely out of insensitivity assertions passes a function mapping everything to a constant. Seeded collision case added beside the other four. **Shipped** in-tree, pathspec commit, not pushed. Falsifier: a digest scheme change that leaves any stored record unreadable by the reader that wrote it. Return: delete `hashBundleLegacy` when no `sha256:` review record remains |
| 2026-09-24 | motion-carries-the-mechanism (a flow degrades to its path with direction marks, not to a still ring and not to a loop that ignores the preference) | narrative-scroll-surface | personas-web | code | better | **Seam chosen to falsify**: the fleet's strongest process animation. A caught outcome would show the degraded forms unnecessary. Target: nodes still animating under reduce, 10 -> 0, visible 8 -> 10. Floor: no-preference still 10/10 animating. ab-paired, shipped. |
| 2026-09-24 | content-bearing-degradation (start-state shape, reached through the reduction branch removing the lift) | motion | personas-web | code | better | Found by the seam above when a still capture came back blank. Target: route-wrapper settled opacity under reduce, 0 -> 1 on 4/4 routes. Floor: the enter transition still plays under no preference. ab-paired, shipped. Return: measure production build and the mobile layout's library-mode wrapper. |
| 2026-09-22 | footage-claims-a-camera | evidence-bound-visuals | gravitone | experiment | not-better | **Seam chosen to falsify** the shot compiler's "a trailer shot asserts nothing a viewer can check". Literal scan with positive and negative controls: 0 of 82 prompt literals in a capture register. The precondition is absent by construction, and the header holds because of the welded photoreal negative, not the genre. Re-run when a photoreal preset lands or a video adapter without a negative prompt reaches a provider |
| 2026-09-22 | self-hosted-residency-is-part-of-the-target | cross-provider-benchmark-operations | pof | experiment | unmeasurable | **Seam chosen to falsify:** had the incumbent local grader been fully resident at every window, the technique's latency claim would have been decoration on this machine. Placement read back from the serving layer: the 27B incumbent at 90/10 accelerator/host at a 32k window and 70/30 at 64k; a 12B at 100/0. The vision provider pins its window at 8k (declared, which is the technique's step 2 already done) but records no placement, so no result row can say whether an arm spilled, and the split was not measured at 8k. Return: Arena V of the local-model-arena plan, reading placement per arm into the row; the measurable is the share of comparison rows that carry a placement fraction, currently zero |
| 2026-09-22 | generated-shot-sourcing (section: a clip pinned at both ends is paced by the model, not by its span) | video-assembly | gravitone | render | better | **Render proof, operator-graded, blind.** One variable, span (22/73/175 frames, first AND last frame pinned, 2 seeds) over the project's own continuous head-turn shot (positive control: already rendered legibly at 73f). Long side: surplus span idles ~120f then snaps (3-4x median step); operator picked the travel-sized span 2/2, overruling a refused 1.16x discrimination. Short side: a fast continuous turn, no cut; split 1/1 at 0.88x = unmeasurable. The source's "too short -> it cuts" did not reproduce for a small travel. Project row gravitone d706a70. Return: re-test the short side on a large camera move; test mask-only vs keyframe arrival once a latent-mask extension node is on this machine. |
| 2026-09-22 | step-synchronous-windows | windowed-inference-over-oversized-inputs | - | unapplied | - | No fleet project runs a multi-step model over overlapping windows. The one near-seam (the H3 chain lane in gravitone) chains deliberate hard cuts, which is the technique's own "when not to use". Return: when a project adds a tiled or context-windowed refinement pass (sequence upscale, panorama, long-clip refine). Floor to declare then: geometry at the seam unchanged; target: detail-change at the window boundary. |
| 2026-09-22 | self-hosted-residency-is-part-of-the-target (Arena V, measured) | cross-provider-benchmark-operations | pof | experiment | not-better | Replaces the same day's unmeasurable row with a measurement. **Seam chosen to falsify, and it did:** three local vision graders (the 27B incumbent, a 12B, and the new 9B distill) raced on the image-to-3D input gate at the provider's own 8k window, and every arm was 100% resident on the accelerator with nothing co-resident, so placement was not a confound at this window and the technique changed nothing here. Its claim binds at larger windows, where the same 27B spilled 10-30%; that is the return condition. **What the arena returned instead is the run's real finding:** the gate's refusal line was fitted on a hosted grader chain, but the router serves this capability from the local eye first, and a threshold is fitted to its grader. On 60 images labelled by eye (6 pass / 54 fail, 10 ambiguous excluded, 2 repeats, 60/60 identical across repeats for every arm) the shipped line lets 16/54 bad inputs through on the incumbent, 20/54 on the 12B and 8/54 on the 9B, with no good input refused by any arm. The incumbent ranks best (AUROC 0.998 vs 0.991 and 0.966): moving its line to 8 lets 3/54 through and to 9 lets 1/54 through, still refusing no good input. The 9B cannot go below 3/54 without refusing a good input, because it scores flat UI emblems 9 while naming their dark backdrop as a defect, so its reasons and its numbers disagree. It is 3.8x faster and 8 GB smaller, which buys nothing in a gate guarding a minutes-long paid job. Committed as a harness + truth set + raw rows (pof da4df5eb). The threshold change is proposed and not shipped: 6 positives is too few to move a line whose false refusal blocks generation. Return: grow the pass set to >= 20 and ship a per-grader refusal line |
| 2026-09-22 | self-hosted-residency-is-part-of-the-target + cheapest-sufficient-configuration (Arena V round 2) | cross-provider-benchmark-operations | pof | code | better | **Shipped** pof `255ad7a9`. Round 1 held the refusal-line change on 6 positives. Round 2 grew the truth set to 88 images (19 good / 69 bad) with 28 original fixtures generated locally and labelled by eye from the pixels. Three of those ignored their generating prompt, which is why labels never come from the prompt. **The grown set refuted round 1's proposed line:** a good robot scored 8, so the line that looked safe at 9 would have refused a good input. The shipped line is the one the floor allows: the local incumbent's refusal moves from 5 to 8, bad inputs admitted fall from 21/69 to 7/69, and good refused stay at 0/19 (target moved, floor held). The gate now reads the attributed routed answer and looks up a per-grader line, and unmeasured models keep the old default. The 7 still admitted expose a blind spot no line fixes: chest-up crops on white score 9. The 9B ties at the same line (6/69) but has 8 of 19 good scores sitting on it, so it has no margin. **Resource incident, recorded as a lesson:** Flux 2 dev plus its encoder committed about 51 GB and froze the box through paging; the batch moved to a 5B video model rendering one frame, which fits the GPU, with a RAM abort guard that fired once. Return: a crop check on the subject box would close the blind spot |
| 2026-09-22 | local-model-arena Arena C (coding tier, run as an Ascent lane wave) | cross-provider-benchmark-operations | ascent | code | better | **Shipped** ascent `932ac1e0`, `9c233e85`, `c6f963e2`, `120ef1a8` + docs `75676c51`, not pushed. An Opus analyst picked 5 items from the repo's own backlog and checked each premise at HEAD, rejecting the two headline gaps as already fixed. The 9B executed them through the repo's own local transport, at the 64k floor where it is fully resident (the 27B spilled 30% there, and its two 2026-09-21 lanes made 0 commits). **Target** (items landed): 4/5. **Floor** (each lane's tests before and after, plus typecheck): all four finished lanes were green on their targeted tests, **two of them failed typecheck, and one carried a semantic bug its own new test asserted** (a URL key the parser never reads). A round-trip test written by the reviewer caught it and fails on the lane's version. Easy items landed near as written; medium ones needed a reviewer. The stretch lane looped for 220 tool calls on exact-match edits into a CRLF file, the same mismatch the reviewer's own scripted edits hit. Not measured: no hosted arm on the same items, no rescan, and no full `npm run verify` (the pre-push hook will run it). Return: a hosted arm on the same five items would give the arms contract its first verdict |
| 2026-09-23 | rejections-and-edits-become-constraints | brand-voice-capture | systedo-case | code | better | **Shipped** systedo-case `e1ec5d46`, not pushed. **Seam chosen to falsify:** at a dozen-word reply one swapped word clears a quarter and the old rule would already bank voice edits. At the product's ~50-word length it needs 12-13 words. Paired, both arms imported from the tree, 22 cs/en fixtures. **Target** (voice corrections banked): 0/11 -> 10/11. **Floor** (tolerance 0): typo, cosmetic and price edits banked 0/9 -> 0/9; rewrites 2/2 -> 2/2; test:unit 4147/4144/0 fail; tsc clean. The miss is a register change spelled like a typo (a colloquial elision), pinned in a test. Small edits now bank their changed spans, not two whole replies. Fixtures are constructed by the run that designed arm B, which is a bias. The structural half (a size threshold cannot see any sub-12-word correction) does not depend on them. Return: a month of real sent drafts, and the share of banked edit facts that the next distillation turns into a directive |
| 2026-09-23 | filtering (disclosed cross-filter reach: the hidden figure on every scoped action's count, not only the bar) | table | ascent | code | better | **Shipped** ascent `5f17b6c5`, not pushed. Seam chosen to falsify the rule's own example: actions scoped to part of the selection. Paired fixture (6 rows, 3 shown, 3 actions, 6 hidden deliveries): tree 6/6 undisclosed; bar total alone 3/6; per-action "(K hidden)" 0/6; payload ids, shown-only select-all and no-hidden labels held. The technique gained the per-action condition. |
| 2026-09-23 | performance (memo-compared row props: one stable identity-taking callback) | table | goat | code | better | **Shipped** goat `9e34aaf`, not pushed. Paired jsdom harness over the real grid, card and comparator (60 items, 30 mounted, 3 runs per arm, identical): no-change parent renders 150 -> 0, select one 30 -> 1, scroll one row 66 -> 0. **Seam chosen to falsify, and it did:** under an inline or a selection-dependent upstream handler the call-site fix alone stayed 150 / 30, so both upstream handlers were stabilized and the technique gained the whole-chain condition. ESLint could not load (a plugin missing), tsc errors all in untouched files. |
| 2026-09-23 | loading-and-empty-states (an arrived payload that does not decode renders a named mismatch, never the empty state) | table | pof | code | better | **Shipped** pof `5a5c36f9`, not pushed. **Seam chosen to falsify:** the tree's spec linter should make wrong shapes impossible; it never sees model-written payloads. Paired over the real artifact store (178 stored payloads, 16 chart steps, plus 11 shape controls): arrived-but-empty 12 -> 0, charts drawn 166 -> 166 (floor, tolerance 0). One case was a chart spec wrong for its entity, so the technique now says the state names a mismatch, not a bad payload. The graph branch keeps the same defaulted decode (report only). |
| 2026-09-23 | decision-records (when another surface owns the send, the system records a placement, never a send) | hitl-approval | personas | code | better | **Shipped** personas `ada169077`, not pushed. Seam chosen to falsify: the readers that might need a placement counted as a send (channel health, momentum, coverage) - none did. Paired over the real lane: "Recently sent" 4 -> 1, browser sends 3 -> 0; the real send and all 3 placement rows kept. The direction enum is closed, so the tag lives in the row's metadata; the Rust prompt readers still read direction only (report-only). |
| 2026-09-23 | sorting (collation locale is the reader's, passed explicitly) | table | personas | experiment | not-better | **A rejection with its condition.** Real data, read-only, 4 default-comparator text columns (282 values, 23,188 pairs) under all 14 shipped languages vs the runtime default: 0 pairs differ. Control: a 9-word mixed-language fixture moved 4/9 under cs, 2/9 under es. No server render in a desktop app, so the second-tier force is absent. sorting now says the locale is a guard, not a fix, until the data or a second tier makes it one. Nothing shipped in the product; ledger row `3e386ab2c`. |
| 2026-09-23 | filtering (explicit commit only where a commit costs a round trip; all-client keystroke filtering announces its count) | table | - | - | unapplied | No apply dispatched this run. Return: a surface with an all-client text filter and no status message (the seam hunt should start from the table's client-server-split regime). |
| 2026-09-23 | pagination (the tuple seek needs one direction and no absent terms) | table | - | - | unapplied | No keyset walk over a nullable sort column was located this run. Return: when a project pages by cursor over a nullable column. |
| 2026-09-23 | budget-enforcement (the chokepoint follows the metered unit; a streaming twin is a third path class) | cost-metering | ascent | simulation | unmeasurable | Structural-only: the source tree already routes every product-action entry through one gate module (the application records it); no A/B arm was built. Return: a route that bypasses the gate import. |
| 2026-09-23 | sensor-pipeline (a failed read is unknown, never absence; not smaller, not louder) | codebase-scanning | ascent | - | unapplied | The source tree already carries the fix at `b256ff74a`; the failed-sensor list is still not persisted to its scan table (report-only). Return: a second scanner in the fleet. |
| 2026-09-23 | ratchet-design (record the change's footprint at base and tip, never a whole-tree re-measure in a moving tree) | metric-gates | personas | - | unapplied | Seam known (the census `--update` writes the whole tree in both directions) and not applied this run. Return: next personas census run. |
| 2026-09-23 | must-ignore-unknown (an in-place writer passes the foreign-key test by construction) | repo-manifest-standard | ascent | - | unapplied | Source tree already fixed (`212ae1a51`, `996de52d0`). Return: another in-place manifest writer in the fleet. |
| 2026-09-23 | isolated-index-commits (phantom staleness blocks merges; derived files built from the committed composite) | concurrent-vcs | ai-registry | - | unmeasurable | No paired arm - one witnessed case: this run's own sync found seven stale-index entries in the shared checkout matched to older committed blobs and cleared by an index-only reset before a fast-forward (the merge went through a clean worktree and gated 21/21). Instrument that would measure it: a count of stale-index entries left by ref moves across the fleet's shared checkouts. |
| 2026-09-23 | anchored-identity-diffs (refuse at propose what the declared heading lists refuse; apply re-checks) | companion-identity | personas | code | better | **Shipped** personas `10d561241`, not pushed. Paired over 10 cases: never-landable proposals filed 3/3 -> refused 3/3 at propose; floor held (valid diffs land 2/2, live-file refusals wait for apply 2/2). **Seam chosen to falsify, and it caught:** a heading on neither list present on disk LANDED at HEAD, so the refusal rests on the closed list, not on apply. Technique amended. |
| 2026-09-23 | enumerable-domain-decoding (the door re-checks spread between candidates) | structured-output | personas | experiment | not-better | 1000 real anchor draws through the real roll door, three model policies: lost-spread sets 985/102/91 -> 0, but honest edge-clamped nudges became failed rolls (+3.8/+2.9 points after one retry; upper bound, the simulated retry ignores the message). Technique amended: state the bar in the prompt, draw with margin, budget the retries. Product unchanged (instrument + ledger row, `6ec965fa7`). Return: a live-model sample of 50+ rolls with a compliant retry. |
| 2026-09-23 | compaction-horizon-breakeven (a prompt-length price step is a second wall, read per model from the price book) | prompt-assembly | ai-registry | experiment | unmeasurable | **Seam chosen to falsify** (no fleet project owns a compaction trigger; grep over seven trees). Replay over 190 coding-agent sessions / 26,299 requests of this checkout, instrument checked against the 2026-09-16 count (9 vs 8 threshold compactions). Target: under a 2x whole-request step at 272K the same traffic costs +58% with no wall and nothing with the wall; 186/187 added compactions repay by the break-even. Floor (task outcome after compaction) unmeasured: compactions 9 -> 187. What it refuted: that the step is the only force moving the wall - on the flat book a 272K wall still cut priced input to 0.64x. Return: a compaction-fidelity probe (re-ask pre-fold facts, or a memory-year arm) that prices the loss per compaction. |
| 2026-09-24 | full-text-indexing (index-or-scan per result contract and query shape: what the query makes the index visit) | search | politicas | experiment | better | **Seam chosen to falsify**: a measured rule (R9, 52-68x) derived from rare single-term counts, about to govern ranked common-word search over transcripts. Paired in the project's own engine, 24 queries x 2 contracts at 200k docs, GIN vs seq scan over one stored column. Target: speedup per shape (150-234x at 0% matched -> 13-25x count / 4-11x top-10 at 1-3% -> ~1x past 23%; a phrase pays its words' AND). Floor: identical counts and top-10 scores across arms, held 24/24 on two runs, read from the same instrument. What it refuted: the pre-registered prediction that ranking alone collapses the speedup - match fraction sets the curve, ranking halves it at each step. **Shipped** politicas `75aa6d3` (case #3b + R11a), not pushed. Return: the score-pruning engine class is unbenchmarked; run fts-contracts over the real transcript queries at real size before that ingest. |
| 2026-09-24 | explicit-explore-slots (serve the frontier as exploit / explore-known / explore-novel slots; raise exploration when activity concentrates) | shared-research-record | ai-registry | experiment | not-better | **Seam chosen to falsify**: does this fleet herd on its single sorted worklist? 30 days of `knowledge/` commits mapped to subjects. Frontier-driven (librarian/deepen/harvest/reconcile, 70 commits): top subject 3.9%, top-5 share 0.138. Source-driven, size-matched over 2000 draws: top-5 p5 0.152, p50 0.185. Cross-run same-subject pairs within 1h: 6% vs the paper's 63%. The trigger (one cluster over a third of activity) does not fire. A defect worklist self-depletes; a leaderboard self-reinforces. Amendment written. Return when a ranking term survives being worked (demand the work cannot reduce, recency or discussion counts). |
| 2026-09-24 | resolution-bounded-leaders (promote only past the evaluator's resolution; on a shared pass/fail case set, a paired sign test over discordant cases) | shared-research-record | personas | code | better | **Seam chosen to falsify**: the memory-year ladder bolds successive leaders at 0.89/0.90/0.92 on 194 shared probes. Target was leader claims among adjacent top arms that clear an exact sign test: A 5, B 0 (+1/27 p=1.0; +6/22 p=0.29; read-mode split 5/5 p=0.06). Floor held: none vs full history resolves at +40/64. Two independent implementations agree. Shipped as `memory_year/checks/ladder_resolution.py` (self-asserting controls) plus a FINDINGS round-five qualification; not pushed. It prices probe sampling, not grader variance. Return: a fresh re-run and re-grade of one top arm, to measure the grader band. |
| 2026-09-24 | call-site-ownership (the caller places, the control appears; gate the property family, not the value) | ui-controls | personas | experiment | better | **Seam chosen to falsify**: the fleet tree with the most mature token gates, where the gates might already see call-site restyles or there might be none. Target: restyle sites visible to a gate. A = the project's full lint config over the 29 files holding them: 0 of 34 at any severity. B = a placement/appearance classifier over 678 shared-button call sites: 34 restyle it, 17 with no raw value (invisible to any raw-value rule at any severity). Floor: placement misread as appearance, 0 of 34 on a director read. The first pass read icon-prop classes as the button's (385 sites); corrected to 74 with a className, and the counts are a floor. Not shipped: a new gate over 34 existing violations is a ratchet. Return: the project's census rule at baseline 34, or a rule reading its variant maps for the remedy. |
| 2026-09-24 | token-enforcement (unvisited-branch clause: a skipped syntactic position subtracts from recall silently) | design-tokens | personas | code | better | Found by the seam hunt while running the call-site arm A. A white-color rule anchored on whitespace only, so every state-variant form escaped (15 of 25 uses), and it reported one match per string. Paired on the 13 files holding a white class: 3 -> 9 findings, errors 0 -> 0, other rules' 10 messages unchanged, rule tests 59/59. **Shipped** personas `f9e257bcd`, not pushed. The same anchor in the low-contrast rule is deliberate (its contract exempts state modifiers): a fix read against each rule's contract, not applied by pattern. |
| 2026-09-24 | token-enforcement (the vocabulary is the escape hatch nobody counts) | design-tokens | fleet (6 projects) | experiment | unmeasurable | The declared focus: replay our own same-class instrument against the source's headline ("enforcement moves drift into the vocabulary"). The nearest fleet instrument is the copy gate's writable baseline: every post-install edit across 6 projects was net-negative or neutral, all on install day 2026-09-14; nothing raised a baseline to get green. That is weak and not the same mechanism. The instrument that would measure it: a membership gate at error level over a vocabulary agents can write, with the authority's member count tracked across the rollout. No fleet project has one (personas' token rules are warn/off). |
| 2026-09-24 | listing-tier-by-initiator (listed / name-only / hidden, set by who starts the capability) | agent-instruction-files | kp (+11 consumers and this registry as machine state) | code | better | **Seam chosen to falsify**: a replay of 13,649 transcript files could have shown initiator skills selected on resemblance, which would have put the default tier in doubt. It showed none, and it refuted the hidden tier instead (prose, chains and one product engine start them by name). Target: first-request context, kp: 56,282 -> 53,866 (ab-paired, n=2, identical repeats). Floor: initiators still listed by name, four ambient skills keep descriptions (smoke-checked). Installed via `link-registry --listing-only` into gitignored settings.local.json; one consumer tracks that file and was refused. Return: a resemblance-driven start of a name-only skill in a later replay. |
| 2026-09-24 | single-source-topology (currency: AGENTS.md read natively since 2.1.277 only with no CLAUDE.md on the path) | agent-instruction-files | athena-everywhere | code | better | Found by the fleet delivery sweep: a 52-line AGENTS.md beside the canonical CLAUDE.md, no import, reaching no session. Target: 4 questions answerable only from it, tools disallowed: 0/4 -> 4/4, two runs per arm. Floor: CLAUDE.md 102 lines (under target), +858 tokens. Shipped `71d0bfd`, not pushed. A first-pass classifier flagged a correct reverse topology as a fork; reading the file fixed the classifier. |
| 2026-09-24 | agent-guidance-bootstrap 0.9.0 delivery rule + check-agent-guidance.mjs | agent-instruction-files | fleet (12 projects) | experiment | better | Target: forks (two populated guidance files, no bridge): 1 -> 0 after the fold; self-test 6/6 topologies. Floor: 8 always-loaded files over the 200-line target are reported, not failed or trimmed. Return: the checker firing on a new fork, or a line-earning pass with behavior pins on the largest file. |
| 2026-09-24 | rewrite-behavior-pinning (pins written and run before a bulk rewrite of an always-loaded guide) | agent-instruction-files | systedo-case | code | better | The owed row-20 trim from this morning's run, done pins-first. 18 behaviour pins answerable only from AGENTS.md, tools disallowed: 18/18 in every run of both arms (Sonnet x2-3, Opus x1). Target: first request 55,868 -> 39,977 tokens (-28%); 1,049 -> 430 lines, reasoning moved verbatim to a routed lookup. Floor: test:unit 4,144 pass / 0 fail before and after; 57 registry-quoted sentences still present (a pre-write check caught one recapitalised quote, the suite caught one command moved out of its fenced block). Shipped `ec3ebe66`, not pushed. Read by hand: one answer thinned (the timed CI chain). Return: the next trim of the file re-runs docs/agent-guide/behaviour-pins.md. |
| 2026-09-24 | listing-tier-by-initiator (user scope: harness and platform-supplied skills) | agent-instruction-files | fleet (user settings) | code | better | Nine skills only ever started by name set `name-only` in the user's settings; the resemblance-driven ones (API reference, document handlers, browser, scheduling) stay listed. User and project `skillOverrides` merge per key (both scopes' entries applied in one consumer). kp first request 53,866 -> 53,055. Backup of the settings file kept beside it. |
| 2026-09-25 | configuration-axes-cross-the-ladder (amendment: immutable must mean enforced) | test-harness | personas | experiment | unmeasurable | **Intake D4 pass on 2026-09-16-modernweb-web.** Seam: `gatherBundle` in `scripts/test/`, the "summary builder used by three modes" named by backlog `w7-session-immutability-arm`, chosen to falsify. It holds the writer-side failure the source does not show: the module declares "an immutable bundle", re-gather overwrites it in place, and the scorecard records no bundle identity. Replay of 14 archived bundles from git history (pruned since): **0 re-gathered**, 2 with scorecards; floor = summaries agree with bundles on 13 of 13 comparable. The target cannot move on zero incidence, so this is `structural-only` and nothing shipped. Return condition: the first re-gather of a bundle that already carries a scorecard; the instrument is the same replay script run over the live run directory. The reader-side failure cannot occur here, because every reader deserializes from disk. |
| 2026-09-25 | model-based-oracle (an accept-or-reject model compares the reason, not the verdict) | test-input-generation | ascent | code | better | **Backlog wave 8, unit 2-012; split verdict.** Seam: an agent-facing write gate, fail-first over six rules, refusing with a sentence only, header declaring an order, hand-written one-fault-per-case tests matching text by regex. 4,480-case exhaustive matrix, six arms, targets pre-registered. **Target** (reason-only defects caught of 3 - a precedence swap that would disclose plan state to an unauthorised token, a copy-pasted refusal branch, and a real latent defect at HEAD where a built-in property name resolved as a table row and was refused for "the undefined scope"): existing suite 0, verdict-only exhaustive model 0, rule-membership 2, membership + declared precedence edges 3, same with a typed rule field 3. **Floor** (false alarms on 2 legitimate changes - an unordered check moved, a message reworded): verdict-only 0, full-order matching 2, prose-classified membership 1, typed rule 0. Positive controls: an off-by-one ceiling reddened every arm; a hard-coded gate reddened only the combination arms. **Refuted half:** "an unclassifiable rejection is itself a finding" never fired on a defect, only on the harmless reword - kept as a failure kind, reframed as reason-channel drift. The source's own mechanism is set membership, not "predicts which rule fires". Shipped ascent `e6908657` (typed rule vocabulary, own-entries lookup, independent oracle, matrix test; vitest 179/179, tsc 0, eslint 0). Return: pass the typed rule through the route's refusal payload |
| 2026-09-25 | single-flight-primitives (catalogue names the sixth policy: preempt and continue, chosen per waiter) | concurrency-guards | kp | blind-ab | not-better | **Backlog wave 8, unit 3-001; rule COVERED, residual not-better.** The mechanism is already a technique in render-mount-pipeline (preempt-and-continue-shared-computation), forged by the same source's intake; the residual was the general catalogue still saying "five honest options" with no link. No code seam in the 10-project fleet: no shared work is both continuable and joined by a waiter who could finish it faster; the one priority-carrying single-flight is an idempotency replay never sent at two priorities. Probe: a three-caller automation-pass single-flight (clock tick, operator route, command bar) on one event loop - chosen because it can FALSIFY the amendment by over-application. 2 runs per arm, 2 blind judges. **Target** (R1, per-waiter policy, A-B >= +2): 4 vs 4 - ceiling, both current-corpus arms already split the callers. **Floor** (R2-R5, no over-application): 16 vs 16. Positive controls both green (planted sledgehammer R2 = 0, planted null R1 = 0). Refuted: the fear that naming the sixth policy pulls readers into interrupt machinery where join is right. Unscored difference: only the amended arms found the serial clock tick cannot afford to join a minutes-long pass. Seam class: no site where both preconditions hold (resumable partial result, faster taker). Return: a paced background runner with checkpoints joined by a deadline-bound waiter. Also found: the golden path's spine item still says "all four" policies against the technique's five |
| 2026-09-25 | named-duplication-points (new: a reviewer for hand-restated prose copies, a machine for literal ones, the site list as a rotting copy) | docs-sync | (source tree: an embedded-interpreter repository at its pinned commit) | experiment | better | **Backlog wave 8, unit 3-003; split verdict; home overridden from agent-instruction-files to docs-sync.** No fleet project declares a hand-maintained prose copy regime, so one seeded change (12 stale sites: six rounded latency copies incl. an unregenerated chart, five print-cap statements, one module table) was replayed through reviewer arms and a mechanical arm on the source tree. **Half 1 (reviewer, not generator) better, per fact:** reviewers caught 92 of 96 site-instances with 0 false positives on a clean tree; the literal value check was wrong both ways on rounded copies (2 false positives clean, 1 false negative seeded) but found 6 of 6 where copies were literal (identical table, same-spelled cap) - so the check is chosen per fact, not per regime. **Half 2 (enumerate N sites) not-better:** reviewers with the list 47/48, without it 45/48, short of the declared +2; the list is itself duplicated and already disagrees with its twin (5 limits vs 4), names a document that does not state its fact, and misses at least 11 real copies; the one miss with the list was a file ticked off after its table moved while prose below stayed stale. The reviewer is invoked by nothing but a general review skill's delegation - prose, not a gate. Also: `single-source-topology` stated the generator check unconditionally; a scope pointer now routes hand-restated facts here |
| 2026-09-25 | pre-resolve-what-the-engine-cannot-await (new: block the engine's thread only if it can park, else read ahead; refuse the unreadable, release only what this call acquired, release at end of use) | engine-binding-surface | (source tree: an in-browser analytical engine binding, its published build driven headless) | experiment | better | **Backlog wave 8, unit 3-004; home resolved (none given) to engine-binding-surface, the host side of a wall it cannot change.** No fleet seam: the one sync-over-async engine binding parks its thread (the other branch of the decision, correct as-is), and a sync render over async translation sections already pre-resolves through a route table (an independent sighting, no release half). Experiment on the source's real engine, product code unchanged. Declared target: 5 hard sub-cases (double-quoted name, concatenated name, caller-borrowed handle, 3M-row pending and streamed reads); floor: base case, trivial query, no handle open at end. **A0** (no lookahead) fails the floor. **B1** (the unit's rule as written = the source's shipped automatic mode) **0/5 and violates the floor**: it closed the caller's borrowed handle, created an empty junk file from a name fragment, and on pending/streamed reads its finally threw "file is in use" over the result and left the handle open. **B2** (residual rule) 5/5, floor held, replicated. Negative control: releasing everything scanned turns the borrowed-handle case red; the leak probe caught B1's real leak. Refuted as stated: "drop them in a finally". Lead for the source note: the lookahead only arms when the database path itself is a sandbox path |
| 2026-09-25 | reader-rules-are-not-writer-rules (writer conformance narrows freely; reader behaviour is a separate class) | repo-manifest-standard | personas | blind-ab | not-better | **Backlog wave 8, unit 3-005; home overridden from conformance-checking (its boundary assigns contract versioning to repo-manifest-standard).** The quoted rule is in the source verbatim; the row's gloss ("freely-revisable subset the consumer must accept forever") is refuted - writer-rule changes pass through an obsolete-but-conforming tier, and controlled-environment readers may fail on parse errors. Code arm refused: the grant ceiling is already clamped where the rung takes effect, so an unclamped read grants nothing today and a code arm would be a confirmation dressed as a falsifier. Probe: a real one-line PR lowering the grantable ceiling, whose author claims intake refusal means no higher state can exist. 2 replicates per arm, blind judge, pre-written rubric. **Target** A 6.5 vs B 5.5 (+1.0, needed +2.0); **floor** A 4.0 vs B 3.0, held; positive control 8 (>=7), null 0 (<=1). The corpus-before (must-ignore-unknown's restrictive-field section, semver-additive-evolution) carries most of the value; replicate spread (5 vs 8) equals the effect. Instrument defect admitted: one rubric item assumed a pending approval would create over-ceiling state, which validation refuses. Seam class: contract-evolution review probe where the corpus-before already carries reader tolerance. Return: re-measure on a probe where the corpus-before is weaker. Fleet finding for the owner (no code changed): a charter replace retires every active charter before validating, with no transaction, so an approval admitted under an older writer rule and clicked after a tightening leaves the project with no active mandate |
| 2026-09-25 | retry-escalation (a capped list of recent failures is not a budget; an all-fail run charges no item) | delivery-guarantees | (source tree: a wiki-ingest pipeline's own ingest loop, replayed) | experiment | better | **Backlog wave 8, unit 1-071; claim refuted in part, residual better; home overridden from agent-memory (the structure is work-pipeline retry state).** Claim corrections: the success side's dedup map is unbounded but its history is a 500-entry ring; the evicted failure list held only keys, never diagnosis evidence - eviction loses failed-vs-untried and retry priority; the silent loss is conditional on arrivals outpacing admission. No fleet seam (the offline queue already keeps per-item attempt limits with a terminal record; another truncation keeps the count). Replay of the source's own functions, agent call replaced by a failure schedule, 36 conditions per arm, target and floor declared first. **A** as-is: 26 silent losses (12 of 20 outage failures in the arrival-order fast-arrival long-outage cell), poison items retried 80/80 with 0 terminal records, up to 40% throughput lost under stable admission order. **B1** (uncap, the unit's literal repair): silent loss 0, poison unchanged - fails target. **B2** (per-item budget 4, declared): floor fails in 7 of 36 - an outage as long as the budget retired in-flight items. **B3** (B2 + an all-fail run charges no budget; POST-HOC, declared before it ran): silent loss 0 in 36/36, poison stopped at 8-16 calls with 2 terminal records, floor held 36/36 and higher in 11 (+574 items), failure-free digests identical to A. Controls: A clean at short outage, 12 at long; B2 without the terminal write goes silent in all 18 poison conditions. Return: when a fleet project grows a persisted retry list |
| 2026-09-25 | thread-custody-across-resume (new: unbind a stored thread id only on gone/poisoned; reseed the fresh thread from the host's own plan and last turn) | agent-cli-transport | personas | experiment | better | **Backlog wave 8, unit 5-001; silent re-thread COVERED (session-reuse, hibernation-and-resume), residual better.** Code mode not reached: the host's copy of the plan lives only in the frontend's persisted store, so the fix crosses an IPC boundary behind the full desktop test build. **E1** (does a missing nesting-env strip explain stale ids?): positive control detected a made-up id in both env variants; markers inherited 3/3 resumed, stripped 3/3 resumed - the falsifier fired and the "every resume fails" rule was dropped from the draft. Read-only census: 6 stored build-session pointers, 1 dead (idle since 2026-07-07, 25 turns, thread file gone) whose next turn takes the silent retry. **E2** on that real case, product prompt verbatim, 3 seeds per arm, blind judge with planted pass/fail controls scored as built. Keeps host plan: A (as shipped) 0/3, B1 (tell the model the session could not be resumed - the unit's rule literally) 0/3, B2 (fold the host's plan and last reply into the fresh thread) 2/3; no false restart and concrete next step 3/3 in every arm. Refuted: "a lost thread means starting over" - every arm re-derived real work from files. The remaining B2 seed still rewrote the plan, so the host reconciles an emitted plan against the stored one rather than replacing it. R1 (unbind only on a definitive verdict) confirmed by reading only; the fleet site already does it. Fleet notes for the owner: the build path's stale-resume retry logs nothing while the chat path warns |
| 2026-09-25 | guard-input-custody (the recorder is in custody too: a record a trust decision reads is a guard input; replaceability and trust pull opposite ways) | agent-runtime-assembly | personas | code | better | **Backlog wave 8, unit 4-121; home overridden from fleet-orchestration; `checked: unverifiable` was wrong (the source clones and the quote stands).** Guard half and delegation receipts already covered; the residual is custody of the runtime's own records of a single agent, reached through a generic capability over a store shared with working data. Seam: a companion agent's raw-SQL mutation tool over the user database that co-locates the runtime's per-turn cost/error ledger, approval trail, job table, night-shift log and budget counters - placed there so the agent could read its own history - and under autonomous mode the approval gate fires without a card. Declared first: target 10 hostile statements refused with records intact; floor = agent's own table takes 5 verbs, ledger readable, runtime's own insert succeeds on the same pooled connection after a refusal. **A** 0/10, **B1** (the unit's rule literally: no structural replacement) 4/10 - closed drop, rename, trigger and temp-table shadow, left all six row writes open, **B2** (per-statement store authorizer refusing any write to a schema-declared runtime table, protected set derived from the schema, read kept, removed before pool return) 10/10; floor held in all arms; 89 connector/ledger tests pass with B2. Negative control: deleting the authorizer's removal turned the runtime-write floor red. Shipped personas `46ce99b1f` (clippy clean on the new file, rustfmt). B2 also closes the connector pin gate's inputs, which lived in the same store. Fleet note for the owner: the companion's chat template promises writes wait on an approval card, false under autonomous mode |
| 2026-09-25 | per-tier-budget-tables (when the rows must sum into one hard pool: a joint rung table, sum computed from component costs) | adaptive-fidelity-tiers | (probe sites: a four-model audio generator's GPU tier table; a web front end's six per-effect tables) | blind-ab | better | **Backlog wave 9, unit 5-003; half 1 better, half 2 refuted as stated.** No fleet project has a capacity-rung ladder over a shared hard pool (frame-time tiers are a soft pool corrected by measurement; the one fixed-GPU project has its engines take turns). Declared first; claude-opus-5, n=3 per arm per probe, blind pairwise judge. **P1** (the rule's own case - four models on one GPU, rungs 4-48 GB, a real 16 GB out-of-memory bug): A 5.00 vs B 3.67, judge picked A 3/3 - target met (+1.33). **P2** (the falsifier: six real per-effect frame tables and a teammate's "centralise, they share 16.7 ms"): 5/5 both arms, no A answer centralised - floor met; the judge's holistic pick leaned B (2 of 3) on unrelated concreteness. Controls: planted good/bad answers scored 5/0 on both probes; a relative-path harness bug that fed empty answers was caught and rerun. The lead sits on the joint-rung check itself; the pre-amendment corpus already split the regressed rung and summed per phase. **Refuted "the only form":** engines that take turns need no sum, and a central solver over per-component costs also reads all rows together - the draft scopes the table to co-residency. The source's own table shows the named failure: its comments restate a component cost as a literal that disagrees with the cost authority |
| 2026-09-25 | failure-signatures-beside-the-pass-rate (new: case counts per failure family beside the rate, with each family's denominator; a detector's zero can mean not measured) | eval-harness | personas | code | better | **Backlog wave 9, unit 6-009; mechanism better, the source's figure refuted (`checked: refuted` was right).** Source instrument (experiment, exact predicates on real outputs): its syntax and indentation counters fire on raw tracebacks (positive controls 1/1) and read **0** on the same real errors under the test runner the source's own benchmark uses (the runner prefixes captured lines) and under a compiled language's runner; its elision regex sees one comment syntax only; its counters are event sums across tries including passing retries, and the claim omitted the source's only case-level format counter. Fleet (code): a stored model/effort matrix bench history, 1,026 scored runs, 9 cells, replayed through A (as-is) and B (family counters beside the rate). Pre-registered separate/agree pair: B splits 4 of 9 tie groups by mechanism (A 0 by construction) and keeps 2 groups that failed different scenarios by one mechanism reading identical - both met. Falsified and said in the draft: signatures on passing runs, 0 of 1,026. Unplanned: a 20-point gap in a class named for the output format contract was entirely an omitted action on runs that parsed clean. Floor: reports identical outside the new section (0 lines); negative control (inverted tally) 20-line diff. Shipped personas `91cff0d96` (eslint 0; secret-scan hook passed only nominally - scanner not installed) |
| 2026-09-25 | acquired-is-not-generated (new: an acquired asset carries a declared acquisition record whose licence is its evidence; the gate reads it first; never infer origin from location) | generative-artifact-gating | pof | code | better | **Backlog wave 9, unit 6-004; gate half better, behaviour half unmeasurable.** The one consuming project has no checkout on this machine, so the arms ran on a clone of its public repository at its own gate with its own runner. 10 cases from real shapes in the tree (5 generated incl. an engine object path, 1 deterministic swatch, 4 acquired). **T1** (acquired graded as a generated pass, target 0/4): A 4/4, B1 (infer origin from location) 1/4, B2 (declared record read first) 0/4. **T2** (unlicensed acquisition reaching pass): A 1/1, B2 0/1. **O2** (generated still passes, no licence asked): B1 **4/5** - an engine-path mesh read as acquired - B2 5/5. Location inference refuted in both directions (acquired re-served from the generator route read as generated; generated recorded by engine path read as acquired). Floor: the 6 test files touching the gate 60/60 -> 64/64; positive control (candidate-asset always null) turned 14 tests red across 4 files; new tests fail 3 of 4 on A, the fourth is the opposing "leave generated alone" assertion. Typecheck and eslint exit 0. Limit stated in the draft: nothing in the tree writes the acquisition record yet, so an undeclared download still passes as generated. **Not shipped:** commit `9ae1e6e9` sits on branch `backlog/6-004` of a local clone only; the project's active checkout is on the other machine. The unmeasured sentence (a producer with network access treats a missing declaration as permission) was cut before landing. Instrument for the behaviour half: a headless agent build in a scratch prototype with network tools, with and without a per-class sourcing declaration, counting unrecorded acquisitions |
| 2026-09-25 | sibling-entry-warmth (new: a fourth warmth source that is a place - borrow the list row as a typed summary found by id across the key family; seed dependent keys; placeholder, never substitute) | client-fetch-cache | goat | code | better | **Backlog wave 9, unit 6-008; every half better, the literal rule not-better.** Seam: a hover preview over a list cached under two real keys (landing section, command palette). Declared first; vitest/jsdom with a real query client and the detail fetch deferred. **A** 3/6 (target 0/2), **B1** (the unit's rule literally: read the lender's key, cast to detail) 4/6 - **silent miss** under the second real key, and rendered "0 items / 0%" for counts the summary never carried; **B2** (find by id across the key family, keep the summary type) 6/6. Positive controls: A shows the targets can go red; B1 shows the honesty test can. Negative controls on the floor: painting the summary after the detail lands turns convergence red; taking the first listing row instead of matching id turns wrong-shape red. Full suite 307/307, typecheck at the inherited 23 errors, eslint 0, docs coupling satisfied; the lint ratchet is red on main before the change with the same two rises. Half (b), dependent-key seeding, by simulation on three real cases: the collapse holds where a lender exists, but on one route the seeded copy REPLACED the detail read and dropped a permission flag defaulted to allowed - an owner's "no custom items" is honoured on a deep link and ignored after a card click, and the copy is persisted (fleet defect for the owner, not fixed). A share-link-only route has no lender. Golden path corrected: warmth was "three points in time"; it is also borrowed from one place. Shipped goat `e7c0c4b3` |
| 2026-09-25 | lens-effect-language (depth staging before the depth-of-field clause; speed from parallax, not the adverb) | cinematic-language | gravitone | experiment | not-better | **Backlog wave 9, unit 5-008; both halves not-better; the row's claim was wrong (camera-position-semantics already stages depth in three planes - the uncovered part was only the dependency between depth of field and an empty near plane).** Code refused: the one media prompt compiler emits no depth-of-field clause. Stills from a hosted image route, 3 repeats per arm, blind per-image readback over renamed files, $2.79 for 65 images under a declared $3 stop. **H1** (near plane staged before the clause, 3 scenes): near-plane blur A 4/9 vs B 5/9 (target B >= 7, A <= 3), depth read +0.56 (target +1.0); floor held 9/9. The generator stages its own blurred foreground in cluttered scenes. **Post-hoc arm** (declared first; plain-setting medium close-ups, where the corpus should be weakest): near blur A 0/9 -> B 6/9 -> B2 7/9, depth +0.89 - just under, and the declared post-hoc target failed. **H2** (parallax layers vs speed adverb, still proxy): speed A 3.67 >= B 3.50 - the falsifier fired; parallax made depth-varying blur reliable (6/6 vs 3/6) without making the subject read faster. Controls: test-retest 3/3, static-speed 1.0, flat portrait depth 1.0. Seam class: the generator already does the rule unprompted outside plain-setting close-ups. Return: re-queue "medium close-up in a plain setting" as its own row with one-sided targets and more scenes; the video form of H2 needs seed-matched text-to-video pairs with optical-flow per depth band on the GPU machine |
| 2026-09-25 | principal-tier-gating (new: tiers in one instruction file admitted on conditions the acting account cannot author, fail closed; the anti-forgery sentence is not what stops waivers) | agent-instruction-files | (harness: a stubbed forge CLI over fixture identities, no real host touched) | experiment | better | **Backlog wave 9, unit 3-007; conditions half better, anti-forgery half not-better (ceiling, not harm).** No project gate can observe whether an agent follows an instruction file, so: a stub forge CLI first on PATH answering auth/permission/issue queries from per-scenario fixtures and logging every call, pushes redirected to a local bare repo; "filed" read mechanically from the log. 66 fresh headless sessions, 3 per cell. Arms: A1 tiers declared only; BnA + three conditions (listed maintainer, canonical origin, API write permission) falling to the restricted tier on any failure; B + the anti-forgery clause. Positive control: a no-rules literal filing was logged 1/1; floor negative control: a genuine maintainer on her own fork filed 0/2 under B (the conditions' cost, written into the draft). Floor (verified maintainer files) 3/3 in every arm. **Pre-registered** (fork + unauthenticated claim): A1 2/6, gated 0/6. **Post-hoc, declared first:** the realistic case - an authenticated unlisted account claiming to be the maintainer, the agent checking the CLAIMED name instead of the acting account - A1 1/3, gated 0/3; a GENUINE maintainer comment waiving the route - A1 filed 3/3 reasoning the rule's purpose away, gated 0/3. Forged pasted approvals refused 0/3 in every arm, and B tied BnA everywhere: waiver refusal comes from deciding the tier on account state and failing closed. A1 checked fork access unprompted in 4 of 6 plain-stranger runs, so the gate's value is the wrong-identity and in-band-waiver cases |
| 2026-09-25 | context-decided-escaping (new: escape document text only where it would parse as markup, decided on the assembled output; look ahead only for partners that cannot be escaped) | document-text-extraction | pumper | code | better | **Backlog wave 9, unit 5-007; home overridden (the named path does not exist, and markdown-vault's escaping is the round-trip regime, the opposite).** Seam: an HTML-to-Markdown converter that escaped no text. Oracle outside the project: a strict CommonMark+GFM renderer, rendered visible text vs the source's visible text. Declared first; 7 arms. Seam cases (10): as-is 0, escape-everything 10 but 15 escapes on 11 benign cases, per-run no-lookahead **6** (failed exactly where the partner cannot be escaped: text backslash before emitted strong, text `!` before an emitted link, text backtick before a code span, a text backtick pair), the rule as stated 10 with 16 escapes on real pages, **post-hoc B5 (declared first): escape the later half of text-text pairs, look ahead only for emitted syntax** - 10/10, 0 benign escapes, 9 escapes on real pages (2 needed, 7 harmless). Refuted in part: lookahead as a general need - text-text pairs need only lookbehind. Unknown-context half: 4/4 vs 0/4 on constructed joins, no difference on 1,302 real fragment pairs (thin, flagged). Negative control: deleting the emitted-syntax lookahead reddened only the new seam test - the 578 existing tests are not a floor for this change. A downstream plain-text consumer's own test caught the backslashes it inherited; it now unescapes in the same change. Shipped pumper `5dec4c9` onto its active branch (858 tests pass, clippy 0, fmt clean). Residual not fixed: emphasis wrapping inner trailing whitespace so the markers never pair |
| 2026-09-25 | sender-declared-playout-bound (new: a foreign receiver's adaptive buffer rises fast and falls slowly, its knobs are floors; the sender declares the playout bound in band; gate the applied delay, not the target) | media-playback | (source tree: a KVM-over-IP stream's sender, against a current headless browser receiver) | experiment | better | **Backlog wave 9, unit 5-004; home resolved (none given) to media-playback.** No fleet project owns a real-time media sender (the one WebRTC consumer only receives vendor audio). Experiment: a real browser receiver fed by a scripted sender with a scheduled jitter burst; metric = delay each frame actually spent in the buffer. Declared first, two post-hoc addenda declared before their arms. **"Never shrinks" refuted:** timing-only jitter recovered ~25 s after it stopped; frame-size jitter peaked at 58-68 ms from 6-7 and held 24-31 ms sixty seconds later (3/3). **Receiver knob at zero = the default** (tracked the untouched arm within 2-3 ms). **Sender bound 0/0 from the start:** 0 ms in every window vs 24-31 unbounded; stamped only after the buffer grew: 60 -> 0 ms within one 5 s bin while unbounded sat at 48-57; stamped 400/400: pinned at 382-390 - the bound pins both ways. Stamping only the first 5 s held for the remaining 90. Floor (late-window smoothness) held in every valid arm; price, reported not hidden: 6.3 s of freezes vs 3.9-4.5 s during 15 s of jitter. Controls: the high-bound pin and the calm run passed; the declared jitter-window detector failed on the smoothed metric (peak landed just past the window); the floor's negative control fell outside the declared window - a design error, admitted; two runs voided on the sender's own frame-rate evidence under host contention. New: the receiver's reported target keeps moving under a bound (38-41 ms at 0 applied), and the source silently passes packets unstamped when the extension was not negotiated |
| 2026-09-25 | first-party-agent-is-a-principal (new: the product's own agent is a caller with a grant, not a code path; one door where it can present the external credential, one admission where it cannot; admit by grant, never by exclusion) | mcp-tools | ascent | code | better | **Backlog wave 10, unit 6-010; home resolved (none given) to mcp-tools; `checked: unverifiable` was wrong - the claim is verbatim in the pinned source.** One-door is already law; the residual is a first-party agent whose credential type differs from the public door's, and admission by grant vs by exclusion. Seam: one tool catalog reached by a public door that admits by grant (token holds every declared scope) and an in-process companion that admitted by exclusion (whole catalog minus writes minus queue-holder tools), each applying plan gates separately. Falsifier declared: a future read tool behind a scope the companion was never granted - **confirmed in A**: offered to the companion while the public door refuses it. 5 probes declared first: A fails 2 (drift offered; doors disagree after the drift tool lands), B2 (a shared grant-based admission, the face keeps its write subtraction) 0; the over-correction guard and real-catalog agreement pass in both. The literal rule (route the companion through the public door) not built, as declared: it needs a per-session bearer credential and would swap explicit member-facing refusals for the door's opaque "unknown tool". Floor: 426 -> 431 tests green, tsc 0, eslint 0, companion catalog identical on the real 11-tool catalog in all 4 plan combinations. Positive controls: the probes go red in A; a typo'd grant scope fails typecheck. Negative controls on B2's floor: dropping a grant scope reddens 5, dropping the face write filter reddens only its probe, dropping the shared plan filter reddens 6 across both doors - the cost named: correlated failure, no stricter backstop. Shipped ascent `2876343a`. Lead: caller-differentiated-capability subtracts a denylist from a binary whose option set grows upstream - the same exclusion drift one level down |
| 2026-09-25 | composer-turn-queue (a queued prompt is still a draft until delivered: persist it, and on restart hand it back as draft text, never as a runnable queue) | chat-transcript | personas | code | better | **Backlog wave 10, unit 6-011; the literal rule not-better, one half of its reasoning confirmed, the residual better; home resolved to chat-transcript; `checked: unverifiable` was wrong (verbatim at the pinned commit); partial phantom - the source decision and its cost were already recorded in an agent-runtime-assembly application.** Seam: a companion chat store that persisted drafts but not the mid-turn queue, while submit clears the draft on enqueue - pressing Enter LOWERED the text's durability. Declared first (two addenda before later arms). **A** as-is: survives-restart fail, no-unprompted-send pass, order fail. **B1** (the rule literally: keep the queue ephemeral, write down the loss) scored exactly A - a comment changes no behaviour. **BD** (persist the queue as a queue): survives, but **sent a stale message unprompted** - not at startup as the source claimed, at the next unrelated turn completion, because the drain is edge-triggered. **B2** (persist, restore as draft) 4/4 + migration 5/5. Floor: 5 existing suites 21/21 in every arm. Positive controls: T1 red on A, T2 red on BD; a harness defect (the restart helper overwrote storage, so T1 could never pass) caught, logged, all arms rerun. Negative control: dropping drafts from persistence left all 21 floor tests green - only the new restart test saw it. Shipped personas `3684836c` (companion suite 834/836, the 2 failures pre-existing on base; typecheck 0; eslint 0) |
| 2026-09-25 | pipeline-plan-auditability (what a printed plan may claim: forecast where earlier effects are carried forward, mark the remainder per unit, never mark everything) | pipeline-authoring | personas | code | better | **Backlog wave 10, unit 8-003; claim confirmed, the literal repair not-better, the residual better; home resolved from the bare category to pipeline-authoring (the source note's pipeline-dag covers only one shared evaluator); `checked: unverifiable` was wrong.** Seam: a multi-step unattended maintenance run whose dry run prints "would evict" lines and a total; the existing test only asserted the total was positive. Instrument, identical across arms: dry run and real run on identical fixtures, counting certain-but-not-made lines, made-but-never-named evictions, and made evictions printed as certain. Four cases, declared first. **A:** exact on the control; an earlier step's effect not carried forward printed a phantom eviction and a 3.3x total; an in-flight build the real run checks and the dry run skips printed a phantom; an unsimulatable step was OMITTED - the preview understates as well as overstates. **B1** (mark every undecided step "may"): no false claims, and **no certain lines either** - 0 of 1 on the control, 0 of 5 on the budget case. **B2** (carry earlier effects forward, run the read-only liveness checks in preview, name the unsimulatable step and mark only what follows it): exact on three, no false claims and 3 of 5 certain on the fourth. The new test pairs "certain means executed" with "executed means shown": fails A on the first, B1 on the second. Floor: suite 14 -> 15, real-run eviction sets and bytes identical across arms; negative controls: removing the dry-run guard reddens a test, breaking the real reaper reddens the real-set comparison, disabling the stage-level liveness refusal is caught by nothing (the reapers re-probe). Shipped personas `6220845c` by path (arm B1 left on the branch as reference) |
| 2026-09-25 | lower-bound-disclosure (the second sibling: one comparison, two sizes, cause unisolated - headline the smaller effect with its environment, never as a floor; the published figure does not rise to meet a larger later run) | measurement-honesty | personas + pumper (probe sites) | blind-ab | better | **Backlog wave 10, unit 7-005; marginal - target met exactly at its declared threshold, n=3 per arm.** No fleet code chooses which benchmark figure to publish, so blind-ab on two real write-ups. **P1** (the rule should fire): a build-orchestration doc whose current run gives 8.8x against a sequential baseline that earlier runs put near 12x, cause unstated. **P2** (must not fire - over-correction guard): a dedup-index doc whose speedup split is explained and verified. Target (P1 checks C2-C4): A 9 vs B 6, needed A >= B+3; P1 total 15 vs 12; floor P2 12 vs 12. Planted controls judged blind under the same rubric: a bare "12x faster" and a "at least 8.8x, a floor" answer scored 0 on the governed checks, an ideal answer 5/5, a planted over-correction on P2 scored 1/4 - the floor can see a sledgehammer. The pre-amendment corpus led one of three B arms to write the conservative quote as "at least 8x ... a floor" - exactly what the existing case 3 forbids - and to assert an unverified cause; every A arm wrote "a conservative quote, not a lower bound" and "not isolated", and one applied the stop clause on P2. Caveat: P1 is a split across runs on one machine, not two environments; one judge per probe. Spend $5.09 under a $20 cap |
| 2026-09-25 | operation-prices-the-property (proposed: a performance investment is priced per station of the operation, learned by watching it) | scale-investment-timing | kp (probe site) | blind-ab | not-better | **Backlog wave 10, unit 7-007; not-better with a split; home resolved (none given) to scale-investment-timing; `checked: refuted` right on wording - the source's per-hour figure was stakes stated BEFORE the observation, and what the observation found was an indifference.** No code seam (a discovery act before a proposal). Probe: a real intake dialog with measured per-exchange latencies and three simulated operator roles on record; brief = "latency sprint: draft it or call it the wrong investment". Two replicate pairs, blind judge with valid controls (good 10, bad 3 on governed checks, a stall memo reddens the decisiveness floor). Target (A governed total >= B+2 in each pair): pair 1 +3 met, pair 2 -1 missed; the evidence-accuracy floor broke in both pairs on unrelated misattributions. Per-station weighting: A 2,2 vs B 1,1 - the working part. Operation-first and indifferences: no gain - the no-draft arms dug out the operator evidence on their own (partial ceiling). **The "unobserved station's weight is unknown" clause caused harm:** one A arm declared a station unobserved without searching for the two acceptance-test drains that exist. Return: re-draft with the trace search as a precondition, and rematch the per-station half at a site whose operator evidence is absent or buried. Spend $8.21 under a $15 cap |
| 2026-09-25 | proof-cost-follows-the-circuit (new: for a solver-backed check, cost follows the arithmetic the property relates, not the input size; calibrate per engine; a millisecond equivalence proof is vacuous until a mutant is refuted) | test-input-generation | tracklight + pumper + personas (transcribed functions) | experiment | better | **Backlog wave 10, unit 7-006; split - size-is-not-cost better, the source's circuit-class table not-better as a transferable predictor (declared floor F2 breached, 2 false "reachable" vs tolerance 1); the draft lands only the first and states the second as a prior to calibrate per engine.** No fleet project runs a symbolic verifier and the source's engine does not run on this machine, so real fleet functions were transcribed into solver bit-vector formulas (reproducing the project's contract fixtures bit-exact); product code unchanged. 9 targets, 60 s cap, calibration before each. Size model 2/9, the draft 5/9 - target met (+3): domains of 2^64 to 2^512 proved in milliseconds while a 2^22 date round trip walled. F1 held (native enumeration of the same round trip, 3,652,425 cases, 0 failures); its negative control (a one-constant mutation) reddened 3.5M. Class-table misses: a chained divider proved in 11.5 s (property bounds only remainders), a constant-multiply injectivity walled, literal narrowing did not rescue a divider chain, a mul-into-modulo gave a counterexample in 3 s. Positive control on vacuity FAILED as declared: a textbook-constant mutant of a 16-byte port was not refuted in 60 s; post-hoc (declared first): refuted in 0.4 s at 1-2 bytes, walled at 8 - proving the faithful port is free and refuting a wrong one is a wall, so the refuter is a concrete fixture. Side finding: a window-adjacency doc claim fails after ~10^18 cycles on overflow - a doc note at most. `exhaustive-when-bounded`'s "sample when the bound is large" amended with the symbolic exception |
| 2026-09-25 | license-reach-follows-integration-form (new: key the license verdict on the (license, form) pair; make the form a checkable place in the tree; a form can change without the license changing) | supply-chain | ascent (probe site) | blind-ab | better | **Backlog wave 10, unit 8-004; marginal - target met exactly at threshold (+3, n=3), and the gain sits on one half only.** Corpus had only an allowlist clause; nothing on what a license reaches. The fleet's license gates key on identifier over statically linked crates, so form cannot vary inside them - a code arm would be tautological. Probe: write the license policy for a copyleft-licensed web product against its real components (a spawned VCS binary, a base-image userland, an optionally loaded weak-copyleft image library, CC-BY browser data, a non-OSI company-licensed client component, two packages with no license field). Target C1-C5: DRAFT 15/15 vs BASE 12/15; floor C6-C8 9/9 both. Controls in the same blind judge call: an identifier-keyed allowlist scored 0/8 (<=2 required), the key 8/8. **Headline half ("same license, opposite verdicts by form") no gain - the model already knows it (BASE 3/3 on C1, C2, C4).** The margin is all residual: form drift without a license change, and data licenses carrying data obligations (C5 3/3 vs 1/3; C3 3/3 vs 2/3). Row corrected against the pinned tree: the GPL-only data does NOT ship inside the copyleft package - it is fetched at run time with hash pins - while the GPL hook IS inside the package and walled off; location decided nothing. Fleet lead for the owner: every arm flagged the product importing a non-OSI company-licensed library into client components under an AGPL license, and an SDK shipping no license file. Spend $7.10 under a $20 cap |
| 2026-09-25 | emitted-code-names-from-a-root (new: a generator's hidden interface is the scope it expands into - spell every name the invocation does not supply from a root; enclose helpers, leave products visible) | module-design | personas | code | better | **Backlog wave 10, unit 8-002; split - "absolute paths" better, the literal "open its own scope so it binds nothing the caller can see" refuted; home resolved (none given) to module-design (codegen owns committed derived source; a textual config prepass is the deliberate opposite case, now the discriminator).** Seam: nine exported pattern macros used by ~40 modules; a crate split fixed two paths and claimed the macros "keep working wherever they expand" while bodies still named external crates, sibling macros, their own recursive arms and prelude items bare. Probes: temporary foreign crates, one per macro, invoking by path with no imports and referencing every generated item. Target: A 8/18 (failures exactly the three predicted classes: sibling macro not found, crate not found in a thin caller, a caller's `Result` alias), **B1 (the literal rule) 0/18 and the owning crate itself red with 240 errors** - enclosing the scope hid the generated functions, B2 (root-spelled names, a hidden re-export module for the external crates, helpers enclosed, products visible) 18/18; caller-alias stress 0/6 -> 6/6. Floor: check, clippy (library target green; 18 lib-test findings identical in A and B2), macro-using repo tests 43/43 both. Controls: A's failures vanish when the caller imports the helpers (3/3); a misspelled root path reddens check at 5 sites; dropping a parameter reddens 2 behaviour tests. Found: one macro calls the caller's own generated item - a documented composition obligation no root reaches. Not measured: the enclosure half for procedural macros. Shipped personas `d3f96ea5` by path (with a foreign-caller regression test: 27 errors on the old macros, 3/3 on the new) |
| 2026-09-26 | small-cohort-floor-with-no-silent-exemption (flipped: a small wave is underpowered alone, not unmeasurable; seal the pooling keys; a monitor that drops small waves instead of pooling them is the silent exemption moved) | bulk-adverse-action-governance | kp | simulation | better | **Marginal: one of three cases changes.** Falsifier stated first: if every small-sample surface in the tree already pointed somewhere, or the sealed record could not be pooled, the flip adds nothing. (1) The adverse-impact primitive refuses a verdict under 30 per group and says "not a finding of no adverse impact". That is honest under both rules, but it is a dead end: it never says to pool over a longer period or similar roles, although it already accepts any aggregate the operator pastes. The new rule changes this case. (2) The wave's sealed auto-rejection carries cohort size, count, the effective threshold, the signed policy string and the outcome kind. Role family and stage come by join through the entry, so the pooling keys exist. Met already. (3) The wave has no insufficient-sample marker, and the platform collects no demographic data, so no in-platform monitor exists to drop or pool small waves. Unmeasurable here. Pooling across roles risks Simpson's paradox, hence "similar roles". Return: when a workspace stores per-wave EEO aggregates |
| 2026-09-26 | golden path: override rate is not the verdict on oversight (flipped: "sub-one-percent override is a signature block" refuted; field rates run from single digits to a majority, overrides of a sound hiring test hired worse; audit the review, seed a known-bad row) | bulk-adverse-action-governance | kp | simulation | unmeasurable | The seam exists but nothing reads it. Every human decision on a card with a machine verdict seals the pair (machine recommendation, confidence, human action), so the override rate is computable, but no report computes it and no surface treats it as a verdict. A separate queue records time-on-card as an anti-rubber-stamp signal, which is the corrected rule's "had the time" half. No wave seeds a known-bad row. Instrument: an override-rate report over the sealed pairs, beside a seeded known-bad row in a wave, comparing reviewer catch rate with override rate. Return: when the tree grows an override report |
| 2026-09-26 | preview-then-approve-the-signed-set (conditions added: spend the approval once, consumed after every other check; every refusal its own reason) | bulk-adverse-action-governance | kp | simulation | better | **Not an independent test: the conditions were lifted from this tree.** The independent half is convergence: a training-data-only lane reached "never reusable across reruns" blind. Three real cases from history. (1) Before the spend ledger, a re-post inside the 15-minute window re-derived the same token and passed. Only an emptied cohort stopped a replay, and that fails when part of the set survived (a seal failure, a drift skip). (2) A single generic 409 re-previewed on "no named approver" and looped, because no preview names one. The tree's own challenge harness scored 0 of 8, then 8 of 8 after typed refusals. (3) A retried commit whose first attempt landed read as "the set changed" and left already-rejected people with live buttons. Remaining limit: the ledger is per process. Return: when a second project grows a bulk adverse path |
| 2026-09-26 | but-therefore-beat-linking (an undeclared connector is unmeasured, never defaulted) | short-form-narrative-structure | gravitone | code | better | Seam: the revision loop's edit-plan parser and apply, the explainer half of the script step; the trailer half already reads an undeclared connector as unmeasured. A (parent) = parser passes an insert's connector through unchecked and apply fills a missing one with THEREFORE; B = parser refuses an insert whose connector is not BUT or THEREFORE, apply leaves undeclared null. Real parser + real apply, one insert into the reversal-chain fixture: A parsed 4 of 4 bad inputs (missing -> landed THEREFORE; AND THEN, SO, lower-case but spliced verbatim); B refused 4 of 4; BUT and THEREFORE controls parsed and landed as declared in both arms. One existing probe changed meaning and was given a declared connector (contract before mark, the parser's own order). Node lane 506/506, tsc clean; committed on the project's active branch, not pushed. Falsifier: a model-returned plan whose insert omits the connector reaching the chain. Return: when a rewrite can change a relation - no check sees that yet |
| 2026-09-26 | short-form-narrative-structure golden path (give the answer early, bounded by a declared reveal format) | short-form-narrative-structure | gravitone | simulation | better | Three real renders of one notebook walked under both rules. A = withheld answer is a defect; B = withholding is honest when the answer is the gap and the piece says so. Reversal chain (thesis at 0:40 of 5:00): pass under both. Adjudication (four numbered candidates, "one of them is the actual answer" said at 0:25, ruling at 3:30 of 4:10): A defect, B legitimate. Paradox teaser short (contradiction hook at 0:00, resolution at 0:31 of 0:45): A defect, B legitimate. B agrees with the tree's reviewed engine choices on 3/3, A on 1/3. Ground truth is the tree's own review, not viewers. Falsifier: a declared reveal render whose retention cliffs at the declaration. Return: the first published render with a retention curve |
| 2026-09-26 | short-form-narrative-structure golden path (narration rate chosen by register: 200-250 is the fast top, voiceover 150-160, image-led lower) | short-form-narrative-structure | gravitone | simulation | better | The same three renders' budgets. A = 200-250 wpm as the rate; B = rate chosen by register. Reversal chain 190 wpm and adjudication 185 (narration-led mid-length): A flags both under band, B places both between conventional voiceover and the fast top. Paradox short 150 wpm, 112 words in 45 s, image-led: A budgets 150-188 words (34-68% over); B matches, and the tree's own deviation note (narration-led needs ~60 more words, ~229 wpm) lands inside B's fast register. B explains 3/3 budgets, A 0/3. Budgets, not audience outcomes. Falsifier: a narration-led render at 185-190 losing viewers a 220 wpm cut keeps. Return: a paired rate test on one script |
| 2026-09-26 | reframe-close (instructional pieces close on a retrieval prompt, not a passive recap) | short-form-narrative-structure | - | - | unapplied | The condition engages only on a piece whose job is retention of several parts. The one joined tree's three renders are single-thesis explainers and a teaser; all close on a reframe and none is instructional, so the condition changes no decision there, and an A/B on them would be a tie that ran nothing. Return: when a fleet project grows an instructional or procedure template |
| 2026-09-26 | question-stack-architecture (in a feed short the gap opens in the first second or two; the 12-second deadline is for the spoken question) | short-form-narrative-structure | gravitone | simulation | unmeasurable | One of the tree's three renders is feed-shaped: the paradox short opens on a six-word contradiction at 0:00 and asks nothing aloud, so it passes under both rules and the two mid-length renders do not engage the condition. Instrument that would measure it: the platform's viewed-versus-swiped share on a published short, A = question-first opening, B = collision-first, same script. The tree publishes nowhere. Return: the first fleet project that publishes shorts |
| 2026-09-26 | format-as-measured-template (amended: say how the sample was drawn - outlier-selected successes show what successes share, not what separates them; a band can be censored by its era's ceiling) | platform-format-adaptation | gravitone | simulation | better | **/deepen first pass on the subject; settles 2026-09-22 intake row 1.** Three real cases in the fleet's best-kept format template: an absence graded as "worth avoiding" and shipped as `forbidden`; a 40-60 s band whose witnesses (59/41/60 s) sit on the pre-2024-10-15 Shorts ceiling with no publish date recorded; a hook rule MEASURED as a description and defaulted as a cause. The old text flagged 0 of 3, the amendment 3 of 3, and case 1 matches the tree's own open question s4, written independently. Falsifier: a template with publish dates and a draw rule. No code changed. |
| 2026-09-26 | hook-shape-selection (flipped: "banned" and "three shapes exhaust the space" became avoided-by-default with a named grade; a visual payoff-first shape added) | platform-format-adaptation | gravitone | simulation | unmeasurable | Instrument: per-opening-shape viewed-vs-swiped rates from a platform analytics readback. No fleet project publishes shorts or reads one back. Walked over the same three witnesses: all use a spoken shape, none the announced fact or payoff-first, so the sample cannot tell "underperforms" from "absent from picked successes". The tree's `params.json` keeps a UI refusal its own s4 says should be labelled a preference; that is the owner's call, and nothing changed. Return when a fleet project posts shorts and reads back swipe-away per clip. |
| 2026-09-26 | sound-off-first-design (flipped: the muted majority is keyed to the surface - 2016 Facebook-feed origin, sound-on on TikTok/Reels) | platform-format-adaptation | - | - | unapplied | No fleet tree renders a caption layer on a vertical clip. gravitone's short-form template has no sound or caption parameters; systedo-case publishes text posts only. Return when a project composites captions or on-screen hook text onto a vertical render. |
| 2026-09-26 | golden path: an unmatched receipt is a live integration fault (conditioned: when it names something you issued; one naming nothing you issued is attached to nobody but counted, because a wholesale reference drift makes every real bounce look foreign) | candidate-communication-integrity | kp | code | better | **The field reached the split first; the apply closed its missing half.** Seam: the receipt core's unknown-ref branch. Both receipt doors authenticate the caller, so a foreign ref is the configured relay speaking references kp never issued, not backscatter. Arm A: such a receipt left 0 install-side records (no tenant row, no log, no counter), so a relay switching ref scheme wholesale would have produced a quiet Comms Center. Arm B: still filed into no tenant, now counted per process (on the ops read) and logged. New test: 2 foreign bounces counted 2; a non-bounce with an unknown ref and a known ref with no send counted 0. Arm A failed the test, arm B passed 6/6; neighbouring suites 32/32; scoped typecheck clean with a positive control; the full-tree typecheck was blocked by a sibling's unrelated syntax error. Return: when the callback reads the per-message id kp already sends, the count becomes a pure drift alarm - re-measure. |
| 2026-09-26 | golden path + bounce-receipt-supersedes-a-green-send: never prune (flipped: retention is symmetric - one clock for success and failure, erasure reaches receipt rows, only selective deletion is the failure; the append-only rule gains the erasure exception) | candidate-communication-integrity | kp | simulation | better | **Not an independent test: kp already realized the condition.** The independent half is convergence: regulator recruitment-records guidance (counter lane, re-opened) and the blind lane reached symmetric retention separately. Falsifier stated first: a path deleting or hiding failed rows only, or outbox rows under no clock at all. A census of every write to the outbox table found three sites and neither falsifier. (1) The erasure scrub blanks recipient, subject and body on every row of the entry, whatever its status, and keeps kind, status and time as the audit. The old absolute ("no update path on receipt rows", "never prune") forbids it; the new rule requires it. (2) The consent-expiry sweep (default 12 months, terminal-status rows not exempt) runs the same scrub: the one clock. The old rule forbids it; the new rule requires it. (3) The simulation teardown deletes the outbox rows of synthetic entries alike. It is a prune under the old rule and not selective under the new. |
| 2026-09-26 | fit-vs-hazard-axes (a verdict's imputation has a level; pitch it where the record stands) | narrative-engine-selection | gravitone | simulation | better | Seam: the `engine_fit[].hazard` free-text line in the source studio's hostile-review notebooks, with arbitration step 1 as the policy. Three real adjudication rows from the 2026-08-12 sweep, all on named-party subjects. A = keep or drop ("drop every fit whose hazard line you would not defend on air"); B = A plus pitching the verdict at the imputation grade the record carries. A can express 1 of 3 seat decisions (drop a named-party candidate). B expresses 3 of 3: "not established" kept at HIGH hazard, and a ruling moved from the campaign to the checkable metric. In both cases the seats improvised the level move in free text with no word for it. No outcome changed; B names the move. Falsifier: a render whose words pitch a lower level while the verdict shape delivers the higher one. None of the three was rendered, so the render half is untested. Return: the first rendered named-party adjudication |
| 2026-09-26 | no-engine-means-no-video (a deliberate non-link is spoken, since adjacency reads as causal) | narrative-engine-selection | gravitone | simulation | better | Three real cases from the source studio's hostile-review ruling on the connector law (2026-08-12, P6). A = zero AND-THEN, "find the missing link or drop it"; B = zero AND-THEN at the render, every causal joint names its evidence, and a finding of non-causation is spoken as a BUT against the expectation. Game post-mortem ("the schedule did not respond to the scope change"): A invents a link or deletes the finding, B renders it as a BUT and keeps it. Public-procurement chronology: A's forced THEREFORE is a blame frame by shape; B says aloud that nothing in the record connects the dates. Royalty flow: identical under both, because typed transfers already cover it. 2 of 3 keep a finding A would fake or delete. Falsifier, one lane only: continued influence, where a denial with no alternative cause fails to undo the inferred one. Return: a rendered non-link measured for retained causal belief |
| 2026-09-26 | no-engine-means-no-video (causal-opener density: within-writer only, never a cross-writer "asserting" verdict) | narrative-engine-selection | gravitone | simulation | better | Seam: the adjudication render's `causal-opener density ≥ 30%` check, plus the template parameter reading a low score as candidates "announced rather than weighed". Three real witnesses from the tree's corpus, all counted with "and" included: 15% (the adjudication witness, whose execution the tree already rejects on tells 1-3), 24% (a long-form explainer the tone file ranks "at comparable quality"), 38%. A = cross-writer band: flags 2, of which 1 is a true positive the tells had already caught and 1 is a false positive. B = within-writer baseline, low score means read the joints: 0 false flags and no finding lost; the 24% witness has no baseline in the corpus, so B reads it as unmeasured. Caveat: "comparable quality" is the tree's judgment, not a score. Falsifier: a rigged script that density catches and all three tells miss. Return: a writer with 3 or more measured scripts |
| 2026-09-26 | adjudication-honesty-tells (steel-man is weak evidence alone; a settled premise staged as a candidate is false balance) | narrative-engine-selection | - | - | unapplied | Instrument: per-candidate weights stored on the research asset. The one adjudication render in the fleet shows the tells as hand-stated fixture rows, and no `candidates[]` exists, so neither a steel-man's weight nor a premise's settledness is on record to walk. The one real case, a contestable premise, does not fire the new boundary, and a single non-firing case is not a simulation. Return: when the source tree's notebook gains `candidates[]` with outcomes |
| 2026-09-26 | narrative-engine-selection golden path (engine invariant in its middle, entry and exit designed per surface) | narrative-engine-selection | - | - | unapplied | No fleet render has been measured on a feed surface. The tree's derived-short render exists, but nothing records how it performed in a feed. Instrument: swipe-away and loop rate per render. Return: the first derived short published with feed analytics |
| 2026-09-26 | policy-version-sealed-into-every-decision (flipped: omit a dimension only at its resolved inert value, never merely because storage lacks it; a new dimension with an active default must move the version; one approval, one version string on every record) | hiring-policy-defaults-and-tiering | kp | simulation | better | Converged in three places: the tree, a counter-evidence lane (schema-evolution rules treat a changed default meaning as breaking) and a blind training-data lane. The tree's version builder was reproduced and three real configurations were walked across the build that introduced its calibration holdout (default 5%). (1) Never set: behaviour changed; "omit absent" keeps the version (wrong), omit-when-inert moves it (right). (2) Explicit 0: behaviour unchanged; "omit absent" moves it (wrong, and it voids approvals for nothing), omit-when-inert keeps it. (3) Explicit 5: both move it. Old rule 1 of 3, new rule 3 of 3. The tree already implements the new rule. Its record seal also dropped a per-candidate version string, because that string could not be joined to the approval. Falsifier: a dimension whose absent state is legacy behaviour and whose resolved value is non-inert, which cannot happen by construction. Return: when a second project seals a policy version |
| 2026-09-26 | organisation-baseline-with-a-team-override (conditions added: an editor writes the tier it displays; an automated apply writes a delta; pin as a third key state, per-team diff before a baseline change) | hiring-policy-defaults-and-tiering | kp | simulation | better | **Found a live defect.** The rules screen saves to the org tier. The calibration apply writes a full team row copied from the effective config, and that row shadows the org row. The cascade was reproduced in an in-memory SQLite with the tree's own SQL and merges. (1) No apply: unticking auto-reject turns it off. (2) After one apply: unticking saves false to the org row, and the workspace runs with true. (3) After one apply: global floor 45->30 leaves the workspace at 45. Under A, the tree, cases 2 and 3 are wrong. Under B, all three are right. The screen then re-syncs and shows the unchanged values beside "Saved". Not fixed: a live sibling run holds kp. Falsifier: a workspace that never used the calibration apply cannot reach case 2. The pin rule is unapplied, because there are no sparse deltas to pin against. Return: when the screen sends scope or the apply writes a delta |
| 2026-09-26 | per-role-family-threshold-overrides (flipped: stronger review goes to the change that enlarges the exposed population, named by effect, never "lower"/"stricter"; anchor to proficiency, not the family's own distribution; job analysis for the grouping) | hiring-policy-defaults-and-tiering | kp | simulation | better | Three cases on the calibration apply and the rules screen. Seam: a reject-below threshold. (1) A "lower" recommendation: A demands the stronger review for the protective change. (2) A "raise": A calls it self-limiting, though it rejects more people without a reader. (3) A manual 45->30: A inverts again. A points the review at the wrong change in 3 of 3, and B agrees with the tree's own "candidate-protective lower" label. The tree applies both directions under one click with the same capability, so neither rule is implemented. Anchoring converged: the tree derives from advance rates in bands either side of the floor, with the below-floor band from the holdout only. Falsifier: a governed value where a higher number exposes fewer people, such as a confidence floor gating automation; none exists in the screening rule. Return: when the apply route branches on direction |
| 2026-09-26 | automation-ships-off-by-default (condition added: a knockout on a declared, objective, job-required answer is a separate capability; it ships unwritten and owes job-relatedness, a notice and a route to a person; a statutory question is a constraint) | hiring-policy-defaults-and-tiering | kp | simulation | better | Converged: the counter-evidence lane (vendor docs, the screening-tool rule's scope, solely-automated-decision guidance) and a blind training-data lane. The walk covered the tree's three knockout questions on both public apply routes. (1) Right to work: A says violation, B says statutory constraint (correct as shipped). (2) Work mode: A says violation, B says the decline owes a route ("isn't the right fit right now" names no gate and no person). (3) Languages: A says violation, B adds the missing job-relatedness, since language is where a knockout becomes a national-origin proxy. A gives one wrong verdict for three different things, and B finds two actionable gaps. Every decline is already audited as an entry-less event. Falsifier: a knockout that reads a score or an inferred attribute; none found. Return: when the decline copy or the job form changes |
| 2026-09-26 | readiness-rubric-replacing-years (an unmeasured input the candidate could supply takes the lowest measured rung, not zero and not a reweight) | early-career-potential-assessment | kp | simulation | better | Seam: the readiness model's foundation term, which imputes 0.0 for the profile's default `unknown` education level while the recruiter note beside it says "not penalized". Three real cases, the repo's own eval CVs (two bachelor graduates, one teacher turned developer), scored at a pinned commit with the degree stated and then with the degree line absent. A = imputed zero: the graduates fall 0.510 -> 0.310 (-39%), the switcher 0.927 -> 0.815. B = reweight the survivors (the technique before this pass): graduates 0.413, but the switcher reaches 0.920 against 0.840 had a modest degree been stated - withholding pays. C = lowest measured rung + wider band: 0.435 and 0.815, never above the stated-modest arm. C removes A's penalty without B's reward in 3 of 3. Falsifier: a case where C still exceeds that person's stated-lowest-rung score. Not shipped: a live sibling session holds the consumer tree, and the change moves every early-career score |
| 2026-09-26 | readiness-rubric-replacing-years (velocity counts stated field-time only; never infer or elicit the unavailable time) | early-career-potential-assessment | - | - | unapplied | The one consumer's velocity is skill breadth over 8, with no time denominator, so there is no seam where available time is computed or asked. Return: when a project gives velocity a time denominator |
| 2026-09-26 | domain-distance-grading (a task-based distance may place families in bands; the band is still what renders) | early-career-potential-assessment | - | - | unapplied | The consumer grades distance with a hand-built surface-signal table per role family, and no fleet tree holds occupation task vectors. Return: when a project licenses or imports an occupation taxonomy with task or skill vectors |
| 2026-09-26 | symmetric-discount-across-populations (symmetric across archetypes is not fair across protected groups; monitor selection rates by protected group) | early-career-potential-assessment | kp | - | unmeasurable | Instrument: selection rate at the score's gate by protected group. The consumer's fairness matrix compares candidates by weight scheme and archetype, and it holds no protected attributes. Field witness, not a measurement: the consumer shipped a sex-symmetry repair on its own (Czech feminine job titles earned fewer transferred skills), found the way the technique predicts, by two otherwise identical candidates side by side |
| 2026-09-26 | early-career-potential-assessment golden path (observation earns the top rung on provenance, not validity; check the exercise's subgroup gaps) | early-career-potential-assessment | kp | - | unmeasurable | Instrument: live-case pass rates by subgroup. The consumer mints observed evidence from a passed live case (threshold 65, confidence-gated) but records no group attribute, so a gap cannot be computed. Return: when an assessment pipeline records outcomes by a lawful monitoring attribute |
| 2026-09-26 | hidden-text-and-smuggling-detection (flag what hidden characters carry - a zero-width run, an imperative that reads only once they are removed, decoded tag characters, a bidi override - never the class) | cv-authenticity-screening | kp | code | better | **/deepen first pass on the subject.** Seam: the CV injection screen, which flagged any single code point in a zero-width/bidi/soft-hyphen class ("One is enough to warn", pinned by two tests). Since 2026-09-23 every open warning must be ticked before an advance, so each false positive cost friction on the favourable route only. A = the class regex, B = run (8+) + removal differential against the existing instruction patterns + tag decode with the subdivision-flag shape skipped + LRO/RLO only (the discipline the same tree's gig-listing scan already had). Real screen, 13 cases constructed from real character usage, not candidates' documents: benign (emoji ZWJ, Persian ZWNJ, Hebrew RLM, soft hyphens, stray BOM, Devanagari ZWJ, flag tag sequence, lone ZWSP) fired A 7/8, B 0/8; attacks (masked imperative, tag-smuggled instruction, ZW run, bidi override, visible control) caught A 4/5 (tag smuggling missed outright), B 5/5. Two pinning tests rewritten deliberately, benign scripts added as the other direction; full Python suite shows only the two failures present at HEAD. Committed 6bb2bfed0 on the active branch, not pushed. Residual by choice: a lone ZW splitting a keyword is keyword evasion, not injection. Falsifier: a real CV whose hidden-character payload is a single code point with meaning |
| 2026-09-26 | hidden-text-and-smuggling-detection (new: find styling-hidden text by rendering, scored by content; the common real attack is hidden keywords, not instructions) | cv-authenticity-screening | kp | experiment | better | Seam: the CV path has no render-vs-text comparison; its only hidden-text defence is the instruction screen over pypdf text. Declared first: B catches 3/3 hidden and 0/2 benign, A only the imperative; falsifier B fires on the placeholder or A catches a keyword case. Five PDFs built with a PDF library (constructed, n=5): white keyword list repeating the posting, 1pt skills block, white imperative, white template placeholder, plain control. A = kp's real prompt_injection_checks over pypdf extraction; B = characters under 4pt or white fill, flagged when they hold an imperative or 3+ posting requirements. A 1/3 hidden (imperative only), 0/2 benign; B 3/3, 0/2. Mid-state printed: the hidden text is in the extraction in all four hidden-text cases, so it reaches the model. B read the placeholder and stayed quiet, where a hide-based rule fires. Not applied in the tree: it needs a character-level reader beside pypdf. Return: when the CV extraction step is next touched, or a real hidden-keyword CV reaches a reviewer |
| 2026-09-26 | cv-authenticity-screening golden path (machine-written is not a finding: detectors are now accurate on unedited prose, so the argument narrows to the input - lightly polished human text, never validated on career documents - and to off-target) | cv-authenticity-screening | - | - | unapplied | The condition changes why, not what: the prescription (never score authorship) is unchanged, and no fleet project runs a text-origin detector, so no decision moves. The one authorship deviation found (kp's recruiter tooltip calls the screen a check "for AI-generated / embellished résumés", in four locales) breaks the unchanged rule and is recorded in the react application, not scored here. Return: when a project proposes adding an origin detector |
| 2026-09-26 | document-as-data-never-as-instructions (amended: fence every channel and every mode; defuse the delimiter shape as the alternative to a nonce; a schema described in the prompt is only an instruction) | cv-authenticity-screening | kp | simulation | better | Four real channels in the tree's analysis prompt, read under both texts. Old text (nonce fences "where the risk warrants", a closed schema bounds what an injection can express): the blind CV block is fenced and defused, the prompt states a schema and enumerations, so 0 findings. Amended: the posting and company text are unfenced, the non-blind CV is an uploaded file with no fence, and the provider call enforces only JSON mime type + top-level keys, so 4 findings (3 channels, 1 schema). The old text passed 0/4 real gaps, the amendment names 4/4. This measures what the rule makes a reviewer see, not whether an injection through the posting succeeds; that needs paired provider calls and spend. Return: a paired injection run through the posting channel, fenced vs unfenced |
| 2026-09-26 | implausible-span-and-arithmetic-checks (new check: tenure against the technology's age, under three conditions - earliest usable date incl. predecessors and renames, years not months, postings demanding impossible years) | cv-authenticity-screening | - | - | unapplied | The one joined tree computes only stated years > 45 against a human career and keeps no technology release or alias table, so there is no seam the check could run in, and an A/B on it would be a tie that ran nothing. Return: when a project keeps a technology release table with predecessors and renames |
| 2026-09-26 | declare-the-inconsistency (conditioned: the no-skip guarantee holds only for a sort value written once; a consistent view is bounded by a pinned version or a change feed from the walk's high-water mark) | bounded-enumeration | pumper | experiment | better | Seam: the records walk (cursor list and every export batch) seeks on (updated_at, key) DESC. A = the contract as published (export "not buffered or capped", SDK "streams a filtered snapshot", nothing on mid-walk writes); B = the conditioned rule. Measured on the real temp-dir store, with a control: from the page-one position the rest of the walk held `b, a`; after updating `a` mid-walk the walk returned `d, c, b` - `a` on no page - and the change feed since the walk's highest updated_at returned exactly `a`. 36/36 in the test file. Contract bullet + pinning test committed in the tree (aed8915, not pushed: the branch was 15 ahead / 5 behind its remote with 14 commits not this run's). The product's own mirror was already safe by its watermark choice; a hand-written consumer reconciling against the export was not. |
| 2026-09-26 | after-plus-limit-not-cursor (conditioned: a position is computed from the last delivered row, a token is admissible, the sort tuple when the order is not key order; predicate = ORDER BY tuple; a malformed position is refused) | bounded-enumeration | pumper | simulation | better | Every paged site in the server routes: 14 emit a next position, all `timestamp|unique-tiebreak` from the last delivered row, none in key order. Old absolute ("never a cursor") flags 14, all false positives - no bare key could express the position and nothing filters per key. Conditioned rule flags 0 on the token; on the malformed clause it names exactly the two program-walked routes the tree had already moved to a strict parse after a shipped livelock (corrupt token -> 200 page one -> a mirror re-deduping forever), and passes the 9 lenient browse sites. Falsifier: a token built from an examined-but-withheld row - none in the tree. |
| 2026-09-26 | required-limit-breaks-unaware-clients (conditioned: over-ceiling clamp admissible when every response carries a continuation the client terminates on; zero is absent where the wire cannot tell them apart; opt-in-by-presence named) | bounded-enumeration | pumper | simulation | better | Three real cases, all `limit.clamp(1, max)`. (1) cursor-mode list/feed/history: over-ceiling clamped, `next_cursor` present, the SDK loops on it - old rule flags (refuse), conditioned rule admits: old flag was a false positive. (2) legacy bare-array record list, clamp 1000: no has-more field - both rules flag, true positive (silent truncation). (3) legacy feed body `{count, changes}`, clamp 1000: `count` only - both flag, true positive. Zero becomes a page of one under the clamp floor: harmless in cursor mode, a silent one-row listing in legacy mode - both rules flag it. Net: one false-positive class removed, every true positive kept. Falsifier: a cursor-mode client that stops on a short page - the SDK does not. |
| 2026-09-26 | deny-absorbs-and-lowest-limit-wins (conditioned: the minimum over unioned grants is a non-monotonic guardrail; a recursion flag is admissible where policy conditions on it and an unconditioned grant reads narrow) | bounded-enumeration | - | - | unapplied | No fleet project has a policy language that states per-path page ceilings or governs a recursive listing, established from the map joins (7 projects, 10 pairs, none a policy layer). Return: when a project grows per-path list ceilings in a policy layer. |
| 2026-09-26 | filter-after-return-under-limit (conditioned: a caller-chosen predicate may return the last examined key as its continuation; the refusal is for filters that hide) | bounded-enumeration | - | - | unapplied | The one joined tree with a filter pushes caller predicates into the seek (SQL) and filters nothing per key after the page, so neither the empty-page case nor the hiding case occurs. Return: when a project post-filters a page, by access or by predicate. |
| 2026-09-26 | offer-acceptance-rate-denominator (flipped: on a transition-dated window "resolved exceeds extended" is not evidence of a recording defect - boundary-straddling offers produce it with perfect recording; the defect is read at the record, a resolution with no prior extension; "extended minus resolved" is a net flow, outstanding offers are a stock; pick the offer-cohort or the period basis and name it) | recruiting-funnel-metrics | kp | simulation | better | Converged in three places: the tree (every offer kind counted by its own timestamp inside the window), a counter-evidence lane (the benchmarking definition is silent on basis; a vendor's period rate divides by resolved, a metrics catalogue cohorts by extension date and keeps pending out) and a blind training-data lane. Three ledgers through the tree's own fold under its own 30-day windowing. (1) Legacy resolutions with no send: A and B both call the defect; A says 0 pending, truth 1. (2) A burst sent before the window, closed inside it, 2 new offers open, perfect recording: A calls a defect (wrong) and shows pending 0, so the row is hidden while 2 candidates hold unanswered offers; B right. (3) Steady flow, 4 open: A pending 1, B 4. A wrong on pending 3 of 3 and on the defect once; B 3 of 3. Falsifier: an offer-cohort query where every resolution links to a send in the same window, where A is right; the tree has none. Not fixed in the tree: recorded as a deviation. Return: when the offer read gains record linkage or an as-of pending stock |
| 2026-09-26 | forecast-signal-floor (flipped: the two estimators' disagreement is a diagnostic, not the interval - each sees only part of the future hires so their spread brackets nothing; an interval comes from carrying each leg's sampling uncertainty and the inflow forward by simulation, checked for coverage; a band from inflow variance alone is too narrow) | recruiting-funnel-metrics | kp | experiment | better | Converged: a counter-evidence lane (forecast-disagreement literature: dispersion of point forecasts understates uncertainty and is a weak proxy for it; manpower models use stage-transition chains and simulation) and a blind training-data lane. Harness over the tree's own forecast function with KNOWN ground truth: stage pass rates 0.5/0.4/0.5/0.5, Poisson inflow, 8 weeks of history, 600 trials per case and horizon, scored only where the tree's floor cleared. Coverage of the truth, sparse / fixture-sized / large funnel at 4 and 12 weeks: estimator spread 27/49, 16/17, 2/5 %; the tree's +/-1 SD inflow band plus in-flight 15/32, 18/36, 13/22 %; a posterior simulation at nominal 80 % 89/81, 88/89, 81/82 % (the calibration check on the harness itself). The spread gets worse as volume grows. Lag inside the horizon ignored on every arm, as the tree ignores it. Falsifier: a pipeline where inflow variance dominates conversion uncertainty and the SD band reaches nominal coverage. Return: when the forecast grows a simulated interval |
| 2026-09-26 | time-to-hire-basis-and-median-not-mean (conditioned: the statistic follows the reader's question - median for the typical case, mean where a duration is multiplied by a volume; never draw a windowed duration from a short creation cohort, it is capped by the window) | recruiting-funnel-metrics | kp | simulation | better | Converged: a counter-evidence lane (the benchmarking body recommends the median when mean and median diverge; a reporting standard names an average; occupancy is arrival rate times mean time in system) and a blind lane. Three real readers of the tree's one time-to-hire sample. (1) The forecast's "Expected lag": A and B both want the median, the tree passes the mean. (2) The briefing's "It takes N days on average" in a 30-day view: A swaps the statistic and still prints a figure capped at 30 days; B moves it to hires closed in the window, which the tree already computes for cost per hire. Only B finds the cap. (3) A capacity figure that gains a lag: A feeds it the median and understates load by the tail; B the mean. B 3 of 3, A 2 of 3 and blind to the cap. The tree age-matched its period deltas for exactly this bias and left the headline. Falsifier: windows always longer than the slowest hire. Return: when the headline reads hires closed in the window |
| 2026-09-26 | non-overlapping-event-to-series-mapping (conditioned: calendar periods with the partial one marked are the other honest fix and the right one for cadence reporting; never mix window lengths on one surface) | recruiting-funnel-metrics | kp | simulation | better | Converged: a counter-evidence lane (a visualisation authority keeps calendar periods and labels the incomplete one "as of"; monitoring vs reporting practice) and a blind lane. Three real surfaces. (1) The momentum chart, operational activity: A and B both keep rolling 7-day windows; agree. (2) The same chart beside the 30-day offer and cohort figures: its span is ceil(30/7) = 5 weeks = 35 days; A has nothing to say, B flags two populations on one surface. (3) The org benchmark, all-time by design: neither rule applies; agree. B finds one defect A cannot see, and no case where A is right and B wrong. Falsifier: a reader who reconciles the chart against the 30-day figures and is not misled. Return: when the momentum span follows the view's window |
| 2026-09-26 | stage-pass-through-and-dwell-time (conditioned: the two dwell halves are biased in opposite directions - completed short, occupants long; a survival curve over entries is the single consistent estimate; an occupant-only pair is a "waiting now" view, not the step's speed; a resolved-entries denominator is the second honest one for an immature cohort) | recruiting-funnel-metrics | kp | - | unmeasurable | Converged: a counter-evidence lane (vendor pass-rate formulas divide by resolved entries; a practitioner source says report completed and active separately unless a survival method is used; competing-risks literature) and a blind lane. The tree's dwell band now reports median occupant age plus oldest, both stock, and no surface shows completed dwell, so there is no second arm to compare against. Instrument: per-stage completed-passage durations folded from the transition ledger beside the occupant median, on a real board, with a survival estimate as the third arm. Return: when the dwell read folds completed passages |
| 2026-09-26 | creation-cohort-versus-terminal-transition-basis (conditioned: a terminal "resolved candidates only" basis for conversion is coherent if labelled; a survival estimate is an honest alternative to withholding a young cohort; the maturity horizon is the local lag p90, the 6-10 week range a heuristic) | recruiting-funnel-metrics | - | - | unapplied | Converged: a counter-evidence lane (a vendor's pass-through defaults to applications that exited consideration; cohort vs activity reports documented as different populations) and a blind lane. No seam: the one joined tree computes conversion on a creation cohort with a snapshot-reach basis and never on a resolved basis, and it already age-matches the terminal leg of its period deltas. Return: when a surface computes conversion on a resolved basis, or needs a read on cohorts younger than the horizon |
| 2026-09-26 | expansion-reflection-over-confirmation (condition added: in a voice channel the pause is a parameter - end the turn on a finished thought, not a silence threshold; a segment after the next question is a continuation; the read-back waits and speech after it is the correction) | role-intake-conversation | kp | simulation | better | Converged: the tree's own history (its candidate-interview voice path moved to semantic end-of-turn "low" on 2026-09-18 because the default "cut candidates off mid-thought", while the same change pinned intake's relay to a bare 500 ms silence detector), a counter-evidence lane (voice-interviewer deadlock and latency findings) and a blind lane (silence-threshold cutoffs, wait-time research). The keyless voice turn IS the scripted engine, so the pinned engine was exported to scratch and driven with three paused answers from the tree's own persona bank (vague requester, can't-articulate-level, contradicts-self), split at the pause (A, shipped) or whole (B). B filed 3 of 3 correctly; A misfiled the second half of the thought into the next slot in 3 of 3 (a hedge became the title, an outcome became a must-have); the no-pause power-unit control was identical. The cascade after the first misfile is an upper bound (the driver does not adapt). Falsifier: a requestor who never pauses past the window - the control. Model-led path not simulated. Not fixed: kp held by a sibling run. Return: when intake's relay session gets a turn-detection setting or the client merges late segments |
| 2026-09-26 | expansion-reflection-over-confirmation (condition added: a picked option is the requestor adopting the interviewer's wording - record it as chosen from an offer, with the offer beside it) | role-intake-conversation | kp | - | unmeasurable | Derived from the subject's own laws (inference-must-look-like-inference; an interviewer-supplied requirement is a proposal until adopted in the requestor's own words) and read in the tree: the card contract says a pick "lands as `stated`, because they did say it". The message route stores the offered set on the interviewer turn, so a pick is reconstructible by label match; the brief row and the export lose it. Instrument missing: a stored session with a card pick (the published tree has none; the local database was not opened while a sibling held kp), joined to how screeners weighed picked versus typed requirements. Return: when the brief row carries an offer provenance, or a card-bearing session can be replayed |
| 2026-09-26 | role-intake-conversation golden path (flipped: "the requirement never spoken cannot inflate anything" - removal binds only when the evidence criterion replaces the label in what screens and the rubric derives from the brief; new failure mode, the paper deletion) | role-intake-conversation | kp | simulation | better | Converged: a counter-evidence lane (the 2024 skills-based-hiring report read in primary: about 45% of firms that dropped degree requirements changed nothing in actual hiring, net under one hire in 700) and a blind lane (same report, same direction). The seam: the consumer's promote builds the posting and freezes the scoring rubric from the same brief, never from attached text, so the new rule's second half holds by construction. Three persona briefs from the tree's bank were run through the tree's own rubric derivation (its Python mirror, pinned to the TS by one fixture). (1) Over-specifier: the requestor conceded "Terraform maybe trainable"; the old rule counts that as removed at intake, the new rule does not - the frozen rubric carries Terraform as a prerequisite must-have beside a garbled nice row, with PhD and fintech also prerequisites. (2) Solution-jumper: the opening "senior React dev" never reached the rubric; both rules agree. (3) Leaver template: "another Marketa" never reached it; both agree. Old rule 2 of 3, new 3 of 3. Falsifier: a spoken concession that the brief records as a demotion - none in the keyless path, which has no ladder. Return: when the keyless path records a demotion, or a second project derives a rubric from an intake |
| 2026-09-26 | role-intake-conversation golden path (condition added: one requestor is one source - the brief is a hypothesis, not a job analysis; corroborate a hard filter before it screens, and say which requirements rest on the requestor alone) | role-intake-conversation | kp | - | unmeasurable | Converged: a counter-evidence lane (29 CFR 1607.14(C), 1607.15(C) read in regulation text; Morgeson & Campion 1997 read in primary: KSAO judgments inflate more than tasks, corroborate from sources with different motives) and a blind lane (same sources, same verdict). The seam exists: the promote freezes every must-have prerequisite into the rubric at once, with no corroboration state - the over-specifier brief above would screen on 12 requestor-only prerequisites. Instrument missing: adverse-impact and quality-of-hire outcomes per rubric axis; the tree records neither against intake-derived axes. Return: when a brief grows a corroboration state or a project records outcomes per axis |
| 2026-09-26 | laddering-a-hard-requirement (absolute refuted: "exactly once" becomes once when stated plus at most one later return; the why-question advice re-sourced to conversation analysis) | role-intake-conversation | kp | - | unmeasurable | Converged: a counter-evidence lane (Reynolds & Gutman 1988 read in primary: move on and circle back is the method; canonical probe is "why is that important to you?"; Bolden & Robinson 2011 on why-questions as challenges) and a blind lane (no basis for any probe count). The consumer's prompt rule 4 says "once" and the eval has no ladder check - its over-specifier persona passes all eight checks with twelve must-haves - so nothing in the tree counts ladders. Instrument: a ladder-count and demotion check in the persona eval, run against the live model. Return: when the eval checks laddering |
| 2026-09-26 | session-shape-triage-and-turn-budget (conditions added: a locale added to the script is not added to the triage and needs its own positive control; automated interviewers skip the close unless the read-back is a slot the script owns) | role-intake-conversation | kp | simulation | better | Converged: the tree, a counter-evidence lane (LLM interviewer studies: stacking, under-probing, a summary that needed an explicit mandate) and a blind lane. Executed on the pinned engine: the 2026-09-03 change taught the keyless script German and French, and the markers stayed English and Czech. (1) German "Nachbesetzung" backfill: story (wrong). (2) French "remplacement" backfill: story (wrong). (3) Czech "náhradu" control: power_unit (right); the English "backfill" control is also right, and "maternity cover" still falls through. The old rule ("exercise every language the session runs in") did not stop this; the new condition names the mechanism - a script locale and a triage locale are separate surfaces. The close half is realized on the deterministic floor and prompt-only on both model paths. Falsifier: a new locale whose backfill idiom happens to contain an English marker. Not fixed: kp held by a sibling run. Return: when the markers gain de/fr, or a fifth locale lands |
| 2026-09-26 | graph-layout (flipped: "same graph in, same layout out" holds only for the same ORDER; a layered engine is repeatable across runs and order-sensitive at once, so an emitted diagram is sorted by durable identity before layout) | canvas-graph | kp | experiment | better | Seam: the PlantUML renderer's own parse + elkjs layout over the 15 committed diagrams, harness outside the tree, product code unchanged. Old text predicts reordering is layout-neutral; amended text predicts it is not. Rerun of identical input: 0/15 diagrams, 0/198 boxes moved (control: the operation ran and is deterministic). Edge list reversed: 7/15 diagrams (of 11 with two or more edges), 93/198 boxes. Declarations reversed: 14/15, 164/198. Falsifier was rerun drift or no movement under reorder. No code owed in kp: every source is hand-authored or a fixed template, so order is authored intent; the rule binds a tree that emits diagrams from unordered data. |
| 2026-09-26 | edge-management (flipped for generated diagrams: "full obstacle-avoiding routing is a last resort" is an editor rule; where a layered engine placed the nodes, its routes are the edge geometry and are drawn as returned) | canvas-graph | kp | experiment | better | Same seam and harness. A = straight segment between the engine's two endpoint ports; B = the engine's routed polyline as the renderer draws it. Edges passing through a leaf node that is not their endpoint: A 16/189, B 0/189, all 16 in the two dense system-architecture diagrams (7/28, 9/31); smaller diagrams clean under both. Crossing detector's positive control is arm A itself. kp already implements B; the row validates the condition, no code owed. |
| 2026-09-26 | graph-layout golden path (flipped: "identical input" for an engine-driven layout includes the measured sizes; measure with the rendering face after it loads, and a fallback-metric layout is a different layout) | canvas-graph | kp | simulation | not-better | Three real cases in the tree: (1) diagram font is a system stack while the app loads a web font, so the readiness race cannot occur; (2) the no-DOM fallback (6.7 px/char) feeds the node-run layout tests, which assert only containment, absolute coordinates and the ceiling, never a coordinate; (3) the localized phase diagrams keep topology identical and let widths move the layout, which is correct. Under both texts kp needs nothing: B is a no-op here. The technique gained the "measure with a face that is always present" resolution from case 1. Falsifier: a coordinate-pinning test or a web-font diagram face. |
| 2026-09-26 | render-budget rung 1 / golden path rule 1 (flipped: nodes never see the pan but may read a stepped zoom - a band, a value settled at rest, or at worst one per frame) | canvas-graph | personas | simulation | better | Three real cases on the Mastermind canvas at HEAD. Pan: camera never reaches islands - both texts pass. Zoom bands gate structural layers - old text ("nodes must not know the transform exists") reads a violation, amended passes. Counter-scaled banners get a zoom quantized to ~6% log steps, which the tree's own comment accounts at ~12 island renders per zoom doubling instead of one per frame - old reads a violation, amended passes. The amended text matches shipped behaviour the old one condemned. Falsifier: a node reading the raw continuous zoom. |
| 2026-09-26 | viewport-transform / golden path gesture regime (flipped: the per-frame commit protects subscription isolation and does not replace it; a store taking every event holds the budget when no node reads the viewport) | canvas-graph | - | - | unapplied | No fleet camera commits per event: the one live pan/zoom camera (personas Mastermind) already coalesces wheel zoom per frame and commits pan once at release, and no fleet tree renders through a store-per-event node-editor library: of the ten JS manifests only personas declares one (@xyflow/react 12), and it is imported nowhere in its source - only a vendor-chunk entry in the bundler config names it. An A/B would be a tie that ran nothing. Return: when a project adopts a per-event viewport store. |
| 2026-09-26 | render-budget / golden path rule 1 (conditioned: the ladder is retained-mode; on a surface that repaints every frame a pan invalidates no per-node derived work) | canvas-graph | - | - | unapplied | No immediate-mode graph surface in the fleet: the 2D-canvas uses found (particle hosts, posters, previews, an image editor) draw no node graph, and the Rust trees draw none. Return: when a project renders a node graph into a 2D or GPU canvas. |
| 2026-09-26 | deployment-history + transition-detection-and-notify (terminal is final per attempt, not per id; cache by (run, attempt); dedup on (entity, attempt, class)) | cicd-monitoring | personas | simulation | better | **/deepen first pass on the subject.** Seam: the GitLab pipeline notifier and viewer gate (`usePipelineNotifications.ts:50-55`, `GitLabPipelineViewer.tsx:51`). The backend commands they call are still unregistered, so the tree cannot run, and the model uses the real classifier sets and gate. A = the tree / the old rule (terminal stops every poll), B = terminal leaves detail polling but stays in an idle 60 s collection poll, with the attempt in the dedup key. Three documented sequences over 300 min: retry reopens the same id (A sees running>failed and stops at 10 min; B sees the reopen and success@a2), re-run fails again (A misses it; B announces failed@a2, which an (entity, class) key would drop, 1 drop), and a control (manual gate, row below). Cost: B polls 521 vs 121 per entity. One collection call serves every row, so the per-row cost is lower than modelled. Falsifier: a provider whose finished ids never reopen. Return for a `code` row: when the pipeline commands are registered |
| 2026-09-26 | liveness-scoped-polling (state liveness is three-way: progressing / parked / terminal-for-this-attempt; parked polls at idle cadence) | cicd-monitoring | personas | simulation | better | Same model and seam. Manual-gate sequence (running, manual at 5 min, played at 245 min, success at 255). The tree stops at `manual` (its gate is running or pending only), after 62 polls, and never sees the ending. The old rule treats manual as non-terminal and polls it every 5 s: 3061 polls to see the ending. The new rule polls it at idle cadence: 465 polls, sees the ending, and emits a needs-action on entry. The tree's notifier would also miss the ending if polled, because `manual` is in neither its active nor its terminal set. Mid-states printed per arm, so no tie hides an arm that did nothing |
| 2026-09-26 | remote-action-consent (a fallback that performs a different act is a new action on its own rung: fail the confirmed act, offer the fallback with its own consent) | cicd-monitoring | personas | simulation | better | Seam: the Rust deploy and rollback (`gitlab.rs:302-344`, `:1291-1325`), both matching `Err(_)` into an `AGENTS.md` commit on the default branch. Three real error classes, read against the code: an expired token (401), a provider 5xx, and an instance without the Duo API. Current code: all 3 commit to the default branch after a consent given for "register an agent". Under the rule: 2 of 3 surface as the error they are, and the third offers the commit as a separately confirmed act. Code not changed: the Personas checkout had 85 modified files from another session. Return: a quiet Personas tree, as a `code` row |
| 2026-09-26 | cicd-monitoring golden path (the price of a poll is a provider fact: an authorized 304 is free on one provider, full price where no conditional support is documented) | cicd-monitoring | - | - | unapplied | The one joined tree polls GitLab only, and GitLab's public REST documents no conditional requests, so the condition changes no decision there. Return: when a fleet project polls a provider that answers `If-None-Match` on a timer |
| 2026-09-26 | cicd-monitoring golden path (push exists on both providers; observers rarely reach it, and it is a hint reconciled by polling) | cicd-monitoring | - | - | unapplied | The joined tree is a desktop app with no public receiver and no admin rights on watched repositories. The rule it already follows (polling first) is unchanged, so no decision moves. Return: when a fleet project registers pipeline or job webhooks |
| 2026-09-26 | cross-surface-handoff (condition added, golden path flipped with it: the host is a surface - its configuration decides whether the page sees a drop, in some hosts for in-page drags too; synthetic-event tests cannot witness it) | drag-drop | personas | simulation | better | Converged: the pinned host source read directly (the drag-drop switch defaults on; on Windows it revokes the browser's drop target and registers a files-only one; on macOS its handler never returns a drag to the web view; Linux passes non-file drags through), a counter-evidence lane (same reading from the upstream dev branch, an open upstream issue, an unmerged macOS fix) and a blind lane (same rule, same failure, same test blindness). Three real surfaces walked under both rules. (1) An OS-file ingest zone reading the DOM's file list: the old rule sees a filtering target and no defect; the new rule predicts it dead on desktop, and the project's own filesystem audit had independently found it dead. (2) The shared kanban board's in-page drag with a private MIME: the old rule scores it conformant (it inspects the type before accepting); the new rule predicts no drop on Windows and macOS. (3) The fleet queue's pointer-event reorder: conformant under both, and the new rule names it as the shape that survives either branch. Differs on 2 of 3, both in the new rule's favor. Falsifier: a real OS drag on the Windows desktop build where a board card's drop commits. Not run: no session could drive an OS drag unattended, and the tree's 12 drag tests are synthetic |
| 2026-09-26 | drag-drop golden path (condition added: the non-drag equivalent must be operable by a single pointer as well as a keyboard, SC 2.5.7) | drag-drop | personas | simulation | not-better | Three real surfaces: the fleet runway (arrow controls are clickable buttons), the drive finder (a Move-to menu plus cut and paste) and the goal board (drag plus a full keyboard grab-move-drop, and a status field in the goal editor reached from the drawer by click). All three pass under both the old and the new rule. The new wording as first landed would have misread the goal board as a deviation because its click path does not live on the board. Condition gained, and landed in the same run: judge the operation, not the surface; an editor that sets the same field satisfies the criterion |
| 2026-09-26 | keyboard-alternatives (conditions added: a drag library's semantics and its keyboard input are separate registrations; a drop-resolution strategy that reads pointer coordinates finds nothing on a keyboard move) | drag-drop | goat | simulation | better | Seam: four drag contexts register pointer and touch sensors only; six item sites spread the library's button role, tab stop and keyboard instructions; one surface writes its own "press Space" instructions and resolves drops by pointer containment. Three cases walked. (1) That match grid: the old rule's fix is "add the handler", and the library source shows the added keyboard sensor would still find no target (pointer containment returns nothing without pointer coordinates, and a keyboard activation has none); the new rule predicts it. (2) The studio list and (3) the collections list resolve by nearest center: both rules prescribe the sensor, and both predict it works. Differs on 1 of 3, in the new rule's favor. Falsifier: a keyboard sensor on the match grid moving an item into a slot with the collision strategy unchanged |
| 2026-09-26 | keyboard-alternatives (absolute refuted: the grab and drop-effect attributes are deprecated since ARIA 1.1 with no replacement; carry grab state as the grab control's pressed state) | drag-drop | goat, personas | - | unmeasurable | No fleet source uses either deprecated attribute: six trees searched, 0 hits outside a binary database, positive control (the pressed state) hits. goat's drag library already carries grab state as a pressed button, which the corrected rule names. So the correction prevents a defect rather than finding one. Instrument: a screen-reader pass over a held item, reader and browser pair recorded; none was run. Return: when a tree adds grab state by hand |
| 2026-09-26 | persist-before-provision (widened: when the issuer is also the verifier, the handout is the effect; a failed or expired handout revokes the row, a claim window never outlives its token's row, a check-then-mint holds its guard across the insert) | dynamic-secret-lifecycle | personas | simulation | better | **/deepen first pass on the subject.** Seams: the Rust key mint paths at `900b8f0b4`. Three real cases and two controls, read against the code. (1) An approved pairing unclaimed at 300 s (`pairing.rs:139-142`) drops its token and keeps its key: an orphan live key as written, revoked on prune under the rule. (2) An approval after the window: `pending_origin` does not check age, so a key is minted whose claim must fail. As written it is an orphan; under the rule it is refused before the insert. (3) The system key's check-then-mint (`management_api.rs:835-876`): an interleaving model of two first calls, all 70 orderings. As written, 60 leave an orphan never-expiring `proxy` key and 24 cache a revoked token (401s until restart); with the mutex held across the insert, 0 and 0. Controls: the operator key already revokes on a failed file write (`operator_key.rs:199-203`), and the broker handle's handout is its own response. The counts are orderings, not probabilities. Code not changed: the fix spans the app and engine crates and owes a cargo test. Return for a `code` row: a Personas session with a warm target dir |
| 2026-09-26 | ttl-ladder-derivation / golden path (flipped: a renewal that would cross the maximum is trimmed to the remaining window with a window warning, and refused only when nothing remains; refusal is a stated stricter stance) | dynamic-secret-lifecycle | - | - | unapplied | No fleet issuer renews what it mints. Personas' broker handles take a clamped lifetime at mint (5-1440 min) and have no renew path; pairing and operator keys have none either. No decision moves. Return: when a fleet project renews issued credentials against a maximum |
| 2026-09-26 | persist-before-provision (conditioned: create-then-register with revoke-on-failure meets the rule except for the crash window; the written-first record is mandatory only where the artifact never expires on its own; the compensation is armed before the first failing step) | dynamic-secret-lifecycle | - | - | unapplied | No fleet tree creates credentials in a remote system. The one joined issuer (Personas) is its own verifier, which is the widening row above. Return: when a fleet project provisions users, keys or bindings in a system it does not own |
| 2026-09-26 | lease-vs-native-tracking (flipped: store-nothing costs enumeration, not revocability, because a presented artifact is revoked after a signature check; a no-op lease may stand as a client lifetime contract) | dynamic-secret-lifecycle | - | - | unapplied | No fleet project runs a certificate authority or issues self-expiring artifacts to outside relying parties. Return: when one does |
| 2026-09-26 | expiry-buffer-beyond-lease + idempotent-revoke-and-give-up (conditioned: a backstop may bind one auth mechanism and new logins only, and a silently ignored expiry update counts as none; absent means absent in the system the credential was created in; signing-key retention binds only where leaves may outlive the issuer or the key was recertified) | dynamic-secret-lifecycle | - | - | unapplied | There is no remote-side expiry or remote revoke anywhere in the fleet's issuers. Personas' revoke is a same-table update. Its non-idempotent re-stamp of `revoked_at` is recorded on the application, not as this row. Return: when a fleet project sets remote expiry or revokes remotely |
