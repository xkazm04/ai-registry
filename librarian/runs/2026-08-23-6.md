---
run: 2026-08-23-6
kind: external-reconcile-wave
dispatched: 6
---

# Run 2026-08-23-6 - wave N1: the lane generalizes, unchanged

The first wave under the written brief ([[2026-08-23-5]]) and the first outside
software-engineering: six llm-observability subjects, six class-A trees, one
sitting. The question the wave was dispatched to answer - does the contract
generalize across domains with no instrument change - is answered: it does. Six
applications landed at or under the 130-line ceiling, every hint resolved, every
citation spot-check passed (12/12 exact), and the only instrument change was the
one the brief predicted: `python` declared in the bundle's `stacks:`, and
`source:` on every application - the lane's first documents to carry it.

## What landed

| subject | tree | application | hint fate |
| --- | --- | --- | --- |
| trace-rollup-and-attribution | Arize-ai/phoenix @ 9478f95 (20.3.0) | python--keyset-trace-pagination | confirmed, richer |
| production-trace-scoring | langfuse/langfuse @ 3c3ca18 (4.16.0) | node--stable-hash-sampling | confirmed; worker switched technique on evidence |
| quality-regression-gating | promptfoo/promptfoo @ 679e7ec (0.122.0) | node--unverified-vs-regressed-exit-states | confirmed |
| cross-provider-benchmark-operations | EleutherAI/lm-evaluation-harness @ 4e7e0d47 (0.4.13.dev0) | python--target-matrix-runs | confirmed, rule 1 inverted |
| federated-benchmark-sharing | mlcommons/inference @ b66003e (v6.1) | python--fixed-task-vocabulary | confirmed; sibling hint deliberately not bound |
| margin-and-unit-economics | BerriAI/litellm @ f005afa (1.99.0) | python--cogs-by-construction | confirmed; two hint sub-claims refuted in the tree's favor |

All six subjects were single-stack; all six debts cleared. The bundle's
single-stack class is now empty.

## Two measured disproofs of technique text, cycle-ready

The wave's sharpest yield is not sightings but executed refutations of two
techniques' own sentences - a first for the lane:

- **keyset-trace-pagination** prescribes the latest-event-time cursor column
  and calls the consequence benign re-appearance. Executed: under the DESC
  order it also prescribes, that column SKIPS rows (a harnessed traversal lost
  trace 5); the safe form is "immutable, or mutable only in the direction that
  duplicates".
- **stable-hash-sampling** words the mechanism as "hash mod N". Executed on
  100k ids: the threshold form nests as the rate rises; mod-N does not, which
  re-draws the population - the exact failure the technique exists to prevent.

Both are wrong-as-written with the correction measured, not judged. Queued as
cycle N1-a rather than edited mid-wave, keeping the wave's diff
applications-only as every wave before it.

## Three upstream-reportable bugs

- **promptfoo**: a run that verifies nothing exits green (`NaN < 100` is
  false), and provider-error runs exit as "regressed"; the unverified
  discriminator is computed and spent on the console banner only.
- **MLPerf submission checker**: dead alias-table targets plus an
  identity-fallthrough classifier crash the whole submission walk with an
  unguarded TypeError, losing valid rows already found.
- **Langfuse**: integer-percent quantization zeroes the analytics read sample
  past ~20M rows while the UI still announces a non-zero rate.

## Convergence

- **nullable-never-zero** (bundle law): confirming sighting - LiteLLM's cost
  chain blocks at the calculator, nulls at the logging object, and coerces to
  0.0 at every consumer.
- **The lossy branch carries a counter** (software-engineering family, banked
  at three): MLPerf's silent division skip is a fourth sighting, and the first
  from another bundle. The family's cycle remains software-engineering's; the
  cross-bundle appearance is recorded here as weight.
- New first sighting, director-placed: a vocabulary's closure must be enforced
  at the place the vocabulary is consumed.

## The scan half

phoenix L4/83 (adoption 86) · langfuse L4/79 (adoption 92) · promptfoo L4/77
(rigor 89) · litellm L4/77 · mlcommons/inference L3/56 ·
lm-evaluation-harness L3/46 (adoption 18 - the excellence-without-AI pattern
beside vega-lite and tailscale). Six trees scanned, watchlist updated.

## Deviations, recorded

- A worker ran `build-index.mjs` mid-wave despite the contract (the index
  briefly said 44 applications); superseded by the close-out rebuild. Worth a
  sentence in the next worker prompt, not a rule.
- The mlcommons checkout aborted on Windows path length mid-clone; the worker
  repaired it in place and said so. 163 sample paths remain unrestorable;
  none were needed.
- Committed locally on `main` by pathspec - same shared checkout, same sibling
  mid-flight, same judgment as [[2026-08-23-3]]. Pull request owed at push
  time.

## Declined

- Editing the two disproven techniques in this sitting - cycle N1-a is queued
  and the wave's diff stays applications-only.
- Binding aggregate-only-digests to MLPerf - the tree refutes that technique's
  foundation, and a refutation-shaped application deserves its own worker.
- The consult-path work for this bundle: personas is connected but has not
  consulted; the baseline ([[2026-08-23-4]]) already records that, and the
  next sweep reads signals/, not this wave.

## Next

Cycle N1-a (two measured disproofs + the banked single-sighted edits if any
converge), then wave N2 per the brief: recruiting's four class-B subjects with
executable artifacts - the wave that proves the specification evidence
standard.
