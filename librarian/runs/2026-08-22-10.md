---
run: 2026-08-22-10
kind: technique-cycle
dispatched: 0
---

# Run 2026-08-22-10 - cycle 4: the eleventh and twelfth laws

Both law questions [[2026-08-22-9]] triggered, answered in one deliberate
pass. `_laws.md` grows from ten convergences to twelve; the header names the
provenance of the three lane-earned laws together.

## absent-guard-is-loud (eleventh)

Four sightings: a webhook gateway performing no dedup when no identity key is
configured; a protocol SDK shipping its characteristic security checks
default-off; an AI SDK's validator-less schema passing values as a cast; and
the sharpest - a policy engine that ships the strict mode fixing its own
worst degradation and does not enable it in its own authorizer. The law's
closing clause carries that fourth sighting: the vendor that ships the safe
mode without self-enabling it has signed the same waiver.

Wired into the three techniques that carry the family's prose from cycle 3:
`duplicate-and-replay-dedup`, `transport-selection`,
`schema-validation-and-repair`.

## unknown-is-not-a-value (twelfth)

Four sightings: an absent ledger read as "never migrated"; an unset sentinel
colliding with the strictest legitimate floor; a nil tri-state zero-filled
into "offline"; a lost outcome published as a synthetic exit code. The law
names the recurring laundering point - the boundary where an optional type
meets a non-optional one - and states its kinship: sibling of
failure-not-empty-success, distinct because it separates UNKNOWN from every
definite value, including zero.

Wired into the three techniques whose existing prose anchors it:
`schema-validation-and-repair` ("absent must be representable distinctly
from every value the field may legitimately hold"), `error-propagation`
("zero-steps-because-current and zero-steps-because-blind are different
facts"), `peer-state-honesty` ("the display never promotes a weak claim to
a strong one").

## Discipline

`_laws.md` re-read before writing (unchanged since cycle 3). Both unread
candidate homes read whole before wiring; `relevance-floors` and
`termination-and-reaping` were NOT wired despite hosting sightings - their
technique texts carry no anchoring prose, and a law citation without
supporting text is decoration. Their applications carry the sightings, which
is enough. Purity grep over `_laws.md`: clean. No technique prose was added
this cycle - both families' prose landed in cycles 3 and earlier, which is
what made this cycle cheap; the law pass is the harvest of placement done
right.

## Standing

The fail-closed family sits at three tree sightings (Argo, Convoy,
containerd) with technique placement from cycle 2; its fourth sighting
triggers the next law question. The lane's ratchet is now fully proven:
sightings -> banked leads -> convergence at two -> technique placement ->
convergence at four -> law. Every rung has been exercised at least twice.

## Declined

- Wiring laws into techniques without anchoring prose (above).
- Renaming failure-not-empty-success's scope to absorb the twelfth law -
  they are siblings, and merging them would blur both.
