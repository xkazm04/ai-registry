---
run: 2026-08-29-4
kind: deepen-batch
domain: software-engineering
dispatched: 7
landed: 7
rejected: 0
---

# Run 2026-08-29-4 — architecture & design-excellence batch, seven workers

Operator-directed `/deepen` round scoped to "coding architecture and design
excellence", with a second deliverable: an impact plan for the fleet on this
device (written to [[2026-08-29-architecture-round]] under `librarian/impact/`).

## Selection

`librarian-scan` fresh (578 attention points on this bundle, demand `reported`):
head of list was quality-gates/error-handling/authorization — all deepened
2026-08-28, no clock or event, skipped under the skill's own law. Chosen instead
on focus + structure + demand: module-design (operator's center of mass, 2 apps),
client-state (18p, single-stack, 4 deviations), entity-lifecycle (13p,
single-stack, never swept), data-access, ipc-contract, scale-investment-timing
(11 hedge qualifiers — the 2026-08-28 heuristic used deliberately),
optional-dependency-degradation (7 qualifiers + an ascent deviation).
delivery-guarantees and schema-driven-ui banked for the next round.

**Demand provenance check that changed the plan:** the scan's deviation counts
for client-state (4) and entity-lifecycle (2) come from `mkdol-dev-box` signals —
the OTHER machine. On this device those pairs are all `state: unknown`. A local
impact plan built on those numbers would have chased evidence it cannot read;
the plan below therefore leans on what the workers read directly.

## Yield (per-subject detail in librarian/subjects/)

| Subject | New technique | Apps | Headline |
| --- | --- | --- | --- |
| ipc-contract | — (refinements) | 3→5 | P (permitted) set; envelope rides the structural channel |
| scale-investment-timing | — | 2→3 | order-of-magnitude claim REFUTED (fat tail); open-vs-bounded arrivals |
| optional-dependency-degradation | — (declined, dry axis) | 2→4 | tunables collapse; no-boot platforms; once-cell poison |
| module-design | io-free-core | 2→5 | observable interface; gate-able subclass carve-out |
| client-state | — | 6→8 | replicated-record species; debounce-is-not-a-guard |
| data-access | read-models-and-projections | 4→9 | retryable closure; three-origin N+1 |
| entity-lifecycle | orphan-reconciliation | 2→5 | third axis; preview through the enforcement path |

995→998 techniques, 471→488 applications; commits 354e7df 22159a7 8e6bda6
9fb6de8 b7a4859 6c3ea0c 735d54a + generated 238ce15. All three new techniques on
lane convergence; ~20 counter-evidence confirmations recorded, two refutations
of the corpus's own numbers. Not one of seven subjects came back dry — same
result as the 2026-08-28 loop, on a bundle whose maturity keeps forecasting
otherwise.

## Cross-subject proposals (Director-adjudicated)

- **realtime-events**: snapshot-then-subscribe contract (monotonic version on
  snapshot + events, apply-if-newer, refetch on gap) — answers the personas-web
  mirror-surface question from waves past. ACCEPTED AS LEAD for the next round
  that opens realtime-events; evidence pinned in the ipc-contract note.
- **error-handling**: mirrored-classifier parity fixtures (byte-identical fixture
  list on both sides of a boundary); blanket error-conversion impls as where
  verdicts die. Banked to its subject's next touch (deepened 2026-08-28).
- **quality-gates**: architecture fitness rules as a gate class (receives
  module-design's structural-spec hand-off). Banked likewise.
- **data-retention**: API-level deletion ≠ storage-level erasure (ghost vectors,
  restored backups). Strong; queue for that subject's first sweep.
- **embedded-db**: pof's second connection with its own WAL pragma — a
  single-writer-holder sighting.
- **test-harness**: pof's fingerprint-the-real-DB tripwire.
- **metric-forecasting / admission-queue / rate-limiting / codegen /
  dead-code / job-coordination**: one-paragraph leads each, recorded in the
  subject notes where they arose.

## Instrument notes

- A worker's mid-flight gate run correctly failed on ANOTHER worker's
  half-wired technique (declared file, not-yet-updated golden path) — the batch
  rule "only failures inside your folder are yours" held; both resolved green.
- `git commit --only <subject-path>` per subject kept seven landings clean in a
  tree with no other sessions today; catalog regenerated in the same quiet tree,
  closing the 2026-08-28 debt.

## Addendum (post-run, worker follow-up)

The client-state worker's background trace refined the provenance picture: the
four recorded client-state deviations trace to the personas gravity-wave
verdict row on THIS device ([[2026-08-29-personas-gravity-wave-1]]:102 — 3
conformant / 4 deviation / 2 n-a / 1 unknown), consistent with the 2026-08-27
personas consult naming async-race-guards. So the demand was partly local after
all — recorded in a backtest file, not in the registry-map, which is why the
map read said "unjudged". The provenance rule sharpens rather than weakens:
deviation counts flow from MORE places than the map (signals windows, backtest
verdict rows), and the map alone under-reports local judgment. Wave 2 of the
impact plan should fold backtest verdicts into the map where they overlap.
