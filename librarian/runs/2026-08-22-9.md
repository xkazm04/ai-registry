---
run: 2026-08-22-9
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-22-9 - the fifth wave: two law questions in one pass

Four workers, four trees from the watchlist and the strongest remaining
matches. Twenty-one single-stack debts cleared across the lane's six runs.

## What landed

| subject | tree | application | hint fate |
| --- | --- | --- | --- |
| p2p-networking | tailscale/tailscale @ de9ec7e (1.103.0) | go--peer-state-honesty | one hint refuted BY ABSENCE OF THE OBJECT |
| pipeline-dag | argoproj/argo-workflows @ f016ad8 (v4.1.2) | go--conditional-edges | join-mode hint refuted: the author writes the boolean |
| authorization | open-policy-agent/opa @ 551581f (1.20.0-dev) | go--failure-direction | confirmed; deviation found one level below it |
| subprocess-lifecycle | containerd/containerd @ 301026e (2.4.0-beta) | go--termination-and-reaping | confirmed; liveness hint partly refuted |

Re-check catches: 4, 4, 5, 10 - the rate holds, and one worker verified its
negative claim (no evaluator import in the validator) by grep rather than
assumption, which the lane should treat as the standard for negative claims.

## Two law questions triggered

**Opt-in-guard reached four sightings**: the fourth is the sharpest - a policy
engine that ships a strict mode fixing its own worst degradation and does not
enable it in its own authorizer. Candidate law name: absent-guard-is-loud.

**Unknown-is-not-a-value reached four sightings in one close-out**: a migration
runner reading an absent ledger as a fresh store; a query engine whose zero
doubles as the unset floor; a mesh daemon zero-filling a nil tri-state into
the hard claim "offline"; a container runtime publishing lost as a synthetic
exit status indistinguishable from an observed one. Candidate law name:
unknown-is-not-a-value. Adjacent to failure-not-empty-success but distinct:
that law separates failure from empty success; this one separates UNKNOWN from
every definite value.

Both go to cycle 4 as law passes, per the rule that promoted
verdict-survives-boundary. Neither is written in a wave close-out.

Also noted: containerd's fail-toward-killing on unreadable spec is the
fail-closed family's third tree - that family is already placed in techniques;
its own law question arrives at four.

## The scan half

argo-workflows L4/71 (adoption 65 - genuinely AI-native) · containerd L3/64 ·
opa L3/62 · consul L3/62 with RIGOR 83, the highest measured yet, at adoption
20 - the excellence-without-AI pattern's strongest example · tailscale L3/49 ·
golang/sync L1/24 at confidence 0.95 - the highest-confidence lowest score in
the dataset, and a mirror-class lesson: the golang org's process lives in an
external review system, so the register's no-PR-signal caveat applies.
Eighteen repos scanned.

## Coordination

The harvest merge rewrote this bundle's domain note and its prose
misattributed the lane's run ids; corrected in place with a dated marker
rather than regenerated. The vault's run files themselves were never in
conflict - prose links were. Lane rule already covers it: a subject note is
appended to; a factual error in a shared note is corrected with a visible
patch, never silently rewritten.

## Declined

- Writing either law in this close-out.
- A liveness-and-heartbeats application from containerd - the honest finding
  is that no periodic pulse exists there; three-quarters would have been
  not-present-by-scope.
