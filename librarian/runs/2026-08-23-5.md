---
run: 2026-08-23-5
kind: lane-design
dispatched: 0
---

# Run 2026-08-23-5 - the lane-design sitting: reconcile past repositories

The sitting [[2026-08-23-3]] recorded as owed and [[2026-08-23-4]] gave its one
input to. Output: `docs/reconcile-brief.md`, the external-reconcile lane's first
written contract - eight runs had executed it from prompts - with the extension to the
six bundles whose counterpart is not always a tree.

## What the design rests on (measured this sitting)

48 single-stack subjects outside software-engineering: recruiting 14, grant-funding
12, civic-intelligence 8, llm-observability 6, media-generation 6, game-production 2.
Their `process` applications cite Python pipelines and prompt pairs - the application
layer needs no change; only the counterpart class does. Four consumers are connected
(kp, pof, personas, systedo-case); civic-intelligence and grant-funding have none.
`use_when` in these bundles is already phrased as decision moments - routing needs no
rewrite for non-code decisions.

## The decisions

- **Four counterpart classes**: A repository (contract unchanged), B specification
  with a conformance artifact, C public record with a version and a query surface,
  D craft canon (the lane does not run; `/deepen` or an accepted-debt note).
- **Evidence per class** - pin, citation, executed evidence - with purity unchanged:
  no agency, standards-body or document name above the application layer; standards
  paraphrased by section, never quoted at length.
- **Three instrument changes, none retroactive**: declarable stacks `spec` and
  `data`; `source:` on every application the lane writes from now on, so the scan
  can retire single-stack for single-source; `accepted:` in a subject note so a
  class-D subject stops scoring two points forever.
- **A fourth hint fate**: not conformance-testable.
- **The consult path is a precondition.** The lane does not run on a bundle whose
  consult path is unknown; the two consumer-less bundles go last and only on the
  operator's explicit acceptance of unmeasured value.
- **Order**: llm-observability (class A, six subjects, one wave - buys the cheapest
  fact: does the contract generalize across domains unchanged), then recruiting (the
  class-B proof on a bundle with witnessed demand; four subjects with executable
  artifacts, not fourteen), then civic as the B/C instrument pilot, then grant, with
  media and game class-A subjects riding free slots and their class-D residue
  accepted.

## Declined

- Dispatching the first wave from this sitting. A worker costs what a worker costs,
  and the operator has chosen every wave so far.
- Verifying candidate counterparts by web search. Every one is graded reputed, as
  the watchlist convention requires, and a worker measures it.
- Building the three instrument changes now. Each lands with the first artifact that
  needs it; building them against nothing is the speculative work the sweep rule
  forbids.

## Deviation, recorded

Committed locally on `main` by pathspec - the sibling session is still mid-flight on
this checkout (three commits in the last hour). Pull request owed at push time.
