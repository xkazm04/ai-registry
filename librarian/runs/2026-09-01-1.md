---
run: 2026-09-01-1
kind: sweep + maturity-flip + inbox-drain + reconcile-wave
domain: all (dispatch in software-engineering, media-generation, llm-observability, game-production)
dispatched: 24
landed: 24
rejected: 0
---

# Run 2026-09-01-1 - the worklist head was saturated, so the run did the three passes nobody owned

The sweep found the corpus structurally flat (0 cap breaches, 0 taxonomy errors, 0 missing
`use_when`, 0 expired, 0 at-risk, 2 application-less subjects) and its demand head fully
touched within the previous four days. Ranking on the corrected demand floor and applying
the saturation law left nothing to dispatch at the head. Three systemic debts had been owed
for days and were cheaper than any single subject: the unused maturity ladder, an inbox of
consumer leads nobody was reading, and two application-less subjects plus one subject whose
reconcile return condition had arrived.

## Instrument

Gates green in order; spot-check `agent-memory` 13/9 matched disk. One stale vault line
found ([[localization]] said all 14 single-stack; the scan reads 13 of 14 on two stacks)
and corrected by append. Running `leads-collect` with a help flag collected 41 leads
instead of printing help - reverted, then run for real on purpose. A live sibling intake
run was on this checkout on `main`; the branch I created was deleted and the work done on
`main` by pathspec, with the pull request built from a worktree.

## Pass 1 - the anydoc bug: already fixed

The one lead whose return condition read "immediately" (an OCR parse ending in
`unwrap_or_default`) had been fixed in the applied project the day the note was written,
with eight tests, on both provider paths. Closed in [[2026-08-31-anydoc]] by append.

## Pass 2 - maturity ladder: 36 flipped, 18 held

Verified against disk by a reader, not by the run notes' word: 29 software-engineering
subjects, 6 llm-observability, 1 localization meet [[standard]]'s `reconciled` definition
(external tree, commit + version pin, worker not the forger, citations re-checked, fates in
the vault note). Four of the 29 lacked the fate line in their vault note; the fate was
copied from their applications' own close and their run notes, then they flipped.
**Held: 18 spec/data-class subjects** (13 localization, 4 recruiting, 1 more) whose pin is
a release + artifact digest, not a commit. They satisfy [[../docs/reconcile-brief]]'s
class-B/C evidence standard and not the standard's literal "and commit". That is a
definitional call for a human: amend the definition to "commit, or the class-B/C pin the
brief specifies" and the next mechanical pass flips them. Not done here.

## Pass 3 - the inbox: 85 leads, 85 rulings

44 rows sat untriaged since 2026-08-29 and 41 more sat uncollected in five project
ledgers. Five read-only triage passes graded every lead against the corpus (COVERED /
EXTENDS / NOVEL / HOMELESS, with a corroboration class). Result: 17 covered, 58 extends,
10 novel, 0 homeless; 7 filed under the wrong bundle or a technique slug, re-homed.
Then scoped writers, one per subject folder, cap 10 concurrent topped up on completion,
each required to corroborate independently (a second source named in every report) before
amending, and to write applications only against trees on this machine at HEAD with every
citation re-opened.

| subject | landed |
| --- | --- |
| client-fetch-cache | in-flight-dedup + swr-design + cache-key-discipline amended; `next--in-flight-dedup` |
| client-state | NEW `effect-identity-and-latched-callbacks`; 3 amendments; `react--` application |
| quality-gates | NEW `instrument-answers-only-its-own-question`; 4 amendments; 2 applications |
| authorization | NEW `read-write-predicate-symmetry`; `node--` application (kp) |
| browser-credential-boundary | NEW `outbound-fetch-destination-validation`; `node--` application with four deviations |
| concurrency-guards | NEW `atomic-file-publish`; `rust--` application (a dozen un-retried publish sites) |
| import-normalization | NEW `overlay-merge-absence-semantics`; intermediate-representation amended; 2 applications |
| accessibility | NEW `hidden-but-mounted-inertness`; `react--` application |
| status-vocabulary | vocabulary-chain-integrity amended (two trees, one inverse mechanism); 2 applications |
| i18n | 4 techniques amended; `react--token-label-separation` |
| ui-controls | 2 amended; `next--micro-interaction-contracts`; shim rule landed technique-only (no tree instance) |
| data-viz | encoding-vocabulary amended; `react--encoding-vocabulary` |
| sql-console | 2 amended narrowly; 2 applications citing state, one negative |
| repo-manifest-standard | 2 amended; 1 application extended (stale cites re-resolved), 1 new |
| cinematic-language (mg) | 3 amended; `process--camera-position-semantics` from dojo records |
| production-pipeline-phasing (mg) | long-run-as-background-job amended; `node--` application |
| trace-rollup-and-attribution (llm) | single-shape-rule closed its own loophole; `react--` application |

Totals: 7 new techniques, ~35 amendments across 17 subjects, 20 inbox applications.
Every writer's upper-layer diff was purity-grepped and read; every application had at
least one citation re-opened by the director against the tree. Zero rejections; one
writer (ui-controls) declined to fabricate an application when the pattern was not
reachable from HEAD, which is the right refusal.

**Banked, with amendment text in the inbox row (35):** 30 EXTENDS/NOVEL leads that did
not fit the review ceiling, plus 5 whose subject was claimed by the live sibling intake
run (error-handling x2, test-harness x2, agent-memory). One proposed new subject
(`agent-role-charters`) is a lead, to be extracted from operated evidence.

**Recurring proposal worth a systematic pass:** eight writers independently proposed a
cross-link or twin sentence in a neighbour they could not touch (client-state <->
client-fetch-cache tokens; i18n <-> status-vocabulary forward/inverse rules; data-viz <->
glyph thresholds; quality-gates <-> test-harness; measurement-honesty <-> single-shape).
That is one pass, not eight dispatches.

## Pass 4 - reconcile wave (3)

| subject | counterpart | fate |
| --- | --- | --- |
| accessibility | AccessKit/accesskit @ 00b517c, 0.25.0 (rust, class A) | confirmed; name precedence runs backwards from the browser's; `described_by` resolved by nobody; single-stack CLOSED |
| judgeable-spec-authoring | cucumber/cucumber-js @ c887bc5, 13.2.1 (node, class A) | confirmed; verdicts classify why the example failed to bind; strict is one bit over one verdict |
| trailer-structure | gravity @ 51c0eea (connected tree; class D has no counterpart) | confirmed; expected negative on ledger/budget refuted; cue parent in the plan, not the picture |

All three at 130 lines; all citations spot-checked against clone or tree. Watchlist rows
added as `measured`. Technique-edit candidates banked at one sighting in each subject note.

## Declined, so it is not re-proposed

- Re-dispatching agent-memory, quality-gates, accessibility, markdown-vault, operator-
  surfaces, client-state, diff-comparison on demand alone - saturation law.
- Flipping the 18 spec/data-class subjects - definitional, human's call.
- Editing `standard.md` from this run - the amendment is proposed in the pull request.
- A class-A/B/C counterpart for `trailer-structure` - class D; the consumer tree is the
  honest first application.

## Owed

- The `reconciled` definition amendment for class-B/C pins, then 18 flips.
- Writers for the 35 banked leads; a neighbour cross-link pass (above).
- `unknown-is-not-a-value` into `vocabulary-chain-integrity` laws.
- A personas change: publish-path rename retries (`atomic-file-publish` application).
- Second sightings for the reconcile lane's technique-edit candidates (accessibility name
  precedence; cue parenthood reaching the shot layer; the one/none/two binding step).
- `assistive-tech-divergence` and `live-region-architecture` applications on the AccessKit
  pin - source-measured divergence the corpus lacks.
- Demand witness for civic-intelligence, grant-funding, localization - still unknown.
