---
run: 2026-09-23-2
kind: deepen-batch
domain: software-engineering
dispatched: 8
landed: 4
rejected: 0
---

# Run 2026-09-23-2 — four subjects away from the companion area, one technique, thirteen apply rows (six moved a rule)

Second `/deepen` batch of the day, Director-reviewed: four research workers, then four
apply workers after the diff review. The operator steered away from the companion
conversation area, which another session holds; every brief said so.

## Selection

Ranked from the regenerated registry map's deviation verdicts (the scan's signals
remained stale - see [[2026-09-23-1]]), companion subjects excluded. Most remaining
verdicts are stale - judged before their subject moved - so the pick favoured subjects
never swept (templates-scaffolding, connector-catalog), the largest block
(credential-vault), and the only fresh verdicts left (app-shell in kp).

## Landed

- [[templates-scaffolding]] - **new technique `instance-upgrade`**, ten corrections.
- [[connector-catalog]] - offer versus resolve reads, the lifecycle's front end, no-probe
  as a third outcome, checked minting.
- [[credential-vault]] - verdict and attempt as two facts, the verdict partition, the
  compatibility boolean, one sensitivity authority.
- [[app-shell]] - where the location lives as a second fork, Back ownership, bounded
  keep-alive.

Director review: purity grep clean on every upper layer; the new technique read in full;
corrections read against each file's voice. One cross-subject convergence arrived without
coordination - the no-probe row (connector-catalog) and the fourth narrowing
(credential-vault) describe the same compatibility boolean from two sides.

## Demand read

Of 22 deviations across the four subjects, **13 were already fixed** in the projects
(most of them on 2026-09-17) and one was superseded by a deliberate project decision the
subject now matches. The stale verdicts were stale in both senses: the subject moved and
so did the code. `/conform --stale` on personas is overdue.

## Apply

Thirteen rows in [[applied]]: 9 `better`, 4 `not-better`. Six moved a rule - the four
not-betters plus two better rows that corrected the technique (a lifecycle bullet backwards
for the suspend-effects primitive; a resolve-risk refinement). The research pass's
predictions were refuted twice by its own apply (a minting gap that a transactional door
check had already closed; an application claim about credential lookups). The new
technique was caught at its offer: a seeder that never bumps a version.

## Propagate

Index, rules, catalog regenerated; registry map regenerated once, after the apply
conditions, and committed in ten projects by pathspec; seam records committed in personas
(10), kp (2) and ascent (1). None pushed. Impact per subject is in each subject note.
