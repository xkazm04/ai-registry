---
run: 2026-09-23-3
kind: deepen-batch
domain: software-engineering
dispatched: 5
landed: 3
rejected: 0
---

# Run 2026-09-23-3 — two companion subjects on fresh demand, four clocks, eight apply rows (two moved a rule, one found a live defect)

A `/deepen` batch, Director-reviewed: three research workers (companion-runtime,
companion-identity, a currency worker for four applications on their own `refresh_by`
clocks), then two apply workers after the diff review.

## Instrument check first

`librarian-scan` ranked on `signals/`, whose two contributor files were dated
2026-08-29 and 2026-08-30. Its top entries (agent-memory, quality-gates) had been worked
repeatedly since, and one ranked deviation count was known to be an `unknown` count
([[2026-09-05-1]]). The regenerated registry map was the fresher demand: 142 live
deviations, 80 subjects. The two companion subjects held the largest block of **fresh**
(non-stale) verdicts in the fleet and neither had a subject note - the scan showed them
at 3 points. Refreshing signals from both machines is owed before the next sweep.

## Landed

- **companion-runtime** - eight corrections, react application, rust applications
  re-witnessed (`rust@1.96`). See [[companion-runtime]].
- **companion-identity** - four corrections, one new rust application. See
  [[companion-identity]].
- **Currency** - four upstream-agent applications re-resolved at HEAD; one substantive
  upstream move (multi-tenant mode now default-on); one of our own claims corrected;
  `refresh_by` overrides kept and moved to 2026-11-05. `check-currency`: 0 at risk.
  Notes on [[embedded-db]], [[tenant-scoped-agent-runtime]], [[prompt-assembly]],
  [[agent-runtime-assembly]].

No new technique: every candidate had a home. Director diff review: purity grep clean
over every upper layer; corrections read against each file's voice; one golden-path
sentence long but accepted.

## Propagate

Rules, index, catalog regenerated; registry map regenerated fleet-wide and committed in
ten projects by pathspec (none pushed). Impact: personas carries 10 stale verdicts on
companion-runtime and 6 on companion-identity. Eight rows in [[applied]]: 6 `better`,
2 `not-better`, both of which wrote a condition into a technique.

## The finding that is not a registry matter

Both apply workers, independently, reached the same defect in personas: the companion's
brain root is a process-global environment variable written by three lock domains, one
lock-free, and it falls back to the operator's real home when cleared. On the operator's
install the self-model has been a test fixture since 2026-09-20, with 26 unreviewed
writes in 4 days and none through any product door. Surfaced to the operator directly;
the registry holds only the mechanism.

## Hygiene notes

- One project's local git config carries a test identity; the map commit there was made
  with an explicit override.
- Apply workers copied the operator's app database into the scratchpad to read it; the
  copies were deleted by the Director at the end of the run. The brief should say: read
  private stores in place, read-only, or delete copies before returning.
