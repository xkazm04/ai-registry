---
run: 2026-08-23-9
kind: technique-cycle
dispatched: 0
---

# Run 2026-08-23-9 - cycle N2-a: the family lands twice, the loophole closes

The cycle queued by [[2026-08-23-8]]. Two actions, both scoped exactly as the
wave's close-out defined them; every other candidate stays banked.

## The family, placed at two sightings

**Search defaults that manufacture candidates** - two sightings in one wave (a
register's name-search default widening given names into a page of
confident-looking strangers; a taxonomy's ranked search returning the nearest
description as rank one). Placed as the rule each consuming technique needed,
phrased for its own context:

- `required-but-missing-as-a-blocking-gate` gains a section, "The verification
  query is part of the gate": a retrieval hit is not a resolution; a lookup
  result is evidence about the candidate only on an exact identifier, an
  explicitly pinned-off widening, or a human-confirmed row - and both error
  directions land on a real person. Plus a decision rule: a lookup matched on
  anything other than an exact identifier is a lead, not evidence.
- `canonical-term-with-surface-aliases` gains a decision rule: a published
  external vocabulary's search endpoint is a retrieval surface, not a
  resolver - rank one as the resolution invents an equivalence no human
  asserted; a result becomes a resolution only when the matched string is a
  declared surface of the returned concept, a check the consumer performs.

## The priority edit: manufactured absence

`minimum-cohort-and-inconclusive-verdicts` carried the loophole its own
counterpart regulation closes: "not evaluable is never a violation" is
exploitable by whoever owns the harness - stop collecting and no run can ever
fail. The precedence paragraph now carries the boundary (the protected reading
belongs only to absence the operator did not control; operator-caused absence
is a compliance finding with a named owner), and the decision rules gain the
matching rule. Stated abstractly - "regulators that govern selection
procedures close the same loophole the same way" - with the regulation, its
section and the worked audit table remaining in the application, per the
standard.

## Declined

- Everything single-sighted stays banked: the per-register answer map, the
  expiry-field-absent case, cache-validity priced from revocation, the
  version pin and zero-or-more-parents entry shape, the unrecognised-member
  and migration-direction rules for the envelope, the synthetic-cohort
  observation (n=1), and both law candidates (the vocabulary-closure phrase
  and "an imported identity is evidence; an imported surface is a
  hypothesis"). Convergence rule applies to all.
- No law action: absence-of-evidence-is-not-evidence gained two witnesses in
  [[2026-08-23-8]] but is already a law in this bundle - witnesses recorded,
  nothing to promote.

## Deviation, recorded

Committed locally on `main` by pathspec; sibling mid-flight; PR owed at push
time.
