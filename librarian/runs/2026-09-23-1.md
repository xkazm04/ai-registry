---
run: 2026-09-23-1
run_id: lib-0923
kind: sweep + instrument repair + deepen + lead drain + apply
domain: software-engineering
dispatched: 9
landed: 19
rejected: 0
---

# Run 2026-09-23-1 - four instruments were lying before any content moved

Operator: sync with GitHub first, then the sweep's recommended order. The sweep's own
report (bare `/librarian`, earlier the same day) had ranked three instrument defects
above any content; this run fixed them first, then found a fourth, then dispatched.

## 0. Sync

Local main had diverged from origin (30 ahead / 68 behind, merge base one day old). The
shared checkout held a paused session's uncommitted contest work plus seven "staged"
entries - each an older committed blob left in the index by ref moves of main, not
anyone's intent. Backed up, index-only reset of the seven, merged origin in a clean
worktree (10 conflicts: 4 generated, regenerated; 6 skill files, each merged skill set
above both sides: librarian 1.6.1, conform 1.8.2, contest 1.3.1, council 0.3.1,
scan-sweep 4.3.0, leonardo 1.5.1), gated 21/21 plus both version checks against both
parents, fast-forwarded main with the paused work carried across byte-for-byte, pushed.
main == origin at `7dd65466`. This run then worked on branch `librarian/2026-09-23-1`.

## 1. The instruments

| instrument | what it said | what was true | fix |
| --- | --- | --- | --- |
| `upstream-check` | 3 citation risks, 2 repos due | 0 and 0: annotated `commit:` pins sent raw to the compare API, and every 404 read as "unreachable" | parse the hash; a 404 is decay only when a direct commit lookup also 404s; self-test gains annotated and fabricated pins |
| `signals-collect` | 1806 deviations on refresh (273 before) | 120: a consult line's one figure credited to every subject it named, on every consult - a state summed as events | latest single-subject line per project is the state; unattributable lines counted in `meta` (109); checked against an independent recount on 3 subjects |
| `librarian-scan` dry streak | 0 for all 471 subjects | unknown: the only writer wrote 0 at note creation | computed from `runs/*/result.json` `idled` rows; `null` = unknown; tested with a landing-last negative control |
| purity (by omission) | clean | 92 published files carried machine home paths - two usernames and the fleet layout, one application quoting a PRIVATE project by absolute path | rewritten repo-relative; new `check-public-paths` in `gate --all`, self-tested, proven red on the old tree (247) and green after |

And a fifth, smaller: seven subject notes had no frontmatter, so the scan scored them
"never swept" (+3) - `diff-comparison` ranked #6 partly on it with a 2026-09-06 sweep
in its own note.

**The ranking the sweep reported was mostly built on these.** After the signals fix the
demand head is still set by the OTHER contributor's file (2026-08-30, pre-fix collector,
scored by max): agent-memory reads 14 there and 3 fresh; quality-gates 13 vs 8;
operator-surfaces-for-llm-spend 10 vs 4. Those subjects were **not** dispatched on that
evidence (declined below). Fresh, state-correct demand put `table` first: 11 standing
deviations across 8 projects, stale verdicts in 6.

## 2. What was dispatched

| worker | scope | result |
| --- | --- | --- |
| deepen | `table` + its 10 leads | 6 published rules corrected, 2 applications re-resolved, no new technique |
| lead drain | llm-agent runtime + companion (9 leads, 4 subjects) | 7 landed, 1 covered, 1 declined |
| lead drain | llm-agent orchestration / prompt / eval (12 leads, 10 subjects) | 8 landed, 2 covered, 2 declined |
| lead drain | engineering-process gates + stewardship (10 leads, 7 subjects) | 10 landed; machine paths removed from 7 applications |
| apply x5 | ascent, pof, goat, personas (TS), personas (Rust) | 7 findings: 5 better (all shipped, not pushed), 2 not-better |

Leads: **41 ruled - 30 landed, 6 covered elsewhere, 5 declined.** `table`: 4 AMEND,
1 AMEND+APPLICATION, 3 COVERED, 2 DECLINE (the subject commit's message says "5 AMEND";
this line is the correct split). 19 subjects landed, one atomic commit each.

## 3. The apply step earned more than the landings

Every apply worker was told to pick the seam that could falsify the corrected rule.
Five of seven came back with something the rule lacked:

- `filtering` (ascent, better): the rule's own example - a hidden total on the bar -
  left 3 of 6 hidden deliveries undisclosed, because each action takes part of the
  selection. The hidden figure now rides on every count read before firing.
- `performance` (goat, better, 150 -> 0 re-renders): the call-site fix alone gained
  nothing under an inline or selection-dependent upstream handler. Stable is a property
  of the whole chain.
- `loading-and-empty-states` (pof, better, 12/178 -> 0): one case was the chart spec
  wrong for its entity, so the state names a mismatch, not a bad payload.
- `anchored-identity-diffs` (personas, better): a heading on neither list LANDED at
  HEAD, so "apply would refuse it" was false; the refusal belongs to the declared list.
- `sorting` (personas, **not-better**): 0 of 23,188 pairs moved across 14 languages
  on real data; a desktop app has no second tier. The reader locale is a guard, not
  a fix, until the data or a second tier makes it one.
- `enumerable-domain-decoding` (personas, **not-better**): the between-candidate
  re-check removed every lost-spread set and cost ~3-4 points of honest rolls in
  retries. Keep it, state the bar in the prompt, budget the retries.
- `decision-records` (personas, better): held as written; a placement is recorded as a
  placement.

14 rows in `applied.md`: 6 better, 2 not-better, 6 unapplied or unmeasurable with
return conditions.

## 4. Attention points did not move, and that is the finding

Of 19 landed subjects, points moved on two (companion-identity 7 -> 4,
agent-browser-control 5 -> 2). `table` stayed 44, `quality-gates` 52, `agent-memory` 56.
The score on these subjects is consumer deviations, a PROJECT state that falls only when
a project re-judges under `/conform`; correcting the standard does not register at all.
A sweep that measures its own success by the scan delta will read this run as idle.
The delta that means something here is the applied ledger and the corrected rules.

## Impact

Maps rebuilt at the run start from the synced main (not from this branch) and committed
in all 12 projects on their active branches, never pushed: **223 verdicts stale**, 87
subjects. Largest: `table` in 6 projects, `fleet-orchestration` 14 in personas,
`markdown-vault` 14 in personas, `credential-vault` 9 in personas, `templates-scaffolding`
8 in personas. Personas holds 149 of the 223.

This run's landings move 19 more subject digests, and the path rewrite moves every
subject whose application carried a machine path (mostly localization and
game-production). **The post-merge map rebuild is owed** - building from an unmerged
branch would stamp digests main does not have.

## Declined, with reasons

- **agent-memory, quality-gates, accessibility, markdown-vault, operator-surfaces**
  as demand-ranked dispatches: their rank came from the stale pre-fix contributor file
  (see section 1). Re-rank after that machine re-collects.
- **agent-operations** (7 thin draft subjects, 68 points): one week old, never applied
  anywhere, demand unknown - a wiki page not yet tried once.
- **Upstream re-scans**: none due once the instrument was fixed. knip dormant (165 days),
  memory-lancedb-pro 4 commits ahead and 74 days short of its tier-3 floor, lago withheld
  by its un-executed handoff.
- **152 single-stack subjects**: a `/reconcile` wave, not this run.
- **Personas `/conform --stale`** (149 verdicts): that project's own run; the list is in
  its map.
- Leads declined inside the drains (5): table L328, L332; agent-cli-transport L123
  (homonym join, proposed to authorization); agent-instruction-files L162 (wrong
  subject; covered by templates-scaffolding); durable-agent-operations L369 (belongs to
  session-continuation; its registry-side fact was fixed in this run instead).

## Owed

- Merge the PR, then `build-registry-map` over the fleet and commit each project's map.
- The other contributor re-collects with the fixed collector; until then its file sets
  the demand head.
- 52 open leads remain (the inbox had 93 open plus 20 collected; 41 ruled).
- Proposals from workers for subjects outside their scope: selection-model page vs
  predicate; placeholder-design two-layer fallback; subprocess-lifecycle lane freed before
  the kill is confirmed; commit-verification (a swept commit must build alone);
  project-identity-and-joins park-don't-drop; session-continuation stuck-loop counter
  recomputed from outcomes; connector-catalog application.
- `build-registry-map` `revisionsBehind` goes negative after a taxonomy move.
- A subject note without frontmatter should fail loudly, not read as never swept.
- The paths removed from the published lanes remain in git history; a history rewrite is
  the owner's call. `docs/plans/fleet-backfill-2026-09-20.md` names the private project
  in a fleet table (outside the applications rule; the owner's call).
- 4 applications expire 2026-10-15.

## Project-side findings (report only, owned by each project)

goat: dead grid components and a positional row key that remounts on resort. personas:
a numerator and total under different filters over a truncated window; placement rows
still read as sent by two prompt builders; an unknown-case heading mismatch between
propose and apply; a relay token reachable by a hostile page (low impact); a census
`--update` that records the whole tree in both directions; a table that sorts every
column without a comparator as text. kp: the doc-sync predicate passes any doc under
the prefix, and one trailer dismisses a whole PR; eval resume matches rows by scenario
name only. pof: the graph branch keeps the defaulted decode; a destructive-hint default
of false. ascent: a dual-match 18 days past its stated end; failed sensors not persisted;
a settled row stays selected into the next batch.
