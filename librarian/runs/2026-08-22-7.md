---
run: 2026-08-22-7
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-22-7 - the fourth wave, finished through an outage

Four workers, four trees, and a machine-wide network outage to GitHub and the
model API that killed two workers mid-run. Seventeen single-stack debts now
cleared across five runs; 48 remain.

## What landed

| subject | tree | application | note |
| --- | --- | --- | --- |
| structured-output | vercel/ai @ ed857f5 (7.0.77) | node--schema-validation-and-repair | worker completed |
| concurrency-guards | golang/sync @ 3ffd83c (v0.22.0) | go--release-guarantees | worker completed |
| health-checks | hashicorp/consul @ 6c576af (2.1.0-dev) | go--check-scheduling | orphaned mid-trim; director adopted |
| retrieval | weaviate/weaviate @ adcffc5 (1.40.0-dev) | go--relevance-floors | orphaned un-re-checked; director adopted |

## The outage, and what it proved about the lane

Two workers died to API connection loss after their clones and reads were
done. Both drafts were on disk; neither was confirmed re-checked. The lane's
own law - an external reconcile without a citation re-check is a liability -
does not care whose hands run the check, so the director ran it: ~20 probes on
the Consul draft, ~15 on the Weaviate draft, every one landed. The orphaned
drafts were the most accurate first drafts of any wave, and were still not
exempt. Repo pins re-verified against the clones (both exact).

The length ceiling met its first judgment call: Consul was finished to exactly
130; Weaviate came down 170 -> 137 in two passes and stopped there, because
the remaining mass is four deviations and a refuted hint - evidence, not
prose. Recorded as the ceiling's operating rule: enforce to the line while
cutting prose, stop when the next cut is a finding.

## Findings of the wave

The Vercel AI SDK's repair budget of one enforced structurally (the retry
calls the non-repairing function - stronger than any counter); its schema
abstraction silently degrading to a cast when no validator is attached;
Weaviate's floor driving fetch depth rather than post-filtering, beside a
lexical lane that borrows another lane's floor units; Consul designing
probe-on-render OUT (readers structurally cannot trigger probes) while its
eight interval runners never back off; and golang/sync's identity-checked
eviction existing precisely because Forget is an unverified door.

## Convergence: a third family crosses the threshold

**An opt-in guard silently degrades to absent** - three sightings across three
waves: a webhook gateway with no idempotency key configured performs no dedup
at all; a protocol SDK ships its characteristic security checks default-off;
an AI SDK's schema without a runtime validator passes values as a cast. The
family's claim: a protection that must be mounted is a protection most
installations lack, and absence must be loud. Queued for cycle 3 beside the
standing law candidate (verdict-vocabulary, still at three sightings).

## Incidents

- The network outage itself: GitHub unreachable (DNS), model API dropping
  connections. Two of four scans completed (vercel/ai L4/80 adoption 83 -
  validating its watchlist grade; weaviate L4/66); consul and golang/sync
  scans are OWED and retried when the network returns.
- A shell-quoting failure in a two-heredoc command - moved to script files,
  which the scratch-namespacing rule already required.

## Declined

- Trimming Weaviate below 137 (above).
- A fifth technique edit for the opt-in-guard family in this close-out - it
  goes to a deliberate cycle, as every family has.
