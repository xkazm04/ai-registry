---
run: 2026-08-22-8
kind: technique-cycle
dispatched: 0
---

# Run 2026-08-22-8 - cycle 3: one family placed, one promoted to law

The two families [[2026-08-22-7]] queued, at different rungs and treated
accordingly.

## Family: an opt-in guard silently degrades to absent (three sightings)

Sighted as a webhook gateway performing no dedup when no identity key is
configured, a protocol SDK shipping its characteristic security checks
default-off, and an AI SDK whose schema without a runtime validator passes
values as a cast. Landed as technique edits in the three sighting homes:

- `structured-output/schema-validation-and-repair` - a new "silently skip"
  entry beside "silently coerce": the door validates, warns loudly, or
  rejects; what it never does is pass because it could not check.
- `mcp-tools/transport-selection` - the local-listener checks are on unless
  deliberately removed; a framework that protects its examples and not its
  installations has an opt-in guard converging on absent fleet-wide.
- `webhook-ingestion/duplicate-and-replay-dedup` - the content-digest
  fallback is the default, not a feature; running without dedup is an
  explicit logged choice, never the quiet consequence of a missing config
  row.

## Promotion: verdict-survives-boundary becomes the tenth law

The verdict-vocabulary family reached its FOURTH sighting in wave 4 - the
positive form, an AI SDK's typed give-up error carrying the candidate and
distinct parse-versus-schema verdicts - which is the trigger the cycle-2
decline set: the law pass, not a fourth technique edit.

`_laws.md` gains `verdict-survives-boundary`, written to the file's own
shape (one paragraph, anchor id, the header updated from nine to ten with
the tenth's provenance named). Wired into the `laws:` frontmatter of the
four techniques that already carry the rule in prose: admission-vocabulary,
token-refresh-lifecycle, storm-control, schema-validation-and-repair. The
sightings, for the record: a retry layer's refusal enum erased at the
caller boundary (negative); an admission system's reason-labelled rejection
counters (positive); a secrets client discriminating a server taxonomy by
substring (negative); a structured-output door's typed verdicts and typed
give-up (positive). Two positive and two negative sightings is the right
mix for a law - it has been seen kept and seen broken.

## Discipline

Read `_laws.md` whole before writing into it - first time this campaign has
touched the file. Read both unread technique homes whole. Purity grep over
every edited upper-layer file: clean. Status stays forged everywhere;
promotion of a claim into the law layer does not promote any document's
maturity.

## Standing

The opt-in-guard family is now at three sightings WITH technique placement;
per the same rule, its fourth sighting triggers its own law-pass question
(likely name: absent-guard-is-loud). The mcp-tools note's
"documented opt-in security" theme merges into this family - one family, not
two.

## Declined

- Writing the opt-in-guard law now, at three sightings - the verdict family
  waited for four, and consistency is the bank's whole value.
