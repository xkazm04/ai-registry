---
run: 2026-08-22-11
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-22-11 - the sixth wave: the lane pivots to cores

The rust backlog is nearly drained, so this wave drew second stacks from
framework-agnostic cores for react subjects - the same transplant proof by a
different route. Twenty-five single-stack debts cleared across seven runs.

## What landed

| subject | tree | application | hint fate |
| --- | --- | --- | --- |
| client-fetch-cache | TanStack/query @ 40321a0 (5.102.0) | node--cache-key-discipline | confirmed and exceeded |
| design-tokens | style-dictionary @ 29f1b25 (5.5.2) | node--cross-language-token-parity | taxonomy hint refuted by archaeology |
| versioning-snapshots | restic/restic @ a80be14 (0.19.1-dev) | go--retention-and-pruning | confirmed; two alternatives refuted on scope |
| agent-chaining | n8n-io/n8n @ fbd9449 (2.36.0) | node--cycle-and-depth-guards | locus refuted: the real guard is a TYPE |

## Method notes

Re-check catches: 6, 2, 2, 3 - and two firsts. The TanStack worker verified
its central deviation EMPIRICALLY, running the hash function to prove the
Map/Set collision rather than reading it - the lane's first executed-evidence
verification, and the standard the re-check should prefer wherever the claim
is runnable. One worker corrected a wrong assigned path by finding the real
one - the director's own path hint was stale against the harvest-moved tree,
which is the assignment-side version of canonical-name resolution.

The restic draft came in at 160; director-trimmed to 147, stopping where the
next cut was a finding - the operating rule's second exercise.

## The wave's sharpest findings

restic's own documentation records an attack the technique never anticipated:
winner-per-bucket thinning hands eviction to whoever controls the sort key -
one injected empty snapshot per period evicts every legitimate one. The
retention section of the technique has no threat model at all; that lead is
the wave's most valuable. Beside it: a token compiler whose throwing
transform silently ships the untransformed value at the default warn level; a
cache whose unrecognized key types flatten to {} and collide; and an agent
engine that makes delegation depth unrepresentable by type while its sibling
chaining path has no guard at all.

## Convergence

**one-mechanism-or-two-that-agree reached three sightings**: Prisma's two
transaction wrappers (wave 2), Argo's two expression dialects (wave 5), n8n's
two chaining paths with one hard-capped and one unbounded (this wave). The
family's claim: a second implementation of one concern shares an evaluator, a
vocabulary and a record format - or the author learns one and is wrong about
the other. Placement-ready for cycle 5.

Law-confirming sightings, no action needed: verdict-survives-boundary (n8n's
max-iterations erased to completed), unknown-is-not-a-value (TanStack's
flatten-to-{}), absent-guard-is-loud (Style Dictionary's warn default).
fail-closed remains at three trees, one from its law question.

## The scan half

n8n L5/89 at adoption 92 - THE REGISTER'S NEW #1, displacing prisma (87) -
fitting for the wave's most agent-flavored tree. tanstack/query L3/62 ·
style-dictionary L3/57 · restic L3/48 at adoption 14, the classic
excellence-without-AI profile. Twenty-two repos scanned. TanStack's canonical
owner is capitalized (`TanStack/query`) - canonicalization earning its keep
again.

## Declined

- Trimming restic below 147 (operating rule).
- Placing one-mechanism-or-two-that-agree in this close-out - cycle 5's work.
