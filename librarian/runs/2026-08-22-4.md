---
run: 2026-08-22-4
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-22-4 - the second wave

Four workers, four subjects, four trees — dispatched after the technique cycle
[[2026-08-22-3]] so this wave read the corpus the last wave improved.

## What landed

| subject | tree | application | second stack |
| --- | --- | --- | --- |
| observability-telemetry | getsentry/sentry-javascript @ bc57430 (10.67.0) | node--remote-telemetry-economics | yes |
| self-healing | argoproj/argo-rollouts @ 9f8d111 (1.2.0) | go--auto-rollback | yes |
| webhook-ingestion | frain-dev/convoy @ 2b60cc6 (v26.6.6) | go--duplicate-and-replay-dedup | yes |
| admission-queue | kubernetes/apiserver @ 80186b5 (1.38 dev, staging mirror) | go--priority-and-fairness | yes |

Nine single-stack debts now cleared across the lane's three runs; 56 remain.

## The method's numbers, second wave

Hints: two confirmed, one held, and one worker refuted two hints with evidence
(an offline transport that is a retry wrapper, not crash storage). Citation
re-check catches: 9, 10, 3, 4 — the ~1-in-10 rate holding, and the APF worker's
pass caught the method's most instructive failure yet: **its own draft had
invented an illustrative numeric**, exactly the fabrication class the pilot's
design predicted a mechanical pass must exist to catch.

A director spot-check disagreed with a worker once — the 32 MB multipart claim
looked wrong against the main ingest path — and the worker was right: the
hardcoded ceiling sits in the dedup key-extraction path, beside the properly
bounded main read. The lesson runs both directions: verify the worker, and
verify the verification against the exact path the claim names.

## Convergence found across waves

**Fail closed when the gate itself breaks** reached its second sighting: a
progressive-delivery controller treating a dying measurement pipeline as
abort-worthy, and a webhook gateway aborting the create on a dedup lock/lookup
error. Banked as the first candidate for the next technique cycle — not applied
here, because a wave's close-out is not the deliberate cycle that edit deserves.

Also noted, same family opposite outcome: the API server's reason-labelled
rejection counter is the exemplary form of the verdict vocabulary that wave 1
watched a migration runner erase at its boundary.

## The scan half

sentry-javascript L4/81 (conf 0.62) · argo-rollouts L3/58 · convoy L3/56
(conf 0.67) · kubernetes/apiserver L2/32. The apiserver row repeats the sqlite
lesson in process form: a staging mirror carries no PRs or CI, so rigor 42 for
one of the most heavily gated codebases in existence. Mirror repos need their
own label beside the outside-in one. Two rows under 0.85 confidence — the
confidence field belongs beside any published score.

## Banked

Eighteen further single-sighted leads across the four subject notes, and eight
cross-subject proposals routed to their owning subjects (payload-bounds and
sender-authentication each receive two security-relevant ones from the Convoy
read: pre-auth reads bound by the same cap; consumed bodies restored
byte-identically).

## Declined

- Applying the fail-closed lesson in this run (deliberate cycle next, as above).
- A wait-telemetry application from the APF material — the worker's honest
  finding is that telemetry is APF's weakest layer, so the subject's next pass
  should treat it as deviation evidence, not as an application's spine.
