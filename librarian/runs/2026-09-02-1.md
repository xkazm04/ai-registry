---
run: 2026-09-02-1
kind: sweep + deepen-batch + inbox-ruling
domain: software-engineering
dispatched: 7
landed: 7
rejected: 0
---

# Run 2026-09-02-1 — seven fresh targets, six inbox rows ruled, one new stack

A `/librarian` sweep followed by a `run`. The sweep found the same shape as
[[2026-08-30-1]]: structure flat (max 5 points on any subject once demand is
stripped), the worklist head 100% consumer deviations, ranked on the floor of the
range because the two-machine duplicate block is still present and contributor
identity is still unresolved. Demand is UNKNOWN, not zero, for civic-intelligence,
grant-funding and localization.

## Instrument

- Gates green in order (check-bundles → build-index → build-catalog), then the scan.
- Spot-check passed: `agent-memory` reported 13 techniques / 9 applications; the
  folder holds exactly 13 and 9.
- `leads-collect.mjs` appended 41 new consumer leads to `librarian/inbox.md`
  (87 rows total, none ruled before this run). It writes on every invocation,
  including `--help` — the run board memory already records this.
- **Trunk was red on `rules/` again.** `rules/ai-registry-game-production.md` and
  `rules/ai-registry-media-generation.md` were stale by two applications each
  before this run touched anything; regenerated here. Third occurrence of the
  same defect ([[2026-08-30-1]] found three stale artifacts). The gate exists
  and is green in CI only because nobody pushes without it; every session that
  lands knowledge from another skill skips it.
- The Edit tool writes CRLF on this box. Every worker file was normalised with
  `sed 's/\r$//'` before review so the diff showed the change and not the line
  endings — the `.gitattributes` incident, one layer up.

## Selection

Ranked head suppressed as too recent: quality-gates (swept 2026-09-01), agent-memory,
client-state, fleet-orchestration, async-ui-states, settings (all 2026-08-31). No
clock, no event. accessibility (#3, 46 points) declined a third time for the same
reason: single-stack react, a reconcile job, not a research one.

Chosen: the seven highest-ranked subjects with no recent touch, four of them never
swept, three single-stack. Six inbox rows named four of them and were handed to the
workers as leads with the reporting projects stripped.

| Subject | Before | After | Headline |
| --- | --- | --- | --- |
| import-normalization | 6t / 2a / node,rust | 6t / 2a | absent-null-empty are three words; expansion cap (CVE-2026-45304); sniffing-sink condition |
| browser-credential-boundary | 6t / 2a / sql | 6t / 3a / next,sql | **single-stack closed**; five engine-doc corrections (owner bypass, restrictive policies, count-returns-0, rules-engine refusal, invoker views version-gated); mark-the-module |
| demo-data-plane | 6t / 2a / react | 6t / 3a / go,react | **single-stack closed**; one-interface-many-planes gains its condition (provenance on the value when planes mix); address-carried plane allowed |
| terminal-multiplexing | 7t / 3a | 8t / 4a / c,react,rust | NEW `multi-client-fan-out`; K counts attachments; GPU-context count corrected |
| guided-tours | 6t / 2a / react | 6t / 4a / node,react | **single-stack closed**; two engine trees; focus-trap absolute refined under SC 2.1.2; SC 2.2.2 / 2.4.11 anchored |
| sql-console | 6t / 3a | 6t / 3a | read-only envelope is session state not a credential (stacked COMMIT); read-shaped writes; stale-success/error pair; agent lane has no consent gate |
| plan-entitlements | 6t / 3a / node,process | 6t / 5a / elixir,node,process,spec | retries-exhausted state; compare state never event order; unknown tenant at grant = retry; **new stack `elixir`** |

1093 → 1094 techniques, 582 → 589 applications on the bundle. Version witness 210 → 212.
Not one of seven came back dry — same as every batch on this bundle to date.

## Inbox rulings (six rows struck)

- import-normalization "empty collection means not mentioned" — **landed, restated**:
  RFC 7396 says a present empty array IS a wholesale replace, so the rule is "the
  format declares which spelling means untouched, and the guard tests presence,
  never content". Corroborated by two public issue threads (2015, 2020).
- sql-console "bind the pending statement to its execution target" — **already in the
  forge text**; landed as `use_when` rows so a consulting agent routes to it.
- sql-console "clear the previous result when a re-run fails" — **landed** as the
  stale-success/error pair in `result-fidelity`; two unrelated public trackers.
- demo-data-plane "invert the no-plane-identity rule" — **landed as a condition, not an
  inversion**: right for one-plane calls, over-strong for mixed-provenance results.
  The lead's ~42 breaches are the expected shape under the condition.
- browser-credential-boundary "enumerate the guard by what the client imports" —
  **landed** as "Mark the module, not the entry point".
- browser-credential-boundary "a URL inside a dependency's response is attacker
  input; re-validate every redirect hop" — **declined here, placed as a lead on
  credential-vault** (`brokered-egress` is the closest home; three other files touch
  SSRF without owning it).

## Cross-subject proposals (Director-adjudicated)

- **Shared file: `stacks:` += `elixir`** — accepted (same call as `spec` and `gemini-cli`
  on 2026-08-30). The gate rejected the application until it was declared.
- **A rules-language document store as a stack** (a second engine kind for the
  credential boundary, with a real condition on default-deny-plus-defaulted-owner) —
  declined this run: naming a stack for a hosted product family is a vocabulary
  decision, not a batch decision. Banked on the subject note.
- **Proposed law "presence before content"** (all-of over an empty collection is
  vacuously true; sibling of failure-not-empty-success) — three sightings, one
  subject. Not a law yet; recorded on the import-normalization note.
- **Proposed law "a fallback must not change a value's provenance"** — fourth and
  fifth sightings (value provenance on every object; failure provenance on the
  envelope). Worker suggests "value or failure". Still one subject. Recorded.
- **Proposed law "a control is only as strong as the fact's cheapest publication"** —
  fourth sighting, still inside security. Return condition unmet.
- Leads placed on existing notes: credential-vault (SSRF home), streaming-output
  (slowest un-paused reader pins the tail), modal-stack (third-party overlays mint
  their own z-index; popover collision belongs there), webhook-ingestion (provider
  waits on the endpoint's acknowledgement), table (stale-success/error pair on
  re-fetch).
- Not placed (no note, would read as swept): pty-management (one sentence pointing at
  M-heads-one-device), fleet-orchestration/lifecycle-signals (per-subscriber
  staleness age), llm-agent grounding (provenance of content fed to a model),
  cost-metering (two-cycle-plus-margin quota enforcement), a feature-flags subject
  (forge candidate — the bundle has none and plan-entitlements now has a seam with
  nothing to link to).

## Declined

- accessibility — third decline, same reason, see above.
- guided-tours "offer policy / targeting" technique — blind lane predicted it, no
  search lane reached a primary source; banked.
- sql-console "engine-level read-only enforcement" as a seventh technique — converged
  but is one clause of the guard story; landed as a clause.
- browser-credential-boundary "RLS is not a complete isolation boundary against
  composed SQL" — true per engine docs, but a universal without its condition under
  the operator-grammar regime; banked.
- Ruling on the other 81 inbox rows — out of this run's scope. They are the cheapest
  work on the table and still unruled.

## Owed

- The 81 unruled inbox rows. Heaviest clusters: cinematic-language (media-generation,
  seven application-class rows from one project), client-state, repo-manifest-standard,
  visual-style-locking.
- accessibility — a reconcile wave, three declines deep.
- 84 single-stack subjects remain (was 87). One defect, one wave: `/reconcile`.
- The maturity ladder — still every status `forged`.
- `media-generation/trailer-structure` and `game-production/judgeable-spec-authoring` —
  application-less.
- `rules/` regeneration is skipped by every non-librarian landing. Either the intake
  and deepen skills name it in their gate sequence, or a pre-commit hook does.
- Contributor identity — demand remains a range; this run ranked on the floor.

## Impact (step 8, run after #7 and #8 merged)

The fleet maps were regenerated with per-subject staleness after this run's
landings reached `main`. Verdicts judged against a subject this run changed:

| Subject | Stale verdicts |
| --- | --- |
| import-normalization | personas (1) |
| browser-credential-boundary | ascent (4) |
| demo-data-plane | systedo-case (1) |
| terminal-multiplexing | personas (1) |
| guided-tours | personas (2), personas-web (1) |
| sql-console | personas (4) |
| plan-entitlements | systedo-case (1), ascent (2) |

17 verdicts across four projects; fleet-wide 216 of 287. Each project's map lists
them under `staleSubjects`; that is the project's `/conform --stale` queue. Maps
committed by pathspec on each project's active branch, never pushed. The apply
rows for this run's new technique and flipped rules are in `librarian/applied.md`
dated 2026-09-02 with run `librarian-2026-09-02-1`.

### Applied (seven workers, one per landed finding)

| Finding | Project | Mode | Verdict |
| --- | --- | --- | --- |
| multi-client-fan-out (new technique) | personas | simulation | unapplied - condition holds nowhere in the fleet; negative application written |
| mark the module, not the entry point | ascent | experiment | better - 2/118 unmarked modules reached by value that a barrel marker cannot see |
| one-interface-many-planes condition | systedo-case | experiment | better - ~42 breaches become 14 expected + 6 real mislabels |
| presence before content | personas | experiment | better - fixed seam 3/3 → 0/9; one unfixed same-shape guard found |
| missing-anchor-degradation | personas | experiment | better - ledger arm sees the degraded run the flag cannot |
| compare state, never event order | ascent | **code** | better - 4/9 → 0/9 resurrecting sequences, committed on the active branch |
| read-shaped writes in the mutation class | personas | **code** | better - 8/9 → 0/9 passed as read-only, both guard sides, committed on the active branch |

Two code landings, four experiments, one honest unapplied. Every project write sits
on the project's active branch, unpushed; `.ai/applied.jsonl` committed once per
project by the Director. Seven application documents landed or updated in the
registry, each carrying `applied:` and `ab_verdict:`. Rows in `librarian/applied.md`.

Worker facts worth the next pass: the two tour engines in the fleet run no focus
trap at all, so overlay-precedence's conditioned rule has no seam here yet; the
billing provider's order event ships a status snapshot rather than a transition
pair, which removes the technique's cheaper form; a confirmed `FOR UPDATE` in the
desktop host now trips the row-limit injector instead of the guard.
