---
run: 2026-09-05-1
kind: deepen-batch
domain: software-engineering, llm-observability
dispatched: 4
landed: 4
rejected: 0
---

# Run 2026-09-05-1 — four subjects, two foundations re-measured, one technique, four apply rows (three of them moved a rule)

A `/deepen` batch (Director-reviewed, four research workers, four apply workers)
with no `/librarian` sweep in front of it. Scan, selection, diff review and the
whole of step 4 were run by the Director.

## Instrument

- `check-currency --json`: expired 0, at-risk 0, 58 stack-drift applications
  before the run (59 after: new applications with a witness are compared to the
  fleet maximum like every other).
- `librarian-scan --top 25`: worklist head unchanged from [[2026-09-04-1]]
  (agent-memory, quality-gates, accessibility, markdown-vault - all touched within
  five days, no event). The duplicate llm-observability block still halves that
  bundle's demand.
- Spot-check: index said agent-instruction-files 14 techniques / 11 applications;
  the folder holds 14 and 11. `build-index` produced no diff on trunk: the digest was
  fresh at dispatch, for the first time in five runs.
- Pre-flight `git status`: clean. A sibling session installed a skill and touched the
  marketplace file mid-run; every commit here is pathspec'd and neither entered trunk.

## Selection

| Subject | Why | Before | After |
| --- | --- | --- | --- |
| client-state | demand #6 (7 consults, up to 10 deviations); one pass 2026-08-29 whose note predicted "yield from fleet deviations, not survey"; intake grew it since | 12t / 12a | 12t / 13a, 8 techniques corrected or amended |
| usage-limit-governance | never swept, no note, 6 real deviations (12 summed under the duplicate block) | 7t / 3a | 7t / 5a, golden path + 6 techniques corrected |
| agent-instruction-files | never deepened; forged from one tree at 5 techniques, grown to 14 by intake in ten days; 4 drift rows | 14t / 11a | 14t / 13a, golden path + 10 techniques corrected |
| stream-proxy-hop | never deepened; single stack; all 3 applications drift rows; five techniques untouched since the forge | 6t / 3a | 7t / 5a, second stack, one new technique |

Declined: diff-comparison and operator-surfaces-for-llm-spend (both 40 points,
both deepened 2026-08-30, no event since); accessibility and markdown-vault as
yesterday.

Blind lane first in every worker. Convergence: client-state 9/14, usage-limit
7/11, agent-instruction-files 7/11, stream-proxy-hop 7/8; every blind-only item
was an internal contradiction between techniques landed one at a time.

## Landed (Director read every diff)

**Two foundations fell to the same failure - a measured result stated without
its scale.**
- agent-instruction-files: "compliance falls with instruction count, roughly
  uniformly; position barely matters" was a dense-list benchmark (10-500
  instructions in one prompt) stated for repository files; the one file-scale
  factorial study returns an affirmative null for size and position and finds the
  leak in session length. Golden path and three techniques now carry the predicate.
- stream-proxy-hop: "the client does not reconnect" after a clean close is the
  hand-rolled reader's shape; the standard client reconnects on a clean end of body
  by specification and fires its error event. The node application had endorsed a
  tree comment saying the opposite.

**One new technique**: `stream-proxy-hop/lifetime-cap-rotation` - total-duration
caps a heartbeat cannot defeat; blind lane, a function platform's docs and a fleet
route's header comment converged; no prior art in the bundle's use_when or slugs.

**Absolutes and numbers that fell** (qualifier-edge lane, fourth batch running):
"typically thirty to sixty seconds" (documented defaults 10s to 125s across five
vendors); "only an inline gateway prevents" (any pre-provider seat, each
approximate, the provider's own docs admitting non-instantaneous enforcement);
"tenants never block each other" (they share the lock pool); "wall clock at ingest
satisfies monotonicity" (named exceptions); "overload control sheds
probabilistically" (much of it; the hashed-level precedent re-rolls hourly);
"narrower is nearly always the default" (only where the default is the tightest
member); "minutes, not seconds" (predicated); "no-transform stops buffering" (it
stops re-encoding); "every intermediary logs the URL" (those terminating transport
security); "the strongest inversions rewrite the most words" (on three seeds);
"the 25k-token floor" (one fleet measurement, dated); "38% ... unattended" (the
paper's axis is CLI vs IDE, the gloss is ours).

**Internal contradictions the blind lane caught**: status-fsms routing a failed
reload to `stale` with no evidence slot against its own failure law; line-earning's
"never admitted" against capability-coverage-contract; the golden path's "only
inline" against the technique's provider seat; the monotone shed set vacuous for
fresh ids; the three-way reconnect-shape disagreement between technique, golden
path and application.

**Banked leads whose return condition was met**: rust--status-fsms from a fleet
backend (2026-08-29 lead; the tree added a typed status 2026-09-04).

**Confirmed, no edit** (~35 across the batch): never-a-timestamp tokens against
clock coarsening; the standard stream client cannot set headers and stops on a
rejected handshake; sliding-log windows are exact; GCRA is a regulator not a
window; hop-by-hop headers stripped by proxies; the 38.3% / 49.5% / 32.3% figures
against their paper; the vendor's 80% prompt cut, re-dated.

## Drift re-verification

| Application | Tree | Runtime observed | Before -> after |
| --- | --- | --- | --- |
| agent-instruction-files node--enforcement-demotion | kp, ascent, pumper | node 24 (two trees), CI 20 (one) | node@22 -> node@24 |
| agent-instruction-files node--host-contract-compilation | public repo, fresh clone | bun only, no node witness | node@22 -> withdrawn |
| agent-instruction-files node--rewrite-behavior-pinning | ai-registry | CI pins 20 | kept node@20 |
| agent-instruction-files node--single-source-topology | public repo @ pinned commit | workflow node 22 | kept node@22; two false citations corrected |
| stream-proxy-hop node--idle-heartbeat-injection, node--upstream-status-normalization | the fleet's node hop | CI node 22 | kept node@22, 23/23 line facts |
| stream-proxy-hop node--credential-attachment-at-the-hop | gravity, kp | 22 and 24 | withdrawn (three trees, three pins, written per tree) |
| client-state react--status-fsms, react--singleton-lifecycle | personas | react 19 | witness added, 27/27 re-resolved |
| usage-limit-governance rust x2 | tracklight | rust-toolchain 1.96.1 | witness added; module split re-addressed |
| usage-limit-governance process | nine vendor sources | - | five facts corrected, refresh_by 2026-12-05 |

Two rust witnesses for one tree in one batch (MSRV floor vs observed toolchain);
aligned to the observed with the floor in the body.

## Cross-subject proposals (Director-adjudicated)

- **Held for the operator**: "the safe default is the failure you can see" as a
  law - six sightings in client-state alone. Hierarchical budgets (org > workspace >
  key) as a usage-limit-governance technique or a new subject.
- **Banked in the target subjects' notes**: retry-backoff (stable-connection reset,
  second sighting); realtime-events (origin-side replay window with a reset event, a
  rust origin in the fleet); error-handling/crash-capture (platform timeout kills the
  function); webhook-ingestion (HMAC over a re-serialised body); rate-limiting
  (429-vs-403 refusal split; the two-bucket counter); prompt-assembly (an
  adjacent-contradiction null, n=1); breach-alerting (burn-rate framing).
- **Not landed**: tear-safe external-store reads (web only, no fleet sighting);
  URL-addressed state as a fourth species (home-ambiguous); a per-host discovery
  table (belongs with the harness capability matrix).

## Propagation (step 4)

- Index, rules, catalog regenerated on a clean tree; nine project maps regenerated
  and committed by pathspec on each active branch; fleet map regenerated and
  committed. 215 verdicts fleet-wide stale after regeneration (247 before; the
  number moves with digests, not with conform runs).
- Impact for the touched subjects: personas carries 3 stale client-state verdicts;
  every other pair on all four subjects is `unknown` - never judged, so the queue is
  a first `/conform`, not a re-judge.
- **Fleet declaration gap**: the fleet's one real node stream hop is a checkout
  that exists on this machine and is not declared for it, so its map was not
  regenerated and its two live defects are visible to no map.

## Applied (step 4, four apply workers, four rows in `librarian/applied.md`)

| Finding | Project | Mode | Verdict |
| --- | --- | --- | --- |
| any pre-provider seat prevents; client seat honesty rules | tracklight | code | better - all three SDKs at parity under one contract fixture; the missing worst-case sentence shipped to the README; the reading refuted the morning's rust application (TTL bounds refusal, not spend) - corrected same hour |
| lifetime-cap-rotation (new) | ascent | experiment | **not-better** - excluded twice by its own rules; condition earned: bounded by construction is a sum, not a term |
| file-scale leak is session length (golden path) | kp | experiment | **not-better** - 373 transcripts replayed; single-event builder topology has no axis to leak along; the lever is a gate on the builder surface; condition in the golden path |
| reset on stable, clean close reconnectable | personas | code | better - reset lived in one arm only; four replayed close sequences; on a project branch because the test binary does not load here |

Three apply workers died on the session rate limit mid-run and were resumed from
their transcripts with the tree state as the first ask; one had a harness file in a
project tree and removed it. Two condition paragraphs arrived carrying the
experiment's numbers into upper layers and were trimmed to mechanism-plus-pointer.

## Declined

- Second node application from ascent's stream routes: origins, not hops.
- A client-side-seat technique: landed as a fourth seat in the existing technique.
- Renaming "unattended CLI" throughout: kept as our flagged inference.
- Committing anything another session owned: the marketplace file and the new
  skill folder stayed out of every commit.

## Owed

- The fleet declaration for the undeclared checkout on this machine (one line in
  `projects.json`), then a map run and `/conform` on that hop - two live defects.
- The rust witness convention (observed toolchain vs manifest floor) in the brief
  and in `check-currency`.
- The law candidate and the hierarchical-budget proposal (operator).
- Personas: fast-forward the apply branch once the project gate runs on a machine
  whose test binary loads; a task row for the relay's three standing deviations.
- Unchanged from [[2026-09-04-1]]: quality-gates split, merge-result gating, the
  bun witness, the max-major drift comparison, the stale-verdict queue nobody drains.
