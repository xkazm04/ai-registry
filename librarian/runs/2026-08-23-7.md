---
run: 2026-08-23-7
kind: technique-cycle
dispatched: 0
---

# Run 2026-08-23-7 - cycle N1-a: two measured disproofs land

The cycle queued by [[2026-08-23-6]]. Both edits are corrections of technique
text an external harness disproved, not convergence placements - the first
cycle of that kind. Director-edited, purity-grepped, voice kept.

## What changed

- **keyset-trace-pagination** (trace-rollup-and-attribution): the cursor
  column gains its missing constraint - immutable, or mutable only in the
  direction that duplicates. The prior text prescribed the latest-event-time
  column and called the consequence a benign re-appearance; the harness showed
  that under the newest-first order the same text prescribes, that column
  silently SKIPS rows. The technique now derives the asymmetry (mutation
  direction vs traversal direction), names start-time as the pageable column,
  states the newest-started-is-not-newest-active trade, and sends true
  activity ordering to restart-from-head. The measurement stays in the
  application, per the standard.
- **stable-hash-sampling** (production-trace-scoring): the mechanism is now a
  threshold on a hash mapped to [0,1); "hash mod N" is demoted to a variant
  acceptable only for a rate that never moves, with the reason stated -
  thresholds nest as the rate rises, residue classes do not, so mod-N re-buys
  a sample's worth of verdicts on every rate change. The "N <= 1" decision
  rule reworded to "a rate of 1" for consistency.

## Declined

- Every single-sighted proposal from the wave stays banked: salting,
  join-preservation, the settle-window second shape, the NaN-degenerate rule
  (checked against the rust application - no second sighting there), the
  vocabulary-closure law question, the lossy-rollup justification. The
  convergence rule applies to all of them.

## Deviation, recorded

Committed locally on `main` by pathspec; sibling mid-flight (software-
engineering grew 143 -> 146 subjects under it during this sitting). Pull
request owed at push time.
