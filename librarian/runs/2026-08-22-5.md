---
run: 2026-08-22-5
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-22-5 - the third wave

Four workers, four trees. Thirteen single-stack debts now cleared across the
lane's four runs; 52 remain.

## What landed

| subject | tree | application | second stack |
| --- | --- | --- | --- |
| scheduling | temporalio/temporal @ 6805cae (1.32.0) | go--next-run-computation | yes |
| job-coordination | riverqueue/river @ f748a5c (v0.44.1) | go--step-position-and-resumability | yes |
| credential-vault | hashicorp/vault @ c3d7264 (2.2.0-beta1) | go--token-refresh-lifecycle | yes |
| mcp-tools | modelcontextprotocol/typescript-sdk @ 3924de9 (2.0.0-alpha) | node--transport-selection | yes |

## Evidence-first, third confirmation

One worker's three hints were ALL refuted or outranked (a first-class
resumable-step feature nobody hinted at won on evidence); another refuted its
second hint structurally (OSS rotation is fourteen lines of constants plus
stubs — Enterprise-only); a third confirmed its hint and *strengthened* it (a
dual-era tree that shows the technique's claims as a literal diff). The hints
exist to start the read, never to end it, and three waves have now proven the
workers treat them that way.

Re-check catches: 6, 3, 11, 5 — including a second fabricated-range incident
(a compression pass inventing a contiguous span over unrelated code) and one
worker catching itself about to repeat a code comment's false claim about its
own default. The Vault worker's 1-per-4.5 rate came from drafting via
sed-window arithmetic — a method note: draft from open files, not from window
offsets.

## The wave's sharpest findings

A schedule engine that DROPS the spring-forward occurrence and FIRES TWICE on
fall-back, against a technique that says once-at-first-valid-instant and
first-copy-only; a code comment claiming a compute bound is "disabled by
default" while the shipped config enables it; a secrets server that retires a
failed token acquisition but orphans a live upstream credential on the secret
path of the same file; a job queue whose rescuer STRUCTURALLY cannot recover a
resumable frontier (the rescue parameter struct has no metadata field); and a
protocol SDK whose characteristic security check is opt-in-off on every
transport while every lesser gate is mandatory.

## Convergence status - two families ready for the next cycle

- **Fail closed when the gate itself breaks** (double-sighted in wave 2,
  unchanged).
- **The verdict vocabulary must survive the boundary** — now THREE sightings:
  wave 1's refusal enum erased into a generic error at the retry boundary;
  wave 2's exemplary reason-labelled rejection counter as the positive form;
  this wave's server-side failure taxonomy collapsing to a client-side
  substring check. The family spans subjects, so the cycle must decide the
  home: a technique edit per sighting home, or a candidate law. That decision
  belongs to the cycle, not this close-out.

## Incidents

**The scratchpad collision, upgraded from hazard to incident.** Two workers
used the same generic scratch filename; one executed the other's script,
applying that worker's edits to the Vault application mid-flight. The outcome
was benign only because the script was idempotent-shaped and the owning worker
re-verified its final content. Lane rule from here: workers namespace every
scratch file under their own worker directory, and never run a script from the
shared scratchpad root.

**Length creep.** 173/146/136/130 lines against a 60-130 guide, drifting up
wave over wave. Density still earns it, but the next wave's prompts carry a
firmer ceiling and the instruction to move overflow into the subject note's
leads rather than the application.

## Declined

- Editing any technique in this close-out — two convergence families are
  explicitly queued for the deliberate cycle that follows.
- A rotation-and-remediation application from the Vault tree — the honest
  finding is that the OSS tree cannot support one.

## The scan half

mcp typescript-sdk L4/76 (highest adoption yet, 78) · temporal L4/73 ·
vault L3/58 (rigor 79 / adoption 18 — the smithy pattern) · river L3/48.
Twelve repos now scanned across L2-L5.
