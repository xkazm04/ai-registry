---
run: 2026-09-04-1
kind: deepen-batch
domain: software-engineering
dispatched: 3
landed: 3
rejected: 0
---

# Run 2026-09-04-1 — three demand-ranked subjects, every absolute attacked, seven applications re-witnessed

A `/deepen` batch (Director-reviewed, three workers) with no `/librarian` sweep in
front of it. Scan, selection and propagation were run by the Director; the
`/deepen` skill's step 4 ran once over the whole batch.

## Instrument

- `librarian-scan --top 25`: 413 subjects, `use_when` 2816/2816, 2 reporting
  installations, demand a range (370 certain, up to 550). Worklist head: agent-memory
  56, quality-gates 52, accessibility 44, markdown-vault 44.
- `check-currency --json`: expired 0, at-risk 0, **58 stack-drift applications**
  (verified against an older runtime major than the fleet runs); by subject,
  quality-gates 5, agent-instruction-files 4, agent-runtime-assembly 4. That table,
  not the worklist, is what put agent-runtime-assembly on the shortlist: zero
  attention points (no installation reports on it) and the highest drift count of
  any never-deepened subject.
- Spot-check: index said agent-memory 18 techniques / 17 applications; the folder
  holds 18 and 17.
- **Trunk was stale on the per-subject digest before this run touched anything**:
  the 603b9547 landing changed agent-memory without regenerating `index.json`. Fourth
  trunk-stale generated artifact in a week, first time it is the digest rather than
  `rules/`. Regenerated and committed alone before dispatch.
- **A live sibling session shared this checkout** the whole run (three subjects
  half-landed, a new skill installed, generated files drifting). Every commit here
  is pathspec'd, and the generated artifacts were built in a detached worktree of
  HEAD at a short path so the sibling's uncommitted subjects did not enter trunk.

## Selection

Demand first, staleness second, both as computed inputs:

| Subject | Why | Before | After |
| --- | --- | --- | --- |
| quality-gates | demand #2 (13 deviations, one installation); 5 drift applications; one `/deepen` pass ever (2026-08-28, 11 techniques then) and 14 techniques arrived since by single-source intake | 25t / 24a | 25t / 24a, 4 techniques + golden path corrected, 5 applications re-witnessed |
| agent-memory | demand #1 (14 deviations on each installation); 2 drift applications; four claims the 2026-08-30 pass recorded as not attacked; three intake landings today | 18t / 17a | 18t / 18a, 7 techniques corrected or amended, one new application |
| agent-runtime-assembly | never deepened; forged 2026-09-02 from one tree and grown 6 -> 14 techniques by seven intake runs in 48 hours; 4 drift applications; demand unknown | 14t / 15a | 14t / 15a, 7 techniques + golden path corrected |

Declined: accessibility (#3) - fourth pass on the same reason, it is a reconcile job
and the 2026-09-01 reconcile closed its single-stack debt; markdown-vault (#4) -
deepened 2026-08-30, one drift application, no event.

Blind lane first in every worker (a written prediction file before any search); the
web lane then ran unconstrained. Convergence held: quality-gates 2 of 2 blind
techniques reached by the web lane (both declined as files - prior art and a
home-ambiguous proposal); agent-runtime-assembly 4 of 5 blind items; agent-memory
2 of 5, the rest were internal-consistency findings the web could not have reached.

## Landed (Director read every diff)

**No new technique in the batch** - three subjects at 25, 18 and 14 techniques, and
every candidate either had prior art in a sibling subject or was home-ambiguous.
The yield was corrections, which is the point of the counter-evidence lane:

- **Absolutes and numbers that fell** (the qualifier-edge lane, third batch running):
  "the editor catches most defects" (no measurement supports "most"; the one
  industrial study supports earlier-is-acted-on-more); "a simulated counter is a
  function of the tree alone" (the reference counter's own manual: highly, not
  perfectly, reproducible - ASLR and library sizes move it); "most routers resolve
  by first match in mount order" (specificity routers exist and there mount order
  protects nothing); "storage grows with the square of the turn count" (in
  references; in bytes only when content is copied); "an unparseable instant is
  treated as new" (age zero is maximum freshness - a writer emitting bad timestamps
  ranks first); "the lowest-importance members demote" (a column sort disagrees with
  the store's own value model 10x in one real case).
- **Internal contradictions found by the blind lane**: operator-tier-code-loading's
  optional-by-default load failure contradicted its own run-time fail-closed rule for
  intercepting hooks; additive-input's per-item budget reset let an automated
  producer buy unlimited calls; durable-store-failure-posture (landed today by intake)
  did not know recall-injection already owned failed recall.
- **Banked leads whose return condition was met**: deliberate forget vs expiry
  (2026-08-26) - both stores in one tree now carry a scoped key tombstone lifted only
  by an explicit write; the head-to-head memory harness (2026-08-26) - structurally
  met by the memory-year harness, measurement pending its year run; "this registry is
  itself an instance" (2026-08-22) - answered with `claude-code--memory-governance`.
- **Fleet trees correcting the corpus**: personas' hook dispatcher makes the wrapped
  result obtainable only from the continuation (stronger than the technique's
  prose); personas' active cap ranks by column and is the case the one-value-model
  rule exists to prevent (the tree is wrong, the technique gained the mechanism).
- **Confirmed, no edit** (first-class, ~15 across the batch): hooks observe and never
  mutate; one hook system per repository; the false-positive survival band; driver
  specifications carry a first-class indeterminate status; cache-status headers carry
  the three attribution channels; one value model for retrieval and forgetting;
  human-gated identity as a posture (a 2026 mutability paper prescribes governance
  depth, n=30/cell).

## Drift re-verification (the staleness clock, paid)

| Application | Tree | Runtime observed | Before -> after |
| --- | --- | --- | --- |
| quality-gates next--prose-rule-drift | pof | next 16.3 | next@15 -> next@16, 5/5 |
| quality-gates node--chokepoint-tag-registry | systedo-case | node 24 (.nvmrc + CI) | node@22 -> node@24, 9/9, registry grew 970 -> 1,265 lines / 24 goldens |
| quality-gates node--prose-rule-drift | ascent + registry | node 24 | node@22 -> node@24; both rules re-measured (0 of 588 over cap; unbacked checker now 50 violations across six repos, was 27 across four) |
| quality-gates node--renameable-detector-keys, node--blocking-by-input-determinism | ai-registry (+ personas, pumper) | CI pins node 20 | kept node@20 - the drift is the fleet's local runtime, not the document's |
| agent-memory node--lane-reconciliation, node--probe-without-write-back | goat (main) - a tree the fleet map names and the dispatch did not | node 24.14 | node@22 -> node@24, 21/21, one path corrected |
| agent-memory rust--decay-and-forgetting | personas | rustc 1.97 | kept, `verified_on` moved |
| agent-runtime-assembly next--additive-input | pof | next 16.3 | next@15 -> next@16, 3/3 |
| agent-runtime-assembly node--guard-input-custody | ai-registry | CI pins node 20, 7 jobs (was 4) | kept node@20 |
| agent-runtime-assembly node--indeterminate-closure | fresh clone | node 22.15 (mise) | unchanged, 3/3 |
| agent-runtime-assembly node--operator-tier-code-loading | fresh clone | **bun 1.3, no node witness** | `verified_against` withdrawn - the stack vocabulary has no `bun` |

Two facts from the table for the currency instrument: (1) `check-currency` compares
against the fleet's *maximum* reported major (24), while four of these applications
were read from trees whose own CI pins 20 - "drift" there is the developer box
outrunning the tree's pin, and the honest witness stays at the pin; (2) a `node@22`
witness on a bun-pinned tree had no witness at all - the first `verified_against`
found to be a guess rather than a read.

## Cross-subject proposals (Director-adjudicated)

- **Landed as one sentence each**: prompt-safety -> memory-governance (the write-door
  counterpart of the attacker-controlled-span rule); suppression-hygiene ->
  false-positive-economics (reverse link).
- **Banked with return conditions** in the target subjects' vault notes: mcp-tools
  (task extension wire facts), session-continuation (fail direction by handler
  family), subprocess-lifecycle (grace-deadline cancellation, home contested),
  eval-harness (judge-stability sighting, +37 points on re-judge).
- **Split proposal, not acted on**: quality-gates at 25 techniques holds two subjects.
  Seam: predicate gates (a verdict is a property of the tree) vs **metric gates** (a
  number compared to a recorded number; the design problems are baseline provenance
  and instrument noise). Would move ratchet-design, counted-set-snapshot,
  deterministic-proxy-gate, operation-assertion-gates and the founding-baseline half
  of excess-indicts-the-instrument; hinge blocking-by-input-determinism. Worker's
  evidence: those five cite each other far more than the other twenty. Operator
  decision; recorded here and in the subject note.
- **Coverage hole with zero prior art**: gate the *merge result*, not the branch head
  (two individually green changes composing red; merge queues re-verify on a temporary
  branch and need their own trigger). Home ambiguous between quality-gates and a
  delivery/branching subject. Second sighting wanted before a file.
- **Fleet fact for whoever owns CI**: Node 20 reached end-of-life 2026-04-30 and the
  hosted runner removes it this autumn; ai-registry (every job), pumper and one personas
  job still pin 20. Not this run's scope; the deployment-contract lane's.

## Declined

- Two new-technique candidates in quality-gates: assertion-form suppressions (prior
  art: dead-code/suppression-hygiene, with a rust application measuring 63 of 71
  permit-form suppressions inert) and the merge-result gap (no convergence with a
  second sighting).
- Hook re-entrancy as a full technique in agent-runtime-assembly: landed as a section;
  a file waits for a second runtime documenting inner-call hook semantics.
- The memory-poisoning lane as a file: home split with prompt-safety, so a section in
  memory-governance and a sentence in prompt-safety.
- Regenerating `librarian/fleet-map.*`: the working-tree diff there predates this run
  and belongs to whoever ran `build-fleet-map`; left uncommitted, not mine.

## Owed

- The quality-gates split decision (operator).
- A `bun` entry in the stack vocabulary, or a rule that a bun-pinned tree is witnessed
  as `node@<none>`; until then one application carries no runtime witness.
- `check-currency`'s drift comparison should read the tree's own pin where the
  application records one; four of this run's ten drift rows were false drift.
- The memory-year harness year run (agent-memory's next productive pass, and
  eval-harness's judge-stability re-judge).
- The 2026-08-30 impact plan's unanswered conform queue: fleet-wide 247 of 287
  recorded verdicts were stale before this run and are stale after it. Nobody has
  run `/conform --stale` since the per-subject digest landed.

## Applied (step 4, two apply workers, eight rows in `librarian/applied.md`)

| Flipped rule | Project | Mode | Verdict |
| --- | --- | --- | --- |
| memory-value-model: unparseable instant is unknown, counted | personas | experiment | unmeasurable - 807 of 807 timestamps parse; the counter arm B adds is the instrument |
| decay-and-forgetting: cap ranks by the value model | personas | experiment | better - 1/10, 2/20, 3/30 rows archived that recall would serve first; production cap has never fired |
| consolidation: compare instants for a state-valued claim | personas | simulation | **not-better** - the weigher never inverted a state (0/128); condition earned: a code-owned canonical key first (3.6 keys per state minted) |
| operator-tier-code-loading: load default by declared kind | personas | simulation | better - one load policy for every kind; a first interceptor would inherit log-and-continue (A 1/3, B 0/3) |
| host-routes-win: conditional on matching discipline | - | - | unapplied - no fleet host mounts a route it did not author; the one router is the specificity family |
| enforcement-binding: skip semantics are a platform fact | ai-registry fleet audit | experiment | **not-better** - A and B find the same 6 (all pumper, all behind one schedule condition); condition earned: single-platform fleet, constant read. By-product: kp binds 3 of 11 declared contexts live, systedo-case 2 of 6 |
| deterministic-proxy-gate: tree + toolchain, measure the spread | personas | simulation | better - a bundle-size baseline held across a bundler major unmeasured; cross-machine residue 0.1 KB/chunk; condition earned: toolchain pin is the whole pin for artifact-size counters |
| gate-laddering: editor rung "most defects" | - | - | unapplied - instrument named: a per-rung fix-rate log |

Three `not-better`/condition rows, each written back into its technique the same
hour. Ledger lines committed by pathspec on personas (master, +7) and pumper (master,
+1); no project code changed. Three application documents created, three updated.
