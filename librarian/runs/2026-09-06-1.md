---
run: 2026-09-06-1
kind: subject-split
domain: software-engineering
dispatched: 0
landed: 1
rejected: 0
---

# Run 2026-09-06-1 - quality-gates split: metric-gates

Operator-directed execution of the split proposed in [[2026-09-04-1]]. No workers; the
Director did the move, the two golden paths and the link repair by hand, gate-clean.

## What moved

| From quality-gates | To metric-gates |
| --- | --- |
| `ratchet-design`, `counted-set-snapshot`, `operation-assertion-gates`, `deterministic-proxy-gate` | same slugs, whole files, `subject:` rewritten |
| `node--ratchet-design`, `node--deterministic-proxy-gate`, `node--operation-assertion-gates` | same, with their `applied:` / `ab_verdict:` intact |
| golden-path sections "When the input is fine and the instrument is not" and "Ratchets" | migrated verbatim, plus two new sections (founding measurement; a baseline records where it was taken) |

quality-gates: 25 -> 21 techniques, 26 -> 23 applications, title "Quality gates"
(was "& ratchets"), a boundary paragraph naming the neighbour and the hinge, two pointer
sections where the migrated ones stood. metric-gates: 4 techniques, 3 applications,
registered in `taxonomy.json` under `engineering-process/standards-and-gates`.

## Two departures from the proposal, both recorded in the subject notes

- `excess-indicts-the-instrument` stays whole in quality-gates. The proposal moved its
  "founding-baseline half"; a technique split in two is two weak techniques, and the
  file's subject is the instrument's scope declaration, which is predicate-side.
  metric-gates cites it for the founding reading.
- The founding-baseline concern that the half would have carried is a golden-path
  section in metric-gates instead, alongside a second new section raised from the
  2026-09-04 apply row (a bundle-size baseline held across a bundler major because
  nothing in the file said what toolchain it was measured under).

## Links

Six cross-subject links rewritten from `./` to `../../<subject>/techniques/`, in both
directions; two inbound links from other subjects (i18n/completeness-gates,
supply-chain/secret-scanning-architecture) repointed; one prose self-reference in
ratchet-design ("the rest of this subject") re-addressed to quality-gates.
`check-bundles` green; the first run failed on exactly the links a hand move breaks,
plus the missing taxonomy entry - the gate did its job.

## Ledger continuity

`librarian/applied.md` rows for the moved techniques (2026-08-31 operation-assertion-
gates, 2026-09-04 deterministic-proxy-gate) keep `quality-gates` as their subject: they
record what was true when the test ran. Same for the project ledgers. The fleet maps
re-match these four techniques' `use_when` under the new slug on regeneration; pairs the
maps held for quality-gates lose those triggers and metric-gates gains them.

## Owed

- metric-gates demand is unknown (new slug, no installation reports). The first
  `/conform` pass on a project with a ratchet or a size gate is where it becomes real.
- Four techniques is thin; two candidates named in the subject note (baseline
  derivation stamps; two-consumer ratchet), each waiting on a second sighting.

## Impact (step 4)

Registry maps regenerated across the fleet: 3,617 pairs, 287 judged, fleet-wide stale
verdicts unchanged at 247 (this split moves no verdict; quality-gates' two stale pairs
remain gravity (1), personas (1)). metric-gates matched two contexts, both in
systedo-case, all `unknown`. Maps committed by pathspec on nine projects. **kp and
ascent were mid-merge in another session**: `git commit` refused a partial commit, the
staged file was restored to unstaged, and the regenerated map sits in their working
trees for that session or the next run to commit. Owed.
