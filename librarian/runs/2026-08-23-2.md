---
run: 2026-08-23-2
kind: external-reconcile-wave
dispatched: 4
---

# Run 2026-08-23-2 - the seventh wave: executed evidence becomes the norm

Four workers, four trees; twenty-nine single-stack debts cleared across eight
runs. This wave is the first where every worker used executed evidence
somewhere: a capturing logger proving silent compiles, a stripped-types
harness proving a reset ceiling and a double-apply, a measured double-submit
race, and grep-scoped negative claims throughout.

## What landed

| subject | tree | application | hint fate |
| --- | --- | --- | --- |
| modal-stack | floating-ui @ 0eb8c98 (1.8.0) | node--anchored-positioning | confirmed, two sub-claims sharpened |
| form | TanStack/form @ 57a855b (1.33.5) | node--submit-lifecycle | confirmed |
| data-viz | vega/vega-lite @ 4c03edb (6.4.3) | node--scale-and-axis-design | confirmed; sibling hint PASSED OVER |
| cost-metering | openmeterio/openmeter @ 7e57a39 | go--spend-attribution | confirmed; price-tables proven out-of-scope |

A third hint fate entered the vocabulary beside confirmed and refuted:
**passed over** - the tree owns a fifth of the technique, and binding would
have documented a fraction. The honest fraction is a lead, not a document.

## The wave's sharpest findings

A MEASURED double-submit bug in a current release: TanStack Form's in-flight
guard is nested under attempts<=1 (a hatch added to clear stale errors on
retry), so two calls 10ms apart invoke onSubmit twice - the strongest
upstream-reportable finding of the campaign, beside the group API's missing
gate entirely and docs prescribing the exact pattern their own prose warns
against. Also: Vega-Lite shipping truncated bar charts with zero warnings and
silently manufacturing dual axes; Floating UI's silent 50-reset ceiling
returning a wrong position rather than an error; OpenMeter's dead uniqueness
check (seen reallocated inside its loop).

## Convergence

A family recognized ACROSS waves rather than within one: **the lossy branch
carries a counter** - three sightings (a webhook gateway absorbing duplicates
uncounted, wave 2; an agent engine silently truncating forwarded output, wave
6; a metering pipeline whose dedupe package declares no metrics at all, this
wave), with the API server's reason-labelled rejection counter as the
positive form (wave 2). Placement-ready for cycle 6.

Law-confirming sightings, no action needed: absent-guard-is-loud (dedupe off
by default), unknown-is-not-a-value (missing group-by flattened to "" - the
write model refusing the nullable slot the read model has).

One technique-amendment candidate of unusual weight, single-sighted: spend
attribution axes CAN resolve at query time over stored raw payloads, applying
new axes retroactively - the technique's write-time rule is a sufficient
condition stated as a necessary one. Banked awaiting a second sighting.

## The scan half

openmeter L4/71 adoption 66 - the commercial-infra-velocity pattern ·
tanstack/form L3/60 · vega-lite L3/57 (rigor 72, adoption 28 -
excellence-without-AI) · floating-ui L3/47. Twenty-six repos scanned.

## Declined

- Cycle 6 in this close-out - the librarian sweep is next by the operator's
  own sequencing, and the new family holds until after it.
- An encoding-vocabulary application from a compiler that owns a fifth of it.
